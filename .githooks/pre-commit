#!/bin/sh
set -eu

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks is required before committing this repository." >&2
  exit 1
fi

exec gitleaks git --staged --no-banner --no-color --redact=100 .
