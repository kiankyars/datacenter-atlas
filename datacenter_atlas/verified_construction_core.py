"""Build and validate the public Verified Construction Core preview.

The preview is intentionally separate from the historical publication-carrier
chain.  It selects only manually reviewed project/site pairs from open-seed
v97 and refuses to describe the small cohort as the final 100-site product.
"""

from __future__ import annotations

import base64
import binascii
import csv
import hashlib
import io
import json
import math
import os
import re
import shutil
import tempfile
import unicodedata
import xml.etree.ElementTree as ET
import zlib
from collections import Counter, defaultdict
from collections.abc import Callable
from dataclasses import dataclass
from datetime import date
from pathlib import Path
from typing import Any, Mapping, Sequence
from uuid import UUID

from .repository import stable_id as atlas_stable_id


ROOT = Path(__file__).resolve().parents[1]
SOURCE_RELEASE_ID = "2026-07-22-open-seed-v97"
SOURCE_RELEASE = ROOT / "releases" / SOURCE_RELEASE_ID
GEOMETRY_RELEASES = {
    "global-open-v3": ROOT / "releases" / "2026-07-18-global-open-v3",
}
LEGACY_PREVIEW_V01_DIR = (
    ROOT / "verified_construction_core" / "2026-08-19-preview-v0.1"
)
LEGACY_PREVIEW_V01_MANIFEST_SHA256 = (
    "50c37999d6e14156910cdd0c66fb973c554414dcc29917226746afc15dc673b7"
)
LEGACY_PREVIEW_V02_DIR = (
    ROOT / "verified_construction_core" / "2026-08-20-preview-v0.2"
)
LEGACY_PREVIEW_V02_MANIFEST_SHA256 = (
    "e69b6d0570710dd3ef4802601652852d24047fb19f1e9bf6ebd5dad9ac29ae8d"
)
LEGACY_PREVIEW_V03_DIR = (
    ROOT / "verified_construction_core" / "2026-08-20-preview-v0.3"
)
LEGACY_PREVIEW_V03_MANIFEST_SHA256 = (
    "8a17e9e4d126bc210a34c2b9a0bd2fd1737402ca9d1069dd5411067831e6d1ac"
)
LEGACY_PREVIEW_V03_COMMIT = "f2964d751c25f28cea04c56d8feefb1a029fd371"
LEGACY_PREVIEW_V04_DIR = (
    ROOT / "verified_construction_core" / "2026-08-20-preview-v0.4"
)
LEGACY_PREVIEW_V04_MANIFEST_SHA256 = (
    "26bdf0930f21acd8db8b52d6f58803206d3ef8f0cde83d5d2ae80abc113895a7"
)
LEGACY_PREVIEW_V05_DIR = (
    ROOT / "verified_construction_core" / "2026-08-20-preview-v0.5"
)
LEGACY_PREVIEW_V05_MANIFEST_SHA256 = (
    "be699da97cc8386c30ff2414fcaa8264870e663197a039a1371a3bcc20fbc23c"
)
LEGACY_PREVIEW_V05_COMMIT = "30d4259bca2557da81fb85b805eff0ddd35868a4"
LEGACY_PREVIEW_V06_DIR = (
    ROOT / "verified_construction_core" / "2026-08-20-preview-v0.6"
)
LEGACY_PREVIEW_V06_MANIFEST_SHA256 = (
    "05070fab668b1ddd575745cefe3dc36600cd27b48f90702939229c1372674bd5"
)
LEGACY_PREVIEW_V06_COMMIT = "ec9cfe665e79cf76ea6b209a5a6e33c8fd1553c6"
CURRENT_V07_PREVIEW_ID = "2026-08-20-preview-v0.7"
CURRENT_V07_PREVIEW_DIR = ROOT / "verified_construction_core" / CURRENT_V07_PREVIEW_ID
CURRENT_V07_REVIEW_DATE = date(2026, 8, 20)
CURRENT_V07_DEFINITION_PATHS = (
    (
        "review_definition_sha256",
        ROOT / "definitions" / "verified-construction-core-v0.7-reviewed-sites.json",
    ),
    (
        "imagery_review_definition_sha256",
        ROOT / "definitions" / "verified-construction-core-v0.7-imagery-reviews.json",
    ),
    (
        "provenance_definition_sha256",
        ROOT / "definitions" / "verified-construction-core-v0.7-provenance.json",
    ),
    (
        "overlay_definition_sha256",
        ROOT / "definitions" / "verified-construction-core-reviewed-overlays-v5.json",
    ),
)
V07_REVIEW_DEFINITION_SHA256 = (
    "e6fd80ddb57b49f40cc939a56bc4764e265b2580dc95cba22a4a07cfbd7cb096"
)
V07_IMAGERY_REVIEW_DEFINITION_SHA256 = (
    "93afb53f21a7931202237d0688237a74fd8b7aa01646700785c1f368d69f4e42"
)
V07_PROVENANCE_DEFINITION_SHA256 = (
    "ef4d730fe87bcf2f2e6a99831f2a045023d4f7024d273fc0c82ba52b52648d6f"
)
V07_OVERLAY_DEFINITION_SHA256 = (
    "aa857c31d1b747749fec75b94afc16c48f8a0b28bc025d66f4a58b830cdfff3d"
)
V07_REVIEW_DEFINITION = (
    ROOT / "definitions" / "verified-construction-core-v0.7-reviewed-sites.json"
)
V07_IMAGERY_REVIEW_DEFINITION = (
    ROOT / "definitions" / "verified-construction-core-v0.7-imagery-reviews.json"
)
V07_PROVENANCE_DEFINITION = (
    ROOT / "definitions" / "verified-construction-core-v0.7-provenance.json"
)
V07_OVERLAY_DEFINITION = (
    ROOT / "definitions" / "verified-construction-core-reviewed-overlays-v5.json"
)
REVIEW_DEFINITION = (
    ROOT / "definitions" / "verified-construction-core-v0.6-reviewed-sites.json"
)
IMAGERY_REVIEW_DEFINITION = (
    ROOT / "definitions" / "verified-construction-core-v0.6-imagery-reviews.json"
)
PROVENANCE_DEFINITION = (
    ROOT / "definitions" / "verified-construction-core-v0.6-provenance.json"
)
OVERLAY_DEFINITION = (
    ROOT / "definitions" / "verified-construction-core-reviewed-overlays-v4.json"
)
V03_REVIEW_DEFINITION_SHA256 = (
    "ee56ff41dc6ea59df67de6ab462540c8a3fe3747ca3e1504cb9228481c25c06b"
)
V03_IMAGERY_REVIEW_DEFINITION_SHA256 = (
    "eec896bd0c1b26fe95110ab3023e3d713c9ff4c6965412683b4d5e945ad09d7a"
)
V03_PROVENANCE_DEFINITION_SHA256 = (
    "50c3e82d2a9ae6a759b36344e34556ed527027b5c6397153d051adf61dce6561"
)
V03_OVERLAY_DEFINITION_SHA256 = (
    "57540f73c5cac475bd8e9e64fc5d8eb2188158ef977b477a0c0e8d7158caf1b7"
)
V04_REVIEW_DEFINITION_SHA256 = (
    "cc71ed6bc107fa585caf3726f7625fd0fdd982c5699a5e19218bbeb1dec7c97c"
)
V04_IMAGERY_REVIEW_DEFINITION_SHA256 = (
    "897a9cbe1f85dea871a3c267069390d5db7ab1206691068769a14fb0b7882798"
)
V04_PROVENANCE_DEFINITION_SHA256 = (
    "9d51d2e998c6d902a6410dfb3a2ce8dde8ee4b722d483bec65de4fda1d6e636e"
)
V04_OVERLAY_DEFINITION_SHA256 = (
    "65b01f335e60c1f7a886100089bd3a4fe4da79c074233a49bb60b9b65ab8129f"
)
V05_REVIEW_DEFINITION_SHA256 = (
    "ec1c7dfc9f9516567ca2c58e29b631cf11df4bba4212df3fc875d0a4b5236775"
)
V05_IMAGERY_REVIEW_DEFINITION_SHA256 = (
    "90969eb2e15210b7a398ff2cc1a2d12ba1b2aa5213e5f4cfe31021ec71c2b083"
)
V05_PROVENANCE_DEFINITION_SHA256 = (
    "bbd0e4665605e7af6389d6730211d096c737ee038886cfdacf848f87a2ee16af"
)
V05_OVERLAY_DEFINITION_SHA256 = (
    "d71ce264327ccf523cb7f000d559c0a4953524d0224dbdcc7e91e36d8e4b98c5"
)
V06_REVIEW_DEFINITION_SHA256 = (
    "494b7d92638678aa991e15c81e52f9f5e5955ffab0e84e20f1df83dd82ecc06c"
)
V06_IMAGERY_REVIEW_DEFINITION_SHA256 = (
    "0fe6726cbca33ec95e90bc669d5f25383ab14a84fb9c2d668b77ff643861cb00"
)
V06_PROVENANCE_DEFINITION_SHA256 = (
    "5f504762991ea5c20854eab17a017cf97587bd47b6b11974d7681b37cd869ee4"
)
V06_OVERLAY_DEFINITION_SHA256 = (
    "f495067d8cef674b88d82b8b2554ccd7a043b68b7df527f8fd6d8ef49beda5a8"
)
PREVIEW_ID = "2026-08-20-preview-v0.6"
PREVIEW_DIR = ROOT / "verified_construction_core" / PREVIEW_ID
REVIEW_DATE = date(2026, 8, 20)
MAX_STATUS_AGE_DAYS = 90
CURRENT_DELTA_COUNTRY_ISO_A2 = {
    "Hong Kong": "HK",
    "India": "IN",
    "Indonesia": "ID",
    "Norway": "NO",
}

PHYSICAL_STATUSES = {
    "civil_works",
    "commissioning",
    "expansion",
    "foundations",
    "mep_electrical",
    "shell",
    "site_preparation",
    "under_construction",
}
AUTHORITATIVE_STATUS_METHODS = {
    "authoritative_construction_start",
    "authoritative_physical_status_update",
    "government_record",
    "physical_observation",
}
POWER_METRICS = {
    "grid_connection_mw",
    "gross_facility_mw",
    "critical_it_mw",
    "generation_nameplate_mw",
}
ENERGY_METRICS = {"annual_energy_mwh"}
EFFICIENCY_METRICS = {"pue"}
METRIC_UNITS = {
    "grid_connection_mw": "MW",
    "gross_facility_mw": "MW",
    "critical_it_mw": "MW",
    "generation_nameplate_mw": "MW",
    "annual_energy_mwh": "MWh/year",
    "pue": "ratio",
}
ESTIMATE_METHODS = {
    "reported",
    "calculated",
    "permit_inference",
    "equipment_inference",
    "imagery_inference",
    "modeled",
    "unknown",
}
CAPACITY_STAGES = {
    "requested",
    "contracted",
    "design",
    "planned",
    "installed",
    "energized",
    "operational",
    "measured",
    "forecast",
    "unknown",
}
FINAL_REQUIREMENTS = {
    "site_count": 100,
    "country_count": 40,
    "non_us_site_count": 50,
    "maximum_single_country_share": 0.40,
    "blind_review_sample_size": 20,
    "blind_review_minimum_agreements": 19,
}
PREVIEW_SOURCE_PIPELINE_ROW_COUNT = 531
PREVIEW_SELECTION_FIRST_FAILURE_COUNTS = {
    "entity_kind_not_project": 49,
    "not_in_reviewed_site_geometry_allowlist": 114,
    "selected": 29,
    "status_not_physical": 12,
    "status_outside_90_day_window": 327,
}
CLEAN_CLONE_REBUILD_REASON = (
    "v97 source payloads remain local-only; the preview is clean-clone validatable "
    "but not clean-clone rebuildable"
)
SEMANTIC_GUARDRAILS = {
    "legacy_construction_verified_changed": False,
    "current_status_inferred": False,
    "imagery_creates_lifecycle_claim": False,
    "campus_geometry_misrepresented_as_project_footprint": False,
    "locality_centroids_accepted": False,
    "model_only_status_accepted": False,
    "unadjudicated_imagery_conflict_resolved": False,
}

PROJECT_FIELDS = (
    "project_id",
    "project_stable_key",
    "site_id",
    "physical_site_stable_key",
    "name",
    "country",
    "country_iso_a2",
    "latitude",
    "longitude",
    "geometry_json",
    "geometry_type",
    "geometry_source_entity_kind",
    "geometry_derivation",
    "geometry_method",
    "geometry_scope_class",
    "geometry_authority_class",
    "geometry_use_scope",
    "geometry_precision_scope",
    "horizontal_uncertainty_metres",
    "horizontal_uncertainty_unknown_reason",
    "geometry_evidence_id",
    "last_observed_physical_status",
    "status_as_of",
    "status_age_days_at_review",
    "status_method",
    "status_evidence_id",
    "verification_posture",
    "independent_imagery_verification",
    "imagery_review_outcome",
    "development_type",
    "development_type_unknown_reason",
    "operating_model",
    "operating_model_unknown_reason",
    "operating_model_evidence_id",
    "workloads_json",
    "workload_unknown_reason",
    "role_claims_json",
    "power_observations_json",
    "power_unknown_reason",
    "annual_energy_observations_json",
    "annual_energy_unknown_reason",
    "efficiency_observations_json",
    "efficiency_unknown_reason",
    "owner",
    "operator",
    "users",
    "tenants",
    "customers",
    "status_source_url",
    "geometry_source_url",
)

SITE_FIELDS = (
    "site_id",
    "physical_site_stable_key",
    "name",
    "country",
    "country_iso_a2",
    "latitude",
    "longitude",
    "geometry_json",
    "geometry_type",
    "geometry_source_entity_kinds_json",
    "geometry_derivations_json",
    "geometry_methods_json",
    "geometry_scope_classes_json",
    "geometry_authority_classes_json",
    "geometry_use_scopes_json",
    "geometry_precision_scopes_json",
    "horizontal_uncertainty_metres",
    "horizontal_uncertainty_unknown_reason",
    "geometry_evidence_ids_json",
    "project_count",
    "project_ids_json",
    "project_stable_keys_json",
    "statuses_json",
    "oldest_status_as_of",
    "newest_status_as_of",
    "verification_posture",
    "independent_imagery_verification",
    "imagery_review_outcomes_json",
)

LEGACY_PROJECT_FIELDS_V03 = tuple(
    field
    for field in PROJECT_FIELDS
    if field not in {"geometry_authority_class", "geometry_use_scope"}
)
LEGACY_SITE_FIELDS_V03 = tuple(
    field
    for field in SITE_FIELDS
    if field
    not in {"geometry_authority_classes_json", "geometry_use_scopes_json"}
)

EVIDENCE_FIELDS = (
    "evidence_id",
    "roles_json",
    "project_ids_json",
    "kind",
    "title",
    "source_url",
    "publisher",
    "source_family",
    "license",
    "attribution",
    "published_at",
    "retrieved_at",
    "content_hash",
)

GLOBAL_GEOMETRY_ENTITY_FIELDS = (
    "entity_id",
    "entity_kind",
    "stable_key",
    "name",
    "latitude",
    "longitude",
    "country",
    "country_iso_a2",
    "country_iso_a3",
    "country_assignment_status",
    "country_assignment_method",
    "country_assignment_confidence",
    "country_assignment_evidence_id",
    "country_boundary_feature_id",
    "source_country_tag",
    "source_country_method",
    "source_country_qid",
    "address",
    "owner",
    "operator",
    "users",
    "status",
    "status_as_of",
    "status_confidence",
    "status_method",
    "status_evidence_id",
    "operating_model",
    "operating_model_confidence",
    "operating_model_evidence_id",
    "workloads_json",
    "capacity_estimates_json",
    "geometry_json",
    "tags_json",
    "snapshot_as_of",
    "snapshot_confidence",
    "snapshot_evidence_id",
    "source_url",
    "source_publisher",
    "source_license",
    "source_retrieved_at",
)
GLOBAL_GEOMETRY_EVIDENCE_FIELDS = (
    "evidence_id",
    "kind",
    "title",
    "source_url",
    "publisher",
    "source_family",
    "license",
    "attribution",
    "published_at",
    "retrieved_at",
    "content_hash",
)
SOURCE_ENTITY_FIELDS = (
    "entity_id",
    "entity_kind",
    "stable_key",
    "name",
    "latitude",
    "longitude",
    "country",
    "country_iso_a2",
    "country_iso_a3",
    "country_assignment_status",
    "country_assignment_method",
    "country_assignment_confidence",
    "country_assignment_evidence_id",
    "country_boundary_feature_id",
    "source_country_tag",
    "source_country_method",
    "source_country_qid",
    "address",
    "owner",
    "operator",
    "users",
    "tenants",
    "customers",
    "status",
    "status_as_of",
    "status_confidence",
    "status_method",
    "status_evidence_id",
    "operating_model",
    "operating_model_confidence",
    "operating_model_evidence_id",
    "workloads_json",
    "capacity_estimates_json",
    "geometry_json",
    "tags_json",
    "snapshot_as_of",
    "snapshot_confidence",
    "snapshot_evidence_id",
    "source_url",
    "source_publisher",
    "source_license",
    "source_retrieved_at",
)
EXACT_COMPONENT_FIELDS = (
    "component_id",
    "occurrence_id",
    "release_id",
    "entity_id",
    "entity_kind",
    "stable_key",
    "source_family",
    "source_root",
    "snapshot_evidence_id",
    "component_member_count",
    "identity_proof_parent_occurrence_id",
    "identity_proof_token",
    "typed_identity_tokens_json",
    "ambiguous_identity_tokens_json",
)
EXACT_RELATIONSHIP_FIELDS = (
    "relationship_id",
    "relationship_type",
    "subject_component_id",
    "subject_kind",
    "object_component_id",
    "object_kind",
    "decision_basis",
    "typed_identity_tokens_json",
    "source_release_ids_json",
    "raw_relationship_count",
)
QSCALE_IDENTITY_EVIDENCE = {
    "key": "qscale-contact-q01-campus-address-captured-2026-08-20",
    "kind": "company_disclosure",
    "title": "QScale contact page: Headquarters - Q01 Campus",
    "source_url": "https://www.qscale.com/company/contact",
    "publisher": "QScale",
    "source_family": "qscale_company_contact",
    "published_at": None,
    "retrieved_at": "2026-08-20T17:23:05Z",
    "license": "all-rights-reserved",
    "attribution": "QScale",
    "excerpt": (
        "QScale labels 2280 Albert-Dion St, Levis, Quebec G7A 5M9 as "
        "Headquarters - Q01 Campus."
    ),
    "content_hash": "c68dfa97bf860f780ad80e345d346591b33bc4ecde699154059c8fb1480bb9aa",
    "content_hash_scope": (
        "SHA-256 of the exact 41398-byte content-decoded credential-free public "
        "response body captured at retrieved_at."
    ),
    "captured_bytes": 41398,
    "rights_scope": (
        "Only this compact factual extraction and the response-body hash are "
        "redistributed; the HTML and publisher media are not."
    ),
    "address_extraction": {
        "site_label": "Headquarters - Q01 Campus",
        "house_number": "2280",
        "street": "Albert-Dion St",
        "locality": "Levis",
        "region": "Quebec",
        "postcode": "G7A 5M9",
    },
}
GREEN_IDENTITY_EVIDENCE = {
    "key": "green-contact-enterprise-campus-zrh1-address-captured-2026-08-20",
    "kind": "company_disclosure",
    "title": "Green enterprise contact page: Campus ZRH1",
    "source_url": "https://www.green.ch/en/contact-enterprise",
    "publisher": "Green",
    "source_family": "green_company_contact",
    "published_at": None,
    "retrieved_at": "2026-08-20T18:40:35Z",
    "license": "all-rights-reserved",
    "attribution": "Green",
    "excerpt": (
        "Green labels its Campus ZRH1 data-center site at Industriestrasse 31, "
        "5242 Lupfig, Switzerland."
    ),
    "content_hash": "19144da94289763ead81898082377a4c1e735155e706b70debc5e012e41ca043",
    "content_hash_scope": (
        "SHA-256 of the exact 215015-byte content-decoded credential-free public "
        "response body captured at retrieved_at."
    ),
    "captured_bytes": 215015,
    "rights_scope": (
        "Only this compact factual extraction and the response-body hash are "
        "redistributed; the HTML and publisher media are not."
    ),
    "address_extraction": {
        "site_label": "Campus ZRH1",
        "organization": "Green Datacenter AG",
        "house_number": "31",
        "street": "Industriestrasse",
        "postcode": "5242",
        "locality": "Lupfig",
        "country_as_published": "Schweiz",
    },
}
BRIDGE_REVIEW_SEMANTICS = {
    "curated:atnorth-ice02-reykjanesbaer-campus:phase-2-expansion": {
        "geometry_method": "openstreetmap_named_campus_fence_boundary",
        "geometry_scope_class": "reviewed_openstreetmap_named_campus_fence",
        "horizontal_uncertainty_unknown_reason": (
            "OpenStreetMap does not state horizontal positional accuracy; "
            "community-mapped geometry is not a cadastral or survey product."
        ),
        "identity_basis": (
            "The curated source names atNorth ICE02 in Reykjanesbær and the OSM "
            "object names ICE02 Campus with operator atNorth in Iceland. This is "
            "a manual branded-campus identity review, not an automated fuzzy merge."
        ),
        "precision_scope": (
            "The frozen global-open-v3 Polygon is a reviewed named ICE02 campus "
            "fence/site locator inherited by the phase-two project through the "
            "explicit project_targets relationship. It is not an official, "
            "cadastral, or survey boundary; a complete current campus extent; a "
            "building footprint; or a Phase 2 footprint."
        ),
    },
    "curated:qscale-q01-levis-campus:building-b": {
        "geometry_method": (
            "openstreetmap_named_addressed_facility_polygon_as_campus_locator"
        ),
        "geometry_scope_class": (
            "reviewed_openstreetmap_named_addressed_qscale_facility_building_polygon"
        ),
        "horizontal_uncertainty_unknown_reason": (
            "OpenStreetMap does not state horizontal positional accuracy; "
            "community-mapped geometry is not a cadastral or survey product."
        ),
        "identity_basis": (
            "The v97 project explicitly targets the Q01 campus; QScale's first-party "
            "contact page names the exact OSM-tagged address as Headquarters - Q01 "
            "Campus; the OSM feature is named QScale and carries the same house "
            "number, street and postal code."
        ),
        "precision_scope": (
            "Contributor-mapped polygon for one named QScale data-centre building "
            "at Q01's exact first-party address, used only to locate the Q01 "
            "campus. It is not the Building B footprint or centroid, the Q01 "
            "campus boundary, a parcel, a construction extent, or a survey-accuracy "
            "claim."
        ),
    },
    "curated:green-campus-zrh1-lupfig:data-center-4": {
        "geometry_method": (
            "openstreetmap_exactly_addressed_data_center_building_polygon_as_campus_locator"
        ),
        "geometry_scope_class": (
            "reviewed_openstreetmap_addressed_green_data_center_building_polygon"
        ),
        "horizontal_uncertainty_unknown_reason": (
            "OpenStreetMap does not state horizontal positional accuracy; "
            "community-mapped geometry is not a cadastral or survey product."
        ),
        "identity_basis": (
            "The v97 DC4 project explicitly targets Campus ZRH1; Green's first-party "
            "enterprise contact page labels Campus ZRH1 at Industriestrasse 31, 5242 "
            "Lupfig; the OSM data-center building carries the same house number, "
            "street, postcode, and locality and is described as Green Datacenter "
            "Lupfig."
        ),
        "precision_scope": (
            "Contributor-mapped polygon for one data-center building at Green's exact "
            "first-party Campus ZRH1 address, used only to locate Campus ZRH1. It is "
            "not the DC4 footprint or centroid, the Campus ZRH1 boundary, a parcel, "
            "a construction extent, or a survey-accuracy claim."
        ),
    },
    "curated:related-openai-oracle-stargate-michigan-saline:current-build": {
        "geometry_method": (
            "openstreetmap_named_website_tagged_construction_site_polygon_as_campus_locator"
        ),
        "geometry_scope_class": (
            "reviewed_openstreetmap_named_website_tagged_barn_construction_site_polygon"
        ),
        "horizontal_uncertainty_unknown_reason": (
            "OpenStreetMap does not state horizontal positional accuracy; "
            "community-mapped geometry is not a cadastral or survey product."
        ),
        "identity_basis": (
            "The v97 project explicitly targets The Barn Stargate campus; the curated "
            "Related Digital source names The Barn in Saline Township; and the OSM "
            "feature in the same locality is named The Barn Data Center and carries "
            "the Saline Barn website. This is a manual distinctive-name, locality, "
            "and website-tag identity review, not an automated fuzzy merge."
        ),
        "precision_scope": (
            "The frozen global-open-v3 Polygon is a contributor-mapped "
            "construction-site locator for the named Barn data-center campus, "
            "inherited by the current-build project through the explicit v14 "
            "project_targets relationship. It is not an official, cadastral, or "
            "survey boundary; a verified complete 250-acre campus extent; a building "
            "or project footprint; a construction extent; or a source of lifecycle, "
            "capacity, energy, operating-status, or imagery claims."
        ),
    },
    "curated:microsoft-mount-pleasant-datacenter-campus:second-facility": {
        "geometry_method": (
            "openstreetmap_named_construction_area_polygon_as_campus_locator"
        ),
        "geometry_scope_class": (
            "reviewed_openstreetmap_named_microsoft_fairwater_phase_2_construction_area_polygon"
        ),
        "horizontal_uncertainty_unknown_reason": (
            "OpenStreetMap does not state horizontal positional accuracy; "
            "community-mapped geometry is not a cadastral or survey product."
        ),
        "identity_basis": (
            "Microsoft's first-party source places the ongoing second facility "
            "immediately adjacent to its first Mount Pleasant facility. The OSM "
            "object is named Microsoft Fairwater AI Phase 2, is tagged as data-centre "
            "construction, and lies in the same Mount Pleasant Fairwater development. "
            "This supports a manual branded-campus association only; the OSM Phase 2 "
            "label is not treated as proof that the polygon is the official "
            "second-facility footprint or that the two naming systems are "
            "phase-equivalent."
        ),
        "precision_scope": (
            "Contributor-mapped polygon for a named Microsoft Fairwater Phase 2 "
            "construction area, used only to locate the Mount Pleasant datacenter "
            "campus. It is not an official campus boundary, the complete campus "
            "extent, a parcel, a building footprint, the official second-facility "
            "footprint, a construction-progress extent, or a survey-accuracy claim."
        ),
    },
    "curated:amazon-salem-township-innovation-campus:active-buildout": {
        "geometry_method": (
            "openstreetmap_named_addressed_aws_building_polygon_as_campus_locator"
        ),
        "geometry_scope_class": (
            "reviewed_openstreetmap_named_addressed_aws_data_centre_building_polygon"
        ),
        "horizontal_uncertainty_unknown_reason": (
            "OpenStreetMap does not state horizontal positional accuracy; "
            "community-mapped geometry is not a cadastral or survey product."
        ),
        "identity_basis": (
            "Amazon's first-party project page identifies an AWS Salem Township "
            "innovation campus with an active buildout and an already operating "
            "campus at the same location. The OSM feature is a named Amazon AWS "
            "data-centre building operated by Amazon Web Services at 1125 Electron "
            "Avenue, Berwick, within the official source's approximate Salem Township "
            "campus locality. This is a manual operator-and-proximity campus "
            "association only; it does not identify the mapped building as part of "
            "the active buildout."
        ),
        "precision_scope": (
            "Contributor-mapped polygon for one named and addressed Amazon AWS "
            "data-centre building, used only to locate the Salem Township innovation "
            "campus. It is not an official campus boundary, the complete campus "
            "extent, a parcel, the active-buildout footprint or centroid, a "
            "construction extent, a current-building count, or a survey-accuracy "
            "claim."
        ),
    },
}
BRIDGE_IMAGERY_OUTCOMES = {
    "curated:atnorth-ice02-reykjanesbaer-campus:phase-2-expansion": (
        "not_reviewed_for_core_preview"
    ),
    "curated:qscale-q01-levis-campus:building-b": "not_reviewed_for_core_preview",
    "curated:green-campus-zrh1-lupfig:data-center-4": (
        "not_reviewed_for_core_preview"
    ),
    "curated:related-openai-oracle-stargate-michigan-saline:current-build": (
        "tracked_identity_bound_visible_change_followup_only_no_construction_claim"
    ),
    "curated:microsoft-mount-pleasant-datacenter-campus:second-facility": (
        "tracked_identity_bound_uncertain_unsuperseded_no_construction_claim"
    ),
    "curated:amazon-salem-township-innovation-campus:active-buildout": (
        "tracked_identity_bound_uncertain_unsuperseded_no_construction_claim"
    ),
}
BRIDGE_PARENT_ROW_BINDINGS = {
    "curated:atnorth-ice02-reykjanesbaer-campus:phase-2-expansion": {
        "geometry_entity": {
            "release_id": "global-open-v3",
            "member": "entities.csv",
            "stable_key": "osm:way/1223207434",
            "line": 8529,
            "bytes": 1041,
            "sha256": "df6f4e13b51c13e5d42a01918c65893db20b6fa72086b84b72599b5b4a242694",
        },
        "geometry_evidence": {
            "release_id": "global-open-v3",
            "member": "evidence.csv",
            "evidence_id": "6aa2c3ec-e7cc-59f0-8cd1-4b6e68a51fef",
            "line": 2862,
            "bytes": 333,
            "sha256": "4ec5acd6326b3e1534f717f4a42bbe7b41fc8fd53e5c28fb0f1a4491e851da54",
        },
        "relationship": {
            "release_id": "2026-07-22-public-open-v14",
            "member": "relationships.csv",
            "relationship_id": "relationship:e13f118626f658c577834962767191a8d02916b82c2b56252aba6c6dfa69ea8d",
            "line": 2201,
            "bytes": 305,
            "sha256": "4b5dd1fcc3a031ccb2a26ffb733a9371ef52a2a015c2c5bfa584295109c08bff",
        },
        "subject_member": {
            "release_id": "2026-07-22-public-open-v14",
            "member": "component-members.csv",
            "stable_key": "curated:atnorth-ice02-reykjanesbaer-campus:phase-2-expansion",
            "line": 634,
            "bytes": 357,
            "sha256": "c5db41475a1f19ddf83aef195464aac5e2b9cc295de340ee7dabefdd351ad8b1",
        },
        "object_member": {
            "release_id": "2026-07-22-public-open-v14",
            "member": "component-members.csv",
            "stable_key": "curated:atnorth-ice02-reykjanesbaer-campus",
            "line": 72,
            "bytes": 338,
            "sha256": "31642e06408faca71e067c65f792c9abc354e440f11c431878dcbe00570ebc1d",
        },
    },
    "curated:qscale-q01-levis-campus:building-b": {
        "geometry_entity": {
            "release_id": "global-open-v3",
            "member": "entities.csv",
            "stable_key": "osm:way/1171439340",
            "line": 3493,
            "bytes": 942,
            "sha256": "899b6788c3b7aab63173ce3e33f6300b6f8464eadb855a69be697650011a2c78",
        },
        "geometry_evidence": {
            "release_id": "global-open-v3",
            "member": "evidence.csv",
            "evidence_id": "8dff3085-994d-5629-b19b-7f2f88c44967",
            "line": 3833,
            "bytes": 327,
            "sha256": "fe74862dd4748f3b7445509439962abc71f804e4dd254bd172680ea137de1536",
        },
        "relationship": {
            "release_id": "2026-07-22-public-open-v14",
            "member": "relationships.csv",
            "relationship_id": "relationship:92fd864b2df171e3ddc065c7bf679f124c1d512b77be7174f97b4045d48f6718",
            "line": 1478,
            "bytes": 305,
            "sha256": "4ff08880b94a0cc65fa96067bfab90ed43f7c335f88902e60b719a3a06782379",
        },
        "subject_member": {
            "release_id": "2026-07-22-public-open-v14",
            "member": "component-members.csv",
            "stable_key": "curated:qscale-q01-levis-campus:building-b",
            "line": 693,
            "bytes": 333,
            "sha256": "3f2443cee768e393a21cd54276fcda34a6c34c5b4dc9ee128bc40e363112fe26",
        },
        "object_member": {
            "release_id": "2026-07-22-public-open-v14",
            "member": "component-members.csv",
            "stable_key": "curated:qscale-q01-levis-campus",
            "line": 295,
            "bytes": 321,
            "sha256": "48e517fcf239ab01cfd81f7884e124095a0645cc0bbbddbde357e6d77f0496ea",
        },
    },
    "curated:green-campus-zrh1-lupfig:data-center-4": {
        "geometry_entity": {
            "release_id": "global-open-v3",
            "member": "entities.csv",
            "stable_key": "osm:way/242248627",
            "line": 1482,
            "bytes": 1204,
            "sha256": "0089e033a00ea6034decfebd8ca8700c1c57413424b6982a4ab416922312cf31",
        },
        "geometry_evidence": {
            "release_id": "global-open-v3",
            "member": "evidence.csv",
            "evidence_id": "a488471c-6dc2-593f-92df-db707862cbfc",
            "line": 4457,
            "bytes": 346,
            "sha256": "217bc4b5fea78cbfc2ecec2dbd5658dedbf47aa47484b58ec9c8f35704ce1a3b",
        },
        "relationship": {
            "release_id": "2026-07-22-public-open-v14",
            "member": "relationships.csv",
            "relationship_id": "relationship:a20d17ac9a8b24d64952d63afc20e386dfe9b087ec25685d7a38695c2e2b62a6",
            "line": 1628,
            "bytes": 305,
            "sha256": "545ac14096b373e3b47fe3f3b292ee7d82f9c7fe9ff62ad742f027f87fa583b5",
        },
        "subject_member": {
            "release_id": "2026-07-22-public-open-v14",
            "member": "component-members.csv",
            "stable_key": "curated:green-campus-zrh1-lupfig:data-center-4",
            "line": 355,
            "bytes": 363,
            "sha256": "292e8e6b0c1027b8b501d720689a96cffdd77b46b558211dee9810aa3666f6ea",
        },
        "object_member": {
            "release_id": "2026-07-22-public-open-v14",
            "member": "component-members.csv",
            "stable_key": "curated:green-campus-zrh1-lupfig",
            "line": 438,
            "bytes": 348,
            "sha256": "f8788e9d68ddf1468f9c988a0b7a35ec2bed0bd87f2f8140162cfd6009989af1",
        },
    },
    "curated:related-openai-oracle-stargate-michigan-saline:current-build": {
        "geometry_entity": {
            "release_id": "global-open-v3",
            "member": "entities.csv",
            "stable_key": "osm:way/1528327802",
            "line": 7488,
            "bytes": 1046,
            "sha256": "b1d287181b8b9c1efaa0de257c0d1ccde33e7c440e62fc5fc5192b07891d208c",
        },
        "geometry_evidence": {
            "release_id": "global-open-v3",
            "member": "evidence.csv",
            "evidence_id": "2c1a0857-1368-581b-adfa-96bf72cc9c16",
            "line": 1243,
            "bytes": 341,
            "sha256": "c10955e4fae8fbc890eb24711dac9f54b97c3655cb2b49b0902e7b16256ad781",
        },
        "relationship": {
            "release_id": "2026-07-22-public-open-v14",
            "member": "relationships.csv",
            "relationship_id": "relationship:3bcf7d690a6c8e7b1dddf8e45ddb457847d5a589a3dd753a50107343b065279f",
            "line": 633,
            "bytes": 305,
            "sha256": "5b391e31ace079b39e0249fe7d5eec4c4e4af2df5022cb283d18ea5827707159",
        },
        "subject_member": {
            "release_id": "2026-07-22-public-open-v14",
            "member": "component-members.csv",
            "stable_key": (
                "curated:related-openai-oracle-stargate-michigan-saline:current-build"
            ),
            "line": 509,
            "bytes": 379,
            "sha256": "0e0fa97fd888d1bd69cb57225e2db4997166d46f27211f4532d14c69f4d35b92",
        },
        "object_member": {
            "release_id": "2026-07-22-public-open-v14",
            "member": "component-members.csv",
            "stable_key": "curated:related-openai-oracle-stargate-michigan-saline",
            "line": 432,
            "bytes": 364,
            "sha256": "0d7f6ee7355975f4a7bfde019f296bf96e9e8c87e93d3a07e437fa8c4fe8d72f",
        },
    },
    "curated:microsoft-mount-pleasant-datacenter-campus:second-facility": {
        "geometry_entity": {
            "release_id": "global-open-v3",
            "member": "entities.csv",
            "stable_key": "osm:way/1528490094",
            "line": 8156,
            "bytes": 951,
            "sha256": "4e10305dbbf68253152eedaa22f5b4bca66fc86116761d4f638371a032cb9652",
        },
        "geometry_evidence": {
            "release_id": "global-open-v3",
            "member": "evidence.csv",
            "evidence_id": "ab38ac59-f17b-5f63-b060-392d2bebd078",
            "line": 4620,
            "bytes": 351,
            "sha256": "ae126f114e1132c8ba0a5e9e22602087d414d0718133c89eabbf9e361b8b67bb",
        },
        "relationship": {
            "release_id": "2026-07-22-public-open-v14",
            "member": "relationships.csv",
            "relationship_id": "relationship:5331540b613864aed98b22addbeaec3104ec83a5007a409654a27454209d4fd1",
            "line": 854,
            "bytes": 305,
            "sha256": "b6abab4859428f9a5227cc7999b1db23accd6e4e723500ae50853747451fa595",
        },
        "subject_member": {
            "release_id": "2026-07-22-public-open-v14",
            "member": "component-members.csv",
            "stable_key": (
                "curated:microsoft-mount-pleasant-datacenter-campus:second-facility"
            ),
            "line": 858,
            "bytes": 365,
            "sha256": "d7ee099cc646e475fa89f534761968463dc89a0bc89ae603074991ce928077e2",
        },
        "object_member": {
            "release_id": "2026-07-22-public-open-v14",
            "member": "component-members.csv",
            "stable_key": "curated:microsoft-mount-pleasant-datacenter-campus",
            "line": 700,
            "bytes": 348,
            "sha256": "32417f946e727399cf83a3a3990130f664689da823646c359b3ba158ac740c07",
        },
    },
    "curated:amazon-salem-township-innovation-campus:active-buildout": {
        "geometry_entity": {
            "release_id": "global-open-v3",
            "member": "entities.csv",
            "stable_key": "osm:way/1334234458",
            "line": 4855,
            "bytes": 1184,
            "sha256": "a26c3cfb01c137b816d7e03c8407c87494c89de93cd4ddab492fa30e9728956c",
        },
        "geometry_evidence": {
            "release_id": "global-open-v3",
            "member": "evidence.csv",
            "evidence_id": "13a65a07-1b3a-58a3-9ca7-b98723686f7c",
            "line": 583,
            "bytes": 335,
            "sha256": "9186bac4aa9570f15608a0e0098d9c7fb898876d380fcaa6f0c06102de32943a",
        },
        "relationship": {
            "release_id": "2026-07-22-public-open-v14",
            "member": "relationships.csv",
            "relationship_id": "relationship:3f242c8c7209bd11809916cebef051602992a3db7d4e0e6ea4e0f5f1da706b62",
            "line": 662,
            "bytes": 305,
            "sha256": "52efa9753c85574c744875734ef69bb5ea40160f5d23f64fb462303ef9220f62",
        },
        "subject_member": {
            "release_id": "2026-07-22-public-open-v14",
            "member": "component-members.csv",
            "stable_key": "curated:amazon-salem-township-innovation-campus:active-buildout",
            "line": 834,
            "bytes": 400,
            "sha256": "d8488e3961f15786331a0845a17fd1fae927d47a0c14dc95e4ccf5e0ac8d8c66",
        },
        "object_member": {
            "release_id": "2026-07-22-public-open-v14",
            "member": "component-members.csv",
            "stable_key": "curated:amazon-salem-township-innovation-campus",
            "line": 590,
            "bytes": 383,
            "sha256": "f6cd0c01918b12bb068e4a650204ae250964ab065baebcfd3bd55b2afba14084",
        },
    },
}


class VerifiedConstructionCoreError(ValueError):
    """Raised when a source, review decision, or preview violates the contract."""


@dataclass(frozen=True)
class PreviewProfile:
    preview_id: str
    preview_dir: Path
    reviewed_at: date
    base_preview_id: str
    base_preview_dir: Path
    base_manifest_sha256: str
    base_commit: str
    definition_pins: tuple[tuple[str, Path, str], ...]


def _sha256_bytes(payload: bytes) -> str:
    return hashlib.sha256(payload).hexdigest()


def _sha256_file(path: Path) -> str:
    digest = hashlib.sha256()
    with path.open("rb") as handle:
        for chunk in iter(lambda: handle.read(1024 * 1024), b""):
            digest.update(chunk)
    return digest.hexdigest()


def load_current_v07_profile(
    definition_sha256: Mapping[str, str],
) -> PreviewProfile:
    """Load v0.7 only after its exact definition hashes are supplied."""
    expected_fields = {field for field, _ in CURRENT_V07_DEFINITION_PATHS}
    if set(definition_sha256) != expected_fields:
        raise VerifiedConstructionCoreError(
            "current v0.7 definition pins are incomplete"
        )
    pins: list[tuple[str, Path, str]] = []
    for field, path in CURRENT_V07_DEFINITION_PATHS:
        expected_sha256 = definition_sha256[field]
        if not re.fullmatch(r"[0-9a-f]{64}", expected_sha256):
            raise VerifiedConstructionCoreError(
                f"current v0.7 {field} pin is invalid"
            )
        if (
            path.is_symlink()
            or not path.is_file()
            or _sha256_file(path) != expected_sha256
        ):
            raise VerifiedConstructionCoreError(
                f"current v0.7 {field} source hash differs"
            )
        pins.append((field, path, expected_sha256))
    return PreviewProfile(
        preview_id=CURRENT_V07_PREVIEW_ID,
        preview_dir=CURRENT_V07_PREVIEW_DIR,
        reviewed_at=CURRENT_V07_REVIEW_DATE,
        base_preview_id="2026-08-20-preview-v0.6",
        base_preview_dir=LEGACY_PREVIEW_V06_DIR,
        base_manifest_sha256=LEGACY_PREVIEW_V06_MANIFEST_SHA256,
        base_commit=LEGACY_PREVIEW_V06_COMMIT,
        definition_pins=tuple(pins),
    )


def _current_definition_pins() -> tuple[tuple[str, Path, str], ...]:
    return (
        (
            "review_definition_sha256",
            REVIEW_DEFINITION,
            V06_REVIEW_DEFINITION_SHA256,
        ),
        (
            "imagery_review_definition_sha256",
            IMAGERY_REVIEW_DEFINITION,
            V06_IMAGERY_REVIEW_DEFINITION_SHA256,
        ),
        (
            "provenance_definition_sha256",
            PROVENANCE_DEFINITION,
            V06_PROVENANCE_DEFINITION_SHA256,
        ),
        (
            "overlay_definition_sha256",
            OVERLAY_DEFINITION,
            V06_OVERLAY_DEFINITION_SHA256,
        ),
    )


def _validate_current_definition_pins() -> None:
    for field, definition, expected_sha256 in _current_definition_pins():
        if (
            definition.is_symlink()
            or not definition.is_file()
            or _sha256_file(definition) != expected_sha256
        ):
            raise VerifiedConstructionCoreError(
                f"preview {field} source hash differs"
            )


def _json_bytes(value: object) -> bytes:
    return (
        json.dumps(value, ensure_ascii=False, sort_keys=True, separators=(",", ":"))
        + "\n"
    ).encode("utf-8")


def _csv_bytes(rows: Sequence[Mapping[str, Any]], fields: Sequence[str]) -> bytes:
    buffer = io.StringIO(newline="")
    writer = csv.DictWriter(buffer, fieldnames=fields, lineterminator="\n")
    writer.writeheader()
    for row in rows:
        writer.writerow({field: row.get(field, "") for field in fields})
    return buffer.getvalue().encode("utf-8")


def _load_json(path: Path) -> Any:
    try:
        return json.loads(path.read_text(encoding="utf-8"))
    except (OSError, json.JSONDecodeError) as error:
        raise VerifiedConstructionCoreError(f"cannot read canonical JSON {path}") from error


def _load_csv(
    path: Path, expected_fields: Sequence[str] | None = None
) -> list[dict[str, str]]:
    try:
        with path.open(newline="", encoding="utf-8") as handle:
            reader = csv.DictReader(handle)
            if expected_fields is not None and tuple(reader.fieldnames or ()) != tuple(
                expected_fields
            ):
                raise VerifiedConstructionCoreError(
                    f"CSV field order differs: {path}"
                )
            return list(reader)
    except OSError as error:
        raise VerifiedConstructionCoreError(f"cannot read CSV {path}") from error


def _load_jsonl(path: Path) -> list[dict[str, Any]]:
    try:
        rows = [
            json.loads(line)
            for line in path.read_text(encoding="utf-8").splitlines()
            if line.strip()
        ]
    except (OSError, json.JSONDecodeError) as error:
        raise VerifiedConstructionCoreError(f"cannot read JSONL {path}") from error
    if any(not isinstance(row, dict) for row in rows):
        raise VerifiedConstructionCoreError(f"JSONL row differs: {path}")
    return rows


def _calendar_date(value: str, field: str) -> date:
    try:
        parsed = date.fromisoformat(value)
    except (TypeError, ValueError) as error:
        raise VerifiedConstructionCoreError(f"{field} must be YYYY-MM-DD") from error
    return parsed


def _stable_id(prefix: str, key: str) -> str:
    return f"{prefix}-{hashlib.sha256(key.encode()).hexdigest()[:20]}"


def _parse_json_field(value: str, field: str) -> Any:
    try:
        return json.loads(value)
    except json.JSONDecodeError as error:
        raise VerifiedConstructionCoreError(f"invalid {field} JSON") from error


def _finite_number(value: Any) -> bool:
    return (
        not isinstance(value, bool)
        and isinstance(value, (int, float))
        and math.isfinite(value)
    )


def _validate_typed_observation(
    observation: Any, allowed_metrics: set[str]
) -> dict[str, Any]:
    fields = {
        "as_of_date",
        "base",
        "confidence",
        "evidence_id",
        "high",
        "low",
        "method",
        "metric",
        "notes",
        "stage",
        "target_date",
        "unit",
    }
    if not isinstance(observation, dict) or set(observation) != fields:
        raise VerifiedConstructionCoreError("typed-metric observation schema differs")
    metric = observation["metric"]
    if metric not in allowed_metrics or observation["unit"] != METRIC_UNITS[metric]:
        raise VerifiedConstructionCoreError("typed-metric metric/unit differs")
    low = observation["low"]
    base = observation["base"]
    high = observation["high"]
    if not all(_finite_number(value) for value in (low, base, high)):
        raise VerifiedConstructionCoreError("typed-metric interval is not finite")
    if low < 0 or not low <= base <= high or (metric == "pue" and low <= 0):
        raise VerifiedConstructionCoreError("typed-metric interval differs")
    confidence = observation["confidence"]
    if not _finite_number(confidence) or not 0 <= confidence <= 1:
        raise VerifiedConstructionCoreError("typed-metric confidence differs")
    if observation["method"] not in ESTIMATE_METHODS:
        raise VerifiedConstructionCoreError("typed-metric method differs")
    if observation["stage"] not in CAPACITY_STAGES:
        raise VerifiedConstructionCoreError("typed-metric stage differs")
    if not isinstance(observation["evidence_id"], str) or not observation[
        "evidence_id"
    ]:
        raise VerifiedConstructionCoreError("typed-metric evidence id differs")
    if not isinstance(observation["notes"], str):
        raise VerifiedConstructionCoreError("typed-metric notes differ")
    _calendar_date(observation["as_of_date"], "typed-metric as_of_date")
    target_date = observation["target_date"]
    if target_date is not None:
        _calendar_date(target_date, "typed-metric target_date")
    return observation


SOURCE_WORKLOAD_FIELDS = {
    "as_of_date",
    "confidence",
    "evidence_id",
    "method",
    "workload",
}


def _validate_source_workload_observation(observation: Any) -> dict[str, Any]:
    if not isinstance(observation, dict) or set(observation) != SOURCE_WORKLOAD_FIELDS:
        raise VerifiedConstructionCoreError("workload observation schema differs")
    confidence = observation["confidence"]
    if not _finite_number(confidence) or not 0 <= confidence <= 1:
        raise VerifiedConstructionCoreError("workload confidence differs")
    for field in ("evidence_id", "method", "workload"):
        if not isinstance(observation[field], str) or not observation[field]:
            raise VerifiedConstructionCoreError(f"workload {field} differs")
    _calendar_date(observation["as_of_date"], "workload as_of_date")
    return observation


def _validate_workload_observation(observation: Any) -> dict[str, Any]:
    fields = SOURCE_WORKLOAD_FIELDS | {"deployment_scope"}
    if not isinstance(observation, dict) or set(observation) != fields:
        raise VerifiedConstructionCoreError("workload observation schema differs")
    _validate_source_workload_observation(
        {field: observation[field] for field in SOURCE_WORKLOAD_FIELDS}
    )
    if observation["deployment_scope"] not in {
        "intended",
        "operational",
        "unknown",
    }:
        raise VerifiedConstructionCoreError("workload deployment scope differs")
    return observation


def _validate_role_claim(claim: Any) -> dict[str, Any]:
    fields = {"evidence_id", "party", "relationship_scope", "role"}
    if not isinstance(claim, dict) or set(claim) != fields:
        raise VerifiedConstructionCoreError("role claim schema differs")
    for field in ("evidence_id", "party", "relationship_scope", "role"):
        if not isinstance(claim[field], str) or not claim[field]:
            raise VerifiedConstructionCoreError(f"role claim {field} differs")
    if claim["role"] not in ROLE_COLUMNS or claim["relationship_scope"] not in {
        "intended",
        "current",
        "unknown",
    }:
        raise VerifiedConstructionCoreError("role claim semantics differ")
    return claim


def _verify_source_release() -> dict[str, Any]:
    manifest_path = SOURCE_RELEASE / "manifest.json"
    manifest = _load_json(manifest_path)
    files = manifest.get("files")
    if not isinstance(files, dict):
        raise VerifiedConstructionCoreError("source release manifest has no files map")
    for name in ("entities.csv", "construction_pipeline.csv", "evidence.csv"):
        metadata = files.get(name)
        if not isinstance(metadata, dict):
            raise VerifiedConstructionCoreError(f"source manifest does not bind {name}")
        path = SOURCE_RELEASE / name
        if not path.is_file():
            raise VerifiedConstructionCoreError(f"source payload is not hydrated: {path}")
        if path.stat().st_size != metadata.get("bytes"):
            raise VerifiedConstructionCoreError(f"source payload byte count differs: {name}")
        if _sha256_file(path) != metadata.get("sha256"):
            raise VerifiedConstructionCoreError(f"source payload hash differs: {name}")
    return manifest


def _geometry_release_manifest(
    release_id: str, expected_sha256: str
) -> tuple[Path, dict[str, Any]]:
    release = GEOMETRY_RELEASES.get(release_id)
    if release is None:
        raise VerifiedConstructionCoreError(
            f"reviewed overlay geometry release is unknown: {release_id}"
        )
    manifest_path = release / "manifest.json"
    manifest = _load_json(manifest_path)
    if _sha256_file(manifest_path) != expected_sha256:
        raise VerifiedConstructionCoreError(
            f"reviewed overlay geometry release manifest differs: {release_id}"
        )
    files = manifest.get("files")
    if not isinstance(files, dict) or not isinstance(files.get("entities.csv"), dict):
        raise VerifiedConstructionCoreError(
            f"reviewed overlay geometry release does not bind entities.csv: {release_id}"
        )
    return release, manifest


def _geometry_release_entities(
    release_id: str,
    expected_manifest_sha256: str,
    cache: dict[tuple[str, str], dict[str, dict[str, str]]],
) -> dict[str, dict[str, str]]:
    cache_key = (release_id, expected_manifest_sha256)
    if cache_key in cache:
        return cache[cache_key]
    release, manifest = _geometry_release_manifest(
        release_id, expected_manifest_sha256
    )
    entities_path = release / "entities.csv"
    metadata = manifest["files"]["entities.csv"]
    if not entities_path.is_file():
        raise VerifiedConstructionCoreError(
            f"reviewed overlay geometry payload is not hydrated: {entities_path}"
        )
    if entities_path.stat().st_size != metadata.get("bytes") or _sha256_file(
        entities_path
    ) != metadata.get("sha256"):
        raise VerifiedConstructionCoreError(
            f"reviewed overlay geometry payload differs: {release_id}"
        )
    entities = _load_csv(entities_path)
    entities_by_key = {row["stable_key"]: row for row in entities}
    if len(entities_by_key) != len(entities):
        raise VerifiedConstructionCoreError(
            f"reviewed overlay geometry stable keys are not unique: {release_id}"
        )
    cache[cache_key] = entities_by_key
    return entities_by_key


REVIEWED_SITE_LEGACY_FIELDS = {
    "project_stable_key",
    "physical_site_stable_key",
    "source_input_path",
    "source_input_sha256",
    "geometry_evidence_key",
    "geometry_method",
    "geometry_scope_class",
    "horizontal_uncertainty_metres",
    "precision_scope",
    "decision_basis",
}
REVIEWED_SITE_FIELDS = REVIEWED_SITE_LEGACY_FIELDS | {
    "geometry_entity",
    "geometry_derivation",
}
REVIEWED_SITE_OVERLAY_FIELDS = REVIEWED_SITE_FIELDS | {"geometry_overlay_id"}


def _reviewed_acceptances_from(
    path: Path, seen: frozenset[Path] = frozenset()
) -> tuple[list[dict[str, Any]], list[dict[str, Any]]]:
    path = path.resolve()
    definitions_root = (ROOT / "definitions").resolve()
    if path.parent != definitions_root or path in seen:
        raise VerifiedConstructionCoreError("reviewed-site contract path or cycle differs")
    if not path.is_file():
        raise VerifiedConstructionCoreError("reviewed-site contract is absent")
    reviewed = _load_json(path)
    if not isinstance(reviewed, dict):
        raise VerifiedConstructionCoreError("reviewed-site contract differs")
    contract_id = reviewed.get("contract_id")
    expected_id = path.stem
    if contract_id != expected_id:
        raise VerifiedConstructionCoreError("reviewed-site contract id differs")
    reviewed_as_of = _calendar_date(
        reviewed.get("reviewed_as_of"), "reviewed-site reviewed_as_of"
    )
    if reviewed_as_of > REVIEW_DATE:
        raise VerifiedConstructionCoreError("reviewed-site contract date differs")
    acceptances = reviewed.get("acceptances")
    if not isinstance(acceptances, list) or not acceptances:
        raise VerifiedConstructionCoreError("reviewed-site acceptances are empty")

    if "base_contract" not in reviewed:
        if set(reviewed) != {
            "contract_id",
            "review_scope",
            "reviewed_as_of",
            "acceptances",
        } or contract_id != "verified-construction-core-v0.1-reviewed-sites":
            raise VerifiedConstructionCoreError("reviewed-site leaf contract differs")
        normalized: list[dict[str, Any]] = []
        for index, acceptance in enumerate(acceptances):
            if not isinstance(acceptance, dict) or set(acceptance) != REVIEWED_SITE_LEGACY_FIELDS:
                raise VerifiedConstructionCoreError(
                    f"reviewed-site leaf acceptance {index} has unexpected fields"
                )
            normalized.append(
                {
                    **acceptance,
                    "geometry_entity": "project",
                    "geometry_derivation": "direct_geometry",
                }
            )
        return normalized, normalized

    if set(reviewed) != {
        "contract_id",
        "review_scope",
        "reviewed_as_of",
        "base_contract",
        "acceptances",
    }:
        raise VerifiedConstructionCoreError("reviewed-site successor fields differ")
    base = reviewed["base_contract"]
    if not isinstance(base, dict) or set(base) != {
        "path",
        "sha256",
        "default_geometry_entity",
        "default_geometry_derivation",
    }:
        raise VerifiedConstructionCoreError("reviewed-site base contract differs")
    if base.get("default_geometry_entity") != "project" or base.get(
        "default_geometry_derivation"
    ) != "direct_geometry":
        raise VerifiedConstructionCoreError("reviewed-site base defaults differ")
    relative = Path(base.get("path", ""))
    if relative.is_absolute() or ".." in relative.parts:
        raise VerifiedConstructionCoreError("reviewed-site base path differs")
    base_path = (ROOT / relative).resolve()
    if (
        base_path.parent != definitions_root
        or not base_path.is_file()
        or _sha256_file(base_path) != base.get("sha256")
    ):
        raise VerifiedConstructionCoreError("reviewed-site base hash differs")
    inherited, _ = _reviewed_acceptances_from(base_path, seen | {path})
    delta: list[dict[str, Any]] = []
    for index, acceptance in enumerate(acceptances):
        if not isinstance(acceptance, dict) or frozenset(acceptance) not in {
            frozenset(REVIEWED_SITE_FIELDS),
            frozenset(REVIEWED_SITE_OVERLAY_FIELDS),
        }:
            raise VerifiedConstructionCoreError(
                f"reviewed-site delta acceptance {index} has unexpected fields"
            )
        delta.append({**acceptance, "geometry_overlay_id": acceptance.get("geometry_overlay_id")})
    flattened = [*inherited, *delta]
    keys = [row["project_stable_key"] for row in flattened]
    if len(set(keys)) != len(keys):
        raise VerifiedConstructionCoreError("reviewed-site project keys are not unique")
    for acceptance in flattened:
        acceptance.setdefault("geometry_overlay_id", None)
        if acceptance["geometry_entity"] not in {"project", "campus"}:
            raise VerifiedConstructionCoreError("reviewed-site geometry entity differs")
        if acceptance["geometry_derivation"] not in {
            "direct_geometry",
            "coordinates_to_point",
            "cross_source_overlay",
            "official_parcel_union",
        }:
            raise VerifiedConstructionCoreError("reviewed-site geometry derivation differs")
        overlay_id = acceptance.get("geometry_overlay_id")
        if overlay_id is not None:
            if (
                not isinstance(overlay_id, str)
                or not overlay_id
                or acceptance.get("geometry_evidence_key") is not None
            ):
                raise VerifiedConstructionCoreError(
                    "reviewed-site overlay geometry source differs"
                )
        elif acceptance["geometry_derivation"] in {
            "cross_source_overlay",
            "official_parcel_union",
        }:
            raise VerifiedConstructionCoreError("reviewed-site overlay geometry source differs")
    return flattened, delta


def _reviewed_acceptances() -> list[dict[str, Any]]:
    acceptances, _ = _reviewed_acceptances_from(REVIEW_DEFINITION)
    if REVIEW_DEFINITION.stem != "verified-construction-core-v0.6-reviewed-sites":
        raise VerifiedConstructionCoreError("current reviewed-site contract differs")
    reviewed = _load_json(REVIEW_DEFINITION)
    if reviewed.get("reviewed_as_of") != REVIEW_DATE.isoformat():
        raise VerifiedConstructionCoreError("current reviewed-site date differs")
    return acceptances


ROLE_COLUMNS = {
    "owner": "owner",
    "operator": "operator",
    "user": "users",
    "tenant": "tenants",
    "customer": "customers",
}


def _repository_input(path_text: str, expected_sha256: str, field: str) -> Path:
    relative = Path(path_text)
    if relative.is_absolute() or ".." in relative.parts:
        raise VerifiedConstructionCoreError(f"{field} path escapes the repository")
    path = ROOT / relative
    if (
        path.is_symlink()
        or not path.is_file()
        or _sha256_file(path) != expected_sha256
    ):
        raise VerifiedConstructionCoreError(f"{field} source hash differs")
    return path


def _evidence_from_pinned_source(
    binding: Mapping[str, Any], *, evidence_key_field: str, evidence_id_field: str
) -> tuple[dict[str, Any], dict[str, Any]]:
    path = _repository_input(
        str(binding["source_input_path"]),
        str(binding["source_input_sha256"]),
        "provenance",
    )
    source = _load_json(path)
    project = source.get("project")
    if not isinstance(project, dict) or project.get("stable_key") != binding.get(
        "project_stable_key"
    ):
        raise VerifiedConstructionCoreError("provenance project identity differs")
    evidence = source.get("evidence")
    if not isinstance(evidence, list):
        raise VerifiedConstructionCoreError("provenance source evidence differs")
    key = binding[evidence_key_field]
    matches = [row for row in evidence if isinstance(row, dict) and row.get("key") == key]
    if len(matches) != 1:
        raise VerifiedConstructionCoreError("provenance evidence key differs")
    pinned = matches[0]
    evidence_id = binding[evidence_id_field]
    if evidence_id != atlas_stable_id(
        "evidence", "curated-official", key, pinned.get("content_hash")
    ):
        raise VerifiedConstructionCoreError("provenance evidence id differs")
    required = {
        "kind",
        "title",
        "source_url",
        "publisher",
        "source_family",
        "license",
        "attribution",
        "retrieved_at",
        "content_hash",
    }
    if any(not isinstance(pinned.get(field), str) or not pinned[field] for field in required):
        raise VerifiedConstructionCoreError("provenance evidence fields differ")
    published_at = pinned.get("published_at")
    if published_at is not None and not isinstance(published_at, str):
        raise VerifiedConstructionCoreError("provenance evidence publication date differs")
    row = {
        "evidence_id": evidence_id,
        "kind": pinned["kind"],
        "title": pinned["title"],
        "source_url": pinned["source_url"],
        "publisher": pinned["publisher"],
        "source_family": pinned["source_family"],
        "license": pinned["license"],
        "attribution": pinned["attribution"],
        "published_at": published_at or "",
        "retrieved_at": pinned["retrieved_at"],
        "content_hash": pinned["content_hash"],
    }
    return row, pinned


def _provenance_contract_v03(path: Path) -> dict[str, Any]:
    validate_frozen_v02(LEGACY_PREVIEW_V02_DIR)
    reviewed = _load_json(path)
    expected_fields = {
        "contract_id",
        "review_scope",
        "reviewed_as_of",
        "base_contract",
        "workload_scope_bindings",
        "role_bindings",
        "excluded_source_roles",
    }
    if not isinstance(reviewed, dict) or set(reviewed) != expected_fields:
        raise VerifiedConstructionCoreError("provenance contract fields differ")
    if (
        reviewed.get("contract_id") != "verified-construction-core-v0.3-provenance"
        or reviewed.get("reviewed_as_of") != REVIEW_DATE.isoformat()
    ):
        raise VerifiedConstructionCoreError("provenance contract identity differs")
    base = reviewed.get("base_contract")
    if not isinstance(base, dict) or set(base) != {
        "preview_manifest",
        "reviewed_sites_definition",
    }:
        raise VerifiedConstructionCoreError("provenance base contract differs")
    expected_base = {
        "preview_manifest": (
            "verified_construction_core/2026-08-20-preview-v0.2/manifest.json",
            LEGACY_PREVIEW_V02_MANIFEST_SHA256,
        ),
        "reviewed_sites_definition": (
            "definitions/verified-construction-core-v0.2-reviewed-sites.json",
            "13f4d6e82c9057b1dd95319e19e30c2bae0ac0f1848d6e6dee2bbc10b5dde1d0",
        ),
    }
    for name, (expected_path, expected_sha256) in expected_base.items():
        item = base.get(name)
        if not isinstance(item, dict) or set(item) != {"path", "sha256"}:
            raise VerifiedConstructionCoreError("provenance base item differs")
        if item != {"path": expected_path, "sha256": expected_sha256}:
            raise VerifiedConstructionCoreError("provenance base pin differs")
        _repository_input(expected_path, expected_sha256, "provenance base")

    workload_fields = {
        "project_stable_key",
        "source_input_path",
        "source_input_sha256",
        "evidence_key",
        "evidence_id",
        "workload",
        "as_of_date",
        "method",
        "confidence",
        "deployment_scope",
        "semantic_scope",
    }
    role_fields = {
        "project_stable_key",
        "source_input_path",
        "source_input_sha256",
        "role",
        "party",
        "evidence_key",
        "evidence_id",
        "relationship_scope",
        "semantic_scope",
    }
    excluded_fields = {
        "project_stable_key",
        "source_input_path",
        "source_input_sha256",
        "role",
        "party",
        "candidate_evidence_key",
        "candidate_evidence_id",
        "relationship_scope",
        "semantic_scope",
        "reason",
    }
    workload_bindings = reviewed.get("workload_scope_bindings")
    role_bindings = reviewed.get("role_bindings")
    exclusions = reviewed.get("excluded_source_roles")
    if not all(isinstance(rows, list) for rows in (workload_bindings, role_bindings, exclusions)):
        raise VerifiedConstructionCoreError("provenance binding collections differ")
    evidence_rows: dict[str, dict[str, Any]] = {}
    workload_keys: set[tuple[str, str, str]] = set()
    for index, binding in enumerate(workload_bindings):
        if not isinstance(binding, dict) or set(binding) != workload_fields:
            raise VerifiedConstructionCoreError(
                f"workload provenance binding {index} differs"
            )
        key = (
            binding["project_stable_key"],
            binding["evidence_id"],
            binding["workload"],
        )
        if key in workload_keys or binding["deployment_scope"] != "intended":
            raise VerifiedConstructionCoreError("workload provenance identity differs")
        workload_keys.add(key)
        _calendar_date(binding["as_of_date"], "workload provenance as_of_date")
        if not _finite_number(binding["confidence"]) or not 0 <= binding["confidence"] <= 1:
            raise VerifiedConstructionCoreError("workload provenance confidence differs")
        evidence_row, pinned = _evidence_from_pinned_source(
            binding,
            evidence_key_field="evidence_key",
            evidence_id_field="evidence_id",
        )
        metadata = pinned.get("metadata")
        if not isinstance(metadata, dict) or metadata.get("workload_scope") != binding[
            "semantic_scope"
        ]:
            raise VerifiedConstructionCoreError("workload provenance scope differs")
        evidence_rows[evidence_row["evidence_id"]] = evidence_row

    role_keys: set[tuple[str, str, str]] = set()
    for index, binding in enumerate(role_bindings):
        if not isinstance(binding, dict) or set(binding) != role_fields:
            raise VerifiedConstructionCoreError(f"role provenance binding {index} differs")
        key = (
            binding["project_stable_key"],
            binding["role"],
            binding["party"],
        )
        if (
            key in role_keys
            or binding["role"] not in ROLE_COLUMNS
            or binding["relationship_scope"] != "intended"
        ):
            raise VerifiedConstructionCoreError("role provenance identity differs")
        role_keys.add(key)
        evidence_row, pinned = _evidence_from_pinned_source(
            binding,
            evidence_key_field="evidence_key",
            evidence_id_field="evidence_id",
        )
        metadata = pinned.get("metadata")
        if not isinstance(metadata, dict) or metadata.get("role_scope") != binding[
            "semantic_scope"
        ]:
            raise VerifiedConstructionCoreError("role provenance scope differs")
        existing = evidence_rows.get(evidence_row["evidence_id"])
        if existing is not None and existing != evidence_row:
            raise VerifiedConstructionCoreError("provenance evidence collision")
        evidence_rows[evidence_row["evidence_id"]] = evidence_row

    excluded_keys: set[tuple[str, str, str]] = set()
    for index, exclusion in enumerate(exclusions):
        if not isinstance(exclusion, dict) or set(exclusion) != excluded_fields:
            raise VerifiedConstructionCoreError(f"role exclusion {index} differs")
        key = (
            exclusion["project_stable_key"],
            exclusion["role"],
            exclusion["party"],
        )
        if (
            key in excluded_keys
            or key in role_keys
            or exclusion["role"] not in ROLE_COLUMNS
            or exclusion["relationship_scope"] != "not_established"
            or not isinstance(exclusion["reason"], str)
            or not exclusion["reason"]
        ):
            raise VerifiedConstructionCoreError("role exclusion identity differs")
        excluded_keys.add(key)
        _evidence_from_pinned_source(
            exclusion,
            evidence_key_field="candidate_evidence_key",
            evidence_id_field="candidate_evidence_id",
        )
    base_projects = {
        row["project_stable_key"]: row
        for row in _load_csv(LEGACY_PREVIEW_V02_DIR / "projects.csv")
    }
    base_workloads: set[tuple[str, str, str, str, str, float]] = set()
    base_roles: set[tuple[str, str, str]] = set()
    for project_key, project in base_projects.items():
        workloads = _parse_json_field(project["workloads_json"], "base workloads")
        if not isinstance(workloads, list):
            raise VerifiedConstructionCoreError("provenance base workloads differ")
        for workload in workloads:
            _validate_source_workload_observation(workload)
            base_workloads.add(
                (
                    project_key,
                    workload["evidence_id"],
                    workload["workload"],
                    workload["as_of_date"],
                    workload["method"],
                    workload["confidence"],
                )
            )
        for role, column in ROLE_COLUMNS.items():
            for party in project[column].split(";"):
                if party.strip():
                    base_roles.add((project_key, role, party.strip()))
    bound_workloads = {
        (
            row["project_stable_key"],
            row["evidence_id"],
            row["workload"],
            row["as_of_date"],
            row["method"],
            row["confidence"],
        )
        for row in workload_bindings
    }
    if bound_workloads != base_workloads:
        raise VerifiedConstructionCoreError(
            "provenance workload bindings differ from frozen v0.2"
        )
    if role_keys | excluded_keys != base_roles:
        raise VerifiedConstructionCoreError(
            "provenance role decisions differ from frozen v0.2"
        )
    return {
        "workload_scope_bindings": workload_bindings,
        "role_bindings": role_bindings,
        "excluded_source_roles": exclusions,
        "evidence_rows": evidence_rows,
    }


def _provenance_contract_v04(path: Path) -> dict[str, Any]:
    reviewed = _load_json(path)
    expected_fields = {
        "contract_id",
        "review_scope",
        "reviewed_as_of",
        "base_contract",
        "workload_scope_bindings",
        "role_bindings",
        "excluded_source_roles",
    }
    if not isinstance(reviewed, dict) or set(reviewed) != expected_fields:
        raise VerifiedConstructionCoreError("provenance contract fields differ")
    if (
        reviewed.get("contract_id") != "verified-construction-core-v0.4-provenance"
        or reviewed.get("reviewed_as_of") != REVIEW_DATE.isoformat()
        or reviewed.get("workload_scope_bindings") != []
        or reviewed.get("role_bindings") != []
    ):
        raise VerifiedConstructionCoreError("provenance current contract differs")
    expected_base = {
        "path": "definitions/verified-construction-core-v0.3-provenance.json",
        "sha256": V03_PROVENANCE_DEFINITION_SHA256,
    }
    if reviewed.get("base_contract") != expected_base:
        raise VerifiedConstructionCoreError("provenance base pin differs")
    base_path = _repository_input(
        expected_base["path"], expected_base["sha256"], "provenance base"
    )
    base = _provenance_contract_v03(base_path)
    exclusions = reviewed.get("excluded_source_roles")
    expected_projects = {
        "curated:atnorth-ice02-reykjanesbaer-campus:phase-2-expansion": "atNorth",
        "curated:qscale-q01-levis-campus:building-b": "QScale",
    }
    excluded_fields = {
        "project_stable_key",
        "source_input_path",
        "source_input_sha256",
        "role",
        "party",
        "candidate_evidence_key",
        "candidate_evidence_id",
        "relationship_scope",
        "semantic_scope",
        "reason",
    }
    if not isinstance(exclusions, list) or len(exclusions) != len(expected_projects):
        raise VerifiedConstructionCoreError("provenance current exclusions differ")
    seen: set[str] = set()
    for index, exclusion in enumerate(exclusions):
        if not isinstance(exclusion, dict) or set(exclusion) != excluded_fields:
            raise VerifiedConstructionCoreError(
                f"provenance current exclusion {index} differs"
            )
        project_key = exclusion["project_stable_key"]
        if (
            project_key in seen
            or exclusion.get("party") != expected_projects.get(project_key)
            or exclusion.get("role") != "operator"
            or exclusion.get("relationship_scope") != "not_established"
            or not isinstance(exclusion.get("semantic_scope"), str)
            or not exclusion["semantic_scope"]
            or not isinstance(exclusion.get("reason"), str)
            or not exclusion["reason"]
        ):
            raise VerifiedConstructionCoreError(
                "provenance current exclusion identity differs"
            )
        seen.add(project_key)
        _evidence_from_pinned_source(
            exclusion,
            evidence_key_field="candidate_evidence_key",
            evidence_id_field="candidate_evidence_id",
        )
    if seen != set(expected_projects):
        raise VerifiedConstructionCoreError("provenance current cohort differs")
    base_exclusion_keys = {
        (row["project_stable_key"], row["role"], row["party"])
        for row in base["excluded_source_roles"]
    }
    current_exclusion_keys = {
        (row["project_stable_key"], row["role"], row["party"])
        for row in exclusions
    }
    if base_exclusion_keys & current_exclusion_keys:
        raise VerifiedConstructionCoreError("provenance inherited exclusion differs")
    return {
        "workload_scope_bindings": base["workload_scope_bindings"],
        "role_bindings": base["role_bindings"],
        "excluded_source_roles": [*base["excluded_source_roles"], *exclusions],
        "evidence_rows": base["evidence_rows"],
    }


def _provenance_contract_v05(path: Path) -> dict[str, Any]:
    reviewed = _load_json(path)
    expected_fields = {
        "contract_id",
        "review_scope",
        "reviewed_as_of",
        "base_contract",
        "workload_scope_bindings",
        "role_bindings",
        "excluded_source_roles",
    }
    if not isinstance(reviewed, dict) or set(reviewed) != expected_fields:
        raise VerifiedConstructionCoreError("provenance contract fields differ")
    if (
        reviewed.get("contract_id") != "verified-construction-core-v0.5-provenance"
        or reviewed.get("reviewed_as_of") != REVIEW_DATE.isoformat()
    ):
        raise VerifiedConstructionCoreError("provenance current contract differs")
    expected_base = {
        "path": "definitions/verified-construction-core-v0.4-provenance.json",
        "sha256": V04_PROVENANCE_DEFINITION_SHA256,
    }
    if reviewed.get("base_contract") != expected_base:
        raise VerifiedConstructionCoreError("provenance base pin differs")
    base_path = _repository_input(
        expected_base["path"], expected_base["sha256"], "provenance base"
    )
    base = _provenance_contract_v04(base_path)
    workload_fields = {
        "project_stable_key",
        "source_input_path",
        "source_input_sha256",
        "evidence_key",
        "evidence_id",
        "workload",
        "as_of_date",
        "method",
        "confidence",
        "deployment_scope",
        "semantic_scope",
    }
    role_fields = {
        "project_stable_key",
        "source_input_path",
        "source_input_sha256",
        "role",
        "party",
        "evidence_key",
        "evidence_id",
        "relationship_scope",
        "semantic_scope",
    }
    excluded_fields = {
        "project_stable_key",
        "source_input_path",
        "source_input_sha256",
        "role",
        "party",
        "candidate_evidence_key",
        "candidate_evidence_id",
        "relationship_scope",
        "semantic_scope",
        "reason",
    }
    workload_bindings = reviewed.get("workload_scope_bindings")
    role_bindings = reviewed.get("role_bindings")
    exclusions = reviewed.get("excluded_source_roles")
    if (
        not isinstance(workload_bindings, list)
        or not isinstance(role_bindings, list)
        or not isinstance(exclusions, list)
        or len(workload_bindings) != 2
        or len(role_bindings) != 2
        or len(exclusions) != 2
    ):
        raise VerifiedConstructionCoreError("provenance current collections differ")
    evidence_rows = dict(base["evidence_rows"])

    def pinned_source(binding: Mapping[str, Any]) -> dict[str, Any]:
        path = _repository_input(
            binding["source_input_path"],
            binding["source_input_sha256"],
            "provenance current source",
        )
        source = _load_json(path)
        if not isinstance(source, dict):
            raise VerifiedConstructionCoreError("provenance current source differs")
        return source

    expected_workloads = {
        "curated:related-openai-oracle-stargate-michigan-saline:current-build": (
            "ai_specialized_unspecified"
        ),
        "curated:amazon-salem-township-innovation-campus:active-buildout": "mixed",
    }
    workload_keys: set[tuple[str, str, str]] = set()
    for index, binding in enumerate(workload_bindings):
        if not isinstance(binding, dict) or set(binding) != workload_fields:
            raise VerifiedConstructionCoreError(
                f"provenance current workload {index} differs"
            )
        project_key = binding["project_stable_key"]
        key = (project_key, binding["evidence_id"], binding["workload"])
        if (
            key in workload_keys
            or binding["workload"] != expected_workloads.get(project_key)
            or binding["deployment_scope"] != "intended"
            or not isinstance(binding["semantic_scope"], str)
            or not binding["semantic_scope"]
        ):
            raise VerifiedConstructionCoreError(
                "provenance current workload identity differs"
            )
        workload_keys.add(key)
        source = pinned_source(binding)
        matches = [
            row
            for row in source.get("workloads", [])
            if isinstance(row, dict)
            and row.get("entity") == "project"
            and row.get("value") == binding["workload"]
            and row.get("evidence_key") == binding["evidence_key"]
        ]
        if len(matches) != 1 or any(
            matches[0].get(field) != binding[field]
            for field in ("as_of_date", "method", "confidence")
        ):
            raise VerifiedConstructionCoreError(
                "provenance current workload projection differs"
            )
        evidence_row, _ = _evidence_from_pinned_source(
            binding,
            evidence_key_field="evidence_key",
            evidence_id_field="evidence_id",
        )
        existing = evidence_rows.get(evidence_row["evidence_id"])
        if existing is not None and existing != evidence_row:
            raise VerifiedConstructionCoreError("provenance evidence collision")
        evidence_rows[evidence_row["evidence_id"]] = evidence_row
    if {key[0] for key in workload_keys} != set(expected_workloads):
        raise VerifiedConstructionCoreError("provenance current workload cohort differs")

    expected_roles = {
        (
            "curated:related-openai-oracle-stargate-michigan-saline:current-build",
            "customer",
            "OpenAI",
        ),
        (
            "curated:related-openai-oracle-stargate-michigan-saline:current-build",
            "customer",
            "Oracle",
        ),
    }
    role_keys: set[tuple[str, str, str]] = set()
    for index, binding in enumerate(role_bindings):
        if not isinstance(binding, dict) or set(binding) != role_fields:
            raise VerifiedConstructionCoreError(
                f"provenance current role {index} differs"
            )
        key = (
            binding["project_stable_key"],
            binding["role"],
            binding["party"],
        )
        source = pinned_source(binding)
        source_roles = source.get("project", {}).get("roles", {})
        if (
            key not in expected_roles
            or key in role_keys
            or binding["relationship_scope"] != "intended"
            or not isinstance(binding["semantic_scope"], str)
            or not binding["semantic_scope"]
            or not isinstance(source_roles, dict)
            or binding["party"] not in source_roles.get(binding["role"], [])
        ):
            raise VerifiedConstructionCoreError(
                "provenance current role identity differs"
            )
        role_keys.add(key)
        evidence_row, _ = _evidence_from_pinned_source(
            binding,
            evidence_key_field="evidence_key",
            evidence_id_field="evidence_id",
        )
        existing = evidence_rows.get(evidence_row["evidence_id"])
        if existing is not None and existing != evidence_row:
            raise VerifiedConstructionCoreError("provenance evidence collision")
        evidence_rows[evidence_row["evidence_id"]] = evidence_row
    if role_keys != expected_roles:
        raise VerifiedConstructionCoreError("provenance current role cohort differs")

    expected_exclusions = {
        (
            "curated:microsoft-mount-pleasant-datacenter-campus:second-facility",
            "operator",
            "Microsoft",
        ),
        (
            "curated:amazon-salem-township-innovation-campus:active-buildout",
            "operator",
            "Amazon Web Services",
        ),
    }
    exclusion_keys: set[tuple[str, str, str]] = set()
    for index, exclusion in enumerate(exclusions):
        if not isinstance(exclusion, dict) or set(exclusion) != excluded_fields:
            raise VerifiedConstructionCoreError(
                f"provenance current exclusion {index} differs"
            )
        key = (
            exclusion["project_stable_key"],
            exclusion["role"],
            exclusion["party"],
        )
        source = pinned_source(exclusion)
        source_roles = source.get("project", {}).get("roles", {})
        if (
            key not in expected_exclusions
            or key in exclusion_keys
            or exclusion["relationship_scope"] != "not_established"
            or not isinstance(exclusion["semantic_scope"], str)
            or not exclusion["semantic_scope"]
            or not isinstance(exclusion["reason"], str)
            or not exclusion["reason"]
            or not isinstance(source_roles, dict)
            or exclusion["party"] not in source_roles.get(exclusion["role"], [])
        ):
            raise VerifiedConstructionCoreError(
                "provenance current exclusion identity differs"
            )
        exclusion_keys.add(key)
        _evidence_from_pinned_source(
            exclusion,
            evidence_key_field="candidate_evidence_key",
            evidence_id_field="candidate_evidence_id",
        )
    if exclusion_keys != expected_exclusions:
        raise VerifiedConstructionCoreError(
            "provenance current exclusion cohort differs"
        )
    base_workload_keys = {
        (row["project_stable_key"], row["evidence_id"], row["workload"])
        for row in base["workload_scope_bindings"]
    }
    base_role_keys = {
        (row["project_stable_key"], row["role"], row["party"])
        for row in [*base["role_bindings"], *base["excluded_source_roles"]]
    }
    if base_workload_keys & workload_keys or base_role_keys & (
        role_keys | exclusion_keys
    ):
        raise VerifiedConstructionCoreError("provenance inherited decision differs")
    return {
        "workload_scope_bindings": [
            *base["workload_scope_bindings"],
            *workload_bindings,
        ],
        "role_bindings": [*base["role_bindings"], *role_bindings],
        "excluded_source_roles": [
            *base["excluded_source_roles"],
            *exclusions,
        ],
        "evidence_rows": evidence_rows,
    }


def _provenance_contract() -> dict[str, Any]:
    reviewed = _load_json(PROVENANCE_DEFINITION)
    expected_fields = {
        "contract_id",
        "review_scope",
        "reviewed_as_of",
        "base_contract",
        "workload_scope_bindings",
        "role_bindings",
        "excluded_source_roles",
    }
    if not isinstance(reviewed, dict) or set(reviewed) != expected_fields:
        raise VerifiedConstructionCoreError("provenance contract fields differ")
    if (
        reviewed.get("contract_id") != "verified-construction-core-v0.6-provenance"
        or reviewed.get("reviewed_as_of") != REVIEW_DATE.isoformat()
        or reviewed.get("workload_scope_bindings") != []
        or reviewed.get("excluded_source_roles") != []
    ):
        raise VerifiedConstructionCoreError("provenance current contract differs")
    expected_base = {
        "path": "definitions/verified-construction-core-v0.5-provenance.json",
        "sha256": V05_PROVENANCE_DEFINITION_SHA256,
    }
    if reviewed.get("base_contract") != expected_base:
        raise VerifiedConstructionCoreError("provenance base pin differs")
    base_path = _repository_input(
        expected_base["path"], expected_base["sha256"], "provenance base"
    )
    base = _provenance_contract_v05(base_path)
    role_fields = {
        "project_stable_key",
        "source_input_path",
        "source_input_sha256",
        "role",
        "party",
        "evidence_key",
        "evidence_id",
        "relationship_scope",
        "semantic_scope",
    }
    bindings = reviewed.get("role_bindings")
    if not isinstance(bindings, list) or len(bindings) != 1:
        raise VerifiedConstructionCoreError("provenance current collections differ")
    binding = bindings[0]
    expected_key = (
        "curated:skygard-osl1-hovinbyen-campus:phase-2",
        "operator",
        "Skygard",
    )
    if (
        not isinstance(binding, dict)
        or set(binding) != role_fields
        or (
            binding.get("project_stable_key"),
            binding.get("role"),
            binding.get("party"),
        )
        != expected_key
        or binding.get("relationship_scope") != "intended"
        or not isinstance(binding.get("semantic_scope"), str)
        or not binding["semantic_scope"]
    ):
        raise VerifiedConstructionCoreError("provenance current role differs")
    source_path = _repository_input(
        binding["source_input_path"],
        binding["source_input_sha256"],
        "provenance current source",
    )
    source = _load_json(source_path)
    roles = source.get("project", {}).get("roles", {})
    if not isinstance(roles, dict) or binding["party"] not in roles.get(
        binding["role"], []
    ):
        raise VerifiedConstructionCoreError("provenance source role differs")
    evidence_row, _ = _evidence_from_pinned_source(
        binding,
        evidence_key_field="evidence_key",
        evidence_id_field="evidence_id",
    )
    evidence_rows = dict(base["evidence_rows"])
    existing = evidence_rows.get(evidence_row["evidence_id"])
    if existing is not None and existing != evidence_row:
        raise VerifiedConstructionCoreError("provenance evidence collision")
    evidence_rows[evidence_row["evidence_id"]] = evidence_row
    inherited_keys = {
        (row["project_stable_key"], row["role"], row["party"])
        for row in [*base["role_bindings"], *base["excluded_source_roles"]]
    }
    if expected_key in inherited_keys:
        raise VerifiedConstructionCoreError("provenance inherited decision differs")
    return {
        "workload_scope_bindings": base["workload_scope_bindings"],
        "role_bindings": [*base["role_bindings"], binding],
        "excluded_source_roles": base["excluded_source_roles"],
        "evidence_rows": evidence_rows,
    }


def _portable_source_inputs() -> list[dict[str, Any]]:
    _, delta = _reviewed_acceptances_from(REVIEW_DEFINITION)
    rows_by_path: dict[str, dict[str, Any]] = {}

    def add_input(
        path_text: str,
        expected_sha256: str,
        *,
        parents: Sequence[Mapping[str, str]] = (),
    ) -> None:
        path = _repository_input(path_text, expected_sha256, "portable source")
        deduplicated = {
            (item["path"], item["sha256"]): dict(item) for item in parents
        }
        for parent in deduplicated.values():
            _repository_input(parent["path"], parent["sha256"], "portable parent")
        row = {
            "path": path_text,
            "bytes": path.stat().st_size,
            "sha256": expected_sha256,
            "parent_manifests": sorted(
                deduplicated.values(), key=lambda item: item["path"]
            ),
        }
        existing = rows_by_path.get(path_text)
        if existing is not None and existing != row:
            raise VerifiedConstructionCoreError("portable source binding collision")
        rows_by_path[path_text] = row

    for acceptance in delta:
        add_input(
            acceptance["source_input_path"], acceptance["source_input_sha256"]
        )
    overlays = _reviewed_overlays(hydrated_crosscheck=False)
    for overlay in overlays["delta_overlays"]:
        bridge = overlays["bridges_by_overlay_id"][overlay["overlay_id"]]
        parents = [
            {
                "path": bridge["construction_source"]["release"]["manifest"]["path"],
                "sha256": bridge["construction_source"]["release"]["manifest"]["sha256"],
            }
        ]
        relationship_manifest = bridge["construction_source"]["project_to_campus"].get(
            "manifest"
        )
        if isinstance(relationship_manifest, dict):
            parents.append(
                {
                    "path": relationship_manifest["path"],
                    "sha256": relationship_manifest["sha256"],
                }
            )
        geometry_release = bridge.get("geometry_release")
        if isinstance(geometry_release, dict) and isinstance(
            geometry_release.get("manifest"), dict
        ):
            geometry_manifest = geometry_release["manifest"]
            parents.append(
                {
                    "path": geometry_manifest["path"],
                    "sha256": geometry_manifest["sha256"],
                }
            )
            if str(geometry_manifest["path"]).startswith("sources/"):
                add_input(geometry_manifest["path"], geometry_manifest["sha256"])
                for binding in geometry_release.get("members", {}).values():
                    add_input(
                        binding["path"],
                        binding["sha256"],
                        parents=(
                            {
                                "path": geometry_manifest["path"],
                                "sha256": geometry_manifest["sha256"],
                            },
                        ),
                    )
        elif isinstance(geometry_release, dict) and isinstance(
            geometry_release.get("capture"), dict
        ):
            capture = geometry_release["capture"]
            add_input(capture["path"], capture["sha256"])
        add_input(
            overlay["bridge_path"],
            overlay["bridge_sha256"],
            parents=parents,
        )
    return sorted(rows_by_path.values(), key=lambda row: row["path"])


def _validate_v06_inheritance(
    projects: Sequence[Mapping[str, str]],
    sites: Sequence[Mapping[str, str]],
    evidence: Sequence[Mapping[str, str]],
) -> None:
    validate_frozen_v05(LEGACY_PREVIEW_V05_DIR)
    base_projects = {
        row["project_stable_key"]: row
        for row in _load_csv(
            LEGACY_PREVIEW_V05_DIR / "projects.csv", PROJECT_FIELDS
        )
    }
    current = {row["project_stable_key"]: row for row in projects}
    if not set(base_projects) < set(current):
        raise VerifiedConstructionCoreError("preview v0.5 inheritance cohort differs")
    for key, base in base_projects.items():
        inherited = current[key]
        for field, value in base.items():
            if inherited.get(field) != value:
                raise VerifiedConstructionCoreError(
                    f"preview inherited project field differs: {field}"
                )

    base_sites = {
        row["site_id"]: row
        for row in _load_csv(
            LEGACY_PREVIEW_V05_DIR / "sites.csv", SITE_FIELDS
        )
    }
    current_sites = {row["site_id"]: row for row in sites}
    if not set(base_sites) < set(current_sites):
        raise VerifiedConstructionCoreError("preview v0.5 site inheritance differs")
    for site_id, base in base_sites.items():
        if any(current_sites[site_id].get(field) != value for field, value in base.items()):
            raise VerifiedConstructionCoreError(
                "preview inherited site fields differ"
            )

    base_evidence = {
        row["evidence_id"]: row
        for row in _load_csv(
            LEGACY_PREVIEW_V05_DIR / "evidence.csv", EVIDENCE_FIELDS
        )
    }
    current_evidence = {row["evidence_id"]: row for row in evidence}
    if not set(base_evidence) < set(current_evidence):
        raise VerifiedConstructionCoreError("preview v0.5 evidence inheritance differs")
    for evidence_id, base in base_evidence.items():
        inherited = current_evidence[evidence_id]
        if inherited != base:
            raise VerifiedConstructionCoreError(
                "preview inherited evidence content differs"
            )


def _validate_v06_delta_project(
    row: Mapping[str, str],
    acceptance: Mapping[str, Any],
    evidence_by_id: Mapping[str, Mapping[str, str]],
    expected_imagery_outcome: str,
    bridge: Mapping[str, Any],
    workload_binding_by_key: Mapping[
        tuple[str, str, str], Mapping[str, Any]
    ],
    role_bindings_by_project: Mapping[str, Sequence[Mapping[str, Any]]],
) -> None:
    source_path = _repository_input(
        acceptance["source_input_path"],
        acceptance["source_input_sha256"],
        "current reviewed-site",
    )
    source = _load_json(source_path)
    project = source.get("project")
    campus = source.get("campus")
    source_evidence = source.get("evidence")
    if not all(isinstance(value, dict) for value in (project, campus)) or not isinstance(
        source_evidence, list
    ):
        raise VerifiedConstructionCoreError("current reviewed-site source differs")
    if (
        project["stable_key"] != row["project_stable_key"]
        or campus["stable_key"] != row["physical_site_stable_key"]
        or project["name"] != row["name"]
        or project["country"] != row["country"]
    ):
        raise VerifiedConstructionCoreError("current reviewed-site identity differs")
    geometry_source = bridge["geometry_entity"]
    expected_geometry = geometry_source["geometry"]
    if (
        _parse_json_field(row["geometry_json"], "geometry") != expected_geometry
        or float(row["latitude"]) != float(geometry_source["latitude"])
        or float(row["longitude"]) != float(geometry_source["longitude"])
    ):
        raise VerifiedConstructionCoreError("current reviewed-site geometry differs")

    evidence_by_key = {
        item["key"]: item
        for item in source_evidence
        if isinstance(item, dict) and isinstance(item.get("key"), str)
    }

    def verify_evidence(key: str, evidence_id: str) -> None:
        pinned = evidence_by_key.get(key)
        if pinned is None or evidence_id != atlas_stable_id(
            "evidence", "curated-official", key, pinned.get("content_hash")
        ):
            raise VerifiedConstructionCoreError(
                "current reviewed-site evidence identity differs"
            )
        expected = {
            "evidence_id": evidence_id,
            "kind": pinned["kind"],
            "title": pinned["title"],
            "source_url": pinned["source_url"],
            "publisher": pinned["publisher"],
            "source_family": pinned["source_family"],
            "license": pinned["license"],
            "attribution": pinned["attribution"],
            "published_at": pinned.get("published_at") or "",
            "retrieved_at": pinned["retrieved_at"],
            "content_hash": pinned["content_hash"],
        }
        actual = evidence_by_id.get(evidence_id)
        if actual is None or any(actual.get(field) != value for field, value in expected.items()):
            raise VerifiedConstructionCoreError(
                "current reviewed-site evidence content differs"
            )

    bridge_evidence = _geometry_evidence_projection(bridge)
    if (
        row["geometry_evidence_id"] != bridge_evidence["evidence_id"]
        or row["geometry_source_url"] != bridge_evidence["source_url"]
    ):
        raise VerifiedConstructionCoreError("current reviewed-site geometry evidence differs")
    published_bridge_evidence = evidence_by_id.get(bridge_evidence["evidence_id"])
    if published_bridge_evidence is None or any(
        published_bridge_evidence.get(field) != value
        for field, value in bridge_evidence.items()
    ):
        raise VerifiedConstructionCoreError(
            "current reviewed-site bridge evidence content differs"
        )
    lifecycle = [
        item
        for item in source.get("lifecycle", [])
        if isinstance(item, dict) and item.get("entity") == "project"
    ]
    if not lifecycle:
        raise VerifiedConstructionCoreError("current reviewed-site lifecycle differs")
    latest = max(lifecycle, key=lambda item: item["as_of_date"])
    status_evidence = evidence_by_key.get(latest["evidence_key"])
    expected_status_id = atlas_stable_id(
        "evidence",
        "curated-official",
        latest["evidence_key"],
        status_evidence.get("content_hash") if status_evidence else None,
    )
    if (
        row["last_observed_physical_status"] != latest["value"]
        or row["status_as_of"] != latest["as_of_date"]
        or row["status_method"] != latest["method"]
        or row["status_evidence_id"] != expected_status_id
    ):
        raise VerifiedConstructionCoreError("current reviewed-site status differs")
    verify_evidence(latest["evidence_key"], expected_status_id)
    if row["status_source_url"] != evidence_by_key[latest["evidence_key"]]["source_url"]:
        raise VerifiedConstructionCoreError("current reviewed-site status URL differs")

    expected_capacity: list[dict[str, Any]] = []
    for observation in source.get("capacities", []):
        if not isinstance(observation, dict) or observation.get("entity") != "project":
            continue
        evidence_key = observation["evidence_key"]
        pinned = evidence_by_key[evidence_key]
        evidence_id = atlas_stable_id(
            "evidence", "curated-official", evidence_key, pinned["content_hash"]
        )
        verify_evidence(evidence_key, evidence_id)
        expected_capacity.append(
            {
                **{
                    field: (
                        float(value)
                        if field in {"low", "base", "high"} and value is not None
                        else value
                    )
                    for field, value in observation.items()
                    if field not in {"entity", "evidence_key"}
                },
                "evidence_id": evidence_id,
            }
        )
    actual_capacity = []
    for field in (
        "power_observations_json",
        "annual_energy_observations_json",
        "efficiency_observations_json",
    ):
        actual_capacity.extend(_parse_json_field(row[field], field))
    if actual_capacity != expected_capacity:
        raise VerifiedConstructionCoreError(
            "current reviewed-site typed metrics differ"
        )
    project_operating_models = [
        item
        for item in source.get("operating_models", [])
        if isinstance(item, dict) and item.get("entity") == "project"
    ]
    if project_operating_models:
        raise VerifiedConstructionCoreError(
            "current reviewed-site unsupported normalized claims differ"
        )
    expected_workloads: list[dict[str, Any]] = []
    source_workloads = source.get("workloads", [])
    if not isinstance(source_workloads, list) or any(
        not isinstance(item, dict) or item.get("entity") != "project"
        for item in source_workloads
    ):
        raise VerifiedConstructionCoreError(
            "current reviewed-site workload source differs"
        )
    for source_workload in source_workloads:
        evidence_key = source_workload.get("evidence_key")
        pinned = evidence_by_key.get(evidence_key)
        if pinned is None:
            raise VerifiedConstructionCoreError(
                "current reviewed-site workload evidence differs"
            )
        evidence_id = atlas_stable_id(
            "evidence", "curated-official", evidence_key, pinned["content_hash"]
        )
        verify_evidence(evidence_key, evidence_id)
        projected = {
            "as_of_date": source_workload["as_of_date"],
            "confidence": float(source_workload["confidence"]),
            "evidence_id": evidence_id,
            "method": source_workload["method"],
            "workload": source_workload["value"],
        }
        binding = workload_binding_by_key.get(
            (project["stable_key"], evidence_id, source_workload["value"])
        )
        if binding is None or any(
            binding.get(field) != value for field, value in projected.items()
        ):
            raise VerifiedConstructionCoreError(
                "current reviewed-site workload binding differs"
            )
        expected_workloads.append(
            {**projected, "deployment_scope": binding["deployment_scope"]}
        )
    expected_role_claims = sorted(
        [
            {
                "evidence_id": binding["evidence_id"],
                "party": binding["party"],
                "relationship_scope": binding["relationship_scope"],
                "role": binding["role"],
            }
            for binding in role_bindings_by_project.get(
                project["stable_key"], []
            )
        ],
        key=lambda claim: (claim["role"], claim["party"], claim["evidence_id"]),
    )
    role_projection: dict[str, list[str]] = {
        role: [] for role in ROLE_COLUMNS
    }
    for claim in expected_role_claims:
        role_projection[claim["role"]].append(claim["party"])

    country_iso_a2 = CURRENT_DELTA_COUNTRY_ISO_A2.get(project["country"])
    if country_iso_a2 is None:
        raise VerifiedConstructionCoreError(
            "current reviewed-site country code differs"
        )
    power = [item for item in expected_capacity if item["metric"] in POWER_METRICS]
    energy = [item for item in expected_capacity if item["metric"] in ENERGY_METRICS]
    efficiency = [
        item for item in expected_capacity if item["metric"] in EFFICIENCY_METRICS
    ]
    uncertainty = acceptance["horizontal_uncertainty_metres"]
    expected_row = {
        "project_id": atlas_stable_id(
            "entity", project["stable_key"], "project"
        ),
        "project_stable_key": project["stable_key"],
        "site_id": _stable_id("vcc-site", campus["stable_key"]),
        "physical_site_stable_key": campus["stable_key"],
        "name": project["name"],
        "country": project["country"],
        "country_iso_a2": country_iso_a2,
        "latitude": str(geometry_source["latitude"]),
        "longitude": str(geometry_source["longitude"]),
        "geometry_json": _json_bytes(expected_geometry).decode().strip(),
        "geometry_type": expected_geometry["type"],
        "geometry_source_entity_kind": acceptance["geometry_entity"],
        "geometry_derivation": acceptance["geometry_derivation"],
        "geometry_method": acceptance["geometry_method"],
        "geometry_scope_class": acceptance["geometry_scope_class"],
        "geometry_authority_class": bridge["review_decision"][
            "geometry_authority_class"
        ],
        "geometry_use_scope": bridge["review_decision"]["geometry_use_scope"],
        "geometry_precision_scope": acceptance["precision_scope"],
        "horizontal_uncertainty_metres": (
            "" if uncertainty is None else str(uncertainty)
        ),
        "horizontal_uncertainty_unknown_reason": bridge["review_decision"][
            "horizontal_uncertainty_unknown_reason"
        ],
        "geometry_evidence_id": row["geometry_evidence_id"],
        "last_observed_physical_status": latest["value"],
        "status_as_of": latest["as_of_date"],
        "status_age_days_at_review": str(
            (REVIEW_DATE - _calendar_date(latest["as_of_date"], "status_as_of")).days
        ),
        "status_method": latest["method"],
        "status_evidence_id": expected_status_id,
        "verification_posture": _verification_posture(
            bridge["review_decision"]["geometry_authority_class"],
            bridge["review_decision"]["geometry_use_scope"],
        ),
        "independent_imagery_verification": "false",
        "imagery_review_outcome": expected_imagery_outcome,
        "development_type": "unknown",
        "development_type_unknown_reason": (
            "source evidence does not distinguish greenfield, expansion, or retrofit"
        ),
        "operating_model": "unknown",
        "operating_model_unknown_reason": "not established by selected evidence",
        "operating_model_evidence_id": "",
        "workloads_json": _json_bytes(expected_workloads).decode().strip(),
        "workload_unknown_reason": (
            "" if expected_workloads else "not established by selected evidence"
        ),
        "role_claims_json": _json_bytes(expected_role_claims).decode().strip(),
        "power_observations_json": _json_bytes(power).decode().strip(),
        "power_unknown_reason": (
            "" if power else "no typed project power observation; not estimated"
        ),
        "annual_energy_observations_json": _json_bytes(energy).decode().strip(),
        "annual_energy_unknown_reason": (
            "" if energy else "no scoped annual-energy inputs; not estimated"
        ),
        "efficiency_observations_json": _json_bytes(efficiency).decode().strip(),
        "efficiency_unknown_reason": (
            "" if efficiency else "no scoped PUE or WUE observation"
        ),
        "owner": "; ".join(sorted(role_projection["owner"])),
        "operator": "; ".join(sorted(role_projection["operator"])),
        "users": "; ".join(sorted(role_projection["user"])),
        "tenants": "; ".join(sorted(role_projection["tenant"])),
        "customers": "; ".join(sorted(role_projection["customer"])),
        "status_source_url": evidence_by_key[latest["evidence_key"]]["source_url"],
        "geometry_source_url": bridge_evidence["source_url"],
    }
    if dict(row) != expected_row:
        differing = sorted(
            field for field in PROJECT_FIELDS if row.get(field) != expected_row[field]
        )
        raise VerifiedConstructionCoreError(
            f"current reviewed-site project projection differs: {differing}"
        )


def _imagery_contract_v03(
    path: Path,
) -> tuple[str, list[dict[str, Any]]]:
    reviewed = _load_json(path)
    if not isinstance(reviewed, dict) or set(reviewed) != {
        "contract_id",
        "review_scope",
        "reviewed_as_of",
        "base_contract",
        "default_outcome",
        "records",
    }:
        raise VerifiedConstructionCoreError("imagery-review contract fields differ")
    if reviewed.get("contract_id") != "verified-construction-core-v0.3-imagery-reviews":
        raise VerifiedConstructionCoreError("imagery-review contract id differs")
    if reviewed.get("reviewed_as_of") != REVIEW_DATE.isoformat():
        raise VerifiedConstructionCoreError("imagery-review contract date differs")
    base = reviewed.get("base_contract")
    if not isinstance(base, dict) or set(base) != {"path", "sha256"}:
        raise VerifiedConstructionCoreError("imagery-review base contract differs")
    if base != {
        "path": "definitions/verified-construction-core-v0.2-imagery-reviews.json",
        "sha256": "0516a3281d0a3a1734a4bacb8f8d9e5419d0ae8dc22df36c098c5375f84ee613",
    }:
        raise VerifiedConstructionCoreError("imagery-review base pin differs")
    base_path = _repository_input(base["path"], base["sha256"], "imagery-review base")
    base_reviewed = _load_json(base_path)
    if not isinstance(base_reviewed, dict) or set(base_reviewed) != {
        "contract_id",
        "review_scope",
        "reviewed_as_of",
        "default_outcome",
        "records",
    }:
        raise VerifiedConstructionCoreError("imagery-review base fields differ")
    if (
        base_reviewed.get("contract_id")
        != "verified-construction-core-v0.2-imagery-reviews"
        or base_reviewed.get("default_outcome") != reviewed.get("default_outcome")
    ):
        raise VerifiedConstructionCoreError("imagery-review base identity differs")
    default_outcome = reviewed.get("default_outcome")
    if default_outcome != "not_reviewed_for_core_preview":
        raise VerifiedConstructionCoreError("imagery-review default differs")
    base_records = base_reviewed.get("records")
    delta_records = reviewed.get("records")
    if (
        not isinstance(base_records, list)
        or not base_records
        or not isinstance(delta_records, list)
        or not delta_records
    ):
        raise VerifiedConstructionCoreError("imagery-review records are empty")
    records = [*base_records, *delta_records]
    expected_delta_outcomes = {
        "curated:kao-data-harlow-campus:klon-03-building": (
            "tracked_identity_bound_uncertain_with_unadjudicated_later_clear_"
            "conflict_no_construction_claim"
        ),
    }
    required = {
        "project_stable_key",
        "project_entity_id",
        "outcome",
        "portable_identity_binding",
        "identity_binding_basis",
        "primary_review_source_path",
        "primary_review_source_sha256",
        "primary_review_id",
        "primary_blind_id",
        "primary_queue_id",
        "source_comparison_sha256",
        "primary_verdict",
        "local_identity_lineage",
        "later_review_conflict",
        "no_claim_guardrail",
    }
    keys: set[str] = set()
    delta_keys: set[str] = set()
    for index, record in enumerate(records):
        if not isinstance(record, dict) or set(record) != required:
            raise VerifiedConstructionCoreError(
                f"imagery-review record {index} has unexpected fields"
            )
        key = record["project_stable_key"]
        if not isinstance(key, str) or not key or key in keys:
            raise VerifiedConstructionCoreError("imagery-review project key differs")
        keys.add(key)
        if index >= len(base_records):
            delta_keys.add(key)
            if record["outcome"] != expected_delta_outcomes.get(key):
                raise VerifiedConstructionCoreError(
                    "imagery-review delta outcome differs"
                )
        try:
            if str(UUID(record["project_entity_id"])) != record["project_entity_id"]:
                raise ValueError
        except (ValueError, AttributeError) as error:
            raise VerifiedConstructionCoreError(
                "imagery-review project entity id differs"
            ) from error
        if record["no_claim_guardrail"] is not True or not isinstance(
            record["portable_identity_binding"], bool
        ):
            raise VerifiedConstructionCoreError("imagery-review guardrail differs")
        for field in (
            "outcome",
            "identity_binding_basis",
            "primary_review_id",
            "primary_blind_id",
            "primary_queue_id",
            "source_comparison_sha256",
        ):
            if not isinstance(record[field], str) or not record[field]:
                raise VerifiedConstructionCoreError(
                    f"imagery-review {field} differs"
                )
        relative_path = Path(record["primary_review_source_path"])
        if relative_path.is_absolute() or ".." in relative_path.parts:
            raise VerifiedConstructionCoreError("imagery-review source path escapes")
        source_path = ROOT / relative_path
        if not source_path.is_file() or _sha256_file(source_path) != record[
            "primary_review_source_sha256"
        ]:
            raise VerifiedConstructionCoreError("imagery-review source hash differs")
        source_review = _load_json(source_path)
        if source_review.get("review_id") != record["primary_review_id"]:
            raise VerifiedConstructionCoreError("imagery-review source id differs")
        blind_id = record["primary_blind_id"]
        decisions = source_review.get("decisions")
        lineage = source_review.get("lineage_records")
        if isinstance(decisions, list):
            matches = [row for row in decisions if row.get("blind_id") == blind_id]
            if len(matches) != 1 or any(
                matches[0].get(field) != value
                for field, value in record["primary_verdict"].items()
            ):
                raise VerifiedConstructionCoreError("imagery-review verdict differs")
        elif isinstance(lineage, list):
            matches = [row for row in lineage if row.get("blind_id") == blind_id]
            if len(matches) != 1:
                raise VerifiedConstructionCoreError("imagery-review lineage differs")
            match = matches[0]
            members = match.get("members")
            if not isinstance(members, list) or len(members) != 1:
                raise VerifiedConstructionCoreError("imagery-review member differs")
            member = members[0]
            semantics = source_review.get("decision_semantics", {}).get(
                match.get("visual_verdict")
            )
            expected_verdict = {
                "semantics": semantics,
                "visual_verdict": match.get("visual_verdict"),
            }
            if (
                member.get("stable_key") != key
                or member.get("entity_id") != record["project_entity_id"]
                or member.get("queue_id") != record["primary_queue_id"]
                or match.get("source_comparison_sha256")
                != record["source_comparison_sha256"]
                or expected_verdict != record["primary_verdict"]
            ):
                raise VerifiedConstructionCoreError(
                    "imagery-review identity-bound lineage differs"
                )
        else:
            raise VerifiedConstructionCoreError("imagery-review source format differs")
        local_lineage = record["local_identity_lineage"]
        if record["portable_identity_binding"]:
            if local_lineage is not None:
                raise VerifiedConstructionCoreError(
                    "portable imagery-review identity has local-only lineage"
                )
        else:
            if not isinstance(local_lineage, dict) or set(local_lineage) != {
                "analyst_review_path",
                "analyst_review_sha256",
                "queue_path",
                "queue_sha256",
            }:
                raise VerifiedConstructionCoreError(
                    "local imagery-review identity lineage differs"
                )
            analyst_path = ROOT / local_lineage["analyst_review_path"]
            queue_path = ROOT / local_lineage["queue_path"]
            if analyst_path.exists() != queue_path.exists():
                raise VerifiedConstructionCoreError(
                    "local imagery-review identity lineage is partially hydrated"
                )
            if analyst_path.exists():
                if (
                    _sha256_file(analyst_path)
                    != local_lineage["analyst_review_sha256"]
                    or _sha256_file(queue_path) != local_lineage["queue_sha256"]
                ):
                    raise VerifiedConstructionCoreError(
                        "local imagery-review identity lineage hash differs"
                    )
                analyst_matches = [
                    row
                    for row in _load_jsonl(analyst_path)
                    if row.get("blind_id") == blind_id
                ]
                queue_matches = [
                    row
                    for row in _load_jsonl(queue_path)
                    if row.get("queue_id") == record["primary_queue_id"]
                ]
                if len(analyst_matches) != 1 or len(queue_matches) != 1:
                    raise VerifiedConstructionCoreError(
                        "local imagery-review identity lineage is not unique"
                    )
                queue_entity = queue_matches[0].get("entity", {})
                if (
                    analyst_matches[0].get("queue_id")
                    != record["primary_queue_id"]
                    or analyst_matches[0]
                    .get("input_artifacts", {})
                    .get("comparison.png", {})
                    .get("sha256")
                    != record["source_comparison_sha256"]
                    or queue_entity.get("stable_key") != key
                    or queue_entity.get("id") != record["project_entity_id"]
                ):
                    raise VerifiedConstructionCoreError(
                        "local imagery-review identity lineage differs"
                    )
        conflict = record["later_review_conflict"]
        if conflict is not None:
            if not isinstance(conflict, dict) or set(conflict) != {
                "blind_id",
                "identity_binding_status",
                "local_analyst_review_path",
                "local_analyst_review_sha256",
                "local_queue_path",
                "local_queue_sha256",
                "queue_id",
                "review_source_path",
                "review_source_sha256",
                "source_comparison_sha256",
                "supersedes_primary",
                "verdict",
            }:
                raise VerifiedConstructionCoreError("imagery-review conflict differs")
            if conflict["supersedes_primary"] is not False:
                raise VerifiedConstructionCoreError(
                    "imagery-review conflict cannot supersede without adjudication"
                )
            conflict_path = ROOT / conflict["review_source_path"]
            if not conflict_path.is_file() or _sha256_file(conflict_path) != conflict[
                "review_source_sha256"
            ]:
                raise VerifiedConstructionCoreError("imagery-review conflict hash differs")
            conflict_source = _load_json(conflict_path)
            conflict_matches = [
                row
                for row in conflict_source.get("decisions", [])
                if row.get("blind_id") == conflict["blind_id"]
            ]
            if len(conflict_matches) != 1 or any(
                conflict_matches[0].get(field) != value
                for field, value in conflict["verdict"].items()
            ):
                raise VerifiedConstructionCoreError(
                    "imagery-review conflict verdict differs"
                )
            conflict_analyst_path = ROOT / conflict["local_analyst_review_path"]
            conflict_queue_path = ROOT / conflict["local_queue_path"]
            if conflict_analyst_path.exists() != conflict_queue_path.exists():
                raise VerifiedConstructionCoreError(
                    "imagery-review conflict lineage is partially hydrated"
                )
            if conflict_analyst_path.exists():
                if (
                    _sha256_file(conflict_analyst_path)
                    != conflict["local_analyst_review_sha256"]
                    or _sha256_file(conflict_queue_path)
                    != conflict["local_queue_sha256"]
                ):
                    raise VerifiedConstructionCoreError(
                        "imagery-review conflict lineage hash differs"
                    )
                conflict_analyst_matches = [
                    row
                    for row in _load_jsonl(conflict_analyst_path)
                    if row.get("blind_id") == conflict["blind_id"]
                ]
                conflict_queue_matches = [
                    row
                    for row in _load_jsonl(conflict_queue_path)
                    if row.get("queue_id") == conflict["queue_id"]
                ]
                if (
                    len(conflict_analyst_matches) != 1
                    or len(conflict_queue_matches) != 1
                ):
                    raise VerifiedConstructionCoreError(
                        "imagery-review conflict lineage is not unique"
                    )
                conflict_analyst = conflict_analyst_matches[0]
                conflict_entity = conflict_queue_matches[0].get("entity", {})
                if (
                    conflict_analyst.get("queue_id") != conflict["queue_id"]
                    or conflict_analyst.get("source_entity_lineage", {}).get("id")
                    != record["project_entity_id"]
                    or conflict_analyst
                    .get("input_artifacts", {})
                    .get("comparison.png", {})
                    .get("sha256")
                    != conflict["source_comparison_sha256"]
                    or conflict["source_comparison_sha256"]
                    != record["source_comparison_sha256"]
                    or conflict_entity.get("stable_key") != key
                    or conflict_entity.get("id") != record["project_entity_id"]
                ):
                    raise VerifiedConstructionCoreError(
                        "imagery-review conflict identity lineage differs"
                    )
    if delta_keys != set(expected_delta_outcomes):
        raise VerifiedConstructionCoreError("imagery-review delta cohort differs")
    return default_outcome, records


def _imagery_contract_v04(path: Path) -> tuple[str, list[dict[str, Any]]]:
    reviewed = _load_json(path)
    if not isinstance(reviewed, dict) or set(reviewed) != {
        "contract_id",
        "review_scope",
        "reviewed_as_of",
        "base_contract",
        "default_outcome",
        "records",
    }:
        raise VerifiedConstructionCoreError("imagery-review contract fields differ")
    if (
        reviewed.get("contract_id")
        != "verified-construction-core-v0.4-imagery-reviews"
        or reviewed.get("reviewed_as_of") != REVIEW_DATE.isoformat()
        or reviewed.get("default_outcome") != "not_reviewed_for_core_preview"
        or reviewed.get("records") != []
    ):
        raise VerifiedConstructionCoreError("imagery-review current contract differs")
    base = reviewed.get("base_contract")
    expected_base = {
        "path": "definitions/verified-construction-core-v0.3-imagery-reviews.json",
        "sha256": V03_IMAGERY_REVIEW_DEFINITION_SHA256,
    }
    if base != expected_base:
        raise VerifiedConstructionCoreError("imagery-review base pin differs")
    base_path = _repository_input(
        expected_base["path"], expected_base["sha256"], "imagery-review base"
    )
    default_outcome, records = _imagery_contract_v03(base_path)
    if default_outcome != reviewed["default_outcome"]:
        raise VerifiedConstructionCoreError("imagery-review default differs")
    return default_outcome, records


def _imagery_contract_v05(path: Path) -> tuple[str, list[dict[str, Any]]]:
    reviewed = _load_json(path)
    expected_fields = {
        "contract_id",
        "review_scope",
        "reviewed_as_of",
        "base_contract",
        "default_outcome",
        "records",
    }
    if not isinstance(reviewed, dict) or set(reviewed) != expected_fields:
        raise VerifiedConstructionCoreError("imagery-review contract fields differ")
    if (
        reviewed.get("contract_id")
        != "verified-construction-core-v0.5-imagery-reviews"
        or reviewed.get("reviewed_as_of") != REVIEW_DATE.isoformat()
        or reviewed.get("default_outcome") != "not_reviewed_for_core_preview"
    ):
        raise VerifiedConstructionCoreError("imagery-review current contract differs")
    expected_base = {
        "path": "definitions/verified-construction-core-v0.4-imagery-reviews.json",
        "sha256": V04_IMAGERY_REVIEW_DEFINITION_SHA256,
    }
    if reviewed.get("base_contract") != expected_base:
        raise VerifiedConstructionCoreError("imagery-review base pin differs")
    base_path = _repository_input(
        expected_base["path"], expected_base["sha256"], "imagery-review base"
    )
    default_outcome, inherited = _imagery_contract_v04(base_path)
    delta = reviewed.get("records")
    if not isinstance(delta, list) or len(delta) != 3:
        raise VerifiedConstructionCoreError("imagery-review current cohort differs")
    required = {
        "project_stable_key",
        "project_entity_id",
        "outcome",
        "portable_identity_binding",
        "identity_binding_basis",
        "primary_review_source_path",
        "primary_review_source_sha256",
        "primary_review_id",
        "primary_blind_id",
        "primary_queue_id",
        "source_comparison_sha256",
        "primary_verdict",
        "local_identity_lineage",
        "later_review_conflict",
        "no_claim_guardrail",
    }
    expected_outcomes = {
        "curated:related-openai-oracle-stargate-michigan-saline:current-build": (
            "tracked_identity_bound_visible_change_followup_only_no_construction_claim"
        ),
        "curated:microsoft-mount-pleasant-datacenter-campus:second-facility": (
            "tracked_identity_bound_uncertain_unsuperseded_no_construction_claim"
        ),
        "curated:amazon-salem-township-innovation-campus:active-buildout": (
            "tracked_identity_bound_uncertain_unsuperseded_no_construction_claim"
        ),
    }
    inherited_keys = {row["project_stable_key"] for row in inherited}
    delta_keys: set[str] = set()
    for index, record in enumerate(delta):
        if not isinstance(record, dict) or set(record) != required:
            raise VerifiedConstructionCoreError(
                f"imagery-review current record {index} differs"
            )
        key = record["project_stable_key"]
        if (
            key in inherited_keys
            or key in delta_keys
            or record["outcome"] != expected_outcomes.get(key)
            or record["portable_identity_binding"] is not True
            or record["local_identity_lineage"] is not None
            or record["later_review_conflict"] is not None
            or record["no_claim_guardrail"] is not True
            or not isinstance(record["identity_binding_basis"], str)
            or not record["identity_binding_basis"]
            or record["project_entity_id"]
            != atlas_stable_id("entity", key, "project")
        ):
            raise VerifiedConstructionCoreError(
                "imagery-review current identity differs"
            )
        delta_keys.add(key)
        source_path = _repository_input(
            record["primary_review_source_path"],
            record["primary_review_source_sha256"],
            "imagery-review current source",
        )
        source = _load_json(source_path)
        if source.get("review_id") != record["primary_review_id"]:
            raise VerifiedConstructionCoreError(
                "imagery-review current source id differs"
            )
        matches = [
            row
            for row in source.get("lineage_records", [])
            if isinstance(row, dict)
            and row.get("blind_id") == record["primary_blind_id"]
        ]
        if len(matches) != 1:
            raise VerifiedConstructionCoreError(
                "imagery-review current lineage differs"
            )
        match = matches[0]
        members = match.get("members")
        if not isinstance(members, list) or len(members) != 1:
            raise VerifiedConstructionCoreError(
                "imagery-review current member differs"
            )
        member = members[0]
        expected_verdict = {
            "semantics": source.get("decision_semantics", {}).get(
                match.get("visual_verdict")
            ),
            "visual_verdict": match.get("visual_verdict"),
        }
        if (
            member.get("stable_key") != key
            or member.get("entity_id") != record["project_entity_id"]
            or member.get("queue_id") != record["primary_queue_id"]
            or match.get("source_comparison_sha256")
            != record["source_comparison_sha256"]
            or expected_verdict != record["primary_verdict"]
        ):
            raise VerifiedConstructionCoreError(
                "imagery-review current identity-bound lineage differs"
            )
    if delta_keys != set(expected_outcomes):
        raise VerifiedConstructionCoreError("imagery-review current cohort differs")
    if default_outcome != reviewed["default_outcome"]:
        raise VerifiedConstructionCoreError("imagery-review default differs")
    return default_outcome, [*inherited, *delta]


def _imagery_contract() -> tuple[str, list[dict[str, Any]]]:
    reviewed = _load_json(IMAGERY_REVIEW_DEFINITION)
    expected_fields = {
        "contract_id",
        "review_scope",
        "reviewed_as_of",
        "base_contract",
        "default_outcome",
        "records",
    }
    if not isinstance(reviewed, dict) or set(reviewed) != expected_fields:
        raise VerifiedConstructionCoreError("imagery-review contract fields differ")
    if (
        reviewed.get("contract_id")
        != "verified-construction-core-v0.6-imagery-reviews"
        or reviewed.get("reviewed_as_of") != REVIEW_DATE.isoformat()
        or reviewed.get("default_outcome") != "not_reviewed_for_core_preview"
    ):
        raise VerifiedConstructionCoreError("imagery-review current contract differs")
    expected_base = {
        "path": "definitions/verified-construction-core-v0.5-imagery-reviews.json",
        "sha256": V05_IMAGERY_REVIEW_DEFINITION_SHA256,
    }
    if reviewed.get("base_contract") != expected_base:
        raise VerifiedConstructionCoreError("imagery-review base pin differs")
    base_path = _repository_input(
        expected_base["path"], expected_base["sha256"], "imagery-review base"
    )
    default_outcome, inherited = _imagery_contract_v05(base_path)
    records = reviewed.get("records")
    fields = {
        "project_stable_key",
        "project_entity_id",
        "review_type",
        "outcome",
        "portable_identity_binding",
        "identity_binding_basis",
        "source_path",
        "source_sha256",
        "source_role",
        "publisher",
        "publisher_source_url",
        "publisher_media_redistributed",
        "independent_imagery_verification",
        "used_for_geometry",
        "used_for_status",
        "used_for_capacity",
        "used_for_progress",
        "used_for_building_count",
        "semantic_scope",
        "no_claim_guardrail",
    }
    project_key = (
        "curated:green-mountain-undheim-campus:current-two-building-development"
    )
    if not isinstance(records, list) or len(records) != 1:
        raise VerifiedConstructionCoreError("imagery-review current cohort differs")
    record = records[0]
    if (
        not isinstance(record, dict)
        or set(record) != fields
        or record.get("project_stable_key") != project_key
        or record.get("project_entity_id")
        != atlas_stable_id("entity", project_key, "project")
        or record.get("review_type") != "publisher_contractor_drone_context"
        or record.get("outcome")
        != "first_party_contractor_drone_imagery_present_not_independently_verified"
        or record.get("portable_identity_binding") is not True
        or record.get("source_role")
        != "portable_geometry_bridge_with_publisher_imagery_context"
        or record.get("publisher") != "Backe"
        or record.get("publisher_media_redistributed") is not False
        or record.get("independent_imagery_verification") is not False
        or any(
            record.get(field) is not False
            for field in (
                "used_for_geometry",
                "used_for_status",
                "used_for_capacity",
                "used_for_progress",
                "used_for_building_count",
            )
        )
        or record.get("no_claim_guardrail") is not True
        or any(
            not isinstance(record.get(field), str) or not record[field]
            for field in (
                "identity_binding_basis",
                "publisher_source_url",
                "semantic_scope",
            )
        )
    ):
        raise VerifiedConstructionCoreError("imagery-review current record differs")
    source_path = _repository_input(
        record["source_path"], record["source_sha256"], "imagery-review source"
    )
    bridge = _load_json(source_path)
    imagery = bridge.get("imagery_posture") if isinstance(bridge, dict) else None
    if (
        bridge.get("construction_source", {}).get("project", {}).get("stable_key")
        != project_key
        or not isinstance(imagery, dict)
        or imagery.get("outcome") != record["outcome"]
        or imagery.get("independent_imagery_verification") is not False
        or imagery.get("publisher_imagery", {}).get("publisher") != record["publisher"]
        or imagery.get("publisher_imagery", {}).get("media_redistributed") is not False
        or imagery.get("publisher_imagery", {}).get("source_page")
        != record["publisher_source_url"]
    ):
        raise VerifiedConstructionCoreError("imagery-review bridge binding differs")
    inherited_keys = {row["project_stable_key"] for row in inherited}
    if project_key in inherited_keys or default_outcome != reviewed["default_outcome"]:
        raise VerifiedConstructionCoreError("imagery-review inheritance differs")
    return default_outcome, [*inherited, record]


def _manifest_binding(
    binding: Mapping[str, Any], *, label: str
) -> tuple[Path, dict[str, Any]]:
    if set(binding) != {"path", "bytes", "sha256"}:
        raise VerifiedConstructionCoreError(f"{label} manifest binding differs")
    path = _repository_input(str(binding["path"]), str(binding["sha256"]), label)
    if path.stat().st_size != binding["bytes"]:
        raise VerifiedConstructionCoreError(f"{label} manifest byte count differs")
    manifest = _load_json(path)
    if not isinstance(manifest, dict) or not isinstance(manifest.get("files"), dict):
        raise VerifiedConstructionCoreError(f"{label} manifest differs")
    return path, manifest


def _validate_manifest_members(
    manifest: Mapping[str, Any], members: Mapping[str, Any], *, label: str
) -> None:
    if not isinstance(members, dict) or not members:
        raise VerifiedConstructionCoreError(f"{label} members differ")
    manifest_files = manifest.get("files")
    if not isinstance(manifest_files, dict):
        raise VerifiedConstructionCoreError(f"{label} files differ")
    for name, binding in members.items():
        if not isinstance(binding, dict) or set(binding) != {
            "path",
            "bytes",
            "sha256",
        }:
            raise VerifiedConstructionCoreError(f"{label} member binding differs")
        relative = Path(str(binding["path"]))
        if (
            relative.is_absolute()
            or ".." in relative.parts
            or relative.name != name
            or manifest_files.get(name)
            != {"bytes": binding["bytes"], "sha256": binding["sha256"]}
        ):
            raise VerifiedConstructionCoreError(f"{label} member metadata differs")


def _validate_polygon(geometry: Any, *, label: str) -> None:
    if not isinstance(geometry, dict) or set(geometry) != {"type", "coordinates"}:
        raise VerifiedConstructionCoreError(f"{label} geometry differs")
    if geometry.get("type") != "Polygon":
        raise VerifiedConstructionCoreError(f"{label} geometry type differs")
    coordinates = geometry.get("coordinates")
    if (
        not isinstance(coordinates, list)
        or len(coordinates) != 1
        or not isinstance(coordinates[0], list)
        or len(coordinates[0]) < 4
        or coordinates[0][0] != coordinates[0][-1]
    ):
        raise VerifiedConstructionCoreError(f"{label} polygon ring differs")
    for point in coordinates[0]:
        if (
            not isinstance(point, list)
            or len(point) != 2
            or any(
                isinstance(value, bool)
                or not isinstance(value, (int, float))
                or not math.isfinite(value)
                for value in point
            )
            or not -180 <= point[0] <= 180
            or not -90 <= point[1] <= 90
        ):
            raise VerifiedConstructionCoreError(f"{label} polygon coordinate differs")


def _parent_row_binding(
    source_row: Any, *, release_id: Any, member: str, **identity: Any
) -> dict[str, Any]:
    if not isinstance(source_row, dict) or set(source_row) != {
        "line",
        "bytes",
        "sha256",
        "raw_csv_record_base64",
    }:
        raise VerifiedConstructionCoreError(
            "geometry bridge parent source-row binding differs"
        )
    return {
        "release_id": release_id,
        "member": member,
        **identity,
        "line": source_row["line"],
        "bytes": source_row["bytes"],
        "sha256": source_row["sha256"],
    }


def _validate_bridge_parent_row_bindings(
    bridge: Mapping[str, Any], project_stable_key: str
) -> None:
    expected = BRIDGE_PARENT_ROW_BINDINGS.get(project_stable_key)
    construction = bridge["construction_source"]
    topology = construction["project_to_campus"]
    geometry_release = bridge["geometry_release"]
    entity = bridge["geometry_entity"]
    evidence = bridge["geometry_evidence"]
    relationship = topology.get("relationship")
    subject = topology.get("subject_member")
    object_member = topology.get("object_member")
    if (
        expected is None
        or not isinstance(geometry_release, dict)
        or not isinstance(entity, dict)
        or not isinstance(evidence, dict)
        or not isinstance(relationship, dict)
        or not isinstance(subject, dict)
        or not isinstance(object_member, dict)
    ):
        raise VerifiedConstructionCoreError(
            "geometry bridge parent source-row binding differs"
        )
    actual = {
        "geometry_entity": _parent_row_binding(
            entity.get("source_row"),
            release_id=geometry_release.get("release_id"),
            member="entities.csv",
            stable_key=entity.get("stable_key"),
        ),
        "geometry_evidence": _parent_row_binding(
            evidence.get("source_row"),
            release_id=geometry_release.get("release_id"),
            member="evidence.csv",
            evidence_id=evidence.get("evidence_id"),
        ),
        "relationship": _parent_row_binding(
            relationship.get("source_row"),
            release_id=topology.get("bundle_id"),
            member="relationships.csv",
            relationship_id=relationship.get("relationship_id"),
        ),
        "subject_member": _parent_row_binding(
            subject.get("source_row"),
            release_id=topology.get("bundle_id"),
            member="component-members.csv",
            stable_key=subject.get("stable_key"),
        ),
        "object_member": _parent_row_binding(
            object_member.get("source_row"),
            release_id=topology.get("bundle_id"),
            member="component-members.csv",
            stable_key=object_member.get("stable_key"),
        ),
    }
    if actual != expected:
        raise VerifiedConstructionCoreError(
            "geometry bridge parent source-row binding differs"
        )


def _embedded_csv_source_row(
    binding: Any, expected_fields: Sequence[str], *, label: str
) -> dict[str, str]:
    if not isinstance(binding, dict) or set(binding) != {
        "line",
        "bytes",
        "sha256",
        "raw_csv_record_base64",
    }:
        raise VerifiedConstructionCoreError(f"{label} source-row binding differs")
    line = binding["line"]
    encoded_record = binding["raw_csv_record_base64"]
    if (
        isinstance(line, bool)
        or not isinstance(line, int)
        or line < 2
        or not isinstance(encoded_record, str)
        or not encoded_record
    ):
        raise VerifiedConstructionCoreError(f"{label} source-row payload differs")
    try:
        payload = base64.b64decode(encoded_record, validate=True)
        if base64.b64encode(payload).decode("ascii") != encoded_record:
            raise ValueError("non-canonical base64")
        raw_record = payload.decode("utf-8")
    except (binascii.Error, UnicodeDecodeError, ValueError) as error:
        raise VerifiedConstructionCoreError(
            f"{label} source-row payload differs"
        ) from error
    if not raw_record.endswith("\n") or "\r" in raw_record:
        raise VerifiedConstructionCoreError(f"{label} source-row payload differs")
    if (
        isinstance(binding["bytes"], bool)
        or not isinstance(binding["bytes"], int)
        or len(payload) != binding["bytes"]
        or not isinstance(binding["sha256"], str)
        or _sha256_bytes(payload) != binding["sha256"]
    ):
        raise VerifiedConstructionCoreError(f"{label} source-row hash differs")
    try:
        rows = list(csv.reader(io.StringIO(raw_record, newline="")))
    except csv.Error as error:
        raise VerifiedConstructionCoreError(
            f"{label} source-row CSV differs"
        ) from error
    if len(rows) != 1 or len(rows[0]) != len(expected_fields):
        raise VerifiedConstructionCoreError(f"{label} source-row CSV differs")
    canonical = io.StringIO(newline="")
    csv.writer(canonical, lineterminator="\n").writerow(rows[0])
    if canonical.getvalue() != raw_record:
        raise VerifiedConstructionCoreError(f"{label} source-row encoding differs")
    return dict(zip(expected_fields, rows[0], strict=True))


def _validate_hydrated_source_row(
    path: Path,
    binding: Mapping[str, Any],
    expected_fields: Sequence[str],
    *,
    label: str,
) -> dict[str, str]:
    row = _embedded_csv_source_row(binding, expected_fields, label=label)
    lines = path.read_bytes().splitlines(keepends=True)
    if not lines or lines[0] != _csv_bytes([], expected_fields):
        raise VerifiedConstructionCoreError(f"{label} source header differs")
    line = binding["line"]
    if not 1 <= line <= len(lines):
        raise VerifiedConstructionCoreError(f"{label} source-row line differs")
    payload = lines[line - 1]
    if payload != base64.b64decode(binding["raw_csv_record_base64"], validate=True):
        raise VerifiedConstructionCoreError(f"{label} source-row membership differs")
    return row


def _nullable_csv_value(value: str) -> str | None:
    return value if value else None


def _csv_float(value: str, *, label: str) -> float | None:
    if not value:
        return None
    try:
        parsed = float(value)
    except ValueError as error:
        raise VerifiedConstructionCoreError(f"{label} numeric field differs") from error
    if not math.isfinite(parsed):
        raise VerifiedConstructionCoreError(f"{label} numeric field differs")
    return parsed


def _csv_integer(value: str, *, label: str) -> int:
    try:
        parsed = int(value)
    except ValueError as error:
        raise VerifiedConstructionCoreError(f"{label} integer field differs") from error
    if str(parsed) != value:
        raise VerifiedConstructionCoreError(f"{label} integer field differs")
    return parsed


def _csv_json(value: str, *, label: str) -> Any:
    def object_without_duplicates(
        pairs: list[tuple[str, Any]],
    ) -> dict[str, Any]:
        result: dict[str, Any] = {}
        for key, item in pairs:
            if key in result:
                raise ValueError("duplicate JSON key")
            result[key] = item
        return result

    def reject_nonfinite(constant: str) -> None:
        raise ValueError(f"non-finite JSON number: {constant}")

    try:
        return json.loads(
            value,
            object_pairs_hook=object_without_duplicates,
            parse_constant=reject_nonfinite,
        )
    except (json.JSONDecodeError, ValueError) as error:
        raise VerifiedConstructionCoreError(f"{label} JSON field differs") from error


def _global_geometry_entity_projection(row: Mapping[str, str]) -> dict[str, Any]:
    float_fields = {
        "latitude",
        "longitude",
        "country_assignment_confidence",
        "status_confidence",
        "operating_model_confidence",
        "snapshot_confidence",
    }
    json_fields = {
        "workloads_json": "workloads_json",
        "capacity_estimates_json": "capacity_estimates_json",
        "geometry_json": "geometry",
        "tags_json": "tags",
    }
    projection: dict[str, Any] = {}
    for field in GLOBAL_GEOMETRY_ENTITY_FIELDS:
        target = json_fields.get(field, field)
        if field in float_fields:
            projection[target] = _csv_float(
                row[field], label=f"geometry entity {field}"
            )
        elif field in json_fields:
            projection[target] = _csv_json(
                row[field], label=f"geometry entity {field}"
            )
        else:
            projection[target] = _nullable_csv_value(row[field])
    projection["source_family"] = "openstreetmap"
    projection["source_attribution"] = "© OpenStreetMap contributors"
    if (
        not isinstance(projection["workloads_json"], list)
        or not isinstance(projection["capacity_estimates_json"], list)
        or not isinstance(projection["geometry"], dict)
        or not isinstance(projection["tags"], dict)
    ):
        raise VerifiedConstructionCoreError(
            "geometry bridge entity JSON projection differs"
        )
    return projection


def _global_geometry_evidence_projection(row: Mapping[str, str]) -> dict[str, Any]:
    return {field: _nullable_csv_value(row[field]) for field in row}


def _exact_component_id(kind: str, occurrence_id: str) -> str:
    payload = "\x1f".join(("exact-component-v1", kind, occurrence_id)).encode()
    return f"exact:{hashlib.sha256(payload).hexdigest()}"


def _exact_relationship_id(
    relationship_type: str, subject_component_id: str, object_component_id: str
) -> str:
    payload = "\x1f".join(
        (
            "exact-relationship-v1",
            relationship_type,
            subject_component_id,
            object_component_id,
        )
    ).encode()
    return f"relationship:{hashlib.sha256(payload).hexdigest()}"


def _exact_relationship_projection(row: Mapping[str, str]) -> dict[str, Any]:
    return {
        **{
            field: row[field]
            for field in EXACT_RELATIONSHIP_FIELDS
            if field
            not in {
                "typed_identity_tokens_json",
                "source_release_ids_json",
                "raw_relationship_count",
            }
        },
        "typed_identity_tokens": _csv_json(
            row["typed_identity_tokens_json"],
            label="geometry bridge relationship typed tokens",
        ),
        "source_release_ids": _csv_json(
            row["source_release_ids_json"],
            label="geometry bridge relationship releases",
        ),
        "raw_relationship_count": _csv_integer(
            row["raw_relationship_count"],
            label="geometry bridge relationship count",
        ),
    }


def _normalized_address_part(value: str, *, drop_road_types: bool = False) -> str:
    normalized = unicodedata.normalize("NFKD", value).encode("ascii", "ignore").decode()
    tokens = re.findall(r"[a-z0-9]+", normalized.casefold())
    if drop_road_types:
        tokens = [token for token in tokens if token not in {"rue", "st", "street"}]
    return "".join(tokens)


def _canonical_address_text(value: str) -> str:
    normalized = unicodedata.normalize("NFKC", value).translate(
        str.maketrans("‐‑‒–—―−", "-------")
    )
    return " ".join(normalized.casefold().split())


def _canonical_street(value: str) -> str:
    normalized = _canonical_address_text(value)
    tokens = normalized.split()
    if tokens and tokens[0] == "rue":
        tokens = tokens[1:]
    elif tokens and tokens[-1] in {"st", "st.", "street"}:
        tokens = tokens[:-1]
    return " ".join(tokens)


def _canonical_canadian_postcode(value: str) -> str:
    return re.sub(r"\s+", "", unicodedata.normalize("NFKC", value).upper())


def _validate_exact_component_projection(
    member: Any,
    *,
    entity: Mapping[str, Any],
    entity_kind: str,
    source_family: str,
    snapshot_evidence_id: str,
    label: str,
) -> dict[str, str]:
    if not isinstance(member, dict) or set(member) != {
        "component_id",
        "entity_id",
        "stable_key",
        "source_row",
    }:
        raise VerifiedConstructionCoreError(f"{label} projection fields differ")
    row = _embedded_csv_source_row(
        member["source_row"], EXACT_COMPONENT_FIELDS, label=label
    )
    occurrence_id = f"{SOURCE_RELEASE_ID}:{entity['entity_id']}"
    component_id = _exact_component_id(entity_kind, occurrence_id)
    if (
        member["component_id"] != row["component_id"]
        or member["entity_id"] != row["entity_id"]
        or member["stable_key"] != row["stable_key"]
        or row["component_id"] != component_id
        or row["occurrence_id"] != occurrence_id
        or row["release_id"] != SOURCE_RELEASE_ID
        or row["entity_id"] != entity["entity_id"]
        or row["entity_kind"] != entity_kind
        or row["stable_key"] != entity["stable_key"]
        or row["source_family"] != source_family
        or row["source_root"] != source_family
        or row["snapshot_evidence_id"] != snapshot_evidence_id
        or _csv_integer(row["component_member_count"], label=label) != 1
        or row["identity_proof_parent_occurrence_id"]
        or row["identity_proof_token"]
        or _csv_json(row["typed_identity_tokens_json"], label=label) != []
        or _csv_json(row["ambiguous_identity_tokens_json"], label=label) != []
    ):
        raise VerifiedConstructionCoreError(f"{label} projection differs")
    return row


def _validate_identity_bridge(
    bridge: Mapping[str, Any],
    project: Mapping[str, Any],
    campus: Mapping[str, Any],
    entity: Mapping[str, Any],
) -> None:
    identity_evidence = bridge["identity_bridge_evidence"]
    if entity.get("country") not in {project.get("country"), campus.get("country")}:
        raise VerifiedConstructionCoreError("geometry bridge identity country differs")
    project_key = str(project["stable_key"])
    if project_key == "curated:qscale-q01-levis-campus:building-b":
        if (
            not isinstance(identity_evidence, list)
            or len(identity_evidence) != 1
            or _json_bytes(identity_evidence[0]) != _json_bytes(QSCALE_IDENTITY_EVIDENCE)
        ):
            raise VerifiedConstructionCoreError(
                "geometry bridge identity evidence differs"
            )
        extraction = identity_evidence[0]["address_extraction"]
        tags = entity.get("tags")
        house_number = _canonical_address_text(extraction["house_number"])
        postcode = _canonical_canadian_postcode(extraction["postcode"])
        osm_house_number = _canonical_address_text(
            str(tags.get("addr:housenumber", "")) if isinstance(tags, dict) else ""
        )
        osm_postcode = _canonical_canadian_postcode(
            str(tags.get("addr:postcode", "")) if isinstance(tags, dict) else ""
        )
        if (
            not isinstance(tags, dict)
            or not re.fullmatch(r"[0-9]+[a-z]?", house_number)
            or not re.fullmatch(r"[A-Z][0-9][A-Z][0-9][A-Z][0-9]", postcode)
            or (house_number, _canonical_street(extraction["street"]), postcode)
            != ("2280", "albert-dion", "G7A5M9")
            or (osm_house_number, _canonical_street(str(tags.get("addr:street", ""))), osm_postcode)
            != ("2280", "albert-dion", "G7A5M9")
            or _canonical_address_text(str(entity.get("name", ""))) != "qscale"
            or _canonical_address_text(extraction["site_label"])
            != "headquarters - q01 campus"
        ):
            raise VerifiedConstructionCoreError(
                "geometry bridge QScale address identity differs"
            )
    elif project_key == "curated:green-campus-zrh1-lupfig:data-center-4":
        if (
            not isinstance(identity_evidence, list)
            or len(identity_evidence) != 1
            or _json_bytes(identity_evidence[0]) != _json_bytes(GREEN_IDENTITY_EVIDENCE)
        ):
            raise VerifiedConstructionCoreError(
                "geometry bridge identity evidence differs"
            )
        extraction = identity_evidence[0]["address_extraction"]
        tags = entity.get("tags")
        if (
            not isinstance(tags, dict)
            or (
                _canonical_address_text(extraction["house_number"]),
                _canonical_address_text(extraction["street"]),
                _canonical_address_text(extraction["postcode"]),
                _canonical_address_text(extraction["locality"]),
            )
            != ("31", "industriestrasse", "5242", "lupfig")
            or (
                _canonical_address_text(str(tags.get("addr:housenumber", ""))),
                _canonical_address_text(str(tags.get("addr:street", ""))),
                _canonical_address_text(str(tags.get("addr:postcode", ""))),
                _canonical_address_text(str(tags.get("addr:city", ""))),
            )
            != ("31", "industriestrasse", "5242", "lupfig")
            or _canonical_address_text(extraction["site_label"]) != "campus zrh1"
            or _canonical_address_text(str(tags.get("building", "")))
            != "data_center"
            or _canonical_address_text(str(tags.get("description", "")))
            != "green datacenter lupfig"
        ):
            raise VerifiedConstructionCoreError(
                "geometry bridge Green address identity differs"
            )
    else:
        if identity_evidence != []:
            raise VerifiedConstructionCoreError(
                "geometry bridge identity evidence differs"
            )
        normalized_names = _normalized_address_part(
            " ".join(
                str(value)
                for value in (
                    project.get("name"),
                    campus.get("name"),
                    entity.get("name"),
                )
            )
        )
        tags = entity.get("tags")
        if not isinstance(tags, dict):
            raise VerifiedConstructionCoreError(
                "geometry bridge campus identity differs"
            )
        if project_key == (
            "curated:atnorth-ice02-reykjanesbaer-campus:phase-2-expansion"
        ):
            valid = (
                "ice02" in normalized_names
                and "atnorth" in normalized_names
                and _normalized_address_part(str(entity.get("operator", "")))
                == "atnorth"
            )
        elif project_key == (
            "curated:related-openai-oracle-stargate-michigan-saline:current-build"
        ):
            valid = (
                "thebarn" in normalized_names
                and _canonical_address_text(str(entity.get("name", "")))
                == "the barn data center"
                and tags.get("website") == "https://www.thesalinebarn.com/"
                and tags.get("landuse") == "construction"
                and tags.get("industrial") == "data_centre"
            )
        elif project_key == (
            "curated:microsoft-mount-pleasant-datacenter-campus:second-facility"
        ):
            valid = (
                "microsoftmountpleasant" in normalized_names
                and _canonical_address_text(str(entity.get("name", "")))
                == "microsoft fairwater ai phase 2"
                and tags.get("landuse") == "construction"
                and tags.get("industrial") == "data_centre"
            )
        elif project_key == (
            "curated:amazon-salem-township-innovation-campus:active-buildout"
        ):
            valid = (
                "amazonsalemtownship" in normalized_names
                and _canonical_address_text(str(entity.get("name", "")))
                == "amazon aws phl"
                and _canonical_address_text(str(entity.get("operator", "")))
                == "amazon web services"
                and tags.get("addr:housenumber") == "1125"
                and tags.get("addr:street") == "Electron Avenue"
                and tags.get("addr:postcode") == "18603"
                and tags.get("addr:city") == "Berwick"
            )
        else:
            valid = False
        if not valid:
            raise VerifiedConstructionCoreError(
                "geometry bridge campus identity differs"
            )


def _validate_bridge_acceptance_semantics(
    acceptance: Mapping[str, Any], bridge: Mapping[str, Any], *, label: str
) -> None:
    decision = bridge["review_decision"]
    if (
        acceptance.get("geometry_entity")
        != decision["geometry_target_entity_kind"]
        or any(
            acceptance.get(field) != decision[decision_field]
            for field, decision_field in (
                ("geometry_derivation", "geometry_derivation"),
                ("geometry_method", "geometry_method"),
                ("geometry_scope_class", "geometry_scope_class"),
                ("horizontal_uncertainty_metres", "horizontal_uncertainty_metres"),
                ("precision_scope", "precision_scope"),
            )
        )
    ):
        raise VerifiedConstructionCoreError(f"{label} overlay semantics differ")


def _geometry_evidence_projection(bridge: Mapping[str, Any]) -> dict[str, Any]:
    evidence = bridge["geometry_evidence"]
    return {
        field: evidence[field] if evidence[field] is not None else ""
        for field in EVIDENCE_FIELDS
        if field not in {"roles_json", "project_ids_json"}
    }


V06_BRIDGE_SEMANTICS = {
    "curated:adaniconnex-pune-data-center-campus:pnq04-current-build": {
        "decision": "accepted_as_project_locator",
        "geometry_target_entity_kind": "project",
        "geometry_derivation": "direct_geometry",
        "geometry_method": "official_project_report_coordinate_to_point",
        "geometry_scope_class": "official_source_reported_project_reference_point",
        "geometry_authority_class": "official_source",
        "geometry_use_scope": "project_locator",
        "geometry_stable_key": (
            "curated:adaniconnex-pune-data-center-campus:pnq04-current-build"
        ),
        "horizontal_uncertainty_unknown_reason": (
            "The report does not identify the coordinate's datum, reference "
            "feature, collection method, or horizontal accuracy."
        ),
        "imagery_outcome": "not_reviewed_for_core_preview",
    },
    "curated:goodman-hkg09-kwai-chung-data-centre:current-redevelopment": {
        "decision": "accepted_as_campus_locator",
        "geometry_target_entity_kind": "campus",
        "geometry_derivation": "cross_source_overlay",
        "geometry_method": (
            "official_company_exact_address_to_lands_department_location_search_"
            "and_official_hk80_wgs84_transform"
        ),
        "geometry_scope_class": (
            "official_lands_department_address_building_reference_point"
        ),
        "geometry_authority_class": "official_source",
        "geometry_use_scope": "campus_locator",
        "geometry_stable_key": "curated:goodman-hkg09-kwai-chung-data-centre",
        "horizontal_uncertainty_unknown_reason": (
            "The Location Search API and transformation API do not publish "
            "numeric horizontal accuracy for this selected result; the "
            "transformation service warns against precise-point applications."
        ),
        "imagery_outcome": "not_reviewed_for_core_preview",
    },
    "curated:green-mountain-undheim-campus:current-two-building-development": {
        "decision": "accepted_as_official_boundary",
        "geometry_target_entity_kind": "campus",
        "geometry_derivation": "official_parcel_union",
        "geometry_method": "authoritative_kartverket_cadastral_parcel_union",
        "geometry_scope_class": (
            "official_cadastral_project_site_parcel_union_boundary"
        ),
        "geometry_authority_class": "official_source",
        "geometry_use_scope": "official_boundary",
        "geometry_stable_key": (
            "kartverket:matrikkelteig-union/1121-46/316+317+319"
        ),
        "horizontal_uncertainty_unknown_reason": (
            "Kartverket supplies only a coarse green accuracy class and "
            "explicitly does not expose boundary-line type or detailed boundary "
            "quality."
        ),
        "imagery_outcome": (
            "first_party_contractor_drone_imagery_present_not_independently_verified"
        ),
    },
    "curated:nscale-kvandal-narvik-ai-data-center-campus:initial-25mw-epc-current-build": {
        "decision": "accepted_as_campus_locator",
        "geometry_target_entity_kind": "campus",
        "geometry_derivation": "cross_source_overlay",
        "geometry_method": (
            "kartverket_official_cadastral_parcel_polygon_as_campus_locator"
        ),
        "geometry_scope_class": "official_cadastral_parcel_as_campus_locator",
        "geometry_authority_class": "official_source",
        "geometry_use_scope": "campus_locator",
        "geometry_stable_key": "kartverket:matrikkelen-teig/6508960482",
        "horizontal_uncertainty_unknown_reason": (
            "The captured WFS publishes accuracy class “Grønt” but no numeric "
            "horizontal uncertainty in metres; the bridge does not convert that "
            "class into survey accuracy."
        ),
        "imagery_outcome": "not_reviewed_for_core_preview",
    },
    "curated:skygard-osl1-hovinbyen-campus:phase-2": {
        "decision": "accepted_as_campus_locator",
        "geometry_target_entity_kind": "campus",
        "geometry_derivation": "cross_source_overlay",
        "geometry_method": (
            "official_nve_site_address_to_matrikkelen_epsg4326_address_point"
        ),
        "geometry_scope_class": (
            "official_matrikkelen_verified_address_representative_point"
        ),
        "geometry_authority_class": "official_source",
        "geometry_use_scope": "campus_locator",
        "geometry_stable_key": "curated:skygard-osl1-hovinbyen-campus",
        "horizontal_uncertainty_unknown_reason": (
            "The response marks the address point verified but does not publish "
            "numeric horizontal accuracy for this record."
        ),
        "imagery_outcome": "not_reviewed_for_core_preview",
    },
    **{
        f"curated:stt-jakarta-data-centre-campus:stt-jakarta-{number}": {
            "decision": "accepted_as_campus_locator",
            "geometry_target_entity_kind": "campus",
            "geometry_derivation": "cross_source_overlay",
            "geometry_method": (
                "openstreetmap_exactly_named_stt_jakarta_1_facility_polygon_as_"
                "shared_campus_locator"
            ),
            "geometry_scope_class": (
                "reviewed_openstreetmap_stt_jakarta_1_building_polygon"
            ),
            "geometry_authority_class": "community_mapped",
            "geometry_use_scope": "campus_locator",
            "geometry_stable_key": "osm:way/1533569836",
            "horizontal_uncertainty_unknown_reason": (
                "OpenStreetMap does not state horizontal positional accuracy; "
                "community-mapped geometry is not cadastral or survey geometry."
            ),
            "imagery_outcome": "not_reviewed_for_core_preview",
        }
        for number in (3, 5, 6)
    },
}


def _official_evidence_id(evidence: Mapping[str, Any]) -> str:
    return atlas_stable_id(
        "evidence",
        "curated-official",
        evidence.get("key"),
        evidence.get("content_hash"),
    )


def _validate_v06_fact_evidence(evidence: Any, *, label: str) -> None:
    if not isinstance(evidence, dict):
        raise VerifiedConstructionCoreError(f"{label} differs")
    for field in (
        "key",
        "kind",
        "title",
        "source_url",
        "publisher",
        "source_family",
        "license",
        "attribution",
        "retrieved_at",
        "content_hash",
    ):
        if not isinstance(evidence.get(field), str) or not evidence[field]:
            raise VerifiedConstructionCoreError(f"{label} {field} differs")
    content_hash = evidence["content_hash"]
    if re.fullmatch(r"[0-9a-f]{64}", content_hash) is None:
        raise VerifiedConstructionCoreError(f"{label} content hash differs")
    evidence_id = evidence.get("evidence_id")
    if evidence_id is not None and evidence_id != _official_evidence_id(evidence):
        raise VerifiedConstructionCoreError(f"{label} evidence identity differs")
    if "fact_payload" in evidence:
        if (
            evidence.get("fact_payload_canonical_sha256")
            != _sha256_bytes(_json_bytes(evidence["fact_payload"]))
            or not isinstance(evidence.get("fact_payload_hash_scope"), str)
            or not evidence["fact_payload_hash_scope"]
        ):
            raise VerifiedConstructionCoreError(f"{label} fact payload differs")
    captured_bytes = evidence.get("captured_bytes")
    if captured_bytes is not None and (
        isinstance(captured_bytes, bool)
        or not isinstance(captured_bytes, int)
        or captured_bytes <= 0
    ):
        raise VerifiedConstructionCoreError(f"{label} byte count differs")


def _projected_bridge_observations(
    source: Mapping[str, Any],
    evidence_by_key: Mapping[str, Mapping[str, Any]],
    *,
    collection: str,
    entity_kind: str,
) -> list[dict[str, Any]]:
    rows = source.get(collection, [])
    if not isinstance(rows, list):
        raise VerifiedConstructionCoreError(
            f"geometry bridge {collection} source differs"
        )
    projected: list[dict[str, Any]] = []
    for row in rows:
        if not isinstance(row, dict):
            raise VerifiedConstructionCoreError(
                f"geometry bridge {collection} observation differs"
            )
        if row.get("entity") != entity_kind:
            continue
        pinned = evidence_by_key.get(str(row.get("evidence_key")))
        if pinned is None:
            raise VerifiedConstructionCoreError(
                f"geometry bridge {collection} evidence differs"
            )
        evidence_id = _official_evidence_id(pinned)
        if collection == "capacities":
            observation = {
                **{
                    field: (
                        float(value)
                        if field in {"low", "base", "high"} and value is not None
                        else value
                    )
                    for field, value in row.items()
                    if field not in {"entity", "evidence_key"}
                },
                "evidence_id": evidence_id,
            }
            _validate_typed_observation(
                observation, POWER_METRICS | ENERGY_METRICS | EFFICIENCY_METRICS
            )
        else:
            observation = {
                **{
                    ("workload" if field == "value" else field): value
                    for field, value in row.items()
                    if field not in {"entity", "evidence_key"}
                },
                "evidence_id": evidence_id,
            }
            _validate_source_workload_observation(observation)
        projected.append(observation)
    return projected


def _validate_v06_embedded_release_rows(
    rows: Any,
    members: Mapping[str, Any],
    *,
    hydrated_crosscheck: bool,
) -> None:
    if not isinstance(rows, dict) or not rows:
        raise VerifiedConstructionCoreError("geometry bridge release rows differ")
    for label, binding in rows.items():
        if not isinstance(binding, dict) or set(binding) != {
            "member",
            "source_row",
            *(
                {"evidence_id"}
                if binding.get("member") == "evidence.csv"
                else ({"stable_key"} if "stable_key" in binding else {"entity_id"})
            ),
        }:
            raise VerifiedConstructionCoreError(
                f"geometry bridge release row differs: {label}"
            )
        member = binding["member"]
        fields = (
            GLOBAL_GEOMETRY_EVIDENCE_FIELDS
            if member == "evidence.csv"
            else SOURCE_ENTITY_FIELDS
        )
        if member not in members or member not in {
            "construction_pipeline.csv",
            "entities.csv",
            "evidence.csv",
        }:
            raise VerifiedConstructionCoreError(
                f"geometry bridge release row member differs: {label}"
            )
        row = _embedded_csv_source_row(
            binding["source_row"], fields, label=f"geometry bridge {label}"
        )
        identity_field = next(
            field for field in ("evidence_id", "stable_key", "entity_id") if field in binding
        )
        if row[identity_field] != binding[identity_field]:
            raise VerifiedConstructionCoreError(
                f"geometry bridge release row identity differs: {label}"
            )
        if hydrated_crosscheck:
            metadata = members[member]
            payload_path = _repository_input(
                metadata["path"], metadata["sha256"], f"geometry bridge {member}"
            )
            if payload_path.stat().st_size != metadata["bytes"]:
                raise VerifiedConstructionCoreError(
                    f"geometry bridge hydrated release differs: {member}"
                )
            _validate_hydrated_source_row(
                payload_path,
                binding["source_row"],
                fields,
                label=f"geometry bridge {label}",
            )


def _validate_v06_topology(
    bridge: Mapping[str, Any],
    project: Mapping[str, Any],
    campus: Mapping[str, Any],
    *,
    project_source_family: str,
    project_evidence_id: str,
    campus_source_family: str,
    campus_evidence_id: str,
    hydrated_crosscheck: bool,
) -> None:
    topology = bridge["construction_source"]["project_to_campus"]
    if not isinstance(topology, dict) or set(topology) != {
        "basis",
        "target_entity_id",
        "bundle_id",
        "manifest",
        "members",
        "relationship",
        "subject_member",
        "object_member",
        "supplemental_v97_atlas_feature",
    }:
        raise VerifiedConstructionCoreError("geometry bridge topology fields differ")
    if (
        topology.get("basis") != "v14_project_targets_explicit_parent"
        or topology.get("bundle_id") != "2026-07-22-public-open-v14"
        or topology.get("target_entity_id") != campus.get("entity_id")
        or topology.get("supplemental_v97_atlas_feature") is not None
    ):
        raise VerifiedConstructionCoreError("geometry bridge topology identity differs")
    _, manifest = _manifest_binding(
        topology["manifest"], label="geometry bridge topology release"
    )
    if (
        manifest.get("bundle_id") != topology["bundle_id"]
        or manifest.get("format")
        != "datacenter-atlas-exact-identity-decision-bundle-v1"
        or set(topology["members"])
        != {"component-members.csv", "relationships.csv"}
    ):
        raise VerifiedConstructionCoreError("geometry bridge topology release differs")
    _validate_manifest_members(
        manifest, topology["members"], label="geometry bridge topology release"
    )
    subject = topology["subject_member"]
    object_member = topology["object_member"]
    _validate_exact_component_projection(
        subject,
        entity=project,
        entity_kind="project",
        source_family=project_source_family,
        snapshot_evidence_id=project_evidence_id,
        label="geometry bridge topology subject",
    )
    _validate_exact_component_projection(
        object_member,
        entity=campus,
        entity_kind="campus",
        source_family=campus_source_family,
        snapshot_evidence_id=campus_evidence_id,
        label="geometry bridge topology object",
    )
    relationship = topology["relationship"]
    if not isinstance(relationship, dict) or set(relationship) != {
        "relationship_id",
        "relationship_type",
        "subject_component_id",
        "subject_kind",
        "object_component_id",
        "object_kind",
        "decision_basis",
        "typed_identity_tokens",
        "source_release_ids",
        "raw_relationship_count",
        "source_row",
    }:
        raise VerifiedConstructionCoreError(
            "geometry bridge topology relationship fields differ"
        )
    row = _embedded_csv_source_row(
        relationship["source_row"],
        EXACT_RELATIONSHIP_FIELDS,
        label="geometry bridge topology relationship",
    )
    projection = _exact_relationship_projection(row)
    if (
        {field: value for field, value in relationship.items() if field != "source_row"}
        != projection
        or relationship.get("relationship_type") != "project_targets"
        or relationship.get("subject_component_id") != subject.get("component_id")
        or relationship.get("subject_kind") != "project"
        or relationship.get("object_component_id")
        != object_member.get("component_id")
        or relationship.get("object_kind") != "campus"
        or relationship.get("decision_basis") != "explicit_parent"
        or relationship.get("typed_identity_tokens") != []
        or relationship.get("source_release_ids") != [SOURCE_RELEASE_ID]
        or relationship.get("raw_relationship_count") != 1
        or relationship.get("relationship_id")
        != _exact_relationship_id(
            "project_targets", subject["component_id"], object_member["component_id"]
        )
    ):
        raise VerifiedConstructionCoreError(
            "geometry bridge topology relationship differs"
        )
    if hydrated_crosscheck:
        for name, record, fields in (
            ("relationships.csv", relationship, EXACT_RELATIONSHIP_FIELDS),
            ("component-members.csv", subject, EXACT_COMPONENT_FIELDS),
            ("component-members.csv", object_member, EXACT_COMPONENT_FIELDS),
        ):
            metadata = topology["members"][name]
            payload_path = _repository_input(
                metadata["path"], metadata["sha256"], f"geometry bridge {name}"
            )
            if payload_path.stat().st_size != metadata["bytes"]:
                raise VerifiedConstructionCoreError(
                    f"geometry bridge hydrated topology differs: {name}"
                )
            _validate_hydrated_source_row(
                payload_path,
                record["source_row"],
                fields,
                label=f"geometry bridge {name}",
            )


def _validate_v06_construction_source(
    bridge: Mapping[str, Any], *, hydrated_crosscheck: bool
) -> tuple[dict[str, Any], dict[str, Any]]:
    construction = bridge["construction_source"]
    allowed_fields = {
        "input",
        "release",
        "project",
        "campus",
        "project_to_campus",
        "status_evidence",
    }
    if not isinstance(construction, dict) or set(construction) not in {
        frozenset(allowed_fields),
        frozenset(allowed_fields | {"release_rows"}),
    }:
        raise VerifiedConstructionCoreError("geometry bridge construction source differs")
    source_input = construction["input"]
    if not isinstance(source_input, dict) or set(source_input) != {
        "path",
        "bytes",
        "sha256",
    }:
        raise VerifiedConstructionCoreError("geometry bridge source input differs")
    source_path = _repository_input(
        source_input["path"], source_input["sha256"], "geometry bridge source"
    )
    if source_path.stat().st_size != source_input["bytes"]:
        raise VerifiedConstructionCoreError("geometry bridge source byte count differs")
    source = _load_json(source_path)
    source_project = source.get("project") if isinstance(source, dict) else None
    source_campus = source.get("campus") if isinstance(source, dict) else None
    source_evidence = source.get("evidence") if isinstance(source, dict) else None
    if (
        not isinstance(source_project, dict)
        or not isinstance(source_campus, dict)
        or not isinstance(source_evidence, list)
    ):
        raise VerifiedConstructionCoreError("geometry bridge source record differs")
    evidence_by_key = {
        row["key"]: row
        for row in source_evidence
        if isinstance(row, dict) and isinstance(row.get("key"), str)
    }
    if len(evidence_by_key) != len(source_evidence):
        raise VerifiedConstructionCoreError("geometry bridge source evidence differs")
    lifecycle = [
        row
        for row in source.get("lifecycle", [])
        if isinstance(row, dict) and row.get("entity") == "project"
    ]
    if not lifecycle:
        raise VerifiedConstructionCoreError("geometry bridge lifecycle differs")
    latest = max(lifecycle, key=lambda row: row.get("as_of_date", ""))
    pinned_status = evidence_by_key.get(latest.get("evidence_key"))
    if pinned_status is None:
        raise VerifiedConstructionCoreError("geometry bridge status evidence differs")
    status_id = _official_evidence_id(pinned_status)
    campus_source_evidence = evidence_by_key.get(source_campus.get("evidence_key"))
    if campus_source_evidence is None:
        raise VerifiedConstructionCoreError(
            "geometry bridge campus source evidence differs"
        )
    campus_source_evidence_id = _official_evidence_id(campus_source_evidence)
    project_capacities = _projected_bridge_observations(
        source, evidence_by_key, collection="capacities", entity_kind="project"
    )
    project_workloads = _projected_bridge_observations(
        source, evidence_by_key, collection="workloads", entity_kind="project"
    )
    project = construction["project"]
    expected_project: dict[str, Any] = {
        "stable_key": source_project["stable_key"],
        "entity_id": atlas_stable_id(
            "entity", source_project["stable_key"], "project"
        ),
        "name": source_project["name"],
        "country": source_project["country"],
        "address": source_project["address"],
        "status": latest["value"],
        "status_as_of": latest["as_of_date"],
        "status_age_days_at_review": (
            REVIEW_DATE - _calendar_date(latest["as_of_date"], "bridge status")
        ).days,
        "status_method": latest["method"],
        "status_confidence": latest["confidence"],
        "status_evidence_id": status_id,
        "workloads_json": project_workloads,
        "capacity_estimates_json": project_capacities,
    }
    if isinstance(project, dict) and "capacity_semantics" in project:
        by_metric = {row["metric"]: row["base"] for row in project_capacities}
        expected_semantics = project["capacity_semantics"]
        if (
            not isinstance(expected_semantics, dict)
            or set(expected_semantics)
            != {
                "non_additive",
                "critical_it_mw",
                "gross_facility_mw",
                "annual_energy_observation",
                "guardrail",
            }
            or expected_semantics.get("non_additive") is not True
            or expected_semantics.get("critical_it_mw")
            != by_metric.get("critical_it_mw")
            or expected_semantics.get("gross_facility_mw")
            != by_metric.get("gross_facility_mw")
            or expected_semantics.get("annual_energy_observation") is not None
            or not isinstance(expected_semantics.get("guardrail"), str)
            or not expected_semantics["guardrail"]
        ):
            raise VerifiedConstructionCoreError(
                "geometry bridge capacity semantics differ"
            )
        expected_project["capacity_semantics"] = expected_semantics
    if not isinstance(project, dict) or project != expected_project:
        raise VerifiedConstructionCoreError(
            "geometry bridge construction project projection differs"
        )

    campus_models = [
        row
        for row in source.get("operating_models", [])
        if isinstance(row, dict) and row.get("entity") == "campus"
    ]
    if len(campus_models) > 1:
        raise VerifiedConstructionCoreError("geometry bridge campus model differs")
    model = campus_models[0] if campus_models else None
    model_id = (
        _official_evidence_id(evidence_by_key[model["evidence_key"]])
        if model is not None
        else None
    )
    campus = construction["campus"]
    expected_campus: dict[str, Any] = {
        "stable_key": source_campus["stable_key"],
        "entity_id": atlas_stable_id(
            "entity", source_campus["stable_key"], "campus"
        ),
        "name": source_campus["name"],
        "country": source_campus["country"],
        "address": source_campus["address"],
        "operating_model": model["value"] if model else None,
        "operating_model_confidence": model["confidence"] if model else None,
        "operating_model_evidence_id": model_id,
    }
    campus_workloads = _projected_bridge_observations(
        source, evidence_by_key, collection="workloads", entity_kind="campus"
    )
    campus_capacities = _projected_bridge_observations(
        source, evidence_by_key, collection="capacities", entity_kind="campus"
    )
    if isinstance(campus, dict) and "workloads_json" in campus:
        expected_campus["workloads_json"] = campus_workloads
    elif campus_workloads:
        raise VerifiedConstructionCoreError("geometry bridge campus workloads differ")
    if isinstance(campus, dict) and "capacity_estimates_json" in campus:
        expected_campus["capacity_estimates_json"] = campus_capacities
    elif campus_capacities:
        raise VerifiedConstructionCoreError("geometry bridge campus capacities differ")
    if not isinstance(campus, dict) or campus != expected_campus:
        raise VerifiedConstructionCoreError(
            "geometry bridge construction campus projection differs"
        )

    status = construction["status_evidence"]
    required_status_fields = {
        "evidence_id",
        "kind",
        "title",
        "source_url",
        "publisher",
        "source_family",
        "license",
        "attribution",
        "published_at",
        "retrieved_at",
        "content_hash",
    }
    if (
        not isinstance(status, dict)
        or not required_status_fields <= set(status)
        or frozenset(set(status) - required_status_fields)
        not in {frozenset(), frozenset({"key"}), frozenset({"key", "content_hash_scope"})}
        or status.get("evidence_id") != status_id
        or project["status_evidence_id"] != status_id
    ):
        raise VerifiedConstructionCoreError("geometry bridge status projection differs")
    for field in required_status_fields - {"evidence_id"}:
        if status.get(field) != pinned_status.get(field):
            raise VerifiedConstructionCoreError(
                "geometry bridge status evidence content differs"
            )
    if "key" in status and status["key"] != pinned_status["key"]:
        raise VerifiedConstructionCoreError("geometry bridge status evidence key differs")

    release = construction["release"]
    if not isinstance(release, dict) or set(release) not in {
        frozenset({"release_id", "manifest", "members"}),
        frozenset({"release_id", "manifest", "members", "raw_rows"}),
    } or release.get("release_id") != SOURCE_RELEASE_ID:
        raise VerifiedConstructionCoreError("geometry bridge source release differs")
    _, release_manifest = _manifest_binding(
        release["manifest"], label="geometry bridge source release"
    )
    if set(release["members"]) != {
        "construction_pipeline.csv",
        "entities.csv",
        "evidence.csv",
    }:
        raise VerifiedConstructionCoreError("geometry bridge source members differ")
    _validate_manifest_members(
        release_manifest, release["members"], label="geometry bridge source release"
    )
    embedded_rows = release.get("raw_rows", construction.get("release_rows"))
    if embedded_rows is not None:
        _validate_v06_embedded_release_rows(
            embedded_rows,
            release["members"],
            hydrated_crosscheck=hydrated_crosscheck,
        )
    _validate_v06_topology(
        bridge,
        project,
        campus,
        project_source_family=pinned_status["source_family"],
        project_evidence_id=status_id,
        campus_source_family=campus_source_evidence["source_family"],
        campus_evidence_id=campus_source_evidence_id,
        hydrated_crosscheck=hydrated_crosscheck,
    )
    return project, campus


def _point_inside_ring(point: Sequence[float], ring: Sequence[Sequence[float]]) -> bool:
    x, y = point
    inside = False
    for first, second in zip(ring, ring[1:]):
        x1, y1 = first
        x2, y2 = second
        cross = (x - x1) * (y2 - y1) - (y - y1) * (x2 - x1)
        if abs(cross) <= 1e-12 and min(x1, x2) <= x <= max(x1, x2) and min(
            y1, y2
        ) <= y <= max(y1, y2):
            return True
        if (y1 > y) != (y2 > y):
            intersection = x1 + (y - y1) * (x2 - x1) / (y2 - y1)
            if x < intersection:
                inside = not inside
    return inside


def _validate_v06_official_point_geometry(
    bridge: Mapping[str, Any],
    project: Mapping[str, Any],
    campus: Mapping[str, Any],
) -> None:
    if bridge["geometry_release"] is not None:
        raise VerifiedConstructionCoreError(
            "geometry bridge official point release must be absent"
        )
    entity = bridge["geometry_entity"]
    evidence = bridge["geometry_evidence"]
    expected_entity_fields = {
        "entity_id",
        "entity_kind",
        "stable_key",
        "name",
        "latitude",
        "longitude",
        "country",
        "address",
        "geometry",
        "source_evidence_id",
        "source_url",
        "source_publisher",
        "source_license",
        "source_retrieved_at",
    }
    target = project if entity.get("entity_kind") == "project" else campus
    if (
        not isinstance(entity, dict)
        or set(entity) != expected_entity_fields
        or entity.get("entity_kind") not in {"project", "campus"}
        or any(
            entity.get(field) != target.get(field)
            for field in ("entity_id", "stable_key", "name", "country")
        )
        or not isinstance(evidence, dict)
    ):
        raise VerifiedConstructionCoreError("geometry bridge official point differs")
    geometry = entity["geometry"]
    if (
        not isinstance(geometry, dict)
        or geometry != {
            "type": "Point",
            "coordinates": [entity["longitude"], entity["latitude"]],
        }
        or not _finite_number(entity["latitude"])
        or not _finite_number(entity["longitude"])
        or not -90 <= entity["latitude"] <= 90
        or not -180 <= entity["longitude"] <= 180
    ):
        raise VerifiedConstructionCoreError(
            "geometry bridge official point coordinates differ"
        )
    _validate_v06_fact_evidence(evidence, label="geometry bridge point evidence")
    if (
        evidence.get("evidence_id") != entity.get("source_evidence_id")
        or evidence.get("source_url") != entity.get("source_url")
        or evidence.get("publisher") != entity.get("source_publisher")
        or evidence.get("license") != entity.get("source_license")
        or evidence.get("retrieved_at") != entity.get("source_retrieved_at")
    ):
        raise VerifiedConstructionCoreError(
            "geometry bridge official point evidence differs"
        )
    fact_payload = evidence.get("fact_payload")
    if not isinstance(fact_payload, dict):
        raise VerifiedConstructionCoreError(
            "geometry bridge official point fact payload differs"
        )
    source_family = evidence.get("source_family")
    expected_evidence = {
        "curated:adaniconnex-pune-data-center-campus:pnq04-current-build": (
            "india-adaniconnex-pune-pnq04-compliance-captured-2026-07-21",
            "adaniconnex_environmental_compliance_reports",
            "406e7134f896a916f6ac978a888183f67de62218cbfbd24a90248af52af544f4",
        ),
        "curated:goodman-hkg09-kwai-chung-data-centre:current-redevelopment": (
            "hk-landsd-hk80-to-wgs84-hkg09-address-point-captured-2026-08-20",
            "hong_kong_lands_department_coordinate_transformation_api",
            "8db2983a30966dedfb7470b0712deb3bb82505e82ef65ac1b786f052ad8efb9e",
        ),
        "curated:skygard-osl1-hovinbyen-campus:phase-2": (
            "norway-geonorge-ostre-aker-vei-24c-address-epsg4326-captured-2026-08-20",
            "kartverket_matrikkelen_address_rest_api",
            "de1397fe0cf91cb53d0330d4b1cfbb589b868122b2602100a375c69f4b506de1",
        ),
    }.get(project.get("stable_key"))
    if expected_evidence is None or (
        evidence.get("key"), source_family, evidence.get("content_hash")
    ) != expected_evidence:
        raise VerifiedConstructionCoreError(
            "geometry bridge official point evidence contract differs"
        )
    official_source_contract = {
        "curated:adaniconnex-pune-data-center-campus:pnq04-current-build": {
            "license": "all-rights-reserved",
            "license_url": None,
            "publisher": "Pune Data Center Limited",
            "attribution": "Pune Data Center Limited",
            "published_at": None,
            "retrieved_at": "2026-07-21T16:11:57Z",
            "kind": "company_disclosure",
            "title": "Compliance to Stipulated Conditions in Environment Clearance, October 2024 to March 2025 — Development of Data Center PNQ04",
            "source_url": "https://www.adaniconnex.com/-/media/Project/AdaniConneX/AdaniConneX-AboutUs-Assets/Certifications/Post-EC-compliance-report-PNQ26--with-Anneure.pdf",
            "geometry_license_url": None,
            "geometry_source": "The geometry is an arithmetic normalization of the coordinate printed in the official PNQ04 PDF; the PDF is not redistributed.",
            "fact_payload_sha256": "1d8dfc91502babb1ca58cbfc140d03b25c79197eb00eb47a6f247dc85f45103e",
        },
        "curated:goodman-hkg09-kwai-chung-data-centre:current-redevelopment": {
            "license": "Hong-Kong-DATA.GOV.HK-Terms-of-Use",
            "license_url": "https://data.gov.hk/en/terms-and-conditions",
            "publisher": "Geodetic Survey Section, Lands Department, Government of the Hong Kong SAR",
            "attribution": "Government of the Hong Kong SAR / Lands Department / DATA.GOV.HK",
            "published_at": None,
            "retrieved_at": "2026-08-20T19:57:28Z",
            "kind": "government_coordinate_transformation",
            "title": "Lands Department HK1980 Grid to WGS84 transformation",
            "source_url": "https://www.geodetic.gov.hk/transform/v2/?inSys=hkgrid&outSys=wgsgeog&e=832091&n=825415",
            "geometry_license_url": "https://data.gov.hk/en/terms-and-conditions",
            "geometry_source": "The exact two-field official transformation response is distributed under DATA.GOV.HK terms with Government, Lands Department, and DATA.GOV.HK attribution.",
            "fact_payload_sha256": "ab876378ef271567f2cc44e9d052ac6a6cbb8da2ae3dc48fe785fbdc7718e50c",
        },
        "curated:skygard-osl1-hovinbyen-campus:phase-2": {
            "license": "CC-BY-4.0",
            "license_url": "https://creativecommons.org/licenses/by/4.0/",
            "publisher": "Kartverket / Statens kartverk",
            "attribution": "Kartverket",
            "published_at": None,
            "retrieved_at": "2026-08-20T19:58:01Z",
            "kind": "government_address_register",
            "title": "Matrikkelen address lookup: Østre Aker vei 24C, Oslo",
            "source_url": "https://ws.geonorge.no/adresser/v1/sok?sok=%C3%98stre%20Aker%20vei%2024C%2C%200581%20Oslo&utkoordsys=4326&treffPerSide=10",
            "geometry_license_url": "https://creativecommons.org/licenses/by/4.0/",
            "geometry_source": "The exact Kartverket address fields are open government data under CC BY 4.0 and require Kartverket attribution.",
            "fact_payload_sha256": "ebceb1b62f367711d095f3fb07ad843944f2ec01f0d3125b805c3ead92ea827e",
        },
    }[project["stable_key"]]
    rights = bridge.get("rights")
    if (
        evidence.get("license") != official_source_contract["license"]
        or evidence.get("license_url") != official_source_contract["license_url"]
        or evidence.get("publisher") != official_source_contract["publisher"]
        or evidence.get("attribution") != official_source_contract["attribution"]
        or evidence.get("published_at") != official_source_contract["published_at"]
        or evidence.get("retrieved_at") != official_source_contract["retrieved_at"]
        or evidence.get("kind") != official_source_contract["kind"]
        or evidence.get("title") != official_source_contract["title"]
        or evidence.get("source_url") != official_source_contract["source_url"]
        or evidence.get("fact_payload_canonical_sha256")
        != official_source_contract["fact_payload_sha256"]
        or entity.get("source_license") != official_source_contract["license"]
        or not isinstance(rights, dict)
        or rights.get("geometry_license_url")
        != official_source_contract["geometry_license_url"]
        or rights.get("geometry_source")
        != official_source_contract["geometry_source"]
    ):
        raise VerifiedConstructionCoreError(
            "geometry bridge official point rights contract differs"
        )
    if source_family == "adaniconnex_environmental_compliance_reports":
        dms_pattern = re.compile(r"^([0-9]+)º([0-9]+)ʹ([0-9]+(?:\.[0-9]+)?)ʺ$")

        def decimal_from_dms(value: object) -> float:
            match = dms_pattern.fullmatch(str(value))
            if match is None:
                raise VerifiedConstructionCoreError(
                    "geometry bridge official DMS coordinate differs"
                )
            degrees, minutes, seconds = match.groups()
            if int(minutes) >= 60 or float(seconds) >= 60:
                raise VerifiedConstructionCoreError(
                    "geometry bridge official DMS coordinate differs"
                )
            return int(degrees) + int(minutes) / 60 + float(seconds) / 3600

        latitude = decimal_from_dms(fact_payload.get("latitude_dms"))
        longitude = decimal_from_dms(fact_payload.get("longitude_dms"))
        if (
            fact_payload.get("latitude_decimal") != entity["latitude"]
            or fact_payload.get("longitude_decimal") != entity["longitude"]
            or not math.isclose(latitude, entity["latitude"], abs_tol=1e-10)
            or not math.isclose(longitude, entity["longitude"], abs_tol=1e-10)
        ):
            raise VerifiedConstructionCoreError(
                "geometry bridge official DMS derivation differs"
            )
    elif source_family == "hong_kong_lands_department_coordinate_transformation_api":
        request = fact_payload.get("request_input")
        response = fact_payload.get("response")
        address_facts = next(
            (
                item.get("fact_payload")
                for item in bridge["identity_bridge_evidence"]
                if item.get("source_family")
                == "hong_kong_lands_department_location_search_api"
            ),
            None,
        )
        selected = (
            address_facts.get("selected_result")
            if isinstance(address_facts, dict)
            else None
        )
        if (
            not isinstance(request, dict)
            or not isinstance(response, dict)
            or not isinstance(selected, dict)
            or request
            != {
                "inSys": "hkgrid",
                "outSys": "wgsgeog",
                "e": selected.get("x"),
                "n": selected.get("y"),
            }
            or response
            != {"wgsLat": entity["latitude"], "wgsLong": entity["longitude"]}
        ):
            raise VerifiedConstructionCoreError(
                "geometry bridge official coordinate transform differs"
            )
    elif source_family == "kartverket_matrikkelen_address_rest_api":
        address = fact_payload.get("address")
        point = address.get("representasjonspunkt") if isinstance(address, dict) else None
        if (
            not isinstance(point, dict)
            or point
            != {
                "epsg": "EPSG:4326",
                "lat": entity["latitude"],
                "lon": entity["longitude"],
            }
            or address.get("stedfestingverifisert") is not True
        ):
            raise VerifiedConstructionCoreError(
                "geometry bridge official address point differs"
            )
    else:
        raise VerifiedConstructionCoreError(
            "geometry bridge official point source family differs"
        )


def _validate_v06_osm_geometry(
    bridge: Mapping[str, Any], *, hydrated_crosscheck: bool
) -> None:
    release = bridge["geometry_release"]
    if not isinstance(release, dict) or set(release) != {
        "release_id",
        "as_of",
        "recorded_at",
        "manifest",
        "members",
    } or release.get("release_id") != "global-open-v3":
        raise VerifiedConstructionCoreError("geometry bridge OSM release differs")
    _, manifest = _manifest_binding(
        release["manifest"], label="geometry bridge OSM release"
    )
    if set(release["members"]) != {"entities.csv", "evidence.csv"}:
        raise VerifiedConstructionCoreError("geometry bridge OSM members differ")
    _validate_manifest_members(manifest, release["members"], label="geometry bridge OSM")
    entity = bridge["geometry_entity"]
    evidence = bridge["geometry_evidence"]
    if not isinstance(entity, dict) or not isinstance(evidence, dict):
        raise VerifiedConstructionCoreError("geometry bridge OSM records differ")
    entity_row = _embedded_csv_source_row(
        entity.get("source_row"),
        GLOBAL_GEOMETRY_ENTITY_FIELDS,
        label="geometry bridge OSM entity",
    )
    evidence_row = _embedded_csv_source_row(
        evidence.get("source_row"),
        GLOBAL_GEOMETRY_EVIDENCE_FIELDS,
        label="geometry bridge OSM evidence",
    )
    if (
        {field: value for field, value in entity.items() if field != "source_row"}
        != _global_geometry_entity_projection(entity_row)
        or {field: value for field, value in evidence.items() if field != "source_row"}
        != _global_geometry_evidence_projection(evidence_row)
    ):
        raise VerifiedConstructionCoreError("geometry bridge OSM projection differs")
    match = re.fullmatch(r"osm:(node|way|relation)/([1-9][0-9]*)", entity["stable_key"])
    if match is None:
        raise VerifiedConstructionCoreError("geometry bridge OSM identity differs")
    osm_type, osm_id = match.group(1), int(match.group(2))
    if (
        {field: entity["source_row"].get(field) for field in ("line", "bytes", "sha256")}
        != {
            "line": 8940,
            "bytes": 1154,
            "sha256": "ef3547df7dba55a747f603c66fdef578b680fa70fab621c731a9f076c73a0884",
        }
        or {
            field: evidence["source_row"].get(field)
            for field in ("line", "bytes", "sha256")
        }
        != {
            "line": 6483,
            "bytes": 334,
            "sha256": "3d22a24cad9f59f7d76bf858a7e9a6586ea4501a2264e09343900928b3831786",
        }
        or entity.get("name") != "STT Jakarta 1"
        or entity.get("latitude") != -6.37400965
        or entity.get("longitude") != 107.20106675
        or entity.get("tags")
        != {
            "building": "yes",
            "name": "STT Jakarta 1",
            "operator": "Singapore Technologies Telemedia",
            "operator:wikidata": "Q138426438",
            "ref": "Jakarta 1",
            "telecom": "data_center",
        }
        or evidence.get("content_hash")
        != "dcf9e3616e24371f0d9b0b49ff61da6ee1a31e4be3484826b967ae8b64357016"
        or not _point_inside_ring(
            [entity.get("longitude"), entity.get("latitude")],
            entity.get("geometry", {}).get("coordinates", [[]])[0],
        )
        or
        entity.get("entity_id")
        != atlas_stable_id("entity", entity["stable_key"], entity["entity_kind"])
        or entity.get("source_url")
        != f"https://www.openstreetmap.org/{osm_type}/{osm_id}"
        or entity.get("source_family") != "openstreetmap"
        or entity.get("source_license") != "ODbL-1.0"
        or entity.get("source_attribution") != "© OpenStreetMap contributors"
        or entity.get("status") != "unknown"
        or entity.get("status_method")
        != "osm_geometry_only_no_operational_inference"
        or entity.get("capacity_estimates_json") != []
        or entity.get("workloads_json") != []
        or evidence.get("evidence_id")
        != atlas_stable_id(
            "evidence",
            "osm",
            osm_type,
            str(osm_id),
            evidence.get("retrieved_at"),
            evidence.get("content_hash"),
        )
        or evidence.get("evidence_id") != entity.get("snapshot_evidence_id")
        or evidence.get("source_url") != entity.get("source_url")
        or evidence.get("license") != "ODbL-1.0"
        or evidence.get("attribution") != "© OpenStreetMap contributors"
    ):
        raise VerifiedConstructionCoreError("geometry bridge OSM semantics differ")
    _validate_polygon(entity.get("geometry"), label="geometry bridge OSM")
    rights = bridge.get("rights")
    if (
        not isinstance(rights, dict)
        or rights.get("geometry_license_url")
        != "https://www.openstreetmap.org/copyright"
        or rights.get("geometry_source")
        != "The frozen OSM geometry and object metadata are derived from OpenStreetMap under ODbL 1.0 and require attribution: © OpenStreetMap contributors."
        or rights.get("mixed_rights")
        != "Each upstream source retains its own rights; STT facts are not relicensed under ODbL."
    ):
        raise VerifiedConstructionCoreError("geometry bridge STT rights differ")
    if hydrated_crosscheck:
        for name, record, fields in (
            ("entities.csv", entity, GLOBAL_GEOMETRY_ENTITY_FIELDS),
            ("evidence.csv", evidence, GLOBAL_GEOMETRY_EVIDENCE_FIELDS),
        ):
            metadata = release["members"][name]
            payload_path = _repository_input(
                metadata["path"], metadata["sha256"], f"geometry bridge OSM {name}"
            )
            if payload_path.stat().st_size != metadata["bytes"]:
                raise VerifiedConstructionCoreError(
                    f"geometry bridge hydrated OSM payload differs: {name}"
                )
            _validate_hydrated_source_row(
                payload_path,
                record["source_row"],
                fields,
                label=f"geometry bridge OSM {name}",
            )


def _validate_v06_kvandal_geometry(bridge: Mapping[str, Any]) -> None:
    release = bridge["geometry_release"]
    if not isinstance(release, dict) or set(release) != {
        "release_id",
        "as_of",
        "recorded_at",
        "capture",
        "upstream_payload",
    }:
        raise VerifiedConstructionCoreError("geometry bridge Kvandal release differs")
    capture_binding = release["capture"]
    if (
        not isinstance(capture_binding, dict)
        or capture_binding
        != {
            "path": "sources/verified-construction-core-v0.6-kvandal-kartverket-wfs-capture.json",
            "bytes": 13990,
            "sha256": "677b6285401124aebccd95f0291987b843aa9866c04f18981fae3f451c4d529e",
        }
    ):
        raise VerifiedConstructionCoreError("geometry bridge Kvandal capture differs")
    capture_path = _repository_input(
        capture_binding["path"], capture_binding["sha256"], "Kvandal WFS capture"
    )
    if capture_path.stat().st_size != capture_binding["bytes"]:
        raise VerifiedConstructionCoreError("geometry bridge Kvandal capture bytes differ")
    capture = _load_json(capture_path)
    raw = capture.get("raw_capture") if isinstance(capture, dict) else None
    if not isinstance(raw, dict) or set(raw) != {
        "content_encoding",
        "transfer_decoding",
        "storage_encoding",
        "bytes",
        "sha256",
        "payload_base64",
    }:
        raise VerifiedConstructionCoreError("geometry bridge Kvandal payload differs")
    try:
        payload = base64.b64decode(raw["payload_base64"], validate=True)
    except (binascii.Error, TypeError) as error:
        raise VerifiedConstructionCoreError(
            "geometry bridge Kvandal payload differs"
        ) from error
    if (
        base64.b64encode(payload).decode("ascii") != raw["payload_base64"]
        or len(payload) != raw["bytes"]
        or _sha256_bytes(payload) != raw["sha256"]
        or raw["bytes"] != 4378
        or raw["sha256"]
        != "8d835fa69725193def708489f681c84bb12c384d71e263fca1739c3c539f997b"
        or release["upstream_payload"]
        != {"encoding": "base64", "bytes": raw["bytes"], "sha256": raw["sha256"]}
        or capture.get("captured_at") != release.get("recorded_at")
    ):
        raise VerifiedConstructionCoreError("geometry bridge Kvandal payload hash differs")
    wfs_namespace = "http://www.opengis.net/wfs/2.0"
    gml_namespace = "http://www.opengis.net/gml/3.2"
    app_namespace = (
        "http://skjema.geonorge.no/SOSI/produktspesifikasjon/"
        "Matrikkelen-Eiendomskart-Teig/20211101"
    )
    namespaces = {"wfs": wfs_namespace, "gml": gml_namespace, "app": app_namespace}
    try:
        root = ET.fromstring(payload)
    except ET.ParseError as error:
        raise VerifiedConstructionCoreError(
            "geometry bridge Kvandal XML differs"
        ) from error
    members = root.findall("wfs:member", namespaces)
    if (
        root.tag != f"{{{wfs_namespace}}}FeatureCollection"
        or len(members) != 1
        or len(members[0]) != 1
        or members[0][0].tag != f"{{{app_namespace}}}Teig"
    ):
        raise VerifiedConstructionCoreError("geometry bridge Kvandal XML member differs")
    teig = members[0][0]

    def one_text(path: str) -> str:
        matches = teig.findall(path, namespaces)
        if len(matches) != 1 or matches[0].text is None:
            raise VerifiedConstructionCoreError(
                "geometry bridge Kvandal XML feature differs"
            )
        return matches[0].text.strip()

    points = teig.findall("app:representasjonspunkt/gml:Point", namespaces)
    polygons = teig.findall("app:område/gml:Polygon", namespaces)
    if len(points) != 1 or len(polygons) != 1:
        raise VerifiedConstructionCoreError("geometry bridge Kvandal XML geometry differs")
    point = points[0]
    polygon = polygons[0]
    point_positions = point.findall("gml:pos", namespaces)
    polygon_positions = polygon.findall(
        "gml:exterior/gml:LinearRing/gml:posList", namespaces
    )
    if len(point_positions) != 1 or len(polygon_positions) != 1:
        raise VerifiedConstructionCoreError("geometry bridge Kvandal XML geometry differs")
    try:
        point_values = [float(value) for value in (point_positions[0].text or "").split()]
        polygon_values = [
            float(value) for value in (polygon_positions[0].text or "").split()
        ]
        stored_area = float(one_text("app:teigareal/app:Areal/app:lagretBeregnetAreal"))
    except ValueError as error:
        raise VerifiedConstructionCoreError(
            "geometry bridge Kvandal XML coordinate differs"
        ) from error
    if len(point_values) != 2 or len(polygon_values) < 8 or len(polygon_values) % 2:
        raise VerifiedConstructionCoreError("geometry bridge Kvandal XML coordinate differs")
    raw_point = {"type": "Point", "coordinates": point_values}
    raw_geometry = {
        "type": "Polygon",
        "coordinates": [
            [polygon_values[index : index + 2] for index in range(0, len(polygon_values), 2)]
        ],
    }
    gml_id = teig.get(f"{{{gml_namespace}}}id")
    teig_id = one_text("app:identTeig/app:IdentTeig/app:teigId")
    if (
        not gml_id
        or gml_id != f"teig.{teig_id}"
        or point.get(f"{{{gml_namespace}}}id")
        != f"{gml_id}_APP_REPRESENTASJONSPUNKT"
        or polygon.get(f"{{{gml_namespace}}}id") != f"{gml_id}_APP_OMRÅDE"
        or point.get("srsName") != "EPSG:4326"
        or polygon.get("srsName") != "EPSG:4326"
    ):
        raise VerifiedConstructionCoreError("geometry bridge Kvandal XML identity differs")
    feature = capture.get("feature")
    entity = bridge["geometry_entity"]
    evidence = bridge["geometry_evidence"]
    if (
        not isinstance(feature, dict)
        or not isinstance(entity, dict)
        or not isinstance(evidence, dict)
        or capture.get("request", {}).get("parameters", {}).get("service") != "WFS"
        or capture.get("request", {}).get("parameters", {}).get("version") != "2.0.0"
        or capture.get("request", {}).get("parameters", {}).get("request")
        != "GetFeature"
        or capture.get("request", {}).get("parameters", {}).get("typeNames")
        != "app:Teig"
        or capture.get("request", {}).get("parameters", {}).get("srsName")
        != "EPSG:4326"
        or "<fes:Literal>1806</fes:Literal>"
        not in capture.get("request", {}).get("parameters", {}).get("FILTER", "")
        or "<fes:Literal>10/760</fes:Literal>"
        not in capture.get("request", {}).get("parameters", {}).get("FILTER", "")
        or feature.get("gml_id") != gml_id
        or feature.get("teig_id") != teig_id
        or feature.get("uuid_teig") != one_text("app:uuidTeig")
        or feature.get("update_timestamp") != one_text("app:oppdateringsdato")
        or feature.get("municipality_number") != one_text("app:kommunenummer")
        or feature.get("municipality_name") != one_text("app:kommunenavn")
        or feature.get("cadastral_number_text")
        != one_text("app:matrikkelnummerTekst")
        or feature.get("property_type")
        != one_text("app:matrikkelenhet/app:Matrikkelenhet/app:matrikkelenhetstype")
        or str(feature.get("matrikkelenhet_id"))
        != one_text("app:matrikkelenhet/app:Matrikkelenhet/app:matrikkelenhetId")
        or feature.get("uuid_matrikkelenhet")
        != one_text("app:matrikkelenhet/app:Matrikkelenhet/app:uuidMatrikkelenhet")
        or feature.get("stored_calculated_area_square_metres") != stored_area
        or feature.get("accuracy_class_as_published")
        != one_text("app:noyaktighetsklasseTeig")
        or feature.get("coordinate_reference_system") != "EPSG:4326"
        or feature.get("coordinate_order_as_emitted") != "longitude_latitude"
        or feature.get("geometry") != raw_geometry
        or feature.get("geometry") != entity.get("geometry")
        or feature.get("official_representative_point") != raw_point
        or feature.get("official_representative_point")
        != {"type": "Point", "coordinates": [entity["longitude"], entity["latitude"]]}
        or entity.get("stable_key")
        != f"kartverket:matrikkelen-teig/{feature.get('teig_id')}"
        or entity.get("entity_id")
        != atlas_stable_id("entity", entity["stable_key"], "parcel")
        or entity.get("entity_kind") != "parcel"
        or entity.get("tags", {}).get("cadastral_number_text")
        != feature.get("cadastral_number_text")
        or entity.get("source_capture")
        != {
            **capture_binding,
            "raw_payload_bytes": raw["bytes"],
            "raw_payload_sha256": raw["sha256"],
        }
    ):
        raise VerifiedConstructionCoreError("geometry bridge Kvandal feature differs")
    _validate_polygon(entity["geometry"], label="geometry bridge Kvandal")
    if not _point_inside_ring(raw_point["coordinates"], raw_geometry["coordinates"][0]):
        raise VerifiedConstructionCoreError(
            "geometry bridge Kvandal representative point differs"
        )
    expected_evidence_id = atlas_stable_id(
        "evidence",
        "kartverket",
        "teig",
        feature["teig_id"],
        release["recorded_at"],
        raw["sha256"],
    )
    if (
        evidence.get("evidence_id") != expected_evidence_id
        or evidence.get("content_hash") != raw["sha256"]
        or evidence.get("capture_path") != capture_binding["path"]
        or entity.get("snapshot_evidence_id") != expected_evidence_id
        or entity.get("status_evidence_id") != expected_evidence_id
        or evidence.get("source_url") != entity.get("source_url")
        or evidence.get("publisher") != entity.get("source_publisher")
        or evidence.get("license") != "CC-BY-4.0"
        or evidence.get("attribution") != "Kartverket"
        or evidence.get("publisher") != "Kartverket / Statens kartverk"
        or evidence.get("kind") != "government_record"
        or evidence.get("title") != "Kartverket Matrikkelen parcel 1806-10/760"
        or evidence.get("source_family")
        != "kartverket_matrikkelen_eiendomskart_teig_wfs"
        or evidence.get("published_at") != "2026-04-10T07:48:05.975"
        or evidence.get("retrieved_at") != "2026-08-20T19:58:00Z"
        or evidence.get("source_url") != capture.get("request", {}).get("effective_url")
        or entity.get("source_url") != capture.get("request", {}).get("effective_url")
        or entity.get("source_publisher") != capture.get("source", {}).get("publisher")
        or capture.get("source", {}).get("publisher")
        != "Kartverket / Statens kartverk"
        or capture.get("source", {}).get("license") != "CC-BY-4.0"
    ):
        raise VerifiedConstructionCoreError("geometry bridge Kvandal evidence differs")
    rights = bridge.get("rights")
    if (
        not isinstance(rights, dict)
        or rights.get("geometry_license_url")
        != "https://creativecommons.org/licenses/by/4.0/"
        or rights.get("geometry_service_metadata_url")
        != capture.get("source", {}).get("service_metadata_url")
        or rights.get("geometry_dataset_metadata_url")
        != capture.get("source", {}).get("dataset_metadata_url")
        or rights.get("geometry_source")
        != "The exact Kartverket Matrikkelen-Eiendomskart-Teig WFS response and extracted parcel geometry are redistributed under Creative Commons Attribution 4.0 with attribution to Kartverket."
        or rights.get("mixed_rights")
        != "Each upstream source retains its own rights. The CC BY 4.0 geometry license does not relicense Nscale, Sentia, or NVE source-linked material."
    ):
        raise VerifiedConstructionCoreError("geometry bridge Kvandal rights differ")
    identity = bridge["identity_bridge_evidence"]
    if (
        not isinstance(identity, list)
        or len(identity) != 1
        or identity[0].get("map_identity", {}).get(
            "point_inside_captured_parcel_10_760"
        )
        is not True
        or not _point_inside_ring(
            [
                identity[0]["map_identity"]["derived_wgs84_longitude"],
                identity[0]["map_identity"]["derived_wgs84_latitude"],
            ],
            entity["geometry"]["coordinates"][0],
        )
    ):
        raise VerifiedConstructionCoreError("geometry bridge Kvandal identity differs")


def _parcel_union(rings: Sequence[Sequence[Sequence[float]]]) -> list[list[float]]:
    edge_counts: Counter[tuple[tuple[float, float], tuple[float, float]]] = Counter()
    for ring in rings:
        if len(ring) < 4 or ring[0] != ring[-1]:
            raise VerifiedConstructionCoreError("geometry bridge parcel ring differs")
        for first, second in zip(ring, ring[1:]):
            edge = tuple(sorted((tuple(first), tuple(second))))
            edge_counts[edge] += 1
    adjacency: dict[tuple[float, float], set[tuple[float, float]]] = defaultdict(set)
    for (first, second), count in edge_counts.items():
        if count == 1:
            adjacency[first].add(second)
            adjacency[second].add(first)
        elif count != 2:
            raise VerifiedConstructionCoreError("geometry bridge parcel topology differs")
    if not adjacency or any(len(neighbors) != 2 for neighbors in adjacency.values()):
        raise VerifiedConstructionCoreError("geometry bridge parcel union differs")
    start = min(adjacency)
    ring: list[tuple[float, float]] = [start]
    previous: tuple[float, float] | None = None
    current = start
    while True:
        following = sorted(point for point in adjacency[current] if point != previous)[0]
        if following == start:
            ring.append(start)
            break
        if following in ring or len(ring) > len(adjacency):
            raise VerifiedConstructionCoreError("geometry bridge parcel union differs")
        ring.append(following)
        previous, current = current, following
    if len(ring) != len(adjacency) + 1:
        raise VerifiedConstructionCoreError("geometry bridge parcel union differs")
    signed_area = sum(
        first[0] * second[1] - second[0] * first[1]
        for first, second in zip(ring, ring[1:])
    )
    points = ring[:-1] if signed_area > 0 else list(reversed(ring[:-1]))
    offset = points.index(min(points))
    points = points[offset:] + points[:offset]
    return [list(point) for point in [*points, points[0]]]


def _polygon_area_centroid(
    ring: Sequence[Sequence[float]],
) -> tuple[float, list[float]]:
    origin_x, origin_y = ring[0]
    shifted = [(x - origin_x, y - origin_y) for x, y in ring]
    crosses = [
        first[0] * second[1] - second[0] * first[1]
        for first, second in zip(shifted, shifted[1:])
    ]
    area = sum(crosses) / 2
    if area <= 0:
        raise VerifiedConstructionCoreError("geometry bridge parcel area differs")
    centroid_x = origin_x + sum(
        (first[0] + second[0]) * cross
        for first, second, cross in zip(shifted, shifted[1:], crosses)
    ) / (6 * area)
    centroid_y = origin_y + sum(
        (first[1] + second[1]) * cross
        for first, second, cross in zip(shifted, shifted[1:], crosses)
    ) / (6 * area)
    return area, [centroid_x, centroid_y]


def _validate_v06_undheim_geometry(bridge: Mapping[str, Any]) -> None:
    release = bridge["geometry_release"]
    if not isinstance(release, dict) or set(release) != {
        "release_id",
        "as_of",
        "recorded_at",
        "manifest",
        "members",
    }:
        raise VerifiedConstructionCoreError("geometry bridge Undheim release differs")
    manifest_binding = release["manifest"]
    if not isinstance(manifest_binding, dict) or set(manifest_binding) != {
        "path",
        "bytes",
        "sha256",
    }:
        raise VerifiedConstructionCoreError(
            "geometry bridge Undheim capture manifest binding differs"
        )
    manifest_path = _repository_input(
        manifest_binding["path"],
        manifest_binding["sha256"],
        "geometry bridge Undheim capture manifest",
    )
    if manifest_path.stat().st_size != manifest_binding["bytes"]:
        raise VerifiedConstructionCoreError(
            "geometry bridge Undheim capture manifest bytes differ"
        )
    manifest = _load_json(manifest_path)
    manifest_members = manifest.get("members")
    expected_member_facts = {
        "openapi.json": {
            "bytes": 15558,
            "sha256": "933a65c7f47348f4490899f539481d9e88ee3d50b98f3d6531b34321d57d14bc",
            "url": "https://api.kartverket.no/eiendom/v1/openapi.json",
        },
        "1121-46-316-epsg4258.json": {
            "bytes": 1381,
            "sha256": "46c662a0a110ec84b7bbd550852892d975373597341955c5c95feab57445e3ea",
            "url": "https://api.kartverket.no/eiendom/v1/geokoding?matrikkelnummer=1121-46%2F316&omrade=true&utkoordsys=4258",
        },
        "1121-46-316-epsg25832.json": {
            "bytes": 1509,
            "sha256": "24ab438f6bce9a6656a697c1bf367c4c7042f74b17ae9c4f9db6e0ba8ffde14c",
            "url": "https://api.kartverket.no/eiendom/v1/geokoding?matrikkelnummer=1121-46%2F316&omrade=true&utkoordsys=25832",
        },
        "1121-46-317-epsg4258.json": {
            "bytes": 1630,
            "sha256": "15533ed85e1f3268feea36417158c8024078dcb61c1770162f19cb22128bfeff",
            "url": "https://api.kartverket.no/eiendom/v1/geokoding?matrikkelnummer=1121-46%2F317&omrade=true&utkoordsys=4258",
        },
        "1121-46-317-epsg25832.json": {
            "bytes": 1775,
            "sha256": "1d9cf20ac76ae509fe51420f2ca33847565a778c38a19af38fb9cd349947f252",
            "url": "https://api.kartverket.no/eiendom/v1/geokoding?matrikkelnummer=1121-46%2F317&omrade=true&utkoordsys=25832",
        },
        "1121-46-319-epsg4258.json": {
            "bytes": 944,
            "sha256": "5be1e9b90190df40d27d5b1e640cd736f151e4dd7a5a1d40bf7f58cdf9ced11f",
            "url": "https://api.kartverket.no/eiendom/v1/geokoding?matrikkelnummer=1121-46%2F319&omrade=true&utkoordsys=4258",
        },
        "1121-46-319-epsg25832.json": {
            "bytes": 1030,
            "sha256": "332139a1c5f55a343fcc23f0e5aa353e81d197404a22026589f68de2179701f0",
            "url": "https://api.kartverket.no/eiendom/v1/geokoding?matrikkelnummer=1121-46%2F319&omrade=true&utkoordsys=25832",
        },
    }
    if (
        manifest_path.name != "manifest.json"
        or not isinstance(manifest_members, dict)
        or set(manifest_members) != set(expected_member_facts)
        or manifest.get("schema_id")
        != "datacenter-atlas-public-api-capture-manifest-v1"
        or manifest.get("capture_id")
        != "verified-construction-core-v0.6-undheim-kartverket-capture"
        or not isinstance(manifest.get("source"), dict)
        or manifest["source"].get("api_base_url")
        != "https://api.kartverket.no/eiendom/v1"
        or manifest["source"].get("license") != "CC-BY-4.0"
        or not isinstance(manifest.get("capture"), dict)
        or manifest["capture"].get("authentication") != "none"
        or manifest["capture"].get("http_status") != 200
        or manifest["capture"].get("last_response_at")
        != release.get("recorded_at")
        or release["members"]
        != {
            name: {field: binding[field] for field in ("path", "bytes", "sha256")}
            for name, binding in manifest_members.items()
        }
        or any(
            binding.get("bytes") != expected_member_facts[name]["bytes"]
            or binding.get("sha256") != expected_member_facts[name]["sha256"]
            or binding.get("url") != expected_member_facts[name]["url"]
            for name, binding in manifest_members.items()
        )
    ):
        raise VerifiedConstructionCoreError("geometry bridge Undheim members differ")
    member_payloads: dict[str, dict[str, Any]] = {}
    for name, binding in release["members"].items():
        path = _repository_input(
            binding["path"], binding["sha256"], f"Undheim capture {name}"
        )
        if path.stat().st_size != binding["bytes"]:
            raise VerifiedConstructionCoreError(
                f"geometry bridge Undheim member bytes differ: {name}"
            )
        member_payloads[name] = _load_json(path)
    display_names = [
        f"1121-46-{number}-epsg4258.json" for number in (316, 317, 319)
    ]
    metric_names = [
        f"1121-46-{number}-epsg25832.json" for number in (316, 317, 319)
    ]
    parcel_facts = {
        316: (6492542413, "2025-07-02T10:05:05"),
        317: (6458360571, "2025-07-01T10:08:08"),
        319: (6461028274, "2025-07-01T09:20:20"),
    }

    def capture_feature(name: str, parcel_number: int, epsg: int) -> dict[str, Any]:
        payload = member_payloads[name]
        features = payload.get("features") if isinstance(payload, dict) else None
        expected_crs = (
            None
            if epsg == 4258
            else {"properties": {"name": "EPSG:25832"}, "type": "name"}
        )
        if (
            not isinstance(payload, dict)
            or set(payload) != ({"features", "type"} if epsg == 4258 else {"crs", "features", "type"})
            or payload.get("type") != "FeatureCollection"
            or payload.get("crs") != expected_crs
            or not isinstance(features, list)
            or len(features) != 1
        ):
            raise VerifiedConstructionCoreError(
                f"geometry bridge Undheim capture differs: {name}"
            )
        feature = features[0]
        geometry = feature.get("geometry") if isinstance(feature, dict) else None
        properties = feature.get("properties") if isinstance(feature, dict) else None
        local_id, updated_at = parcel_facts[parcel_number]
        expected_properties = {
            "bruksnummer": parcel_number,
            "festenummer": 0,
            "gardsnummer": 46,
            "hovedområde": True,
            "kommunenummer": "1121",
            "lokalid": local_id,
            "matrikkelnummertekst": f"46/{parcel_number}",
            "nøyaktighetsklasseteig": "Grønt",
            "objekttype": "Teig",
            "oppdateringsdato": updated_at,
            "seksjonsnummer": 0,
            "teigmedflerematrikkelenheter": False,
            "uregistrertjordsameie": False,
        }
        if (
            set(feature) != {"geometry", "properties", "type"}
            or feature.get("type") != "Feature"
            or properties != expected_properties
            or not isinstance(geometry, dict)
        ):
            raise VerifiedConstructionCoreError(
                f"geometry bridge Undheim feature differs: {name}"
            )
        if epsg == 4258:
            _validate_polygon(geometry, label=f"geometry bridge Undheim {name}")
        else:
            coordinates = geometry.get("coordinates")
            ring = coordinates[0] if isinstance(coordinates, list) and len(coordinates) == 1 else None
            if (
                geometry.get("type") != "Polygon"
                or not isinstance(ring, list)
                or len(ring) < 4
                or ring[0] != ring[-1]
                or any(
                    not isinstance(point, list)
                    or len(point) != 2
                    or not all(_finite_number(value) for value in point)
                    for point in ring
                )
            ):
                raise VerifiedConstructionCoreError(
                    f"geometry bridge Undheim metric geometry differs: {name}"
                )
        return feature

    display_features = [
        capture_feature(name, parcel_number, 4258)
        for name, parcel_number in zip(display_names, (316, 317, 319))
    ]
    metric_features = [
        capture_feature(name, parcel_number, 25832)
        for name, parcel_number in zip(metric_names, (316, 317, 319))
    ]
    for display_feature, metric_feature in zip(display_features, metric_features):
        if display_feature["properties"] != metric_feature["properties"]:
            raise VerifiedConstructionCoreError(
                "geometry bridge Undheim paired feature differs"
            )
    display_ring = _parcel_union(
        [feature["geometry"]["coordinates"][0] for feature in display_features]
    )
    metric_ring = _parcel_union(
        [feature["geometry"]["coordinates"][0] for feature in metric_features]
    )
    area, centroid = _polygon_area_centroid(metric_ring)
    entity = bridge["geometry_entity"]
    evidence = bridge["geometry_evidence"]
    derivation = entity.get("derivation") if isinstance(entity, dict) else None
    geometry = {"type": "Polygon", "coordinates": [display_ring]}
    metric_geometry = {"type": "Polygon", "coordinates": [metric_ring]}
    canonical_geometry = json.dumps(
        geometry, ensure_ascii=False, sort_keys=True, separators=(",", ":")
    ).encode("utf-8")
    canonical_metric = json.dumps(
        metric_geometry, ensure_ascii=False, sort_keys=True, separators=(",", ":")
    ).encode("utf-8")
    if (
        not isinstance(entity, dict)
        or not isinstance(evidence, dict)
        or not isinstance(derivation, dict)
        or entity.get("entity_kind") != "campus"
        or entity.get("geometry") != geometry
        or [entity.get("longitude"), entity.get("latitude")]
        != [5.7940859, 58.6593348]
        or derivation.get("method") != "exact_shared_edge_cancellation_union"
        or derivation.get("display_crs") != 4258
        or derivation.get("metric_crs") != 25832
        or derivation.get("union_geometry_type") != "Polygon"
        or derivation.get("union_ring_vertices_including_closure") != 102
        or derivation.get("area_m2") != round(area, 6)
        or derivation.get("area_m2") != 64015.998576
        or any(
            not math.isclose(actual, expected, abs_tol=1e-9)
            for actual, expected in zip(derivation.get("centroid_epsg25832", []), centroid)
        )
        or derivation.get("centroid_epsg25832")
        != [314041.3340706079, 6506564.987444305]
        or derivation.get("representative_point_epsg4258")
        != [5.7940859, 58.6593348]
        or derivation.get("canonical_geometry_bytes") != len(canonical_geometry)
        or derivation.get("canonical_geometry_sha256")
        != _sha256_bytes(canonical_geometry)
        or derivation.get("canonical_geometry_sha256")
        != "fd26948d9030a8c944e64759e6e9dc3d513969d8a8fe4e82563f75b14ff52c37"
        or derivation.get("canonical_metric_geometry_bytes") != len(canonical_metric)
        or derivation.get("canonical_metric_geometry_sha256")
        != _sha256_bytes(canonical_metric)
        or derivation.get("canonical_metric_geometry_sha256")
        != "de076d1ae86ace9ca28b8eda72e8254e695be68b2b0bdae74255e38c128d7b0c"
        or not _point_inside_ring(
            [entity.get("longitude"), entity.get("latitude")], display_ring
        )
    ):
        raise VerifiedConstructionCoreError("geometry bridge Undheim union differs")
    source_features = entity.get("source_features")
    expected_source_features = []
    for parcel_number in (316, 317, 319):
        local_id = parcel_facts[parcel_number][0]
        for epsg in (4258, 25832):
            member = f"1121-46-{parcel_number}-epsg{epsg}.json"
            binding = expected_member_facts[member]
            expected_source_features.append(
                {
                    "member": member,
                    "json_pointer": "/features/0",
                    "matrikkelnummer": f"1121-46/{parcel_number}",
                    "epsg": epsg,
                    "lokalid": local_id,
                    "response_bytes": binding["bytes"],
                    "response_sha256": binding["sha256"],
                }
            )
    if source_features != expected_source_features:
        raise VerifiedConstructionCoreError("geometry bridge Undheim sources differ")
    for row in source_features:
        binding = release["members"].get(row["member"])
        if binding is None or (
            row.get("response_bytes") != binding["bytes"]
            or row.get("response_sha256") != binding["sha256"]
        ):
            raise VerifiedConstructionCoreError(
                "geometry bridge Undheim source binding differs"
            )
    capture_set = manifest.get("geometry_capture_set")
    capture_set_payload = {
        "schema": capture_set.get("schema") if isinstance(capture_set, dict) else None,
        "members": {
            name: {
                "bytes": manifest_members[name]["bytes"],
                "sha256": manifest_members[name]["sha256"],
            }
            for name in manifest_members
            if name != "openapi.json"
        },
    }
    canonical_capture_set = json.dumps(
        capture_set_payload,
        ensure_ascii=False,
        sort_keys=True,
        separators=(",", ":"),
    ).encode("utf-8")
    if (
        not isinstance(capture_set, dict)
        or set(capture_set)
        != {"schema", "member_scope", "canonicalization", "canonical_bytes", "sha256"}
        or capture_set.get("schema")
        != "kartverket-open-property-api-capture-set-v1"
        or capture_set.get("canonical_bytes") != len(canonical_capture_set)
        or capture_set.get("sha256") != _sha256_bytes(canonical_capture_set)
        or capture_set.get("sha256")
        != "6bcd905d036ecde8e55ef4933bf1502f58a58701e5784583fdc3bccc5c6edd1f"
        or evidence.get("content_hash") != capture_set.get("sha256")
    ):
        raise VerifiedConstructionCoreError(
            "geometry bridge Undheim evidence hash differs"
        )
    expected_member_order = [
        "1121-46-316-epsg4258.json",
        "1121-46-316-epsg25832.json",
        "1121-46-317-epsg4258.json",
        "1121-46-317-epsg25832.json",
        "1121-46-319-epsg4258.json",
        "1121-46-319-epsg25832.json",
    ]
    manifest_source = manifest["source"]
    if (
        evidence.get("source_members") != expected_member_order
        or evidence.get("license") != "CC-BY-4.0"
        or evidence.get("attribution") != "© Kartverket"
        or evidence.get("publisher") != "Kartverket"
        or evidence.get("kind") != "government_cadastral_api"
        or evidence.get("title")
        != "Kartverket matrikkelteig union 1121-46/316, 1121-46/317 and 1121-46/319"
        or evidence.get("source_family") != "kartverket_open_property_api"
        or evidence.get("published_at") is not None
        or evidence.get("retrieved_at") != "2026-08-20T19:49:51Z"
        or evidence.get("source_url") != manifest_source.get("api_base_url")
        or entity.get("source_url") != manifest_source.get("api_base_url")
        or entity.get("source_publisher") != manifest_source.get("publisher")
        or entity.get("source_license") != manifest_source.get("license")
    ):
        raise VerifiedConstructionCoreError("geometry bridge Undheim evidence differs")
    rights = bridge.get("rights")
    if (
        not isinstance(rights, dict)
        or rights.get("geometry_license_url")
        != "https://www.kartverket.no/en/api-and-data/terms-of-use"
        or rights.get("geometry_source")
        != "The exact Kartverket API response bodies and derived cadastral geometry are redistributed under CC BY 4.0 with attribution: © Kartverket."
        or rights.get("mixed_rights")
        != "Each upstream source retains its own rights; this bridge does not relicense Green Mountain, Time municipality, JoB Arkitekter, or Backe material under CC BY 4.0."
    ):
        raise VerifiedConstructionCoreError("geometry bridge Undheim rights differ")
    constituents = entity.get("constituent_parcels")
    if not isinstance(constituents, list) or len(constituents) != 3:
        raise VerifiedConstructionCoreError(
            "geometry bridge Undheim constituent parcels differ"
        )
    for row, parcel_number, feature in zip(
        constituents, (316, 317, 319), metric_features
    ):
        ring = feature["geometry"]["coordinates"][0]
        origin_x, origin_y = ring[0]
        shifted_ring = [
            [point[0] - origin_x, point[1] - origin_y] for point in ring
        ]
        signed_area = sum(
            first[0] * second[1] - second[0] * first[1]
            for first, second in zip(shifted_ring, shifted_ring[1:])
        ) / 2
        local_id, updated_at = parcel_facts[parcel_number]
        if (
            not isinstance(row, dict)
            or set(row)
            != {
                "matrikkelnummer",
                "lokalid",
                "oppdateringsdato",
                "nøyaktighetsklasseteig",
                "area_m2",
            }
            or row.get("matrikkelnummer") != f"1121-46/{parcel_number}"
            or row.get("lokalid") != local_id
            or row.get("oppdateringsdato") != updated_at
            or row.get("nøyaktighetsklasseteig") != "Grønt"
            or not math.isclose(
                row.get("area_m2", -1), abs(signed_area), abs_tol=1e-6
            )
        ):
            raise VerifiedConstructionCoreError(
                "geometry bridge Undheim constituent parcels differ"
            )


def _validate_v06_review_semantics(
    bridge: Mapping[str, Any], project: Mapping[str, Any]
) -> None:
    expected = V06_BRIDGE_SEMANTICS.get(project.get("stable_key"))
    decision = bridge["review_decision"]
    expected_fields = {
        "decision",
        "geometry_target_entity_kind",
        "geometry_derivation",
        "geometry_method",
        "geometry_scope_class",
        "geometry_authority_class",
        "geometry_use_scope",
        "horizontal_uncertainty_metres",
        "horizontal_uncertainty_unknown_reason",
        "identity_basis",
        "precision_scope",
        "rejected_claims",
    }
    if not isinstance(decision, dict) or set(decision) not in {
        frozenset(expected_fields),
        frozenset(expected_fields | {"accepted_claims"}),
    } or expected is None:
        raise VerifiedConstructionCoreError("geometry bridge v0.6 review fields differ")
    if any(
        decision.get(field) != expected[field]
        for field in (
            "decision",
            "geometry_target_entity_kind",
            "geometry_derivation",
            "geometry_method",
            "geometry_scope_class",
            "geometry_authority_class",
            "geometry_use_scope",
            "horizontal_uncertainty_unknown_reason",
        )
    ) or bridge["geometry_entity"].get("stable_key") != expected[
        "geometry_stable_key"
    ]:
        raise VerifiedConstructionCoreError("geometry bridge v0.6 semantics differ")
    rejected = decision.get("rejected_claims")
    if (
        decision.get("horizontal_uncertainty_metres") is not None
        or not isinstance(decision.get("horizontal_uncertainty_unknown_reason"), str)
        or not decision["horizontal_uncertainty_unknown_reason"]
        or not isinstance(decision.get("identity_basis"), str)
        or not decision["identity_basis"]
        or not isinstance(decision.get("precision_scope"), str)
        or not decision["precision_scope"]
        or not isinstance(rejected, list)
        or len(rejected) != len(set(rejected))
        or not any("footprint" in claim for claim in rejected)
        or not any("construction_extent" in claim for claim in rejected)
        or not any("geometry_derived" in claim for claim in rejected)
    ):
        raise VerifiedConstructionCoreError("geometry bridge v0.6 guardrails differ")
    imagery = bridge["imagery_posture"]
    if (
        not isinstance(imagery, dict)
        or not {"outcome", "independent_imagery_verification", "basis"}
        <= set(imagery)
        or imagery.get("outcome") != expected["imagery_outcome"]
        or imagery.get("independent_imagery_verification") is not False
        or not isinstance(imagery.get("basis"), str)
        or not imagery["basis"]
    ):
        raise VerifiedConstructionCoreError("geometry bridge v0.6 imagery differs")
    guardrails = bridge.get("claim_guardrails")
    if guardrails is not None and (
        not isinstance(guardrails, dict)
        or set(guardrails)
        != {"capacity_scope", "energy_scope", "role_scope", "workload_scope"}
        or any(not isinstance(value, str) or not value for value in guardrails.values())
    ):
        raise VerifiedConstructionCoreError("geometry bridge v0.6 claim guardrails differ")


def _validate_v06_identity_evidence(
    bridge: Mapping[str, Any], project: Mapping[str, Any]
) -> None:
    project_key = project.get("stable_key")
    expected_contracts = {
        "curated:adaniconnex-pune-data-center-campus:pnq04-current-build": [],
        "curated:goodman-hkg09-kwai-chung-data-centre:current-redevelopment": [
            (
                "goodman-hkg09-current-pipeline-address-captured-2026-08-20",
                "goodman_hong_kong_data_centre_pipeline",
                "3b2c00da63973852fd2ede105850be9a439760b06f1f6fa2a2e9718a9abef16f",
            ),
            (
                "hk-landsd-location-search-57-61-ta-chuen-ping-street-captured-2026-08-20",
                "hong_kong_lands_department_location_search_api",
                "69d3478b64d1fd693368ba681e32b8f9bd9cbd1f5dc3b689f1145365c78402ce",
            ),
        ],
        "curated:green-mountain-undheim-campus:current-two-building-development": [
            (
                "time-municipality-case-2025-1487-captured-2026-08-20",
                "time_municipality_public_archive",
                "6fb7fb7c78b8dc083bcd3617cafad6acff9cd8cb98cf456c269b2773d2c130fd",
            ),
            (
                "time-municipality-undheim-permit-vuu1-captured-2026-08-20",
                "time_municipality_public_archive",
                "a8e374f9b26193c9810816fec3edc41854e8cbc69ecd31a5a5bf733dc540b847",
            ),
            (
                "time-municipality-undheim-approved-situation-plan-green-mountain-captured-2026-08-20",
                "time_municipality_public_archive",
                "3e6f0f59d3bd8112f2351f3a45fbce57d1c4b0383fc1798e86279e7a7df27bb2",
            ),
            (
                "time-municipality-undheim-approved-area-overview-green-mountain-captured-2026-08-20",
                "time_municipality_public_archive",
                "b5567fc90c922ced1a1eff3aa0bbf9d8e762538045496f0a1d16fe4a1ffee680",
            ),
            (
                "backe-undheim-green-mountain-contractor-page-captured-2026-08-20",
                "backe_project_pages",
                "5aeecd8c2bdaa189b46dddb6e6366a2f16847cb0e024f575146770efb7f06d86",
            ),
        ],
        "curated:nscale-kvandal-narvik-ai-data-center-campus:initial-25mw-epc-current-build": [
            (
                "nve-202521094-kvandal-two-data-centers-application-captured-2026-08-20",
                "nve_concession_case_files",
                "8ee750f67e722e6d6ea0fdf415eb30140651c020c994ec4596a852793f65c58a",
            )
        ],
        "curated:skygard-osl1-hovinbyen-campus:phase-2": [
            (
                "skygard-osl1-current-page-captured-2026-08-20",
                "skygard_official_facility_page",
                "b6af40e5edbf7edb82fa9ee5c6b97a2342aea9907a4de1c2ca032e8c09aa6f51",
            ),
            (
                "nve-skygard-ostre-aker-vei-24c-case-captured-2026-08-20",
                "nve_concession_cases",
                "8c3c3e1fc35ed9429510a8d93413a25dbe00f12f1017f075997bc13ed6ad0eae",
            ),
        ],
    }
    stt_contract = [
        (
            "stt-jakarta-campus-expansion-2026-06-10-captured-2026-07-19",
            "stt_gdc_newsroom",
            "f56371010a4c86f72e48fec5078c3b0bb8441cf46d97fb43a1e85f52980fc676",
        ),
        (
            "stt-jakarta-1-factsheet-august-2025-captured-2026-08-20",
            "stt_gdc_facility_factsheets",
            "31aed9991d53e01b94ff65b9e06b1bd3ba0f7c3da0ec7a90de8b6a1e5e068f60",
        ),
    ]
    if project_key in {
        f"curated:stt-jakarta-data-centre-campus:stt-jakarta-{number}"
        for number in (3, 5, 6)
    }:
        expected = stt_contract
    else:
        expected = expected_contracts.get(project_key)
    evidence = bridge.get("identity_bridge_evidence")
    if (
        expected is None
        or not isinstance(evidence, list)
        or [
            (item.get("key"), item.get("source_family"), item.get("content_hash"))
            for item in evidence
            if isinstance(item, dict)
        ]
        != expected
        or any(not isinstance(item, dict) for item in evidence)
    ):
        raise VerifiedConstructionCoreError(
            "geometry bridge v0.6 identity evidence cohort differs"
        )
    by_key = {item["key"]: item for item in evidence}

    def object_field(item: Mapping[str, Any], field: str) -> dict[str, Any]:
        value = item.get(field)
        return value if isinstance(value, dict) else {}

    if project_key and "stt-jakarta-data-centre-campus" in project_key:
        expansion = object_field(by_key[stt_contract[0][0]], "fact_payload")
        factsheet = object_field(by_key[stt_contract[1][0]], "fact_payload")
        if (
            expansion.get("campus_expansion_events")
            != {
                "STT Jakarta 3": "topping out",
                "STT Jakarta 5": "groundbreaking",
                "STT Jakarta 6": "groundbreaking",
            }
            or expansion.get("campus_name_as_reported")
            != "STT Jakarta data centre campus"
            or factsheet.get("facility_name") != "STT Jakarta 1"
            or factsheet.get("campus_relation_as_reported")
            != "Interconnected within the STT Jakarta data centre campus"
            or not str(factsheet.get("location_as_reported", "")).startswith(
                "Block EA-1, Greenland International Industrial Centre"
            )
        ):
            raise VerifiedConstructionCoreError(
                "geometry bridge STT identity facts differ"
            )
    elif project_key == "curated:goodman-hkg09-kwai-chung-data-centre:current-redevelopment":
        goodman = object_field(by_key[expected[0][0]], "fact_payload")
        lands = object_field(by_key[expected[1][0]], "fact_payload")
        selected_result = object_field(lands, "selected_result")
        if (
            goodman.get("facility_code") != "HKG09"
            or goodman.get("address_as_reported")
            != "57-61 Ta Chuen Ping St, Kwai Chung, Hong Kong"
            or lands.get("selected_result_index") != 0
            or selected_result.get("addressEN") != "57-61 TA CHUEN PING STREET  "
            or selected_result.get("x") != 832091.0
            or selected_result.get("y") != 825415.0
        ):
            raise VerifiedConstructionCoreError(
                "geometry bridge Goodman identity facts differ"
            )
    elif project_key == "curated:skygard-osl1-hovinbyen-campus:phase-2":
        skygard = object_field(by_key[expected[0][0]], "fact_payload")
        nve = object_field(by_key[expected[1][0]], "fact_payload")
        if (
            skygard.get("site_code") != "OSL1"
            or skygard.get("site_label") != "OSL1 - Hovinbyen, Oslo"
            or nve.get("case_number") != "19357"
            or nve.get("case_title") != "Datasenter Østre Aker vei 24C"
            or nve.get("applicant") != "SKYGARD 1 AS"
        ):
            raise VerifiedConstructionCoreError(
                "geometry bridge Skygard identity facts differ"
            )
    elif project_key == "curated:green-mountain-undheim-campus:current-two-building-development":
        permit = object_field(by_key[expected[1][0]], "extraction")
        situation = object_field(by_key[expected[2][0]], "extraction")
        overview = object_field(by_key[expected[3][0]], "extraction")
        contractor = object_field(by_key[expected[4][0]], "extraction")
        parcels = ["1121-46/316", "1121-46/317", "1121-46/319"]
        if (
            by_key[expected[0][0]].get("case_number") != "2025/1487"
            or permit.get("applicant") != "VUU1 AS"
            or permit.get("permit_parcels") != parcels[1:]
            or situation.get("client_project_owner") != "Green Mountain AS"
            or situation.get("project_parcels") != parcels
            or overview.get("client_project_owner") != "Green Mountain AS"
            or overview.get("project_parcels") != parcels
            or overview.get("reported_parcel_area_sum_m2") != 64016.0
            or contractor.get("client") != "Green Mountain AS"
        ):
            raise VerifiedConstructionCoreError(
                "geometry bridge Undheim identity facts differ"
            )
    elif project_key == (
        "curated:nscale-kvandal-narvik-ai-data-center-campus:"
        "initial-25mw-epc-current-build"
    ):
        nve = evidence[0]
        case_facts = object_field(nve, "case_facts")
        map_identity = object_field(nve, "map_identity")
        if (
            case_facts.get("nve_case_number") != "202521094"
            or case_facts.get("location")
            != "Skoglund industrial area in Kvandal, north of Bjerkvik, Narvik municipality"
            or map_identity.get("figures") != [3, 4]
            or map_identity.get("source_coordinate_reference_system")
            != "EPSG:25833"
            or map_identity.get("map_center_easting") != 605178.71
            or map_identity.get("map_center_northing") != 7609294.92
            or map_identity.get("derived_wgs84_longitude")
            != 17.580864522839857
            or map_identity.get("derived_wgs84_latitude") != 68.5760344679832
            or map_identity.get("point_inside_captured_parcel_10_760") is not True
        ):
            raise VerifiedConstructionCoreError(
                "geometry bridge Kvandal identity facts differ"
            )


def _validate_v06_geometry_bridge(
    path_text: str,
    expected_sha256: str,
    *,
    hydrated_crosscheck: bool,
) -> dict[str, Any]:
    path = _repository_input(path_text, expected_sha256, "geometry bridge")
    bridge = _load_json(path)
    expected_top = {
        "schema_version",
        "bridge_id",
        "reviewed_at",
        "construction_source",
        "identity_bridge_evidence",
        "geometry_release",
        "geometry_entity",
        "geometry_evidence",
        "lineage_context",
        "imagery_posture",
        "review_decision",
        "rights",
    }
    if not isinstance(bridge, dict) or set(bridge) not in {
        frozenset(expected_top),
        frozenset(expected_top | {"claim_guardrails"}),
    }:
        raise VerifiedConstructionCoreError("geometry bridge v0.6 fields differ")
    if (
        bridge.get("schema_version") not in {1, 2}
        or bridge.get("reviewed_at") != REVIEW_DATE.isoformat()
        or not str(bridge.get("bridge_id", "")).startswith(
            "verified-construction-core-v0.6-"
        )
        or bridge.get("lineage_context") is not None
    ):
        raise VerifiedConstructionCoreError("geometry bridge v0.6 identity differs")
    project, campus = _validate_v06_construction_source(
        bridge, hydrated_crosscheck=hydrated_crosscheck
    )
    _validate_v06_review_semantics(bridge, project)
    _validate_v06_identity_evidence(bridge, project)
    geometry_stable_key = bridge["geometry_entity"].get("stable_key")
    if geometry_stable_key == "osm:way/1533569836":
        _validate_v06_osm_geometry(
            bridge, hydrated_crosscheck=hydrated_crosscheck
        )
    elif geometry_stable_key == "kartverket:matrikkelen-teig/6508960482":
        _validate_v06_kvandal_geometry(bridge)
    elif geometry_stable_key == (
        "kartverket:matrikkelteig-union/1121-46/316+317+319"
    ):
        _validate_v06_undheim_geometry(bridge)
    else:
        _validate_v06_official_point_geometry(bridge, project, campus)
    identity_evidence = bridge["identity_bridge_evidence"]
    if not isinstance(identity_evidence, list):
        raise VerifiedConstructionCoreError("geometry bridge v0.6 identity evidence differs")
    for index, evidence in enumerate(identity_evidence):
        if "fact_payload" in evidence:
            _validate_v06_fact_evidence(
                evidence, label=f"geometry bridge identity evidence {index}"
            )
        elif (
            not isinstance(evidence, dict)
            or not isinstance(evidence.get("source_url"), str)
            or not evidence["source_url"]
            or not isinstance(evidence.get("content_hash"), str)
            or re.fullmatch(r"[0-9a-f]{64}", evidence["content_hash"]) is None
        ):
            raise VerifiedConstructionCoreError(
                f"geometry bridge identity evidence {index} differs"
            )
    rights = bridge["rights"]
    base_rights_fields = {
        "construction_source",
        "identity_bridge_evidence",
        "geometry_source",
        "geometry_license_url",
        "lineage_context",
        "mixed_rights",
    }
    kvandal_rights_fields = base_rights_fields | {
        "geometry_service_metadata_url",
        "geometry_dataset_metadata_url",
        "required_attribution",
    }
    if (
        not isinstance(rights, dict)
        or set(rights)
        != (
            kvandal_rights_fields
            if geometry_stable_key == "kartverket:matrikkelen-teig/6508960482"
            else base_rights_fields
        )
        or rights.get("lineage_context") is not None
        or any(
            not isinstance(rights.get(field), str) or not rights[field]
            for field in ("construction_source", "geometry_source", "mixed_rights")
        )
    ):
        raise VerifiedConstructionCoreError("geometry bridge v0.6 rights differ")
    if geometry_stable_key == "kartverket:matrikkelen-teig/6508960482" and (
        "Kartverket" not in str(rights.get("required_attribution"))
        or not all(
            isinstance(rights.get(field), str) and rights[field].startswith("https://")
            for field in (
                "geometry_service_metadata_url",
                "geometry_dataset_metadata_url",
                "geometry_license_url",
            )
        )
    ):
        raise VerifiedConstructionCoreError("geometry bridge Kvandal rights differ")
    if set(_geometry_evidence_projection(bridge)) != set(EVIDENCE_FIELDS) - {
        "roles_json",
        "project_ids_json",
    }:
        raise VerifiedConstructionCoreError(
            "geometry bridge v0.6 evidence projection differs"
        )
    return bridge


def _validate_geometry_bridge(
    path_text: str,
    expected_sha256: str,
    *,
    hydrated_crosscheck: bool,
) -> dict[str, Any]:
    if Path(path_text).stem.startswith("verified-construction-core-v0.6-"):
        return _validate_v06_geometry_bridge(
            path_text,
            expected_sha256,
            hydrated_crosscheck=hydrated_crosscheck,
        )
    return _validate_legacy_geometry_bridge(
        path_text,
        expected_sha256,
        hydrated_crosscheck=hydrated_crosscheck,
    )


def _validate_legacy_geometry_bridge(
    path_text: str,
    expected_sha256: str,
    *,
    hydrated_crosscheck: bool,
) -> dict[str, Any]:
    path = _repository_input(path_text, expected_sha256, "geometry bridge")
    bridge = _load_json(path)
    expected_top = {
        "schema_version",
        "bridge_id",
        "reviewed_at",
        "construction_source",
        "identity_bridge_evidence",
        "geometry_release",
        "geometry_entity",
        "geometry_evidence",
        "lineage_context",
        "imagery_posture",
        "review_decision",
        "rights",
    }
    if not isinstance(bridge, dict) or set(bridge) != expected_top:
        raise VerifiedConstructionCoreError("geometry bridge fields differ")
    if (
        bridge.get("schema_version") != 1
        or bridge.get("bridge_id") != Path(path_text).stem
        or bridge.get("reviewed_at") != REVIEW_DATE.isoformat()
    ):
        raise VerifiedConstructionCoreError("geometry bridge identity differs")

    construction = bridge["construction_source"]
    if not isinstance(construction, dict) or set(construction) != {
        "input",
        "release",
        "project",
        "campus",
        "project_to_campus",
        "status_evidence",
    }:
        raise VerifiedConstructionCoreError("geometry bridge construction source differs")
    source_input = construction["input"]
    if not isinstance(source_input, dict) or set(source_input) != {
        "path",
        "bytes",
        "sha256",
    }:
        raise VerifiedConstructionCoreError("geometry bridge source input differs")
    source_path = _repository_input(
        source_input["path"], source_input["sha256"], "geometry bridge source"
    )
    if source_path.stat().st_size != source_input["bytes"]:
        raise VerifiedConstructionCoreError("geometry bridge source byte count differs")
    source = _load_json(source_path)
    source_project = source.get("project")
    source_campus = source.get("campus")
    source_evidence = source.get("evidence")
    if (
        not isinstance(source_project, dict)
        or not isinstance(source_campus, dict)
        or not isinstance(source_evidence, list)
    ):
        raise VerifiedConstructionCoreError("geometry bridge source record differs")
    project = construction["project"]
    campus = construction["campus"]
    expected_campus_fields = {
        "stable_key",
        "entity_id",
        "name",
        "country",
        "address",
        "operating_model",
        "operating_model_confidence",
        "operating_model_evidence_id",
    }
    if source_project.get("stable_key") == (
        "curated:related-openai-oracle-stargate-michigan-saline:current-build"
    ):
        expected_campus_fields.add("capacity_estimates_json")
    if (
        not isinstance(project, dict)
        or not isinstance(campus, dict)
        or set(project)
        != {
            "stable_key",
            "entity_id",
            "name",
            "country",
            "address",
            "status",
            "status_as_of",
            "status_age_days_at_review",
            "status_method",
            "status_confidence",
            "status_evidence_id",
            "workloads_json",
            "capacity_estimates_json",
        }
        or set(campus) != expected_campus_fields
        or project.get("stable_key") != source_project.get("stable_key")
        or campus.get("stable_key") != source_campus.get("stable_key")
        or project.get("entity_id")
        != atlas_stable_id("entity", source_project.get("stable_key"), "project")
        or campus.get("entity_id")
        != atlas_stable_id("entity", source_campus.get("stable_key"), "campus")
        or project.get("name") != source_project.get("name")
        or campus.get("name") != source_campus.get("name")
        or project.get("country") != source_project.get("country")
        or campus.get("country") != source_campus.get("country")
    ):
        raise VerifiedConstructionCoreError("geometry bridge construction identity differs")
    project_to_campus = construction["project_to_campus"]
    if (
        not isinstance(project_to_campus, dict)
        or set(project_to_campus)
        != {
            "basis",
            "target_entity_id",
            "bundle_id",
            "manifest",
            "members",
            "relationship",
            "subject_member",
            "object_member",
            "supplemental_v97_atlas_feature",
        }
        or project_to_campus.get("target_entity_id") != campus["entity_id"]
        or project_to_campus.get("basis")
        != "v14_project_targets_explicit_parent"
        or project_to_campus.get("bundle_id") != "2026-07-22-public-open-v14"
    ):
        raise VerifiedConstructionCoreError("geometry bridge site relationship differs")
    _validate_bridge_parent_row_bindings(bridge, project["stable_key"])

    release = construction["release"]
    if not isinstance(release, dict) or set(release) != {
        "release_id",
        "manifest",
        "members",
    } or release.get("release_id") != SOURCE_RELEASE_ID:
        raise VerifiedConstructionCoreError("geometry bridge source release differs")
    _, source_manifest = _manifest_binding(
        release["manifest"], label="geometry bridge source release"
    )
    _validate_manifest_members(
        source_manifest, release["members"], label="geometry bridge source release"
    )
    if set(release["members"]) != {
        "construction_pipeline.csv",
        "entities.csv",
        "evidence.csv",
    }:
        raise VerifiedConstructionCoreError("geometry bridge source members differ")

    status_evidence = construction["status_evidence"]
    if not isinstance(status_evidence, dict) or set(status_evidence) != {
        "evidence_id",
        "key",
        "kind",
        "title",
        "source_url",
        "publisher",
        "source_family",
        "license",
        "attribution",
        "published_at",
        "retrieved_at",
        "content_hash",
    }:
        raise VerifiedConstructionCoreError("geometry bridge status fields differ")
    status_key = status_evidence.get("key") if isinstance(status_evidence, dict) else None
    matches = [
        row
        for row in source_evidence
        if isinstance(row, dict) and row.get("key") == status_key
    ]
    if len(matches) != 1:
        raise VerifiedConstructionCoreError("geometry bridge status evidence differs")
    pinned_status = matches[0]
    expected_status_id = atlas_stable_id(
        "evidence", "curated-official", status_key, pinned_status.get("content_hash")
    )
    lifecycle = [
        row
        for row in source.get("lifecycle", [])
        if isinstance(row, dict) and row.get("entity") == "project"
    ]
    if len(lifecycle) != 1:
        raise VerifiedConstructionCoreError("geometry bridge lifecycle differs")
    latest = lifecycle[0]
    if (
        status_evidence.get("evidence_id") != expected_status_id
        or project.get("status_evidence_id") != expected_status_id
        or project.get("status") != latest.get("value")
        or project.get("status_as_of") != latest.get("as_of_date")
        or project.get("status_method") != latest.get("method")
        or project.get("status_confidence") != latest.get("confidence")
        or project.get("status_age_days_at_review")
        != (REVIEW_DATE - _calendar_date(latest.get("as_of_date"), "bridge status")).days
    ):
        raise VerifiedConstructionCoreError("geometry bridge status projection differs")
    for field in (
        "kind",
        "title",
        "source_url",
        "publisher",
        "source_family",
        "license",
        "attribution",
        "published_at",
        "retrieved_at",
        "content_hash",
    ):
        if status_evidence.get(field) != pinned_status.get(field):
            raise VerifiedConstructionCoreError("geometry bridge status content differs")

    def projected_evidence_id(evidence_key: Any, label: str) -> str:
        evidence_matches = [
            row
            for row in source_evidence
            if isinstance(row, dict) and row.get("key") == evidence_key
        ]
        if len(evidence_matches) != 1:
            raise VerifiedConstructionCoreError(f"geometry bridge {label} evidence differs")
        return atlas_stable_id(
            "evidence",
            "curated-official",
            evidence_key,
            evidence_matches[0].get("content_hash"),
        )

    def projected_capacities(entity_kind: str) -> list[dict[str, Any]]:
        projected: list[dict[str, Any]] = []
        for observation in source.get("capacities", []):
            if not isinstance(observation, dict):
                raise VerifiedConstructionCoreError(
                    "geometry bridge capacity observation differs"
                )
            if observation.get("entity") != entity_kind:
                continue
            evidence_id = projected_evidence_id(
                observation.get("evidence_key"), "capacity"
            )
            projected.append(
                {
                    **{
                        field: (
                            float(value)
                            if field in {"low", "base", "high"} and value is not None
                            else value
                        )
                        for field, value in observation.items()
                        if field not in {"entity", "evidence_key"}
                    },
                    "evidence_id": evidence_id,
                }
            )
        return sorted(
            projected,
            key=lambda row: (
                row.get("metric", ""),
                row.get("stage", ""),
                row.get("evidence_id", ""),
            ),
        )

    expected_capacity = projected_capacities("project")
    expected_workloads: list[dict[str, Any]] = []
    for observation in source.get("workloads", []):
        if not isinstance(observation, dict):
            raise VerifiedConstructionCoreError(
                "geometry bridge workload observation differs"
            )
        if observation.get("entity") != "project":
            continue
        evidence_id = projected_evidence_id(
            observation.get("evidence_key"), "workload"
        )
        expected_workloads.append(
            {
                **{
                    ("workload" if field == "value" else field): value
                    for field, value in observation.items()
                    if field not in {"entity", "evidence_key"}
                },
                "evidence_id": evidence_id,
            }
        )
    expected_project = {
        "stable_key": source_project["stable_key"],
        "entity_id": atlas_stable_id(
            "entity", source_project["stable_key"], "project"
        ),
        "name": source_project["name"],
        "country": source_project["country"],
        "address": source_project["address"],
        "status": latest["value"],
        "status_as_of": latest["as_of_date"],
        "status_age_days_at_review": (
            REVIEW_DATE - _calendar_date(latest["as_of_date"], "bridge status")
        ).days,
        "status_method": latest["method"],
        "status_confidence": latest["confidence"],
        "status_evidence_id": expected_status_id,
        "workloads_json": expected_workloads,
        "capacity_estimates_json": expected_capacity,
    }
    campus_models = [
        row
        for row in source.get("operating_models", [])
        if isinstance(row, dict) and row.get("entity") == "campus"
    ]
    if len(campus_models) > 1:
        raise VerifiedConstructionCoreError("geometry bridge campus model differs")
    campus_model = campus_models[0] if campus_models else None
    campus_model_evidence_id = (
        projected_evidence_id(campus_model["evidence_key"], "campus model")
        if campus_model
        else None
    )
    expected_campus: dict[str, Any] = {
        "stable_key": source_campus["stable_key"],
        "entity_id": atlas_stable_id(
            "entity", source_campus["stable_key"], "campus"
        ),
        "name": source_campus["name"],
        "country": source_campus["country"],
        "address": source_campus["address"],
        "operating_model": campus_model["value"] if campus_model else None,
        "operating_model_confidence": (
            campus_model["confidence"] if campus_model else None
        ),
        "operating_model_evidence_id": campus_model_evidence_id,
    }
    expected_campus_capacity = projected_capacities("campus")
    if project["stable_key"] == (
        "curated:related-openai-oracle-stargate-michigan-saline:current-build"
    ):
        if len(expected_campus_capacity) != 2:
            raise VerifiedConstructionCoreError(
                "geometry bridge Saline campus capacity differs"
            )
        for observation in expected_campus_capacity:
            _validate_typed_observation(
                observation, POWER_METRICS | ENERGY_METRICS | EFFICIENCY_METRICS
            )
        expected_campus["capacity_estimates_json"] = expected_campus_capacity
    elif expected_campus_capacity:
        raise VerifiedConstructionCoreError(
            "geometry bridge unexpected campus capacity differs"
        )
    if (
        _json_bytes(project) != _json_bytes(expected_project)
        or _json_bytes(campus) != _json_bytes(expected_campus)
    ):
        raise VerifiedConstructionCoreError(
            "geometry bridge construction projection differs"
        )

    _, relationship_manifest = _manifest_binding(
        project_to_campus["manifest"], label="geometry bridge topology release"
    )
    if (
        relationship_manifest.get("bundle_id") != project_to_campus["bundle_id"]
        or relationship_manifest.get("format")
        != "datacenter-atlas-exact-identity-decision-bundle-v1"
        or set(project_to_campus["members"])
        != {"component-members.csv", "relationships.csv"}
    ):
        raise VerifiedConstructionCoreError("geometry bridge topology release differs")
    _validate_manifest_members(
        relationship_manifest,
        project_to_campus["members"],
        label="geometry bridge topology release",
    )
    subject_member = project_to_campus["subject_member"]
    object_member = project_to_campus["object_member"]
    _validate_exact_component_projection(
        subject_member,
        entity=project,
        entity_kind="project",
        source_family=pinned_status["source_family"],
        snapshot_evidence_id=expected_status_id,
        label="geometry bridge topology subject",
    )
    _validate_exact_component_projection(
        object_member,
        entity=campus,
        entity_kind="campus",
        source_family=pinned_status["source_family"],
        snapshot_evidence_id=expected_status_id,
        label="geometry bridge topology object",
    )
    relationship = project_to_campus["relationship"]
    if not isinstance(relationship, dict) or set(relationship) != {
        "relationship_id",
        "relationship_type",
        "subject_component_id",
        "subject_kind",
        "object_component_id",
        "object_kind",
        "decision_basis",
        "typed_identity_tokens",
        "source_release_ids",
        "raw_relationship_count",
        "source_row",
    }:
        raise VerifiedConstructionCoreError(
            "geometry bridge topology relationship fields differ"
        )
    relationship_row = _embedded_csv_source_row(
        relationship["source_row"],
        EXACT_RELATIONSHIP_FIELDS,
        label="geometry bridge topology relationship",
    )
    relationship_projection = _exact_relationship_projection(relationship_row)
    actual_relationship_projection = {
        field: value for field, value in relationship.items() if field != "source_row"
    }
    if (
        _json_bytes(actual_relationship_projection)
        != _json_bytes(relationship_projection)
        or relationship["relationship_type"] != "project_targets"
        or relationship["subject_component_id"] != subject_member["component_id"]
        or relationship["subject_kind"] != "project"
        or relationship["object_component_id"] != object_member["component_id"]
        or relationship["object_kind"] != "campus"
        or relationship["decision_basis"] != "explicit_parent"
        or relationship["typed_identity_tokens"] != []
        or relationship["source_release_ids"] != [SOURCE_RELEASE_ID]
        or relationship["raw_relationship_count"] != 1
        or relationship["relationship_id"]
        != _exact_relationship_id(
            relationship["relationship_type"],
            relationship["subject_component_id"],
            relationship["object_component_id"],
        )
    ):
        raise VerifiedConstructionCoreError(
            "geometry bridge topology relationship differs"
        )
    supplemental = project_to_campus["supplemental_v97_atlas_feature"]
    if supplemental is not None:
        raise VerifiedConstructionCoreError(
            "geometry bridge supplemental topology must be absent"
        )

    geometry_release = bridge["geometry_release"]
    if not isinstance(geometry_release, dict) or set(geometry_release) != {
        "release_id",
        "as_of",
        "recorded_at",
        "manifest",
        "members",
    } or geometry_release.get("release_id") != "global-open-v3":
        raise VerifiedConstructionCoreError("geometry bridge geometry release differs")
    _, geometry_manifest = _manifest_binding(
        geometry_release["manifest"], label="geometry bridge geometry release"
    )
    if set(geometry_release["members"]) != {"entities.csv", "evidence.csv"}:
        raise VerifiedConstructionCoreError("geometry bridge geometry members differ")
    _validate_manifest_members(
        geometry_manifest,
        geometry_release["members"],
        label="geometry bridge geometry release",
    )

    entity = bridge["geometry_entity"]
    evidence = bridge["geometry_evidence"]
    if not isinstance(entity, dict) or not isinstance(evidence, dict):
        raise VerifiedConstructionCoreError("geometry bridge geometry records differ")
    entity_row = _embedded_csv_source_row(
        entity.get("source_row"),
        GLOBAL_GEOMETRY_ENTITY_FIELDS,
        label="geometry bridge entities.csv",
    )
    evidence_row = _embedded_csv_source_row(
        evidence.get("source_row"),
        GLOBAL_GEOMETRY_EVIDENCE_FIELDS,
        label="geometry bridge evidence.csv",
    )
    expected_entity_projection = _global_geometry_entity_projection(entity_row)
    expected_evidence_projection = _global_geometry_evidence_projection(evidence_row)
    actual_entity_projection = {
        field: value
        for field, value in entity.items()
        if field != "source_row"
    }
    actual_evidence_projection = {
        field: value for field, value in evidence.items() if field != "source_row"
    }
    if (
        set(entity)
        != {*expected_entity_projection, "source_row"}
        or _json_bytes(actual_entity_projection)
        != _json_bytes(expected_entity_projection)
    ):
        raise VerifiedConstructionCoreError(
            "geometry bridge entity source projection differs"
        )
    if (
        set(evidence) != {*expected_evidence_projection, "source_row"}
        or _json_bytes(actual_evidence_projection)
        != _json_bytes(expected_evidence_projection)
    ):
        raise VerifiedConstructionCoreError(
            "geometry bridge evidence source projection differs"
        )
    stable_key = entity.get("stable_key")
    osm_match = re.fullmatch(r"osm:(node|way|relation)/([1-9][0-9]*)", str(stable_key))
    osm_type = osm_match.group(1) if osm_match else None
    osm_id = int(osm_match.group(2)) if osm_match else None
    expected_entity_kind = (
        "building"
        if project["stable_key"] == "curated:green-campus-zrh1-lupfig:data-center-4"
        else "facility"
    )
    if (
        not isinstance(stable_key, str)
        or osm_match is None
        or entity.get("entity_kind") != expected_entity_kind
        or entity.get("entity_id")
        != atlas_stable_id("entity", stable_key, expected_entity_kind)
        or entity.get("source_url")
        != f"https://www.openstreetmap.org/{osm_type}/{osm_id}"
        or entity.get("source_family") != "openstreetmap"
        or entity.get("source_license") != "ODbL-1.0"
        or entity.get("source_attribution") != "© OpenStreetMap contributors"
        or entity.get("status") != "unknown"
        or entity.get("status_method") != "osm_geometry_only_no_operational_inference"
        or entity.get("operating_model") is not None
        or entity.get("capacity_estimates_json") != []
        or entity.get("workloads_json") != []
    ):
        raise VerifiedConstructionCoreError("geometry bridge OSM entity differs")
    _validate_polygon(entity.get("geometry"), label="geometry bridge")
    for value, lower, upper in (
        (entity.get("latitude"), -90, 90),
        (entity.get("longitude"), -180, 180),
    ):
        if (
            isinstance(value, bool)
            or not isinstance(value, (int, float))
            or not math.isfinite(value)
            or not lower <= value <= upper
        ):
            raise VerifiedConstructionCoreError(
                "geometry bridge representative coordinates differ"
            )
    if (
        evidence.get("evidence_id")
        != atlas_stable_id(
            "evidence",
            "osm",
            osm_type,
            str(osm_id),
            evidence.get("retrieved_at"),
            evidence.get("content_hash"),
        )
        or evidence.get("evidence_id") != entity.get("snapshot_evidence_id")
        or evidence.get("evidence_id") != entity.get("status_evidence_id")
        or evidence.get("source_url") != entity.get("source_url")
        or evidence.get("publisher") != entity.get("source_publisher")
        or evidence.get("source_family") != "openstreetmap"
        or evidence.get("license") != "ODbL-1.0"
        or evidence.get("attribution") != "© OpenStreetMap contributors"
        or not isinstance(evidence.get("published_at"), str)
        or evidence["published_at"][:10] != entity.get("status_as_of")
        or evidence.get("retrieved_at") != entity.get("source_retrieved_at")
    ):
        raise VerifiedConstructionCoreError("geometry bridge OSM evidence differs")
    projection = _geometry_evidence_projection(bridge)
    if set(projection) != set(EVIDENCE_FIELDS) - {"roles_json", "project_ids_json"}:
        raise VerifiedConstructionCoreError("geometry bridge evidence projection differs")

    decision = bridge["review_decision"]
    if not isinstance(decision, dict) or set(decision) != {
        "decision",
        "geometry_target_entity_kind",
        "geometry_derivation",
        "geometry_method",
        "geometry_scope_class",
        "geometry_authority_class",
        "geometry_use_scope",
        "horizontal_uncertainty_metres",
        "horizontal_uncertainty_unknown_reason",
        "identity_basis",
        "precision_scope",
        "rejected_claims",
    }:
        raise VerifiedConstructionCoreError("geometry bridge review fields differ")
    rejected = decision.get("rejected_claims")
    required_rejections = {
        "official_or_cadastral_boundary",
        "project_or_phase_footprint",
        "construction_extent",
        "geometry_derived_lifecycle_or_capacity",
        "imagery_verification",
    }
    if (
        decision.get("decision") != "accepted_as_campus_locator"
        or decision.get("geometry_target_entity_kind") != "campus"
        or decision.get("geometry_derivation") != "cross_source_overlay"
        or not isinstance(decision.get("geometry_method"), str)
        or not decision["geometry_method"]
        or not isinstance(decision.get("geometry_scope_class"), str)
        or not decision["geometry_scope_class"]
        or decision.get("geometry_authority_class") != "community_mapped"
        or decision.get("geometry_use_scope") != "campus_locator"
        or decision.get("horizontal_uncertainty_metres") is not None
        or not isinstance(decision.get("horizontal_uncertainty_unknown_reason"), str)
        or not decision["horizontal_uncertainty_unknown_reason"]
        or not isinstance(decision.get("identity_basis"), str)
        or not decision["identity_basis"]
        or not isinstance(decision.get("precision_scope"), str)
        or not decision["precision_scope"]
        or not isinstance(rejected, list)
        or len(set(rejected)) != len(rejected)
        or not required_rejections <= set(rejected)
    ):
        raise VerifiedConstructionCoreError("geometry bridge review decision differs")
    expected_semantics = BRIDGE_REVIEW_SEMANTICS.get(project["stable_key"])
    if expected_semantics is None or any(
        decision.get(field) != value for field, value in expected_semantics.items()
    ):
        raise VerifiedConstructionCoreError(
            "geometry bridge allowlisted review semantics differ"
        )
    if bridge["lineage_context"] is not None:
        raise VerifiedConstructionCoreError(
            "geometry bridge unbound lineage context must be absent"
        )
    imagery = bridge["imagery_posture"]
    expected_imagery_outcome = BRIDGE_IMAGERY_OUTCOMES.get(project["stable_key"])
    if (
        not isinstance(imagery, dict)
        or set(imagery) != {"outcome", "independent_imagery_verification", "basis"}
        or expected_imagery_outcome is None
        or imagery.get("outcome") != expected_imagery_outcome
        or imagery.get("independent_imagery_verification") is not False
        or not isinstance(imagery.get("basis"), str)
        or not imagery["basis"]
    ):
        raise VerifiedConstructionCoreError("geometry bridge imagery posture differs")
    rights = bridge["rights"]
    if (
        not isinstance(rights, dict)
        or set(rights)
        != {
            "construction_source",
            "identity_bridge_evidence",
            "geometry_source",
            "geometry_license_url",
            "lineage_context",
            "mixed_rights",
        }
        or rights.get("geometry_license_url")
        != "https://www.openstreetmap.org/copyright"
        or "OpenStreetMap contributors" not in str(rights.get("geometry_source"))
        or rights.get("lineage_context") is not None
    ):
        raise VerifiedConstructionCoreError("geometry bridge rights differ")
    _validate_identity_bridge(bridge, project, campus, entity)

    if hydrated_crosscheck:
        for name, record, fields in (
            ("entities.csv", entity, GLOBAL_GEOMETRY_ENTITY_FIELDS),
            ("evidence.csv", evidence, GLOBAL_GEOMETRY_EVIDENCE_FIELDS),
        ):
            metadata = geometry_release["members"][name]
            payload_path = _repository_input(
                metadata["path"], metadata["sha256"], f"geometry bridge {name}"
            )
            if payload_path.stat().st_size != metadata["bytes"]:
                raise VerifiedConstructionCoreError(
                    f"geometry bridge hydrated payload differs: {name}"
                )
            _validate_hydrated_source_row(
                payload_path,
                record["source_row"],
                fields,
                label=f"geometry bridge {name}",
            )
        topology_members = project_to_campus["members"]
        for name, record, fields in (
            ("relationships.csv", relationship, EXACT_RELATIONSHIP_FIELDS),
            ("component-members.csv", subject_member, EXACT_COMPONENT_FIELDS),
            ("component-members.csv", object_member, EXACT_COMPONENT_FIELDS),
        ):
            metadata = topology_members[name]
            payload_path = _repository_input(
                metadata["path"],
                metadata["sha256"],
                f"geometry bridge topology {name}",
            )
            if payload_path.stat().st_size != metadata["bytes"]:
                raise VerifiedConstructionCoreError(
                    f"geometry bridge hydrated topology differs: {name}"
                )
            _validate_hydrated_source_row(
                payload_path,
                record["source_row"],
                fields,
                label=f"geometry bridge {name}",
            )
    return bridge


def _reviewed_overlays_v04(
    path: Path, *, hydrated_crosscheck: bool
) -> dict[str, Any]:
    reviewed = _load_json(path)
    if not isinstance(reviewed, dict) or set(reviewed) != {
        "contract_id",
        "purpose",
        "reviewed_as_of",
        "base_contract",
        "required_fields",
        "allowed_decisions",
        "overlays",
    }:
        raise VerifiedConstructionCoreError("reviewed-overlay contract fields differ")
    if (
        reviewed.get("contract_id")
        != "verified-construction-core-reviewed-overlays-v2"
        or reviewed.get("reviewed_as_of") != REVIEW_DATE.isoformat()
        or set(reviewed.get("allowed_decisions", []))
        != {"queued", "accepted", "excluded"}
    ):
        raise VerifiedConstructionCoreError("reviewed-overlay contract identity differs")
    expected_base = {
        "path": "definitions/verified-construction-core-reviewed-overlays-v1.json",
        "sha256": V03_OVERLAY_DEFINITION_SHA256,
    }
    if reviewed.get("base_contract") != expected_base:
        raise VerifiedConstructionCoreError("reviewed-overlay base pin differs")
    base_path = _repository_input(
        expected_base["path"], expected_base["sha256"], "reviewed-overlay base"
    )
    base = _load_json(base_path)
    base_rows = base.get("overlays") if isinstance(base, dict) else None
    if (
        base.get("contract_id")
        != "verified-construction-core-reviewed-overlays-v1"
        or not isinstance(base_rows, list)
        or any(row.get("decision") == "accepted" for row in base_rows)
    ):
        raise VerifiedConstructionCoreError("reviewed-overlay base differs")
    required = {
        "overlay_id",
        "source_project_stable_key",
        "source_project_entity_id",
        "physical_site_stable_key",
        "physical_site_entity_id",
        "bridge_path",
        "bridge_sha256",
        "decision",
        "decision_reason",
        "reviewed_at",
    }
    if set(reviewed.get("required_fields", [])) != required:
        raise VerifiedConstructionCoreError("reviewed-overlay required fields differ")
    delta = reviewed.get("overlays")
    if not isinstance(delta, list) or len(delta) != 2:
        raise VerifiedConstructionCoreError("reviewed-overlay current cohort differs")
    ids = {row.get("overlay_id") for row in base_rows if isinstance(row, dict)}
    bridges: dict[str, dict[str, Any]] = {}
    for index, row in enumerate(delta):
        if not isinstance(row, dict) or set(row) != required:
            raise VerifiedConstructionCoreError(
                f"reviewed-overlay current row {index} differs"
            )
        overlay_id = row["overlay_id"]
        if (
            not isinstance(overlay_id, str)
            or not overlay_id
            or overlay_id in ids
            or row.get("decision") != "accepted"
            or row.get("reviewed_at") != REVIEW_DATE.isoformat()
            or not isinstance(row.get("decision_reason"), str)
            or not row["decision_reason"]
        ):
            raise VerifiedConstructionCoreError("reviewed-overlay current identity differs")
        ids.add(overlay_id)
        bridge = _validate_geometry_bridge(
            row["bridge_path"],
            row["bridge_sha256"],
            hydrated_crosscheck=hydrated_crosscheck,
        )
        construction = bridge["construction_source"]
        if (
            row["source_project_stable_key"]
            != construction["project"]["stable_key"]
            or row["source_project_entity_id"]
            != construction["project"]["entity_id"]
            or row["physical_site_stable_key"]
            != construction["campus"]["stable_key"]
            or row["physical_site_entity_id"]
            != construction["campus"]["entity_id"]
        ):
            raise VerifiedConstructionCoreError(
                "reviewed-overlay bridge identity differs"
            )
        bridges[overlay_id] = bridge
    return {
        "overlays": [*base_rows, *delta],
        "delta_overlays": delta,
        "bridges_by_overlay_id": bridges,
    }


def _reviewed_overlays_v05(
    path: Path, *, hydrated_crosscheck: bool
) -> dict[str, Any]:
    reviewed = _load_json(path)
    if not isinstance(reviewed, dict) or set(reviewed) != {
        "contract_id",
        "purpose",
        "reviewed_as_of",
        "base_contract",
        "required_fields",
        "allowed_decisions",
        "overlays",
    }:
        raise VerifiedConstructionCoreError("reviewed-overlay contract fields differ")
    if (
        reviewed.get("contract_id")
        != "verified-construction-core-reviewed-overlays-v3"
        or reviewed.get("reviewed_as_of") != REVIEW_DATE.isoformat()
        or set(reviewed.get("allowed_decisions", []))
        != {"queued", "accepted", "excluded"}
    ):
        raise VerifiedConstructionCoreError("reviewed-overlay contract identity differs")
    expected_base = {
        "path": "definitions/verified-construction-core-reviewed-overlays-v2.json",
        "sha256": V04_OVERLAY_DEFINITION_SHA256,
    }
    if reviewed.get("base_contract") != expected_base:
        raise VerifiedConstructionCoreError("reviewed-overlay base pin differs")
    base_path = _repository_input(
        expected_base["path"], expected_base["sha256"], "reviewed-overlay base"
    )
    base = _reviewed_overlays_v04(
        base_path, hydrated_crosscheck=hydrated_crosscheck
    )
    required = {
        "overlay_id",
        "source_project_stable_key",
        "source_project_entity_id",
        "physical_site_stable_key",
        "physical_site_entity_id",
        "bridge_path",
        "bridge_sha256",
        "decision",
        "decision_reason",
        "reviewed_at",
    }
    if set(reviewed.get("required_fields", [])) != required:
        raise VerifiedConstructionCoreError("reviewed-overlay required fields differ")
    delta = reviewed.get("overlays")
    if not isinstance(delta, list) or len(delta) != 4:
        raise VerifiedConstructionCoreError("reviewed-overlay current cohort differs")
    ids = {
        row.get("overlay_id")
        for row in base["overlays"]
        if isinstance(row, dict)
    }
    bridges = dict(base["bridges_by_overlay_id"])
    for index, row in enumerate(delta):
        if not isinstance(row, dict) or set(row) != required:
            raise VerifiedConstructionCoreError(
                f"reviewed-overlay current row {index} differs"
            )
        overlay_id = row["overlay_id"]
        if (
            not isinstance(overlay_id, str)
            or not overlay_id
            or overlay_id in ids
            or row.get("decision") != "accepted"
            or row.get("reviewed_at") != REVIEW_DATE.isoformat()
            or not isinstance(row.get("decision_reason"), str)
            or not row["decision_reason"]
        ):
            raise VerifiedConstructionCoreError(
                "reviewed-overlay current identity differs"
            )
        ids.add(overlay_id)
        bridge = _validate_geometry_bridge(
            row["bridge_path"],
            row["bridge_sha256"],
            hydrated_crosscheck=hydrated_crosscheck,
        )
        construction = bridge["construction_source"]
        if (
            row["source_project_stable_key"]
            != construction["project"]["stable_key"]
            or row["source_project_entity_id"]
            != construction["project"]["entity_id"]
            or row["physical_site_stable_key"]
            != construction["campus"]["stable_key"]
            or row["physical_site_entity_id"]
            != construction["campus"]["entity_id"]
        ):
            raise VerifiedConstructionCoreError(
                "reviewed-overlay bridge identity differs"
            )
        bridges[overlay_id] = bridge
    return {
        "overlays": [*base["overlays"], *delta],
        "delta_overlays": delta,
        "bridges_by_overlay_id": bridges,
    }


def _reviewed_overlays(*, hydrated_crosscheck: bool) -> dict[str, Any]:
    reviewed = _load_json(OVERLAY_DEFINITION)
    if not isinstance(reviewed, dict) or set(reviewed) != {
        "contract_id",
        "purpose",
        "reviewed_as_of",
        "base_contract",
        "required_fields",
        "allowed_decisions",
        "overlays",
    }:
        raise VerifiedConstructionCoreError("reviewed-overlay contract fields differ")
    if (
        reviewed.get("contract_id")
        != "verified-construction-core-reviewed-overlays-v4"
        or reviewed.get("reviewed_as_of") != REVIEW_DATE.isoformat()
        or set(reviewed.get("allowed_decisions", []))
        != {"queued", "accepted", "excluded"}
    ):
        raise VerifiedConstructionCoreError("reviewed-overlay contract identity differs")
    expected_base = {
        "path": "definitions/verified-construction-core-reviewed-overlays-v3.json",
        "sha256": V05_OVERLAY_DEFINITION_SHA256,
    }
    if reviewed.get("base_contract") != expected_base:
        raise VerifiedConstructionCoreError("reviewed-overlay base pin differs")
    base_path = _repository_input(
        expected_base["path"], expected_base["sha256"], "reviewed-overlay base"
    )
    base = _reviewed_overlays_v05(
        base_path, hydrated_crosscheck=hydrated_crosscheck
    )
    required = {
        "overlay_id",
        "source_project_stable_key",
        "source_project_entity_id",
        "physical_site_stable_key",
        "physical_site_entity_id",
        "bridge_path",
        "bridge_sha256",
        "decision",
        "decision_reason",
        "reviewed_at",
    }
    if set(reviewed.get("required_fields", [])) != required:
        raise VerifiedConstructionCoreError("reviewed-overlay required fields differ")
    delta = reviewed.get("overlays")
    if not isinstance(delta, list) or len(delta) != len(V06_BRIDGE_SEMANTICS):
        raise VerifiedConstructionCoreError("reviewed-overlay current cohort differs")
    ids = {
        row.get("overlay_id")
        for row in base["overlays"]
        if isinstance(row, dict)
    }
    bridges = dict(base["bridges_by_overlay_id"])
    project_keys: set[str] = set()
    for index, row in enumerate(delta):
        if not isinstance(row, dict) or set(row) != required:
            raise VerifiedConstructionCoreError(
                f"reviewed-overlay current row {index} differs"
            )
        overlay_id = row["overlay_id"]
        project_key = row["source_project_stable_key"]
        if (
            not isinstance(overlay_id, str)
            or not overlay_id
            or overlay_id in ids
            or project_key in project_keys
            or project_key not in V06_BRIDGE_SEMANTICS
            or row.get("decision") != "accepted"
            or row.get("reviewed_at") != REVIEW_DATE.isoformat()
            or not isinstance(row.get("decision_reason"), str)
            or not row["decision_reason"]
        ):
            raise VerifiedConstructionCoreError(
                "reviewed-overlay current identity differs"
            )
        ids.add(overlay_id)
        project_keys.add(project_key)
        bridge = _validate_geometry_bridge(
            row["bridge_path"],
            row["bridge_sha256"],
            hydrated_crosscheck=hydrated_crosscheck,
        )
        construction = bridge["construction_source"]
        if (
            row["source_project_stable_key"]
            != construction["project"]["stable_key"]
            or row["source_project_entity_id"]
            != construction["project"]["entity_id"]
            or row["physical_site_stable_key"]
            != construction["campus"]["stable_key"]
            or row["physical_site_entity_id"]
            != construction["campus"]["entity_id"]
        ):
            raise VerifiedConstructionCoreError(
                "reviewed-overlay bridge identity differs"
            )
        bridges[overlay_id] = bridge
    if project_keys != set(V06_BRIDGE_SEMANTICS):
        raise VerifiedConstructionCoreError("reviewed-overlay current cohort differs")
    return {
        "overlays": [*base["overlays"], *delta],
        "delta_overlays": delta,
        "bridges_by_overlay_id": bridges,
    }


def _review_contracts() -> tuple[
    list[dict[str, Any]], dict[str, Any], dict[str, dict[str, Any]]
]:
    acceptances = _reviewed_acceptances()
    overlays = _reviewed_overlays(hydrated_crosscheck=True)
    required = {
        "project_stable_key",
        "physical_site_stable_key",
        "source_input_path",
        "source_input_sha256",
        "geometry_entity",
        "geometry_derivation",
        "geometry_evidence_key",
        "geometry_overlay_id",
        "geometry_method",
        "geometry_scope_class",
        "horizontal_uncertainty_metres",
        "precision_scope",
        "decision_basis",
    }
    keys: set[str] = set()
    source_records: dict[str, dict[str, Any]] = {}
    for index, acceptance in enumerate(acceptances):
        if not isinstance(acceptance, dict) or set(acceptance) != required:
            raise VerifiedConstructionCoreError(
                f"reviewed-site acceptance {index} has unexpected fields"
            )
        key = acceptance["project_stable_key"]
        if key in keys:
            raise VerifiedConstructionCoreError(f"duplicate reviewed project: {key}")
        keys.add(key)
        relative_path = Path(acceptance["source_input_path"])
        if relative_path.is_absolute() or ".." in relative_path.parts:
            raise VerifiedConstructionCoreError(
                f"reviewed geometry source path escapes the repository: {relative_path}"
            )
        source_path = ROOT / relative_path
        if not source_path.is_file():
            raise VerifiedConstructionCoreError(
                f"reviewed geometry source is not hydrated: {source_path}"
            )
        if _sha256_file(source_path) != acceptance["source_input_sha256"]:
            raise VerifiedConstructionCoreError(
                f"reviewed geometry source hash differs: {source_path}"
            )
        source_record = _load_json(source_path)
        if not isinstance(source_record, dict):
            raise VerifiedConstructionCoreError(
                f"reviewed geometry source is not an object: {source_path}"
            )
        project = source_record.get("project")
        campus = source_record.get("campus")
        source_evidence = source_record.get("evidence")
        if not isinstance(project, dict) or not isinstance(campus, dict):
            raise VerifiedConstructionCoreError(
                f"reviewed geometry source lacks project/campus: {source_path}"
            )
        if not isinstance(source_evidence, list):
            raise VerifiedConstructionCoreError(
                f"reviewed geometry source lacks evidence: {source_path}"
            )
        if project.get("stable_key") != key:
            raise VerifiedConstructionCoreError(
                f"reviewed geometry project identity differs: {source_path}"
            )
        if campus.get("stable_key") != acceptance["physical_site_stable_key"]:
            raise VerifiedConstructionCoreError(
                f"reviewed geometry campus identity differs: {source_path}"
            )
        if acceptance["geometry_overlay_id"] is None:
            geometry_source_record = (
                project if acceptance["geometry_entity"] == "project" else campus
            )
            if geometry_source_record.get("evidence_key") != acceptance[
                "geometry_evidence_key"
            ]:
                raise VerifiedConstructionCoreError(
                    f"reviewed geometry-source evidence differs: {source_path}"
                )
            matching_evidence = [
                row
                for row in source_evidence
                if isinstance(row, dict)
                and row.get("key") == acceptance["geometry_evidence_key"]
            ]
            if len(matching_evidence) != 1:
                raise VerifiedConstructionCoreError(
                    f"reviewed geometry evidence is not unique: {source_path}"
                )
        uncertainty = acceptance["horizontal_uncertainty_metres"]
        if uncertainty is not None and (
            isinstance(uncertainty, bool)
            or not isinstance(uncertainty, (int, float))
            or uncertainty < 0
        ):
            raise VerifiedConstructionCoreError(
                f"reviewed geometry uncertainty is invalid: {source_path}"
            )
        overlay_id = acceptance["geometry_overlay_id"]
        if overlay_id is not None:
            bridge = overlays["bridges_by_overlay_id"].get(overlay_id)
            if bridge is None:
                raise VerifiedConstructionCoreError(
                    f"reviewed geometry overlay is absent: {source_path}"
                )
            _validate_bridge_acceptance_semantics(
                acceptance, bridge, label=f"reviewed geometry {source_path}"
            )
        source_records[key] = source_record
    referenced_overlays = {
        row["geometry_overlay_id"]
        for row in acceptances
        if row["geometry_overlay_id"] is not None
    }
    if referenced_overlays != set(overlays["bridges_by_overlay_id"]):
        raise VerifiedConstructionCoreError("reviewed overlay references differ")
    return acceptances, overlays, source_records


def _first_failure(row: Mapping[str, str], accepted: set[str]) -> str:
    if row.get("entity_kind") != "project":
        return "entity_kind_not_project"
    if row.get("status") not in PHYSICAL_STATUSES:
        return "status_not_physical"
    if row.get("status_method") not in AUTHORITATIVE_STATUS_METHODS:
        return "status_method_not_authoritative"
    try:
        age = (REVIEW_DATE - date.fromisoformat(row["status_as_of"])).days
    except (KeyError, ValueError):
        return "invalid_status_date"
    if age < 0 or age > MAX_STATUS_AGE_DAYS:
        return "status_outside_90_day_window"
    if row.get("stable_key") not in accepted:
        return "not_in_reviewed_site_geometry_allowlist"
    return "selected"


def _imagery_outcome(
    project_key: str,
    default_outcome: str,
    records_by_key: Mapping[str, Mapping[str, Any]],
) -> str:
    record = records_by_key.get(project_key)
    return default_outcome if record is None else str(record["outcome"])


def _direct_geometry_semantics(geometry_type: str) -> tuple[str, str]:
    if geometry_type in {"Polygon", "MultiPolygon"}:
        return "official_source", "official_boundary"
    return "official_source", "reviewed_site_locator"


def _verification_posture(
    geometry_authority_class: str, geometry_use_scope: str
) -> str:
    if (
        geometry_authority_class == "official_source"
        and geometry_use_scope == "official_boundary"
    ):
        return "recent_authoritative_physical_observation_plus_official_boundary"
    return "recent_authoritative_physical_observation_plus_reviewed_site_locator"


def _is_official_boundary(row: Mapping[str, Any]) -> bool:
    return (
        row.get("geometry_authority_class") == "official_source"
        and row.get("geometry_use_scope") == "official_boundary"
    )


def _is_reviewed_locator(row: Mapping[str, Any]) -> bool:
    return row.get("geometry_use_scope") in {
        "reviewed_site_locator",
        "project_locator",
        "campus_locator",
    }


def _point_from_coordinates(coordinates: Any, project_key: str) -> dict[str, Any]:
    if not isinstance(coordinates, dict) or set(coordinates) != {
        "latitude",
        "longitude",
    }:
        raise VerifiedConstructionCoreError(
            f"reviewed coordinates differ: {project_key}"
        )
    latitude = coordinates["latitude"]
    longitude = coordinates["longitude"]
    for value, lower, upper in (
        (latitude, -90, 90),
        (longitude, -180, 180),
    ):
        if (
            isinstance(value, bool)
            or not isinstance(value, (int, float))
            or not math.isfinite(value)
            or value < lower
            or value > upper
        ):
            raise VerifiedConstructionCoreError(
                f"reviewed coordinates are invalid: {project_key}"
            )
    return {"type": "Point", "coordinates": [longitude, latitude]}


def _resolve_reviewed_geometry(
    acceptance: Mapping[str, Any],
    source_record: Mapping[str, Any],
    project_release_row: Mapping[str, str],
    campus_release_row: Mapping[str, str],
    bridge: Mapping[str, Any] | None,
) -> tuple[dict[str, Any], Mapping[str, Any]]:
    project_key = acceptance["project_stable_key"]
    entity_kind = acceptance["geometry_entity"]
    source_entity = source_record[entity_kind]
    release_entity = (
        project_release_row if entity_kind == "project" else campus_release_row
    )
    if release_entity.get("entity_kind") != entity_kind:
        raise VerifiedConstructionCoreError(
            f"reviewed geometry release entity differs: {project_key}"
        )
    derivation = acceptance["geometry_derivation"]
    if bridge is not None:
        geometry_entity = bridge["geometry_entity"]
        decision = bridge["review_decision"]
        if (
            decision["geometry_target_entity_kind"] != entity_kind
            or decision["geometry_derivation"] != derivation
            or bridge["construction_source"]["project"]["stable_key"]
            != project_key
            or bridge["construction_source"]["campus"]["stable_key"]
            != acceptance["physical_site_stable_key"]
        ):
            raise VerifiedConstructionCoreError(
                f"reviewed geometry bridge identity differs: {project_key}"
            )
        geometry = geometry_entity["geometry"]
        return geometry, geometry_entity
    if derivation in {"cross_source_overlay", "official_parcel_union"}:
        raise VerifiedConstructionCoreError(
            f"reviewed geometry bridge is absent: {project_key}"
        )
    if derivation == "direct_geometry":
        geometry = source_entity.get("geometry")
        if not isinstance(geometry, dict):
            raise VerifiedConstructionCoreError(
                f"reviewed direct geometry is absent: {project_key}"
            )
        if geometry.get("type") == "Point":
            source_point = _point_from_coordinates(
                source_entity.get("coordinates"), project_key
            )
            if source_point != geometry:
                raise VerifiedConstructionCoreError(
                    f"reviewed point geometry and coordinates differ: {project_key}"
                )
    elif derivation == "coordinates_to_point":
        if source_entity.get("geometry") is not None:
            raise VerifiedConstructionCoreError(
                f"coordinate-derived geometry source is not null: {project_key}"
            )
        geometry = _point_from_coordinates(source_entity.get("coordinates"), project_key)
    else:  # The review-contract parser rejects this before geometry resolution.
        raise VerifiedConstructionCoreError(
            f"reviewed geometry derivation differs: {project_key}"
        )
    if geometry.get("type") not in {"Point", "Polygon", "MultiPolygon"}:
        raise VerifiedConstructionCoreError(
            f"reviewed geometry type differs: {project_key}"
        )
    release_geometry = _parse_json_field(
        release_entity.get("geometry_json", ""), "geometry"
    )
    if release_geometry != geometry:
        raise VerifiedConstructionCoreError(
            f"reviewed geometry no longer matches its pinned source: {project_key}"
        )
    try:
        release_latitude = float(release_entity["latitude"])
        release_longitude = float(release_entity["longitude"])
    except (KeyError, TypeError, ValueError) as error:
        raise VerifiedConstructionCoreError(
            f"reviewed geometry representative coordinates differ: {project_key}"
        ) from error
    if (
        not math.isfinite(release_latitude)
        or not math.isfinite(release_longitude)
        or not -90 <= release_latitude <= 90
        or not -180 <= release_longitude <= 180
    ):
        raise VerifiedConstructionCoreError(
            f"reviewed geometry representative coordinates differ: {project_key}"
        )
    if geometry["type"] == "Point" and geometry["coordinates"] != [
        release_longitude,
        release_latitude,
    ]:
        raise VerifiedConstructionCoreError(
            f"reviewed point coordinates no longer match their pinned source: {project_key}"
        )
    return geometry, release_entity


def _preferred_site_geometry_member(
    members: Sequence[Mapping[str, Any]],
) -> Mapping[str, Any]:
    geometry_rank = {"Point": 1, "Polygon": 2, "MultiPolygon": 3}
    semantic_rank = {
        ("official_source", "official_boundary"): 4,
        ("official_source", "reviewed_site_locator"): 3,
        ("official_source", "project_locator"): 3,
        ("official_source", "campus_locator"): 3,
        ("community_mapped", "campus_locator"): 2,
    }
    try:
        highest_semantic_rank = max(
            semantic_rank[
                (row["geometry_authority_class"], row["geometry_use_scope"])
            ]
            for row in members
        )
    except (KeyError, ValueError) as error:
        raise VerifiedConstructionCoreError("preview site geometry rank differs") from error
    candidates = [
        row
        for row in members
        if semantic_rank[
            (row["geometry_authority_class"], row["geometry_use_scope"])
        ]
        == highest_semantic_rank
    ]
    highest_geometry_rank = max(geometry_rank[row["geometry_type"]] for row in candidates)
    candidates = [
        row
        for row in candidates
        if geometry_rank[row["geometry_type"]] == highest_geometry_rank
    ]
    signatures = {
        (row["geometry_json"], str(row["latitude"]), str(row["longitude"]))
        for row in candidates
    }
    if len(signatures) != 1:
        raise VerifiedConstructionCoreError(
            "preview site has conflicting highest-ranked reviewed geometries"
        )
    return candidates[0]


def _final_release_gates(
    sites: Sequence[Mapping[str, Any]], projects: Sequence[Mapping[str, Any]]
) -> dict[str, dict[str, Any]]:
    country_counts = Counter(row["country"] for row in sites)
    site_count = len(sites)
    non_us_count = sum(row["country_iso_a2"] != "US" for row in sites)
    max_share = max(country_counts.values(), default=0) / site_count if site_count else 0
    imagery_count = sum(
        row["imagery_review_outcome"] != "not_reviewed_for_core_preview"
        for row in projects
    )
    return {
        "site_count": {
            "actual": site_count,
            "required": FINAL_REQUIREMENTS["site_count"],
            "passed": site_count == FINAL_REQUIREMENTS["site_count"],
        },
        "country_count": {
            "actual": len(country_counts),
            "required_minimum": FINAL_REQUIREMENTS["country_count"],
            "passed": len(country_counts) >= FINAL_REQUIREMENTS["country_count"],
        },
        "non_us_site_count": {
            "actual": non_us_count,
            "required_minimum": FINAL_REQUIREMENTS["non_us_site_count"],
            "passed": non_us_count >= FINAL_REQUIREMENTS["non_us_site_count"],
        },
        "maximum_single_country_share": {
            "actual": round(max_share, 6),
            "required_maximum": FINAL_REQUIREMENTS["maximum_single_country_share"],
            "passed": max_share <= FINAL_REQUIREMENTS["maximum_single_country_share"],
        },
        "imagery_outcomes_complete": {
            "actual": imagery_count,
            "required": len(projects),
            "passed": imagery_count == len(projects),
        },
        "blind_review": {
            "sample_size": 0,
            "agreements": 0,
            "required_sample_size": FINAL_REQUIREMENTS["blind_review_sample_size"],
            "required_agreements": FINAL_REQUIREMENTS[
                "blind_review_minimum_agreements"
            ],
            "passed": False,
        },
        "clean_clone_rebuild": {
            "passed": False,
            "reason": CLEAN_CLONE_REBUILD_REASON,
        },
    }


def _build_rows() -> dict[str, Any]:
    _validate_current_definition_pins()
    source_manifest = _verify_source_release()
    acceptances, overlays, source_records = _review_contracts()
    default_imagery_outcome, imagery_records = _imagery_contract()
    provenance = _provenance_contract()
    pipeline = _load_csv(SOURCE_RELEASE / "construction_pipeline.csv")
    entities = _load_csv(SOURCE_RELEASE / "entities.csv")
    evidence_rows = _load_csv(SOURCE_RELEASE / "evidence.csv")

    pipeline_by_key = {row["stable_key"]: row for row in pipeline}
    entities_by_key = {row["stable_key"]: row for row in entities}
    evidence_by_id = {row["evidence_id"]: row for row in evidence_rows}
    if len(pipeline_by_key) != len(pipeline):
        raise VerifiedConstructionCoreError("source pipeline stable keys are not unique")
    if len(entities_by_key) != len(entities):
        raise VerifiedConstructionCoreError("source entity stable keys are not unique")
    if len(evidence_by_id) != len(evidence_rows):
        raise VerifiedConstructionCoreError("source evidence identifiers are not unique")
    for evidence_id, provenance_evidence in provenance["evidence_rows"].items():
        existing = evidence_by_id.get(evidence_id)
        if existing is not None and existing != provenance_evidence:
            raise VerifiedConstructionCoreError(
                "source and provenance evidence differ"
            )
        evidence_by_id[evidence_id] = provenance_evidence
    for bridge in overlays["bridges_by_overlay_id"].values():
        geometry_evidence = _geometry_evidence_projection(bridge)
        evidence_id = geometry_evidence["evidence_id"]
        existing = evidence_by_id.get(evidence_id)
        if existing is not None and existing != geometry_evidence:
            raise VerifiedConstructionCoreError(
                "source and bridge geometry evidence differ"
            )
        evidence_by_id[evidence_id] = geometry_evidence
    accepted_keys = {row["project_stable_key"] for row in acceptances}
    imagery_by_key = {row["project_stable_key"]: row for row in imagery_records}
    if not set(imagery_by_key) <= accepted_keys:
        raise VerifiedConstructionCoreError(
            "imagery-review contract references a non-selected project"
        )
    reason_counts = Counter(_first_failure(row, accepted_keys) for row in pipeline)

    projects: list[dict[str, Any]] = []
    acceptance_by_key = {row["project_stable_key"]: row for row in acceptances}
    evidence_usage: dict[str, dict[str, set[str]]] = defaultdict(
        lambda: {"roles": set(), "project_ids": set()}
    )
    workload_bindings = {
        (
            row["project_stable_key"],
            row["evidence_id"],
            row["workload"],
        ): row
        for row in provenance["workload_scope_bindings"]
    }
    role_bindings: dict[str, list[dict[str, Any]]] = defaultdict(list)
    role_exclusions: dict[str, list[dict[str, Any]]] = defaultdict(list)
    for row in provenance["role_bindings"]:
        role_bindings[row["project_stable_key"]].append(row)
    for row in provenance["excluded_source_roles"]:
        role_exclusions[row["project_stable_key"]].append(row)
    used_workload_bindings: set[tuple[str, str, str]] = set()
    used_role_bindings: set[tuple[str, str, str]] = set()
    used_role_exclusions: set[tuple[str, str, str]] = set()

    for project_key in sorted(accepted_keys):
        acceptance = acceptance_by_key[project_key]
        source = pipeline_by_key.get(project_key)
        if source is None:
            raise VerifiedConstructionCoreError(f"reviewed project is absent: {project_key}")
        failure = _first_failure(source, accepted_keys)
        if failure != "selected":
            raise VerifiedConstructionCoreError(
                f"reviewed project no longer passes {failure}: {project_key}"
            )
        site_key = acceptance["physical_site_stable_key"]
        site_source = entities_by_key.get(site_key)
        if site_source is None or site_source.get("entity_kind") != "campus":
            raise VerifiedConstructionCoreError(f"reviewed physical site is absent: {site_key}")
        source_record = source_records[project_key]
        bridge = overlays["bridges_by_overlay_id"].get(
            acceptance["geometry_overlay_id"]
        )
        geometry, geometry_release = _resolve_reviewed_geometry(
            acceptance, source_record, source, site_source, bridge
        )
        project_id = source["entity_id"]
        imagery_record = imagery_by_key.get(project_key)
        if imagery_record is not None and imagery_record["project_entity_id"] != project_id:
            raise VerifiedConstructionCoreError(
                f"imagery-review project identity differs: {project_key}"
            )
        site_id = _stable_id("vcc-site", site_key)
        status_evidence_id = source["status_evidence_id"]
        geometry_evidence_id = (
            bridge["geometry_evidence"]["evidence_id"]
            if bridge is not None
            else geometry_release["snapshot_evidence_id"]
        )
        for evidence_id, role in (
            (status_evidence_id, "physical_status"),
            (geometry_evidence_id, "geometry"),
        ):
            if evidence_id not in evidence_by_id:
                raise VerifiedConstructionCoreError(
                    f"project references absent {role} evidence: {project_key}"
                )
            evidence_usage[evidence_id]["roles"].add(role)
            evidence_usage[evidence_id]["project_ids"].add(project_id)
        pinned_geometry_evidence = (
            _geometry_evidence_projection(bridge)
            if bridge is not None
            else next(
                row
                for row in source_record["evidence"]
                if row.get("key") == acceptance["geometry_evidence_key"]
            )
        )
        published_geometry_evidence = evidence_by_id[geometry_evidence_id]
        for field in (
            "kind",
            "title",
            "source_url",
            "publisher",
            "source_family",
            "license",
            "attribution",
            "published_at",
            "retrieved_at",
            "content_hash",
        ):
            pinned_value = pinned_geometry_evidence.get(field)
            if pinned_value is None:
                pinned_value = ""
            if published_geometry_evidence.get(field, "") != pinned_value:
                raise VerifiedConstructionCoreError(
                    f"reviewed geometry evidence field {field} differs: {project_key}"
                )

        observations = _parse_json_field(
            source["capacity_estimates_json"], "capacity_estimates"
        )
        if not isinstance(observations, list):
            raise VerifiedConstructionCoreError("capacity observations are not a list")
        if any(not isinstance(row, dict) for row in observations):
            raise VerifiedConstructionCoreError("capacity observation is not an object")
        for observation in observations:
            _validate_typed_observation(
                observation, POWER_METRICS | ENERGY_METRICS | EFFICIENCY_METRICS
            )
        unexpected_metrics = {
            row.get("metric")
            for row in observations
            if row.get("metric")
            not in POWER_METRICS | ENERGY_METRICS | EFFICIENCY_METRICS
        }
        if unexpected_metrics:
            raise VerifiedConstructionCoreError(
                f"unclassified typed metrics {sorted(unexpected_metrics)}: {project_key}"
            )
        power = [row for row in observations if row.get("metric") in POWER_METRICS]
        energy = [row for row in observations if row.get("metric") in ENERGY_METRICS]
        efficiency = [
            row for row in observations if row.get("metric") in EFFICIENCY_METRICS
        ]
        source_workloads = _parse_json_field(source["workloads_json"], "workloads")
        if not isinstance(source_workloads, list):
            raise VerifiedConstructionCoreError("workloads are not a list")
        if any(not isinstance(row, dict) for row in source_workloads):
            raise VerifiedConstructionCoreError("workload observation is not an object")
        for observation in observations:
            evidence_id = observation.get("evidence_id")
            metric = observation.get("metric")
            if not evidence_id or evidence_id not in evidence_by_id or not metric:
                raise VerifiedConstructionCoreError(
                    f"typed metric evidence is absent: {project_key}"
                )
            evidence_usage[evidence_id]["roles"].add(f"typed_metric:{metric}")
            evidence_usage[evidence_id]["project_ids"].add(project_id)
        workloads: list[dict[str, Any]] = []
        for source_workload in source_workloads:
            _validate_source_workload_observation(source_workload)
            binding_key = (
                project_key,
                source_workload["evidence_id"],
                source_workload["workload"],
            )
            binding = workload_bindings.get(binding_key)
            if binding is None or any(
                source_workload[field] != binding[field]
                for field in SOURCE_WORKLOAD_FIELDS
            ):
                raise VerifiedConstructionCoreError(
                    f"workload provenance is absent or differs: {project_key}"
                )
            workload = {
                **source_workload,
                "deployment_scope": binding["deployment_scope"],
            }
            _validate_workload_observation(workload)
            workloads.append(workload)
            used_workload_bindings.add(binding_key)
            evidence_id = workload["evidence_id"]
            if not evidence_id or evidence_id not in evidence_by_id:
                raise VerifiedConstructionCoreError(
                    f"workload evidence is absent: {project_key}"
                )
            evidence_usage[evidence_id]["roles"].add("workload")
            evidence_usage[evidence_id]["roles"].add(
                f"workload_scope:{workload['deployment_scope']}"
            )
            evidence_usage[evidence_id]["project_ids"].add(project_id)
        operating_model_evidence_id = source["operating_model_evidence_id"]
        if operating_model_evidence_id:
            if operating_model_evidence_id not in evidence_by_id:
                raise VerifiedConstructionCoreError(
                    f"operating-model evidence is absent: {project_key}"
                )
            evidence_usage[operating_model_evidence_id]["roles"].add(
                "operating_model"
            )
            evidence_usage[operating_model_evidence_id]["project_ids"].add(
                project_id
            )
        source_role_keys: set[tuple[str, str, str]] = set()
        for role, column in ROLE_COLUMNS.items():
            parties = [
                party.strip()
                for party in source[column].split(";")
                if party.strip()
            ]
            if len(parties) != len(set(parties)):
                raise VerifiedConstructionCoreError(
                    f"source role parties are not unique: {project_key}"
                )
            source_role_keys.update((project_key, role, party) for party in parties)
        accepted_role_keys = {
            (project_key, row["role"], row["party"])
            for row in role_bindings.get(project_key, [])
        }
        excluded_role_keys = {
            (project_key, row["role"], row["party"])
            for row in role_exclusions.get(project_key, [])
        }
        if source_role_keys != accepted_role_keys | excluded_role_keys:
            raise VerifiedConstructionCoreError(
                f"source role provenance coverage differs: {project_key}"
            )
        role_claims: list[dict[str, str]] = []
        projected_roles: dict[str, list[str]] = {
            role: [] for role in ROLE_COLUMNS
        }
        for binding in role_bindings.get(project_key, []):
            binding_key = (project_key, binding["role"], binding["party"])
            used_role_bindings.add(binding_key)
            evidence_id = binding["evidence_id"]
            if evidence_id not in evidence_by_id:
                raise VerifiedConstructionCoreError(
                    f"role evidence is absent: {project_key}"
                )
            role_claims.append(
                {
                    "evidence_id": evidence_id,
                    "party": binding["party"],
                    "relationship_scope": binding["relationship_scope"],
                    "role": binding["role"],
                }
            )
            projected_roles[binding["role"]].append(binding["party"])
            evidence_usage[evidence_id]["roles"].add(f"role:{binding['role']}")
            evidence_usage[evidence_id]["project_ids"].add(project_id)
        used_role_exclusions.update(excluded_role_keys)
        role_claims.sort(key=lambda row: (row["role"], row["party"], row["evidence_id"]))
        role_projection = {
            column: "; ".join(sorted(projected_roles[role]))
            for role, column in ROLE_COLUMNS.items()
        }
        status_date = _calendar_date(source["status_as_of"], "status_as_of")
        geometry_type = geometry["type"]
        uncertainty = acceptance["horizontal_uncertainty_metres"]
        if bridge is None:
            geometry_authority_class, geometry_use_scope = (
                _direct_geometry_semantics(geometry_type)
            )
            uncertainty_unknown_reason = (
                "official source does not state positional accuracy"
                if uncertainty is None
                else ""
            )
        else:
            decision = bridge["review_decision"]
            geometry_authority_class = decision["geometry_authority_class"]
            geometry_use_scope = decision["geometry_use_scope"]
            uncertainty_unknown_reason = decision[
                "horizontal_uncertainty_unknown_reason"
            ]
        projects.append(
            {
                "project_id": project_id,
                "project_stable_key": project_key,
                "site_id": site_id,
                "physical_site_stable_key": site_key,
                "name": source["name"],
                "country": source["country"],
                "country_iso_a2": source["country_iso_a2"],
                "latitude": geometry_release["latitude"],
                "longitude": geometry_release["longitude"],
                "geometry_json": _json_bytes(geometry).decode().strip(),
                "geometry_type": geometry_type,
                "geometry_source_entity_kind": acceptance["geometry_entity"],
                "geometry_derivation": acceptance["geometry_derivation"],
                "geometry_method": acceptance["geometry_method"],
                "geometry_scope_class": acceptance["geometry_scope_class"],
                "geometry_authority_class": geometry_authority_class,
                "geometry_use_scope": geometry_use_scope,
                "geometry_precision_scope": acceptance["precision_scope"],
                "horizontal_uncertainty_metres": (
                    "" if uncertainty is None else uncertainty
                ),
                "horizontal_uncertainty_unknown_reason": uncertainty_unknown_reason,
                "geometry_evidence_id": geometry_evidence_id,
                "last_observed_physical_status": source["status"],
                "status_as_of": source["status_as_of"],
                "status_age_days_at_review": (REVIEW_DATE - status_date).days,
                "status_method": source["status_method"],
                "status_evidence_id": status_evidence_id,
                "verification_posture": _verification_posture(
                    geometry_authority_class, geometry_use_scope
                ),
                "independent_imagery_verification": "false",
                "imagery_review_outcome": _imagery_outcome(
                    project_key, default_imagery_outcome, imagery_by_key
                ),
                "development_type": "unknown",
                "development_type_unknown_reason": (
                    "source evidence does not distinguish greenfield, expansion, or retrofit"
                ),
                "operating_model": source["operating_model"] or "unknown",
                "operating_model_unknown_reason": (
                    "" if source["operating_model"] else "not established by selected evidence"
                ),
                "operating_model_evidence_id": operating_model_evidence_id,
                "workloads_json": _json_bytes(workloads).decode().strip(),
                "workload_unknown_reason": (
                    "" if workloads else "not established by selected evidence"
                ),
                "role_claims_json": _json_bytes(role_claims).decode().strip(),
                "power_observations_json": _json_bytes(power).decode().strip(),
                "power_unknown_reason": (
                    "" if power else "no typed project power observation; not estimated"
                ),
                "annual_energy_observations_json": _json_bytes(energy).decode().strip(),
                "annual_energy_unknown_reason": (
                    "" if energy else "no scoped annual-energy inputs; not estimated"
                ),
                "efficiency_observations_json": _json_bytes(efficiency)
                .decode()
                .strip(),
                "efficiency_unknown_reason": (
                    "" if efficiency else "no scoped PUE or WUE observation"
                ),
                "owner": role_projection["owner"],
                "operator": role_projection["operator"],
                "users": role_projection["users"],
                "tenants": role_projection["tenants"],
                "customers": role_projection["customers"],
                "status_source_url": evidence_by_id[status_evidence_id]["source_url"],
                "geometry_source_url": evidence_by_id[geometry_evidence_id]["source_url"],
            }
        )

    if used_workload_bindings != set(workload_bindings):
        raise VerifiedConstructionCoreError("workload provenance has unused bindings")
    expected_role_bindings = {
        (row["project_stable_key"], row["role"], row["party"])
        for row in provenance["role_bindings"]
    }
    expected_role_exclusions = {
        (row["project_stable_key"], row["role"], row["party"])
        for row in provenance["excluded_source_roles"]
    }
    if (
        used_role_bindings != expected_role_bindings
        or used_role_exclusions != expected_role_exclusions
    ):
        raise VerifiedConstructionCoreError("role provenance has unused decisions")

    by_site: dict[str, list[dict[str, Any]]] = defaultdict(list)
    for project in projects:
        by_site[project["physical_site_stable_key"]].append(project)
    sites: list[dict[str, Any]] = []
    for site_key in sorted(by_site):
        members = sorted(by_site[site_key], key=lambda row: row["project_id"])
        site_source = entities_by_key[site_key]
        geometry_member = _preferred_site_geometry_member(members)
        sites.append(
            {
                "site_id": members[0]["site_id"],
                "physical_site_stable_key": site_key,
                "name": site_source["name"],
                "country": site_source["country"],
                "country_iso_a2": site_source["country_iso_a2"],
                "latitude": geometry_member["latitude"],
                "longitude": geometry_member["longitude"],
                "geometry_json": geometry_member["geometry_json"],
                "geometry_type": geometry_member["geometry_type"],
                "geometry_source_entity_kinds_json": _json_bytes(
                    sorted({row["geometry_source_entity_kind"] for row in members})
                ).decode().strip(),
                "geometry_derivations_json": _json_bytes(
                    sorted({row["geometry_derivation"] for row in members})
                ).decode().strip(),
                "geometry_methods_json": _json_bytes(
                    sorted({row["geometry_method"] for row in members})
                ).decode().strip(),
                "geometry_scope_classes_json": _json_bytes(
                    sorted({row["geometry_scope_class"] for row in members})
                ).decode().strip(),
                "geometry_authority_classes_json": _json_bytes(
                    sorted({row["geometry_authority_class"] for row in members})
                ).decode().strip(),
                "geometry_use_scopes_json": _json_bytes(
                    sorted({row["geometry_use_scope"] for row in members})
                ).decode().strip(),
                "geometry_precision_scopes_json": _json_bytes(
                    sorted({row["geometry_precision_scope"] for row in members})
                ).decode().strip(),
                "horizontal_uncertainty_metres": geometry_member[
                    "horizontal_uncertainty_metres"
                ],
                "horizontal_uncertainty_unknown_reason": geometry_member[
                    "horizontal_uncertainty_unknown_reason"
                ],
                "geometry_evidence_ids_json": _json_bytes(
                    sorted({row["geometry_evidence_id"] for row in members})
                ).decode().strip(),
                "project_count": len(members),
                "project_ids_json": _json_bytes(
                    [row["project_id"] for row in members]
                ).decode().strip(),
                "project_stable_keys_json": _json_bytes(
                    [row["project_stable_key"] for row in members]
                ).decode().strip(),
                "statuses_json": _json_bytes(
                    sorted({row["last_observed_physical_status"] for row in members})
                ).decode().strip(),
                "oldest_status_as_of": min(row["status_as_of"] for row in members),
                "newest_status_as_of": max(row["status_as_of"] for row in members),
                "verification_posture": geometry_member["verification_posture"],
                "independent_imagery_verification": "false",
                "imagery_review_outcomes_json": _json_bytes(
                    sorted({row["imagery_review_outcome"] for row in members})
                ).decode().strip(),
            }
        )

    selected_evidence: list[dict[str, Any]] = []
    for evidence_id in sorted(evidence_usage):
        source = evidence_by_id[evidence_id]
        usage = evidence_usage[evidence_id]
        selected_evidence.append(
            {
                **source,
                "roles_json": _json_bytes(sorted(usage["roles"])).decode().strip(),
                "project_ids_json": _json_bytes(
                    sorted(usage["project_ids"])
                ).decode().strip(),
            }
        )

    country_counts = Counter(row["country"] for row in sites)
    gates = _final_release_gates(sites, projects)
    selection_report = {
        "format": "datacenter-atlas-verified-construction-core-selection-v6",
        "release_status": "preview",
        "publishable_as_final": all(gate.get("passed", False) for gate in gates.values()),
        "source_release_id": SOURCE_RELEASE_ID,
        "reviewed_at": REVIEW_DATE.isoformat(),
        "maximum_status_age_days": MAX_STATUS_AGE_DAYS,
        "source_pipeline_row_count": len(pipeline),
        "selected_project_count": len(projects),
        "selected_physical_site_count": len(sites),
        "official_boundary_project_count": sum(
            _is_official_boundary(row) for row in projects
        ),
        "reviewed_site_locator_project_count": sum(
            _is_reviewed_locator(row) for row in projects
        ),
        "non_selected_source_row_count": len(pipeline) - len(projects),
        "selection_first_failure_counts": dict(sorted(reason_counts.items())),
        "country_counts": dict(sorted(country_counts.items())),
        "final_release_gates": gates,
        "imagery_review_provenance": imagery_records,
        "provenance_decisions": {
            "workload_scope_bindings": provenance["workload_scope_bindings"],
            "role_bindings": provenance["role_bindings"],
            "excluded_source_roles": provenance["excluded_source_roles"],
        },
        "reviewed_overlay_queue": overlays.get("overlays", []),
        "semantic_guardrails": SEMANTIC_GUARDRAILS,
    }
    return {
        "source_manifest": source_manifest,
        "projects": projects,
        "sites": sites,
        "evidence": selected_evidence,
        "selection_report": selection_report,
    }


def _geojson(sites: Sequence[Mapping[str, Any]]) -> dict[str, Any]:
    return {
        "type": "FeatureCollection",
        "name": "Data Center Atlas Verified Construction Core preview",
        "release_status": "preview",
        "features": [
            {
                "type": "Feature",
                "id": row["site_id"],
                "geometry": json.loads(row["geometry_json"]),
                "properties": {
                    "site_id": row["site_id"],
                    "physical_site_stable_key": row["physical_site_stable_key"],
                    "name": row["name"],
                    "country": row["country"],
                    "project_count": int(row["project_count"]),
                    "geometry_type": row["geometry_type"],
                    "geometry_scope_classes": json.loads(
                        row["geometry_scope_classes_json"]
                    ),
                    "geometry_authority_classes": json.loads(
                        row["geometry_authority_classes_json"]
                    ),
                    "geometry_use_scopes": json.loads(
                        row["geometry_use_scopes_json"]
                    ),
                    "geometry_precision_scopes": json.loads(
                        row["geometry_precision_scopes_json"]
                    ),
                    "horizontal_uncertainty_metres": (
                        None
                        if row["horizontal_uncertainty_metres"] == ""
                        else float(row["horizontal_uncertainty_metres"])
                    ),
                    "statuses": json.loads(row["statuses_json"]),
                    "newest_status_as_of": row["newest_status_as_of"],
                    "verification_posture": row["verification_posture"],
                    "independent_imagery_verification": False,
                },
            }
            for row in sites
        ],
    }


def _map_attribution_notices(
    evidence: Sequence[Mapping[str, Any]],
) -> tuple[tuple[str, str, str], ...]:
    source_families = {
        str(row.get("source_family", ""))
        for row in evidence
        if "geometry" in json.loads(str(row.get("roles_json", "[]")))
    }
    notices: list[tuple[str, str, str]] = []
    if "openstreetmap" in source_families:
        notices.append(
            (
                "© OpenStreetMap contributors",
                "ODbL 1.0",
                "https://www.openstreetmap.org/copyright",
            )
        )
    if any(source_family.startswith("kartverket_") for source_family in source_families):
        notices.append(
            (
                "© Kartverket",
                "CC BY 4.0",
                "https://www.kartverket.no/en/api-and-data/terms-of-use",
            )
        )
    return tuple(notices)


def _map_html(
    geojson: Mapping[str, Any],
    evidence: Sequence[Mapping[str, Any]],
) -> bytes:
    data = json.dumps(geojson, ensure_ascii=False, sort_keys=True).replace("<", "\\u003c")
    site_count = len(geojson.get("features", []))
    attribution = " · ".join(
        f'<a href="{url}">{label}</a> — {license_name}'
        for label, license_name, url in _map_attribution_notices(evidence)
    )
    document = f"""<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>Verified Construction Core preview</title>
<style>body{{font:14px system-ui;margin:0;color:#17202a}}header{{padding:18px 22px;background:#eef4f7}}main{{display:grid;grid-template-columns:minmax(0,2fr) minmax(260px,1fr);gap:16px;padding:16px}}footer{{padding:0 16px 18px;color:#46545f}}svg{{width:100%;height:auto;background:#f8fafb;border:1px solid #ccd6dc}}.grid{{stroke:#dce4e8;stroke-width:1}}circle{{fill:#b6412e;stroke:#fff;stroke-width:1.5;cursor:pointer}}circle:focus{{outline:2px solid #173b57}}table{{border-collapse:collapse;width:100%}}th,td{{padding:6px;border-bottom:1px solid #ddd;text-align:left}}code{{font-size:12px}}.warning{{color:#7b2d1d;font-weight:700}}@media(max-width:800px){{main{{grid-template-columns:1fr}}}}</style></head>
<body><header><h1>Verified Construction Core v0.6 preview</h1><p class="warning">{site_count} physical sites. This is not the 100-site final release and does not claim independent imagery verification.</p></header>
<main><section><svg id="map" viewBox="0 0 1000 500" role="img" aria-label="Global plot of selected sites"></svg></section><aside><h2 id="name">Select a site</h2><div id="detail"></div><h3>Sites</h3><table><tbody id="rows"></tbody></table></aside></main>
<footer><strong>Map data attribution:</strong> {attribution}. <a href="ATTRIBUTION.txt">Full attribution and source terms</a>.</footer>
<script>const atlas={data};const svg=document.getElementById('map');const ns='http://www.w3.org/2000/svg';
for(let lon=-180;lon<=180;lon+=30){{const l=document.createElementNS(ns,'line');l.setAttribute('x1',(lon+180)/360*1000);l.setAttribute('x2',(lon+180)/360*1000);l.setAttribute('y1',0);l.setAttribute('y2',500);l.setAttribute('class','grid');svg.appendChild(l)}}
for(let lat=-60;lat<=60;lat+=30){{const l=document.createElementNS(ns,'line');l.setAttribute('x1',0);l.setAttribute('x2',1000);l.setAttribute('y1',(90-lat)/180*500);l.setAttribute('y2',(90-lat)/180*500);l.setAttribute('class','grid');svg.appendChild(l)}}
function point(g){{if(g.type==='Point')return g.coordinates;if(g.type==='Polygon'){{const a=g.coordinates[0];return [a.reduce((s,p)=>s+p[0],0)/a.length,a.reduce((s,p)=>s+p[1],0)/a.length]}}const a=g.coordinates[0][0];return [a.reduce((s,p)=>s+p[0],0)/a.length,a.reduce((s,p)=>s+p[1],0)/a.length]}}
function show(f){{document.getElementById('name').textContent=f.properties.name;const d=document.getElementById('detail');d.textContent='';for(const [k,v] of Object.entries(f.properties)){{const p=document.createElement('p');const b=document.createElement('b');b.textContent=k+': ';p.appendChild(b);p.appendChild(document.createTextNode(Array.isArray(v)?v.join(', '):String(v)));d.appendChild(p)}}}}
const tbody=document.getElementById('rows');for(const f of atlas.features){{const [lon,lat]=point(f.geometry);const c=document.createElementNS(ns,'circle');c.setAttribute('cx',(lon+180)/360*1000);c.setAttribute('cy',(90-lat)/180*500);c.setAttribute('r',6);c.setAttribute('tabindex',0);c.setAttribute('aria-label',f.properties.name);c.onclick=()=>show(f);c.onkeydown=e=>{{if(e.key==='Enter')show(f)}};svg.appendChild(c);const tr=document.createElement('tr');const td=document.createElement('td');const a=document.createElement('button');a.textContent=f.properties.name+' — '+f.properties.country;a.onclick=()=>show(f);td.appendChild(a);tr.appendChild(td);tbody.appendChild(tr)}}
</script></body></html>"""
    return document.encode("utf-8")


def _readme(report: Mapping[str, Any]) -> bytes:
    gates = report["final_release_gates"]
    boundary_count = report["official_boundary_project_count"]
    locator_count = report["reviewed_site_locator_project_count"]
    text = f"""# Verified Construction Core v0.6 preview

This tracked preview contains **{report['selected_physical_site_count']} physical sites** and
**{report['selected_project_count']} linked projects** selected from `{SOURCE_RELEASE_ID}`.
Every selected project has a physical-status observation no more than {MAX_STATUS_AGE_DAYS} days
old at the {REVIEW_DATE.isoformat()} review date. {boundary_count} project rows carry official
parcel or surveyed boundary geometry; this describes the geometry attached to the row, not a claim
that construction occupies the entire parcel. The other {locator_count} use explicitly labelled
reviewed locators with their precision limits preserved. Nine selected project rows use
contributor-mapped OSM polygon locators (seven distinct polygons), accepted only as campus locators:
they are not official boundaries, project or phase
footprints, construction extents, or lifecycle evidence. Locality centroids and model-only lifecycle
states fail the selector.

The v0.6 delta adds an official PNQ04 project reference point; official Goodman HKG09 and Skygard
OSL1 campus address/reference points; an official current Kvandal parcel used only as a campus
locator; one shared STT Jakarta 1 OSM campus locator for Jakarta 3, 5, and 6; and the official
three-parcel Undheim project-site union. None of those locator geometries is promoted to a current
building footprint. The Undheim parcel union is an official boundary, but still not a claim that
construction fills the parcels.

This is **not** the final Verified Construction Core v1. It does not change the historical Atlas
`construction_verified=false` field, infer a continuously current state, claim independent imagery
verification, or claim completeness. The final release remains blocked at
{gates['site_count']['actual']}/{gates['site_count']['required']} sites,
{gates['country_count']['actual']}/{gates['country_count']['required_minimum']} countries, and
{gates['non_us_site_count']['actual']}/{gates['non_us_site_count']['required_minimum']} non-US sites.

Files:

- `sites.csv` and `projects.csv`: the reviewed physical-site/project cohort.
- `sites.geojson` and `map.html`: matching clean-clone-readable map products.
- `evidence.csv`: status, geometry, operating-model, workload, and typed-metric evidence.
- `schema.json`: machine-readable field, relationship, GeoJSON, and map contract.
- `selection-report.json`: accounting for every source pipeline row and every final gate.
- `manifest.json` and `manifest.sha256`: byte and SHA-256 bindings.

Missing power, annual energy, operating model, workload, and development type remain explicit
unknowns; the builder never converts missing values to zero. Existing satellite reviews remain
non-claiming analyst evidence. The selection report binds each non-default imagery outcome to its
exact tracked review source and preserves locally unsealed identity lineage and unadjudicated
conflicts explicitly. Two exact Canadian geometry overlays were reviewed but excluded: one recent
scene was unusable and one usable comparison showed no filtered recent-change component.
Backe-published Undheim contractor drone photography is source-linked context only: no image bytes
are redistributed, no independent imagery review is claimed, and it supports no geometry, status,
capacity, progress, or building-count field.

Every workload observation carries a validator-bound `deployment_scope`; all five selected
observations are `intended`, never operational. `role_claims_json` binds each published role to an
evidence ID and relationship scope. Six intended-operator and two intended-customer claims pass
that gate. Four prior CDC/AST owner or operator strings lacked role-specific evidence, so the preview
clears them and preserves the rejected claims and reasons in the provenance contract rather than
laundering status evidence into role evidence. The same conservative rule clears the atNorth,
QScale, Microsoft Mount Pleasant, and Amazon Salem operator candidates; OSM operator tags, campus
operating models, and branded status evidence are identity context, not project-role proof.

Saline's source-level 1,400 MW contracted grid-load observation and modeled annual-energy range are
retained only in the closed bridge as campus-scoped provenance. They are not promoted to project or
site power or energy, and the modeled range is not measured consumption.

The preview validates from a public clean clone. Rebuilding it still requires the locally hydrated
v97 payload, so clean-clone rebuildability is an explicit failed final-release gate rather than an
implied capability.
"""
    return text.encode("utf-8")


def _field_contract(name: str) -> dict[str, Any]:
    json_array_fields = {
        "workloads_json",
        "role_claims_json",
        "power_observations_json",
        "annual_energy_observations_json",
        "efficiency_observations_json",
        "geometry_source_entity_kinds_json",
        "geometry_derivations_json",
        "geometry_methods_json",
        "geometry_scope_classes_json",
        "geometry_authority_classes_json",
        "geometry_use_scopes_json",
        "geometry_precision_scopes_json",
        "geometry_evidence_ids_json",
        "project_ids_json",
        "project_stable_keys_json",
        "statuses_json",
        "imagery_review_outcomes_json",
        "roles_json",
    }
    logical_type = "string"
    if name in json_array_fields:
        logical_type = "json_array"
    elif name == "geometry_json":
        logical_type = "geojson_geometry"
    elif name in {"latitude", "longitude", "horizontal_uncertainty_metres"}:
        logical_type = "number"
    elif name in {"project_count", "status_age_days_at_review"}:
        logical_type = "integer"
    elif name == "independent_imagery_verification":
        logical_type = "boolean"
    elif name in {"status_as_of", "oldest_status_as_of", "newest_status_as_of"}:
        logical_type = "date"
    elif name in {"published_at", "retrieved_at"}:
        logical_type = "date_or_datetime"
    elif name in {"status_source_url", "geometry_source_url", "source_url"}:
        logical_type = "uri"
    elif name == "content_hash":
        logical_type = "sha256"
    allows_empty = name in {
        "horizontal_uncertainty_metres",
        "horizontal_uncertainty_unknown_reason",
        "operating_model_unknown_reason",
        "operating_model_evidence_id",
        "workload_unknown_reason",
        "power_unknown_reason",
        "annual_energy_unknown_reason",
        "efficiency_unknown_reason",
        "owner",
        "operator",
        "users",
        "tenants",
        "customers",
        "license",
        "attribution",
        "published_at",
    }
    contract: dict[str, Any] = {
        "name": name,
        "logical_type": logical_type,
        "allows_empty_string": allows_empty,
    }
    enumerations = {
        "geometry_type": ["Point", "Polygon", "MultiPolygon"],
        "geometry_source_entity_kind": ["project", "campus"],
        "geometry_derivation": [
            "direct_geometry",
            "coordinates_to_point",
            "cross_source_overlay",
            "official_parcel_union",
        ],
        "geometry_authority_class": ["official_source", "community_mapped"],
        "geometry_use_scope": [
            "official_boundary",
            "reviewed_site_locator",
            "project_locator",
            "campus_locator",
        ],
        "last_observed_physical_status": sorted(PHYSICAL_STATUSES),
        "status_method": sorted(AUTHORITATIVE_STATUS_METHODS),
        "independent_imagery_verification": [False],
    }
    if name in enumerations:
        contract["allowed_values"] = enumerations[name]
    return contract


def _schema() -> dict[str, Any]:
    return {
        "format": "datacenter-atlas-verified-construction-core-schema-v6",
        "preview_id": PREVIEW_ID,
        "csv_encoding": "UTF-8",
        "csv_dialect": {
            "delimiter": ",",
            "quote_character": '"',
            "line_terminator": "LF",
            "header": True,
        },
        "tables": {
            "projects.csv": {
                "fields": [_field_contract(name) for name in PROJECT_FIELDS],
                "primary_key": ["project_id"],
            },
            "sites.csv": {
                "fields": [_field_contract(name) for name in SITE_FIELDS],
                "primary_key": ["site_id"],
            },
            "evidence.csv": {
                "fields": [_field_contract(name) for name in EVIDENCE_FIELDS],
                "primary_key": ["evidence_id"],
            },
        },
        "foreign_keys": [
            {
                "from": ["projects.csv", "site_id"],
                "to": ["sites.csv", "site_id"],
            },
            {
                "from": ["projects.csv", "status_evidence_id"],
                "to": ["evidence.csv", "evidence_id"],
            },
            {
                "from": ["projects.csv", "geometry_evidence_id"],
                "to": ["evidence.csv", "evidence_id"],
            },
            {
                "from": ["projects.csv", "operating_model_evidence_id"],
                "to": ["evidence.csv", "evidence_id"],
                "allows_empty_string": True,
            },
        ],
        "json_embedded_evidence_fields": [
            ["projects.csv", "workloads_json", "evidence_id"],
            ["projects.csv", "role_claims_json", "evidence_id"],
            ["projects.csv", "power_observations_json", "evidence_id"],
            ["projects.csv", "annual_energy_observations_json", "evidence_id"],
            ["projects.csv", "efficiency_observations_json", "evidence_id"],
        ],
        "embedded_array_items": {
            "projects.csv:power_observations_json": {
                "item_type": "typed_metric_observation",
                "allowed_metrics": sorted(POWER_METRICS),
            },
            "projects.csv:annual_energy_observations_json": {
                "item_type": "typed_metric_observation",
                "allowed_metrics": sorted(ENERGY_METRICS),
            },
            "projects.csv:efficiency_observations_json": {
                "item_type": "typed_metric_observation",
                "allowed_metrics": sorted(EFFICIENCY_METRICS),
            },
            "projects.csv:workloads_json": {
                "item_type": "workload_observation"
            },
            "projects.csv:role_claims_json": {
                "item_type": "role_claim"
            },
        },
        "embedded_types": {
            "typed_metric_observation": {
                "required_fields": [
                    "as_of_date",
                    "base",
                    "confidence",
                    "evidence_id",
                    "high",
                    "low",
                    "method",
                    "metric",
                    "notes",
                    "stage",
                    "target_date",
                    "unit",
                ],
                "unit_by_metric": METRIC_UNITS,
                "allowed_methods": sorted(ESTIMATE_METHODS),
                "allowed_stages": sorted(CAPACITY_STAGES),
                "interval_constraint": "0 <= low <= base <= high",
                "confidence_constraint": "0 <= confidence <= 1",
                "target_date_allows_null": True,
            },
            "workload_observation": {
                "required_fields": [
                    "as_of_date",
                    "confidence",
                    "deployment_scope",
                    "evidence_id",
                    "method",
                    "workload",
                ],
                "allowed_deployment_scopes": [
                    "intended",
                    "operational",
                    "unknown",
                ],
                "confidence_constraint": "0 <= confidence <= 1",
            },
            "role_claim": {
                "required_fields": [
                    "evidence_id",
                    "party",
                    "relationship_scope",
                    "role",
                ],
                "allowed_relationship_scopes": [
                    "current",
                    "intended",
                    "unknown",
                ],
                "allowed_roles": sorted(ROLE_COLUMNS),
            },
        },
        "geojson": {
            "path": "sites.geojson",
            "feature_id_equals": ["sites.csv", "site_id"],
            "geometry_equals": ["sites.csv", "geometry_json"],
            "allowed_geometry_types": ["Point", "Polygon", "MultiPolygon"],
        },
        "map": {
            "path": "map.html",
            "derived_from": ["sites.geojson", "evidence.csv"],
        },
    }


def _attribution(
    evidence: Sequence[Mapping[str, Any]],
    overlays: Sequence[Mapping[str, Any]],
    imagery_records: Sequence[Mapping[str, Any]],
) -> bytes:
    rows = {
        (row["publisher"], row["license"], row["source_url"])
        for row in evidence
    }
    if any(row.get("source_family") == "openstreetmap" for row in evidence) or any(
        str(row.get("geometry_stable_key", "")).startswith("osm:")
        for row in overlays
    ):
        rows.add(
            (
                "© OpenStreetMap contributors",
                "ODbL-1.0",
                "https://www.openstreetmap.org/copyright",
            )
        )
    lines = [
        f"Data Center Atlas Verified Construction Core {PREVIEW_ID.rsplit('-', 1)[-1]} preview",
        "",
        "Compact derived facts only; third-party terms remain controlling.",
        "",
    ]
    lines.extend(
        f"- {publisher} | {license_name} | {url}"
        for publisher, license_name, url in sorted(rows)
    )
    if imagery_records:
        lines.extend(
            [
                "",
                "Imagery review attribution:",
                "Contains modified Copernicus Sentinel data 2024 and 2026.",
                "Dataset: Copernicus Sentinel-2 Level-2A, accessed through Element 84 Earth Search.",
                "Sentinel Data Legal Notice: https://sentinels.copernicus.eu/documents/247904/690755/Sentinel_Data_Legal_Notice",
            ]
        )
    return ("\n".join(lines) + "\n").encode("utf-8")


def build_preview(output_dir: Path = PREVIEW_DIR) -> dict[str, Any]:
    """Build the preview atomically and refuse to overwrite an existing path."""
    output_dir = Path(output_dir)
    if output_dir.exists():
        raise VerifiedConstructionCoreError(f"refusing to overwrite {output_dir}")
    data = _build_rows()
    sites = data["sites"]
    projects = data["projects"]
    evidence = data["evidence"]
    report = data["selection_report"]
    geojson = _geojson(sites)
    payloads = {
        "ATTRIBUTION.txt": _attribution(
            evidence,
            report["reviewed_overlay_queue"],
            report["imagery_review_provenance"],
        ),
        "README.md": _readme(report),
        "evidence.csv": _csv_bytes(evidence, EVIDENCE_FIELDS),
        "map.html": _map_html(geojson, evidence),
        "projects.csv": _csv_bytes(projects, PROJECT_FIELDS),
        "schema.json": _json_bytes(_schema()),
        "selection-report.json": _json_bytes(report),
        "sites.csv": _csv_bytes(sites, SITE_FIELDS),
        "sites.geojson": _json_bytes(geojson),
    }
    manifest = {
        "format": "datacenter-atlas-verified-construction-core-preview-v6",
        "preview_id": PREVIEW_ID,
        "release_status": "preview",
        "publishable_as_final": report["publishable_as_final"],
        "source_release_id": SOURCE_RELEASE_ID,
        "source_release_manifest_path": str(
            (SOURCE_RELEASE / "manifest.json").relative_to(ROOT)
        ),
        "source_release_manifest_sha256": _sha256_file(SOURCE_RELEASE / "manifest.json"),
        "definition_paths": {
            "imagery_reviews": str(IMAGERY_REVIEW_DEFINITION.relative_to(ROOT)),
            "provenance": str(PROVENANCE_DEFINITION.relative_to(ROOT)),
            "reviewed_overlays": str(OVERLAY_DEFINITION.relative_to(ROOT)),
            "reviewed_sites": str(REVIEW_DEFINITION.relative_to(ROOT)),
        },
        "review_definition_sha256": _sha256_file(REVIEW_DEFINITION),
        "imagery_review_definition_sha256": _sha256_file(
            IMAGERY_REVIEW_DEFINITION
        ),
        "provenance_definition_sha256": _sha256_file(PROVENANCE_DEFINITION),
        "overlay_definition_sha256": _sha256_file(OVERLAY_DEFINITION),
        "portable_source_inputs": _portable_source_inputs(),
        "reviewed_at": REVIEW_DATE.isoformat(),
        "counts": {
            "physical_sites": len(sites),
            "projects": len(projects),
            "evidence": len(evidence),
            "countries": len({row["country"] for row in sites}),
            "non_us_sites": sum(row["country_iso_a2"] != "US" for row in sites),
            "official_boundary_projects": sum(
                _is_official_boundary(row) for row in projects
            ),
            "reviewed_site_locator_projects": sum(
                _is_reviewed_locator(row) for row in projects
            ),
        },
        "files": {
            name: {"bytes": len(payload), "sha256": _sha256_bytes(payload)}
            for name, payload in sorted(payloads.items())
        },
    }
    manifest_bytes = _json_bytes(manifest)
    payloads["manifest.json"] = manifest_bytes
    payloads["manifest.sha256"] = (
        f"{_sha256_bytes(manifest_bytes)}  manifest.json\n".encode()
    )
    output_dir.parent.mkdir(parents=True, exist_ok=True)
    stage = Path(tempfile.mkdtemp(prefix=f".{output_dir.name}.", dir=output_dir.parent))
    try:
        for name, payload in payloads.items():
            path = stage / name
            with path.open("xb") as handle:
                handle.write(payload)
        os.replace(stage, output_dir)
    except Exception:
        shutil.rmtree(stage, ignore_errors=True)
        raise
    validate_preview(output_dir)
    return manifest


def _validate_frozen_preview_inventory(
    path: Path,
    *,
    preview_id: str,
    manifest_sha256: str,
    version_label: str,
) -> dict[str, Any]:
    path = Path(path)
    if path.is_symlink() or not path.is_dir():
        raise VerifiedConstructionCoreError(
            f"frozen {version_label} preview is not a regular directory: {path}"
        )
    manifest_path = path / "manifest.json"
    checksum_path = path / "manifest.sha256"
    if (
        manifest_path.is_symlink()
        or not manifest_path.is_file()
        or checksum_path.is_symlink()
        or not checksum_path.is_file()
    ):
        raise VerifiedConstructionCoreError(
            f"frozen {version_label} manifest trust root differs"
        )
    if _sha256_file(manifest_path) != manifest_sha256:
        raise VerifiedConstructionCoreError(
            f"frozen {version_label} manifest hash differs"
        )
    manifest = _load_json(manifest_path)
    if not isinstance(manifest, dict) or manifest.get("preview_id") != preview_id:
        raise VerifiedConstructionCoreError(
            f"frozen {version_label} identity differs"
        )
    files = manifest.get("files")
    if not isinstance(files, dict):
        raise VerifiedConstructionCoreError(
            f"frozen {version_label} files map is absent"
        )
    expected_names = set(files) | {"manifest.json", "manifest.sha256"}
    if {item.name for item in path.iterdir()} != expected_names:
        raise VerifiedConstructionCoreError(
            f"frozen {version_label} inventory differs"
        )
    for name, metadata in files.items():
        if not isinstance(name, str) or Path(name).name != name:
            raise VerifiedConstructionCoreError(
                f"frozen {version_label} member differs: {name}"
            )
        candidate = path / name
        if (
            not isinstance(metadata, dict)
            or candidate.is_symlink()
            or not candidate.is_file()
            or candidate.stat().st_size != metadata.get("bytes")
            or _sha256_file(candidate) != metadata.get("sha256")
        ):
            raise VerifiedConstructionCoreError(
                f"frozen {version_label} member differs: {name}"
            )
    expected_sum = f"{manifest_sha256}  manifest.json\n"
    if checksum_path.read_text(encoding="utf-8") != expected_sum:
        raise VerifiedConstructionCoreError(
            f"frozen {version_label} checksum differs"
        )
    return manifest


def validate_frozen_v01(
    path: Path = LEGACY_PREVIEW_V01_DIR,
) -> dict[str, Any]:
    """Validate the byte-frozen v0.1 inventory without applying v0.2 semantics."""
    path = Path(path)
    if path.is_symlink() or not path.is_dir():
        raise VerifiedConstructionCoreError(
            f"frozen v0.1 preview is not a regular directory: {path}"
        )
    manifest_path = path / "manifest.json"
    if _sha256_file(manifest_path) != LEGACY_PREVIEW_V01_MANIFEST_SHA256:
        raise VerifiedConstructionCoreError("frozen v0.1 manifest hash differs")
    manifest = _load_json(manifest_path)
    if (
        manifest.get("format")
        != "datacenter-atlas-verified-construction-core-preview-v1"
        or manifest.get("preview_id") != "2026-08-19-preview-v0.1"
        or manifest.get("reviewed_at") != "2026-08-19"
    ):
        raise VerifiedConstructionCoreError("frozen v0.1 identity differs")
    legacy_definition = (
        ROOT / "definitions" / "verified-construction-core-v0.1-reviewed-sites.json"
    )
    if not legacy_definition.is_file() or manifest.get(
        "review_definition_sha256"
    ) != _sha256_file(legacy_definition):
        raise VerifiedConstructionCoreError("frozen v0.1 review definition differs")
    files = manifest.get("files")
    if not isinstance(files, dict):
        raise VerifiedConstructionCoreError("frozen v0.1 files map is absent")
    expected_names = set(files) | {"manifest.json", "manifest.sha256"}
    if {item.name for item in path.iterdir()} != expected_names:
        raise VerifiedConstructionCoreError("frozen v0.1 inventory differs")
    for name, metadata in files.items():
        candidate = path / name
        if (
            candidate.is_symlink()
            or not candidate.is_file()
            or candidate.stat().st_size != metadata.get("bytes")
            or _sha256_file(candidate) != metadata.get("sha256")
        ):
            raise VerifiedConstructionCoreError(
                f"frozen v0.1 member differs: {name}"
            )
    expected_sum = f"{LEGACY_PREVIEW_V01_MANIFEST_SHA256}  manifest.json\n"
    if (path / "manifest.sha256").read_text(encoding="utf-8") != expected_sum:
        raise VerifiedConstructionCoreError("frozen v0.1 checksum differs")
    return manifest


def validate_frozen_v02(
    path: Path = LEGACY_PREVIEW_V02_DIR,
) -> dict[str, Any]:
    """Validate the byte-frozen v0.2 inventory using only immutable pins."""
    path = Path(path)
    if path.is_symlink() or not path.is_dir():
        raise VerifiedConstructionCoreError(
            f"frozen v0.2 preview is not a regular directory: {path}"
        )
    manifest_path = path / "manifest.json"
    if _sha256_file(manifest_path) != LEGACY_PREVIEW_V02_MANIFEST_SHA256:
        raise VerifiedConstructionCoreError("frozen v0.2 manifest hash differs")
    manifest = _load_json(manifest_path)
    expected_pins = {
        "format": "datacenter-atlas-verified-construction-core-preview-v2",
        "preview_id": "2026-08-20-preview-v0.2",
        "reviewed_at": "2026-08-20",
        "source_release_id": "2026-07-22-open-seed-v97",
        "source_release_manifest_sha256": (
            "0a6f41f4239944df27f2ce70e81a089b91cec401f154bbae28412b27a4d00fdd"
        ),
        "review_definition_sha256": (
            "13f4d6e82c9057b1dd95319e19e30c2bae0ac0f1848d6e6dee2bbc10b5dde1d0"
        ),
        "imagery_review_definition_sha256": (
            "0516a3281d0a3a1734a4bacb8f8d9e5419d0ae8dc22df36c098c5375f84ee613"
        ),
        "overlay_definition_sha256": (
            "57540f73c5cac475bd8e9e64fc5d8eb2188158ef977b477a0c0e8d7158caf1b7"
        ),
    }
    if any(manifest.get(field) != value for field, value in expected_pins.items()):
        raise VerifiedConstructionCoreError("frozen v0.2 identity differs")
    files = manifest.get("files")
    if not isinstance(files, dict):
        raise VerifiedConstructionCoreError("frozen v0.2 files map is absent")
    expected_names = set(files) | {"manifest.json", "manifest.sha256"}
    if {item.name for item in path.iterdir()} != expected_names:
        raise VerifiedConstructionCoreError("frozen v0.2 inventory differs")
    for name, metadata in files.items():
        candidate = path / name
        if (
            candidate.is_symlink()
            or not candidate.is_file()
            or candidate.stat().st_size != metadata.get("bytes")
            or _sha256_file(candidate) != metadata.get("sha256")
        ):
            raise VerifiedConstructionCoreError(
                f"frozen v0.2 member differs: {name}"
            )
    expected_sum = f"{LEGACY_PREVIEW_V02_MANIFEST_SHA256}  manifest.json\n"
    if (path / "manifest.sha256").read_text(encoding="utf-8") != expected_sum:
        raise VerifiedConstructionCoreError("frozen v0.2 checksum differs")
    return manifest


def validate_frozen_v03(
    path: Path = LEGACY_PREVIEW_V03_DIR,
) -> dict[str, Any]:
    """Validate the byte-frozen v0.3 inventory using only immutable pins."""
    path = Path(path)
    if path.is_symlink() or not path.is_dir():
        raise VerifiedConstructionCoreError(
            f"frozen v0.3 preview is not a regular directory: {path}"
        )
    manifest_path = path / "manifest.json"
    if _sha256_file(manifest_path) != LEGACY_PREVIEW_V03_MANIFEST_SHA256:
        raise VerifiedConstructionCoreError("frozen v0.3 manifest hash differs")
    manifest = _load_json(manifest_path)
    expected_pins = {
        "format": "datacenter-atlas-verified-construction-core-preview-v3",
        "preview_id": "2026-08-20-preview-v0.3",
        "reviewed_at": "2026-08-20",
        "source_release_id": "2026-07-22-open-seed-v97",
        "source_release_manifest_sha256": (
            "0a6f41f4239944df27f2ce70e81a089b91cec401f154bbae28412b27a4d00fdd"
        ),
        "review_definition_sha256": (
            "ee56ff41dc6ea59df67de6ab462540c8a3fe3747ca3e1504cb9228481c25c06b"
        ),
        "imagery_review_definition_sha256": (
            "eec896bd0c1b26fe95110ab3023e3d713c9ff4c6965412683b4d5e945ad09d7a"
        ),
        "provenance_definition_sha256": (
            "50c3e82d2a9ae6a759b36344e34556ed527027b5c6397153d051adf61dce6561"
        ),
        "overlay_definition_sha256": (
            "57540f73c5cac475bd8e9e64fc5d8eb2188158ef977b477a0c0e8d7158caf1b7"
        ),
    }
    if any(manifest.get(field) != value for field, value in expected_pins.items()):
        raise VerifiedConstructionCoreError("frozen v0.3 identity differs")
    expected_definition_paths = {
        "imagery_reviews": (
            "definitions/verified-construction-core-v0.3-imagery-reviews.json"
        ),
        "provenance": "definitions/verified-construction-core-v0.3-provenance.json",
        "reviewed_overlays": (
            "definitions/verified-construction-core-reviewed-overlays-v1.json"
        ),
        "reviewed_sites": (
            "definitions/verified-construction-core-v0.3-reviewed-sites.json"
        ),
    }
    if manifest.get("definition_paths") != expected_definition_paths:
        raise VerifiedConstructionCoreError("frozen v0.3 definition paths differ")
    definition_hashes = {
        expected_definition_paths["reviewed_sites"]: expected_pins[
            "review_definition_sha256"
        ],
        expected_definition_paths["imagery_reviews"]: expected_pins[
            "imagery_review_definition_sha256"
        ],
        expected_definition_paths["provenance"]: expected_pins[
            "provenance_definition_sha256"
        ],
        expected_definition_paths["reviewed_overlays"]: expected_pins[
            "overlay_definition_sha256"
        ],
        "releases/2026-07-22-open-seed-v97/manifest.json": expected_pins[
            "source_release_manifest_sha256"
        ],
    }
    for relative, expected_sha256 in definition_hashes.items():
        candidate = ROOT / relative
        if (
            candidate.is_symlink()
            or not candidate.is_file()
            or _sha256_file(candidate) != expected_sha256
        ):
            raise VerifiedConstructionCoreError(
                f"frozen v0.3 repository input differs: {relative}"
            )
    portable = manifest.get("portable_source_inputs")
    if not isinstance(portable, list) or len(portable) != 3:
        raise VerifiedConstructionCoreError("frozen v0.3 portable inputs differ")
    for item in portable:
        if not isinstance(item, dict) or set(item) != {
            "path",
            "bytes",
            "sha256",
            "parent_manifest_path",
            "parent_manifest_sha256",
        }:
            raise VerifiedConstructionCoreError("frozen v0.3 portable input differs")
        relative = Path(item["path"])
        if relative.is_absolute() or ".." in relative.parts:
            raise VerifiedConstructionCoreError("frozen v0.3 portable path differs")
        candidate = ROOT / relative
        if (
            candidate.is_symlink()
            or not candidate.is_file()
            or candidate.stat().st_size != item["bytes"]
            or _sha256_file(candidate) != item["sha256"]
        ):
            raise VerifiedConstructionCoreError(
                f"frozen v0.3 portable member differs: {relative}"
            )
        parent_text = item["parent_manifest_path"]
        parent_sha256 = item["parent_manifest_sha256"]
        if parent_text is None:
            if parent_sha256 is not None:
                raise VerifiedConstructionCoreError(
                    "frozen v0.3 portable parent differs"
                )
        else:
            parent = ROOT / parent_text
            if (
                not isinstance(parent_sha256, str)
                or parent.is_symlink()
                or not parent.is_file()
                or _sha256_file(parent) != parent_sha256
            ):
                raise VerifiedConstructionCoreError(
                    "frozen v0.3 portable parent differs"
                )
            parent_manifest = _load_json(parent)
            member_path = str(relative.relative_to(Path(parent_text).parent))
            matches = [
                row
                for row in parent_manifest.get("files", [])
                if isinstance(row, dict) and row.get("path") == member_path
            ]
            if matches != [
                {
                    "path": member_path,
                    "bytes": item["bytes"],
                    "sha256": item["sha256"],
                }
            ]:
                raise VerifiedConstructionCoreError(
                    "frozen v0.3 portable parent membership differs"
                )
    files = manifest.get("files")
    if not isinstance(files, dict):
        raise VerifiedConstructionCoreError("frozen v0.3 files map is absent")
    expected_names = set(files) | {"manifest.json", "manifest.sha256"}
    if {item.name for item in path.iterdir()} != expected_names:
        raise VerifiedConstructionCoreError("frozen v0.3 inventory differs")
    for name, metadata in files.items():
        candidate = path / name
        if (
            candidate.is_symlink()
            or not candidate.is_file()
            or candidate.stat().st_size != metadata.get("bytes")
            or _sha256_file(candidate) != metadata.get("sha256")
        ):
            raise VerifiedConstructionCoreError(
                f"frozen v0.3 member differs: {name}"
            )
    expected_sum = f"{LEGACY_PREVIEW_V03_MANIFEST_SHA256}  manifest.json\n"
    if (path / "manifest.sha256").read_text(encoding="utf-8") != expected_sum:
        raise VerifiedConstructionCoreError("frozen v0.3 checksum differs")
    return manifest


def validate_frozen_v04(
    path: Path = LEGACY_PREVIEW_V04_DIR,
) -> dict[str, Any]:
    """Validate the byte-frozen v0.4 inventory using only immutable pins."""
    path = Path(path)
    if path.is_symlink() or not path.is_dir():
        raise VerifiedConstructionCoreError(
            f"frozen v0.4 preview is not a regular directory: {path}"
        )
    manifest_path = path / "manifest.json"
    checksum_path = path / "manifest.sha256"
    if (
        manifest_path.is_symlink()
        or not manifest_path.is_file()
        or checksum_path.is_symlink()
        or not checksum_path.is_file()
    ):
        raise VerifiedConstructionCoreError(
            "frozen v0.4 manifest trust root differs"
        )
    if _sha256_file(manifest_path) != LEGACY_PREVIEW_V04_MANIFEST_SHA256:
        raise VerifiedConstructionCoreError("frozen v0.4 manifest hash differs")
    manifest = _load_json(manifest_path)
    expected_pins = {
        "format": "datacenter-atlas-verified-construction-core-preview-v4",
        "preview_id": "2026-08-20-preview-v0.4",
        "reviewed_at": "2026-08-20",
        "source_release_id": "2026-07-22-open-seed-v97",
        "source_release_manifest_path": (
            "releases/2026-07-22-open-seed-v97/manifest.json"
        ),
        "source_release_manifest_sha256": (
            "0a6f41f4239944df27f2ce70e81a089b91cec401f154bbae28412b27a4d00fdd"
        ),
        "review_definition_sha256": (
            "cc71ed6bc107fa585caf3726f7625fd0fdd982c5699a5e19218bbeb1dec7c97c"
        ),
        "imagery_review_definition_sha256": (
            "897a9cbe1f85dea871a3c267069390d5db7ab1206691068769a14fb0b7882798"
        ),
        "provenance_definition_sha256": (
            "9d51d2e998c6d902a6410dfb3a2ce8dde8ee4b722d483bec65de4fda1d6e636e"
        ),
        "overlay_definition_sha256": (
            "65b01f335e60c1f7a886100089bd3a4fe4da79c074233a49bb60b9b65ab8129f"
        ),
    }
    if any(manifest.get(field) != value for field, value in expected_pins.items()):
        raise VerifiedConstructionCoreError("frozen v0.4 identity differs")
    expected_definition_paths = {
        "imagery_reviews": (
            "definitions/verified-construction-core-v0.4-imagery-reviews.json"
        ),
        "provenance": "definitions/verified-construction-core-v0.4-provenance.json",
        "reviewed_overlays": (
            "definitions/verified-construction-core-reviewed-overlays-v2.json"
        ),
        "reviewed_sites": (
            "definitions/verified-construction-core-v0.4-reviewed-sites.json"
        ),
    }
    if manifest.get("definition_paths") != expected_definition_paths:
        raise VerifiedConstructionCoreError("frozen v0.4 definition paths differ")
    repository_pins = {
        expected_definition_paths["reviewed_sites"]: expected_pins[
            "review_definition_sha256"
        ],
        expected_definition_paths["imagery_reviews"]: expected_pins[
            "imagery_review_definition_sha256"
        ],
        expected_definition_paths["provenance"]: expected_pins[
            "provenance_definition_sha256"
        ],
        expected_definition_paths["reviewed_overlays"]: expected_pins[
            "overlay_definition_sha256"
        ],
        expected_pins["source_release_manifest_path"]: expected_pins[
            "source_release_manifest_sha256"
        ],
    }
    for relative, expected_sha256 in repository_pins.items():
        candidate = ROOT / relative
        if (
            candidate.is_symlink()
            or not candidate.is_file()
            or _sha256_file(candidate) != expected_sha256
        ):
            raise VerifiedConstructionCoreError(
                f"frozen v0.4 repository input differs: {relative}"
            )
    portable = manifest.get("portable_source_inputs")
    if not isinstance(portable, list) or len(portable) != 4:
        raise VerifiedConstructionCoreError("frozen v0.4 portable inputs differ")
    for item in portable:
        if not isinstance(item, dict) or set(item) != {
            "path",
            "bytes",
            "sha256",
            "parent_manifests",
        }:
            raise VerifiedConstructionCoreError("frozen v0.4 portable input differs")
        relative = Path(item["path"])
        candidate = ROOT / relative
        if (
            relative.is_absolute()
            or ".." in relative.parts
            or candidate.is_symlink()
            or not candidate.is_file()
            or candidate.stat().st_size != item["bytes"]
            or _sha256_file(candidate) != item["sha256"]
        ):
            raise VerifiedConstructionCoreError(
                f"frozen v0.4 portable member differs: {relative}"
            )
        parents = item["parent_manifests"]
        if not isinstance(parents, list):
            raise VerifiedConstructionCoreError("frozen v0.4 portable parents differ")
        for parent in parents:
            if not isinstance(parent, dict) or set(parent) != {"path", "sha256"}:
                raise VerifiedConstructionCoreError(
                    "frozen v0.4 portable parent differs"
                )
            parent_relative = Path(parent["path"])
            parent_path = ROOT / parent_relative
            if (
                parent_relative.is_absolute()
                or ".." in parent_relative.parts
                or parent_path.is_symlink()
                or not parent_path.is_file()
                or _sha256_file(parent_path) != parent["sha256"]
            ):
                raise VerifiedConstructionCoreError(
                    "frozen v0.4 portable parent differs"
                )
    files = manifest.get("files")
    if not isinstance(files, dict):
        raise VerifiedConstructionCoreError("frozen v0.4 files map is absent")
    expected_names = set(files) | {"manifest.json", "manifest.sha256"}
    if {item.name for item in path.iterdir()} != expected_names:
        raise VerifiedConstructionCoreError("frozen v0.4 inventory differs")
    for name, metadata in files.items():
        candidate = path / name
        if (
            candidate.is_symlink()
            or not candidate.is_file()
            or candidate.stat().st_size != metadata.get("bytes")
            or _sha256_file(candidate) != metadata.get("sha256")
        ):
            raise VerifiedConstructionCoreError(
                f"frozen v0.4 member differs: {name}"
            )
    expected_sum = f"{LEGACY_PREVIEW_V04_MANIFEST_SHA256}  manifest.json\n"
    if checksum_path.read_text(encoding="utf-8") != expected_sum:
        raise VerifiedConstructionCoreError("frozen v0.4 checksum differs")
    return manifest


def validate_frozen_v05(
    path: Path = LEGACY_PREVIEW_V05_DIR,
) -> dict[str, Any]:
    """Validate the byte-frozen v0.5 inventory using only immutable pins."""
    path = Path(path)
    if path.is_symlink() or not path.is_dir():
        raise VerifiedConstructionCoreError(
            f"frozen v0.5 preview is not a regular directory: {path}"
        )
    manifest_path = path / "manifest.json"
    checksum_path = path / "manifest.sha256"
    if (
        manifest_path.is_symlink()
        or not manifest_path.is_file()
        or checksum_path.is_symlink()
        or not checksum_path.is_file()
    ):
        raise VerifiedConstructionCoreError(
            "frozen v0.5 manifest trust root differs"
        )
    if _sha256_file(manifest_path) != LEGACY_PREVIEW_V05_MANIFEST_SHA256:
        raise VerifiedConstructionCoreError("frozen v0.5 manifest hash differs")
    manifest = _load_json(manifest_path)
    expected_pins = {
        "format": "datacenter-atlas-verified-construction-core-preview-v5",
        "preview_id": "2026-08-20-preview-v0.5",
        "reviewed_at": "2026-08-20",
        "source_release_id": "2026-07-22-open-seed-v97",
        "source_release_manifest_path": (
            "releases/2026-07-22-open-seed-v97/manifest.json"
        ),
        "source_release_manifest_sha256": (
            "0a6f41f4239944df27f2ce70e81a089b91cec401f154bbae28412b27a4d00fdd"
        ),
        "review_definition_sha256": (
            "ec1c7dfc9f9516567ca2c58e29b631cf11df4bba4212df3fc875d0a4b5236775"
        ),
        "imagery_review_definition_sha256": (
            "90969eb2e15210b7a398ff2cc1a2d12ba1b2aa5213e5f4cfe31021ec71c2b083"
        ),
        "provenance_definition_sha256": (
            "bbd0e4665605e7af6389d6730211d096c737ee038886cfdacf848f87a2ee16af"
        ),
        "overlay_definition_sha256": (
            "d71ce264327ccf523cb7f000d559c0a4953524d0224dbdcc7e91e36d8e4b98c5"
        ),
    }
    if any(manifest.get(field) != value for field, value in expected_pins.items()):
        raise VerifiedConstructionCoreError("frozen v0.5 identity differs")
    expected_definition_paths = {
        "imagery_reviews": (
            "definitions/verified-construction-core-v0.5-imagery-reviews.json"
        ),
        "provenance": "definitions/verified-construction-core-v0.5-provenance.json",
        "reviewed_overlays": (
            "definitions/verified-construction-core-reviewed-overlays-v3.json"
        ),
        "reviewed_sites": (
            "definitions/verified-construction-core-v0.5-reviewed-sites.json"
        ),
    }
    if manifest.get("definition_paths") != expected_definition_paths:
        raise VerifiedConstructionCoreError("frozen v0.5 definition paths differ")
    repository_pins = {
        expected_definition_paths["reviewed_sites"]: expected_pins[
            "review_definition_sha256"
        ],
        expected_definition_paths["imagery_reviews"]: expected_pins[
            "imagery_review_definition_sha256"
        ],
        expected_definition_paths["provenance"]: expected_pins[
            "provenance_definition_sha256"
        ],
        expected_definition_paths["reviewed_overlays"]: expected_pins[
            "overlay_definition_sha256"
        ],
        expected_pins["source_release_manifest_path"]: expected_pins[
            "source_release_manifest_sha256"
        ],
    }
    for relative, expected_sha256 in repository_pins.items():
        candidate = ROOT / relative
        if (
            candidate.is_symlink()
            or not candidate.is_file()
            or _sha256_file(candidate) != expected_sha256
        ):
            raise VerifiedConstructionCoreError(
                f"frozen v0.5 repository input differs: {relative}"
            )
    portable = manifest.get("portable_source_inputs")
    if not isinstance(portable, list) or len(portable) != 8:
        raise VerifiedConstructionCoreError("frozen v0.5 portable inputs differ")
    for item in portable:
        if not isinstance(item, dict) or set(item) != {
            "path",
            "bytes",
            "sha256",
            "parent_manifests",
        }:
            raise VerifiedConstructionCoreError("frozen v0.5 portable input differs")
        relative = Path(item["path"])
        candidate = ROOT / relative
        if (
            relative.is_absolute()
            or ".." in relative.parts
            or candidate.is_symlink()
            or not candidate.is_file()
            or candidate.stat().st_size != item["bytes"]
            or _sha256_file(candidate) != item["sha256"]
        ):
            raise VerifiedConstructionCoreError(
                f"frozen v0.5 portable member differs: {relative}"
            )
        parents = item["parent_manifests"]
        if not isinstance(parents, list):
            raise VerifiedConstructionCoreError("frozen v0.5 portable parents differ")
        for parent in parents:
            if not isinstance(parent, dict) or set(parent) != {"path", "sha256"}:
                raise VerifiedConstructionCoreError(
                    "frozen v0.5 portable parent differs"
                )
            parent_relative = Path(parent["path"])
            parent_path = ROOT / parent_relative
            if (
                parent_relative.is_absolute()
                or ".." in parent_relative.parts
                or parent_path.is_symlink()
                or not parent_path.is_file()
                or _sha256_file(parent_path) != parent["sha256"]
            ):
                raise VerifiedConstructionCoreError(
                    "frozen v0.5 portable parent differs"
                )
    files = manifest.get("files")
    if not isinstance(files, dict):
        raise VerifiedConstructionCoreError("frozen v0.5 files map is absent")
    expected_names = set(files) | {"manifest.json", "manifest.sha256"}
    if {item.name for item in path.iterdir()} != expected_names:
        raise VerifiedConstructionCoreError("frozen v0.5 inventory differs")
    for name, metadata in files.items():
        candidate = path / name
        if (
            candidate.is_symlink()
            or not candidate.is_file()
            or candidate.stat().st_size != metadata.get("bytes")
            or _sha256_file(candidate) != metadata.get("sha256")
        ):
            raise VerifiedConstructionCoreError(
                f"frozen v0.5 member differs: {name}"
            )
    expected_sum = f"{LEGACY_PREVIEW_V05_MANIFEST_SHA256}  manifest.json\n"
    if checksum_path.read_text(encoding="utf-8") != expected_sum:
        raise VerifiedConstructionCoreError("frozen v0.5 checksum differs")
    return manifest


def validate_frozen_v06(
    path: Path = LEGACY_PREVIEW_V06_DIR,
) -> dict[str, Any]:
    """Validate byte-frozen v0.6 using only its immutable inventory pins."""
    return _validate_frozen_preview_inventory(
        path,
        preview_id="2026-08-20-preview-v0.6",
        manifest_sha256=(
            "05070fab668b1ddd575745cefe3dc36600cd27b48f90702939229c1372674bd5"
        ),
        version_label="v0.6",
    )


_FROZEN_PREVIEW_VALIDATORS = (
    ("2026-08-19-preview-v0.1", validate_frozen_v01),
    ("2026-08-20-preview-v0.2", validate_frozen_v02),
    ("2026-08-20-preview-v0.3", validate_frozen_v03),
    ("2026-08-20-preview-v0.4", validate_frozen_v04),
    ("2026-08-20-preview-v0.5", validate_frozen_v05),
    ("2026-08-20-preview-v0.6", validate_frozen_v06),
)


def _frozen_preview_validator(
    preview_id: object,
) -> Callable[[Path], dict[str, Any]] | None:
    for frozen_id, validator in _FROZEN_PREVIEW_VALIDATORS:
        if preview_id == frozen_id:
            return validator
    return None


def validate_frozen_preview(path: Path) -> dict[str, Any]:
    """Dispatch a frozen preview through the immutable literal version table."""
    path = Path(path)
    if path.is_symlink() or not path.is_dir():
        raise VerifiedConstructionCoreError(
            f"frozen preview is not a regular directory: {path}"
        )
    manifest_path = path / "manifest.json"
    if manifest_path.is_symlink() or not manifest_path.is_file():
        raise VerifiedConstructionCoreError("frozen preview manifest trust root differs")
    manifest = _load_json(manifest_path)
    preview_id = manifest.get("preview_id") if isinstance(manifest, dict) else None
    validator = _frozen_preview_validator(preview_id)
    if validator is None:
        raise VerifiedConstructionCoreError("frozen preview id differs")
    return validator(path)


def validate_preview(path: Path = PREVIEW_DIR) -> dict[str, Any]:
    """Dispatch frozen previews by manifest identity and validate the current one."""
    path = Path(path)
    if path.is_symlink() or not path.is_dir():
        raise VerifiedConstructionCoreError(f"preview is not a regular directory: {path}")
    manifest_path = path / "manifest.json"
    if manifest_path.is_symlink() or not manifest_path.is_file():
        raise VerifiedConstructionCoreError("preview manifest trust root differs")
    manifest = _load_json(manifest_path)
    preview_id = manifest.get("preview_id")
    if preview_id == PREVIEW_ID:
        return _validate_current_preview(path)
    validator = _frozen_preview_validator(preview_id)
    if validator is not None:
        return validator(path)
    raise VerifiedConstructionCoreError("preview id differs")


def _validate_current_preview(path: Path) -> dict[str, Any]:
    """Validate the tracked preview without requiring the ignored source corpus."""
    path = Path(path)
    if path.is_symlink() or not path.is_dir():
        raise VerifiedConstructionCoreError(f"preview is not a regular directory: {path}")
    _validate_current_definition_pins()
    manifest_path = path / "manifest.json"
    checksum_path = path / "manifest.sha256"
    if (
        manifest_path.is_symlink()
        or not manifest_path.is_file()
        or checksum_path.is_symlink()
        or not checksum_path.is_file()
    ):
        raise VerifiedConstructionCoreError("preview manifest trust root differs")
    manifest = _load_json(manifest_path)
    expected_manifest_fields = {
        "format",
        "preview_id",
        "release_status",
        "publishable_as_final",
        "source_release_id",
        "source_release_manifest_path",
        "source_release_manifest_sha256",
        "definition_paths",
        "review_definition_sha256",
        "imagery_review_definition_sha256",
        "provenance_definition_sha256",
        "overlay_definition_sha256",
        "portable_source_inputs",
        "reviewed_at",
        "counts",
        "files",
    }
    if set(manifest) != expected_manifest_fields:
        raise VerifiedConstructionCoreError("preview manifest fields differ")
    if manifest.get("format") != "datacenter-atlas-verified-construction-core-preview-v6":
        raise VerifiedConstructionCoreError("preview format differs")
    if manifest.get("preview_id") != PREVIEW_ID:
        raise VerifiedConstructionCoreError("preview id differs")
    if manifest.get("release_status") != "preview" or manifest.get(
        "publishable_as_final"
    ) is not False:
        raise VerifiedConstructionCoreError("preview promotion posture differs")
    if manifest.get("source_release_id") != SOURCE_RELEASE_ID:
        raise VerifiedConstructionCoreError("preview source release differs")
    if manifest.get("reviewed_at") != REVIEW_DATE.isoformat():
        raise VerifiedConstructionCoreError("preview review date differs")
    for field, _, expected_sha256 in _current_definition_pins():
        if manifest.get(field) != expected_sha256:
            raise VerifiedConstructionCoreError(f"preview {field} differs")
    expected_definition_paths = {
        "imagery_reviews": str(IMAGERY_REVIEW_DEFINITION.relative_to(ROOT)),
        "provenance": str(PROVENANCE_DEFINITION.relative_to(ROOT)),
        "reviewed_overlays": str(OVERLAY_DEFINITION.relative_to(ROOT)),
        "reviewed_sites": str(REVIEW_DEFINITION.relative_to(ROOT)),
    }
    if manifest.get("definition_paths") != expected_definition_paths:
        raise VerifiedConstructionCoreError("preview definition paths differ")
    source_manifest_path = SOURCE_RELEASE / "manifest.json"
    if (
        manifest.get("source_release_manifest_path")
        != str(source_manifest_path.relative_to(ROOT))
        or not source_manifest_path.is_file()
        or manifest.get(
        "source_release_manifest_sha256"
        )
        != _sha256_file(source_manifest_path)
    ):
        raise VerifiedConstructionCoreError("preview source manifest hash differs")
    if manifest.get("portable_source_inputs") != _portable_source_inputs():
        raise VerifiedConstructionCoreError("preview portable source inputs differ")
    files = manifest.get("files")
    if not isinstance(files, dict):
        raise VerifiedConstructionCoreError("preview files map is absent")
    required_payloads = {
        "ATTRIBUTION.txt",
        "README.md",
        "evidence.csv",
        "map.html",
        "projects.csv",
        "schema.json",
        "selection-report.json",
        "sites.csv",
        "sites.geojson",
    }
    if set(files) != required_payloads:
        raise VerifiedConstructionCoreError("preview payload inventory differs")
    expected_names = set(files) | {"manifest.json", "manifest.sha256"}
    actual_names = {item.name for item in path.iterdir()}
    if actual_names != expected_names:
        raise VerifiedConstructionCoreError("preview closed inventory differs")
    for name, metadata in files.items():
        candidate = path / name
        if candidate.is_symlink() or not candidate.is_file():
            raise VerifiedConstructionCoreError(f"preview member differs: {name}")
        if candidate.stat().st_size != metadata.get("bytes"):
            raise VerifiedConstructionCoreError(f"preview member byte count differs: {name}")
        if _sha256_file(candidate) != metadata.get("sha256"):
            raise VerifiedConstructionCoreError(f"preview member hash differs: {name}")
    expected_sum = f"{_sha256_file(manifest_path)}  manifest.json\n"
    if checksum_path.read_text(encoding="utf-8") != expected_sum:
        raise VerifiedConstructionCoreError("preview manifest checksum differs")

    sites = _load_csv(path / "sites.csv", SITE_FIELDS)
    projects = _load_csv(path / "projects.csv", PROJECT_FIELDS)
    evidence = _load_csv(path / "evidence.csv", EVIDENCE_FIELDS)
    schema = _load_json(path / "schema.json")
    geojson = _load_json(path / "sites.geojson")
    report = _load_json(path / "selection-report.json")
    if schema != _schema():
        raise VerifiedConstructionCoreError("preview machine-readable schema differs")
    counts = manifest.get("counts", {})
    if not isinstance(counts, dict) or set(counts) != {
        "physical_sites",
        "projects",
        "evidence",
        "countries",
        "non_us_sites",
        "official_boundary_projects",
        "reviewed_site_locator_projects",
    }:
        raise VerifiedConstructionCoreError("preview count fields differ")
    if counts.get("physical_sites") != len(sites) or counts.get("projects") != len(
        projects
    ) or counts.get("evidence") != len(evidence):
        raise VerifiedConstructionCoreError("preview manifest counts differ")
    site_ids = {row["site_id"] for row in sites}
    project_ids = {row["project_id"] for row in projects}
    evidence_ids = {row["evidence_id"] for row in evidence}
    if len(site_ids) != len(sites) or len(project_ids) != len(projects):
        raise VerifiedConstructionCoreError("preview identifiers are not unique")
    if any(row["site_id"] not in site_ids for row in projects):
        raise VerifiedConstructionCoreError("preview project references absent site")
    if {row["site_id"] for row in projects} != site_ids:
        raise VerifiedConstructionCoreError("preview contains a site without a project")
    site_by_id = {row["site_id"]: row for row in sites}
    reviewed_acceptances = _reviewed_acceptances()
    _, reviewed_delta = _reviewed_acceptances_from(REVIEW_DEFINITION)
    reviewed_delta_by_key = {
        row["project_stable_key"]: row for row in reviewed_delta
    }
    reviewed_by_key = {
        row["project_stable_key"]: row for row in reviewed_acceptances
    }
    reviewed_keys = set(reviewed_by_key)
    if {row["project_stable_key"] for row in projects} != reviewed_keys:
        raise VerifiedConstructionCoreError("preview reviewed-project cohort differs")
    default_imagery_outcome, imagery_records = _imagery_contract()
    imagery_by_key = {row["project_stable_key"]: row for row in imagery_records}
    provenance = _provenance_contract()
    overlays = _reviewed_overlays(hydrated_crosscheck=False)
    workload_binding_by_key = {
        (
            row["project_stable_key"],
            row["evidence_id"],
            row["workload"],
        ): row
        for row in provenance["workload_scope_bindings"]
    }
    role_bindings_by_project: dict[str, list[dict[str, Any]]] = defaultdict(list)
    for binding in provenance["role_bindings"]:
        role_bindings_by_project[binding["project_stable_key"]].append(binding)
    evidence_by_id = {row["evidence_id"]: row for row in evidence}
    if len(evidence_by_id) != len(evidence):
        raise VerifiedConstructionCoreError("preview evidence identifiers are not unique")
    _validate_v06_inheritance(projects, sites, evidence)
    for row in projects:
        acceptance = reviewed_by_key[row["project_stable_key"]]
        if row["project_id"] != atlas_stable_id(
            "entity", row["project_stable_key"], "project"
        ):
            raise VerifiedConstructionCoreError("preview project id differs")
        expected_imagery_outcome = _imagery_outcome(
            row["project_stable_key"], default_imagery_outcome, imagery_by_key
        )
        if row["project_stable_key"] in reviewed_delta_by_key:
            overlay_id = reviewed_delta_by_key[row["project_stable_key"]][
                "geometry_overlay_id"
            ]
            bridge = overlays["bridges_by_overlay_id"].get(overlay_id)
            if bridge is None:
                raise VerifiedConstructionCoreError(
                    "preview reviewed geometry bridge is absent"
                )
            _validate_bridge_acceptance_semantics(
                reviewed_delta_by_key[row["project_stable_key"]],
                bridge,
                label="preview reviewed geometry",
            )
            _validate_v06_delta_project(
                row,
                reviewed_delta_by_key[row["project_stable_key"]],
                evidence_by_id,
                expected_imagery_outcome,
                bridge,
                workload_binding_by_key,
                role_bindings_by_project,
            )
        if acceptance.get("geometry_overlay_id") is None:
            expected_authority, expected_use_scope = _direct_geometry_semantics(
                row["geometry_type"]
            )
        else:
            decision = overlays["bridges_by_overlay_id"][
                acceptance["geometry_overlay_id"]
            ]["review_decision"]
            expected_authority = decision["geometry_authority_class"]
            expected_use_scope = decision["geometry_use_scope"]
        if (
            row["geometry_source_entity_kind"] != acceptance["geometry_entity"]
            or row["geometry_derivation"] != acceptance["geometry_derivation"]
            or row["geometry_method"] != acceptance["geometry_method"]
            or row["geometry_scope_class"] != acceptance["geometry_scope_class"]
            or row["geometry_authority_class"] != expected_authority
            or row["geometry_use_scope"] != expected_use_scope
            or row["geometry_precision_scope"] != acceptance["precision_scope"]
            or row["imagery_review_outcome"] != expected_imagery_outcome
        ):
            raise VerifiedConstructionCoreError(
                "preview reviewed geometry or imagery contract differs"
            )
        expected_uncertainty = acceptance["horizontal_uncertainty_metres"]
        if expected_uncertainty is None:
            overlay_id = acceptance.get("geometry_overlay_id")
            expected_unknown_reason = (
                overlays["bridges_by_overlay_id"][overlay_id]["review_decision"][
                    "horizontal_uncertainty_unknown_reason"
                ]
                if overlay_id is not None
                else "official source does not state positional accuracy"
            )
            if row["horizontal_uncertainty_metres"] or row[
                "horizontal_uncertainty_unknown_reason"
            ] != expected_unknown_reason:
                raise VerifiedConstructionCoreError(
                    "preview reviewed geometry precision contract differs"
                )
        else:
            try:
                actual_uncertainty = float(row["horizontal_uncertainty_metres"])
            except ValueError as error:
                raise VerifiedConstructionCoreError(
                    "preview reviewed geometry precision contract differs"
                ) from error
            if (
                actual_uncertainty != float(expected_uncertainty)
                or row["horizontal_uncertainty_unknown_reason"]
            ):
                raise VerifiedConstructionCoreError(
                    "preview reviewed geometry precision contract differs"
                )
        imagery_record = imagery_by_key.get(row["project_stable_key"])
        if imagery_record is not None and imagery_record["project_entity_id"] != row[
            "project_id"
        ]:
            raise VerifiedConstructionCoreError("preview imagery identity differs")
        if row["status_evidence_id"] not in evidence_ids or row[
            "geometry_evidence_id"
        ] not in evidence_ids:
            raise VerifiedConstructionCoreError("preview evidence reference is absent")
        age = (REVIEW_DATE - _calendar_date(row["status_as_of"], "status_as_of")).days
        if int(row["status_age_days_at_review"]) != age:
            raise VerifiedConstructionCoreError("preview status age differs")
        if age < 0 or age > MAX_STATUS_AGE_DAYS:
            raise VerifiedConstructionCoreError("preview status freshness differs")
        if row["last_observed_physical_status"] not in PHYSICAL_STATUSES or row[
            "status_method"
        ] not in AUTHORITATIVE_STATUS_METHODS:
            raise VerifiedConstructionCoreError("preview physical-status gate differs")
        if row["independent_imagery_verification"] != "false":
            raise VerifiedConstructionCoreError("preview imagery claim differs")
        geometry = _parse_json_field(row["geometry_json"], "geometry")
        if not isinstance(geometry, dict) or geometry.get("type") != row[
            "geometry_type"
        ] or row["geometry_type"] not in {"Point", "Polygon", "MultiPolygon"}:
            raise VerifiedConstructionCoreError("preview geometry type differs")
        try:
            latitude = float(row["latitude"])
            longitude = float(row["longitude"])
        except ValueError as error:
            raise VerifiedConstructionCoreError(
                "preview geometry representative coordinates differ"
            ) from error
        if (
            not math.isfinite(latitude)
            or not math.isfinite(longitude)
            or not -90 <= latitude <= 90
            or not -180 <= longitude <= 180
            or (
                geometry["type"] == "Point"
                and geometry.get("coordinates") != [longitude, latitude]
            )
        ):
            raise VerifiedConstructionCoreError(
                "preview geometry representative coordinates differ"
            )
        if row["verification_posture"] != _verification_posture(
            row["geometry_authority_class"], row["geometry_use_scope"]
        ):
            raise VerifiedConstructionCoreError("preview verification posture differs")
        if row["site_id"] != _stable_id(
            "vcc-site", row["physical_site_stable_key"]
        ):
            raise VerifiedConstructionCoreError("preview physical-site id differs")
        site = site_by_id[row["site_id"]]
        if site["physical_site_stable_key"] != row["physical_site_stable_key"]:
            raise VerifiedConstructionCoreError("preview project/site identity differs")
        if row["horizontal_uncertainty_metres"]:
            if row["horizontal_uncertainty_unknown_reason"]:
                raise VerifiedConstructionCoreError("preview geometry precision conflicts")
            if float(row["horizontal_uncertainty_metres"]) < 0:
                raise VerifiedConstructionCoreError("preview geometry precision differs")
        elif not row["horizontal_uncertainty_unknown_reason"]:
            raise VerifiedConstructionCoreError("preview geometry precision reason is absent")
        if row["operating_model"] == "unknown":
            if not row["operating_model_unknown_reason"] or row[
                "operating_model_evidence_id"
            ]:
                raise VerifiedConstructionCoreError(
                    "preview operating-model unknown posture differs"
                )
        else:
            operating_evidence_id = row["operating_model_evidence_id"]
            if row["operating_model_unknown_reason"] or operating_evidence_id not in evidence_by_id:
                raise VerifiedConstructionCoreError(
                    "preview operating-model evidence differs"
                )
            operating_evidence = evidence_by_id[operating_evidence_id]
            if "operating_model" not in _parse_json_field(
                operating_evidence["roles_json"], "roles"
            ) or row["project_id"] not in _parse_json_field(
                operating_evidence["project_ids_json"], "project_ids"
            ):
                raise VerifiedConstructionCoreError(
                    "preview operating-model evidence usage differs"
                )
        for evidence_id, role in (
            (row["status_evidence_id"], "physical_status"),
            (row["geometry_evidence_id"], "geometry"),
        ):
            evidence_row = evidence_by_id[evidence_id]
            source_url_field = (
                "status_source_url"
                if role == "physical_status"
                else "geometry_source_url"
            )
            if row[source_url_field] != evidence_row["source_url"]:
                raise VerifiedConstructionCoreError(
                    "preview evidence source URL differs"
                )
            if role not in _parse_json_field(evidence_row["roles_json"], "roles") or row[
                "project_id"
            ] not in _parse_json_field(
                evidence_row["project_ids_json"], "project_ids"
            ):
                raise VerifiedConstructionCoreError("preview evidence usage differs")
        typed_observations: list[dict[str, Any]] = []
        for field, reason_field, allowed_metrics in (
            ("power_observations_json", "power_unknown_reason", POWER_METRICS),
            (
                "annual_energy_observations_json",
                "annual_energy_unknown_reason",
                ENERGY_METRICS,
            ),
            (
                "efficiency_observations_json",
                "efficiency_unknown_reason",
                EFFICIENCY_METRICS,
            ),
        ):
            observations = _parse_json_field(row[field], field)
            if not isinstance(observations, list) or any(
                not isinstance(observation, dict)
                for observation in observations
            ):
                raise VerifiedConstructionCoreError(
                    "preview typed-metric category differs"
                )
            for observation in observations:
                _validate_typed_observation(observation, allowed_metrics)
            if bool(observations) == bool(row[reason_field]):
                raise VerifiedConstructionCoreError(
                    "preview typed-metric unknown posture differs"
                )
            typed_observations.extend(observations)
        for observation in typed_observations:
            evidence_id = observation.get("evidence_id")
            role = f"typed_metric:{observation.get('metric')}"
            if evidence_id not in evidence_by_id:
                raise VerifiedConstructionCoreError(
                    "preview typed-metric evidence usage differs"
                )
            metric_evidence = evidence_by_id[evidence_id]
            if role not in _parse_json_field(
                metric_evidence["roles_json"], "roles"
            ) or row["project_id"] not in _parse_json_field(
                metric_evidence["project_ids_json"], "project_ids"
            ):
                raise VerifiedConstructionCoreError(
                    "preview typed-metric evidence usage differs"
                )
        workloads = _parse_json_field(row["workloads_json"], "workloads")
        if not isinstance(workloads, list):
            raise VerifiedConstructionCoreError("preview workload list differs")
        for workload in workloads:
            _validate_workload_observation(workload)
            evidence_id = workload.get("evidence_id")
            binding = workload_binding_by_key.get(
                (row["project_stable_key"], evidence_id, workload.get("workload"))
            )
            if binding is None or any(
                workload[field] != binding[field]
                for field in SOURCE_WORKLOAD_FIELDS | {"deployment_scope"}
            ):
                raise VerifiedConstructionCoreError(
                    "preview workload provenance differs"
                )
            if evidence_id not in evidence_by_id:
                raise VerifiedConstructionCoreError(
                    "preview workload evidence usage differs"
                )
            workload_evidence = evidence_by_id[evidence_id]
            workload_roles = _parse_json_field(workload_evidence["roles_json"], "roles")
            if (
                "workload" not in workload_roles
                or f"workload_scope:{workload['deployment_scope']}" not in workload_roles
                or row["project_id"] not in _parse_json_field(
                workload_evidence["project_ids_json"], "project_ids"
                )
            ):
                raise VerifiedConstructionCoreError(
                    "preview workload evidence usage differs"
                )
        role_claims = _parse_json_field(row["role_claims_json"], "role claims")
        if not isinstance(role_claims, list):
            raise VerifiedConstructionCoreError("preview role claims differ")
        for claim in role_claims:
            _validate_role_claim(claim)
        if role_claims != sorted(
            role_claims,
            key=lambda claim: (claim["role"], claim["party"], claim["evidence_id"]),
        ) or len({(claim["role"], claim["party"]) for claim in role_claims}) != len(
            role_claims
        ):
            raise VerifiedConstructionCoreError("preview role claim ordering differs")
        expected_role_claims = sorted(
            [
                {
                    "evidence_id": binding["evidence_id"],
                    "party": binding["party"],
                    "relationship_scope": binding["relationship_scope"],
                    "role": binding["role"],
                }
                for binding in role_bindings_by_project.get(
                    row["project_stable_key"], []
                )
            ],
            key=lambda claim: (claim["role"], claim["party"], claim["evidence_id"]),
        )
        if role_claims != expected_role_claims:
            raise VerifiedConstructionCoreError("preview role provenance differs")
        projected: dict[str, list[str]] = {role: [] for role in ROLE_COLUMNS}
        for claim in role_claims:
            projected[claim["role"]].append(claim["party"])
            role_evidence = evidence_by_id.get(claim["evidence_id"])
            if role_evidence is None or (
                f"role:{claim['role']}"
                not in _parse_json_field(role_evidence["roles_json"], "roles")
                or row["project_id"]
                not in _parse_json_field(
                    role_evidence["project_ids_json"], "project_ids"
                )
            ):
                raise VerifiedConstructionCoreError(
                    "preview role evidence usage differs"
                )
        for role, column in ROLE_COLUMNS.items():
            if row[column] != "; ".join(sorted(projected[role])):
                raise VerifiedConstructionCoreError(
                    "preview role projection differs"
                )
    for evidence_id, expected in provenance["evidence_rows"].items():
        actual = evidence_by_id.get(evidence_id)
        if actual is None or any(
            actual.get(field) != value
            for field, value in expected.items()
            if field not in {"roles_json", "project_ids_json"}
        ):
            raise VerifiedConstructionCoreError(
                "preview provenance evidence content differs"
            )
    expected_usage: dict[str, dict[str, set[str]]] = defaultdict(
        lambda: {"roles": set(), "project_ids": set()}
    )
    for project in projects:
        project_id = project["project_id"]
        for evidence_id, role in (
            (project["status_evidence_id"], "physical_status"),
            (project["geometry_evidence_id"], "geometry"),
        ):
            expected_usage[evidence_id]["roles"].add(role)
            expected_usage[evidence_id]["project_ids"].add(project_id)
        if project["operating_model_evidence_id"]:
            evidence_id = project["operating_model_evidence_id"]
            expected_usage[evidence_id]["roles"].add("operating_model")
            expected_usage[evidence_id]["project_ids"].add(project_id)
        for field in (
            "power_observations_json",
            "annual_energy_observations_json",
            "efficiency_observations_json",
        ):
            for observation in _parse_json_field(project[field], field):
                evidence_id = observation["evidence_id"]
                expected_usage[evidence_id]["roles"].add(
                    f"typed_metric:{observation['metric']}"
                )
                expected_usage[evidence_id]["project_ids"].add(project_id)
        for workload in _parse_json_field(project["workloads_json"], "workloads"):
            evidence_id = workload["evidence_id"]
            expected_usage[evidence_id]["roles"].update(
                {"workload", f"workload_scope:{workload['deployment_scope']}"}
            )
            expected_usage[evidence_id]["project_ids"].add(project_id)
        for claim in _parse_json_field(project["role_claims_json"], "role claims"):
            evidence_id = claim["evidence_id"]
            expected_usage[evidence_id]["roles"].add(f"role:{claim['role']}")
            expected_usage[evidence_id]["project_ids"].add(project_id)
    if set(expected_usage) != evidence_ids:
        raise VerifiedConstructionCoreError("preview evidence closure differs")
    for evidence_id, usage in expected_usage.items():
        evidence_row = evidence_by_id[evidence_id]
        if (
            _parse_json_field(evidence_row["roles_json"], "roles")
            != sorted(usage["roles"])
            or _parse_json_field(
                evidence_row["project_ids_json"], "project_ids"
            )
            != sorted(usage["project_ids"])
        ):
            raise VerifiedConstructionCoreError("preview evidence usage closure differs")
    delta_site_names: dict[str, str] = {}
    for acceptance in reviewed_delta:
        source = _load_json(
            _repository_input(
                acceptance["source_input_path"],
                acceptance["source_input_sha256"],
                "current reviewed-site",
            )
        )
        campus = source.get("campus")
        if (
            not isinstance(campus, dict)
            or campus.get("stable_key") != acceptance["physical_site_stable_key"]
            or not isinstance(campus.get("name"), str)
            or not campus["name"]
        ):
            raise VerifiedConstructionCoreError(
                "current reviewed-site campus identity differs"
            )
        previous = delta_site_names.setdefault(campus["stable_key"], campus["name"])
        if previous != campus["name"]:
            raise VerifiedConstructionCoreError(
                "current reviewed-site campus name differs"
            )
    projects_by_site: dict[str, list[dict[str, str]]] = defaultdict(list)
    for project in projects:
        projects_by_site[project["site_id"]].append(project)
    for site in sites:
        members = sorted(
            projects_by_site[site["site_id"]], key=lambda row: row["project_id"]
        )
        geometry_member = _preferred_site_geometry_member(members)
        expected_arrays = {
            "geometry_source_entity_kinds_json": sorted(
                {row["geometry_source_entity_kind"] for row in members}
            ),
            "geometry_derivations_json": sorted(
                {row["geometry_derivation"] for row in members}
            ),
            "geometry_methods_json": sorted(
                {row["geometry_method"] for row in members}
            ),
            "geometry_scope_classes_json": sorted(
                {row["geometry_scope_class"] for row in members}
            ),
            "geometry_authority_classes_json": sorted(
                {row["geometry_authority_class"] for row in members}
            ),
            "geometry_use_scopes_json": sorted(
                {row["geometry_use_scope"] for row in members}
            ),
            "geometry_precision_scopes_json": sorted(
                {row["geometry_precision_scope"] for row in members}
            ),
            "geometry_evidence_ids_json": sorted(
                {row["geometry_evidence_id"] for row in members}
            ),
            "project_ids_json": [row["project_id"] for row in members],
            "project_stable_keys_json": [
                row["project_stable_key"] for row in members
            ],
            "statuses_json": sorted(
                {row["last_observed_physical_status"] for row in members}
            ),
            "imagery_review_outcomes_json": sorted(
                {row["imagery_review_outcome"] for row in members}
            ),
        }
        if any(
            _parse_json_field(site[field], field) != expected
            for field, expected in expected_arrays.items()
        ):
            raise VerifiedConstructionCoreError("preview site membership differs")
        if int(site["project_count"]) != len(members):
            raise VerifiedConstructionCoreError("preview site project count differs")
        expected_scalars = {
            "physical_site_stable_key": geometry_member[
                "physical_site_stable_key"
            ],
            "country": geometry_member["country"],
            "country_iso_a2": geometry_member["country_iso_a2"],
            "latitude": geometry_member["latitude"],
            "longitude": geometry_member["longitude"],
            "geometry_json": geometry_member["geometry_json"],
            "geometry_type": geometry_member["geometry_type"],
            "horizontal_uncertainty_metres": geometry_member[
                "horizontal_uncertainty_metres"
            ],
            "horizontal_uncertainty_unknown_reason": geometry_member[
                "horizontal_uncertainty_unknown_reason"
            ],
            "oldest_status_as_of": min(row["status_as_of"] for row in members),
            "newest_status_as_of": max(row["status_as_of"] for row in members),
            "verification_posture": geometry_member["verification_posture"],
            "independent_imagery_verification": "false",
        }
        if site["physical_site_stable_key"] in delta_site_names:
            expected_scalars["name"] = delta_site_names[
                site["physical_site_stable_key"]
            ]
        if any(site[field] != expected for field, expected in expected_scalars.items()):
            raise VerifiedConstructionCoreError("preview site projection differs")
        if site["site_id"] != _stable_id(
            "vcc-site", site["physical_site_stable_key"]
        ):
            raise VerifiedConstructionCoreError("preview site id differs")
    if geojson != _geojson(sites):
        raise VerifiedConstructionCoreError("preview GeoJSON and site table differ")
    if (path / "map.html").read_bytes() != _map_html(geojson, evidence):
        raise VerifiedConstructionCoreError("preview map and GeoJSON differ")
    report_fields = {
        "country_counts",
        "final_release_gates",
        "format",
        "imagery_review_provenance",
        "maximum_status_age_days",
        "non_selected_source_row_count",
        "official_boundary_project_count",
        "provenance_decisions",
        "publishable_as_final",
        "release_status",
        "reviewed_at",
        "reviewed_overlay_queue",
        "reviewed_site_locator_project_count",
        "selected_physical_site_count",
        "selected_project_count",
        "selection_first_failure_counts",
        "semantic_guardrails",
        "source_pipeline_row_count",
        "source_release_id",
    }
    if not isinstance(report, dict) or set(report) != report_fields:
        raise VerifiedConstructionCoreError("preview selection-report fields differ")
    expected_gates = _final_release_gates(sites, projects)
    country_counts = dict(sorted(Counter(row["country"] for row in sites).items()))
    expected_values = {
        "format": "datacenter-atlas-verified-construction-core-selection-v6",
        "release_status": "preview",
        "publishable_as_final": all(
            gate.get("passed", False) for gate in expected_gates.values()
        ),
        "source_release_id": SOURCE_RELEASE_ID,
        "reviewed_at": REVIEW_DATE.isoformat(),
        "maximum_status_age_days": MAX_STATUS_AGE_DAYS,
        "source_pipeline_row_count": PREVIEW_SOURCE_PIPELINE_ROW_COUNT,
        "selected_project_count": len(projects),
        "selected_physical_site_count": len(sites),
        "official_boundary_project_count": sum(
            _is_official_boundary(row) for row in projects
        ),
        "reviewed_site_locator_project_count": sum(
            _is_reviewed_locator(row) for row in projects
        ),
        "non_selected_source_row_count": PREVIEW_SOURCE_PIPELINE_ROW_COUNT
        - len(projects),
        "selection_first_failure_counts": PREVIEW_SELECTION_FIRST_FAILURE_COUNTS,
        "country_counts": country_counts,
        "final_release_gates": expected_gates,
        "imagery_review_provenance": imagery_records,
        "provenance_decisions": {
            "workload_scope_bindings": provenance["workload_scope_bindings"],
            "role_bindings": provenance["role_bindings"],
            "excluded_source_roles": provenance["excluded_source_roles"],
        },
        "semantic_guardrails": SEMANTIC_GUARDRAILS,
    }
    if any(report.get(field) != value for field, value in expected_values.items()):
        raise VerifiedConstructionCoreError("preview selection-report values differ")
    if (path / "README.md").read_bytes() != _readme(report):
        raise VerifiedConstructionCoreError("preview README differs")
    expected_overlays = overlays["overlays"]
    if report.get("reviewed_overlay_queue") != expected_overlays:
        raise VerifiedConstructionCoreError("preview reviewed-overlay queue differs")
    if not isinstance(expected_overlays, list):
        raise VerifiedConstructionCoreError("preview reviewed-overlay definition differs")
    if (path / "ATTRIBUTION.txt").read_bytes() != _attribution(
        evidence, expected_overlays, imagery_records
    ):
        raise VerifiedConstructionCoreError("preview attribution differs")
    for overlay in expected_overlays:
        if "geometry_release_id" in overlay:
            _geometry_release_manifest(
                overlay.get("geometry_release_id", ""),
                overlay.get("geometry_release_manifest_sha256", ""),
            )
    derived_counts = {
        "physical_sites": len(sites),
        "projects": len(projects),
        "evidence": len(evidence),
        "countries": len({row["country"] for row in sites}),
        "non_us_sites": sum(row["country_iso_a2"] != "US" for row in sites),
        "official_boundary_projects": sum(
            _is_official_boundary(row) for row in projects
        ),
        "reviewed_site_locator_projects": sum(
            _is_reviewed_locator(row) for row in projects
        ),
    }
    if counts != derived_counts:
        raise VerifiedConstructionCoreError("preview derived counts differ")
    if counts != {
        "physical_sites": 26,
        "projects": 29,
        "evidence": 63,
        "countries": 17,
        "non_us_sites": 20,
        "official_boundary_projects": 4,
        "reviewed_site_locator_projects": 25,
    }:
        raise VerifiedConstructionCoreError("preview expected cohort counts differ")
    return manifest


V07_COUNTRY_ISO_A2 = {
    "France": "FR",
    "Italy": "IT",
    "Korea, Republic of": "KR",
    "South Africa": "ZA",
    "Spain": "ES",
    "Sweden": "SE",
}
V07_SELECTION_FIRST_FAILURE_COUNTS = {
    "entity_kind_not_project": 49,
    "not_in_reviewed_site_geometry_allowlist": 107,
    "selected": 36,
    "status_not_physical": 12,
    "status_outside_90_day_window": 327,
}
V07_OVERLAY_REQUIRED_FIELDS = frozenset(
    {
        "bridge_bytes",
        "bridge_decision",
        "bridge_id",
        "bridge_path",
        "bridge_schema",
        "bridge_sha256",
        "country",
        "decision",
        "decision_reason",
        "geometry_authority_class",
        "geometry_derivation",
        "geometry_entity",
        "geometry_entity_id",
        "geometry_entity_stable_key",
        "geometry_evidence_id",
        "geometry_evidence_key",
        "geometry_method",
        "geometry_scope_class",
        "geometry_source_kind",
        "geometry_use_scope",
        "horizontal_uncertainty_metres",
        "horizontal_uncertainty_unknown_reason",
        "official_boundary",
        "overlay_id",
        "physical_site_entity_id",
        "physical_site_stable_key",
        "portable_capture_binding",
        "portable_input_binding",
        "precision_scope",
        "project_to_campus_decision_basis",
        "project_to_campus_relationship_id",
        "project_to_campus_relationship_type",
        "rejected_claims",
        "reviewed_at",
        "source_input_bytes",
        "source_input_path",
        "source_input_sha256",
        "source_project_entity_id",
        "source_project_stable_key",
    }
)
V07_POINT_COORDINATES = {
    "curated:aws-walqa-huesca-data-center:current-build": (
        -0.4564361111111111,
        42.11381388888889,
    ),
    "curated:atnorth-swe02-stockholm-campus:current-development": (
        17.919052862381037,
        59.42019318407011,
    ),
    "curated:alto-sp01-granada-data-center-campus:phase-1-10mw-critical-it": (
        -3.7520406,
        37.0832838,
    ),
    "curated:teraco-ct1-rondebosch-data-centre:current-expansion": (
        18.4649,
        -33.9712,
    ),
    "curated:digital-edge-seoul-bupyeong-campus:sel3-phase-2": (
        126.7109,
        37.518,
    ),
}
V07_BRIDGE_INPUT_SHA256 = {
    "curated:alto-sp01-granada-data-center-campus:phase-1-10mw-critical-it": (
        "1159f75260a833f0c212501c3212c5ca091b3628c3829a78ad955898ad220272",
        "5e2f4e431b877d403d3f34d1d5de689450cfa58d1670c9e8b038f39f21d5cc80",
    ),
    "curated:atnorth-swe02-stockholm-campus:current-development": (
        "dcb8bbd8cda0018e5c8e9f28ca56d43ade9ae5e7dfa26aaef0dec85f19024b35",
        "c69edf4c7c7b7f705ce3489f3ee1f112fab2032c0483a3a1b4ace77d84283169",
    ),
    "curated:aws-walqa-huesca-data-center:current-build": (
        "b52be2ca48aa422529372ce6b721ff98c00d8b0aa4fea0a0e15b79fb1fb4b65a",
        "31048bd15869c30f78292a709f2d18e1b8423eacdbfda2faff9a9e04c0983698",
    ),
    "curated:colt-villebon-sur-yvette-campus:paris2-current-facility-build": (
        "37d8b17fc6fa2de15acf57065e3b8b225ef57d45a2d9d6a9cba33662140d6ed9",
        "aac09ee130b7d721f0605d8ea0f9916955baf6666f692eb397f7fcb6dc6737ff",
    ),
    "curated:digital-edge-seoul-bupyeong-campus:sel3-phase-2": (
        "3fb6ea1c02d44a60e1c48289f5cc089965eb3596e90fd0ce46d018a6701a4a6a",
        "b8f6ff6a9cebe6630bf9757eb78c56af1cd63cd0b949c9f72615bb0ae44ff67b",
    ),
    "curated:microsoft-san-bovio-italy-datacenter:initial-development": (
        "913510cdb274ffd280425a0b2cdefa9e8f7b8abb7562ed38a8e7bb924c33b9a8",
        "00c9e055f0df79ea1226033b40aca969f8b2948e5f5b4d9f8a913ea735f11e9f",
    ),
    "curated:teraco-ct1-rondebosch-data-centre:current-expansion": (
        "96db9e011ef5c895bb7c20e83b9d23c5e25fac4574082c26c9617393b75f9d3b",
        "2a73a6dacb84bfcd5cee8e51bf08955f9863dca328117833d94d06634fa43eea",
    ),
}


def _v07_repository_path(relative: str) -> Path:
    path = Path(relative)
    if path.is_absolute() or ".." in path.parts:
        raise VerifiedConstructionCoreError("v0.7 repository path escapes")
    resolved = (ROOT / path).resolve()
    if resolved.parent == ROOT.resolve() or ROOT.resolve() not in resolved.parents:
        raise VerifiedConstructionCoreError("v0.7 repository path differs")
    return resolved


def _v07_definition_paths() -> dict[str, tuple[Path, str]]:
    return {
        "review_definition_sha256": (
            ROOT / "definitions/verified-construction-core-v0.7-reviewed-sites.json",
            V07_REVIEW_DEFINITION_SHA256,
        ),
        "imagery_review_definition_sha256": (
            ROOT / "definitions/verified-construction-core-v0.7-imagery-reviews.json",
            V07_IMAGERY_REVIEW_DEFINITION_SHA256,
        ),
        "provenance_definition_sha256": (
            ROOT / "definitions/verified-construction-core-v0.7-provenance.json",
            V07_PROVENANCE_DEFINITION_SHA256,
        ),
        "overlay_definition_sha256": (
            ROOT / "definitions/verified-construction-core-reviewed-overlays-v5.json",
            V07_OVERLAY_DEFINITION_SHA256,
        ),
    }


def _validate_v07_definition_pins() -> None:
    for field, (path, expected) in _v07_definition_paths().items():
        if path.is_symlink() or not path.is_file() or _sha256_file(path) != expected:
            raise VerifiedConstructionCoreError(f"current v0.7 {field} source hash differs")


def _v07_hydrated_crosscheck_available() -> bool:
    required = (
        ROOT / "releases/2026-07-22-open-seed-v97/construction_pipeline.csv",
        ROOT / "releases/2026-07-22-open-seed-v97/entities.csv",
        ROOT / "releases/2026-07-22-open-seed-v97/evidence.csv",
        ROOT / "releases/2026-07-18-global-open-v3/entities.csv",
        ROOT / "releases/2026-07-18-global-open-v3/evidence.csv",
        ROOT
        / "exact_identity_decisions/2026-07-22-public-open-v14/component-members.csv",
        ROOT
        / "exact_identity_decisions/2026-07-22-public-open-v14/relationships.csv",
    )
    present = [path.is_file() for path in required]
    if any(present) and not all(present):
        raise VerifiedConstructionCoreError(
            "v0.7 hydrated cross-check inputs are only partially present"
        )
    return all(present)


def _v07_source_evidence(source: Mapping[str, Any]) -> dict[str, dict[str, Any]]:
    rows = source.get("evidence")
    if not isinstance(rows, list) or not rows:
        raise VerifiedConstructionCoreError("v0.7 source evidence differs")
    result: dict[str, dict[str, Any]] = {}
    for row in rows:
        if not isinstance(row, dict):
            raise VerifiedConstructionCoreError("v0.7 source evidence differs")
        evidence_id = _official_evidence_id(row)
        projected = {
            field: (row.get(field) if row.get(field) is not None else "")
            for field in EVIDENCE_FIELDS
            if field not in {"evidence_id", "roles_json", "project_ids_json"}
        }
        projected["evidence_id"] = evidence_id
        if evidence_id in result:
            raise VerifiedConstructionCoreError("v0.7 source evidence is not unique")
        result[evidence_id] = projected
    return result


def _v07_source_roles(project: Mapping[str, Any]) -> dict[str, list[str]]:
    normalized = project.get("normalized_roles")
    if normalized is not None:
        if not isinstance(normalized, dict):
            raise VerifiedConstructionCoreError("v0.7 normalized roles differ")
        source = normalized
    else:
        source = project
    result: dict[str, list[str]] = {}
    for role, column in ROLE_COLUMNS.items():
        value = source.get(column)
        if value is None:
            parties: list[str] = []
        elif isinstance(value, str):
            parties = [party.strip() for party in value.split(";") if party.strip()]
        elif isinstance(value, list) and all(isinstance(party, str) for party in value):
            parties = [party.strip() for party in value if party.strip()]
        else:
            raise VerifiedConstructionCoreError("v0.7 source roles differ")
        if len(parties) != len(set(parties)):
            raise VerifiedConstructionCoreError("v0.7 source roles are not unique")
        result[role] = sorted(parties)
    return result


def _v07_validate_capture_binding(binding: Any, *, required: bool) -> Path | None:
    if binding is None:
        if required:
            raise VerifiedConstructionCoreError("v0.7 portable capture is absent")
        return None
    if not isinstance(binding, dict) or not {"path", "bytes", "sha256"} <= set(binding):
        raise VerifiedConstructionCoreError("v0.7 portable capture binding differs")
    path = _repository_input(binding["path"], binding["sha256"], "v0.7 capture")
    if path.stat().st_size != binding["bytes"]:
        raise VerifiedConstructionCoreError("v0.7 portable capture byte count differs")
    return path


def _validate_v07_colt_capture(
    bridge: Mapping[str, Any], binding: Mapping[str, Any]
) -> None:
    path = _v07_validate_capture_binding(binding, required=True)
    capture = _load_json(path)
    selection = capture.get("selection") if isinstance(capture, dict) else None
    feature = selection.get("selected_feature") if isinstance(selection, dict) else None
    if (
        capture.get("capture_id")
        != "verified-construction-core-v0.7-colt-paris2-dgfip-ap8-capture"
        or capture.get("schema_version") != 1
        or not isinstance(feature, dict)
        or feature.get("id") != "91661000AP0008"
        or selection.get("selected_feature_count") != 1
        or selection.get("selected_feature_canonical_sha256")
        != _sha256_bytes(_json_bytes(feature))
        or selection.get("selected_feature_canonical_bytes") != len(_json_bytes(feature))
        or feature.get("geometry") != bridge["geometry_entity"]["geometry"]
        or capture.get("source", {}).get("license") != "etalab-2.0"
    ):
        raise VerifiedConstructionCoreError("v0.7 Colt capture replay differs")
    fact = bridge["geometry_evidence"]["fact_payload"]
    ring = feature["geometry"]["coordinates"][0]
    origin = ring[0]
    translated_ring = [
        [point[0] - origin[0], point[1] - origin[1]] for point in ring
    ]
    twice_area = sum(
        left[0] * right[1] - right[0] * left[1]
        for left, right in zip(translated_ring, translated_ring[1:])
    )
    centroid = [
        round(
            origin[index]
            +
            sum(
                (left[index] + right[index])
                * (left[0] * right[1] - right[0] * left[1])
                for left, right in zip(translated_ring, translated_ring[1:])
            )
            / (3 * twice_area),
            10,
        )
        for index in (0, 1)
    ]
    if (
        fact.get("selected_feature_canonical_sha256")
        != selection["selected_feature_canonical_sha256"]
        or fact.get("cadastral_feature_id") != feature["id"]
        or fact.get("contenance_m2_as_reported")
        != feature.get("properties", {}).get("contenance")
        or centroid != [2.2199845682, 48.690997187]
        or [
            bridge["geometry_entity"].get("longitude"),
            bridge["geometry_entity"].get("latitude"),
        ]
        != centroid
    ):
        raise VerifiedConstructionCoreError("v0.7 Colt fact projection differs")


def _validate_v07_san_capture(
    bridge: Mapping[str, Any], binding: Mapping[str, Any]
) -> None:
    path = _v07_validate_capture_binding(binding, required=True)
    capture = _load_json(path)
    try:
        raw = base64.b64decode(
            capture["payload"]["raw_response_body_base64"], validate=True
        )
        payload = json.loads(raw)
    except (KeyError, TypeError, ValueError, binascii.Error, json.JSONDecodeError) as error:
        raise VerifiedConstructionCoreError("v0.7 San Bovio capture differs") from error
    response = capture.get("response", {})
    if (
        len(raw) != binding.get("raw_payload_bytes")
        or _sha256_bytes(raw) != binding.get("raw_payload_sha256")
        or len(raw) != response.get("decoded_body_bytes")
        or _sha256_bytes(raw) != response.get("decoded_body_sha256")
    ):
        raise VerifiedConstructionCoreError("v0.7 San Bovio raw capture differs")
    elements = payload.get("elements")
    if not isinstance(elements, list):
        raise VerifiedConstructionCoreError("v0.7 San Bovio capture elements differ")
    ways = [row for row in elements if row.get("type") == "way"]
    nodes = {row.get("id"): row for row in elements if row.get("type") == "node"}
    if len(ways) != 1 or ways[0].get("id") != 554352050 or len(nodes) != 13:
        raise VerifiedConstructionCoreError("v0.7 San Bovio capture object differs")
    way = ways[0]
    try:
        ring = [[nodes[node_id]["lon"], nodes[node_id]["lat"]] for node_id in way["nodes"]]
    except (KeyError, TypeError) as error:
        raise VerifiedConstructionCoreError("v0.7 San Bovio capture ring differs") from error
    entity = bridge["geometry_entity"]
    representative = entity.get("representative_point", {})
    projected = representative.get("projected_centroid", {})
    tools = representative.get("toolchain", {})
    if (
        entity.get("geometry") != {"type": "Polygon", "coordinates": [ring]}
        or representative.get("coordinates")
        != [9.310638890241082, 45.46212956614725]
        or [entity.get("longitude"), entity.get("latitude")]
        != [9.310638890241082, 45.46212956614725]
        or projected
        != {
            "crs": "EPSG:32632",
            "easting_m": 524285.4851843531,
            "northing_m": 5034336.149184176,
        }
        or tools != {"pyproj": "3.7.2", "proj": "9.5.1", "shapely": "2.1.2"}
        or entity.get("geometry_metrics", {}).get("area_m2") != 84562.7109
        or way.get("tags") != {"landuse": "industrial", "name": "Ex Postal Market"}
        or way.get("timestamp") != "2022-07-19T17:15:33Z"
    ):
        raise VerifiedConstructionCoreError("v0.7 San Bovio geometry replay differs")


def _validate_v07_topology(
    bridge: Mapping[str, Any],
    project: Mapping[str, Any],
    campus: Mapping[str, Any],
    *,
    hydrated_crosscheck: bool,
) -> None:
    topology = bridge["construction_source"]["project_to_campus"]
    if not isinstance(topology, dict) or set(topology) != {
        "basis",
        "target_entity_id",
        "bundle_id",
        "manifest",
        "members",
        "relationship",
        "subject_member",
        "object_member",
        "supplemental_v97_atlas_feature",
    }:
        raise VerifiedConstructionCoreError("v0.7 topology fields differ")
    if (
        topology.get("basis") != "v14_project_targets_explicit_parent"
        or topology.get("bundle_id") != "2026-07-22-public-open-v14"
        or topology.get("target_entity_id") != campus.get("entity_id")
        or topology.get("supplemental_v97_atlas_feature") is not None
    ):
        raise VerifiedConstructionCoreError("v0.7 topology identity differs")
    _, manifest = _manifest_binding(
        topology["manifest"], label="v0.7 topology release"
    )
    if (
        manifest.get("bundle_id") != topology["bundle_id"]
        or manifest.get("format")
        != "datacenter-atlas-exact-identity-decision-bundle-v1"
        or set(topology["members"])
        != {"component-members.csv", "relationships.csv"}
    ):
        raise VerifiedConstructionCoreError("v0.7 topology release differs")
    _validate_manifest_members(
        manifest, topology["members"], label="v0.7 topology release"
    )
    component_records = (
        ("subject", topology["subject_member"], project, "project"),
        ("object", topology["object_member"], campus, "campus"),
    )
    for label, record, entity, kind in component_records:
        if not isinstance(record, dict) or set(record) != {
            "component_id",
            "entity_id",
            "stable_key",
            "source_row",
        }:
            raise VerifiedConstructionCoreError(
                f"v0.7 topology {label} fields differ"
            )
        row = _embedded_csv_source_row(
            record["source_row"],
            EXACT_COMPONENT_FIELDS,
            label=f"v0.7 topology {label}",
        )
        occurrence_id = f"{SOURCE_RELEASE_ID}:{entity['entity_id']}"
        if (
            record["entity_id"] != entity["entity_id"]
            or record["stable_key"] != entity["stable_key"]
            or row["release_id"] != SOURCE_RELEASE_ID
            or row["entity_id"] != entity["entity_id"]
            or row["entity_kind"] != kind
            or row["stable_key"] != entity["stable_key"]
            or row["occurrence_id"] != occurrence_id
            or row["component_id"] != _exact_component_id(kind, occurrence_id)
            or record["component_id"] != row["component_id"]
            or row["component_member_count"] != "1"
            or row["identity_proof_parent_occurrence_id"]
            or _csv_json(row["typed_identity_tokens_json"], label="v0.7 topology tokens")
            != []
            or _csv_json(row["ambiguous_identity_tokens_json"], label="v0.7 topology ambiguity")
            != []
            or not row["source_root"]
            or row["source_family"] != row["source_root"]
            or not row["snapshot_evidence_id"]
        ):
            raise VerifiedConstructionCoreError(
                f"v0.7 topology {label} projection differs"
            )
    relationship = topology["relationship"]
    row = _embedded_csv_source_row(
        relationship.get("source_row"),
        EXACT_RELATIONSHIP_FIELDS,
        label="v0.7 topology relationship",
    )
    projection = _exact_relationship_projection(row)
    subject = topology["subject_member"]
    object_member = topology["object_member"]
    if (
        not isinstance(relationship, dict)
        or {field: value for field, value in relationship.items() if field != "source_row"}
        != projection
        or relationship.get("relationship_type") != "project_targets"
        or relationship.get("subject_component_id") != subject["component_id"]
        or relationship.get("subject_kind") != "project"
        or relationship.get("object_component_id") != object_member["component_id"]
        or relationship.get("object_kind") != "campus"
        or relationship.get("decision_basis") != "explicit_parent"
        or relationship.get("typed_identity_tokens") != []
        or relationship.get("source_release_ids") != [SOURCE_RELEASE_ID]
        or relationship.get("raw_relationship_count") != 1
        or relationship.get("relationship_id")
        != _exact_relationship_id(
            "project_targets", subject["component_id"], object_member["component_id"]
        )
    ):
        raise VerifiedConstructionCoreError("v0.7 topology relationship differs")
    if hydrated_crosscheck:
        for name, record, fields in (
            ("relationships.csv", relationship, EXACT_RELATIONSHIP_FIELDS),
            ("component-members.csv", subject, EXACT_COMPONENT_FIELDS),
            ("component-members.csv", object_member, EXACT_COMPONENT_FIELDS),
        ):
            metadata = topology["members"][name]
            payload_path = _repository_input(
                metadata["path"], metadata["sha256"], f"v0.7 {name}"
            )
            if payload_path.stat().st_size != metadata["bytes"]:
                raise VerifiedConstructionCoreError(
                    f"v0.7 hydrated topology differs: {name}"
                )
            _validate_hydrated_source_row(
                payload_path,
                record["source_row"],
                fields,
                label=f"v0.7 {name}",
            )


def _validate_v07_bridge(
    acceptance: Mapping[str, Any],
    overlay: Mapping[str, Any],
    *,
    hydrated_crosscheck: bool,
) -> tuple[dict[str, Any], dict[str, Any]]:
    project_key = acceptance["project_stable_key"]
    immutable_pins = V07_BRIDGE_INPUT_SHA256.get(project_key)
    if immutable_pins != (
        acceptance["bridge_sha256"],
        acceptance["source_input_sha256"],
    ):
        raise VerifiedConstructionCoreError("v0.7 immutable bridge profile differs")
    path = _repository_input(
        acceptance["bridge_path"], acceptance["bridge_sha256"], "geometry bridge"
    )
    if path.stat().st_size != acceptance["bridge_bytes"]:
        raise VerifiedConstructionCoreError("v0.7 bridge byte count differs")
    bridge = _load_json(path)
    expected_top = {
        "bridge_id",
        "bridge_schema",
        "claim_guardrails",
        "construction_source",
        "geometry_capture",
        "geometry_entity",
        "geometry_evidence",
        "geometry_release",
        "identity_bridge_evidence",
        "imagery_posture",
        "lineage_context",
        "review_decision",
        "reviewed_at",
        "rights",
        "schema_version",
    }
    if (
        not isinstance(bridge, dict)
        or set(bridge) != expected_top
        or bridge.get("schema_version") != 1
        or bridge.get("bridge_id") != acceptance["bridge_id"]
        or bridge.get("bridge_schema") != acceptance["bridge_schema"]
        or bridge.get("reviewed_at") != CURRENT_V07_REVIEW_DATE.isoformat()
        or bridge.get("geometry_release") is not None
    ):
        raise VerifiedConstructionCoreError("v0.7 bridge identity differs")
    schema = bridge["bridge_schema"]
    allowed_schemas = {
        "verified-construction-core-v0.7-direct-official-geometry-bridge-v1",
        "verified-construction-core-v0.7-cross-source-campus-geometry-bridge-v1",
        "verified-construction-core-v0.7-capture-replayed-campus-geometry-bridge-v1",
    }
    if schema not in allowed_schemas:
        raise VerifiedConstructionCoreError("v0.7 bridge schema differs")
    construction = bridge["construction_source"]
    source_input = construction.get("input")
    expected_input = {
        "path": acceptance["source_input_path"],
        "bytes": acceptance["source_input_bytes"],
        "sha256": acceptance["source_input_sha256"],
    }
    if source_input != expected_input or acceptance["portable_input_binding"] != expected_input:
        raise VerifiedConstructionCoreError("v0.7 bridge source input differs")
    source_path = _repository_input(
        expected_input["path"], expected_input["sha256"], "v0.7 source input"
    )
    if source_path.stat().st_size != expected_input["bytes"]:
        raise VerifiedConstructionCoreError("v0.7 source input byte count differs")
    source = _load_json(source_path)
    source_project = source.get("project") if isinstance(source, dict) else None
    source_campus = source.get("campus") if isinstance(source, dict) else None
    if (
        not isinstance(source_project, dict)
        or not isinstance(source_campus, dict)
        or source_project.get("stable_key") != acceptance["project_stable_key"]
        or source_campus.get("stable_key") != acceptance["physical_site_stable_key"]
        or atlas_stable_id("entity", acceptance["project_stable_key"], "project")
        != acceptance["project_entity_id"]
        or atlas_stable_id("entity", acceptance["physical_site_stable_key"], "campus")
        != acceptance["physical_site_entity_id"]
    ):
        raise VerifiedConstructionCoreError("v0.7 source identity differs")
    source_evidence = _v07_source_evidence(source)
    project = construction.get("project")
    campus = construction.get("campus")
    if (
        not isinstance(project, dict)
        or not isinstance(campus, dict)
        or project.get("stable_key") != acceptance["project_stable_key"]
        or project.get("entity_id") != acceptance["project_entity_id"]
        or campus.get("stable_key") != acceptance["physical_site_stable_key"]
        or campus.get("entity_id") != acceptance["physical_site_entity_id"]
        or project.get("country") != acceptance["country"]
        or campus.get("country") != acceptance["country"]
    ):
        raise VerifiedConstructionCoreError("v0.7 bridge construction identity differs")
    status = construction.get("status_evidence")
    if (
        not isinstance(status, dict)
        or status.get("evidence_id") != project.get("status_evidence_id")
        or status.get("evidence_id") not in source_evidence
    ):
        raise VerifiedConstructionCoreError("v0.7 status evidence differs")
    for field in GLOBAL_GEOMETRY_EVIDENCE_FIELDS:
        expected = source_evidence[status["evidence_id"]].get(field, "")
        actual = status.get(field)
        if actual is None:
            actual = ""
        if actual != expected:
            raise VerifiedConstructionCoreError("v0.7 status projection differs")
    release = construction.get("release")
    if not isinstance(release, dict) or release.get("release_id") != SOURCE_RELEASE_ID:
        raise VerifiedConstructionCoreError("v0.7 source release differs")
    _, release_manifest = _manifest_binding(
        release["manifest"], label="v0.7 source release"
    )
    _validate_manifest_members(
        release_manifest, release["members"], label="v0.7 source release"
    )
    embedded_rows = release.get("raw_rows", construction.get("release_rows"))
    if embedded_rows is not None:
        _validate_v06_embedded_release_rows(
            embedded_rows,
            release["members"],
            hydrated_crosscheck=hydrated_crosscheck,
        )
    else:
        for label, record, member in (
            ("project", project, "construction_pipeline.csv"),
            ("campus", campus, "entities.csv"),
        ):
            row = _embedded_csv_source_row(
                record.get("source_row"),
                SOURCE_ENTITY_FIELDS,
                label=f"v0.7 {label}",
            )
            if (
                row["stable_key"] != record["stable_key"]
                or row["entity_id"] != record["entity_id"]
            ):
                raise VerifiedConstructionCoreError(
                    f"v0.7 {label} source-row projection differs"
                )
            if hydrated_crosscheck:
                metadata = release["members"][member]
                payload_path = _repository_input(
                    metadata["path"], metadata["sha256"], f"v0.7 {member}"
                )
                _validate_hydrated_source_row(
                    payload_path,
                    record["source_row"],
                    SOURCE_ENTITY_FIELDS,
                    label=f"v0.7 {label}",
                )
        status_row = _embedded_csv_source_row(
            status.get("source_row"),
            GLOBAL_GEOMETRY_EVIDENCE_FIELDS,
            label="v0.7 status",
        )
        if status_row["evidence_id"] != status["evidence_id"]:
            raise VerifiedConstructionCoreError(
                "v0.7 status source-row projection differs"
            )
        if hydrated_crosscheck:
            metadata = release["members"]["evidence.csv"]
            payload_path = _repository_input(
                metadata["path"], metadata["sha256"], "v0.7 evidence.csv"
            )
            _validate_hydrated_source_row(
                payload_path,
                status["source_row"],
                GLOBAL_GEOMETRY_EVIDENCE_FIELDS,
                label="v0.7 status",
            )
    evidence_by_key = {
        row["key"]: row for row in source["evidence"] if isinstance(row, dict)
    }
    campus_evidence = evidence_by_key.get(source_campus.get("evidence_key"))
    if campus_evidence is None:
        raise VerifiedConstructionCoreError("v0.7 campus evidence differs")
    _validate_v07_topology(
        bridge, project, campus, hydrated_crosscheck=hydrated_crosscheck
    )
    topology = construction["project_to_campus"]
    relationship = topology["relationship"]
    if (
        relationship.get("relationship_id")
        != acceptance["project_to_campus_relationship_id"]
        or relationship.get("relationship_type")
        != acceptance["project_to_campus_relationship_type"]
        or relationship.get("decision_basis")
        != acceptance["project_to_campus_decision_basis"]
    ):
        raise VerifiedConstructionCoreError("v0.7 topology definition differs")
    entity = bridge["geometry_entity"]
    evidence = bridge["geometry_evidence"]
    _validate_v06_fact_evidence(evidence, label="v0.7 geometry evidence")
    if (
        entity.get("entity_kind") != acceptance["geometry_entity"]
        or entity.get("stable_key") != acceptance["geometry_entity_stable_key"]
        or entity.get("entity_id") != acceptance["geometry_entity_id"]
        or entity.get("source_evidence_id") != acceptance["geometry_evidence_id"]
        or evidence.get("key") != acceptance["geometry_evidence_key"]
        or evidence.get("evidence_id") != acceptance["geometry_evidence_id"]
        or evidence.get("kind") != acceptance["geometry_source_kind"]
        or evidence.get("evidence_id") != _official_evidence_id(evidence)
        or entity.get("geometry", {}).get("type") not in {"Point", "Polygon"}
    ):
        raise VerifiedConstructionCoreError("v0.7 geometry projection differs")
    decision = bridge["review_decision"]
    decision_bindings = {
        "decision": "bridge_decision",
        "geometry_target_entity_kind": "geometry_entity",
        "geometry_derivation": "geometry_derivation",
        "geometry_method": "geometry_method",
        "geometry_scope_class": "geometry_scope_class",
        "geometry_authority_class": "geometry_authority_class",
        "geometry_use_scope": "geometry_use_scope",
        "horizontal_uncertainty_metres": "horizontal_uncertainty_metres",
        "horizontal_uncertainty_unknown_reason": "horizontal_uncertainty_unknown_reason",
        "precision_scope": "precision_scope",
        "rejected_claims": "rejected_claims",
    }
    if any(decision.get(field) != acceptance[target] for field, target in decision_bindings.items()):
        raise VerifiedConstructionCoreError("v0.7 review semantics differ")
    if decision.get("official_boundary", acceptance["official_boundary"]) != acceptance[
        "official_boundary"
    ]:
        raise VerifiedConstructionCoreError("v0.7 official-boundary semantics differ")
    if project_key in V07_POINT_COORDINATES:
        expected_point = list(V07_POINT_COORDINATES[project_key])
        fact = evidence["fact_payload"]
        if project_key == "curated:atnorth-swe02-stockholm-campus:current-development":
            fact_point = [
                fact.get("coordinate_transform", {}).get("longitude"),
                fact.get("coordinate_transform", {}).get("latitude"),
            ]
        elif project_key == "curated:alto-sp01-granada-data-center-campus:phase-1-10mw-critical-it":
            fact_point = [
                fact.get("location_link_resolution", {}).get("longitude"),
                fact.get("location_link_resolution", {}).get("latitude"),
            ]
        else:
            fact_point = [
                fact.get("longitude_decimal"),
                fact.get("latitude_decimal"),
            ]
        if (
            entity["geometry"] != {"type": "Point", "coordinates": expected_point}
            or [entity["longitude"], entity["latitude"]] != expected_point
            or fact_point != expected_point
        ):
            raise VerifiedConstructionCoreError("v0.7 point transform differs")
    if project_key == "curated:aws-walqa-huesca-data-center:current-build":
        fact = evidence["fact_payload"]
        expected_lat = 42 + 6 / 60 + 49.73 / 3600
        expected_lon = -(0 + 27 / 60 + 23.17 / 3600)
        if fact.get("latitude_decimal") != expected_lat or fact.get("longitude_decimal") != expected_lon:
            raise VerifiedConstructionCoreError("v0.7 Walqa DMS conversion differs")
    if project_key == "curated:atnorth-swe02-stockholm-campus:current-development":
        transform_fact = evidence["fact_payload"].get("coordinate_transform", {})
        source_point = evidence["fact_payload"].get("source_map_point", {})
        if (
            source_point != {"crs": "EPSG:3011", "x": 145404.34988421336, "y": 6589480.87816898}
            or transform_fact.get("tool") != "pyproj 3.7.2 / PROJ 9.5.1"
            or transform_fact.get("longitude") != 17.919052862381037
            or transform_fact.get("latitude") != 59.42019318407011
        ):
            raise VerifiedConstructionCoreError("v0.7 SWE02 transform differs")
    portable_capture = acceptance["portable_capture_binding"]
    if project_key == "curated:colt-villebon-sur-yvette-campus:paris2-current-facility-build":
        if bridge["geometry_capture"] != {
            field: portable_capture[field] for field in ("path", "bytes", "sha256")
        }:
            raise VerifiedConstructionCoreError("v0.7 Colt capture binding differs")
        _validate_v07_colt_capture(bridge, portable_capture)
    elif project_key == "curated:microsoft-san-bovio-italy-datacenter:initial-development":
        if bridge["geometry_capture"] != portable_capture:
            raise VerifiedConstructionCoreError("v0.7 San capture binding differs")
        _validate_v07_san_capture(bridge, portable_capture)
    elif bridge["geometry_capture"] is not None or portable_capture is not None:
        raise VerifiedConstructionCoreError("v0.7 compact-fact capture posture differs")
    for index, identity in enumerate(bridge["identity_bridge_evidence"]):
        _validate_v06_fact_evidence(
            identity, label=f"v0.7 identity evidence {index}"
        )
    if (
        not isinstance(bridge["claim_guardrails"], dict)
        or not bridge["claim_guardrails"]
        or any(not isinstance(value, str) or not value for value in bridge["claim_guardrails"].values())
        or not isinstance(bridge["rights"], dict)
        or bridge["imagery_posture"].get("independent_imagery_verification") is not False
    ):
        raise VerifiedConstructionCoreError("v0.7 guardrails differ")
    if project_key == "curated:microsoft-san-bovio-italy-datacenter:initial-development" and (
        bridge["rights"].get("geometry_attribution_url")
        != "https://www.openstreetmap.org/copyright"
        or bridge["rights"].get("geometry_license_url")
        != "https://opendatacommons.org/licenses/odbl/1-0/"
    ):
        raise VerifiedConstructionCoreError("v0.7 OSM rights differ")
    if project_key == "curated:digital-edge-seoul-bupyeong-campus:sel3-phase-2" and (
        entity.get("country") != "Korea, Republic of"
        or "SEL2" not in decision.get("identity_basis", "")
        or "adjacent" not in decision.get("identity_basis", "")
    ):
        raise VerifiedConstructionCoreError("v0.7 SEL3 shared-campus identity differs")
    return bridge, source


def _v07_contracts(*, hydrated_crosscheck: bool) -> dict[str, Any]:
    _validate_v07_definition_pins()
    reviewed = _load_json(
        ROOT / "definitions/verified-construction-core-v0.7-reviewed-sites.json"
    )
    overlays = _load_json(
        ROOT / "definitions/verified-construction-core-reviewed-overlays-v5.json"
    )
    provenance = _load_json(
        ROOT / "definitions/verified-construction-core-v0.7-provenance.json"
    )
    imagery = _load_json(
        ROOT / "definitions/verified-construction-core-v0.7-imagery-reviews.json"
    )
    if (
        set(reviewed) != {"contract_id", "review_scope", "reviewed_as_of", "base_contract", "acceptances"}
        or reviewed.get("contract_id") != "verified-construction-core-v0.7-reviewed-sites"
        or reviewed.get("reviewed_as_of") != CURRENT_V07_REVIEW_DATE.isoformat()
        or reviewed.get("base_contract")
        != {
            "path": "definitions/verified-construction-core-v0.6-reviewed-sites.json",
            "sha256": V06_REVIEW_DEFINITION_SHA256,
            "default_geometry_entity": "project",
            "default_geometry_derivation": "direct_geometry",
        }
        or not isinstance(reviewed.get("acceptances"), list)
        or len(reviewed["acceptances"]) != 7
    ):
        raise VerifiedConstructionCoreError("v0.7 reviewed-site contract differs")
    if (
        set(overlays)
        != {"contract_id", "purpose", "reviewed_as_of", "base_contract", "required_fields", "allowed_decisions", "overlays"}
        or overlays.get("contract_id")
        != "verified-construction-core-reviewed-overlays-v5"
        or overlays.get("reviewed_as_of") != CURRENT_V07_REVIEW_DATE.isoformat()
        or overlays.get("base_contract")
        != {
            "path": "definitions/verified-construction-core-reviewed-overlays-v4.json",
            "sha256": V06_OVERLAY_DEFINITION_SHA256,
        }
        or set(overlays.get("allowed_decisions", [])) != {"queued", "accepted", "excluded"}
        or not isinstance(overlays.get("overlays"), list)
        or len(overlays["overlays"]) != 7
    ):
        raise VerifiedConstructionCoreError("v0.7 overlay contract differs")
    required_overlay_fields = set(overlays["required_fields"])
    if required_overlay_fields != V07_OVERLAY_REQUIRED_FIELDS:
        raise VerifiedConstructionCoreError("v0.7 overlay required fields differ")
    if any(not isinstance(row, dict) or set(row) != required_overlay_fields for row in overlays["overlays"]):
        raise VerifiedConstructionCoreError("v0.7 overlay row schema differs")
    acceptances = reviewed["acceptances"]
    acceptance_by_key = {row.get("project_stable_key"): row for row in acceptances}
    overlay_by_key = {row.get("source_project_stable_key"): row for row in overlays["overlays"]}
    if (
        len(acceptance_by_key) != 7
        or len(overlay_by_key) != 7
        or set(acceptance_by_key) != set(overlay_by_key)
    ):
        raise VerifiedConstructionCoreError("v0.7 definition cohort differs")
    bridges: dict[str, dict[str, Any]] = {}
    sources: dict[str, dict[str, Any]] = {}
    for project_key in sorted(acceptance_by_key):
        acceptance = acceptance_by_key[project_key]
        overlay = overlay_by_key[project_key]
        crosswalk = {
            "country": "country",
            "physical_site_stable_key": "physical_site_stable_key",
            "physical_site_entity_id": "physical_site_entity_id",
            "source_input_path": "source_input_path",
            "source_input_bytes": "source_input_bytes",
            "source_input_sha256": "source_input_sha256",
            "bridge_id": "bridge_id",
            "bridge_schema": "bridge_schema",
            "bridge_path": "bridge_path",
            "bridge_bytes": "bridge_bytes",
            "bridge_sha256": "bridge_sha256",
            "project_to_campus_relationship_id": "project_to_campus_relationship_id",
            "project_to_campus_relationship_type": "project_to_campus_relationship_type",
            "project_to_campus_decision_basis": "project_to_campus_decision_basis",
            "geometry_entity": "geometry_entity",
            "geometry_entity_stable_key": "geometry_entity_stable_key",
            "geometry_entity_id": "geometry_entity_id",
            "geometry_evidence_key": "geometry_evidence_key",
            "geometry_evidence_id": "geometry_evidence_id",
            "geometry_source_kind": "geometry_source_kind",
            "geometry_derivation": "geometry_derivation",
            "geometry_method": "geometry_method",
            "geometry_scope_class": "geometry_scope_class",
            "geometry_authority_class": "geometry_authority_class",
            "geometry_use_scope": "geometry_use_scope",
            "official_boundary": "official_boundary",
            "horizontal_uncertainty_metres": "horizontal_uncertainty_metres",
            "horizontal_uncertainty_unknown_reason": "horizontal_uncertainty_unknown_reason",
            "precision_scope": "precision_scope",
            "bridge_decision": "bridge_decision",
            "portable_input_binding": "portable_input_binding",
            "portable_capture_binding": "portable_capture_binding",
            "rejected_claims": "rejected_claims",
        }
        if (
            any(acceptance[a] != overlay[o] for a, o in crosswalk.items())
            or acceptance["project_entity_id"] != overlay["source_project_entity_id"]
            or acceptance["project_stable_key"] != overlay["source_project_stable_key"]
            or acceptance["geometry_overlay_id"] != overlay["overlay_id"]
            or acceptance["decision_basis"] != overlay["decision_reason"]
            or overlay["decision"] != "accepted"
            or overlay["reviewed_at"] != CURRENT_V07_REVIEW_DATE.isoformat()
        ):
            raise VerifiedConstructionCoreError("v0.7 acceptance-overlay binding differs")
        bridge, source = _validate_v07_bridge(
            acceptance, overlay, hydrated_crosscheck=hydrated_crosscheck
        )
        bridges[project_key] = bridge
        sources[project_key] = source
    if (
        provenance.get("contract_id") != "verified-construction-core-v0.7-provenance"
        or provenance.get("reviewed_as_of") != CURRENT_V07_REVIEW_DATE.isoformat()
        or provenance.get("base_contract")
        != {
            "path": "definitions/verified-construction-core-v0.6-provenance.json",
            "sha256": V06_PROVENANCE_DEFINITION_SHA256,
        }
        or len(provenance.get("workload_scope_bindings", [])) != 3
        or len(provenance.get("role_bindings", [])) != 1
        or len(provenance.get("excluded_source_roles", [])) != 1
    ):
        raise VerifiedConstructionCoreError("v0.7 provenance contract differs")
    workload_keys = {
        (row["project_stable_key"], row["evidence_id"], row["workload"])
        for row in provenance["workload_scope_bindings"]
    }
    expected_workloads = {
        (
            "curated:alto-sp01-granada-data-center-campus:phase-1-10mw-critical-it",
            "f9be097a-99d8-5f60-9672-146a49d37f53",
            workload,
        )
        for workload in ("ai_specialized_unspecified", "general_cloud", "hpc")
    }
    role = provenance["role_bindings"][0]
    exclusion = provenance["excluded_source_roles"][0]
    if (
        workload_keys != expected_workloads
        or any(row.get("deployment_scope") != "intended" for row in provenance["workload_scope_bindings"])
        or (role["project_stable_key"], role["role"], role["party"], role["relationship_scope"])
        != (
            "curated:alto-sp01-granada-data-center-campus:phase-1-10mw-critical-it",
            "operator",
            "Alto Infrastructure",
            "intended",
        )
        or (exclusion["project_stable_key"], exclusion["role"], exclusion["party"], exclusion["relationship_scope"])
        != (
            "curated:aws-walqa-huesca-data-center:current-build",
            "operator",
            "Amazon Web Services",
            "not_established",
        )
    ):
        raise VerifiedConstructionCoreError("v0.7 provenance semantics differ")
    for project_key, bridge in bridges.items():
        source_roles = _v07_source_roles(bridge["construction_source"]["project"])
        accepted = {
            (row["role"], row["party"])
            for row in provenance["role_bindings"]
            if row["project_stable_key"] == project_key
        }
        excluded = {
            (row["role"], row["party"])
            for row in provenance["excluded_source_roles"]
            if row["project_stable_key"] == project_key
        }
        observed = {(role_name, party) for role_name, parties in source_roles.items() for party in parties}
        if observed != accepted | excluded:
            raise VerifiedConstructionCoreError("v0.7 source role coverage differs")
    if (
        imagery.get("contract_id") != "verified-construction-core-v0.7-imagery-reviews"
        or imagery.get("reviewed_as_of") != CURRENT_V07_REVIEW_DATE.isoformat()
        or imagery.get("base_contract")
        != {
            "path": "definitions/verified-construction-core-v0.6-imagery-reviews.json",
            "sha256": V06_IMAGERY_REVIEW_DEFINITION_SHA256,
        }
        or imagery.get("default_outcome") != "not_reviewed_for_core_preview"
        or len(imagery.get("records", [])) != 1
    ):
        raise VerifiedConstructionCoreError("v0.7 imagery contract differs")
    image = imagery["records"][0]
    primary_path = _repository_input(
        image["primary_review_source_path"],
        image["primary_review_source_sha256"],
        "v0.7 imagery review",
    )
    primary = _load_json(primary_path)
    matches = [
        row
        for row in primary.get("lineage_records", [])
        if row.get("blind_id") == image["primary_blind_id"]
    ]
    if (
        image.get("project_stable_key")
        != "curated:aws-walqa-huesca-data-center:current-build"
        or image.get("portable_identity_binding") is not False
        or image.get("primary_blind_id") != "V57-S003"
        or image.get("primary_queue_id") != "satq-78500568b1f6227789ae36f4"
        or image.get("source_comparison_sha256")
        != "c383fcd9f922a2b232ea72f58694517273ec9cc986077f541641eff4a51ba123"
        or image.get("primary_verdict")
        != {"semantics": "reject_imagery_promotion", "visual_verdict": "R"}
        or image.get("independent_imagery_verification") is not False
        or image.get("no_claim_guardrail") is not True
        or len(matches) != 1
        or matches[0].get("queue_id") != image["primary_queue_id"]
        or matches[0].get("visual_verdict") != "R"
        or matches[0].get("source_visual_artifacts", {}).get("comparison.png", {}).get("sha256")
        != image["source_comparison_sha256"]
    ):
        raise VerifiedConstructionCoreError("v0.7 Walqa imagery lineage differs")
    local = image["local_identity_lineage"]
    local_path = ROOT / local["singleton_ready_path"]
    if local_path.exists():
        if local_path.is_symlink() or _sha256_file(local_path) != local["singleton_ready_sha256"]:
            raise VerifiedConstructionCoreError("v0.7 Walqa local imagery lineage differs")
        lines = local_path.read_bytes().splitlines(keepends=True)
        line_number = local["singleton_ready_line_number"]
        if not 1 <= line_number <= len(lines) or _sha256_bytes(lines[line_number - 1]) != local["singleton_ready_line_sha256"]:
            raise VerifiedConstructionCoreError("v0.7 Walqa local imagery line differs")
    return {
        "reviewed": reviewed,
        "overlays": overlays,
        "provenance": provenance,
        "imagery": imagery,
        "acceptance_by_key": acceptance_by_key,
        "overlay_by_key": overlay_by_key,
        "bridges": bridges,
        "sources": sources,
    }


def _v07_evidence_projection(row: Mapping[str, Any]) -> dict[str, Any]:
    return {
        field: (row.get(field) if row.get(field) is not None else "")
        for field in EVIDENCE_FIELDS
        if field not in {"roles_json", "project_ids_json"}
    }


def _v07_build_delta_rows(contracts: Mapping[str, Any]) -> dict[str, Any]:
    provenance = contracts["provenance"]
    imagery = contracts["imagery"]
    workload_by_key = {
        (row["project_stable_key"], row["evidence_id"], row["workload"]): row
        for row in provenance["workload_scope_bindings"]
    }
    roles_by_project: dict[str, list[dict[str, Any]]] = defaultdict(list)
    for row in provenance["role_bindings"]:
        roles_by_project[row["project_stable_key"]].append(row)
    image_by_project = {row["project_stable_key"]: row for row in imagery["records"]}
    evidence_pool: dict[str, dict[str, Any]] = {}
    evidence_usage: dict[str, dict[str, set[str]]] = defaultdict(
        lambda: {"roles": set(), "project_ids": set()}
    )
    projects: list[dict[str, Any]] = []
    sites: list[dict[str, Any]] = []
    for project_key in sorted(contracts["bridges"]):
        bridge = contracts["bridges"][project_key]
        source = contracts["sources"][project_key]
        acceptance = contracts["acceptance_by_key"][project_key]
        construction = bridge["construction_source"]
        source_evidence = _v07_source_evidence(source)
        evidence_pool.update(source_evidence)
        geometry_evidence = _v07_evidence_projection(bridge["geometry_evidence"])
        evidence_pool[geometry_evidence["evidence_id"]] = geometry_evidence
        project = construction["project"]
        campus = construction["campus"]
        project_id = project["entity_id"]
        site_id = _stable_id("vcc-site", campus["stable_key"])
        status_evidence_id = project["status_evidence_id"]
        geometry_evidence_id = bridge["geometry_evidence"]["evidence_id"]
        evidence_usage[status_evidence_id]["roles"].add("physical_status")
        evidence_usage[status_evidence_id]["project_ids"].add(project_id)
        evidence_usage[geometry_evidence_id]["roles"].add("geometry")
        evidence_usage[geometry_evidence_id]["project_ids"].add(project_id)
        observations = project.get("capacity_estimates_json", [])
        if not isinstance(observations, list):
            raise VerifiedConstructionCoreError("v0.7 capacity observations differ")
        for observation in observations:
            _validate_typed_observation(
                observation, POWER_METRICS | ENERGY_METRICS | EFFICIENCY_METRICS
            )
            evidence_id = observation["evidence_id"]
            if evidence_id not in evidence_pool:
                raise VerifiedConstructionCoreError("v0.7 capacity evidence differs")
            evidence_usage[evidence_id]["roles"].add(
                f"typed_metric:{observation['metric']}"
            )
            evidence_usage[evidence_id]["project_ids"].add(project_id)
        power = [row for row in observations if row["metric"] in POWER_METRICS]
        energy = [row for row in observations if row["metric"] in ENERGY_METRICS]
        efficiency = [row for row in observations if row["metric"] in EFFICIENCY_METRICS]
        workloads: list[dict[str, Any]] = []
        for source_workload in project.get("workloads_json", []):
            binding = workload_by_key.get(
                (project_key, source_workload["evidence_id"], source_workload["workload"])
            )
            if binding is None or any(
                source_workload[field] != binding[field]
                for field in SOURCE_WORKLOAD_FIELDS
            ):
                raise VerifiedConstructionCoreError("v0.7 workload provenance differs")
            workload = {**source_workload, "deployment_scope": binding["deployment_scope"]}
            _validate_workload_observation(workload)
            workloads.append(workload)
            evidence_id = workload["evidence_id"]
            evidence_usage[evidence_id]["roles"].update(
                {"workload", f"workload_scope:{workload['deployment_scope']}"}
            )
            evidence_usage[evidence_id]["project_ids"].add(project_id)
        role_claims: list[dict[str, str]] = []
        projected_roles = {role: [] for role in ROLE_COLUMNS}
        for binding in roles_by_project.get(project_key, []):
            evidence_id = binding["evidence_id"]
            if evidence_id not in evidence_pool:
                raise VerifiedConstructionCoreError("v0.7 role evidence differs")
            role_claims.append(
                {
                    "evidence_id": evidence_id,
                    "party": binding["party"],
                    "relationship_scope": binding["relationship_scope"],
                    "role": binding["role"],
                }
            )
            projected_roles[binding["role"]].append(binding["party"])
            evidence_usage[evidence_id]["roles"].add(f"role:{binding['role']}")
            evidence_usage[evidence_id]["project_ids"].add(project_id)
        role_claims.sort(key=lambda row: (row["role"], row["party"], row["evidence_id"]))
        entity = bridge["geometry_entity"]
        geometry = entity["geometry"]
        status_date = _calendar_date(project["status_as_of"], "v0.7 status_as_of")
        image = image_by_project.get(project_key)
        image_outcome = (
            image["outcome"]
            if image is not None
            else imagery["default_outcome"]
        )
        row = {
            "project_id": project_id,
            "project_stable_key": project_key,
            "site_id": site_id,
            "physical_site_stable_key": campus["stable_key"],
            "name": project["name"],
            "country": acceptance["country"],
            "country_iso_a2": V07_COUNTRY_ISO_A2[acceptance["country"]],
            "latitude": entity["latitude"],
            "longitude": entity["longitude"],
            "geometry_json": _json_bytes(geometry).decode().strip(),
            "geometry_type": geometry["type"],
            "geometry_source_entity_kind": acceptance["geometry_entity"],
            "geometry_derivation": acceptance["geometry_derivation"],
            "geometry_method": acceptance["geometry_method"],
            "geometry_scope_class": acceptance["geometry_scope_class"],
            "geometry_authority_class": acceptance["geometry_authority_class"],
            "geometry_use_scope": acceptance["geometry_use_scope"],
            "geometry_precision_scope": acceptance["precision_scope"],
            "horizontal_uncertainty_metres": (
                "" if acceptance["horizontal_uncertainty_metres"] is None else acceptance["horizontal_uncertainty_metres"]
            ),
            "horizontal_uncertainty_unknown_reason": acceptance["horizontal_uncertainty_unknown_reason"],
            "geometry_evidence_id": geometry_evidence_id,
            "last_observed_physical_status": project["status"],
            "status_as_of": project["status_as_of"],
            "status_age_days_at_review": (CURRENT_V07_REVIEW_DATE - status_date).days,
            "status_method": project["status_method"],
            "status_evidence_id": status_evidence_id,
            "verification_posture": _verification_posture(
                acceptance["geometry_authority_class"], acceptance["geometry_use_scope"]
            ),
            "independent_imagery_verification": "false",
            "imagery_review_outcome": image_outcome,
            "development_type": "unknown",
            "development_type_unknown_reason": "source evidence does not distinguish greenfield, expansion, or retrofit",
            "operating_model": project.get("operating_model") or "unknown",
            "operating_model_unknown_reason": (
                "" if project.get("operating_model") else "not established by selected evidence"
            ),
            "operating_model_evidence_id": project.get("operating_model_evidence_id") or "",
            "workloads_json": _json_bytes(workloads).decode().strip(),
            "workload_unknown_reason": "" if workloads else "not established by selected evidence",
            "role_claims_json": _json_bytes(role_claims).decode().strip(),
            "power_observations_json": _json_bytes(power).decode().strip(),
            "power_unknown_reason": "" if power else "no typed project power observation; not estimated",
            "annual_energy_observations_json": _json_bytes(energy).decode().strip(),
            "annual_energy_unknown_reason": "" if energy else "no scoped annual-energy inputs; not estimated",
            "efficiency_observations_json": _json_bytes(efficiency).decode().strip(),
            "efficiency_unknown_reason": "" if efficiency else "no scoped PUE or WUE observation",
            "owner": "; ".join(sorted(projected_roles["owner"])),
            "operator": "; ".join(sorted(projected_roles["operator"])),
            "users": "; ".join(sorted(projected_roles["user"])),
            "tenants": "; ".join(sorted(projected_roles["tenant"])),
            "customers": "; ".join(sorted(projected_roles["customer"])),
            "status_source_url": evidence_pool[status_evidence_id]["source_url"],
            "geometry_source_url": geometry_evidence["source_url"],
        }
        projects.append(row)
        sites.append(
            {
                "site_id": site_id,
                "physical_site_stable_key": campus["stable_key"],
                "name": campus["name"],
                "country": row["country"],
                "country_iso_a2": row["country_iso_a2"],
                "latitude": row["latitude"],
                "longitude": row["longitude"],
                "geometry_json": row["geometry_json"],
                "geometry_type": row["geometry_type"],
                "geometry_source_entity_kinds_json": _json_bytes([row["geometry_source_entity_kind"]]).decode().strip(),
                "geometry_derivations_json": _json_bytes([row["geometry_derivation"]]).decode().strip(),
                "geometry_methods_json": _json_bytes([row["geometry_method"]]).decode().strip(),
                "geometry_scope_classes_json": _json_bytes([row["geometry_scope_class"]]).decode().strip(),
                "geometry_authority_classes_json": _json_bytes([row["geometry_authority_class"]]).decode().strip(),
                "geometry_use_scopes_json": _json_bytes([row["geometry_use_scope"]]).decode().strip(),
                "geometry_precision_scopes_json": _json_bytes([row["geometry_precision_scope"]]).decode().strip(),
                "horizontal_uncertainty_metres": row["horizontal_uncertainty_metres"],
                "horizontal_uncertainty_unknown_reason": row["horizontal_uncertainty_unknown_reason"],
                "geometry_evidence_ids_json": _json_bytes([geometry_evidence_id]).decode().strip(),
                "project_count": 1,
                "project_ids_json": _json_bytes([project_id]).decode().strip(),
                "project_stable_keys_json": _json_bytes([project_key]).decode().strip(),
                "statuses_json": _json_bytes([row["last_observed_physical_status"]]).decode().strip(),
                "oldest_status_as_of": row["status_as_of"],
                "newest_status_as_of": row["status_as_of"],
                "verification_posture": row["verification_posture"],
                "independent_imagery_verification": "false",
                "imagery_review_outcomes_json": _json_bytes([image_outcome]).decode().strip(),
            }
        )
    evidence = [
        {
            **evidence_pool[evidence_id],
            "roles_json": _json_bytes(sorted(usage["roles"])).decode().strip(),
            "project_ids_json": _json_bytes(sorted(usage["project_ids"])).decode().strip(),
        }
        for evidence_id, usage in sorted(evidence_usage.items())
    ]
    return {"projects": projects, "sites": sites, "evidence": evidence}


def _v07_portable_source_inputs(contracts: Mapping[str, Any]) -> list[dict[str, Any]]:
    base_manifest = _load_json(LEGACY_PREVIEW_V06_DIR / "manifest.json")
    rows = list(base_manifest["portable_source_inputs"])
    new: dict[str, dict[str, Any]] = {}
    for acceptance in contracts["reviewed"]["acceptances"]:
        for binding in (
            acceptance["portable_input_binding"],
            {
                "path": acceptance["bridge_path"],
                "bytes": acceptance["bridge_bytes"],
                "sha256": acceptance["bridge_sha256"],
            },
            acceptance["portable_capture_binding"],
        ):
            if binding is None:
                continue
            record = {
                "path": binding["path"],
                "bytes": binding["bytes"],
                "sha256": binding["sha256"],
                "parent_manifests": [],
            }
            previous = new.get(record["path"])
            if previous is not None and previous != record:
                raise VerifiedConstructionCoreError("v0.7 portable input differs")
            new[record["path"]] = record
    rows.extend(new.values())
    rows.sort(key=lambda row: row["path"])
    if len(rows) != 41 or len({row["path"] for row in rows}) != 41:
        raise VerifiedConstructionCoreError("v0.7 portable input count differs")
    for row in rows:
        path = _repository_input(row["path"], row["sha256"], "v0.7 portable input")
        if path.stat().st_size != row["bytes"]:
            raise VerifiedConstructionCoreError("v0.7 portable input byte count differs")
    return rows


def _v07_map_attribution_notices(
    evidence: Sequence[Mapping[str, Any]],
) -> tuple[tuple[str, str, str], ...]:
    families = {
        row["source_family"]
        for row in evidence
        if "geometry" in json.loads(row["roles_json"])
    }
    notices = list(_map_attribution_notices(evidence))
    if "etalab_dgfip_cadastre_geojson" in families:
        notices.append(
            (
                "Direction générale des Finances publiques (DGFiP) — Cadastre Etalab — millésime 1 June 2026",
                "Licence Ouverte 2.0",
                "https://www.data.gouv.fr/pages/legal/licences/etalab-2.0",
            )
        )
    if "hong_kong_lands_department_coordinate_transformation_api" in families:
        notices.append(
            (
                "Government of the Hong Kong SAR — Lands Department",
                "DATA.GOV.HK Terms of Use",
                "https://data.gov.hk/en/terms-and-conditions",
            )
        )
    if families & {
        "latvia_vzd_address_register",
        "latvia_vzd_cadastre_and_lvrtc_site_records",
    }:
        notices.append(
            (
                "Valsts zemes dienests (State Land Service of Latvia)",
                "CC BY 4.0",
                "https://www.vzd.gov.lv/en",
            )
        )
    if "denver_open_data_property_parcels" in families:
        notices.append(
            (
                "City and County of Denver",
                "CC BY 3.0",
                "https://www.denvergov.org/opendata",
            )
        )
    return tuple(notices)


def _v07_map_html(
    geojson: Mapping[str, Any], evidence: Sequence[Mapping[str, Any]]
) -> bytes:
    document = _map_html(geojson, evidence).decode("utf-8")
    document = document.replace(
        "Verified Construction Core v0.6 preview",
        "Verified Construction Core v0.7 preview",
    )
    old = " · ".join(
        f'<a href="{url}">{label}</a> — {license_name}'
        for label, license_name, url in _map_attribution_notices(evidence)
    )
    new = " · ".join(
        f'<a href="{url}">{label}</a> — {license_name}'
        for label, license_name, url in _v07_map_attribution_notices(evidence)
    )
    if old not in document:
        raise VerifiedConstructionCoreError("v0.7 map attribution seam differs")
    return document.replace(old, new, 1).encode("utf-8")


def _v07_schema() -> dict[str, Any]:
    schema = _schema()
    schema["format"] = "datacenter-atlas-verified-construction-core-schema-v7"
    schema["preview_id"] = CURRENT_V07_PREVIEW_ID
    fields = schema["tables"]["projects.csv"]["fields"]
    derivation = next(row for row in fields if row["name"] == "geometry_derivation")
    derivation["allowed_values"] = [
        "capture_replayed_geometry",
        "coordinates_to_point",
        "cross_source_geometry",
        "cross_source_overlay",
        "direct_geometry",
        "official_parcel_union",
    ]
    schema["map"]["derived_from"] = ["sites.geojson", "evidence.csv"]
    return schema


def _v07_readme(report: Mapping[str, Any]) -> bytes:
    return f"""# Verified Construction Core v0.7 preview

This tracked, non-final preview contains **{report['selected_physical_site_count']} physical sites**
and **{report['selected_project_count']} linked projects** in {len(report['country_counts'])}
countries. Every project retains a dated authoritative physical-status observation and an exact
reviewed geometry scope. Five project rows carry official boundaries; the remaining 31 are
explicit project or campus locators, never construction footprints by implication.

The seven-row v0.7 delta adds Colt PAR2, AWS Walqa, atNorth SWE02, Alto SP01, Teraco CT1,
Digital Edge SEL3, and Microsoft San Bovio. Colt uses an Etalab cadastral parcel as its official
project boundary. Walqa, SWE02, Alto, Teraco, and SEL3 use narrowly scoped official-source points.
SEL3 inherits only a shared-campus locator from adjacent SEL2. San Bovio uses a capture-replayed
OpenStreetMap former-industrial-site polygon only as a community-mapped campus locator.

Alto contributes three intended-only workloads and one intended-operator claim. AWS branding and
its campus operating model do not establish a project operator. Walqa's V57-S003 imagery verdict
remains a locally unsealed blind rejection and creates no construction, identity, geometry, role,
workload, capacity, or lifecycle claim.

The artifact is deterministic and rebuildable in a clean clone from the 41 manifest-bound portable
inputs plus the frozen v0.6 artifact. Ignored v97, global-v3, and v14 payloads are optional hydrated
cross-checks, not build dependencies. Missing power, energy, PUE, WUE, workloads, roles, operating
models, and development types remain explicit unknowns rather than zero.

This is not the final 100-site Verified Construction Core v1. Its final-release gates remain
unsatisfied and `publishable_as_final` is false.
""".encode("utf-8")


def _v07_attribution(evidence: Sequence[Mapping[str, Any]]) -> bytes:
    rows = {(row["publisher"], row["license"], row["source_url"]) for row in evidence}
    lines = [
        "Data Center Atlas Verified Construction Core v0.7 preview",
        "",
        "Compact derived facts only; third-party terms remain controlling.",
        "",
        *(
            f"- {publisher} | {license_name} | {url}"
            for publisher, license_name, url in sorted(rows)
        ),
        "",
        "Map-provider terms:",
        *(
            f"- {label} | {license_name} | {url}"
            for label, license_name, url in _v07_map_attribution_notices(evidence)
        ),
    ]
    return ("\n".join(lines) + "\n").encode("utf-8")


def _v07_payloads() -> tuple[dict[str, bytes], dict[str, Any], list[dict[str, Any]]]:
    validate_frozen_v06(LEGACY_PREVIEW_V06_DIR)
    contracts = _v07_contracts(
        hydrated_crosscheck=_v07_hydrated_crosscheck_available()
    )
    base_projects = _load_csv(LEGACY_PREVIEW_V06_DIR / "projects.csv", PROJECT_FIELDS)
    base_sites = _load_csv(LEGACY_PREVIEW_V06_DIR / "sites.csv", SITE_FIELDS)
    base_evidence = _load_csv(LEGACY_PREVIEW_V06_DIR / "evidence.csv", EVIDENCE_FIELDS)
    base_report = _load_json(LEGACY_PREVIEW_V06_DIR / "selection-report.json")
    delta = _v07_build_delta_rows(contracts)
    projects = sorted([*base_projects, *delta["projects"]], key=lambda row: row["project_stable_key"])
    sites = sorted([*base_sites, *delta["sites"]], key=lambda row: row["physical_site_stable_key"])
    evidence = sorted([*base_evidence, *delta["evidence"]], key=lambda row: row["evidence_id"])
    if (
        len(projects) != 36
        or len(sites) != 33
        or len(evidence) != 79
        or len({row["project_id"] for row in projects}) != 36
        or len({row["site_id"] for row in sites}) != 33
        or len({row["evidence_id"] for row in evidence}) != 79
    ):
        raise VerifiedConstructionCoreError("v0.7 cohort count or uniqueness differs")
    gates = _final_release_gates(sites, projects)
    gates["clean_clone_rebuild"] = {
        "passed": True,
        "reason": "all current-delta inputs and the frozen base artifact are tracked and hash-bound",
    }
    country_counts = dict(sorted(Counter(row["country"] for row in sites).items()))
    report = {
        **base_report,
        "format": "datacenter-atlas-verified-construction-core-selection-v7",
        "publishable_as_final": all(gate.get("passed", False) for gate in gates.values()),
        "reviewed_at": CURRENT_V07_REVIEW_DATE.isoformat(),
        "selected_project_count": 36,
        "selected_physical_site_count": 33,
        "official_boundary_project_count": 5,
        "reviewed_site_locator_project_count": 31,
        "non_selected_source_row_count": 495,
        "selection_first_failure_counts": V07_SELECTION_FIRST_FAILURE_COUNTS,
        "country_counts": country_counts,
        "final_release_gates": gates,
        "imagery_review_provenance": [
            *base_report["imagery_review_provenance"],
            *contracts["imagery"]["records"],
        ],
        "provenance_decisions": {
            key: [
                *base_report["provenance_decisions"][key],
                *contracts["provenance"][key],
            ]
            for key in ("workload_scope_bindings", "role_bindings", "excluded_source_roles")
        },
        "reviewed_overlay_queue": [
            *base_report["reviewed_overlay_queue"],
            *contracts["overlays"]["overlays"],
        ],
    }
    if (
        len(country_counts) != 23
        or sum(row["country_iso_a2"] != "US" for row in sites) != 27
        or sum(_is_official_boundary(row) for row in projects) != 5
        or sum(_is_reviewed_locator(row) for row in projects) != 31
        or sum(V07_SELECTION_FIRST_FAILURE_COUNTS.values()) != 531
    ):
        raise VerifiedConstructionCoreError("v0.7 expected accounting differs")
    geojson = _geojson(sites)
    geojson["name"] = "Data Center Atlas Verified Construction Core v0.7 preview"
    payloads = {
        "ATTRIBUTION.txt": _v07_attribution(evidence),
        "README.md": _v07_readme(report),
        "evidence.csv": _csv_bytes(evidence, EVIDENCE_FIELDS),
        "map.html": _v07_map_html(geojson, evidence),
        "projects.csv": _csv_bytes(projects, PROJECT_FIELDS),
        "schema.json": _json_bytes(_v07_schema()),
        "selection-report.json": _json_bytes(report),
        "sites.csv": _csv_bytes(sites, SITE_FIELDS),
        "sites.geojson": _json_bytes(geojson),
    }
    portable = _v07_portable_source_inputs(contracts)
    return payloads, report, portable


def _v07_manifest(
    payloads: Mapping[str, bytes],
    report: Mapping[str, Any],
    portable: Sequence[Mapping[str, Any]],
) -> dict[str, Any]:
    return {
        "format": "datacenter-atlas-verified-construction-core-preview-v7",
        "preview_id": CURRENT_V07_PREVIEW_ID,
        "release_status": "preview",
        "publishable_as_final": report["publishable_as_final"],
        "base_preview_id": "2026-08-20-preview-v0.6",
        "base_preview_manifest_sha256": LEGACY_PREVIEW_V06_MANIFEST_SHA256,
        "base_preview_commit": LEGACY_PREVIEW_V06_COMMIT,
        "source_release_id": SOURCE_RELEASE_ID,
        "source_release_manifest_path": "releases/2026-07-22-open-seed-v97/manifest.json",
        "source_release_manifest_sha256": "0a6f41f4239944df27f2ce70e81a089b91cec401f154bbae28412b27a4d00fdd",
        "definition_paths": {
            "imagery_reviews": "definitions/verified-construction-core-v0.7-imagery-reviews.json",
            "provenance": "definitions/verified-construction-core-v0.7-provenance.json",
            "reviewed_overlays": "definitions/verified-construction-core-reviewed-overlays-v5.json",
            "reviewed_sites": "definitions/verified-construction-core-v0.7-reviewed-sites.json",
        },
        "review_definition_sha256": V07_REVIEW_DEFINITION_SHA256,
        "imagery_review_definition_sha256": V07_IMAGERY_REVIEW_DEFINITION_SHA256,
        "provenance_definition_sha256": V07_PROVENANCE_DEFINITION_SHA256,
        "overlay_definition_sha256": V07_OVERLAY_DEFINITION_SHA256,
        "portable_source_inputs": list(portable),
        "reviewed_at": CURRENT_V07_REVIEW_DATE.isoformat(),
        "counts": {
            "physical_sites": 33,
            "projects": 36,
            "evidence": 79,
            "countries": 23,
            "non_us_sites": 27,
            "official_boundary_projects": 5,
            "reviewed_site_locator_projects": 31,
        },
        "files": {
            name: {"bytes": len(payload), "sha256": _sha256_bytes(payload)}
            for name, payload in sorted(payloads.items())
        },
    }


_build_v06_preview = build_preview
_validate_v06_preview_dispatch = validate_preview


def build_preview(output_dir: Path = CURRENT_V07_PREVIEW_DIR) -> dict[str, Any]:
    """Build the current v0.7 preview from tracked, hash-bound inputs."""
    output_dir = Path(output_dir)
    if output_dir.exists():
        raise VerifiedConstructionCoreError(f"refusing to overwrite {output_dir}")
    payloads, report, portable = _v07_payloads()
    manifest = _v07_manifest(payloads, report, portable)
    manifest_bytes = _json_bytes(manifest)
    complete = {
        **payloads,
        "manifest.json": manifest_bytes,
        "manifest.sha256": f"{_sha256_bytes(manifest_bytes)}  manifest.json\n".encode(),
    }
    output_dir.parent.mkdir(parents=True, exist_ok=True)
    stage = Path(tempfile.mkdtemp(prefix=f".{output_dir.name}.", dir=output_dir.parent))
    try:
        for name, payload in complete.items():
            with (stage / name).open("xb") as handle:
                handle.write(payload)
        os.replace(stage, output_dir)
    except Exception:
        shutil.rmtree(stage, ignore_errors=True)
        raise
    validate_preview(output_dir)
    return manifest


def _validate_v07_preview(path: Path) -> dict[str, Any]:
    path = Path(path)
    if path.is_symlink() or not path.is_dir():
        raise VerifiedConstructionCoreError("v0.7 preview directory differs")
    manifest_path = path / "manifest.json"
    checksum_path = path / "manifest.sha256"
    if (
        manifest_path.is_symlink()
        or not manifest_path.is_file()
        or checksum_path.is_symlink()
        or not checksum_path.is_file()
    ):
        raise VerifiedConstructionCoreError("v0.7 manifest trust root differs")
    manifest_bytes = manifest_path.read_bytes()
    if checksum_path.read_bytes() != f"{_sha256_bytes(manifest_bytes)}  manifest.json\n".encode():
        raise VerifiedConstructionCoreError("v0.7 manifest checksum differs")
    manifest = _load_json(manifest_path)
    files = manifest.get("files") if isinstance(manifest, dict) else None
    if not isinstance(files, dict):
        raise VerifiedConstructionCoreError("v0.7 manifest files differ")
    expected_inventory = set(files) | {"manifest.json", "manifest.sha256"}
    if {item.name for item in path.iterdir()} != expected_inventory:
        raise VerifiedConstructionCoreError("v0.7 preview inventory differs")
    for name, metadata in files.items():
        member = path / name
        if (
            Path(name).name != name
            or member.is_symlink()
            or not member.is_file()
            or member.stat().st_size != metadata.get("bytes")
            or _sha256_file(member) != metadata.get("sha256")
        ):
            raise VerifiedConstructionCoreError(f"v0.7 member differs: {name}")
    payloads, report, portable = _v07_payloads()
    expected_manifest = _v07_manifest(payloads, report, portable)
    if manifest != expected_manifest:
        raise VerifiedConstructionCoreError("v0.7 manifest semantics differ")
    for name, expected in payloads.items():
        if (path / name).read_bytes() != expected:
            raise VerifiedConstructionCoreError(f"v0.7 generated member differs: {name}")
    return manifest


def validate_preview(path: Path = CURRENT_V07_PREVIEW_DIR) -> dict[str, Any]:
    """Validate current v0.7 or dispatch an immutable v0.1-v0.6 preview."""
    path = Path(path)
    manifest_path = path / "manifest.json"
    if path.is_symlink() or not path.is_dir() or manifest_path.is_symlink() or not manifest_path.is_file():
        raise VerifiedConstructionCoreError("preview manifest trust root differs")
    manifest = _load_json(manifest_path)
    preview_id = manifest.get("preview_id") if isinstance(manifest, dict) else None
    if preview_id == CURRENT_V07_PREVIEW_ID:
        return _validate_v07_preview(path)
    if _frozen_preview_validator(preview_id) is not None:
        return validate_frozen_preview(path)
    raise VerifiedConstructionCoreError("preview id differs")


__all__ = [
    "CURRENT_V07_DEFINITION_PATHS",
    "CURRENT_V07_PREVIEW_DIR",
    "CURRENT_V07_PREVIEW_ID",
    "CURRENT_V07_REVIEW_DATE",
    "IMAGERY_REVIEW_DEFINITION",
    "LEGACY_PREVIEW_V01_DIR",
    "LEGACY_PREVIEW_V02_DIR",
    "LEGACY_PREVIEW_V03_DIR",
    "LEGACY_PREVIEW_V04_DIR",
    "LEGACY_PREVIEW_V05_COMMIT",
    "LEGACY_PREVIEW_V05_DIR",
    "LEGACY_PREVIEW_V06_COMMIT",
    "LEGACY_PREVIEW_V06_DIR",
    "LEGACY_PREVIEW_V06_MANIFEST_SHA256",
    "PREVIEW_DIR",
    "PreviewProfile",
    "PROVENANCE_DEFINITION",
    "VerifiedConstructionCoreError",
    "build_preview",
    "load_current_v07_profile",
    "validate_frozen_preview",
    "validate_frozen_v01",
    "validate_frozen_v02",
    "validate_frozen_v03",
    "validate_frozen_v04",
    "validate_frozen_v05",
    "validate_frozen_v06",
    "validate_preview",
]




_build_v07_preview = build_preview
_validate_v07_preview_dispatch = validate_preview

LEGACY_PREVIEW_V07_DIR = (
    ROOT / "verified_construction_core" / "2026-08-20-preview-v0.7"
)
LEGACY_PREVIEW_V07_MANIFEST_SHA256 = (
    "36d3f40d2a6ce8c4cf960adb680a3788a216552ca2e53c72fedc7aa3d5d97d6a"
)
LEGACY_PREVIEW_V07_COMMIT = "86bb4c589a0a6bf3e6d07a2a94c399b69fb016d6"
CURRENT_V08_PREVIEW_ID = "2026-08-20-preview-v0.8"
CURRENT_V08_PREVIEW_DIR = ROOT / "verified_construction_core" / CURRENT_V08_PREVIEW_ID
CURRENT_V08_REVIEW_DATE = date(2026, 8, 20)
V08_REVIEW_DEFINITION_SHA256 = (
    "8171064f679bc5c000079e88259ec7ec6379c267593c4f602ac3788ed9f8b4cb"
)
V08_IMAGERY_REVIEW_DEFINITION_SHA256 = (
    "69f2aa259fb7b5d03c2d0bda25847f74255fde182bd5b89e673bf532b9dd14b6"
)
V08_PROVENANCE_DEFINITION_SHA256 = (
    "a4a8fbda2d06a368f6030127d6f3e3eb73f22b95626e31c6719d88515f15fe48"
)
V08_OVERLAY_DEFINITION_SHA256 = (
    "e270e02e49f6d17313d32edce5d3348af9e301b407065861650fedcd7a9bdcc7"
)
V08_REVIEW_DEFINITION = (
    ROOT / "definitions" / "verified-construction-core-v0.8-reviewed-sites.json"
)
V08_IMAGERY_REVIEW_DEFINITION = (
    ROOT / "definitions" / "verified-construction-core-v0.8-imagery-reviews.json"
)
V08_PROVENANCE_DEFINITION = (
    ROOT / "definitions" / "verified-construction-core-v0.8-provenance.json"
)
V08_OVERLAY_DEFINITION = (
    ROOT / "definitions" / "verified-construction-core-reviewed-overlays-v6.json"
)
CURRENT_V08_DEFINITION_PATHS = (
    ("review_definition_sha256", V08_REVIEW_DEFINITION),
    ("imagery_review_definition_sha256", V08_IMAGERY_REVIEW_DEFINITION),
    ("provenance_definition_sha256", V08_PROVENANCE_DEFINITION),
    ("overlay_definition_sha256", V08_OVERLAY_DEFINITION),
)
V08_COUNTRY_ISO_A2 = {
    "Australia": "AU",
    "Austria": "AT",
    "Germany": "DE",
    "Iceland": "IS",
    "Türkiye": "TR",
    "United Kingdom": "GB",
}
V08_SELECTION_FIRST_FAILURE_COUNTS = {
    "entity_kind_not_project": 49,
    "not_in_reviewed_site_geometry_allowlist": 99,
    "selected": 44,
    "status_not_physical": 12,
    "status_outside_90_day_window": 327,
}
V08_OVERLAY_REQUIRED_FIELDS = frozenset(
    {
        "bridge_bytes",
        "bridge_decision",
        "bridge_id",
        "bridge_path",
        "bridge_schema",
        "bridge_sha256",
        "country",
        "decision",
        "decision_reason",
        "geometry_authority_class",
        "geometry_derivation",
        "geometry_entity",
        "geometry_entity_id",
        "geometry_entity_stable_key",
        "geometry_evidence_id",
        "geometry_evidence_key",
        "geometry_method",
        "geometry_scope_class",
        "geometry_source_kind",
        "geometry_target_entity_id",
        "geometry_target_entity_kind",
        "geometry_target_entity_stable_key",
        "geometry_use_scope",
        "horizontal_uncertainty_metres",
        "horizontal_uncertainty_unknown_reason",
        "official_boundary",
        "overlay_id",
        "physical_site_entity_id",
        "physical_site_stable_key",
        "portable_capture_binding",
        "portable_input_binding",
        "precision_scope",
        "project_to_campus_decision_basis",
        "project_to_campus_relationship_id",
        "project_to_campus_relationship_type",
        "rejected_claims",
        "reviewed_at",
        "source_input_bytes",
        "source_input_path",
        "source_input_sha256",
        "source_project_entity_id",
        "source_project_stable_key",
    }
)
V08_BRIDGE_PROFILES = {
    "curated:borealis-blonduos-data-center-campus:expansion-current-build": {
        "bridge_sha256": "096d5b905a62ff0254c98db90d2028245b6f3c5ffd45e509517fce60d916b295",
        "geometry_projection_sha256": "0288cb983ee8bd39898ab3a946debfbb9fc61ceccbd6edcff0e237a3a4ca7c63",
        "rights_sha256": "5f21ca2812fc601cfb7583208917a17c5e5cb171b7b0d13fca17e22b2a2e6312",
        "review_decision_sha256": "8f01aa13bc02f4bbae6ca35de5ff58e4a161409e48e01443cb97745d091a75c7",
        "claim_guardrails_sha256": "d62e7eec83bcb74b61474e0405600d9b69e1363ad27999660486220b050ae521",
        "imagery_posture_sha256": "cf3a5a5f1100ce1b6972d1ba83f645b119307fe1b6c5ce91ba50484d84816493",
        "source_sha256": "9f6a3292d2c3b7df30574bd27af1837f906a60bc2de30ca43b402619d2465680",
        "schema": "verified-construction-core-v0.8-global-v3-geometry-bridge-v1",
        "geometry_kind": "facility",
        "geometry_stable_key": "osm:way/1227173411",
        "target_kind": "campus",
        "target_stable_key": "curated:borealis-blonduos-data-center-campus",
        "geometry_evidence_id": "b24c0246-2488-55e4-9022-3609124fd8c8",
        "geometry_evidence_key": None,
        "geometry_source_kind": "openstreetmap",
        "longitude": -20.24300365,
        "latitude": 65.64727965,
    },
    "curated:cdc-laverton-melbourne-campus:current-build": {
        "bridge_sha256": "c6d0058184bdecdeb175e18b0c72c5c31ef9cee26ee735abc9d90239e2967f82",
        "geometry_projection_sha256": "88446ad2c9be9b91848504919b8ef5f88ca9279f43801b21af725762b540e5e8",
        "rights_sha256": "1f04323b5ab29f2f3ec80a9fd5a38db8a0adab5ce3c04a9004cfeed91347f7a5",
        "review_decision_sha256": "a047d1eb2c5fd315ae3e31af5d447493137d976ef4304283937177f33d2d4a7f",
        "claim_guardrails_sha256": "ca71d023349a5e2a85b8459d11df79e5885af0fe9491e5b5a9c19000996d261d",
        "imagery_posture_sha256": "cf3a5a5f1100ce1b6972d1ba83f645b119307fe1b6c5ce91ba50484d84816493",
        "source_sha256": "27f91ee149d0932fc73894a71364924dd00aed0640513f8df6af0717769309e6",
        "schema": "verified-construction-core-v0.8-global-v3-geometry-bridge-v1",
        "geometry_kind": "facility",
        "geometry_stable_key": "osm:way/263915226",
        "target_kind": "campus",
        "target_stable_key": "curated:cdc-laverton-melbourne-campus",
        "geometry_evidence_id": "e07bd94a-9734-5f78-a7b6-d69e757357c2",
        "geometry_evidence_key": None,
        "geometry_source_kind": "openstreetmap",
        "longitude": 144.7753871,
        "latitude": -37.8415726,
    },
    "curated:colt-frankfurt3-sossenheim-campus:frankfurt3-current-facility-build": {
        "bridge_sha256": "3b9c2f22dd3a52c4160d12904f373ab5d972eddd40f3023de8f174f24d85dcbe",
        "geometry_projection_sha256": "f49f56f20997d62fe9eee47a47d9b4fc050360d9af05baabba25aac7f4dde360",
        "rights_sha256": "f95d3ae2bfe2d41944d0918c2bf25193f541510abfed43949e79d6cdf18151b0",
        "review_decision_sha256": "9a26c9955ec05eb2551a4e2ce4bb3d0d61568144afd5f3fce900448d44848978",
        "claim_guardrails_sha256": "a183b9be6a3e91489e895e27964198c6bd3e9eada054cf2a2ea83117f81057cf",
        "imagery_posture_sha256": "cf3a5a5f1100ce1b6972d1ba83f645b119307fe1b6c5ce91ba50484d84816493",
        "source_sha256": "322e3929b2542204ed2f543fd9462f9f2cf9cd2afeb76b9448ece38a82295c9d",
        "schema": "verified-construction-core-v0.8-global-v3-geometry-bridge-v1",
        "geometry_kind": "building",
        "geometry_stable_key": "osm:way/1417621831",
        "target_kind": "project",
        "target_stable_key": "curated:colt-frankfurt3-sossenheim-campus:frankfurt3-current-facility-build",
        "geometry_evidence_id": "d0865f65-7e1e-59ab-989c-2d49ec5ce196",
        "geometry_evidence_key": None,
        "geometry_source_kind": "openstreetmap",
        "longitude": 8.5875645,
        "latitude": 50.124526200000005,
    },
    "curated:digital-realty-vienna-vie13-vie16-expansion:vie13-phase-1-current-build": {
        "bridge_sha256": "24f7609d2eee8cc8d6e14412cdb2db3681539026b97f2a1ae6c2827684b41af1",
        "geometry_projection_sha256": "a39f3314397a86182d1074f6b566bb72e1178bee11155fa9365ca080e3df5241",
        "rights_sha256": "b6ac3f0a471f7b905a41b04c5d8fe73d18a767b7eb85eacfe39834266337aec6",
        "review_decision_sha256": "8f2d1e83d8c4022d1f6e36cb891bfbaab6563a0fb5a1bf3988e164e459127406",
        "claim_guardrails_sha256": "5a25977ec0fb1578248bd483a0bea48cf9ad3aaeaab83efe664ee99d0739fae6",
        "imagery_posture_sha256": "c2529484418d5da1dbacfed6ad4cbde2da340618680dee45966f03818a101642",
        "source_sha256": "ddf94f3bbf02d4a4d9a4afeafc4c9eb5ce76f817f8d93b1f9033869801d14e4e",
        "schema": "verified-construction-core-v0.8-direct-first-party-geometry-bridge-v1",
        "geometry_kind": "project",
        "geometry_stable_key": "curated:digital-realty-vienna-vie13-vie16-expansion:vie13-phase-1-current-build",
        "target_kind": "project",
        "target_stable_key": "curated:digital-realty-vienna-vie13-vie16-expansion:vie13-phase-1-current-build",
        "geometry_evidence_id": "4bbf6d0c-4fcb-5dd0-9504-4322a80117b8",
        "geometry_evidence_key": "digital-realty-vie13-live-facility-page-point-captured-2026-08-20",
        "geometry_source_kind": "company_facility_page",
        "direct_source_url": "https://www.digitalrealty.com/data-centers/emea/vienna/vie13",
        "direct_source_publisher": "Digital Realty",
        "direct_source_license": "all-rights-reserved",
        "direct_source_retrieved_at": "2026-08-20T22:43:07Z",
        "direct_source_family": "digital_realty_facility_pages",
        "direct_source_kind": "company_facility_page",
        "longitude": 16.424095,
        "latitude": 48.270994,
    },
    "curated:enka-data-solutions-eds-ist-01-tuzla-data-center:initial-build": {
        "bridge_sha256": "038899bbb54371dbdc6a9769a568366dbb1a9b2b8b939e5dc6571850b32f142d",
        "geometry_projection_sha256": "8322e698ba3f72009c3f4ad5498043bbcc7ce26aab7ae45e265ee125664b1d89",
        "rights_sha256": "b6ac3f0a471f7b905a41b04c5d8fe73d18a767b7eb85eacfe39834266337aec6",
        "review_decision_sha256": "0c4d4c1a410203fa9df4af246be94079948a6ee90e017a9e8af3c6ec8f16275d",
        "claim_guardrails_sha256": "f2df10329d983cf2830c9d063714d15af22a192888554f55f4ca7a214491bb82",
        "imagery_posture_sha256": "c2529484418d5da1dbacfed6ad4cbde2da340618680dee45966f03818a101642",
        "source_sha256": "9d8f0924edde254b9a4aaa2b9e84afcfccecddb3ebd8c320aa16dcc95d31a4cf",
        "schema": "verified-construction-core-v0.8-direct-first-party-geometry-bridge-v1",
        "geometry_kind": "project",
        "geometry_stable_key": "curated:enka-data-solutions-eds-ist-01-tuzla-data-center:initial-build",
        "target_kind": "project",
        "target_stable_key": "curated:enka-data-solutions-eds-ist-01-tuzla-data-center:initial-build",
        "geometry_evidence_id": "959963fd-1d27-5927-a972-8a04fd97727e",
        "geometry_evidence_key": "enka-eds-ist01-tuzla-live-facility-page-point-captured-2026-08-20",
        "geometry_source_kind": "company_facility_page",
        "direct_source_url": "https://enkadatasolutions.com/locations/eds-ist-01-tuzla/",
        "direct_source_publisher": "ENKA Data Solutions",
        "direct_source_license": "all-rights-reserved",
        "direct_source_retrieved_at": "2026-08-20T22:43:43Z",
        "direct_source_family": "enka_data_solutions_facility_pages",
        "direct_source_kind": "company_facility_page",
        "longitude": 29.323671897992956,
        "latitude": 40.835152897406935,
    },
    "curated:equinix-mu4-munich-data-center:phase-3": {
        "bridge_sha256": "d7979e4127902b2ed5996426a1e0f3677e6cffb490a3367f4a16340ac8748faf",
        "geometry_projection_sha256": "1b9886331a90e7d8d8c374ad3643bbf9f3ca2867010483d7a31da9fec5f73e22",
        "rights_sha256": "c511f452a115e7fc1b69b80fe6069a1a3c4d79f1ed8e706d04f946f870b4b33b",
        "review_decision_sha256": "cb74f8f530c3078313cf922c4e258d4ff9d7d967f41ce9ba811935da745dabb3",
        "claim_guardrails_sha256": "ba78f4a33929db737d0256c07226f3cc8d9f9ea705f374dd96e78f7e0e05d2aa",
        "imagery_posture_sha256": "cf3a5a5f1100ce1b6972d1ba83f645b119307fe1b6c5ce91ba50484d84816493",
        "source_sha256": "87caf33997afbd8663486511815a141e6c7cb18d59ee5951648a21d3591a27d2",
        "schema": "verified-construction-core-v0.8-global-v3-geometry-bridge-v1",
        "geometry_kind": "facility",
        "geometry_stable_key": "osm:way/899514480",
        "target_kind": "campus",
        "target_stable_key": "curated:equinix-mu4-munich-data-center",
        "geometry_evidence_id": "efe5b5c9-7394-5f0c-951b-051197168c49",
        "geometry_evidence_key": None,
        "geometry_source_kind": "openstreetmap",
        "longitude": 11.6824555,
        "latitude": 48.14701805,
    },
    "curated:macquarie-ic3-super-west-facility:phase-1-build": {
        "bridge_sha256": "6ba2c6a538d4d834adcceac45a080cda70391f0fcceb5b5d3420d37812caa540",
        "geometry_projection_sha256": "f4e560883e133af90207a95d436fbffb95eeec71f053eddad76844f11c44678b",
        "rights_sha256": "52c4af65d27973d84dd439dc5259fd6446edba19b55e4a0ac68c3822da5d1bf3",
        "review_decision_sha256": "827ab2b061ed3a187b041cbd266a354046515ff791e6f0370f58aacc9e649a0d",
        "claim_guardrails_sha256": "6b01772208a5e1c7e4b6bd59084d39f31683e6067a30eae38115fc80bc61e2f9",
        "imagery_posture_sha256": "cf3a5a5f1100ce1b6972d1ba83f645b119307fe1b6c5ce91ba50484d84816493",
        "source_sha256": "9fe5625d1cbb2d59dbeb8d329ab1b79fb65a7e43346161f2c92901076cc7cb4f",
        "schema": "verified-construction-core-v0.8-global-v3-geometry-bridge-v1",
        "geometry_kind": "project",
        "geometry_stable_key": "osm:way/1298979790:development-project",
        "target_kind": "campus",
        "target_stable_key": "curated:macquarie-ic3-super-west-facility",
        "geometry_evidence_id": "7b2675bd-76c0-54b0-82d1-3f5d6cb328b2",
        "geometry_evidence_key": None,
        "geometry_source_kind": "openstreetmap",
        "longitude": 151.12696035,
        "latitude": -33.780962349999996,
    },
    "curated:pure-dc-brent-cross-lon01-campus:b2-composite-build": {
        "bridge_sha256": "6cb44cf1473741da182fbd348eef4ecef6ccf53f37467cbc32728a7c03143858",
        "geometry_projection_sha256": "c27131cdd11fff31aab83f5192c85c2d126cdf177d22ab5ff6e7af4b70cc4c1a",
        "rights_sha256": "149978275e4abba2f03ca7a33e296793cb3408edd02e0c6be05649193cf1782f",
        "review_decision_sha256": "73463377039f09cd0707aebbac502670dc294c838e7f3f50e318008bee1f323d",
        "claim_guardrails_sha256": "c99c773cb4444e129918ab57cc09d1d39fd15379754dc17094045bba08c2eadd",
        "imagery_posture_sha256": "cf3a5a5f1100ce1b6972d1ba83f645b119307fe1b6c5ce91ba50484d84816493",
        "source_sha256": "792dc6b6db721fd7efd4bf00493b6546264acfe8a563b6b4ad222be282834c1d",
        "schema": "verified-construction-core-v0.8-global-v3-geometry-bridge-v1",
        "geometry_kind": "building",
        "geometry_stable_key": "osm:way/38246899",
        "target_kind": "campus",
        "target_stable_key": "curated:pure-dc-brent-cross-lon01-campus",
        "geometry_evidence_id": "ee256a2f-4e9b-5224-a6ce-e4f95c4a11ed",
        "geometry_evidence_key": None,
        "geometry_source_kind": "openstreetmap",
        "longitude": -0.23718785,
        "latitude": 51.570622900000004,
    },
}
V08_SOURCE_PROFILES = {
    "curated:borealis-blonduos-data-center-campus:expansion-current-build": {
        "v97_rows_sha256": "d7a4932a72f9b1bc80fee09cfb77a58dcd23bc18270b2e397c5991f98ae516f4",
        "topology_sha256": "11fde75491f34af08e0be60d93d59c74dd586879cd7a2ace4c16c70b3ab9e70f",
        "used_evidence_ids": ("bd494c0b-3936-5cc0-844a-67cd0db22812",),
        "used_evidence_sha256": "f34d4dfb3c8c2270d094b0804a825e40c964966bbe1444af75b39091eb7b592a",
    },
    "curated:cdc-laverton-melbourne-campus:current-build": {
        "v97_rows_sha256": "2bdd8625f73161025b25ab956b839bb340757c897bac88474c10b0b86a36898e",
        "topology_sha256": "4a5f52dc25a6764a3ff12a6bcb52dbf3e260dbcd07c18f82d28fcd1904ad5b11",
        "used_evidence_ids": ("d05112cf-0117-5f1a-9b7a-4087ec4f5c8f",),
        "used_evidence_sha256": "2143a3b9b7822f63732972f36e0998492d11e0103119eb421d7b4a2c8b69b48b",
    },
    "curated:colt-frankfurt3-sossenheim-campus:frankfurt3-current-facility-build": {
        "v97_rows_sha256": "a1fb22af9f00a6a6073bc66a7e7179f0cf9aa5898ffa59be89259f6578220759",
        "topology_sha256": "961b6c9e06b59c0d24789c06744f052c281a3756cf1de677a836793e736a8dcf",
        "used_evidence_ids": ("bf2135c6-1aa5-58e8-a318-7e67915aa1bb",),
        "used_evidence_sha256": "dc2b8c404e3e1d2e91ec700c3540869f98ed45b6d6a2b04070e13f28f996122a",
    },
    "curated:digital-realty-vienna-vie13-vie16-expansion:vie13-phase-1-current-build": {
        "v97_rows_sha256": "c78e9e3efd52d49c87563e422042a81865ad0d9d97ed6a0abd77bb0482379f39",
        "topology_sha256": "0bf10f26fc56ec6b7caf6512a5ada28f5156fc7148434a555955545dd9db17e1",
        "used_evidence_ids": (
            "7305379e-4152-58ec-9152-c2d7af3d39c1",
            "9ef151e3-b5f8-5421-b9df-f5ac0bfca4a6",
        ),
        "used_evidence_sha256": "ea0d78a9b3d95606883cc5c09bb4204013ae9f07bcbdfe08b6f4f74dda20a688",
    },
    "curated:enka-data-solutions-eds-ist-01-tuzla-data-center:initial-build": {
        "v97_rows_sha256": "b6808debfde6f9e4e72d37fde65544481672a714b95ba53ef16f365d9ae15e2c",
        "topology_sha256": "cf9d533494cf5d786a7b1555f3dfc40568a58ca284f5f15f9f6d79ba1f22981c",
        "used_evidence_ids": (
            "0dd3dc39-1457-5bc3-b7d2-7ff44183aed3",
            "6b7130b5-f5f5-50c8-9689-6b8e348e0015",
            "a63792de-87ff-5a78-8a99-e53260b301f1",
        ),
        "used_evidence_sha256": "0a7772feb89273c22be7b4786aa85c01a74a67089b4e4b79ad8771c4ec16053d",
    },
    "curated:equinix-mu4-munich-data-center:phase-3": {
        "v97_rows_sha256": "3075f7428ea79979de7220a1c8816b60e6d675b457e942e389af8045fa844330",
        "topology_sha256": "5d9ee2ab8bca06a70390215ce8345591342cce5b0356db59b475fc2a28c21ca5",
        "used_evidence_ids": ("4302b7fb-1bf4-5dc7-abf5-9ba24f4556fa",),
        "used_evidence_sha256": "82d9f4c9760d2cbb45a87ae0d823a6d91098a50bf47e751d30c2a15306165552",
    },
    "curated:macquarie-ic3-super-west-facility:phase-1-build": {
        "v97_rows_sha256": "f13e64b833052e1b5e5693a858f56878aaa1e8e087a85e0f6c0d132df7b0344f",
        "topology_sha256": "af8b04506c7140f2fcde1291829b7229a6fa9f810eb35c96d042f109124e5d67",
        "used_evidence_ids": (
            "0ba37f72-6f9b-5d8f-8b8e-8b66e03dbb11",
            "10cad266-3d5e-591d-a1c4-8ded1e082c7b",
            "654559c0-3ba5-519b-8215-80eaaf854f5a",
        ),
        "used_evidence_sha256": "e9aa5225cfd73aa886e85c01d380ae325e9018e4f5763fabfaa0663ab6e20a9e",
    },
    "curated:pure-dc-brent-cross-lon01-campus:b2-composite-build": {
        "v97_rows_sha256": "2ca481bb4be7c2016cf8089ad3f1620bb74673c6264405eceb4000213a6942f8",
        "topology_sha256": "3d9e0c4eceeaeb0aa42bfe5b4e21c0de6d67bc55cc091059d0e86e49e294047f",
        "used_evidence_ids": ("a9d0173c-a00c-5936-b2d2-493a61fc0d53",),
        "used_evidence_sha256": "eb0eed606135148cc774dc50b45524354fa7c56939600172a82e6e842ba2ea29",
    },
}
V08_EVIDENCE_IDS = frozenset(
    {
        "0ba37f72-6f9b-5d8f-8b8e-8b66e03dbb11",
        "0dd3dc39-1457-5bc3-b7d2-7ff44183aed3",
        "10cad266-3d5e-591d-a1c4-8ded1e082c7b",
        "4302b7fb-1bf4-5dc7-abf5-9ba24f4556fa",
        "4bbf6d0c-4fcb-5dd0-9504-4322a80117b8",
        "654559c0-3ba5-519b-8215-80eaaf854f5a",
        "6b7130b5-f5f5-50c8-9689-6b8e348e0015",
        "7305379e-4152-58ec-9152-c2d7af3d39c1",
        "7b2675bd-76c0-54b0-82d1-3f5d6cb328b2",
        "959963fd-1d27-5927-a972-8a04fd97727e",
        "9ef151e3-b5f8-5421-b9df-f5ac0bfca4a6",
        "a63792de-87ff-5a78-8a99-e53260b301f1",
        "a9d0173c-a00c-5936-b2d2-493a61fc0d53",
        "b24c0246-2488-55e4-9022-3609124fd8c8",
        "bd494c0b-3936-5cc0-844a-67cd0db22812",
        "bf2135c6-1aa5-58e8-a318-7e67915aa1bb",
        "d05112cf-0117-5f1a-9b7a-4087ec4f5c8f",
        "d0865f65-7e1e-59ab-989c-2d49ec5ce196",
        "e07bd94a-9734-5f78-a7b6-d69e757357c2",
        "ee256a2f-4e9b-5224-a6ce-e4f95c4a11ed",
        "efe5b5c9-7394-5f0c-951b-051197168c49",
    }
)


def load_current_v08_profile(
    definition_sha256: Mapping[str, str],
) -> PreviewProfile:
    """Load v0.8 only after all exact definition hashes are supplied."""
    expected_fields = {field for field, _ in CURRENT_V08_DEFINITION_PATHS}
    if set(definition_sha256) != expected_fields:
        raise VerifiedConstructionCoreError(
            "current v0.8 definition pins are incomplete"
        )
    pins: list[tuple[str, Path, str]] = []
    for field, path in CURRENT_V08_DEFINITION_PATHS:
        expected_sha256 = definition_sha256[field]
        if re.fullmatch(r"[0-9a-f]{64}", expected_sha256) is None:
            raise VerifiedConstructionCoreError(f"current v0.8 {field} pin is invalid")
        if path.is_symlink() or not path.is_file() or _sha256_file(path) != expected_sha256:
            raise VerifiedConstructionCoreError(
                f"current v0.8 {field} source hash differs"
            )
        pins.append((field, path, expected_sha256))
    return PreviewProfile(
        preview_id=CURRENT_V08_PREVIEW_ID,
        preview_dir=CURRENT_V08_PREVIEW_DIR,
        reviewed_at=CURRENT_V08_REVIEW_DATE,
        base_preview_id="2026-08-20-preview-v0.7",
        base_preview_dir=LEGACY_PREVIEW_V07_DIR,
        base_manifest_sha256=LEGACY_PREVIEW_V07_MANIFEST_SHA256,
        base_commit=LEGACY_PREVIEW_V07_COMMIT,
        definition_pins=tuple(pins),
    )


def validate_frozen_v07(
    path: Path = LEGACY_PREVIEW_V07_DIR,
) -> dict[str, Any]:
    """Validate byte-frozen v0.7 using only its immutable inventory pins."""
    return _validate_frozen_preview_inventory(
        path,
        preview_id="2026-08-20-preview-v0.7",
        manifest_sha256=LEGACY_PREVIEW_V07_MANIFEST_SHA256,
        version_label="v0.7",
    )


_FROZEN_PREVIEW_VALIDATORS = (
    *_FROZEN_PREVIEW_VALIDATORS,
    ("2026-08-20-preview-v0.7", validate_frozen_v07),
)


def _v08_definition_paths() -> dict[str, tuple[Path, str]]:
    return {
        "review_definition_sha256": (
            V08_REVIEW_DEFINITION,
            V08_REVIEW_DEFINITION_SHA256,
        ),
        "imagery_review_definition_sha256": (
            V08_IMAGERY_REVIEW_DEFINITION,
            V08_IMAGERY_REVIEW_DEFINITION_SHA256,
        ),
        "provenance_definition_sha256": (
            V08_PROVENANCE_DEFINITION,
            V08_PROVENANCE_DEFINITION_SHA256,
        ),
        "overlay_definition_sha256": (
            V08_OVERLAY_DEFINITION,
            V08_OVERLAY_DEFINITION_SHA256,
        ),
    }


def _validate_v08_definition_pins() -> None:
    for field, (path, expected) in _v08_definition_paths().items():
        if path.is_symlink() or not path.is_file() or _sha256_file(path) != expected:
            raise VerifiedConstructionCoreError(
                f"current v0.8 {field} source hash differs"
            )


def _v08_hydrated_crosscheck_available() -> bool:
    required = (
        ROOT / "releases/2026-07-22-open-seed-v97/construction_pipeline.csv",
        ROOT / "releases/2026-07-22-open-seed-v97/entities.csv",
        ROOT / "releases/2026-07-22-open-seed-v97/evidence.csv",
        ROOT / "releases/2026-07-18-global-open-v3/entities.csv",
        ROOT / "releases/2026-07-18-global-open-v3/evidence.csv",
        ROOT
        / "exact_identity_decisions/2026-07-22-public-open-v14/component-members.csv",
        ROOT
        / "exact_identity_decisions/2026-07-22-public-open-v14/relationships.csv",
    )
    present = [path.is_file() for path in required]
    if any(present) and not all(present):
        raise VerifiedConstructionCoreError(
            "v0.8 hydrated cross-check inputs are only partially present"
        )
    return all(present)


def _v08_source_evidence(source: Mapping[str, Any]) -> dict[str, dict[str, Any]]:
    return _v07_source_evidence(source)


def _v08_normalized_roles(entity: Mapping[str, Any]) -> dict[str, Any]:
    source_roles = entity.get("roles") or {}
    if not isinstance(source_roles, dict):
        raise VerifiedConstructionCoreError("v0.8 source roles differ")
    values: dict[str, list[str]] = {}
    for role in ("owner", "operator", "users", "tenants", "customers"):
        aliases = (role, role[:-1]) if role.endswith("s") else (role,)
        parties: list[str] = []
        for alias in aliases:
            observed = source_roles.get(alias, [])
            if observed is None:
                continue
            if isinstance(observed, str):
                observed = [observed]
            if not isinstance(observed, list) or any(
                not isinstance(party, str) or not party.strip() for party in observed
            ):
                raise VerifiedConstructionCoreError("v0.8 source roles differ")
            parties.extend(party.strip() for party in observed)
        if len(parties) != len(set(parties)):
            raise VerifiedConstructionCoreError("v0.8 source roles are not unique")
        values[role] = sorted(parties)
    return {
        "owner": "; ".join(values["owner"]) or None,
        "operator": "; ".join(values["operator"]) or None,
        "users": values["users"],
        "tenants": values["tenants"],
        "customers": values["customers"],
    }


def _v08_source_entity_projection(
    source: Mapping[str, Any],
    evidence_by_key: Mapping[str, Mapping[str, Any]],
    *,
    entity_kind: str,
) -> dict[str, Any]:
    entity = source.get(entity_kind)
    if not isinstance(entity, dict):
        raise VerifiedConstructionCoreError(
            f"v0.8 {entity_kind} source record differs"
        )
    models = [
        row
        for row in source.get("operating_models", [])
        if isinstance(row, dict) and row.get("entity") == entity_kind
    ]
    if len(models) > 1:
        raise VerifiedConstructionCoreError(
            f"v0.8 {entity_kind} operating model differs"
        )
    model = models[0] if models else None
    model_evidence_id = None
    if model is not None:
        evidence = evidence_by_key.get(model.get("evidence_key"))
        if evidence is None:
            raise VerifiedConstructionCoreError(
                f"v0.8 {entity_kind} operating-model evidence differs"
            )
        model_evidence_id = _official_evidence_id(evidence)
    result = {
        "stable_key": entity["stable_key"],
        "entity_id": atlas_stable_id("entity", entity["stable_key"], entity_kind),
        "name": entity["name"],
        "country": entity["country"],
        "address": entity["address"],
        "operating_model": model["value"] if model else None,
        "operating_model_confidence": model["confidence"] if model else None,
        "operating_model_evidence_id": model_evidence_id,
        "workloads_json": _projected_bridge_observations(
            source,
            evidence_by_key,
            collection="workloads",
            entity_kind=entity_kind,
        ),
        "capacity_estimates_json": _projected_bridge_observations(
            source,
            evidence_by_key,
            collection="capacities",
            entity_kind=entity_kind,
        ),
    }
    if entity_kind == "project":
        lifecycle = [
            row
            for row in source.get("lifecycle", [])
            if isinstance(row, dict) and row.get("entity") == "project"
        ]
        if not lifecycle:
            raise VerifiedConstructionCoreError("v0.8 lifecycle differs")
        latest = max(lifecycle, key=lambda row: row.get("as_of_date", ""))
        status_evidence = evidence_by_key.get(latest.get("evidence_key"))
        if status_evidence is None:
            raise VerifiedConstructionCoreError("v0.8 status evidence differs")
        result.update(
            {
                "status": latest["value"],
                "status_as_of": latest["as_of_date"],
                "status_age_days_at_review": (
                    CURRENT_V08_REVIEW_DATE
                    - _calendar_date(latest["as_of_date"], "v0.8 status")
                ).days,
                "status_method": latest["method"],
                "status_confidence": latest["confidence"],
                "status_evidence_id": _official_evidence_id(status_evidence),
            }
        )
    return result


def _v08_construction_row_bindings(
    construction: Mapping[str, Any],
) -> Mapping[str, Any]:
    embedded = construction.get("release_rows")
    if embedded is not None:
        if not isinstance(embedded, dict):
            raise VerifiedConstructionCoreError("v0.8 release rows differ")
        return embedded
    return {
        "project": construction["project"].get("source_row"),
        "campus": construction["campus"].get("source_row"),
        "status_evidence": construction["status_evidence"].get("source_row"),
    }


def _v08_csv_roles(row: Mapping[str, str]) -> dict[str, Any]:
    roles = _v07_source_roles(row)
    return {
        "owner": "; ".join(roles["owner"]) or None,
        "operator": "; ".join(roles["operator"]) or None,
        "users": roles["user"],
        "tenants": roles["tenant"],
        "customers": roles["customer"],
    }


def _v08_csv_entity_projection(
    row: Mapping[str, str], *, entity_kind: str
) -> dict[str, Any]:
    if (
        row.get("entity_kind") != entity_kind
        or row.get("entity_id")
        != atlas_stable_id("entity", row.get("stable_key", ""), entity_kind)
    ):
        raise VerifiedConstructionCoreError(
            f"v0.8 v97 {entity_kind} identity differs"
        )
    workloads = _csv_json(
        row["workloads_json"], label=f"v0.8 v97 {entity_kind} workloads"
    )
    capacities = _csv_json(
        row["capacity_estimates_json"],
        label=f"v0.8 v97 {entity_kind} capacities",
    )
    if not isinstance(workloads, list) or not isinstance(capacities, list):
        raise VerifiedConstructionCoreError(
            f"v0.8 v97 {entity_kind} observations differ"
        )
    for observation in workloads:
        _validate_source_workload_observation(observation)
    for observation in capacities:
        _validate_typed_observation(
            observation, POWER_METRICS | ENERGY_METRICS | EFFICIENCY_METRICS
        )
    result = {
        "stable_key": row["stable_key"],
        "entity_id": row["entity_id"],
        "name": row["name"],
        "country": row["country"],
        "address": row["address"],
        "operating_model": _nullable_csv_value(row["operating_model"]),
        "operating_model_confidence": _csv_float(
            row["operating_model_confidence"],
            label=f"v0.8 v97 {entity_kind} operating model",
        ),
        "operating_model_evidence_id": _nullable_csv_value(
            row["operating_model_evidence_id"]
        ),
        "workloads_json": workloads,
        "capacity_estimates_json": capacities,
        "normalized_roles": _v08_csv_roles(row),
    }
    if entity_kind == "project":
        status_as_of = _nullable_csv_value(row["status_as_of"])
        if status_as_of is None:
            raise VerifiedConstructionCoreError("v0.8 v97 project status differs")
        result.update(
            {
                "status": _nullable_csv_value(row["status"]),
                "status_as_of": status_as_of,
                "status_age_days_at_review": (
                    CURRENT_V08_REVIEW_DATE
                    - _calendar_date(status_as_of, "v0.8 v97 status")
                ).days,
                "status_confidence": _csv_float(
                    row["status_confidence"], label="v0.8 v97 status"
                ),
                "status_method": _nullable_csv_value(row["status_method"]),
                "status_evidence_id": _nullable_csv_value(
                    row["status_evidence_id"]
                ),
            }
        )
        if any(result[field] is None for field in ("status", "status_method", "status_evidence_id")):
            raise VerifiedConstructionCoreError("v0.8 v97 project status differs")
    elif any(
        row[field]
        for field in (
            "status",
            "status_as_of",
            "status_confidence",
            "status_method",
            "status_evidence_id",
        )
    ):
        raise VerifiedConstructionCoreError("v0.8 v97 campus status differs")
    return result


def _v08_actual_entity_projection(
    entity: Mapping[str, Any], *, entity_kind: str
) -> dict[str, Any]:
    result = {
        field: entity.get(field)
        for field in (
            "stable_key",
            "entity_id",
            "name",
            "country",
            "address",
            "operating_model",
            "operating_model_confidence",
            "operating_model_evidence_id",
            "workloads_json",
            "capacity_estimates_json",
        )
    }
    result["normalized_roles"] = entity.get(
        "normalized_roles",
        {
            field: entity.get(field)
            for field in ("owner", "operator", "users", "tenants", "customers")
        },
    )
    if entity_kind == "project":
        result.update(
            {
                field: entity.get(field)
                for field in (
                    "status",
                    "status_as_of",
                    "status_age_days_at_review",
                    "status_confidence",
                    "status_method",
                    "status_evidence_id",
                )
            }
        )
    return result


def _v08_validate_embedded_construction_rows(
    bridge: Mapping[str, Any], *, hydrated_crosscheck: bool
) -> dict[str, Any]:
    construction = bridge["construction_source"]
    release = construction["release"]
    embedded = construction.get("release_rows")
    if embedded is not None:
        _validate_v06_embedded_release_rows(
            embedded,
            release["members"],
            hydrated_crosscheck=hydrated_crosscheck,
        )
        if not {
            "construction_pipeline_project",
            "entities_project",
            "entities_campus",
            "evidence_snapshot",
        } <= set(embedded):
            raise VerifiedConstructionCoreError("v0.8 release-row inventory differs")
        project_row = _embedded_csv_source_row(
            embedded["construction_pipeline_project"]["source_row"],
            SOURCE_ENTITY_FIELDS,
            label="v0.8 construction pipeline project",
        )
        entities_project_row = _embedded_csv_source_row(
            embedded["entities_project"]["source_row"],
            SOURCE_ENTITY_FIELDS,
            label="v0.8 entities project",
        )
        if project_row != entities_project_row:
            raise VerifiedConstructionCoreError(
                "v0.8 v97 project rows differ"
            )
        campus_row = _embedded_csv_source_row(
            embedded["entities_campus"]["source_row"],
            SOURCE_ENTITY_FIELDS,
            label="v0.8 entities campus",
        )
        evidence_rows: dict[str, dict[str, str]] = {}
        for label, binding in embedded.items():
            if binding.get("member") != "evidence.csv":
                continue
            row = _embedded_csv_source_row(
                binding["source_row"],
                GLOBAL_GEOMETRY_EVIDENCE_FIELDS,
                label=f"v0.8 {label}",
            )
            evidence_id = row["evidence_id"]
            if evidence_id in evidence_rows:
                raise VerifiedConstructionCoreError(
                    "v0.8 embedded evidence rows are not unique"
                )
            evidence_rows[evidence_id] = row
    else:
        entity_rows: dict[str, dict[str, str]] = {}
        for label, record, member, kind in (
            (
                "project",
                construction["project"],
                "construction_pipeline.csv",
                "project",
            ),
            ("campus", construction["campus"], "entities.csv", "campus"),
        ):
            row = _embedded_csv_source_row(
                record.get("source_row"),
                SOURCE_ENTITY_FIELDS,
                label=f"v0.8 {label}",
            )
            if (
                row["stable_key"] != record["stable_key"]
                or row["entity_id"] != record["entity_id"]
                or row["entity_kind"] != kind
            ):
                raise VerifiedConstructionCoreError(
                    f"v0.8 {label} source-row projection differs"
                )
            entity_rows[kind] = row
            if hydrated_crosscheck:
                metadata = release["members"][member]
                payload_path = _repository_input(
                    metadata["path"], metadata["sha256"], f"v0.8 {member}"
                )
                _validate_hydrated_source_row(
                    payload_path,
                    record["source_row"],
                    SOURCE_ENTITY_FIELDS,
                    label=f"v0.8 {label}",
                )
        project_row = entity_rows["project"]
        campus_row = entity_rows["campus"]
        status = construction["status_evidence"]
        status_row = _embedded_csv_source_row(
            status.get("source_row"),
            GLOBAL_GEOMETRY_EVIDENCE_FIELDS,
            label="v0.8 status",
        )
        if status_row["evidence_id"] != status["evidence_id"]:
            raise VerifiedConstructionCoreError(
                "v0.8 status source-row projection differs"
            )
        evidence_rows = {status_row["evidence_id"]: status_row}
        if hydrated_crosscheck:
            metadata = release["members"]["evidence.csv"]
            payload_path = _repository_input(
                metadata["path"], metadata["sha256"], "v0.8 evidence.csv"
            )
            _validate_hydrated_source_row(
                payload_path,
                status["source_row"],
                GLOBAL_GEOMETRY_EVIDENCE_FIELDS,
                label="v0.8 status",
            )
    status_id = construction["status_evidence"].get("evidence_id")
    if status_id not in evidence_rows:
        raise VerifiedConstructionCoreError("v0.8 embedded status evidence differs")
    return {
        "project": project_row,
        "campus": campus_row,
        "evidence": evidence_rows,
    }


def _v08_hydrated_evidence_rows(
    release: Mapping[str, Any], evidence_ids: Sequence[str]
) -> dict[str, dict[str, str]]:
    metadata = release["members"]["evidence.csv"]
    path = _repository_input(
        metadata["path"], metadata["sha256"], "v0.8 evidence.csv"
    )
    if path.stat().st_size != metadata["bytes"]:
        raise VerifiedConstructionCoreError("v0.8 hydrated evidence differs")
    expected = set(evidence_ids)
    result: dict[str, dict[str, str]] = {}
    with path.open(newline="", encoding="utf-8") as handle:
        reader = csv.DictReader(handle)
        if reader.fieldnames != list(GLOBAL_GEOMETRY_EVIDENCE_FIELDS):
            raise VerifiedConstructionCoreError(
                "v0.8 hydrated evidence header differs"
            )
        for row in reader:
            evidence_id = row["evidence_id"]
            if evidence_id not in expected:
                continue
            if evidence_id in result:
                raise VerifiedConstructionCoreError(
                    "v0.8 hydrated evidence is not unique"
                )
            result[evidence_id] = row
    if set(result) != expected:
        raise VerifiedConstructionCoreError("v0.8 hydrated evidence row differs")
    return result


def _v08_validate_construction_source(
    bridge: Mapping[str, Any],
    acceptance: Mapping[str, Any],
    *,
    hydrated_crosscheck: bool,
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    project_key = acceptance["project_stable_key"]
    source_profile = V08_SOURCE_PROFILES.get(project_key)
    if source_profile is None:
        raise VerifiedConstructionCoreError("v0.8 source profile differs")
    construction = bridge.get("construction_source")
    if not isinstance(construction, dict) or set(construction) not in {
        frozenset(
            {"input", "release", "project", "campus", "project_to_campus", "status_evidence"}
        ),
        frozenset(
            {
                "input",
                "release",
                "release_rows",
                "project",
                "campus",
                "project_to_campus",
                "status_evidence",
            }
        ),
    }:
        raise VerifiedConstructionCoreError("v0.8 construction source differs")
    if (
        _sha256_bytes(_json_bytes(_v08_construction_row_bindings(construction)))
        != source_profile["v97_rows_sha256"]
        or _sha256_bytes(_json_bytes(construction.get("project_to_campus")))
        != source_profile["topology_sha256"]
    ):
        raise VerifiedConstructionCoreError("v0.8 immutable source profile differs")
    expected_input = {
        "path": acceptance["source_input_path"],
        "bytes": acceptance["source_input_bytes"],
        "sha256": acceptance["source_input_sha256"],
    }
    if (
        construction.get("input") != expected_input
        or acceptance.get("portable_input_binding") != expected_input
    ):
        raise VerifiedConstructionCoreError("v0.8 source input binding differs")
    source_path = _repository_input(
        expected_input["path"], expected_input["sha256"], "v0.8 source input"
    )
    if source_path.stat().st_size != expected_input["bytes"]:
        raise VerifiedConstructionCoreError("v0.8 source input byte count differs")
    source = _load_json(source_path)
    if not isinstance(source, dict):
        raise VerifiedConstructionCoreError("v0.8 source record differs")
    source_evidence = source.get("evidence")
    if not isinstance(source_evidence, list) or not source_evidence:
        raise VerifiedConstructionCoreError("v0.8 source evidence differs")
    evidence_by_key = {
        row["key"]: row
        for row in source_evidence
        if isinstance(row, dict) and isinstance(row.get("key"), str)
    }
    if len(evidence_by_key) != len(source_evidence):
        raise VerifiedConstructionCoreError("v0.8 source evidence differs")
    expected_project = _v08_source_entity_projection(
        source, evidence_by_key, entity_kind="project"
    )
    expected_campus = _v08_source_entity_projection(
        source, evidence_by_key, entity_kind="campus"
    )
    project = construction.get("project")
    campus = construction.get("campus")
    if not isinstance(project, dict) or not isinstance(campus, dict):
        raise VerifiedConstructionCoreError("v0.8 construction entities differ")
    for actual, expected, source_entity, label in (
        (project, expected_project, source["project"], "project"),
        (campus, expected_campus, source["campus"], "campus"),
    ):
        for field, value in expected.items():
            if actual.get(field) != value:
                raise VerifiedConstructionCoreError(
                    f"v0.8 construction {label} projection differs"
                )
        roles = _v08_normalized_roles(source_entity)
        if "normalized_roles" in actual:
            if actual["normalized_roles"] != roles:
                raise VerifiedConstructionCoreError(
                    f"v0.8 construction {label} roles differ"
                )
        elif any(actual.get(field) != value for field, value in roles.items()):
            raise VerifiedConstructionCoreError(
                f"v0.8 construction {label} roles differ"
            )
    if (
        project.get("stable_key") != acceptance["project_stable_key"]
        or project.get("entity_id") != acceptance["project_entity_id"]
        or campus.get("stable_key") != acceptance["physical_site_stable_key"]
        or campus.get("entity_id") != acceptance["physical_site_entity_id"]
        or project.get("country") != acceptance["country"]
        or campus.get("country") != acceptance["country"]
    ):
        raise VerifiedConstructionCoreError(
            "v0.8 publication target projection differs"
        )
    if "status" in campus and any(
        campus.get(field) is not None
        for field in (
            "status",
            "status_as_of",
            "status_age_days_at_review",
            "status_method",
            "status_confidence",
            "status_evidence_id",
        )
    ):
        raise VerifiedConstructionCoreError("v0.8 campus status leakage differs")
    status = construction.get("status_evidence")
    evidence_pool = _v08_source_evidence(source)
    status_id = expected_project["status_evidence_id"]
    if not isinstance(status, dict) or status.get("evidence_id") != status_id:
        raise VerifiedConstructionCoreError("v0.8 status evidence differs")
    expected_status = evidence_pool.get(status_id)
    if expected_status is None or any(
        ("" if status.get(field) is None else status.get(field))
        != expected_status[field]
        for field in GLOBAL_GEOMETRY_EVIDENCE_FIELDS
    ):
        raise VerifiedConstructionCoreError("v0.8 status evidence projection differs")
    release = construction.get("release")
    if (
        not isinstance(release, dict)
        or release.get("release_id") != SOURCE_RELEASE_ID
        or set(release.get("members", {}))
        != {"construction_pipeline.csv", "entities.csv", "evidence.csv"}
    ):
        raise VerifiedConstructionCoreError("v0.8 source release differs")
    _, manifest = _manifest_binding(release["manifest"], label="v0.8 source release")
    _validate_manifest_members(manifest, release["members"], label="v0.8 source release")
    raw_rows = _v08_validate_embedded_construction_rows(
        bridge, hydrated_crosscheck=hydrated_crosscheck
    )
    for entity_kind, actual in (("project", project), ("campus", campus)):
        raw_projection = _v08_csv_entity_projection(
            raw_rows[entity_kind], entity_kind=entity_kind
        )
        if _v08_actual_entity_projection(
            actual, entity_kind=entity_kind
        ) != raw_projection:
            raise VerifiedConstructionCoreError(
                f"v0.8 v97 {entity_kind} projection differs"
            )
    used_evidence_ids = tuple(source_profile["used_evidence_ids"])
    if tuple(sorted(used_evidence_ids)) != used_evidence_ids:
        raise VerifiedConstructionCoreError("v0.8 source evidence profile differs")
    try:
        used_evidence = [evidence_pool[evidence_id] for evidence_id in used_evidence_ids]
    except KeyError as error:
        raise VerifiedConstructionCoreError(
            "v0.8 used source evidence differs"
        ) from error
    if (
        _sha256_bytes(_json_bytes(used_evidence))
        != source_profile["used_evidence_sha256"]
    ):
        raise VerifiedConstructionCoreError("v0.8 used source evidence differs")
    for evidence_id, row in raw_rows["evidence"].items():
        projected = evidence_pool.get(evidence_id)
        if projected is None or any(
            projected[field] != row[field]
            for field in GLOBAL_GEOMETRY_EVIDENCE_FIELDS
        ):
            raise VerifiedConstructionCoreError(
                "v0.8 embedded evidence projection differs"
            )
    if hydrated_crosscheck:
        hydrated_evidence = _v08_hydrated_evidence_rows(
            release, used_evidence_ids
        )
        for evidence_id, projected in zip(
            used_evidence_ids, used_evidence, strict=True
        ):
            if any(
                projected[field] != hydrated_evidence[evidence_id][field]
                for field in GLOBAL_GEOMETRY_EVIDENCE_FIELDS
            ):
                raise VerifiedConstructionCoreError(
                    "v0.8 hydrated evidence projection differs"
                )
    _validate_v07_topology(
        bridge,
        project,
        campus,
        hydrated_crosscheck=hydrated_crosscheck,
    )
    relationship = construction["project_to_campus"]["relationship"]
    if (
        relationship.get("relationship_id")
        != acceptance["project_to_campus_relationship_id"]
        or relationship.get("relationship_type")
        != acceptance["project_to_campus_relationship_type"]
        or relationship.get("decision_basis")
        != acceptance["project_to_campus_decision_basis"]
    ):
        raise VerifiedConstructionCoreError("v0.8 topology definition differs")
    return project, campus, source


def _validate_v08_direct_geometry(
    bridge: Mapping[str, Any], acceptance: Mapping[str, Any]
) -> None:
    if bridge.get("geometry_release") is not None:
        raise VerifiedConstructionCoreError("v0.8 direct geometry release differs")
    entity = bridge.get("geometry_entity")
    evidence = bridge.get("geometry_evidence")
    if not isinstance(entity, dict) or not isinstance(evidence, dict):
        raise VerifiedConstructionCoreError("v0.8 direct geometry differs")
    profile = V08_BRIDGE_PROFILES.get(acceptance.get("project_stable_key"))
    if profile is None:
        raise VerifiedConstructionCoreError("v0.8 direct source profile differs")
    expected_source = {
        "source_url": profile.get("direct_source_url"),
        "source_publisher": profile.get("direct_source_publisher"),
        "source_license": profile.get("direct_source_license"),
        "source_retrieved_at": profile.get("direct_source_retrieved_at"),
    }
    entity_source = {
        field: entity.get(field)
        for field in (
            "source_url",
            "source_publisher",
            "source_license",
            "source_retrieved_at",
        )
    }
    evidence_source = {
        "source_url": evidence.get("source_url"),
        "source_publisher": evidence.get("publisher"),
        "source_license": evidence.get("license"),
        "source_retrieved_at": evidence.get("retrieved_at"),
    }
    if (
        None in expected_source.values()
        or entity_source != expected_source
        or evidence_source != expected_source
        or evidence.get("effective_url") != expected_source["source_url"]
        or evidence.get("source_family") != profile.get("direct_source_family")
        or evidence.get("kind") != profile.get("direct_source_kind")
        or evidence.get("license_url") is not None
        or evidence.get("published_at") is not None
    ):
        raise VerifiedConstructionCoreError("v0.8 direct source metadata differs")
    _validate_v06_fact_evidence(evidence, label="v0.8 direct geometry evidence")
    fact = evidence.get("fact_payload")
    geometry = entity.get("geometry")
    if not isinstance(fact, dict) or not isinstance(geometry, dict):
        raise VerifiedConstructionCoreError("v0.8 direct geometry fact differs")
    geometry_bytes = _json_bytes(geometry)[:-1]
    try:
        reported_point = [
            float(fact["longitude_as_reported"]),
            float(fact["latitude_as_reported"]),
        ]
    except (KeyError, TypeError, ValueError) as error:
        raise VerifiedConstructionCoreError(
            "v0.8 direct coordinate strings differ"
        ) from error
    point = [entity.get("longitude"), entity.get("latitude")]
    if (
        entity.get("entity_kind") != "project"
        or entity.get("entity_id") != acceptance["geometry_entity_id"]
        or entity.get("stable_key") != acceptance["geometry_entity_stable_key"]
        or entity.get("entity_id")
        != atlas_stable_id("entity", entity.get("stable_key", ""), "project")
        or acceptance.get("geometry_target_entity_kind") != "project"
        or entity.get("stable_key")
        != acceptance.get("geometry_target_entity_stable_key")
        or entity.get("entity_id") != acceptance.get("geometry_target_entity_id")
        or entity.get("source_evidence_id") != evidence.get("evidence_id")
        or geometry != {"type": "Point", "coordinates": point}
        or reported_point != point
        or [fact.get("longitude"), fact.get("latitude")] != point
        or evidence.get("geometry_canonical_bytes") != len(geometry_bytes)
        or evidence.get("geometry_canonical_sha256")
        != _sha256_bytes(geometry_bytes)
        or evidence.get("evidence_id") != _official_evidence_id(evidence)
        or evidence.get("key") != acceptance["geometry_evidence_key"]
        or evidence.get("kind") != acceptance["geometry_source_kind"]
        or evidence.get("capture_reference") is not None
    ):
        raise VerifiedConstructionCoreError("v0.8 direct geometry projection differs")


def _validate_v08_osm_geometry(
    bridge: Mapping[str, Any],
    acceptance: Mapping[str, Any],
    *,
    hydrated_crosscheck: bool,
) -> None:
    release = bridge.get("geometry_release")
    if (
        not isinstance(release, dict)
        or set(release)
        != {"release_id", "as_of", "recorded_at", "manifest", "members"}
        or release.get("release_id") != "global-open-v3"
        or set(release.get("members", {})) != {"entities.csv", "evidence.csv"}
    ):
        raise VerifiedConstructionCoreError("v0.8 OSM release differs")
    _, manifest = _manifest_binding(release["manifest"], label="v0.8 OSM release")
    _validate_manifest_members(manifest, release["members"], label="v0.8 OSM release")
    entity = bridge.get("geometry_entity")
    evidence = bridge.get("geometry_evidence")
    if not isinstance(entity, dict) or not isinstance(evidence, dict):
        raise VerifiedConstructionCoreError("v0.8 OSM records differ")
    entity_row = _embedded_csv_source_row(
        entity.get("source_row"),
        GLOBAL_GEOMETRY_ENTITY_FIELDS,
        label="v0.8 OSM entity",
    )
    evidence_row = _embedded_csv_source_row(
        evidence.get("source_row"),
        GLOBAL_GEOMETRY_EVIDENCE_FIELDS,
        label="v0.8 OSM evidence",
    )
    if (
        {field: value for field, value in entity.items() if field != "source_row"}
        != _global_geometry_entity_projection(entity_row)
        or {field: value for field, value in evidence.items() if field != "source_row"}
        != _global_geometry_evidence_projection(evidence_row)
    ):
        raise VerifiedConstructionCoreError("v0.8 OSM source projection differs")
    geometry = entity.get("geometry")
    _validate_polygon(geometry, label="v0.8 OSM geometry")
    ring = geometry["coordinates"][0]
    midpoint = [
        (min(point[0] for point in ring) + max(point[0] for point in ring)) / 2,
        (min(point[1] for point in ring) + max(point[1] for point in ring)) / 2,
    ]
    url_match = re.fullmatch(
        r"https://www\.openstreetmap\.org/(node|way|relation)/([1-9][0-9]*)",
        str(entity.get("source_url")),
    )
    if url_match is None:
        raise VerifiedConstructionCoreError("v0.8 OSM source URL differs")
    osm_type, osm_id = url_match.groups()
    expected_osm_status = (
        ("under_construction", "osm_explicit_construction_tag")
        if entity.get("stable_key")
        == "osm:way/1298979790:development-project"
        else ("unknown", "osm_geometry_only_no_operational_inference")
    )
    if (
        entity.get("entity_kind") != acceptance["geometry_entity"]
        or entity.get("stable_key") != acceptance["geometry_entity_stable_key"]
        or entity.get("entity_id") != acceptance["geometry_entity_id"]
        or entity.get("entity_id")
        != atlas_stable_id("entity", entity["stable_key"], entity["entity_kind"])
        or [entity.get("longitude"), entity.get("latitude")] != midpoint
        or evidence.get("evidence_id") != acceptance["geometry_evidence_id"]
        or acceptance["geometry_evidence_key"] is not None
        or evidence.get("evidence_id")
        != atlas_stable_id(
            "evidence",
            "osm",
            osm_type,
            osm_id,
            evidence.get("retrieved_at"),
            evidence.get("content_hash"),
        )
        or evidence.get("evidence_id") != entity.get("snapshot_evidence_id")
        or evidence.get("source_url") != entity.get("source_url")
        or evidence.get("publisher") != entity.get("source_publisher")
        or evidence.get("source_family") != "openstreetmap"
        or entity.get("source_family") != "openstreetmap"
        or evidence.get("license") != "ODbL-1.0"
        or entity.get("source_license") != "ODbL-1.0"
        or evidence.get("attribution") != "© OpenStreetMap contributors"
        or entity.get("source_attribution") != "© OpenStreetMap contributors"
        or (entity.get("status"), entity.get("status_method"))
        != expected_osm_status
        or entity.get("capacity_estimates_json") != []
        or entity.get("workloads_json") != []
    ):
        raise VerifiedConstructionCoreError("v0.8 OSM semantics differ")
    rights = bridge.get("rights")
    if (
        not isinstance(rights, dict)
        or rights.get("geometry_license_url")
        != "https://opendatacommons.org/licenses/odbl/1-0/"
        or rights.get("geometry_attribution_url")
        != "https://www.openstreetmap.org/copyright"
        or "OpenStreetMap contributors" not in str(rights.get("geometry_source"))
    ):
        raise VerifiedConstructionCoreError("v0.8 OSM rights differ")
    if hydrated_crosscheck:
        for name, record, fields in (
            ("entities.csv", entity, GLOBAL_GEOMETRY_ENTITY_FIELDS),
            ("evidence.csv", evidence, GLOBAL_GEOMETRY_EVIDENCE_FIELDS),
        ):
            metadata = release["members"][name]
            path = _repository_input(
                metadata["path"], metadata["sha256"], f"v0.8 OSM {name}"
            )
            if path.stat().st_size != metadata["bytes"]:
                raise VerifiedConstructionCoreError(
                    f"v0.8 hydrated OSM payload differs: {name}"
                )
            _validate_hydrated_source_row(
                path,
                record["source_row"],
                fields,
                label=f"v0.8 OSM {name}",
            )


def _validate_v08_bridge(
    acceptance: Mapping[str, Any],
    overlay: Mapping[str, Any],
    *,
    hydrated_crosscheck: bool,
) -> tuple[dict[str, Any], dict[str, Any]]:
    project_key = acceptance["project_stable_key"]
    profile = V08_BRIDGE_PROFILES.get(project_key)
    if (
        profile is None
        or acceptance.get("bridge_sha256") != profile["bridge_sha256"]
        or acceptance.get("source_input_sha256") != profile["source_sha256"]
        or acceptance.get("bridge_schema") != profile["schema"]
        or acceptance.get("geometry_entity") != profile["geometry_kind"]
        or acceptance.get("geometry_entity_stable_key")
        != profile["geometry_stable_key"]
        or acceptance.get("geometry_target_entity_kind") != profile["target_kind"]
        or acceptance.get("geometry_target_entity_stable_key")
        != profile["target_stable_key"]
        or acceptance.get("geometry_evidence_id")
        != profile["geometry_evidence_id"]
        or acceptance.get("geometry_evidence_key")
        != profile["geometry_evidence_key"]
        or acceptance.get("geometry_source_kind")
        != profile["geometry_source_kind"]
    ):
        raise VerifiedConstructionCoreError("v0.8 immutable bridge profile differs")
    path = _repository_input(
        acceptance["bridge_path"], acceptance["bridge_sha256"], "geometry bridge"
    )
    if path.stat().st_size != acceptance["bridge_bytes"]:
        raise VerifiedConstructionCoreError("v0.8 bridge byte count differs")
    bridge = _load_json(path)
    expected_top = {
        "bridge_id",
        "bridge_schema",
        "claim_guardrails",
        "construction_source",
        "geometry_capture",
        "geometry_entity",
        "geometry_evidence",
        "geometry_release",
        "identity_bridge_evidence",
        "imagery_posture",
        "lineage_context",
        "review_decision",
        "reviewed_at",
        "rights",
        "schema_version",
    }
    if (
        not isinstance(bridge, dict)
        or set(bridge) != expected_top
        or bridge.get("schema_version") != 1
        or bridge.get("bridge_id") != acceptance["bridge_id"]
        or bridge.get("bridge_schema") != acceptance["bridge_schema"]
        or bridge.get("reviewed_at") != CURRENT_V08_REVIEW_DATE.isoformat()
        or bridge.get("geometry_capture") is not None
        or acceptance.get("portable_capture_binding") is not None
        or bridge.get("identity_bridge_evidence") != []
    ):
        raise VerifiedConstructionCoreError("v0.8 bridge identity differs")
    geometry_projection = {
        field: bridge[field]
        for field in ("geometry_release", "geometry_entity", "geometry_evidence")
    }
    if (
        _sha256_bytes(_json_bytes(geometry_projection))
        != profile["geometry_projection_sha256"]
    ):
        raise VerifiedConstructionCoreError("v0.8 geometry profile differs")
    rights = bridge.get("rights")
    if (
        not isinstance(rights, dict)
        or _sha256_bytes(_json_bytes(rights)) != profile["rights_sha256"]
    ):
        raise VerifiedConstructionCoreError("v0.8 rights profile differs")
    if (
        _sha256_bytes(_json_bytes(bridge.get("review_decision")))
        != profile["review_decision_sha256"]
    ):
        raise VerifiedConstructionCoreError("v0.8 review decision profile differs")
    if (
        _sha256_bytes(_json_bytes(bridge.get("claim_guardrails")))
        != profile["claim_guardrails_sha256"]
    ):
        raise VerifiedConstructionCoreError("v0.8 claim guardrails profile differs")
    if (
        _sha256_bytes(_json_bytes(bridge.get("imagery_posture")))
        != profile["imagery_posture_sha256"]
    ):
        raise VerifiedConstructionCoreError("v0.8 imagery posture profile differs")
    project, campus, source = _v08_validate_construction_source(
        bridge,
        acceptance,
        hydrated_crosscheck=hydrated_crosscheck,
    )
    entity = bridge["geometry_entity"]
    evidence = bridge["geometry_evidence"]
    if (
        entity.get("entity_kind") != acceptance["geometry_entity"]
        or entity.get("stable_key") != acceptance["geometry_entity_stable_key"]
        or entity.get("entity_id") != acceptance["geometry_entity_id"]
        or evidence.get("evidence_id") != acceptance["geometry_evidence_id"]
        or [entity.get("longitude"), entity.get("latitude")]
        != [profile["longitude"], profile["latitude"]]
    ):
        raise VerifiedConstructionCoreError("v0.8 geometry source identity differs")
    target = project if profile["target_kind"] == "project" else campus
    if (
        target["stable_key"] != acceptance["geometry_target_entity_stable_key"]
        or target["entity_id"] != acceptance["geometry_target_entity_id"]
        or target["stable_key"] != profile["target_stable_key"]
    ):
        raise VerifiedConstructionCoreError("v0.8 geometry target identity differs")
    decision = bridge.get("review_decision")
    decision_bindings = {
        "decision": "bridge_decision",
        "geometry_target_entity_kind": "geometry_target_entity_kind",
        "geometry_derivation": "geometry_derivation",
        "geometry_method": "geometry_method",
        "geometry_scope_class": "geometry_scope_class",
        "geometry_authority_class": "geometry_authority_class",
        "geometry_use_scope": "geometry_use_scope",
        "horizontal_uncertainty_metres": "horizontal_uncertainty_metres",
        "horizontal_uncertainty_unknown_reason": "horizontal_uncertainty_unknown_reason",
        "precision_scope": "precision_scope",
        "rejected_claims": "rejected_claims",
    }
    if not isinstance(decision, dict) or any(
        decision.get(field) != acceptance[target_field]
        for field, target_field in decision_bindings.items()
    ):
        raise VerifiedConstructionCoreError("v0.8 review semantics differ")
    if (
        decision.get("official_boundary") is not False
        or acceptance.get("official_boundary") is not False
        or (
            profile["target_kind"] == "campus"
            and acceptance.get("geometry_use_scope") != "campus_locator"
        )
        or (
            profile["target_kind"] == "project"
            and acceptance.get("geometry_use_scope") != "project_locator"
        )
    ):
        raise VerifiedConstructionCoreError("v0.8 geometry scope differs")
    if profile["schema"].endswith("direct-first-party-geometry-bridge-v1"):
        _validate_v08_direct_geometry(bridge, acceptance)
    else:
        _validate_v08_osm_geometry(
            bridge,
            acceptance,
            hydrated_crosscheck=hydrated_crosscheck,
        )
    imagery = bridge.get("imagery_posture")
    if (
        not isinstance(imagery, dict)
        or imagery.get("outcome") != "not_reviewed_for_core_preview"
        or imagery.get("independent_imagery_verification") is not False
        or not isinstance(imagery.get("basis"), str)
        or not imagery["basis"]
        or not isinstance(bridge.get("claim_guardrails"), dict)
        or not bridge["claim_guardrails"]
        or any(
            not isinstance(value, str) or not value
            for value in bridge["claim_guardrails"].values()
        )
    ):
        raise VerifiedConstructionCoreError("v0.8 imagery or guardrails differ")
    return bridge, source


def _v08_contracts(*, hydrated_crosscheck: bool) -> dict[str, Any]:
    _validate_v08_definition_pins()
    reviewed = _load_json(V08_REVIEW_DEFINITION)
    overlays = _load_json(V08_OVERLAY_DEFINITION)
    provenance = _load_json(V08_PROVENANCE_DEFINITION)
    imagery = _load_json(V08_IMAGERY_REVIEW_DEFINITION)
    if (
        not isinstance(reviewed, dict)
        or set(reviewed)
        != {"contract_id", "review_scope", "reviewed_as_of", "base_contract", "acceptances"}
        or reviewed.get("contract_id")
        != "verified-construction-core-v0.8-reviewed-sites"
        or reviewed.get("reviewed_as_of") != CURRENT_V08_REVIEW_DATE.isoformat()
        or reviewed.get("base_contract")
        != {
            "path": "definitions/verified-construction-core-v0.7-reviewed-sites.json",
            "sha256": V07_REVIEW_DEFINITION_SHA256,
            "default_geometry_entity": "project",
            "default_geometry_derivation": "direct_geometry",
        }
        or not isinstance(reviewed.get("acceptances"), list)
        or len(reviewed["acceptances"]) != 8
    ):
        raise VerifiedConstructionCoreError("v0.8 reviewed-site contract differs")
    if (
        not isinstance(overlays, dict)
        or set(overlays)
        != {
            "contract_id",
            "purpose",
            "reviewed_as_of",
            "base_contract",
            "required_fields",
            "allowed_decisions",
            "overlays",
        }
        or overlays.get("contract_id")
        != "verified-construction-core-reviewed-overlays-v6"
        or overlays.get("reviewed_as_of") != CURRENT_V08_REVIEW_DATE.isoformat()
        or overlays.get("base_contract")
        != {
            "path": "definitions/verified-construction-core-reviewed-overlays-v5.json",
            "sha256": V07_OVERLAY_DEFINITION_SHA256,
        }
        or set(overlays.get("allowed_decisions", []))
        != {"queued", "accepted", "excluded"}
        or not isinstance(overlays.get("overlays"), list)
        or len(overlays["overlays"]) != 8
    ):
        raise VerifiedConstructionCoreError("v0.8 overlay contract differs")
    if set(overlays.get("required_fields", [])) != V08_OVERLAY_REQUIRED_FIELDS:
        raise VerifiedConstructionCoreError("v0.8 overlay required fields differ")
    if any(
        not isinstance(row, dict) or set(row) != V08_OVERLAY_REQUIRED_FIELDS
        for row in overlays["overlays"]
    ):
        raise VerifiedConstructionCoreError("v0.8 overlay row schema differs")
    acceptances = reviewed["acceptances"]
    acceptance_by_key = {
        row.get("project_stable_key"): row
        for row in acceptances
        if isinstance(row, dict)
    }
    overlay_by_key = {
        row.get("source_project_stable_key"): row
        for row in overlays["overlays"]
        if isinstance(row, dict)
    }
    if (
        len(acceptance_by_key) != 8
        or len(overlay_by_key) != 8
        or set(acceptance_by_key) != set(V08_BRIDGE_PROFILES)
        or set(overlay_by_key) != set(V08_BRIDGE_PROFILES)
        or set(V08_SOURCE_PROFILES) != set(V08_BRIDGE_PROFILES)
    ):
        raise VerifiedConstructionCoreError("v0.8 definition cohort differs")
    profiled_evidence_ids = {
        evidence_id
        for profile in V08_SOURCE_PROFILES.values()
        for evidence_id in profile["used_evidence_ids"]
    } | {
        profile["geometry_evidence_id"]
        for profile in V08_BRIDGE_PROFILES.values()
    }
    if profiled_evidence_ids != V08_EVIDENCE_IDS:
        raise VerifiedConstructionCoreError("v0.8 evidence profile differs")
    bridges: dict[str, dict[str, Any]] = {}
    sources: dict[str, dict[str, Any]] = {}
    crosswalk = {
        "country": "country",
        "physical_site_stable_key": "physical_site_stable_key",
        "physical_site_entity_id": "physical_site_entity_id",
        "source_input_path": "source_input_path",
        "source_input_bytes": "source_input_bytes",
        "source_input_sha256": "source_input_sha256",
        "bridge_id": "bridge_id",
        "bridge_schema": "bridge_schema",
        "bridge_path": "bridge_path",
        "bridge_bytes": "bridge_bytes",
        "bridge_sha256": "bridge_sha256",
        "project_to_campus_relationship_id": "project_to_campus_relationship_id",
        "project_to_campus_relationship_type": "project_to_campus_relationship_type",
        "project_to_campus_decision_basis": "project_to_campus_decision_basis",
        "geometry_entity": "geometry_entity",
        "geometry_entity_stable_key": "geometry_entity_stable_key",
        "geometry_entity_id": "geometry_entity_id",
        "geometry_target_entity_kind": "geometry_target_entity_kind",
        "geometry_target_entity_stable_key": "geometry_target_entity_stable_key",
        "geometry_target_entity_id": "geometry_target_entity_id",
        "geometry_evidence_key": "geometry_evidence_key",
        "geometry_evidence_id": "geometry_evidence_id",
        "geometry_source_kind": "geometry_source_kind",
        "geometry_derivation": "geometry_derivation",
        "geometry_method": "geometry_method",
        "geometry_scope_class": "geometry_scope_class",
        "geometry_authority_class": "geometry_authority_class",
        "geometry_use_scope": "geometry_use_scope",
        "official_boundary": "official_boundary",
        "horizontal_uncertainty_metres": "horizontal_uncertainty_metres",
        "horizontal_uncertainty_unknown_reason": "horizontal_uncertainty_unknown_reason",
        "precision_scope": "precision_scope",
        "bridge_decision": "bridge_decision",
        "portable_input_binding": "portable_input_binding",
        "portable_capture_binding": "portable_capture_binding",
        "rejected_claims": "rejected_claims",
    }
    for project_key in sorted(acceptance_by_key):
        acceptance = acceptance_by_key[project_key]
        overlay = overlay_by_key[project_key]
        if (
            any(acceptance[left] != overlay[right] for left, right in crosswalk.items())
            or acceptance["project_entity_id"]
            != overlay["source_project_entity_id"]
            or acceptance["project_stable_key"]
            != overlay["source_project_stable_key"]
            or acceptance["geometry_overlay_id"] != overlay["overlay_id"]
            or acceptance["decision_basis"] != overlay["decision_reason"]
            or overlay["decision"] != "accepted"
            or overlay["reviewed_at"] != CURRENT_V08_REVIEW_DATE.isoformat()
        ):
            raise VerifiedConstructionCoreError(
                "v0.8 acceptance-overlay binding differs"
            )
        bridge, source = _validate_v08_bridge(
            acceptance,
            overlay,
            hydrated_crosscheck=hydrated_crosscheck,
        )
        bridges[project_key] = bridge
        sources[project_key] = source
    if (
        not isinstance(provenance, dict)
        or set(provenance)
        != {
            "contract_id",
            "review_scope",
            "reviewed_as_of",
            "base_contract",
            "workload_scope_bindings",
            "role_bindings",
            "excluded_source_roles",
            "context_evidence_bindings",
        }
        or provenance.get("contract_id")
        != "verified-construction-core-v0.8-provenance"
        or provenance.get("reviewed_as_of") != CURRENT_V08_REVIEW_DATE.isoformat()
        or provenance.get("base_contract")
        != {
            "path": "definitions/verified-construction-core-v0.7-provenance.json",
            "sha256": V07_PROVENANCE_DEFINITION_SHA256,
        }
        or provenance.get("workload_scope_bindings") != []
        or len(provenance.get("role_bindings", [])) != 1
        or len(provenance.get("excluded_source_roles", [])) != 2
        or len(provenance.get("context_evidence_bindings", [])) != 2
    ):
        raise VerifiedConstructionCoreError("v0.8 provenance contract differs")
    role = provenance["role_bindings"][0]
    exclusions = {
        (
            row.get("project_stable_key"),
            row.get("role"),
            row.get("party"),
            row.get("candidate_evidence_id"),
            row.get("relationship_scope"),
        )
        for row in provenance["excluded_source_roles"]
    }
    contexts = {
        (
            row.get("project_stable_key"),
            row.get("evidence_id"),
            row.get("usage_role"),
        )
        for row in provenance["context_evidence_bindings"]
    }
    if (
        (
            role.get("project_stable_key"),
            role.get("role"),
            role.get("party"),
            role.get("evidence_id"),
            role.get("relationship_scope"),
        )
        != (
            "curated:cdc-laverton-melbourne-campus:current-build",
            "operator",
            "CDC Data Centres",
            "d05112cf-0117-5f1a-9b7a-4087ec4f5c8f",
            "intended",
        )
        or exclusions
        != {
            (
                "curated:borealis-blonduos-data-center-campus:expansion-current-build",
                "operator",
                "Borealis Data Center",
                "bd494c0b-3936-5cc0-844a-67cd0db22812",
                "not_established",
            ),
            (
                "curated:borealis-blonduos-data-center-campus:expansion-current-build",
                "utility",
                "Landsvirkjun",
                "bd494c0b-3936-5cc0-844a-67cd0db22812",
                "not_established",
            ),
        }
        or contexts
        != {
            (
                "curated:enka-data-solutions-eds-ist-01-tuzla-data-center:initial-build",
                "0dd3dc39-1457-5bc3-b7d2-7ff44183aed3",
                "geometry_identity",
            ),
            (
                "curated:macquarie-ic3-super-west-facility:phase-1-build",
                "0ba37f72-6f9b-5d8f-8b8e-8b66e03dbb11",
                "project_context",
            ),
        }
    ):
        raise VerifiedConstructionCoreError("v0.8 provenance semantics differ")
    for collection, evidence_field in (
        (provenance["role_bindings"], "evidence_id"),
        (provenance["excluded_source_roles"], "candidate_evidence_id"),
        (provenance["context_evidence_bindings"], "evidence_id"),
    ):
        for binding in collection:
            project_key = binding["project_stable_key"]
            acceptance = acceptance_by_key[project_key]
            source_evidence = _v08_source_evidence(sources[project_key])
            if (
                binding.get("source_input_path") != acceptance["source_input_path"]
                or binding.get("source_input_sha256")
                != acceptance["source_input_sha256"]
                or binding[evidence_field] not in source_evidence
            ):
                raise VerifiedConstructionCoreError(
                    "v0.8 provenance evidence binding differs"
                )
    if (
        not isinstance(imagery, dict)
        or set(imagery)
        != {
            "contract_id",
            "review_scope",
            "reviewed_as_of",
            "base_contract",
            "default_outcome",
            "records",
        }
        or imagery.get("contract_id")
        != "verified-construction-core-v0.8-imagery-reviews"
        or imagery.get("reviewed_as_of") != CURRENT_V08_REVIEW_DATE.isoformat()
        or imagery.get("base_contract")
        != {
            "path": "definitions/verified-construction-core-v0.7-imagery-reviews.json",
            "sha256": V07_IMAGERY_REVIEW_DEFINITION_SHA256,
        }
        or imagery.get("default_outcome") != "not_reviewed_for_core_preview"
        or imagery.get("records") != []
    ):
        raise VerifiedConstructionCoreError("v0.8 imagery contract differs")
    return {
        "reviewed": reviewed,
        "overlays": overlays,
        "provenance": provenance,
        "imagery": imagery,
        "acceptance_by_key": acceptance_by_key,
        "overlay_by_key": overlay_by_key,
        "bridges": bridges,
        "sources": sources,
    }


def _v08_build_delta_rows(contracts: Mapping[str, Any]) -> dict[str, Any]:
    provenance = contracts["provenance"]
    roles_by_project: dict[str, list[dict[str, Any]]] = defaultdict(list)
    for row in provenance["role_bindings"]:
        roles_by_project[row["project_stable_key"]].append(row)
    contexts_by_project: dict[str, list[dict[str, Any]]] = defaultdict(list)
    for row in provenance["context_evidence_bindings"]:
        contexts_by_project[row["project_stable_key"]].append(row)
    evidence_pool: dict[str, dict[str, Any]] = {}
    evidence_usage: dict[str, dict[str, set[str]]] = defaultdict(
        lambda: {"roles": set(), "project_ids": set()}
    )
    projects: list[dict[str, Any]] = []
    sites: list[dict[str, Any]] = []
    for project_key in sorted(contracts["bridges"]):
        bridge = contracts["bridges"][project_key]
        source = contracts["sources"][project_key]
        acceptance = contracts["acceptance_by_key"][project_key]
        construction = bridge["construction_source"]
        source_evidence = _v08_source_evidence(source)
        evidence_pool.update(source_evidence)
        geometry_evidence = _v07_evidence_projection(bridge["geometry_evidence"])
        evidence_pool[geometry_evidence["evidence_id"]] = geometry_evidence
        project = construction["project"]
        campus = construction["campus"]
        project_id = project["entity_id"]
        site_id = _stable_id("vcc-site", campus["stable_key"])
        status_evidence_id = project["status_evidence_id"]
        geometry_evidence_id = bridge["geometry_evidence"]["evidence_id"]
        evidence_usage[status_evidence_id]["roles"].add("physical_status")
        evidence_usage[status_evidence_id]["project_ids"].add(project_id)
        evidence_usage[geometry_evidence_id]["roles"].add("geometry")
        evidence_usage[geometry_evidence_id]["project_ids"].add(project_id)
        observations = project.get("capacity_estimates_json", [])
        if not isinstance(observations, list):
            raise VerifiedConstructionCoreError("v0.8 capacity observations differ")
        for observation in observations:
            _validate_typed_observation(
                observation, POWER_METRICS | ENERGY_METRICS | EFFICIENCY_METRICS
            )
            evidence_id = observation["evidence_id"]
            if evidence_id not in evidence_pool:
                raise VerifiedConstructionCoreError("v0.8 capacity evidence differs")
            evidence_usage[evidence_id]["roles"].add(
                f"typed_metric:{observation['metric']}"
            )
            evidence_usage[evidence_id]["project_ids"].add(project_id)
        power = [row for row in observations if row["metric"] in POWER_METRICS]
        energy = [row for row in observations if row["metric"] in ENERGY_METRICS]
        efficiency = [
            row for row in observations if row["metric"] in EFFICIENCY_METRICS
        ]
        if project.get("workloads_json") != []:
            raise VerifiedConstructionCoreError("v0.8 workload promotion differs")
        role_claims: list[dict[str, str]] = []
        projected_roles = {role: [] for role in ROLE_COLUMNS}
        for binding in roles_by_project.get(project_key, []):
            evidence_id = binding["evidence_id"]
            if evidence_id not in evidence_pool:
                raise VerifiedConstructionCoreError("v0.8 role evidence differs")
            role_claims.append(
                {
                    "evidence_id": evidence_id,
                    "party": binding["party"],
                    "relationship_scope": binding["relationship_scope"],
                    "role": binding["role"],
                }
            )
            projected_roles[binding["role"]].append(binding["party"])
            evidence_usage[evidence_id]["roles"].add(f"role:{binding['role']}")
            evidence_usage[evidence_id]["project_ids"].add(project_id)
        role_claims.sort(key=lambda row: (row["role"], row["party"], row["evidence_id"]))
        operating_model = project.get("operating_model")
        operating_model_evidence_id = project.get("operating_model_evidence_id")
        if operating_model:
            if operating_model_evidence_id not in evidence_pool:
                raise VerifiedConstructionCoreError(
                    "v0.8 operating-model evidence differs"
                )
            evidence_usage[operating_model_evidence_id]["roles"].add(
                "operating_model"
            )
            evidence_usage[operating_model_evidence_id]["project_ids"].add(project_id)
        elif operating_model_evidence_id is not None:
            raise VerifiedConstructionCoreError(
                "v0.8 operating-model identity differs"
            )
        for binding in contexts_by_project.get(project_key, []):
            evidence_id = binding["evidence_id"]
            if evidence_id not in evidence_pool:
                raise VerifiedConstructionCoreError("v0.8 context evidence differs")
            evidence_usage[evidence_id]["roles"].add(
                f"context:{binding['usage_role']}"
            )
            evidence_usage[evidence_id]["project_ids"].add(project_id)
        entity = bridge["geometry_entity"]
        geometry = entity["geometry"]
        status_date = _calendar_date(project["status_as_of"], "v0.8 status_as_of")
        image_outcome = contracts["imagery"]["default_outcome"]
        row = {
            "project_id": project_id,
            "project_stable_key": project_key,
            "site_id": site_id,
            "physical_site_stable_key": campus["stable_key"],
            "name": project["name"],
            "country": acceptance["country"],
            "country_iso_a2": V08_COUNTRY_ISO_A2[acceptance["country"]],
            "latitude": entity["latitude"],
            "longitude": entity["longitude"],
            "geometry_json": _json_bytes(geometry).decode().strip(),
            "geometry_type": geometry["type"],
            "geometry_source_entity_kind": acceptance["geometry_entity"],
            "geometry_derivation": acceptance["geometry_derivation"],
            "geometry_method": acceptance["geometry_method"],
            "geometry_scope_class": acceptance["geometry_scope_class"],
            "geometry_authority_class": acceptance["geometry_authority_class"],
            "geometry_use_scope": acceptance["geometry_use_scope"],
            "geometry_precision_scope": acceptance["precision_scope"],
            "horizontal_uncertainty_metres": (
                ""
                if acceptance["horizontal_uncertainty_metres"] is None
                else acceptance["horizontal_uncertainty_metres"]
            ),
            "horizontal_uncertainty_unknown_reason": acceptance[
                "horizontal_uncertainty_unknown_reason"
            ],
            "geometry_evidence_id": geometry_evidence_id,
            "last_observed_physical_status": project["status"],
            "status_as_of": project["status_as_of"],
            "status_age_days_at_review": (
                CURRENT_V08_REVIEW_DATE - status_date
            ).days,
            "status_method": project["status_method"],
            "status_evidence_id": status_evidence_id,
            "verification_posture": _verification_posture(
                acceptance["geometry_authority_class"],
                acceptance["geometry_use_scope"],
            ),
            "independent_imagery_verification": "false",
            "imagery_review_outcome": image_outcome,
            "development_type": "unknown",
            "development_type_unknown_reason": "source evidence does not distinguish greenfield, expansion, or retrofit",
            "operating_model": operating_model or "unknown",
            "operating_model_unknown_reason": (
                "" if operating_model else "not established by selected evidence"
            ),
            "operating_model_evidence_id": operating_model_evidence_id or "",
            "workloads_json": "[]",
            "workload_unknown_reason": "not established by selected evidence",
            "role_claims_json": _json_bytes(role_claims).decode().strip(),
            "power_observations_json": _json_bytes(power).decode().strip(),
            "power_unknown_reason": "" if power else "no typed project power observation; not estimated",
            "annual_energy_observations_json": _json_bytes(energy).decode().strip(),
            "annual_energy_unknown_reason": "" if energy else "no scoped annual-energy inputs; not estimated",
            "efficiency_observations_json": _json_bytes(efficiency).decode().strip(),
            "efficiency_unknown_reason": "" if efficiency else "no scoped PUE or WUE observation",
            "owner": "; ".join(sorted(projected_roles["owner"])),
            "operator": "; ".join(sorted(projected_roles["operator"])),
            "users": "; ".join(sorted(projected_roles["user"])),
            "tenants": "; ".join(sorted(projected_roles["tenant"])),
            "customers": "; ".join(sorted(projected_roles["customer"])),
            "status_source_url": evidence_pool[status_evidence_id]["source_url"],
            "geometry_source_url": geometry_evidence["source_url"],
        }
        projects.append(row)
        sites.append(
            {
                "site_id": site_id,
                "physical_site_stable_key": campus["stable_key"],
                "name": campus["name"],
                "country": row["country"],
                "country_iso_a2": row["country_iso_a2"],
                "latitude": row["latitude"],
                "longitude": row["longitude"],
                "geometry_json": row["geometry_json"],
                "geometry_type": row["geometry_type"],
                "geometry_source_entity_kinds_json": _json_bytes(
                    [row["geometry_source_entity_kind"]]
                ).decode().strip(),
                "geometry_derivations_json": _json_bytes(
                    [row["geometry_derivation"]]
                ).decode().strip(),
                "geometry_methods_json": _json_bytes(
                    [row["geometry_method"]]
                ).decode().strip(),
                "geometry_scope_classes_json": _json_bytes(
                    [row["geometry_scope_class"]]
                ).decode().strip(),
                "geometry_authority_classes_json": _json_bytes(
                    [row["geometry_authority_class"]]
                ).decode().strip(),
                "geometry_use_scopes_json": _json_bytes(
                    [row["geometry_use_scope"]]
                ).decode().strip(),
                "geometry_precision_scopes_json": _json_bytes(
                    [row["geometry_precision_scope"]]
                ).decode().strip(),
                "horizontal_uncertainty_metres": row[
                    "horizontal_uncertainty_metres"
                ],
                "horizontal_uncertainty_unknown_reason": row[
                    "horizontal_uncertainty_unknown_reason"
                ],
                "geometry_evidence_ids_json": _json_bytes(
                    [geometry_evidence_id]
                ).decode().strip(),
                "project_count": 1,
                "project_ids_json": _json_bytes([project_id]).decode().strip(),
                "project_stable_keys_json": _json_bytes([project_key]).decode().strip(),
                "statuses_json": _json_bytes(
                    [row["last_observed_physical_status"]]
                ).decode().strip(),
                "oldest_status_as_of": row["status_as_of"],
                "newest_status_as_of": row["status_as_of"],
                "verification_posture": row["verification_posture"],
                "independent_imagery_verification": "false",
                "imagery_review_outcomes_json": _json_bytes(
                    [image_outcome]
                ).decode().strip(),
            }
        )
    if set(evidence_usage) != V08_EVIDENCE_IDS:
        raise VerifiedConstructionCoreError("v0.8 projected evidence allowlist differs")
    evidence = [
        {
            **evidence_pool[evidence_id],
            "roles_json": _json_bytes(sorted(usage["roles"])).decode().strip(),
            "project_ids_json": _json_bytes(
                sorted(usage["project_ids"])
            ).decode().strip(),
        }
        for evidence_id, usage in sorted(evidence_usage.items())
    ]
    return {"projects": projects, "sites": sites, "evidence": evidence}


def _v08_portable_source_inputs(
    contracts: Mapping[str, Any],
) -> list[dict[str, Any]]:
    base_manifest = _load_json(LEGACY_PREVIEW_V07_DIR / "manifest.json")
    rows = list(base_manifest["portable_source_inputs"])
    new: dict[str, dict[str, Any]] = {}
    for acceptance in contracts["reviewed"]["acceptances"]:
        for binding in (
            acceptance["portable_input_binding"],
            {
                "path": acceptance["bridge_path"],
                "bytes": acceptance["bridge_bytes"],
                "sha256": acceptance["bridge_sha256"],
            },
        ):
            record = {
                "path": binding["path"],
                "bytes": binding["bytes"],
                "sha256": binding["sha256"],
                "parent_manifests": [],
            }
            previous = new.get(record["path"])
            if previous is not None and previous != record:
                raise VerifiedConstructionCoreError("v0.8 portable input differs")
            new[record["path"]] = record
    rows.extend(new.values())
    rows.sort(key=lambda row: row["path"])
    if len(rows) != 57 or len({row["path"] for row in rows}) != 57:
        raise VerifiedConstructionCoreError("v0.8 portable input count differs")
    for row in rows:
        path = _repository_input(row["path"], row["sha256"], "v0.8 portable input")
        if path.stat().st_size != row["bytes"]:
            raise VerifiedConstructionCoreError(
                "v0.8 portable input byte count differs"
            )
    return rows


def _v08_map_html(
    geojson: Mapping[str, Any], evidence: Sequence[Mapping[str, Any]]
) -> bytes:
    document = _v07_map_html(geojson, evidence).decode("utf-8")
    if "Verified Construction Core v0.7 preview" not in document:
        raise VerifiedConstructionCoreError("v0.8 map title seam differs")
    return document.replace(
        "Verified Construction Core v0.7 preview",
        "Verified Construction Core v0.8 preview",
    ).encode("utf-8")


def _v08_schema() -> dict[str, Any]:
    schema = _v07_schema()
    schema["format"] = "datacenter-atlas-verified-construction-core-schema-v8"
    schema["preview_id"] = CURRENT_V08_PREVIEW_ID
    return schema


def _v08_readme(report: Mapping[str, Any]) -> bytes:
    return f"""# Verified Construction Core v0.8 preview

This tracked, non-final preview contains **{report['selected_physical_site_count']} physical sites**
and **{report['selected_project_count']} linked projects** in {len(report['country_counts'])}
countries. Every project retains a dated authoritative physical-status observation and an exact
reviewed geometry scope. Five project rows carry official boundaries; the remaining 39 are
explicit project or campus locators, never construction footprints by implication.

The eight-row v0.8 delta adds Digital Realty VIE13, ENKA EDS IST 01, Colt FRA3, Macquarie IC3
Super West Phase 1, Equinix MU4 Phase 3, CDC Laverton, Pure DC LON01 B2, and the Borealis
Blönduós expansion. VIE13 and IST 01 use direct first-party facility-page points as project
locators. Six frozen OpenStreetMap polygons are retained as contributor-mapped geometry: Colt's
named building is a project locator, while Macquarie, Equinix, CDC, Pure DC, and Borealis use
explicit parent-campus locators. None is an official boundary or current construction extent.

Digital Realty VIE13 retains a source-bound colocation operating model. CDC contributes one
intended operator claim. Borealis operator and utility strings remain explicit non-publication
decisions. ENKA identity context and Macquarie's parent-facility design PUE remain context-only;
campus capacity and PUE values are not promoted into project metrics. No v0.8 workload is added.

The artifact is deterministic and rebuildable in a clean clone from the 57 manifest-bound portable
inputs plus the frozen v0.7 artifact. Ignored v97, global-v3, and v14 payloads are optional hydrated
cross-checks, not build dependencies. All eight v0.8 rows inherit the not-reviewed imagery outcome
and remain `independent_imagery_verification=false`.

This is not the final 100-site Verified Construction Core v1. Its final-release gates remain
unsatisfied and `publishable_as_final` is false.
""".encode("utf-8")


def _v08_attribution(evidence: Sequence[Mapping[str, Any]]) -> bytes:
    rows = {(row["publisher"], row["license"], row["source_url"]) for row in evidence}
    lines = [
        "Data Center Atlas Verified Construction Core v0.8 preview",
        "",
        "Compact derived facts only; third-party terms remain controlling.",
        "",
        *(
            f"- {publisher} | {license_name} | {url}"
            for publisher, license_name, url in sorted(rows)
        ),
        "",
        "Map-provider terms:",
        *(
            f"- {label} | {license_name} | {url}"
            for label, license_name, url in _v07_map_attribution_notices(evidence)
        ),
    ]
    return ("\n".join(lines) + "\n").encode("utf-8")


def _v08_payloads() -> tuple[dict[str, bytes], dict[str, Any], list[dict[str, Any]]]:
    validate_frozen_v07(LEGACY_PREVIEW_V07_DIR)
    contracts = _v08_contracts(
        hydrated_crosscheck=_v08_hydrated_crosscheck_available()
    )
    base_projects = _load_csv(LEGACY_PREVIEW_V07_DIR / "projects.csv", PROJECT_FIELDS)
    base_sites = _load_csv(LEGACY_PREVIEW_V07_DIR / "sites.csv", SITE_FIELDS)
    base_evidence = _load_csv(LEGACY_PREVIEW_V07_DIR / "evidence.csv", EVIDENCE_FIELDS)
    base_report = _load_json(LEGACY_PREVIEW_V07_DIR / "selection-report.json")
    delta = _v08_build_delta_rows(contracts)
    projects = sorted(
        [*base_projects, *delta["projects"]],
        key=lambda row: row["project_stable_key"],
    )
    sites = sorted(
        [*base_sites, *delta["sites"]],
        key=lambda row: row["physical_site_stable_key"],
    )
    evidence = sorted(
        [*base_evidence, *delta["evidence"]],
        key=lambda row: row["evidence_id"],
    )
    if (
        len(projects) != 44
        or len(sites) != 41
        or len(evidence) != 100
        or len({row["project_id"] for row in projects}) != 44
        or len({row["site_id"] for row in sites}) != 41
        or len({row["evidence_id"] for row in evidence}) != 100
    ):
        raise VerifiedConstructionCoreError("v0.8 cohort count or uniqueness differs")
    gates = _final_release_gates(sites, projects)
    gates["clean_clone_rebuild"] = {
        "passed": True,
        "reason": "all current-delta inputs and the frozen base artifact are tracked and hash-bound",
    }
    country_counts = dict(sorted(Counter(row["country"] for row in sites).items()))
    provenance_decisions = {
        key: [
            *base_report["provenance_decisions"][key],
            *contracts["provenance"][key],
        ]
        for key in ("workload_scope_bindings", "role_bindings", "excluded_source_roles")
    }
    provenance_decisions["context_evidence_bindings"] = contracts["provenance"][
        "context_evidence_bindings"
    ]
    report = {
        **base_report,
        "format": "datacenter-atlas-verified-construction-core-selection-v8",
        "publishable_as_final": all(
            gate.get("passed", False) for gate in gates.values()
        ),
        "reviewed_at": CURRENT_V08_REVIEW_DATE.isoformat(),
        "selected_project_count": 44,
        "selected_physical_site_count": 41,
        "official_boundary_project_count": 5,
        "reviewed_site_locator_project_count": 39,
        "non_selected_source_row_count": 487,
        "selection_first_failure_counts": V08_SELECTION_FIRST_FAILURE_COUNTS,
        "country_counts": country_counts,
        "final_release_gates": gates,
        "imagery_review_provenance": list(
            base_report["imagery_review_provenance"]
        ),
        "provenance_decisions": provenance_decisions,
        "reviewed_overlay_queue": [
            *base_report["reviewed_overlay_queue"],
            *contracts["overlays"]["overlays"],
        ],
    }
    if (
        len(country_counts) != 25
        or sum(row["country_iso_a2"] != "US" for row in sites) != 35
        or sum(_is_official_boundary(row) for row in projects) != 5
        or sum(_is_reviewed_locator(row) for row in projects) != 39
        or sum(V08_SELECTION_FIRST_FAILURE_COUNTS.values()) != 531
    ):
        raise VerifiedConstructionCoreError("v0.8 expected accounting differs")
    geojson = _geojson(sites)
    geojson["name"] = "Data Center Atlas Verified Construction Core v0.8 preview"
    payloads = {
        "ATTRIBUTION.txt": _v08_attribution(evidence),
        "README.md": _v08_readme(report),
        "evidence.csv": _csv_bytes(evidence, EVIDENCE_FIELDS),
        "map.html": _v08_map_html(geojson, evidence),
        "projects.csv": _csv_bytes(projects, PROJECT_FIELDS),
        "schema.json": _json_bytes(_v08_schema()),
        "selection-report.json": _json_bytes(report),
        "sites.csv": _csv_bytes(sites, SITE_FIELDS),
        "sites.geojson": _json_bytes(geojson),
    }
    portable = _v08_portable_source_inputs(contracts)
    return payloads, report, portable


def _v08_manifest(
    payloads: Mapping[str, bytes],
    report: Mapping[str, Any],
    portable: Sequence[Mapping[str, Any]],
) -> dict[str, Any]:
    return {
        "format": "datacenter-atlas-verified-construction-core-preview-v8",
        "preview_id": CURRENT_V08_PREVIEW_ID,
        "release_status": "preview",
        "publishable_as_final": report["publishable_as_final"],
        "base_preview_id": "2026-08-20-preview-v0.7",
        "base_preview_manifest_sha256": LEGACY_PREVIEW_V07_MANIFEST_SHA256,
        "base_preview_commit": LEGACY_PREVIEW_V07_COMMIT,
        "source_release_id": SOURCE_RELEASE_ID,
        "source_release_manifest_path": "releases/2026-07-22-open-seed-v97/manifest.json",
        "source_release_manifest_sha256": "0a6f41f4239944df27f2ce70e81a089b91cec401f154bbae28412b27a4d00fdd",
        "definition_paths": {
            "imagery_reviews": "definitions/verified-construction-core-v0.8-imagery-reviews.json",
            "provenance": "definitions/verified-construction-core-v0.8-provenance.json",
            "reviewed_overlays": "definitions/verified-construction-core-reviewed-overlays-v6.json",
            "reviewed_sites": "definitions/verified-construction-core-v0.8-reviewed-sites.json",
        },
        "review_definition_sha256": V08_REVIEW_DEFINITION_SHA256,
        "imagery_review_definition_sha256": V08_IMAGERY_REVIEW_DEFINITION_SHA256,
        "provenance_definition_sha256": V08_PROVENANCE_DEFINITION_SHA256,
        "overlay_definition_sha256": V08_OVERLAY_DEFINITION_SHA256,
        "portable_source_inputs": list(portable),
        "reviewed_at": CURRENT_V08_REVIEW_DATE.isoformat(),
        "counts": {
            "physical_sites": 41,
            "projects": 44,
            "evidence": 100,
            "countries": 25,
            "non_us_sites": 35,
            "official_boundary_projects": 5,
            "reviewed_site_locator_projects": 39,
        },
        "files": {
            name: {"bytes": len(payload), "sha256": _sha256_bytes(payload)}
            for name, payload in sorted(payloads.items())
        },
    }


def build_preview(output_dir: Path = CURRENT_V08_PREVIEW_DIR) -> dict[str, Any]:
    """Build the current v0.8 preview from tracked, hash-bound inputs."""
    output_dir = Path(output_dir)
    if output_dir.exists():
        raise VerifiedConstructionCoreError(f"refusing to overwrite {output_dir}")
    payloads, report, portable = _v08_payloads()
    manifest = _v08_manifest(payloads, report, portable)
    manifest_bytes = _json_bytes(manifest)
    complete = {
        **payloads,
        "manifest.json": manifest_bytes,
        "manifest.sha256": f"{_sha256_bytes(manifest_bytes)}  manifest.json\n".encode(),
    }
    output_dir.parent.mkdir(parents=True, exist_ok=True)
    stage = Path(tempfile.mkdtemp(prefix=f".{output_dir.name}.", dir=output_dir.parent))
    try:
        for name, payload in complete.items():
            with (stage / name).open("xb") as handle:
                handle.write(payload)
        os.replace(stage, output_dir)
    except Exception:
        shutil.rmtree(stage, ignore_errors=True)
        raise
    validate_preview(output_dir)
    return manifest


def _validate_v08_preview(path: Path) -> dict[str, Any]:
    path = Path(path)
    if path.is_symlink() or not path.is_dir():
        raise VerifiedConstructionCoreError("v0.8 preview directory differs")
    manifest_path = path / "manifest.json"
    checksum_path = path / "manifest.sha256"
    if (
        manifest_path.is_symlink()
        or not manifest_path.is_file()
        or checksum_path.is_symlink()
        or not checksum_path.is_file()
    ):
        raise VerifiedConstructionCoreError("v0.8 manifest trust root differs")
    manifest_bytes = manifest_path.read_bytes()
    if checksum_path.read_bytes() != (
        f"{_sha256_bytes(manifest_bytes)}  manifest.json\n".encode()
    ):
        raise VerifiedConstructionCoreError("v0.8 manifest checksum differs")
    manifest = _load_json(manifest_path)
    files = manifest.get("files") if isinstance(manifest, dict) else None
    if not isinstance(files, dict):
        raise VerifiedConstructionCoreError("v0.8 manifest files differ")
    expected_inventory = set(files) | {"manifest.json", "manifest.sha256"}
    if {item.name for item in path.iterdir()} != expected_inventory:
        raise VerifiedConstructionCoreError("v0.8 preview inventory differs")
    for name, metadata in files.items():
        member = path / name
        if (
            Path(name).name != name
            or member.is_symlink()
            or not member.is_file()
            or member.stat().st_size != metadata.get("bytes")
            or _sha256_file(member) != metadata.get("sha256")
        ):
            raise VerifiedConstructionCoreError(f"v0.8 member differs: {name}")
    payloads, report, portable = _v08_payloads()
    if manifest != _v08_manifest(payloads, report, portable):
        raise VerifiedConstructionCoreError("v0.8 manifest semantics differ")
    for name, expected in payloads.items():
        if (path / name).read_bytes() != expected:
            raise VerifiedConstructionCoreError(
                f"v0.8 generated member differs: {name}"
            )
    return manifest


def validate_preview(path: Path = CURRENT_V08_PREVIEW_DIR) -> dict[str, Any]:
    """Validate current v0.8 or dispatch an immutable v0.1-v0.7 preview."""
    path = Path(path)
    manifest_path = path / "manifest.json"
    if (
        path.is_symlink()
        or not path.is_dir()
        or manifest_path.is_symlink()
        or not manifest_path.is_file()
    ):
        raise VerifiedConstructionCoreError("preview manifest trust root differs")
    manifest = _load_json(manifest_path)
    preview_id = manifest.get("preview_id") if isinstance(manifest, dict) else None
    if preview_id == CURRENT_V08_PREVIEW_ID:
        return _validate_v08_preview(path)
    validator = _frozen_preview_validator(preview_id)
    if validator is not None:
        return validator(path)
    raise VerifiedConstructionCoreError("preview id differs")


__all__ = [
    "CURRENT_V07_DEFINITION_PATHS",
    "CURRENT_V07_PREVIEW_DIR",
    "CURRENT_V07_PREVIEW_ID",
    "CURRENT_V07_REVIEW_DATE",
    "CURRENT_V08_DEFINITION_PATHS",
    "CURRENT_V08_PREVIEW_DIR",
    "CURRENT_V08_PREVIEW_ID",
    "CURRENT_V08_REVIEW_DATE",
    "IMAGERY_REVIEW_DEFINITION",
    "LEGACY_PREVIEW_V01_DIR",
    "LEGACY_PREVIEW_V02_DIR",
    "LEGACY_PREVIEW_V03_DIR",
    "LEGACY_PREVIEW_V04_DIR",
    "LEGACY_PREVIEW_V05_COMMIT",
    "LEGACY_PREVIEW_V05_DIR",
    "LEGACY_PREVIEW_V06_COMMIT",
    "LEGACY_PREVIEW_V06_DIR",
    "LEGACY_PREVIEW_V06_MANIFEST_SHA256",
    "LEGACY_PREVIEW_V07_COMMIT",
    "LEGACY_PREVIEW_V07_DIR",
    "LEGACY_PREVIEW_V07_MANIFEST_SHA256",
    "PREVIEW_DIR",
    "PreviewProfile",
    "PROVENANCE_DEFINITION",
    "VerifiedConstructionCoreError",
    "build_preview",
    "load_current_v07_profile",
    "load_current_v08_profile",
    "validate_frozen_preview",
    "validate_frozen_v01",
    "validate_frozen_v02",
    "validate_frozen_v03",
    "validate_frozen_v04",
    "validate_frozen_v05",
    "validate_frozen_v06",
    "validate_frozen_v07",
    "validate_preview",
]


LEGACY_PREVIEW_V08_DIR = (
    ROOT / "verified_construction_core" / "2026-08-20-preview-v0.8"
)
LEGACY_PREVIEW_V08_MANIFEST_SHA256 = (
    "a916aa4a438510ac5c1c26b359c7581dc18ca55de4c40ffd59c93424cad70b08"
)
LEGACY_PREVIEW_V08_COMMIT = "26782bec3107fdbc778af2b57aa425f013a6a243"
CURRENT_V09_PREVIEW_ID = "2026-08-20-preview-v0.9"
CURRENT_V09_PREVIEW_DIR = ROOT / "verified_construction_core" / CURRENT_V09_PREVIEW_ID
CURRENT_V09_REVIEW_DATE = date(2026, 8, 20)
V09_REVIEW_DEFINITION_SHA256 = (
    "6bb8b0e3863dea07ff1edac91dfffbd7f7efdd5531506ceb6dd8fd8c379ca7bf"
)
V09_OVERLAY_DEFINITION_SHA256 = (
    "cc15e0d3c933411f6eece37c1506f95213463d92f061b70c522ddf3660b098a6"
)
V09_PROVENANCE_DEFINITION_SHA256 = (
    "ae18a4ff01960de5806838eab338e6e32c5b16d52ac4a1a0f4d85796314c6f38"
)
V09_IMAGERY_REVIEW_DEFINITION_SHA256 = (
    "384897196dd72a916c2f3963ce616e646578c067879e7aaf4a91801aa31749b7"
)
V09_REVIEW_DEFINITION = (
    ROOT / "definitions" / "verified-construction-core-v0.9-reviewed-sites.json"
)
V09_OVERLAY_DEFINITION = (
    ROOT / "definitions" / "verified-construction-core-reviewed-overlays-v7.json"
)
V09_PROVENANCE_DEFINITION = (
    ROOT / "definitions" / "verified-construction-core-v0.9-provenance.json"
)
V09_IMAGERY_REVIEW_DEFINITION = (
    ROOT / "definitions" / "verified-construction-core-v0.9-imagery-reviews.json"
)
CURRENT_V09_DEFINITION_PATHS = (
    ("review_definition_sha256", V09_REVIEW_DEFINITION),
    ("imagery_review_definition_sha256", V09_IMAGERY_REVIEW_DEFINITION),
    ("provenance_definition_sha256", V09_PROVENANCE_DEFINITION),
    ("overlay_definition_sha256", V09_OVERLAY_DEFINITION),
)
V09_COUNTRY_ISO_A2 = {
    "Australia": "AU",
    "Germany": "DE",
    "United States": "US",
}
V09_SELECTION_FIRST_FAILURE_COUNTS = {
    "entity_kind_not_project": 49,
    "not_in_reviewed_site_geometry_allowlist": 96,
    "selected": 47,
    "status_not_physical": 12,
    "status_outside_90_day_window": 327,
}
V09_ACCEPTANCE_REQUIRED_FIELDS = frozenset(
    {
        "bridge_bytes",
        "bridge_decision",
        "bridge_id",
        "bridge_path",
        "bridge_schema",
        "bridge_sha256",
        "country",
        "decision_basis",
        "geometry_authority_class",
        "geometry_derivation",
        "geometry_entity",
        "geometry_entity_id",
        "geometry_entity_stable_key",
        "geometry_evidence_id",
        "geometry_evidence_key",
        "geometry_method",
        "geometry_overlay_id",
        "geometry_scope_class",
        "geometry_source_kind",
        "geometry_target_entity_id",
        "geometry_target_entity_kind",
        "geometry_target_entity_stable_key",
        "geometry_use_scope",
        "horizontal_uncertainty_metres",
        "horizontal_uncertainty_unknown_reason",
        "official_boundary",
        "physical_site_entity_id",
        "physical_site_stable_key",
        "portable_capture_bindings",
        "portable_input_binding",
        "precision_scope",
        "project_entity_id",
        "project_stable_key",
        "project_to_campus_decision_basis",
        "project_to_campus_relationship_id",
        "project_to_campus_relationship_type",
        "rejected_claims",
        "source_input_bytes",
        "source_input_path",
        "source_input_sha256",
    }
)
V09_OVERLAY_REQUIRED_FIELDS = frozenset(
    {
        "bridge_bytes",
        "bridge_decision",
        "bridge_id",
        "bridge_path",
        "bridge_schema",
        "bridge_sha256",
        "country",
        "decision",
        "decision_reason",
        "geometry_authority_class",
        "geometry_derivation",
        "geometry_entity",
        "geometry_entity_id",
        "geometry_entity_stable_key",
        "geometry_evidence_id",
        "geometry_evidence_key",
        "geometry_method",
        "geometry_scope_class",
        "geometry_source_kind",
        "geometry_target_entity_id",
        "geometry_target_entity_kind",
        "geometry_target_entity_stable_key",
        "geometry_use_scope",
        "horizontal_uncertainty_metres",
        "horizontal_uncertainty_unknown_reason",
        "official_boundary",
        "overlay_id",
        "physical_site_entity_id",
        "physical_site_stable_key",
        "portable_capture_bindings",
        "portable_input_binding",
        "precision_scope",
        "project_to_campus_decision_basis",
        "project_to_campus_relationship_id",
        "project_to_campus_relationship_type",
        "rejected_claims",
        "reviewed_at",
        "source_input_bytes",
        "source_input_path",
        "source_input_sha256",
        "source_project_entity_id",
        "source_project_stable_key",
    }
)
V09_BRIDGE_PROFILES = {
    "curated:cdc-marsden-park-campus:early-construction": {
        "bridge_path": "sources/verified-construction-core-v0.9-cdc-marsden-park-campus-geometry-bridge.json",
        "bridge_bytes": 26002,
        "bridge_sha256": "7624792e77b57807ff648c1c40e771e8fa9a72d603919dcf25b2e8f1ed436a69",
        "source_path": "sources/curated-official-2026-07-21-cdc-marsden-park-current-build.json",
        "source_bytes": 3529,
        "source_sha256": "8ef701ac0b4346aed089996487a149dd1aea2545ce11b0b30c53faf5a1807322",
        "schema": "verified-construction-core-v0.9-capture-replayed-campus-geometry-bridge-v1",
        "acceptance_sha256": "5507696f5b9a2d05e3323e4e84a9be55c40d9187e356472660e2384fb132b1ca",
        "overlay_sha256": "16508defbb1120f249c7061e0ae399051aa5b73779b38657c9c5d6a767674136",
        "portable_capture_bindings_sha256": "667841409f2ed7ff7df561c9b0eb0afdde6e9e95f9cf95a2c810ca67a94b2e48",
        "geometry_projection_sha256": "98c6db5cbf9d4b901086d16389956f962ae173518c312c7c923897dbf63f5ab0",
        "rights_sha256": "b2716a97317a7da820ed737d36f7c750c00718d4438806f913e0b136a4d6bc23",
        "review_decision_sha256": "204e20e56c53f67945219f807eda699c6f2a3384dad0ad14e4506a4aa2a62745",
        "claim_guardrails_sha256": "f1fd260abc11afda35b0fe887a50a62c73dc7fa1923625cbeb41bd16c171095d",
        "imagery_posture_sha256": "35220474a47d462ded125e8bffaa271f4a40027bdd2de3d0a9e91384bbc79134",
        "geometry_capture_sha256": "3eac26c920d686461b41782def2d1dd0dff02c79b1fa8814b5c8e4f7ae7da5cb",
        "identity_bridge_evidence_sha256": "37517e5f3dc66819f61f5a7bb8ace1921282415f10551d2defa5c3eb0985b570",
        "lineage_context_sha256": "38e0b9de817f645c4bec37c0d4a3e58baecccb040f5718dc069a72c7385a0bed",
        "geometry_kind": "facility",
        "geometry_stable_key": "osm:way/1336972876",
        "geometry_entity_id": "74d3226e-ce5e-50bd-9258-3bb57fbf6310",
        "target_kind": "campus",
        "target_stable_key": "curated:cdc-marsden-park-campus",
        "target_entity_id": "9d945d35-0ede-57a5-8b37-42789dd1a630",
        "geometry_evidence_id": "ce28239d-1c35-5043-9373-97416961a743",
        "geometry_evidence_key": "osm-way-1336972876-cdc-marsden-park-campus-captured-2026-08-20",
        "geometry_source_kind": "open_map_geometry",
        "longitude": 150.8363658,
        "latitude": -33.72368555,
    },
    "curated:digital-realty-330-east-cermak-chicago:current-facility-build": {
        "bridge_path": "sources/verified-construction-core-v0.9-digital-realty-330-east-cermak-project-geometry-bridge.json",
        "bridge_bytes": 37769,
        "bridge_sha256": "5b32961df7601c828c9e2892ccd25ad0d7f4bf466569c7e7d6af48def8ac278a",
        "source_path": "sources/curated-official-2026-07-19-digital-realty-330-east-cermak-chicago.json",
        "source_bytes": 4477,
        "source_sha256": "0fecb93df76c3a2f4ad276da3b3fc5710551f843109ea9f43971a7d57a1fa8a5",
        "schema": "verified-construction-core-v0.9-capture-geometry-bridge-v1",
        "acceptance_sha256": "bd374a4d6385e761034d5087ef4702f0cdab9e4bc06720d9abc9d143a23ab25c",
        "overlay_sha256": "dcdee508cf60456033e96a70828054dbd6486096489f7c7ce99fffca3bcbb2b3",
        "portable_capture_bindings_sha256": "9287d6a09d2e1a5f73d29a593ca5abf4e8c08930f83bad2d4d60805423ac55b2",
        "geometry_projection_sha256": "b40f8854a7e1fe63953347347ec46924bcfcbbbb62d814a8d22f88182971ebc0",
        "rights_sha256": "923505b5808b159ed397833f7ae8af947ad7c3053dc1b50f8ad913750f1b8a04",
        "review_decision_sha256": "bb76c5ce8e0a12b1c808be435b2c93cf6549774645909d30b331ab86b51a83e4",
        "claim_guardrails_sha256": "1c5f9d1c6c0a4bc34e80a09f4108a872b693c96fb873bb2bbd17d67cca4abac6",
        "imagery_posture_sha256": "cf3a5a5f1100ce1b6972d1ba83f645b119307fe1b6c5ce91ba50484d84816493",
        "geometry_capture_sha256": "028db8d17e850a69fe8592823e6f61fe478bbbec982eed9068da88d8925630ac",
        "identity_bridge_evidence_sha256": "0d805f69e6c74395753ebc1519f7d49c01eafaec7895f66b08e706c4f69cb20b",
        "lineage_context_sha256": "a6ad7cac170bd2f790ec84f584c51bb1f6969b70fcc36e099c4a9a5671c79535",
        "geometry_kind": "building",
        "geometry_stable_key": "osm:way/210537873",
        "geometry_entity_id": "4dd573ab-0e31-5570-a57b-e580ea1da51d",
        "target_kind": "project",
        "target_stable_key": "curated:digital-realty-330-east-cermak-chicago:current-facility-build",
        "target_entity_id": "01baa3bb-ea56-5f41-8c67-82dd84f87d51",
        "geometry_evidence_id": "b2a12b58-767d-5517-9d63-a5d6fc6e9426",
        "geometry_evidence_key": None,
        "geometry_source_kind": "openstreetmap",
        "longitude": -87.6195243,
        "latitude": 41.85322925,
    },
    "curated:ntt-frankfurt-1-campus:7-3mw-expansion": {
        "bridge_path": "sources/verified-construction-core-v0.9-ntt-fra1-expansion-campus-geometry-bridge.json",
        "bridge_bytes": 45027,
        "bridge_sha256": "f2e3be5faf8065fc0bc449e2118b5d4d6d1fe3700672bee879c8a15bf052665e",
        "source_path": "sources/curated-official-2026-07-19-ntt-frankfurt-1.json",
        "source_bytes": 5913,
        "source_sha256": "6d5451e1139123dc290522e156ea1cfc46ec737a1d9561e44c8e5a8a1150cf29",
        "schema": "verified-construction-core-v0.9-global-v3-geometry-bridge-v1",
        "acceptance_sha256": "3d555ad6f556700d79422a2d1509126646b43fc4e7572b0c2fea2f3dd59ecf87",
        "overlay_sha256": "f6d5212b28fb8b5a99186772caf456b234c1b88f520775f2d38c9d6b72a124e2",
        "portable_capture_bindings_sha256": "37517e5f3dc66819f61f5a7bb8ace1921282415f10551d2defa5c3eb0985b570",
        "geometry_projection_sha256": "370f45162ce708420f7414ea2c5a59d6556329b5fd03daae939e76902e9130b5",
        "rights_sha256": "28fda9bb4d27f9e899d55b8d63f2240cbfad4a8304b49e07751c133c93181686",
        "review_decision_sha256": "09afc184d93aaa13413736d9697d59e6316b3af78ffc8d247de07480156aa337",
        "claim_guardrails_sha256": "f4ce43ab269270712cfc4d582bfcae6ea4b0fc494a4b2dfa070a52e1cca6d190",
        "imagery_posture_sha256": "cf3a5a5f1100ce1b6972d1ba83f645b119307fe1b6c5ce91ba50484d84816493",
        "geometry_capture_sha256": "38e0b9de817f645c4bec37c0d4a3e58baecccb040f5718dc069a72c7385a0bed",
        "identity_bridge_evidence_sha256": "e68595dece4818a9f2453e34552af83d30cd166024e98b3672447f30c26c4290",
        "lineage_context_sha256": "38e0b9de817f645c4bec37c0d4a3e58baecccb040f5718dc069a72c7385a0bed",
        "geometry_kind": "facility",
        "geometry_stable_key": "osm:way/697501723",
        "geometry_entity_id": "0b5e2a41-add7-5192-954f-df460cc6c04f",
        "target_kind": "campus",
        "target_stable_key": "curated:ntt-frankfurt-1-campus",
        "target_entity_id": "dd70ea3f-898c-5a05-8ba8-cdda76f0991e",
        "geometry_evidence_id": "eb188ade-b0ff-5b98-bbd4-83cee821ca4b",
        "geometry_evidence_key": None,
        "geometry_source_kind": "openstreetmap",
        "longitude": 8.6019867,
        "latitude": 50.129269550000004,
    },
}
V09_SOURCE_PROFILES = {
    "curated:cdc-marsden-park-campus:early-construction": {
        "source_schema_version": "1.1",
        "source_semantic_sha256": "de2271f7a5846dd73a45d666936b23840a81b16a8787a19b07d1fb5fe853100d",
        "release_sha256": "769d30ceb091cf84376cb4744959b75415209bbff173947f8ebfa1cffd80a377",
        "v97_rows_sha256": "c13f124d984a7d2e8f7e398e33559a5117436ba2d29918cae9df52a858737dd5",
        "topology_sha256": "581c72b8cfeb67cc28970564d882e4e89c473cd51ffa00445b17c9e30b5d2ffc",
        "used_evidence_ids": ("56fa3df3-1fe1-50af-80c1-0e2f0f14d610",),
        "used_evidence_sha256": "bc6b7ea92ab282d9e02c3fb3bd318e52347c394cc751e6e73862189b5e487e0f",
    },
    "curated:digital-realty-330-east-cermak-chicago:current-facility-build": {
        "source_schema_version": "1.0",
        "source_semantic_sha256": "e3e61d68df94ea0d676a9ad26a4bfcbd167350d1b5a20255214c542bb621a8f5",
        "release_sha256": "769d30ceb091cf84376cb4744959b75415209bbff173947f8ebfa1cffd80a377",
        "v97_rows_sha256": "d7b22d18d1bd0c549eff1f23d9275d4ab5ff11ce024aeab601591ad39de4d1ba",
        "topology_sha256": "6630d2cefb3774605b6f4e4fdcdaa15d1f08d7e7e01349a3af67ec0c9e2505f1",
        "used_evidence_ids": ("9f47299f-1ce7-579f-b0b8-1bd454faabd5",),
        "used_evidence_sha256": "a276c0882c09486b89f546d21e7d526f8436a4f85237eae78476fd2b2fa6073a",
    },
    "curated:ntt-frankfurt-1-campus:7-3mw-expansion": {
        "source_schema_version": "1.0",
        "source_semantic_sha256": "6d7a36c062c74acc14be9c640a2395d26d152f9abef6a37afe42667c86bcde56",
        "release_sha256": "769d30ceb091cf84376cb4744959b75415209bbff173947f8ebfa1cffd80a377",
        "v97_rows_sha256": "8049385c0b5b254a4918059e25d1e1bb2144ba4fae91a83b9c460413f21e1d3c",
        "topology_sha256": "fc3b3c66cb7abe1a3695faa219b63cad2b6cc32c39aab90fd5110eb71bce87d1",
        "used_evidence_ids": ("603f155d-0c9d-5428-8e24-36a685af8aa7",),
        "used_evidence_sha256": "a27504b9e5a07b2b05d1861ad92ab0cdc719e5d09712fa74df387774bac0f39a",
    },
}
V09_EVIDENCE_IDS = frozenset(
    {
        "1df077b4-ffa3-5938-a5fe-88d5b4028ad4",
        "39cdd4b0-60f0-5d50-9a38-59d72c0a9d53",
        "56fa3df3-1fe1-50af-80c1-0e2f0f14d610",
        "603f155d-0c9d-5428-8e24-36a685af8aa7",
        "9f47299f-1ce7-579f-b0b8-1bd454faabd5",
        "b2a12b58-767d-5517-9d63-a5d6fc6e9426",
        "ce28239d-1c35-5043-9373-97416961a743",
        "eb188ade-b0ff-5b98-bbd4-83cee821ca4b",
    }
)
V09_PROVENANCE_SEMANTICS_SHA256 = (
    "d8692818c8ec5143b0b914045d2f99c80afe05133f2a79f47e93c22bc0bb2af0"
)
V09_CITY_TERMS_URL = (
    "https://www.chicago.gov/city/en/narr/foia/data_disclaimer.html"
)
V09_CITY_NOTICE_SHA256 = (
    "307e6fa3d02f49ba5d7532ec3d8f82c1e5c614e03c6c09e4fe1c0d9598e1dbb6"
)
V09_CITY_RIGHTS_CONTRACT_SHA256 = (
    "ea3015e49c67045646b78aecc992eb91e61a6be45f4c9802d1ab65b8433a5ebd"
)


def load_current_v09_profile(
    definition_sha256: Mapping[str, str],
) -> PreviewProfile:
    """Load v0.9 only after all exact definition hashes are supplied."""
    expected_fields = {field for field, _ in CURRENT_V09_DEFINITION_PATHS}
    if set(definition_sha256) != expected_fields:
        raise VerifiedConstructionCoreError(
            "current v0.9 definition pins are incomplete"
        )
    pins: list[tuple[str, Path, str]] = []
    for field, path in CURRENT_V09_DEFINITION_PATHS:
        expected_sha256 = definition_sha256[field]
        if re.fullmatch(r"[0-9a-f]{64}", expected_sha256) is None:
            raise VerifiedConstructionCoreError(f"current v0.9 {field} pin is invalid")
        if path.is_symlink() or not path.is_file() or _sha256_file(path) != expected_sha256:
            raise VerifiedConstructionCoreError(
                f"current v0.9 {field} source hash differs"
            )
        pins.append((field, path, expected_sha256))
    return PreviewProfile(
        preview_id=CURRENT_V09_PREVIEW_ID,
        preview_dir=CURRENT_V09_PREVIEW_DIR,
        reviewed_at=CURRENT_V09_REVIEW_DATE,
        base_preview_id=CURRENT_V08_PREVIEW_ID,
        base_preview_dir=LEGACY_PREVIEW_V08_DIR,
        base_manifest_sha256=LEGACY_PREVIEW_V08_MANIFEST_SHA256,
        base_commit=LEGACY_PREVIEW_V08_COMMIT,
        definition_pins=tuple(pins),
    )


def validate_frozen_v08(
    path: Path = LEGACY_PREVIEW_V08_DIR,
) -> dict[str, Any]:
    """Validate byte-frozen v0.8 using only its immutable inventory pins."""
    return _validate_frozen_preview_inventory(
        path,
        preview_id=CURRENT_V08_PREVIEW_ID,
        manifest_sha256=LEGACY_PREVIEW_V08_MANIFEST_SHA256,
        version_label="v0.8",
    )


_FROZEN_PREVIEW_VALIDATORS = (
    *_FROZEN_PREVIEW_VALIDATORS,
    ("2026-08-20-preview-v0.8", validate_frozen_v08),
)


def _v09_definition_paths() -> dict[str, tuple[Path, str]]:
    return {
        "review_definition_sha256": (
            V09_REVIEW_DEFINITION,
            V09_REVIEW_DEFINITION_SHA256,
        ),
        "imagery_review_definition_sha256": (
            V09_IMAGERY_REVIEW_DEFINITION,
            V09_IMAGERY_REVIEW_DEFINITION_SHA256,
        ),
        "provenance_definition_sha256": (
            V09_PROVENANCE_DEFINITION,
            V09_PROVENANCE_DEFINITION_SHA256,
        ),
        "overlay_definition_sha256": (
            V09_OVERLAY_DEFINITION,
            V09_OVERLAY_DEFINITION_SHA256,
        ),
    }


def _validate_v09_definition_pins() -> None:
    for field, (path, expected) in _v09_definition_paths().items():
        if path.is_symlink() or not path.is_file() or _sha256_file(path) != expected:
            raise VerifiedConstructionCoreError(
                f"current v0.9 {field} source hash differs"
            )


def _v09_hydrated_crosscheck_available() -> bool:
    required = (
        ROOT / "releases/2026-07-22-open-seed-v97/construction_pipeline.csv",
        ROOT / "releases/2026-07-22-open-seed-v97/entities.csv",
        ROOT / "releases/2026-07-22-open-seed-v97/evidence.csv",
        ROOT / "releases/2026-07-18-global-open-v3/entities.csv",
        ROOT / "releases/2026-07-18-global-open-v3/evidence.csv",
        ROOT
        / "exact_identity_decisions/2026-07-22-public-open-v14/component-members.csv",
        ROOT
        / "exact_identity_decisions/2026-07-22-public-open-v14/relationships.csv",
    )
    present = [path.is_file() for path in required]
    if any(present) and not all(present):
        raise VerifiedConstructionCoreError(
            "v0.9 hydrated cross-check inputs are only partially present"
        )
    return all(present)
_build_v08_preview = build_preview
_validate_v08_preview_dispatch = validate_preview


def _v09_validate_construction_source(
    bridge: Mapping[str, Any],
    acceptance: Mapping[str, Any],
    *,
    hydrated_crosscheck: bool,
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    project_key = acceptance["project_stable_key"]
    source_profile = V09_SOURCE_PROFILES.get(project_key)
    if source_profile is None:
        raise VerifiedConstructionCoreError("v0.9 source profile differs")
    construction = bridge.get("construction_source")
    expected_fields = {
        "input",
        "release",
        "release_rows",
        "project",
        "campus",
        "project_to_campus",
        "status_evidence",
    }
    if not isinstance(construction, dict) or set(construction) != expected_fields:
        raise VerifiedConstructionCoreError("v0.9 construction source differs")
    if (
        _sha256_bytes(_json_bytes(_v08_construction_row_bindings(construction)))
        != source_profile["v97_rows_sha256"]
        or _sha256_bytes(_json_bytes(construction.get("project_to_campus")))
        != source_profile["topology_sha256"]
    ):
        raise VerifiedConstructionCoreError("v0.9 immutable source profile differs")
    expected_input = {
        "path": acceptance["source_input_path"],
        "bytes": acceptance["source_input_bytes"],
        "sha256": acceptance["source_input_sha256"],
    }
    if (
        construction.get("input") != expected_input
        or acceptance.get("portable_input_binding") != expected_input
    ):
        raise VerifiedConstructionCoreError("v0.9 source input binding differs")
    source_path = _repository_input(
        expected_input["path"], expected_input["sha256"], "v0.9 source input"
    )
    if source_path.stat().st_size != expected_input["bytes"]:
        raise VerifiedConstructionCoreError("v0.9 source input byte count differs")
    source = _load_json(source_path)
    expected_source_fields = {
        "schema_version",
        "project",
        "campus",
        "evidence",
        "lifecycle",
        "operating_models",
        "workloads",
        "capacities",
    }
    if (
        not isinstance(source, dict)
        or set(source) != expected_source_fields
        or source.get("schema_version") != source_profile["source_schema_version"]
        or _sha256_bytes(_json_bytes(source))
        != source_profile["source_semantic_sha256"]
    ):
        raise VerifiedConstructionCoreError("v0.9 source record differs")
    source_evidence = source.get("evidence")
    if not isinstance(source_evidence, list) or not source_evidence:
        raise VerifiedConstructionCoreError("v0.9 source evidence differs")
    evidence_by_key = {
        row["key"]: row
        for row in source_evidence
        if isinstance(row, dict) and isinstance(row.get("key"), str)
    }
    if len(evidence_by_key) != len(source_evidence):
        raise VerifiedConstructionCoreError("v0.9 source evidence differs")
    expected_project = _v08_source_entity_projection(
        source, evidence_by_key, entity_kind="project"
    )
    expected_campus = _v08_source_entity_projection(
        source, evidence_by_key, entity_kind="campus"
    )
    project = construction.get("project")
    campus = construction.get("campus")
    if not isinstance(project, dict) or not isinstance(campus, dict):
        raise VerifiedConstructionCoreError("v0.9 construction entities differ")
    for actual, expected, source_entity, label in (
        (project, expected_project, source["project"], "project"),
        (campus, expected_campus, source["campus"], "campus"),
    ):
        for field, value in expected.items():
            if actual.get(field) != value:
                raise VerifiedConstructionCoreError(
                    f"v0.9 construction {label} projection differs"
                )
        roles = _v08_normalized_roles(source_entity)
        if actual.get("normalized_roles") != roles:
            raise VerifiedConstructionCoreError(
                f"v0.9 construction {label} roles differ"
            )
    if (
        project.get("stable_key") != acceptance["project_stable_key"]
        or project.get("entity_id") != acceptance["project_entity_id"]
        or campus.get("stable_key") != acceptance["physical_site_stable_key"]
        or campus.get("entity_id") != acceptance["physical_site_entity_id"]
        or project.get("country") != acceptance["country"]
        or campus.get("country") != acceptance["country"]
    ):
        raise VerifiedConstructionCoreError(
            "v0.9 publication target projection differs"
        )
    if "status" in campus and any(
        campus.get(field) is not None
        for field in (
            "status",
            "status_as_of",
            "status_age_days_at_review",
            "status_method",
            "status_confidence",
            "status_evidence_id",
        )
    ):
        raise VerifiedConstructionCoreError("v0.9 campus status leakage differs")
    status = construction.get("status_evidence")
    evidence_pool = _v08_source_evidence(source)
    status_id = expected_project["status_evidence_id"]
    if not isinstance(status, dict) or status.get("evidence_id") != status_id:
        raise VerifiedConstructionCoreError("v0.9 status evidence differs")
    expected_status = evidence_pool.get(status_id)
    if expected_status is None or any(
        ("" if status.get(field) is None else status.get(field))
        != expected_status[field]
        for field in GLOBAL_GEOMETRY_EVIDENCE_FIELDS
    ):
        raise VerifiedConstructionCoreError("v0.9 status evidence projection differs")
    release = construction.get("release")
    if (
        not isinstance(release, dict)
        or _sha256_bytes(_json_bytes(release))
        != source_profile["release_sha256"]
        or release.get("release_id") != SOURCE_RELEASE_ID
        or set(release.get("members", {}))
        != {"construction_pipeline.csv", "entities.csv", "evidence.csv"}
    ):
        raise VerifiedConstructionCoreError("v0.9 source release differs")
    _, manifest = _manifest_binding(release["manifest"], label="v0.9 source release")
    _validate_manifest_members(manifest, release["members"], label="v0.9 source release")
    raw_rows = _v09_validate_embedded_construction_rows(
        bridge, hydrated_crosscheck=hydrated_crosscheck
    )
    for entity_kind, actual in (("project", project), ("campus", campus)):
        raw_projection = _v09_csv_entity_projection(
            raw_rows[entity_kind], entity_kind=entity_kind
        )
        if _v08_actual_entity_projection(
            actual, entity_kind=entity_kind
        ) != raw_projection:
            raise VerifiedConstructionCoreError(
                f"v0.9 v97 {entity_kind} projection differs"
            )
    used_evidence_ids = tuple(source_profile["used_evidence_ids"])
    if tuple(sorted(used_evidence_ids)) != used_evidence_ids:
        raise VerifiedConstructionCoreError("v0.9 source evidence profile differs")
    try:
        used_evidence = [evidence_pool[evidence_id] for evidence_id in used_evidence_ids]
    except KeyError as error:
        raise VerifiedConstructionCoreError(
            "v0.9 used source evidence differs"
        ) from error
    if (
        _sha256_bytes(_json_bytes(used_evidence))
        != source_profile["used_evidence_sha256"]
    ):
        raise VerifiedConstructionCoreError("v0.9 used source evidence differs")
    for evidence_id, row in raw_rows["evidence"].items():
        projected = evidence_pool.get(evidence_id)
        if projected is None or any(
            projected[field] != row[field]
            for field in GLOBAL_GEOMETRY_EVIDENCE_FIELDS
        ):
            raise VerifiedConstructionCoreError(
                "v0.9 embedded evidence projection differs"
            )
    if hydrated_crosscheck:
        hydrated_evidence = _v08_hydrated_evidence_rows(
            release, used_evidence_ids
        )
        for evidence_id, projected in zip(
            used_evidence_ids, used_evidence, strict=True
        ):
            if any(
                projected[field] != hydrated_evidence[evidence_id][field]
                for field in GLOBAL_GEOMETRY_EVIDENCE_FIELDS
            ):
                raise VerifiedConstructionCoreError(
                    "v0.9 hydrated evidence projection differs"
                )
    _validate_v07_topology(
        bridge,
        project,
        campus,
        hydrated_crosscheck=hydrated_crosscheck,
    )
    relationship = construction["project_to_campus"]["relationship"]
    if (
        relationship.get("relationship_id")
        != acceptance["project_to_campus_relationship_id"]
        or relationship.get("relationship_type")
        != acceptance["project_to_campus_relationship_type"]
        or relationship.get("decision_basis")
        != acceptance["project_to_campus_decision_basis"]
    ):
        raise VerifiedConstructionCoreError("v0.9 topology definition differs")
    return project, campus, source


def _v09_validate_embedded_construction_rows(
    bridge: Mapping[str, Any], *, hydrated_crosscheck: bool
) -> dict[str, Any]:
    construction = bridge["construction_source"]
    release = construction["release"]
    embedded = construction.get("release_rows")
    project_key = construction["project"]["stable_key"]
    evidence_label = (
        "evidence_status"
        if project_key
        == "curated:digital-realty-330-east-cermak-chicago:current-facility-build"
        else "evidence_snapshot"
    )
    expected_inventory = {
        "construction_pipeline_project",
        "entities_project",
        "entities_campus",
        evidence_label,
    }
    if not isinstance(embedded, dict) or set(embedded) != expected_inventory:
        raise VerifiedConstructionCoreError("v0.9 release-row inventory differs")
    _validate_v06_embedded_release_rows(
        embedded,
        release["members"],
        hydrated_crosscheck=hydrated_crosscheck,
    )
    project_row = _embedded_csv_source_row(
        embedded["construction_pipeline_project"]["source_row"],
        SOURCE_ENTITY_FIELDS,
        label="v0.9 construction pipeline project",
    )
    entities_project_row = _embedded_csv_source_row(
        embedded["entities_project"]["source_row"],
        SOURCE_ENTITY_FIELDS,
        label="v0.9 entities project",
    )
    campus_row = _embedded_csv_source_row(
        embedded["entities_campus"]["source_row"],
        SOURCE_ENTITY_FIELDS,
        label="v0.9 entities campus",
    )
    evidence_row = _embedded_csv_source_row(
        embedded[evidence_label]["source_row"],
        GLOBAL_GEOMETRY_EVIDENCE_FIELDS,
        label="v0.9 status evidence",
    )
    if (
        project_row != entities_project_row
        or project_row["stable_key"] != construction["project"]["stable_key"]
        or campus_row["stable_key"] != construction["campus"]["stable_key"]
        or evidence_row["evidence_id"]
        != construction["status_evidence"]["evidence_id"]
    ):
        raise VerifiedConstructionCoreError("v0.9 embedded source projection differs")
    return {
        "project": project_row,
        "campus": campus_row,
        "evidence": {evidence_row["evidence_id"]: evidence_row},
    }


def _v09_csv_entity_projection(
    row: Mapping[str, str], *, entity_kind: str
) -> dict[str, Any]:
    if entity_kind == "project":
        return _v08_csv_entity_projection(row, entity_kind=entity_kind)
    lifecycle_fields = (
        "status",
        "status_as_of",
        "status_confidence",
        "status_method",
        "status_evidence_id",
    )
    observed_lifecycle = tuple(row[field] for field in lifecycle_fields)
    if row.get("stable_key") == "curated:ntt-frankfurt-1-campus":
        expected_lifecycle = (
            "operational",
            "2026-07-19",
            "0.99",
            "authoritative_status_update",
            "603f155d-0c9d-5428-8e24-36a685af8aa7",
        )
    else:
        expected_lifecycle = ("", "", "", "", "")
    if observed_lifecycle != expected_lifecycle:
        raise VerifiedConstructionCoreError("v0.9 v97 campus lifecycle differs")
    lifecycle_stripped = dict(row)
    for field in lifecycle_fields:
        lifecycle_stripped[field] = ""
    return _v08_csv_entity_projection(
        lifecycle_stripped, entity_kind="campus"
    )


V09_CAPTURE_PROFILES = {
    "verified-construction-core-v0.9-cdc-marsden-park-osm-way-1336972876-full-capture": {
        "path": "sources/verified-construction-core-v0.9-cdc-marsden-park-osm-way-1336972876-full-capture.json",
        "bytes": 6885,
        "sha256": "94bef2d27fb856bc928e0bad6ff9ed448e9aa5bf96dda56ab07638400aebc9e6",
        "semantic_sha256": "64e05510f09ec8a8f33c441f3a20810528fb567a7c03ad934fcec93fc7a3e23c",
        "capture_role": "selected_geometry_replay",
        "evidence_id": "ce28239d-1c35-5043-9373-97416961a743",
        "raw_response_bytes": 2178,
        "raw_response_sha256": "f176dfe0fd79fca768f9290dc5aa2204df379204403753640b3d48015b0fe8d8",
        "way_id": 1336972876,
        "way_version": 4,
        "raw_way_sha256": "274ec82649bd77b504e5de809f6105a94e0c89994ed14d5cd682a30c4f0255df",
        "enriched_way_sha256": "7477b09c1453cf64065ba7dd7078ae531a291e300aba390c05b051a44652e0e9",
        "polygon_sha256": "2229969f413dd9dd53f2acb00efd1f2eb5a8057dbdfc149d3b636273831c4e48",
        "fact_payload_sha256": "50fb7f5e461314fd7d0fa3a7d1339e724ceb6603a6b9692a02bae2d208ab0684",
    },
    "verified-construction-core-v0.9-digital-realty-330-east-cermak-osm-way-210537873-v6-capture": {
        "path": "sources/verified-construction-core-v0.9-digital-realty-330-east-cermak-osm-way-210537873-v6-capture.json",
        "bytes": 11755,
        "sha256": "38581e896bfd89edeb87ea5c5329a66f2b0b29edc5fa8e96ee39221aac39a73e",
        "semantic_sha256": "080dcc68cecc6da250f5edb6ebc463dc65a550739508992509b98d291558b57b",
        "capture_role": "selected_geometry_replay",
        "evidence_id": "b2a12b58-767d-5517-9d63-a5d6fc6e9426",
        "raw_response_bytes": 2599,
        "raw_response_sha256": "1e1e38e44ae5d4d3505e911e5430680bab3949759d0ca6693fe230cd263578b5",
        "way_id": 210537873,
        "way_version": 6,
        "raw_way_sha256": "1b0026e2d3c4941dc2248fc26ab516d4c337469a9e20a903a476c44c36060f7f",
        "enriched_way_sha256": "8f39e5ebd458a531152e933ac3b71cc105ad71692dd1da8920002d004570d5e9",
        "polygon_sha256": "61394750e1163f26668161d1fe5bdf127abcab2e4e5104c0c64455eebdee41f5",
    },
    "verified-construction-core-v0.9-digital-realty-330-east-cermak-city-building-footprint-871169-capture": {
        "path": "sources/verified-construction-core-v0.9-digital-realty-330-east-cermak-city-building-footprint-871169-capture.json",
        "bytes": 11987,
        "sha256": "83ed9380f77b3ee0ffa91c4c6ea5cb2a9b7a7ec8e49b340058f883b01c6420c1",
        "semantic_sha256": "1b13acf24e0e83844a1d79f7de2c8bf5dfdffb1b9a6b1c9e460e130c3f95e811",
        "capture_role": "identity_context",
        "evidence_id": "39cdd4b0-60f0-5d50-9a38-59d72c0a9d53",
        "raw_response_bytes": 1086,
        "raw_response_sha256": "9d9838b72d74b3be5db4c5c0cbad94a866ad31c2af0ed623bdef68e2e2b5feaa",
        "geometry_sha256": "7ca2ddfd1720555ccb326fa900cdb30ff8c3fad7c2e7915655b66282398018b9",
        "properties_sha256": "bb37677f310c3b56a96c764e49ba8c7f0e9eae5025f83340b3774c600557ad6e",
    },
}
V09_NTT_IDENTITY_FACT_SHA256 = (
    "c63c5411afcf91dc35a0e16a7c27e4d8234e4810810a9feb97a568ac7920d582"
)


def _v09_json_without_lf(value: object) -> bytes:
    return json.dumps(
        value,
        ensure_ascii=False,
        sort_keys=True,
        separators=(",", ":"),
    ).encode("utf-8")


def _v09_load_capture_bindings(
    acceptance: Mapping[str, Any], bridge: Mapping[str, Any]
) -> dict[str, dict[str, Any]]:
    bindings = acceptance.get("portable_capture_bindings")
    if not isinstance(bindings, list):
        raise VerifiedConstructionCoreError("v0.9 capture bindings differ")
    captures: dict[str, dict[str, Any]] = {}
    expected_binding_fields = {
        "capture_id",
        "capture_role",
        "evidence_id",
        "path",
        "bytes",
        "sha256",
    }
    expected_inventory = {
        "curated:cdc-marsden-park-campus:early-construction": {
            "verified-construction-core-v0.9-cdc-marsden-park-"
            "osm-way-1336972876-full-capture"
        },
        "curated:digital-realty-330-east-cermak-chicago:current-facility-build": {
            "verified-construction-core-v0.9-digital-realty-330-east-cermak-"
            "osm-way-210537873-v6-capture",
            "verified-construction-core-v0.9-digital-realty-330-east-cermak-"
            "city-building-footprint-871169-capture",
        },
        "curated:ntt-frankfurt-1-campus:7-3mw-expansion": set(),
    }[acceptance["project_stable_key"]]
    if {
        row.get("capture_id") for row in bindings if isinstance(row, dict)
    } != expected_inventory:
        raise VerifiedConstructionCoreError("v0.9 capture inventory differs")
    for binding in bindings:
        if not isinstance(binding, dict) or set(binding) != expected_binding_fields:
            raise VerifiedConstructionCoreError("v0.9 capture binding schema differs")
        capture_id = binding["capture_id"]
        profile = V09_CAPTURE_PROFILES.get(capture_id)
        if (
            profile is None
            or any(
                binding[field] != profile[field]
                for field in expected_binding_fields - {"capture_id"}
            )
            or capture_id in captures
        ):
            raise VerifiedConstructionCoreError("v0.9 immutable capture profile differs")
        path = _repository_input(
            binding["path"], binding["sha256"], "v0.9 portable capture"
        )
        if path.stat().st_size != binding["bytes"]:
            raise VerifiedConstructionCoreError("v0.9 capture byte count differs")
        capture = _load_json(path)
        if (
            not isinstance(capture, dict)
            or capture.get("capture_id") != capture_id
            or capture.get("schema_version") != 1
            or _sha256_bytes(_json_bytes(capture)) != profile["semantic_sha256"]
        ):
            raise VerifiedConstructionCoreError("v0.9 capture identity differs")
        if capture_id.endswith("marsden-park-osm-way-1336972876-full-capture"):
            expected_top = {
                "capture_id",
                "capture_stability",
                "captured_at",
                "object_expectations",
                "payload",
                "replay_checksums",
                "request",
                "response",
                "rights",
                "schema_version",
            }
            expected_schema = None
        elif capture_id.endswith("osm-way-210537873-v6-capture"):
            expected_top = {
                "capture_id",
                "capture_schema",
                "deterministic_identity",
                "guardrails",
                "portable_recapture",
                "review_observation",
                "reviewed_at",
                "rights",
                "schema_version",
                "selection",
                "source",
            }
            expected_schema = "verified-construction-core-v0.9-osm-api-way-capture-v1"
        else:
            expected_top = {
                "capture_id",
                "capture_schema",
                "dataset_metadata_compact",
                "deterministic_identity",
                "guardrails",
                "portable_recapture",
                "review_observation",
                "reviewed_at",
                "rights",
                "schema_version",
                "selection",
                "source",
                "terms_capture",
            }
            expected_schema = (
                "verified-construction-core-v0.9-city-of-chicago-"
                "building-footprints-row-capture-v1"
            )
        if (
            set(capture) != expected_top
            or capture.get("capture_schema") != expected_schema
        ):
            raise VerifiedConstructionCoreError("v0.9 capture schema differs")
        captures[capture_id] = capture
    geometry_capture = bridge.get("geometry_capture")
    selected = [
        row for row in bindings if row["capture_role"] == "selected_geometry_replay"
    ]
    context = [row for row in bindings if row["capture_role"] == "identity_context"]
    if selected:
        if len(selected) != 1 or not isinstance(geometry_capture, dict):
            raise VerifiedConstructionCoreError("v0.9 selected capture binding differs")
        selected_binding = selected[0]
        reference = geometry_capture.get("capture_reference")
        if reference is None:
            reference = {
                field: geometry_capture.get(field)
                for field in ("path", "bytes", "sha256")
            }
        if (
            reference
            != {
                field: selected_binding[field]
                for field in ("path", "bytes", "sha256")
            }
            or geometry_capture.get("capture_id") != selected_binding["capture_id"]
            or selected_binding["evidence_id"]
            != bridge["geometry_evidence"]["evidence_id"]
        ):
            raise VerifiedConstructionCoreError("v0.9 selected capture projection differs")
    elif geometry_capture is not None:
        raise VerifiedConstructionCoreError("v0.9 unexpected geometry capture differs")
    identity_by_id = {
        row.get("evidence_id"): row
        for row in bridge.get("identity_bridge_evidence", [])
        if isinstance(row, dict)
    }
    if len(identity_by_id) != len(bridge.get("identity_bridge_evidence", [])):
        raise VerifiedConstructionCoreError("v0.9 identity evidence differs")
    for binding in context:
        evidence = identity_by_id.get(binding["evidence_id"])
        if evidence is None or evidence.get("capture_reference") != {
            field: binding[field] for field in ("path", "bytes", "sha256")
        }:
            raise VerifiedConstructionCoreError("v0.9 context capture projection differs")
    return captures


def _v09_replay_osm_capture(
    capture: Mapping[str, Any], profile: Mapping[str, Any]
) -> dict[str, Any]:
    if "payload" in capture:
        encoded = capture.get("payload", {}).get("raw_response_body_base64")
        response = capture.get("response", {})
        expected_bytes = response.get("decoded_body_bytes")
        expected_sha256 = response.get("decoded_body_sha256")
    else:
        recapture = capture.get("portable_recapture", {})
        encoded = recapture.get("raw_response_base64")
        expected_bytes = recapture.get("response_bytes")
        expected_sha256 = recapture.get("response_sha256")
    if not isinstance(encoded, str):
        raise VerifiedConstructionCoreError("v0.9 OSM capture payload differs")
    try:
        raw = base64.b64decode(encoded, validate=True)
    except (binascii.Error, ValueError) as error:
        raise VerifiedConstructionCoreError(
            "v0.9 OSM capture base64 differs"
        ) from error
    if (
        len(raw) != expected_bytes
        or _sha256_bytes(raw) != expected_sha256
        or len(raw) != profile["raw_response_bytes"]
        or _sha256_bytes(raw) != profile["raw_response_sha256"]
    ):
        raise VerifiedConstructionCoreError("v0.9 OSM capture response differs")
    try:
        payload = json.loads(raw)
        elements = payload["elements"]
    except (KeyError, TypeError, json.JSONDecodeError) as error:
        raise VerifiedConstructionCoreError(
            "v0.9 OSM capture JSON differs"
        ) from error
    ways = [
        row
        for row in elements
        if isinstance(row, dict)
        and row.get("type") == "way"
        and row.get("id") == profile["way_id"]
    ]
    node_by_id = {
        row["id"]: row
        for row in elements
        if isinstance(row, dict) and row.get("type") == "node"
    }
    if len(ways) != 1 or ways[0].get("version") != profile["way_version"]:
        raise VerifiedConstructionCoreError("v0.9 OSM capture way differs")
    way = ways[0]
    try:
        geometry_rows = [
            {"lat": node_by_id[node_id]["lat"], "lon": node_by_id[node_id]["lon"]}
            for node_id in way["nodes"]
        ]
    except (KeyError, TypeError) as error:
        raise VerifiedConstructionCoreError(
            "v0.9 OSM capture node topology differs"
        ) from error
    enriched = {**way, "geometry": geometry_rows}
    geometry = {
        "type": "Polygon",
        "coordinates": [
            [[row["lon"], row["lat"]] for row in geometry_rows]
        ],
    }
    _validate_polygon(geometry, label="v0.9 captured OSM geometry")
    if (
        way["nodes"][0] != way["nodes"][-1]
        or _sha256_bytes(_v09_json_without_lf(way)) != profile["raw_way_sha256"]
        or _sha256_bytes(_v09_json_without_lf(enriched))
        != profile["enriched_way_sha256"]
        or _sha256_bytes(_v09_json_without_lf(geometry))
        != profile["polygon_sha256"]
    ):
        raise VerifiedConstructionCoreError("v0.9 OSM replay checksum differs")
    ring = geometry["coordinates"][0]
    midpoint = [
        (min(point[0] for point in ring) + max(point[0] for point in ring)) / 2,
        (min(point[1] for point in ring) + max(point[1] for point in ring)) / 2,
    ]
    return {
        "raw": raw,
        "way": way,
        "enriched_way": enriched,
        "geometry": geometry,
        "midpoint": midpoint,
    }


def _v09_validate_city_capture(
    capture: Mapping[str, Any], bridge: Mapping[str, Any]
) -> dict[str, str]:
    profile = V09_CAPTURE_PROFILES[capture["capture_id"]]
    recapture = capture.get("portable_recapture")
    if not isinstance(recapture, dict) or not isinstance(
        recapture.get("raw_response_base64"), str
    ):
        raise VerifiedConstructionCoreError("v0.9 City capture payload differs")
    try:
        raw = base64.b64decode(
            recapture["raw_response_base64"], validate=True
        )
    except (binascii.Error, ValueError) as error:
        raise VerifiedConstructionCoreError(
            "v0.9 City capture base64 differs"
        ) from error
    if (
        len(raw) != profile["raw_response_bytes"]
        or _sha256_bytes(raw) != profile["raw_response_sha256"]
        or recapture.get("response_bytes") != len(raw)
        or recapture.get("response_sha256") != _sha256_bytes(raw)
        or recapture.get("exact_match_to_review_observation") is not True
    ):
        raise VerifiedConstructionCoreError("v0.9 City response differs")
    try:
        payload = json.loads(raw)
    except json.JSONDecodeError as error:
        raise VerifiedConstructionCoreError("v0.9 City JSON differs") from error
    selection = capture.get("selection")
    if (
        not isinstance(selection, dict)
        or payload != [selection.get("row")]
        or selection.get("row_count") != 1
        or selection.get("row_identifier_field") != "bldg_id"
        or selection.get("row_identifier") != "871169"
        or selection["row"].get("bldg_id") != "871169"
    ):
        raise VerifiedConstructionCoreError("v0.9 City row differs")
    row = selection["row"]
    geometry = row.get("the_geom")
    properties = {field: value for field, value in row.items() if field != "the_geom"}
    if (
        _sha256_bytes(_v09_json_without_lf(geometry))
        != profile["geometry_sha256"]
        or _sha256_bytes(_v09_json_without_lf(properties))
        != profile["properties_sha256"]
        or row.get("bldg_statu") != "ACTIVE"
        or row.get("f_add1") != "2132"
        or row.get("st_name1") != "CALUMET"
    ):
        raise VerifiedConstructionCoreError("v0.9 City row projection differs")
    source = capture.get("source")
    if (
        not isinstance(source, dict)
        or source.get("publisher") != "City of Chicago"
        or source.get("attribution") != "City of Chicago"
        or source.get("license") != "SEE_TERMS_OF_USE"
        or source.get("license_url") != V09_CITY_TERMS_URL
    ):
        raise VerifiedConstructionCoreError("v0.9 City source rights differ")
    terms = capture.get("terms_capture")
    contract = terms.get("rights_contract") if isinstance(terms, dict) else None
    if not isinstance(contract, dict):
        raise VerifiedConstructionCoreError("v0.9 City terms contract differs")
    notice = contract.get("required_disclaimer")
    additional = contract.get("additional_terms_requirement")
    if (
        terms.get("effective_url") != V09_CITY_TERMS_URL
        or terms.get("response_bytes") != 54228
        or terms.get("response_sha256")
        != "8d949c15a5251b0b749af6742f247f0925e4a05962bb125fdbfe16ebb209c877"
        or _sha256_bytes(_v09_json_without_lf(contract))
        != V09_CITY_RIGHTS_CONTRACT_SHA256
        or not isinstance(notice, str)
        or len(notice.split()) != 79
        or len(notice) != 455
        or len(notice.encode("utf-8")) != 461
        or _sha256_bytes(notice.encode("utf-8")) != V09_CITY_NOTICE_SHA256
        or not isinstance(additional, str)
        or not additional
    ):
        raise VerifiedConstructionCoreError("v0.9 City legal notice differs")
    evidence_rows = bridge.get("identity_bridge_evidence")
    if not isinstance(evidence_rows, list) or len(evidence_rows) != 1:
        raise VerifiedConstructionCoreError("v0.9 City identity evidence differs")
    evidence = evidence_rows[0]
    bridge_terms = bridge.get("rights", {}).get("city_data_terms_contract")
    expected_id = atlas_stable_id(
        "evidence",
        "city-of-chicago-building-footprints",
        "syp8-uezg",
        "871169",
        profile["raw_response_sha256"],
    )
    if (
        evidence.get("evidence_id") != expected_id
        or evidence.get("content_hash") != profile["raw_response_sha256"]
        or evidence.get("publisher") != "City of Chicago"
        or evidence.get("attribution") != "City of Chicago"
        or evidence.get("license") != "SEE_TERMS_OF_USE"
        or evidence.get("license_url") != V09_CITY_TERMS_URL
        or evidence.get("terms_capture_reference", {}).get(
            "rights_contract_canonical_sha256"
        )
        != V09_CITY_RIGHTS_CONTRACT_SHA256
        or not isinstance(bridge_terms, dict)
        or bridge_terms.get("terms_url") != V09_CITY_TERMS_URL
        or bridge_terms.get("required_disclaimer") != notice
        or bridge_terms.get("additional_terms_requirement") != additional
        or bridge_terms.get("required_disclaimer_sha256")
        != V09_CITY_NOTICE_SHA256
        or bridge.get("lineage_context", {})
        .get("municipal_building_crosswalk", {})
        .get("city_geometry_selected")
        is not False
    ):
        raise VerifiedConstructionCoreError("v0.9 City bridge rights differ")
    return {
        "attribution": "City of Chicago",
        "terms_url": V09_CITY_TERMS_URL,
        "required_disclaimer": notice,
        "additional_terms_requirement": additional,
    }


def _v09_validate_cdc_geometry(
    bridge: Mapping[str, Any],
    acceptance: Mapping[str, Any],
    captures: Mapping[str, Mapping[str, Any]],
) -> None:
    capture_id = (
        "verified-construction-core-v0.9-cdc-marsden-park-"
        "osm-way-1336972876-full-capture"
    )
    capture = captures.get(capture_id)
    if capture is None or bridge.get("geometry_release") is not None:
        raise VerifiedConstructionCoreError("v0.9 CDC capture differs")
    profile = V09_CAPTURE_PROFILES[capture_id]
    replay = _v09_replay_osm_capture(capture, profile)
    entity = bridge.get("geometry_entity")
    evidence = bridge.get("geometry_evidence")
    if not isinstance(entity, dict) or not isinstance(evidence, dict):
        raise VerifiedConstructionCoreError("v0.9 CDC geometry differs")
    _validate_v06_fact_evidence(evidence, label="v0.9 CDC geometry evidence")
    fact = evidence.get("fact_payload")
    expected_capture_reference = {
        "path": profile["path"],
        "bytes": profile["bytes"],
        "sha256": profile["sha256"],
        "payload_bytes": profile["raw_response_bytes"],
        "payload_sha256": profile["raw_response_sha256"],
    }
    if (
        not isinstance(fact, dict)
        or entity.get("entity_kind") != "facility"
        or entity.get("stable_key") != "osm:way/1336972876"
        or entity.get("entity_id")
        != atlas_stable_id("entity", "osm:way/1336972876", "facility")
        or entity.get("entity_id") != acceptance["geometry_entity_id"]
        or entity.get("geometry") != replay["geometry"]
        or [entity.get("longitude"), entity.get("latitude")]
        != replay["midpoint"]
        or entity.get("tags") != replay["way"].get("tags")
        or entity.get("source_evidence_id") != evidence.get("evidence_id")
        or entity.get("source_url")
        != "https://api.openstreetmap.org/api/0.6/way/1336972876/full.json"
        or entity.get("source_publisher") != "OpenStreetMap contributors"
        or entity.get("source_license") != "ODbL-1.0"
        or evidence.get("evidence_id") != _official_evidence_id(evidence)
        or evidence.get("evidence_id") != acceptance["geometry_evidence_id"]
        or evidence.get("key") != acceptance["geometry_evidence_key"]
        or evidence.get("kind") != "open_map_geometry"
        or evidence.get("content_hash") != profile["raw_response_sha256"]
        or evidence.get("capture") != expected_capture_reference
        or evidence.get("fact_payload_canonical_sha256")
        != profile["fact_payload_sha256"]
        or fact.get("object_id") != profile["way_id"]
        or fact.get("object_version") != profile["way_version"]
        or fact.get("tags") != replay["way"].get("tags")
        or fact.get("ordered_node_ids") != replay["way"].get("nodes")
        or fact.get("ordered_ring_coordinates_lon_lat")
        != replay["geometry"]["coordinates"][0]
        or fact.get("representative_point", {}).get("coordinates")
        != replay["midpoint"]
    ):
        raise VerifiedConstructionCoreError("v0.9 CDC geometry replay differs")
    rights = bridge.get("rights")
    if (
        not isinstance(rights, dict)
        or rights.get("geometry_license_url")
        != "https://opendatacommons.org/licenses/odbl/1-0/"
        or rights.get("geometry_attribution_url")
        != "https://www.openstreetmap.org/copyright"
        or "OpenStreetMap contributors" not in str(rights.get("geometry_source"))
    ):
        raise VerifiedConstructionCoreError("v0.9 CDC rights differ")


def _v09_validate_cermak_rejections(
    bridge: Mapping[str, Any], *, hydrated_crosscheck: bool
) -> None:
    rejected = (
        bridge.get("lineage_context", {})
        .get("hard_rejected_global_v3_source_objects", {})
    )
    release = rejected.get("release") if isinstance(rejected, dict) else None
    objects = rejected.get("objects") if isinstance(rejected, dict) else None
    if (
        not isinstance(release, dict)
        or release.get("release_id") != "global-open-v3"
        or not isinstance(objects, list)
        or len(objects) != 2
    ):
        raise VerifiedConstructionCoreError("v0.9 Cermak rejection context differs")
    _, manifest = _manifest_binding(release["manifest"], label="v0.9 Cermak rejection")
    _validate_manifest_members(
        manifest, release["members"], label="v0.9 Cermak rejection"
    )
    expected_keys = {"osm:way/156520409", "osm:node/10910879064"}
    for item in objects:
        if (
            not isinstance(item, dict)
            or item.get("stable_key") not in expected_keys
            or item.get("contributes_to_identity") is not False
            or item.get("contributes_to_geometry") is not False
            or not isinstance(item.get("reason"), str)
            or not item["reason"]
        ):
            raise VerifiedConstructionCoreError("v0.9 Cermak rejected object differs")
        entity_row = _embedded_csv_source_row(
            item.get("source_row"),
            GLOBAL_GEOMETRY_ENTITY_FIELDS,
            label="v0.9 Cermak rejected entity",
        )
        entity_projection = _global_geometry_entity_projection(entity_row)
        evidence = item.get("evidence")
        if not isinstance(evidence, dict):
            raise VerifiedConstructionCoreError("v0.9 Cermak rejected evidence differs")
        evidence_row = _embedded_csv_source_row(
            evidence.get("source_row"),
            GLOBAL_GEOMETRY_EVIDENCE_FIELDS,
            label="v0.9 Cermak rejected evidence",
        )
        evidence_projection = _global_geometry_evidence_projection(evidence_row)
        if (
            entity_projection.get("stable_key") != item["stable_key"]
            or entity_projection.get("entity_id") != item.get("entity_id")
            or entity_projection.get("entity_kind") != item.get("entity_kind")
            or entity_projection.get("tags", {}).get("addr:housenumber") != "350"
            or evidence_projection.get("evidence_id")
            != evidence.get("evidence_id")
            or evidence_projection.get("source_url") != evidence.get("source_url")
            or evidence.get("evidence_id") != item.get("snapshot_evidence_id")
        ):
            raise VerifiedConstructionCoreError(
                "v0.9 Cermak rejected source projection differs"
            )
        if hydrated_crosscheck:
            for name, record, fields in (
                ("entities.csv", item, GLOBAL_GEOMETRY_ENTITY_FIELDS),
                ("evidence.csv", evidence, GLOBAL_GEOMETRY_EVIDENCE_FIELDS),
            ):
                metadata = release["members"][name]
                path = _repository_input(
                    metadata["path"],
                    metadata["sha256"],
                    f"v0.9 Cermak rejected {name}",
                )
                _validate_hydrated_source_row(
                    path,
                    record["source_row"],
                    fields,
                    label=f"v0.9 Cermak rejected {name}",
                )


def _v09_validate_cermak_geometry(
    bridge: Mapping[str, Any],
    acceptance: Mapping[str, Any],
    captures: Mapping[str, Mapping[str, Any]],
    *,
    hydrated_crosscheck: bool,
) -> dict[str, str]:
    osm_capture_id = (
        "verified-construction-core-v0.9-digital-realty-330-east-cermak-"
        "osm-way-210537873-v6-capture"
    )
    city_capture_id = (
        "verified-construction-core-v0.9-digital-realty-330-east-cermak-"
        "city-building-footprint-871169-capture"
    )
    osm_capture = captures.get(osm_capture_id)
    city_capture = captures.get(city_capture_id)
    if (
        osm_capture is None
        or city_capture is None
        or bridge.get("geometry_release") is not None
    ):
        raise VerifiedConstructionCoreError("v0.9 Cermak captures differ")
    profile = V09_CAPTURE_PROFILES[osm_capture_id]
    replay = _v09_replay_osm_capture(osm_capture, profile)
    entity = bridge.get("geometry_entity")
    evidence = bridge.get("geometry_evidence")
    if not isinstance(entity, dict) or not isinstance(evidence, dict):
        raise VerifiedConstructionCoreError("v0.9 Cermak geometry differs")
    expected_evidence_id = atlas_stable_id(
        "evidence",
        "osm",
        "way",
        "210537873",
        evidence.get("retrieved_at"),
        profile["enriched_way_sha256"],
    )
    if (
        entity.get("entity_kind") != "building"
        or entity.get("stable_key") != "osm:way/210537873"
        or entity.get("entity_id")
        != atlas_stable_id("entity", "osm:way/210537873", "building")
        or entity.get("entity_id") != acceptance["geometry_entity_id"]
        or entity.get("geometry") != replay["geometry"]
        or [entity.get("longitude"), entity.get("latitude")]
        != replay["midpoint"]
        or entity.get("tags") != replay["way"].get("tags")
        or entity.get("tags", {}).get("addr:housenumber") != "330"
        or entity.get("tags", {}).get("chicago:building_id") != "871169"
        or entity.get("status") != "unknown"
        or entity.get("status_method")
        != "osm_existing_building_geometry_only_no_operational_or_project_lifecycle_inference"
        or entity.get("capacity_estimates_json") != []
        or entity.get("workloads_json") != []
        or evidence.get("evidence_id") != expected_evidence_id
        or evidence.get("evidence_id") != acceptance["geometry_evidence_id"]
        or evidence.get("key") is not None
        or evidence.get("kind") != "openstreetmap"
        or evidence.get("content_hash") != profile["enriched_way_sha256"]
        or evidence.get("source_url") != entity.get("source_url")
        or evidence.get("publisher") != entity.get("source_publisher")
        or evidence.get("license") != "ODbL-1.0"
        or evidence.get("attribution") != "© OpenStreetMap contributors"
        or evidence.get("published_at") != replay["way"].get("timestamp")
        or evidence.get("capture_reference")
        != {
            field: profile[field] for field in ("path", "bytes", "sha256")
        }
    ):
        raise VerifiedConstructionCoreError("v0.9 Cermak geometry replay differs")
    _v09_validate_cermak_rejections(
        bridge, hydrated_crosscheck=hydrated_crosscheck
    )
    return _v09_validate_city_capture(city_capture, bridge)


def _v09_point_strictly_inside_ring(
    point: Sequence[float], ring: Sequence[Sequence[float]]
) -> bool:
    x, y = point
    inside = False
    for left, right in zip(ring, ring[1:]):
        x1, y1 = left
        x2, y2 = right
        cross = (x - x1) * (y2 - y1) - (y - y1) * (x2 - x1)
        if abs(cross) <= 1e-12 and min(x1, x2) <= x <= max(x1, x2) and min(
            y1, y2
        ) <= y <= max(y1, y2):
            return False
        if (y1 > y) != (y2 > y):
            intersection = (x2 - x1) * (y - y1) / (y2 - y1) + x1
            if x < intersection:
                inside = not inside
    return inside


def _v09_segments_intersect(
    first_start: Sequence[float],
    first_end: Sequence[float],
    second_start: Sequence[float],
    second_end: Sequence[float],
) -> bool:
    def cross(
        origin: Sequence[float], left: Sequence[float], right: Sequence[float]
    ) -> float:
        return (left[0] - origin[0]) * (right[1] - origin[1]) - (
            left[1] - origin[1]
        ) * (right[0] - origin[0])

    def on_segment(
        start: Sequence[float], point: Sequence[float], end: Sequence[float]
    ) -> bool:
        return (
            min(start[0], end[0]) <= point[0] <= max(start[0], end[0])
            and min(start[1], end[1]) <= point[1] <= max(start[1], end[1])
        )

    a = cross(first_start, first_end, second_start)
    b = cross(first_start, first_end, second_end)
    c = cross(second_start, second_end, first_start)
    d = cross(second_start, second_end, first_end)
    epsilon = 1e-12
    if (a > epsilon and b < -epsilon or a < -epsilon and b > epsilon) and (
        c > epsilon and d < -epsilon or c < -epsilon and d > epsilon
    ):
        return True
    return any(
        abs(value) <= epsilon and on_segment(start, point, end)
        for value, start, point, end in (
            (a, first_start, second_start, first_end),
            (b, first_start, second_end, first_end),
            (c, second_start, first_start, second_end),
            (d, second_start, first_end, second_end),
        )
    )


def _v09_validate_ntt_identity(bridge: Mapping[str, Any]) -> None:
    rows = bridge.get("identity_bridge_evidence")
    if not isinstance(rows, list) or len(rows) != 1:
        raise VerifiedConstructionCoreError("v0.9 NTT identity evidence differs")
    evidence = rows[0]
    _validate_v06_fact_evidence(evidence, label="v0.9 NTT identity evidence")
    fact = evidence.get("fact_payload")
    entity = bridge.get("geometry_entity")
    if not isinstance(fact, dict) or not isinstance(entity, dict):
        raise VerifiedConstructionCoreError("v0.9 NTT identity fact differs")
    geometry = entity.get("geometry")
    _validate_polygon(geometry, label="v0.9 NTT geometry")
    ring = geometry["coordinates"][0]
    objects = fact.get("osm_address_objects")
    exact_address = {
        "city": "Frankfurt am Main",
        "house_number": "100",
        "postcode": "60489",
        "street": "Eschborner Landstraße",
        "country": "Germany",
    }
    if (
        evidence.get("evidence_id") != _official_evidence_id(evidence)
        or evidence.get("evidence_id")
        != "1df077b4-ffa3-5938-a5fe-88d5b4028ad4"
        or evidence.get("fact_payload_canonical_sha256")
        != V09_NTT_IDENTITY_FACT_SHA256
        or evidence.get("content_hash") != V09_NTT_IDENTITY_FACT_SHA256
        or evidence.get("license") != "ODbL-1.0"
        or evidence.get("attribution") != "© OpenStreetMap contributors"
        or fact.get("official_source_address_fields") != exact_address
        or fact.get("frozen_geometry_container", {}).get("stable_key")
        != entity.get("stable_key")
        or fact.get("frozen_geometry_container", {}).get("entity_id")
        != entity.get("entity_id")
        or fact.get("frozen_geometry_container", {}).get("geometry_sha256")
        != _sha256_bytes(_v09_json_without_lf(geometry))
        or not isinstance(objects, list)
        or len(objects) != 3
    ):
        raise VerifiedConstructionCoreError("v0.9 NTT identity projection differs")
    expected_keys = {
        "osm:node/9049265268@1",
        "osm:way/88875308@2",
        "osm:way/88875371@6",
    }
    if {item.get("stable_key") for item in objects} != expected_keys:
        raise VerifiedConstructionCoreError("v0.9 NTT address inventory differs")
    for item in objects:
        tags = item.get("tags") if isinstance(item, dict) else None
        object_geometry = item.get("geometry") if isinstance(item, dict) else None
        if (
            not isinstance(item, dict)
            or item.get("stable_key") not in expected_keys
            or not isinstance(tags, dict)
            or tags.get("addr:housenumber") != "100"
            or tags.get("addr:street") != "Eschborner Landstraße"
            or tags.get("addr:postcode") != "60489"
            or tags.get("addr:city") != "Frankfurt am Main"
            or item.get("containment_result")
            != "strictly_inside_frozen_osm_way_697501723_polygon"
            or not isinstance(object_geometry, dict)
        ):
            raise VerifiedConstructionCoreError("v0.9 NTT address object differs")
        if object_geometry.get("type") == "Point":
            points = [object_geometry.get("coordinates")]
            address_segments: list[tuple[Sequence[float], Sequence[float]]] = []
        elif object_geometry.get("type") == "Polygon":
            _validate_polygon(object_geometry, label="v0.9 NTT address geometry")
            points = object_geometry.get("coordinates", [[]])[0]
            address_segments = list(zip(points, points[1:]))
        else:
            raise VerifiedConstructionCoreError("v0.9 NTT address geometry differs")
        if not points or not all(
            isinstance(point, list)
            and len(point) == 2
            and _v09_point_strictly_inside_ring(point, ring)
            for point in points
        ):
            raise VerifiedConstructionCoreError("v0.9 NTT containment differs")
        container_segments = list(zip(ring, ring[1:]))
        if any(
            _v09_segments_intersect(start, end, boundary_start, boundary_end)
            for start, end in address_segments
            for boundary_start, boundary_end in container_segments
        ):
            raise VerifiedConstructionCoreError(
                "v0.9 NTT address segment containment differs"
            )
    address_match = fact.get("address_match")
    if (
        not isinstance(address_match, dict)
        or address_match.get("exact_unicode_value_match") is not True
        or address_match.get("matched_object_count") != 3
        or address_match.get("all_objects_strictly_contained") is not True
    ):
        raise VerifiedConstructionCoreError("v0.9 NTT address match differs")


def _validate_v09_bridge(
    acceptance: Mapping[str, Any],
    overlay: Mapping[str, Any],
    *,
    hydrated_crosscheck: bool,
) -> tuple[dict[str, Any], dict[str, Any], dict[str, str] | None]:
    project_key = acceptance["project_stable_key"]
    profile = V09_BRIDGE_PROFILES.get(project_key)
    if (
        profile is None
        or _sha256_bytes(_json_bytes(acceptance)) != profile["acceptance_sha256"]
        or _sha256_bytes(_json_bytes(overlay)) != profile["overlay_sha256"]
        or acceptance.get("bridge_path") != profile["bridge_path"]
        or acceptance.get("bridge_bytes") != profile["bridge_bytes"]
        or acceptance.get("bridge_sha256") != profile["bridge_sha256"]
        or acceptance.get("source_input_path") != profile["source_path"]
        or acceptance.get("source_input_bytes") != profile["source_bytes"]
        or acceptance.get("source_input_sha256") != profile["source_sha256"]
        or acceptance.get("bridge_schema") != profile["schema"]
        or acceptance.get("geometry_entity") != profile["geometry_kind"]
        or acceptance.get("geometry_entity_stable_key")
        != profile["geometry_stable_key"]
        or acceptance.get("geometry_entity_id") != profile["geometry_entity_id"]
        or acceptance.get("geometry_target_entity_kind") != profile["target_kind"]
        or acceptance.get("geometry_target_entity_stable_key")
        != profile["target_stable_key"]
        or acceptance.get("geometry_target_entity_id") != profile["target_entity_id"]
        or acceptance.get("geometry_evidence_id")
        != profile["geometry_evidence_id"]
        or acceptance.get("geometry_evidence_key")
        != profile["geometry_evidence_key"]
        or acceptance.get("geometry_source_kind")
        != profile["geometry_source_kind"]
        or _sha256_bytes(_json_bytes(acceptance.get("portable_capture_bindings")))
        != profile["portable_capture_bindings_sha256"]
    ):
        raise VerifiedConstructionCoreError("v0.9 immutable bridge profile differs")
    path = _repository_input(
        acceptance["bridge_path"], acceptance["bridge_sha256"], "geometry bridge"
    )
    if path.stat().st_size != acceptance["bridge_bytes"]:
        raise VerifiedConstructionCoreError("v0.9 bridge byte count differs")
    bridge = _load_json(path)
    expected_top = {
        "bridge_id",
        "bridge_schema",
        "claim_guardrails",
        "construction_source",
        "geometry_capture",
        "geometry_entity",
        "geometry_evidence",
        "geometry_release",
        "identity_bridge_evidence",
        "imagery_posture",
        "lineage_context",
        "review_decision",
        "reviewed_at",
        "rights",
        "schema_version",
    }
    if (
        not isinstance(bridge, dict)
        or set(bridge) != expected_top
        or bridge.get("schema_version") != 1
        or bridge.get("bridge_id") != acceptance["bridge_id"]
        or bridge.get("bridge_schema") != acceptance["bridge_schema"]
        or bridge.get("reviewed_at") != CURRENT_V09_REVIEW_DATE.isoformat()
    ):
        raise VerifiedConstructionCoreError("v0.9 bridge identity differs")
    frozen_objects = {
        "geometry_projection_sha256": {
            field: bridge[field]
            for field in ("geometry_release", "geometry_entity", "geometry_evidence")
        },
        "rights_sha256": bridge.get("rights"),
        "review_decision_sha256": bridge.get("review_decision"),
        "claim_guardrails_sha256": bridge.get("claim_guardrails"),
        "imagery_posture_sha256": bridge.get("imagery_posture"),
        "geometry_capture_sha256": bridge.get("geometry_capture"),
        "identity_bridge_evidence_sha256": bridge.get("identity_bridge_evidence"),
        "lineage_context_sha256": bridge.get("lineage_context"),
    }
    for field, value in frozen_objects.items():
        if _sha256_bytes(_json_bytes(value)) != profile[field]:
            raise VerifiedConstructionCoreError(
                f"v0.9 {field.removesuffix('_sha256').replace('_', ' ')} profile differs"
            )
    project, campus, source = _v09_validate_construction_source(
        bridge,
        acceptance,
        hydrated_crosscheck=hydrated_crosscheck,
    )
    entity = bridge["geometry_entity"]
    evidence = bridge["geometry_evidence"]
    if (
        entity.get("entity_kind") != acceptance["geometry_entity"]
        or entity.get("stable_key") != acceptance["geometry_entity_stable_key"]
        or entity.get("entity_id") != acceptance["geometry_entity_id"]
        or evidence.get("evidence_id") != acceptance["geometry_evidence_id"]
        or [entity.get("longitude"), entity.get("latitude")]
        != [profile["longitude"], profile["latitude"]]
    ):
        raise VerifiedConstructionCoreError("v0.9 geometry source identity differs")
    target = project if profile["target_kind"] == "project" else campus
    if (
        target["stable_key"] != profile["target_stable_key"]
        or target["entity_id"] != profile["target_entity_id"]
        or target["stable_key"] != acceptance["geometry_target_entity_stable_key"]
        or target["entity_id"] != acceptance["geometry_target_entity_id"]
    ):
        raise VerifiedConstructionCoreError("v0.9 geometry target identity differs")
    decision = bridge.get("review_decision")
    decision_bindings = {
        "decision": "bridge_decision",
        "geometry_target_entity_kind": "geometry_target_entity_kind",
        "geometry_derivation": "geometry_derivation",
        "geometry_method": "geometry_method",
        "geometry_scope_class": "geometry_scope_class",
        "geometry_authority_class": "geometry_authority_class",
        "geometry_use_scope": "geometry_use_scope",
        "horizontal_uncertainty_metres": "horizontal_uncertainty_metres",
        "horizontal_uncertainty_unknown_reason": "horizontal_uncertainty_unknown_reason",
        "precision_scope": "precision_scope",
        "rejected_claims": "rejected_claims",
    }
    if not isinstance(decision, dict) or any(
        decision.get(field) != acceptance[target_field]
        for field, target_field in decision_bindings.items()
    ):
        raise VerifiedConstructionCoreError("v0.9 review semantics differ")
    if (
        decision.get("official_boundary") is not False
        or acceptance.get("official_boundary") is not False
        or (
            profile["target_kind"] == "campus"
            and acceptance.get("geometry_use_scope") != "campus_locator"
        )
        or (
            profile["target_kind"] == "project"
            and acceptance.get("geometry_use_scope") != "project_locator"
        )
    ):
        raise VerifiedConstructionCoreError("v0.9 geometry scope differs")
    captures = _v09_load_capture_bindings(acceptance, bridge)
    city_terms = None
    if project_key == "curated:cdc-marsden-park-campus:early-construction":
        _v09_validate_cdc_geometry(bridge, acceptance, captures)
    elif project_key == (
        "curated:digital-realty-330-east-cermak-chicago:current-facility-build"
    ):
        city_terms = _v09_validate_cermak_geometry(
            bridge,
            acceptance,
            captures,
            hydrated_crosscheck=hydrated_crosscheck,
        )
    else:
        _validate_v08_osm_geometry(
            bridge,
            acceptance,
            hydrated_crosscheck=hydrated_crosscheck,
        )
        _v09_validate_ntt_identity(bridge)
    imagery = bridge.get("imagery_posture")
    if (
        not isinstance(imagery, dict)
        or imagery.get("outcome") != "not_reviewed_for_core_preview"
        or imagery.get("independent_imagery_verification") is not False
        or not isinstance(imagery.get("basis"), str)
        or not imagery["basis"]
        or not isinstance(bridge.get("claim_guardrails"), dict)
        or not bridge["claim_guardrails"]
        or any(
            not isinstance(value, str) or not value
            for value in bridge["claim_guardrails"].values()
        )
    ):
        raise VerifiedConstructionCoreError("v0.9 imagery or guardrails differ")
    return bridge, source, city_terms


def _v09_contracts(*, hydrated_crosscheck: bool) -> dict[str, Any]:
    _validate_v09_definition_pins()
    reviewed = _load_json(V09_REVIEW_DEFINITION)
    overlays = _load_json(V09_OVERLAY_DEFINITION)
    provenance = _load_json(V09_PROVENANCE_DEFINITION)
    imagery = _load_json(V09_IMAGERY_REVIEW_DEFINITION)
    if (
        not isinstance(reviewed, dict)
        or set(reviewed)
        != {
            "contract_id",
            "review_scope",
            "reviewed_as_of",
            "base_contract",
            "acceptances",
        }
        or reviewed.get("contract_id")
        != "verified-construction-core-v0.9-reviewed-sites"
        or reviewed.get("reviewed_as_of") != CURRENT_V09_REVIEW_DATE.isoformat()
        or reviewed.get("base_contract")
        != {
            "path": "definitions/verified-construction-core-v0.8-reviewed-sites.json",
            "sha256": V08_REVIEW_DEFINITION_SHA256,
            "default_geometry_entity": "project",
            "default_geometry_derivation": "direct_geometry",
        }
        or not isinstance(reviewed.get("acceptances"), list)
        or len(reviewed["acceptances"]) != 3
        or any(
            not isinstance(row, dict) or set(row) != V09_ACCEPTANCE_REQUIRED_FIELDS
            for row in reviewed["acceptances"]
        )
    ):
        raise VerifiedConstructionCoreError("v0.9 reviewed-site contract differs")
    if (
        not isinstance(overlays, dict)
        or set(overlays)
        != {
            "contract_id",
            "purpose",
            "reviewed_as_of",
            "base_contract",
            "required_fields",
            "allowed_decisions",
            "overlays",
        }
        or overlays.get("contract_id")
        != "verified-construction-core-reviewed-overlays-v7"
        or overlays.get("reviewed_as_of") != CURRENT_V09_REVIEW_DATE.isoformat()
        or overlays.get("base_contract")
        != {
            "path": "definitions/verified-construction-core-reviewed-overlays-v6.json",
            "sha256": V08_OVERLAY_DEFINITION_SHA256,
        }
        or set(overlays.get("required_fields", [])) != V09_OVERLAY_REQUIRED_FIELDS
        or set(overlays.get("allowed_decisions", []))
        != {"queued", "accepted", "excluded"}
        or not isinstance(overlays.get("overlays"), list)
        or len(overlays["overlays"]) != 3
        or any(
            not isinstance(row, dict) or set(row) != V09_OVERLAY_REQUIRED_FIELDS
            for row in overlays["overlays"]
        )
    ):
        raise VerifiedConstructionCoreError("v0.9 overlay contract differs")
    acceptance_by_key = {
        row.get("project_stable_key"): row for row in reviewed["acceptances"]
    }
    overlay_by_key = {
        row.get("source_project_stable_key"): row for row in overlays["overlays"]
    }
    if (
        len(acceptance_by_key) != 3
        or len(overlay_by_key) != 3
        or set(acceptance_by_key) != set(V09_BRIDGE_PROFILES)
        or set(overlay_by_key) != set(V09_BRIDGE_PROFILES)
        or set(V09_SOURCE_PROFILES) != set(V09_BRIDGE_PROFILES)
    ):
        raise VerifiedConstructionCoreError("v0.9 definition cohort differs")
    profiled_evidence_ids = {
        evidence_id
        for profile in V09_SOURCE_PROFILES.values()
        for evidence_id in profile["used_evidence_ids"]
    } | {
        profile["geometry_evidence_id"]
        for profile in V09_BRIDGE_PROFILES.values()
    } | {
        "39cdd4b0-60f0-5d50-9a38-59d72c0a9d53",
        "1df077b4-ffa3-5938-a5fe-88d5b4028ad4",
    }
    if profiled_evidence_ids != V09_EVIDENCE_IDS:
        raise VerifiedConstructionCoreError("v0.9 evidence profile differs")
    crosswalk = {
        "country": "country",
        "physical_site_stable_key": "physical_site_stable_key",
        "physical_site_entity_id": "physical_site_entity_id",
        "source_input_path": "source_input_path",
        "source_input_bytes": "source_input_bytes",
        "source_input_sha256": "source_input_sha256",
        "bridge_id": "bridge_id",
        "bridge_schema": "bridge_schema",
        "bridge_path": "bridge_path",
        "bridge_bytes": "bridge_bytes",
        "bridge_sha256": "bridge_sha256",
        "project_to_campus_relationship_id": "project_to_campus_relationship_id",
        "project_to_campus_relationship_type": "project_to_campus_relationship_type",
        "project_to_campus_decision_basis": "project_to_campus_decision_basis",
        "geometry_entity": "geometry_entity",
        "geometry_entity_stable_key": "geometry_entity_stable_key",
        "geometry_entity_id": "geometry_entity_id",
        "geometry_target_entity_kind": "geometry_target_entity_kind",
        "geometry_target_entity_stable_key": "geometry_target_entity_stable_key",
        "geometry_target_entity_id": "geometry_target_entity_id",
        "geometry_evidence_key": "geometry_evidence_key",
        "geometry_evidence_id": "geometry_evidence_id",
        "geometry_source_kind": "geometry_source_kind",
        "geometry_derivation": "geometry_derivation",
        "geometry_method": "geometry_method",
        "geometry_scope_class": "geometry_scope_class",
        "geometry_authority_class": "geometry_authority_class",
        "geometry_use_scope": "geometry_use_scope",
        "official_boundary": "official_boundary",
        "horizontal_uncertainty_metres": "horizontal_uncertainty_metres",
        "horizontal_uncertainty_unknown_reason": "horizontal_uncertainty_unknown_reason",
        "precision_scope": "precision_scope",
        "bridge_decision": "bridge_decision",
        "portable_input_binding": "portable_input_binding",
        "portable_capture_bindings": "portable_capture_bindings",
        "rejected_claims": "rejected_claims",
    }
    bridges: dict[str, dict[str, Any]] = {}
    sources: dict[str, dict[str, Any]] = {}
    city_terms_rows: list[dict[str, str]] = []
    for project_key in sorted(acceptance_by_key):
        acceptance = acceptance_by_key[project_key]
        overlay = overlay_by_key[project_key]
        if (
            any(acceptance[left] != overlay[right] for left, right in crosswalk.items())
            or acceptance["project_entity_id"]
            != overlay["source_project_entity_id"]
            or acceptance["project_stable_key"]
            != overlay["source_project_stable_key"]
            or acceptance["geometry_overlay_id"] != overlay["overlay_id"]
            or acceptance["decision_basis"] != overlay["decision_reason"]
            or overlay["decision"] != "accepted"
            or overlay["reviewed_at"] != CURRENT_V09_REVIEW_DATE.isoformat()
        ):
            raise VerifiedConstructionCoreError(
                "v0.9 acceptance-overlay binding differs"
            )
        bridge, source, city_terms = _validate_v09_bridge(
            acceptance,
            overlay,
            hydrated_crosscheck=hydrated_crosscheck,
        )
        bridges[project_key] = bridge
        sources[project_key] = source
        if city_terms is not None:
            city_terms_rows.append(city_terms)
    if len(city_terms_rows) != 1:
        raise VerifiedConstructionCoreError("v0.9 City terms inventory differs")
    if (
        not isinstance(provenance, dict)
        or set(provenance)
        != {
            "contract_id",
            "review_scope",
            "reviewed_as_of",
            "base_contract",
            "workload_scope_bindings",
            "role_bindings",
            "excluded_source_roles",
            "context_evidence_bindings",
        }
        or provenance.get("contract_id")
        != "verified-construction-core-v0.9-provenance"
        or provenance.get("reviewed_as_of") != CURRENT_V09_REVIEW_DATE.isoformat()
        or provenance.get("base_contract")
        != {
            "path": "definitions/verified-construction-core-v0.8-provenance.json",
            "sha256": V08_PROVENANCE_DEFINITION_SHA256,
        }
    ):
        raise VerifiedConstructionCoreError("v0.9 provenance contract differs")
    provenance_semantics = {
        field: provenance.get(field)
        for field in (
            "workload_scope_bindings",
            "role_bindings",
            "excluded_source_roles",
            "context_evidence_bindings",
        )
    }
    if (
        _sha256_bytes(_json_bytes(provenance_semantics))
        != V09_PROVENANCE_SEMANTICS_SHA256
        or provenance["workload_scope_bindings"] != []
        or provenance["excluded_source_roles"] != []
        or len(provenance["role_bindings"]) != 1
        or len(provenance["context_evidence_bindings"]) != 2
    ):
        raise VerifiedConstructionCoreError("v0.9 provenance semantics differ")
    role = provenance["role_bindings"][0]
    if (
        role.get("project_stable_key"),
        role.get("role"),
        role.get("party"),
        role.get("evidence_id"),
        role.get("relationship_scope"),
    ) != (
        "curated:cdc-marsden-park-campus:early-construction",
        "operator",
        "CDC Data Centres",
        "56fa3df3-1fe1-50af-80c1-0e2f0f14d610",
        "intended",
    ):
        raise VerifiedConstructionCoreError("v0.9 role semantics differ")
    role_project = role["project_stable_key"]
    role_acceptance = acceptance_by_key[role_project]
    if (
        role.get("source_input_path") != role_acceptance["source_input_path"]
        or role.get("source_input_sha256")
        != role_acceptance["source_input_sha256"]
        or role["evidence_id"] not in _v08_source_evidence(sources[role_project])
    ):
        raise VerifiedConstructionCoreError("v0.9 role evidence differs")
    expected_contexts = {
        (
            "curated:digital-realty-330-east-cermak-chicago:current-facility-build",
            "39cdd4b0-60f0-5d50-9a38-59d72c0a9d53",
            "geometry_identity",
        ),
        (
            "curated:ntt-frankfurt-1-campus:7-3mw-expansion",
            "1df077b4-ffa3-5938-a5fe-88d5b4028ad4",
            "geometry_identity",
        ),
    }
    actual_contexts = {
        (
            row.get("project_stable_key"),
            row.get("evidence_id"),
            row.get("usage_role"),
        )
        for row in provenance["context_evidence_bindings"]
    }
    if actual_contexts != expected_contexts:
        raise VerifiedConstructionCoreError("v0.9 context semantics differ")
    for binding in provenance["context_evidence_bindings"]:
        project_key = binding["project_stable_key"]
        acceptance = acceptance_by_key[project_key]
        evidence_ids = {
            row["evidence_id"]
            for row in bridges[project_key]["identity_bridge_evidence"]
        }
        if (
            binding.get("bridge_path") != acceptance["bridge_path"]
            or binding.get("bridge_sha256") != acceptance["bridge_sha256"]
            or binding.get("evidence_origin") != "identity_bridge_evidence"
            or binding["evidence_id"] not in evidence_ids
        ):
            raise VerifiedConstructionCoreError(
                "v0.9 context evidence binding differs"
            )
    ntt_key = "curated:ntt-frankfurt-1-campus:7-3mw-expansion"
    ntt_construction = bridges[ntt_key]["construction_source"]
    ntt_project_capacity = ntt_construction["project"]["capacity_estimates_json"]
    ntt_campus_capacity = ntt_construction["campus"]["capacity_estimates_json"]
    marsden = bridges[
        "curated:cdc-marsden-park-campus:early-construction"
    ]["construction_source"]
    if (
        len(ntt_project_capacity) != 1
        or ntt_project_capacity[0].get("metric") != "critical_it_mw"
        or ntt_project_capacity[0].get("base") != 7.3
        or ntt_project_capacity[0].get("stage") != "planned"
        or [row.get("base") for row in ntt_campus_capacity] != [70.1, 77.4]
        or marsden["project"]["capacity_estimates_json"] != []
        or marsden["campus"]["capacity_estimates_json"] != []
        or any(
            bridge["construction_source"]["project"]["workloads_json"] != []
            for bridge in bridges.values()
        )
    ):
        raise VerifiedConstructionCoreError("v0.9 publication guardrails differ")
    if (
        not isinstance(imagery, dict)
        or set(imagery)
        != {
            "contract_id",
            "review_scope",
            "reviewed_as_of",
            "base_contract",
            "default_outcome",
            "records",
        }
        or imagery.get("contract_id")
        != "verified-construction-core-v0.9-imagery-reviews"
        or imagery.get("reviewed_as_of") != CURRENT_V09_REVIEW_DATE.isoformat()
        or imagery.get("base_contract")
        != {
            "path": "definitions/verified-construction-core-v0.8-imagery-reviews.json",
            "sha256": V08_IMAGERY_REVIEW_DEFINITION_SHA256,
        }
        or imagery.get("default_outcome") != "not_reviewed_for_core_preview"
        or imagery.get("records") != []
    ):
        raise VerifiedConstructionCoreError("v0.9 imagery contract differs")
    return {
        "reviewed": reviewed,
        "overlays": overlays,
        "provenance": provenance,
        "imagery": imagery,
        "acceptance_by_key": acceptance_by_key,
        "overlay_by_key": overlay_by_key,
        "bridges": bridges,
        "sources": sources,
        "city_terms": city_terms_rows[0],
    }


def _v09_build_delta_rows(contracts: Mapping[str, Any]) -> dict[str, Any]:
    provenance = contracts["provenance"]
    roles_by_project: dict[str, list[dict[str, Any]]] = defaultdict(list)
    for row in provenance["role_bindings"]:
        roles_by_project[row["project_stable_key"]].append(row)
    contexts_by_project: dict[str, list[dict[str, Any]]] = defaultdict(list)
    for row in provenance["context_evidence_bindings"]:
        contexts_by_project[row["project_stable_key"]].append(row)
    evidence_pool: dict[str, dict[str, Any]] = {}
    evidence_usage: dict[str, dict[str, set[str]]] = defaultdict(
        lambda: {"roles": set(), "project_ids": set()}
    )
    projects: list[dict[str, Any]] = []
    sites: list[dict[str, Any]] = []
    for project_key in sorted(contracts["bridges"]):
        bridge = contracts["bridges"][project_key]
        source = contracts["sources"][project_key]
        acceptance = contracts["acceptance_by_key"][project_key]
        construction = bridge["construction_source"]
        source_evidence = _v08_source_evidence(source)
        evidence_pool.update(source_evidence)
        geometry_evidence = _v07_evidence_projection(bridge["geometry_evidence"])
        evidence_pool[geometry_evidence["evidence_id"]] = geometry_evidence
        identity_evidence = bridge.get("identity_bridge_evidence")
        if not isinstance(identity_evidence, list):
            raise VerifiedConstructionCoreError(
                "v0.9 identity evidence inventory differs"
            )
        for identity_row in identity_evidence:
            projected_identity = _v07_evidence_projection(identity_row)
            evidence_id = projected_identity["evidence_id"]
            existing = evidence_pool.get(evidence_id)
            if existing is not None and existing != projected_identity:
                raise VerifiedConstructionCoreError(
                    "v0.9 identity evidence projection differs"
                )
            evidence_pool[evidence_id] = projected_identity
        project = construction["project"]
        campus = construction["campus"]
        project_id = project["entity_id"]
        site_id = _stable_id("vcc-site", campus["stable_key"])
        status_evidence_id = project["status_evidence_id"]
        geometry_evidence_id = bridge["geometry_evidence"]["evidence_id"]
        evidence_usage[status_evidence_id]["roles"].add("physical_status")
        evidence_usage[status_evidence_id]["project_ids"].add(project_id)
        evidence_usage[geometry_evidence_id]["roles"].add("geometry")
        evidence_usage[geometry_evidence_id]["project_ids"].add(project_id)
        observations = project.get("capacity_estimates_json", [])
        if not isinstance(observations, list):
            raise VerifiedConstructionCoreError("v0.9 capacity observations differ")
        for observation in observations:
            _validate_typed_observation(
                observation, POWER_METRICS | ENERGY_METRICS | EFFICIENCY_METRICS
            )
            evidence_id = observation["evidence_id"]
            if evidence_id not in evidence_pool:
                raise VerifiedConstructionCoreError(
                    "v0.9 capacity evidence differs"
                )
            evidence_usage[evidence_id]["roles"].add(
                f"typed_metric:{observation['metric']}"
            )
            evidence_usage[evidence_id]["project_ids"].add(project_id)
        power = [row for row in observations if row["metric"] in POWER_METRICS]
        energy = [row for row in observations if row["metric"] in ENERGY_METRICS]
        efficiency = [
            row for row in observations if row["metric"] in EFFICIENCY_METRICS
        ]
        if project.get("workloads_json") != []:
            raise VerifiedConstructionCoreError("v0.9 workload promotion differs")
        role_claims: list[dict[str, str]] = []
        projected_roles = {role: [] for role in ROLE_COLUMNS}
        for binding in roles_by_project.get(project_key, []):
            evidence_id = binding["evidence_id"]
            if evidence_id not in evidence_pool:
                raise VerifiedConstructionCoreError("v0.9 role evidence differs")
            role_claims.append(
                {
                    "evidence_id": evidence_id,
                    "party": binding["party"],
                    "relationship_scope": binding["relationship_scope"],
                    "role": binding["role"],
                }
            )
            projected_roles[binding["role"]].append(binding["party"])
            evidence_usage[evidence_id]["roles"].add(f"role:{binding['role']}")
            evidence_usage[evidence_id]["project_ids"].add(project_id)
        role_claims.sort(
            key=lambda row: (row["role"], row["party"], row["evidence_id"])
        )
        operating_model = project.get("operating_model")
        operating_model_evidence_id = project.get("operating_model_evidence_id")
        if operating_model:
            if operating_model_evidence_id not in evidence_pool:
                raise VerifiedConstructionCoreError(
                    "v0.9 operating-model evidence differs"
                )
            evidence_usage[operating_model_evidence_id]["roles"].add(
                "operating_model"
            )
            evidence_usage[operating_model_evidence_id]["project_ids"].add(
                project_id
            )
        elif operating_model_evidence_id is not None:
            raise VerifiedConstructionCoreError(
                "v0.9 operating-model identity differs"
            )
        for binding in contexts_by_project.get(project_key, []):
            evidence_id = binding["evidence_id"]
            if evidence_id not in evidence_pool:
                raise VerifiedConstructionCoreError("v0.9 context evidence differs")
            evidence_usage[evidence_id]["roles"].add(
                f"context:{binding['usage_role']}"
            )
            evidence_usage[evidence_id]["project_ids"].add(project_id)
        entity = bridge["geometry_entity"]
        geometry = entity["geometry"]
        status_date = _calendar_date(project["status_as_of"], "v0.9 status_as_of")
        image_outcome = contracts["imagery"]["default_outcome"]
        row = {
            "project_id": project_id,
            "project_stable_key": project_key,
            "site_id": site_id,
            "physical_site_stable_key": campus["stable_key"],
            "name": project["name"],
            "country": acceptance["country"],
            "country_iso_a2": V09_COUNTRY_ISO_A2[acceptance["country"]],
            "latitude": entity["latitude"],
            "longitude": entity["longitude"],
            "geometry_json": _json_bytes(geometry).decode().strip(),
            "geometry_type": geometry["type"],
            "geometry_source_entity_kind": acceptance["geometry_entity"],
            "geometry_derivation": acceptance["geometry_derivation"],
            "geometry_method": acceptance["geometry_method"],
            "geometry_scope_class": acceptance["geometry_scope_class"],
            "geometry_authority_class": acceptance["geometry_authority_class"],
            "geometry_use_scope": acceptance["geometry_use_scope"],
            "geometry_precision_scope": acceptance["precision_scope"],
            "horizontal_uncertainty_metres": (
                ""
                if acceptance["horizontal_uncertainty_metres"] is None
                else acceptance["horizontal_uncertainty_metres"]
            ),
            "horizontal_uncertainty_unknown_reason": acceptance[
                "horizontal_uncertainty_unknown_reason"
            ],
            "geometry_evidence_id": geometry_evidence_id,
            "last_observed_physical_status": project["status"],
            "status_as_of": project["status_as_of"],
            "status_age_days_at_review": (
                CURRENT_V09_REVIEW_DATE - status_date
            ).days,
            "status_method": project["status_method"],
            "status_evidence_id": status_evidence_id,
            "verification_posture": _verification_posture(
                acceptance["geometry_authority_class"],
                acceptance["geometry_use_scope"],
            ),
            "independent_imagery_verification": "false",
            "imagery_review_outcome": image_outcome,
            "development_type": "unknown",
            "development_type_unknown_reason": "source evidence does not distinguish greenfield, expansion, or retrofit",
            "operating_model": operating_model or "unknown",
            "operating_model_unknown_reason": (
                "" if operating_model else "not established by selected evidence"
            ),
            "operating_model_evidence_id": operating_model_evidence_id or "",
            "workloads_json": "[]",
            "workload_unknown_reason": "not established by selected evidence",
            "role_claims_json": _json_bytes(role_claims).decode().strip(),
            "power_observations_json": _json_bytes(power).decode().strip(),
            "power_unknown_reason": (
                "" if power else "no typed project power observation; not estimated"
            ),
            "annual_energy_observations_json": _json_bytes(energy)
            .decode()
            .strip(),
            "annual_energy_unknown_reason": (
                ""
                if energy
                else "no scoped annual-energy inputs; not estimated"
            ),
            "efficiency_observations_json": _json_bytes(efficiency)
            .decode()
            .strip(),
            "efficiency_unknown_reason": (
                "" if efficiency else "no scoped PUE or WUE observation"
            ),
            "owner": "; ".join(sorted(projected_roles["owner"])),
            "operator": "; ".join(sorted(projected_roles["operator"])),
            "users": "; ".join(sorted(projected_roles["user"])),
            "tenants": "; ".join(sorted(projected_roles["tenant"])),
            "customers": "; ".join(sorted(projected_roles["customer"])),
            "status_source_url": evidence_pool[status_evidence_id]["source_url"],
            "geometry_source_url": geometry_evidence["source_url"],
        }
        projects.append(row)
        sites.append(
            {
                "site_id": site_id,
                "physical_site_stable_key": campus["stable_key"],
                "name": campus["name"],
                "country": row["country"],
                "country_iso_a2": row["country_iso_a2"],
                "latitude": row["latitude"],
                "longitude": row["longitude"],
                "geometry_json": row["geometry_json"],
                "geometry_type": row["geometry_type"],
                "geometry_source_entity_kinds_json": _json_bytes(
                    [row["geometry_source_entity_kind"]]
                )
                .decode()
                .strip(),
                "geometry_derivations_json": _json_bytes(
                    [row["geometry_derivation"]]
                )
                .decode()
                .strip(),
                "geometry_methods_json": _json_bytes([row["geometry_method"]])
                .decode()
                .strip(),
                "geometry_scope_classes_json": _json_bytes(
                    [row["geometry_scope_class"]]
                )
                .decode()
                .strip(),
                "geometry_authority_classes_json": _json_bytes(
                    [row["geometry_authority_class"]]
                )
                .decode()
                .strip(),
                "geometry_use_scopes_json": _json_bytes(
                    [row["geometry_use_scope"]]
                )
                .decode()
                .strip(),
                "geometry_precision_scopes_json": _json_bytes(
                    [row["geometry_precision_scope"]]
                )
                .decode()
                .strip(),
                "horizontal_uncertainty_metres": row[
                    "horizontal_uncertainty_metres"
                ],
                "horizontal_uncertainty_unknown_reason": row[
                    "horizontal_uncertainty_unknown_reason"
                ],
                "geometry_evidence_ids_json": _json_bytes(
                    [geometry_evidence_id]
                )
                .decode()
                .strip(),
                "project_count": 1,
                "project_ids_json": _json_bytes([project_id]).decode().strip(),
                "project_stable_keys_json": _json_bytes([project_key])
                .decode()
                .strip(),
                "statuses_json": _json_bytes(
                    [row["last_observed_physical_status"]]
                )
                .decode()
                .strip(),
                "oldest_status_as_of": row["status_as_of"],
                "newest_status_as_of": row["status_as_of"],
                "verification_posture": row["verification_posture"],
                "independent_imagery_verification": "false",
                "imagery_review_outcomes_json": _json_bytes([image_outcome])
                .decode()
                .strip(),
            }
        )
    if set(evidence_usage) != V09_EVIDENCE_IDS:
        raise VerifiedConstructionCoreError(
            "v0.9 projected evidence allowlist differs"
        )
    evidence = [
        {
            **evidence_pool[evidence_id],
            "roles_json": _json_bytes(sorted(usage["roles"])).decode().strip(),
            "project_ids_json": _json_bytes(sorted(usage["project_ids"]))
            .decode()
            .strip(),
        }
        for evidence_id, usage in sorted(evidence_usage.items())
    ]
    return {"projects": projects, "sites": sites, "evidence": evidence}


def _v09_portable_source_inputs(
    contracts: Mapping[str, Any],
) -> list[dict[str, Any]]:
    base_manifest = _load_json(LEGACY_PREVIEW_V08_DIR / "manifest.json")
    rows = list(base_manifest["portable_source_inputs"])
    new: dict[str, dict[str, Any]] = {}
    for acceptance in contracts["reviewed"]["acceptances"]:
        bindings = [
            acceptance["portable_input_binding"],
            {
                "path": acceptance["bridge_path"],
                "bytes": acceptance["bridge_bytes"],
                "sha256": acceptance["bridge_sha256"],
            },
            *acceptance["portable_capture_bindings"],
        ]
        for binding in bindings:
            record = {
                "path": binding["path"],
                "bytes": binding["bytes"],
                "sha256": binding["sha256"],
                "parent_manifests": [],
            }
            previous = new.get(record["path"])
            if previous is not None and previous != record:
                raise VerifiedConstructionCoreError("v0.9 portable input differs")
            new[record["path"]] = record
    rows.extend(new.values())
    rows.sort(key=lambda row: row["path"])
    if len(rows) != 66 or len({row["path"] for row in rows}) != 66:
        raise VerifiedConstructionCoreError("v0.9 portable input count differs")
    for row in rows:
        path = _repository_input(row["path"], row["sha256"], "v0.9 portable input")
        if path.stat().st_size != row["bytes"]:
            raise VerifiedConstructionCoreError(
                "v0.9 portable input byte count differs"
            )
    return rows


def _v09_map_html(
    geojson: Mapping[str, Any], evidence: Sequence[Mapping[str, Any]]
) -> bytes:
    document = _v08_map_html(geojson, evidence).decode("utf-8")
    if "Verified Construction Core v0.8 preview" not in document:
        raise VerifiedConstructionCoreError("v0.9 map title seam differs")
    return document.replace(
        "Verified Construction Core v0.8 preview",
        "Verified Construction Core v0.9 preview",
    ).encode("utf-8")


def _v09_schema() -> dict[str, Any]:
    schema = _v08_schema()
    schema["format"] = "datacenter-atlas-verified-construction-core-schema-v9"
    schema["preview_id"] = CURRENT_V09_PREVIEW_ID
    return schema


def _v09_city_legal_notice(city_terms: Mapping[str, str]) -> str:
    if set(city_terms) != {
        "additional_terms_requirement",
        "attribution",
        "required_disclaimer",
        "terms_url",
    }:
        raise VerifiedConstructionCoreError("v0.9 City terms projection differs")
    notice = city_terms["required_disclaimer"]
    if (
        city_terms["attribution"] != "City of Chicago"
        or city_terms["terms_url"] != V09_CITY_TERMS_URL
        or _sha256_bytes(notice.encode("utf-8")) != V09_CITY_NOTICE_SHA256
    ):
        raise VerifiedConstructionCoreError("v0.9 City legal notice differs")
    return f"""City of Chicago data notice

Attribution: {city_terms['attribution']}
Terms: {city_terms['terms_url']}

Required disclaimer:
{notice}

Additional terms reminder:
{city_terms['additional_terms_requirement']}"""


def _v09_readme(
    report: Mapping[str, Any], city_terms: Mapping[str, str]
) -> bytes:
    legal_notice = _v09_city_legal_notice(city_terms)
    return f"""# Verified Construction Core v0.9 preview

This tracked, non-final preview contains **{report['selected_physical_site_count']} physical sites**
and **{report['selected_project_count']} linked projects** in {len(report['country_counts'])}
countries. Every project retains a dated authoritative physical-status observation and an exact
reviewed geometry scope. Five project rows carry official boundaries; the remaining 42 are
explicit project or campus locators, never construction footprints by implication.

The three-row v0.9 delta adds CDC Marsden Park, Digital Realty 330 East Cermak, and the NTT FRA1
7.3 MW expansion. CDC and NTT use explicit campus locators. Cermak uses the selected OpenStreetMap
330 East Cermak building as a project locator; the City of Chicago building record is retained only
as identity context and the separate 350 East Cermak objects remain rejected. OpenStreetMap and
City lifecycle tags do not create or alter project lifecycle claims.

Marsden Park publishes no 504 MW or 1 GW capacity claim and retains only the intended CDC operator
binding. NTT publishes one planned 7.3 MW project-scoped critical-IT observation. Its 70.1 MW and
77.4 MW campus values remain non-additive context, while the 120 MVA electrical figure remains
untyped. No v0.9 workload is added.

The artifact is deterministic and rebuildable in a clean clone from the 66 manifest-bound portable
inputs plus the frozen v0.8 artifact. Ignored v97, global-v3, and v14 payloads are optional hydrated
cross-checks, not build dependencies. All three v0.9 rows inherit the not-reviewed imagery outcome
and remain `independent_imagery_verification=false`.

## City of Chicago terms

{legal_notice}

This is not the final 100-site Verified Construction Core v1. Its final-release gates remain
unsatisfied and `publishable_as_final` is false.
""".encode("utf-8")


def _v09_attribution(
    evidence: Sequence[Mapping[str, Any]], city_terms: Mapping[str, str]
) -> bytes:
    rows = {(row["publisher"], row["license"], row["source_url"]) for row in evidence}
    lines = [
        "Data Center Atlas Verified Construction Core v0.9 preview",
        "",
        "Compact derived facts only; third-party terms remain controlling.",
        "",
        *(
            f"- {publisher} | {license_name} | {url}"
            for publisher, license_name, url in sorted(rows)
        ),
        "",
        "Map-provider terms:",
        *(
            f"- {label} | {license_name} | {url}"
            for label, license_name, url in _v07_map_attribution_notices(evidence)
        ),
        "",
        _v09_city_legal_notice(city_terms),
    ]
    return ("\n".join(lines) + "\n").encode("utf-8")


def _v09_payloads() -> tuple[dict[str, bytes], dict[str, Any], list[dict[str, Any]]]:
    validate_frozen_v08(LEGACY_PREVIEW_V08_DIR)
    contracts = _v09_contracts(
        hydrated_crosscheck=_v09_hydrated_crosscheck_available()
    )
    base_projects = _load_csv(LEGACY_PREVIEW_V08_DIR / "projects.csv", PROJECT_FIELDS)
    base_sites = _load_csv(LEGACY_PREVIEW_V08_DIR / "sites.csv", SITE_FIELDS)
    base_evidence = _load_csv(
        LEGACY_PREVIEW_V08_DIR / "evidence.csv", EVIDENCE_FIELDS
    )
    base_report = _load_json(LEGACY_PREVIEW_V08_DIR / "selection-report.json")
    delta = _v09_build_delta_rows(contracts)
    projects = sorted(
        [*base_projects, *delta["projects"]],
        key=lambda row: row["project_stable_key"],
    )
    sites = sorted(
        [*base_sites, *delta["sites"]],
        key=lambda row: row["physical_site_stable_key"],
    )
    evidence = sorted(
        [*base_evidence, *delta["evidence"]],
        key=lambda row: row["evidence_id"],
    )
    if (
        len(projects) != 47
        or len(sites) != 44
        or len(evidence) != 108
        or len({row["project_id"] for row in projects}) != 47
        or len({row["site_id"] for row in sites}) != 44
        or len({row["evidence_id"] for row in evidence}) != 108
    ):
        raise VerifiedConstructionCoreError(
            "v0.9 cohort count or uniqueness differs"
        )
    gates = _final_release_gates(sites, projects)
    gates["clean_clone_rebuild"] = {
        "passed": True,
        "reason": "all current-delta inputs and the frozen base artifact are tracked and hash-bound",
    }
    country_counts = dict(sorted(Counter(row["country"] for row in sites).items()))
    provenance_decisions = {
        key: [
            *base_report["provenance_decisions"][key],
            *contracts["provenance"][key],
        ]
        for key in (
            "workload_scope_bindings",
            "role_bindings",
            "excluded_source_roles",
            "context_evidence_bindings",
        )
    }
    report = {
        **base_report,
        "format": "datacenter-atlas-verified-construction-core-selection-v9",
        "publishable_as_final": all(
            gate.get("passed", False) for gate in gates.values()
        ),
        "reviewed_at": CURRENT_V09_REVIEW_DATE.isoformat(),
        "selected_project_count": 47,
        "selected_physical_site_count": 44,
        "official_boundary_project_count": 5,
        "reviewed_site_locator_project_count": 42,
        "non_selected_source_row_count": 484,
        "selection_first_failure_counts": V09_SELECTION_FIRST_FAILURE_COUNTS,
        "country_counts": country_counts,
        "final_release_gates": gates,
        "imagery_review_provenance": list(
            base_report["imagery_review_provenance"]
        ),
        "provenance_decisions": provenance_decisions,
        "reviewed_overlay_queue": [
            *base_report["reviewed_overlay_queue"],
            *contracts["overlays"]["overlays"],
        ],
    }
    if (
        len(country_counts) != 25
        or sum(row["country_iso_a2"] != "US" for row in sites) != 37
        or sum(_is_official_boundary(row) for row in projects) != 5
        or sum(_is_reviewed_locator(row) for row in projects) != 42
        or sum(V09_SELECTION_FIRST_FAILURE_COUNTS.values()) != 531
        or gates["imagery_outcomes_complete"] != {
            "actual": 10,
            "required": 47,
            "passed": False,
        }
        or len(provenance_decisions["context_evidence_bindings"]) != 4
    ):
        raise VerifiedConstructionCoreError("v0.9 expected accounting differs")
    geojson = _geojson(sites)
    geojson["name"] = "Data Center Atlas Verified Construction Core v0.9 preview"
    city_terms = contracts["city_terms"]
    payloads = {
        "ATTRIBUTION.txt": _v09_attribution(evidence, city_terms),
        "README.md": _v09_readme(report, city_terms),
        "evidence.csv": _csv_bytes(evidence, EVIDENCE_FIELDS),
        "map.html": _v09_map_html(geojson, evidence),
        "projects.csv": _csv_bytes(projects, PROJECT_FIELDS),
        "schema.json": _json_bytes(_v09_schema()),
        "selection-report.json": _json_bytes(report),
        "sites.csv": _csv_bytes(sites, SITE_FIELDS),
        "sites.geojson": _json_bytes(geojson),
    }
    portable = _v09_portable_source_inputs(contracts)
    return payloads, report, portable


def _v09_manifest(
    payloads: Mapping[str, bytes],
    report: Mapping[str, Any],
    portable: Sequence[Mapping[str, Any]],
) -> dict[str, Any]:
    return {
        "format": "datacenter-atlas-verified-construction-core-preview-v9",
        "preview_id": CURRENT_V09_PREVIEW_ID,
        "release_status": "preview",
        "publishable_as_final": report["publishable_as_final"],
        "base_preview_id": CURRENT_V08_PREVIEW_ID,
        "base_preview_manifest_sha256": LEGACY_PREVIEW_V08_MANIFEST_SHA256,
        "base_preview_commit": LEGACY_PREVIEW_V08_COMMIT,
        "source_release_id": SOURCE_RELEASE_ID,
        "source_release_manifest_path": "releases/2026-07-22-open-seed-v97/manifest.json",
        "source_release_manifest_sha256": "0a6f41f4239944df27f2ce70e81a089b91cec401f154bbae28412b27a4d00fdd",
        "definition_paths": {
            "imagery_reviews": "definitions/verified-construction-core-v0.9-imagery-reviews.json",
            "provenance": "definitions/verified-construction-core-v0.9-provenance.json",
            "reviewed_overlays": "definitions/verified-construction-core-reviewed-overlays-v7.json",
            "reviewed_sites": "definitions/verified-construction-core-v0.9-reviewed-sites.json",
        },
        "review_definition_sha256": V09_REVIEW_DEFINITION_SHA256,
        "imagery_review_definition_sha256": V09_IMAGERY_REVIEW_DEFINITION_SHA256,
        "provenance_definition_sha256": V09_PROVENANCE_DEFINITION_SHA256,
        "overlay_definition_sha256": V09_OVERLAY_DEFINITION_SHA256,
        "portable_source_inputs": list(portable),
        "reviewed_at": CURRENT_V09_REVIEW_DATE.isoformat(),
        "counts": {
            "physical_sites": 44,
            "projects": 47,
            "evidence": 108,
            "countries": 25,
            "non_us_sites": 37,
            "official_boundary_projects": 5,
            "reviewed_site_locator_projects": 42,
        },
        "files": {
            name: {"bytes": len(payload), "sha256": _sha256_bytes(payload)}
            for name, payload in sorted(payloads.items())
        },
    }


def build_preview(output_dir: Path = CURRENT_V09_PREVIEW_DIR) -> dict[str, Any]:
    """Build the current v0.9 preview from tracked, hash-bound inputs."""
    output_dir = Path(output_dir)
    if output_dir.exists():
        raise VerifiedConstructionCoreError(f"refusing to overwrite {output_dir}")
    payloads, report, portable = _v09_payloads()
    manifest = _v09_manifest(payloads, report, portable)
    manifest_bytes = _json_bytes(manifest)
    complete = {
        **payloads,
        "manifest.json": manifest_bytes,
        "manifest.sha256": f"{_sha256_bytes(manifest_bytes)}  manifest.json\n".encode(),
    }
    output_dir.parent.mkdir(parents=True, exist_ok=True)
    stage = Path(tempfile.mkdtemp(prefix=f".{output_dir.name}.", dir=output_dir.parent))
    try:
        for name, payload in complete.items():
            with (stage / name).open("xb") as handle:
                handle.write(payload)
        os.replace(stage, output_dir)
    except Exception:
        shutil.rmtree(stage, ignore_errors=True)
        raise
    validate_preview(output_dir)
    return manifest


def _validate_v09_preview(path: Path) -> dict[str, Any]:
    path = Path(path)
    if path.is_symlink() or not path.is_dir():
        raise VerifiedConstructionCoreError("v0.9 preview directory differs")
    manifest_path = path / "manifest.json"
    checksum_path = path / "manifest.sha256"
    if (
        manifest_path.is_symlink()
        or not manifest_path.is_file()
        or checksum_path.is_symlink()
        or not checksum_path.is_file()
    ):
        raise VerifiedConstructionCoreError("v0.9 manifest trust root differs")
    manifest_bytes = manifest_path.read_bytes()
    if checksum_path.read_bytes() != (
        f"{_sha256_bytes(manifest_bytes)}  manifest.json\n".encode()
    ):
        raise VerifiedConstructionCoreError("v0.9 manifest checksum differs")
    manifest = _load_json(manifest_path)
    files = manifest.get("files") if isinstance(manifest, dict) else None
    if not isinstance(files, dict):
        raise VerifiedConstructionCoreError("v0.9 manifest files differ")
    expected_inventory = set(files) | {"manifest.json", "manifest.sha256"}
    if {item.name for item in path.iterdir()} != expected_inventory:
        raise VerifiedConstructionCoreError("v0.9 preview inventory differs")
    for name, metadata in files.items():
        member = path / name
        if (
            Path(name).name != name
            or member.is_symlink()
            or not member.is_file()
            or member.stat().st_size != metadata.get("bytes")
            or _sha256_file(member) != metadata.get("sha256")
        ):
            raise VerifiedConstructionCoreError(f"v0.9 member differs: {name}")
    payloads, report, portable = _v09_payloads()
    if manifest != _v09_manifest(payloads, report, portable):
        raise VerifiedConstructionCoreError("v0.9 manifest semantics differ")
    for name, expected in payloads.items():
        if (path / name).read_bytes() != expected:
            raise VerifiedConstructionCoreError(
                f"v0.9 generated member differs: {name}"
            )
    city_terms = _v09_contracts(
        hydrated_crosscheck=_v09_hydrated_crosscheck_available()
    )["city_terms"]
    legal_notice = _v09_city_legal_notice(city_terms).encode("utf-8")
    for name in ("README.md", "ATTRIBUTION.txt"):
        if legal_notice not in (path / name).read_bytes():
            raise VerifiedConstructionCoreError(
                f"v0.9 City legal notice missing from {name}"
            )
    return manifest


def validate_preview(path: Path = CURRENT_V09_PREVIEW_DIR) -> dict[str, Any]:
    """Validate current v0.9 or dispatch an immutable v0.1-v0.8 preview."""
    path = Path(path)
    manifest_path = path / "manifest.json"
    if (
        path.is_symlink()
        or not path.is_dir()
        or manifest_path.is_symlink()
        or not manifest_path.is_file()
    ):
        raise VerifiedConstructionCoreError("preview manifest trust root differs")
    manifest = _load_json(manifest_path)
    preview_id = manifest.get("preview_id") if isinstance(manifest, dict) else None
    if preview_id == CURRENT_V09_PREVIEW_ID:
        return _validate_v09_preview(path)
    validator = _frozen_preview_validator(preview_id)
    if validator is not None:
        return validator(path)
    raise VerifiedConstructionCoreError("preview id differs")


__all__ = [
    "CURRENT_V07_DEFINITION_PATHS",
    "CURRENT_V07_PREVIEW_DIR",
    "CURRENT_V07_PREVIEW_ID",
    "CURRENT_V07_REVIEW_DATE",
    "CURRENT_V08_DEFINITION_PATHS",
    "CURRENT_V08_PREVIEW_DIR",
    "CURRENT_V08_PREVIEW_ID",
    "CURRENT_V08_REVIEW_DATE",
    "CURRENT_V09_DEFINITION_PATHS",
    "CURRENT_V09_PREVIEW_DIR",
    "CURRENT_V09_PREVIEW_ID",
    "CURRENT_V09_REVIEW_DATE",
    "IMAGERY_REVIEW_DEFINITION",
    "LEGACY_PREVIEW_V01_DIR",
    "LEGACY_PREVIEW_V02_DIR",
    "LEGACY_PREVIEW_V03_DIR",
    "LEGACY_PREVIEW_V04_DIR",
    "LEGACY_PREVIEW_V05_COMMIT",
    "LEGACY_PREVIEW_V05_DIR",
    "LEGACY_PREVIEW_V06_COMMIT",
    "LEGACY_PREVIEW_V06_DIR",
    "LEGACY_PREVIEW_V06_MANIFEST_SHA256",
    "LEGACY_PREVIEW_V07_COMMIT",
    "LEGACY_PREVIEW_V07_DIR",
    "LEGACY_PREVIEW_V07_MANIFEST_SHA256",
    "LEGACY_PREVIEW_V08_COMMIT",
    "LEGACY_PREVIEW_V08_DIR",
    "LEGACY_PREVIEW_V08_MANIFEST_SHA256",
    "PREVIEW_DIR",
    "PreviewProfile",
    "PROVENANCE_DEFINITION",
    "VerifiedConstructionCoreError",
    "build_preview",
    "load_current_v07_profile",
    "load_current_v08_profile",
    "load_current_v09_profile",
    "validate_frozen_preview",
    "validate_frozen_v01",
    "validate_frozen_v02",
    "validate_frozen_v03",
    "validate_frozen_v04",
    "validate_frozen_v05",
    "validate_frozen_v06",
    "validate_frozen_v07",
    "validate_frozen_v08",
    "validate_preview",
]


# v0.10 is layered only after the complete v0.9 implementation.
_build_v09_preview = build_preview
_validate_v09_preview_dispatch = validate_preview

LEGACY_PREVIEW_V09_DIR = ROOT / "verified_construction_core" / "2026-08-20-preview-v0.9"
LEGACY_PREVIEW_V09_MANIFEST_SHA256 = "d9d9975f8e70dbe8aab14c82f0af4aed1cd9d25d8eb3566ae458c60026c7b5c8"
LEGACY_PREVIEW_V09_COMMIT = "d358b53f0518830aec5b654bfb8e7e1df8ba134a"
CURRENT_V10_PREVIEW_ID = "2026-08-20-preview-v0.10"
CURRENT_V10_PREVIEW_DIR = ROOT / "verified_construction_core" / CURRENT_V10_PREVIEW_ID
CURRENT_V10_REVIEW_DATE = date(2026, 8, 20)
V10_REVIEW_DEFINITION_SHA256 = "6eaaf362f2ec92e753c0199e972a9d616c8f9ae120a496a2ce9bf7ba514af813"
V10_OVERLAY_DEFINITION_SHA256 = "4f3c4227381e52fe9255ded079d2897c5a579c8e94a5d56bbec5bee474f8b9fc"
V10_PROVENANCE_DEFINITION_SHA256 = "4ca9f02c25392d1a5aeccab9252868cb7e156145c969c64c6ee53dd74ad6f6d9"
V10_IMAGERY_REVIEW_DEFINITION_SHA256 = "7a316cfd4bb78868fbf5feff4b3228c3a5fe5625d5bd8bf7ec56e6f6ad29284a"
V10_REVIEW_DEFINITION = ROOT / "definitions" / "verified-construction-core-v0.10-reviewed-sites.json"
V10_OVERLAY_DEFINITION = ROOT / "definitions" / "verified-construction-core-reviewed-overlays-v8.json"
V10_PROVENANCE_DEFINITION = ROOT / "definitions" / "verified-construction-core-v0.10-provenance.json"
V10_IMAGERY_REVIEW_DEFINITION = ROOT / "definitions" / "verified-construction-core-v0.10-imagery-reviews.json"
CURRENT_V10_DEFINITION_PATHS = (
    ("review_definition_sha256", V10_REVIEW_DEFINITION),
    ("imagery_review_definition_sha256", V10_IMAGERY_REVIEW_DEFINITION),
    ("provenance_definition_sha256", V10_PROVENANCE_DEFINITION),
    ("overlay_definition_sha256", V10_OVERLAY_DEFINITION),
)
V10_SELECTION_FIRST_FAILURE_COUNTS = {
    "entity_kind_not_project": 49,
    "not_in_reviewed_site_geometry_allowlist": 93,
    "selected": 50,
    "status_not_physical": 12,
    "status_outside_90_day_window": 327,
}
V10_COUNTRY_ISO_A2 = {"Germany": "DE", "United Kingdom": "GB", "United States": "US"}
V10_ACCEPTANCE_REQUIRED_FIELDS = V09_ACCEPTANCE_REQUIRED_FIELDS
V10_OVERLAY_REQUIRED_FIELDS = V09_OVERLAY_REQUIRED_FIELDS
V10_OVERLAY_REQUIRED_FIELD_ORDER = ('country',
 'physical_site_stable_key',
 'physical_site_entity_id',
 'source_input_path',
 'source_input_bytes',
 'source_input_sha256',
 'bridge_id',
 'bridge_schema',
 'bridge_path',
 'bridge_bytes',
 'bridge_sha256',
 'project_to_campus_relationship_id',
 'project_to_campus_relationship_type',
 'project_to_campus_decision_basis',
 'geometry_entity',
 'geometry_entity_stable_key',
 'geometry_entity_id',
 'geometry_target_entity_kind',
 'geometry_target_entity_stable_key',
 'geometry_target_entity_id',
 'geometry_evidence_key',
 'geometry_evidence_id',
 'geometry_source_kind',
 'geometry_derivation',
 'overlay_id',
 'geometry_method',
 'geometry_scope_class',
 'geometry_authority_class',
 'geometry_use_scope',
 'official_boundary',
 'horizontal_uncertainty_metres',
 'horizontal_uncertainty_unknown_reason',
 'precision_scope',
 'bridge_decision',
 'portable_input_binding',
 'portable_capture_bindings',
 'rejected_claims',
 'source_project_stable_key',
 'source_project_entity_id',
 'decision',
 'decision_reason',
 'reviewed_at')
V10_EVIDENCE_IDS = frozenset({
    "0cad69d1-3151-5073-af8e-73f8401bf2cb",
    "465b57e0-58b6-56aa-8b00-728b9cc9c201",
    "6bf6472d-a1f7-53fd-b0d5-2317d80df706",
    "6e4b0bfe-23de-5439-9dc0-3688a382a285",
    "850336d5-4d8f-5821-ae65-cb195961e7b6",
    "b0d7fbf5-7de2-58e0-a54f-d6fec6d08a6a",
    "c0b3ca09-1341-5395-9301-9f6a2d67cf0c",
    "e0cbb7f3-f4ab-52c7-a4ed-10424c78e411",
})
V10_PROVENANCE_SEMANTICS_SHA256 = "5f7ade193dcdfc65e72004185daf349edb85f6c8f0edd3784292205d12ea23ad"
V10_CAPACITY_FIELDS = (
    "entity_id", "entity_kind", "name", "metric", "stage", "unit",
    "low", "base", "high", "method", "confidence", "as_of_date",
    "target_date", "evidence_id", "notes", "source_url",
    "source_publisher", "source_license", "source_retrieved_at",
)
V10_BRIDGE_PROFILES = {'curated:colt-london-hayes-campus:london4-current-facility-build': {'acceptance_sha256': '24af4d1d8d9270bfdd0a0cdb698e9b908c9aa42da1f5446092c7c4e3101aabbb',
                                                                     'bridge_bytes': 35538,
                                                                     'bridge_id': 'verified-construction-core-v0.10-colt-london4-project-geometry-bridge',
                                                                     'bridge_path': 'sources/verified-construction-core-v0.10-colt-london4-project-geometry-bridge.json',
                                                                     'bridge_sha256': '8eb58d7d46eda6cae3b161e4a2e7c75a28371e4a9dbd1cc06ffd77fecbefe883',
                                                                     'construction_sha256': 'd5bd67e030f618e331aacd2c9141882afff4e2906440fd2a36949838cc4c3af7',
                                                                     'enriched_way_sha256': 'ed7d8e290bb1600dcb013e40b6fcc95ada3aad357b3a0e3083008301b0ed1b09',
                                                                     'evidence_field': 'capacity_evidence',
                                                                     'geometry_entity_id': 'd34a5fdb-5c7e-5da7-8b79-ffeebc48be25',
                                                                     'geometry_evidence_id': 'c0b3ca09-1341-5395-9301-9f6a2d67cf0c',
                                                                     'geometry_evidence_key': 'osm-way-1495920404-colt-london4-captured-2026-08-20',
                                                                     'geometry_kind': 'building',
                                                                     'geometry_source_kind': 'openstreetmap',
                                                                     'geometry_stable_key': 'osm:way/1495920404',
                                                                     'latitude': 51.5095591,
                                                                     'longitude': -0.394089,
                                                                     'overlay_sha256': 'c1c79a4039e21e66fcf22f0a8014cc97d91da19e89d2b85f9be96d4f74e6eb59',
                                                                     'polygon_sha256': '60c1d42e5bf550c100334cb77426d229a268c3eb849dd0932fe7348d317cb8e1',
                                                                     'raw_response_bytes': 1639,
                                                                     'raw_response_sha256': '3bd140b2eb8fa082c8727955d4fef1bd26fa668356c8654918ac922b5ae6eef6',
                                                                     'raw_way_sha256': '70faf0e8cca8652c815ced5f25a16a420dfba9206994ec0c18e48dc91d608e15',
                                                                     'release_rows_sha256': 'a40d74ec2ae6da784b3e66a1eff5a3b861105fe0f083082d97a5c40a8bea1e59',
                                                                     'release_sha256': 'fc927c3f55d942fd40c0bd6addc5fe303aa82fe25ee1814e54e36adb0b58c432',
                                                                     'schema': 'verified-construction-core-v0.10-portable-capture-replayed-project-geometry-bridge-input-v1',
                                                                     'semantic_hashes': {'claim_guardrails': 'e5aa26f4ae55ec36512ee83a46bc050f5009ea89c8237e11c62d9d9d101ec249',
                                                                                         'false_match_guardrails': '7cdec7697b45b059d3d6f28add77f81b542a6f80dde50713ba4de1bf55e50b79',
                                                                                         'geometry_capture': '46050e653bd57381fb88253caecdd693d435badfb33921c474fab90a05f605b7',
                                                                                         'geometry_entity': '2edf84f15665301685d8c4403db22e9bedecb6765842b59bac1f575a43617ea8',
                                                                                         'geometry_evidence': 'ac06f7d53039bf4d7cbd0dbaa58c5c68135c8e8fd80490bd3be73a4b657e0bc2',
                                                                                         'geometry_release': '38e0b9de817f645c4bec37c0d4a3e58baecccb040f5718dc069a72c7385a0bed',
                                                                                         'identity_bridge_evidence': 'c87382ea6928a2bd7c3e90f3d8549135a3e4ab6e5f368662d9a03547a5645f6a',
                                                                                         'imagery_posture': '5371874de7b49eee64e534372b02c526792438cbd7c0b983223f7cf6c634d036',
                                                                                         'materialization_scope': '39541f7699bb81ca619518e2d85dd5190ecae6cd89e862dc67f023abac36acad',
                                                                                         'review_decision': '86c7dc707a44cb9e2dd38b633cb67a78d09cbc64db86831c184b26fdab436872',
                                                                                         'rights': 'f836dbf3626d0ed322aa9dc8536a8636a21b15412b284d244e744d0c35ab2349'},
                                                                     'source_bytes': 11304,
                                                                     'source_evidence_ids': ('465b57e0-58b6-56aa-8b00-728b9cc9c201',
                                                                                             '6e4b0bfe-23de-5439-9dc0-3688a382a285'),
                                                                     'source_path': 'sources/curated-official-2026-07-19-colt-london4-hayes.json',
                                                                     'source_semantic_sha256': 'b185eefb33dab41ba8efe688efb9243977664abef558b03f00bffb9c0788d47e',
                                                                     'source_sha256': '44a4569bed0c12103d726d0e73f8a05078f4528c74c7f032dfc5feaa9ea0b05b',
                                                                     'target_entity_id': '0bac3fe4-c3dd-54ba-a500-e37599191432',
                                                                     'target_kind': 'project',
                                                                     'target_stable_key': 'curated:colt-london-hayes-campus:london4-current-facility-build',
                                                                     'topology_sha256': '79b307b0c51c2e960a5fa5d792539effaa48b13271542f9dbe3353ae95c7517a',
                                                                     'way_id': 1495920404,
                                                                     'way_version': 3},
 'curated:cyrusone-fra7-frankfurt-westside-campus:current-multi-building-development': {'acceptance_sha256': 'a702e78aca1ae18fc9606381fa924ef96a2a1c0bd6ad58abe3d69539e8f8f4b1',
                                                                                        'bridge_bytes': 30050,
                                                                                        'bridge_id': 'verified-construction-core-v0.10-cyrusone-fra7-project-geometry-bridge',
                                                                                        'bridge_path': 'sources/verified-construction-core-v0.10-cyrusone-fra7-project-geometry-bridge.json',
                                                                                        'bridge_sha256': '43390493b8b88719f8932c1a0781f94d69192642d105f15e2f66b4a479d4b402',
                                                                                        'construction_sha256': '693f55b7f392ee8a168ae6bf89bfc9dce972edda78ddf0499f4efb664a478ef3',
                                                                                        'evidence_field': 'capacity_evidence',
                                                                                        'geometry_entity_id': '58f9a985-bfa8-521c-ab85-132aec6799ed',
                                                                                        'geometry_evidence_id': '850336d5-4d8f-5821-ae65-cb195961e7b6',
                                                                                        'geometry_evidence_key': 'rp-darmstadt-cyrusone-fra7-ff7-l1-coordinate-captured-2026-08-20',
                                                                                        'geometry_kind': 'project',
                                                                                        'geometry_source_kind': 'government_permit',
                                                                                        'geometry_stable_key': 'curated:cyrusone-fra7-frankfurt-westside-campus:current-multi-building-development',
                                                                                        'latitude': 50.093438822,
                                                                                        'longitude': 8.5805137288,
                                                                                        'overlay_sha256': '4700aaf9d6b82a3d579e3f3a53d2e22f8263a5ccc4bbae272d2690ffd6efba7e',
                                                                                        'release_rows_sha256': '3e22a111b472b5f57c38c7c893e512e90865d123c3bbea37dd41189c5fde7772',
                                                                                        'release_sha256': 'fc927c3f55d942fd40c0bd6addc5fe303aa82fe25ee1814e54e36adb0b58c432',
                                                                                        'schema': 'verified-construction-core-v0.10-portable-direct-official-point-geometry-bridge-input-v1',
                                                                                        'semantic_hashes': {'claim_guardrails': '1104143bf5d3db7213127c4ede7c83080f8a0f4fc7f59ccbe3a7e4509cc29d04',
                                                                                                            'false_match_guardrails': '7297971f54ec08466d88833851d71d1947381aa3b5416a346c54d1bc63655e15',
                                                                                                            'geometry_capture': 'fb681389c7d4584df1b56e6774d0767249eb517d019e49089d8b819f43bbeb88',
                                                                                                            'geometry_entity': '27c9a09ef893751b72a4cedd2874a7f12d8a4254dacef9d04505717843fd8aa3',
                                                                                                            'geometry_evidence': 'de9cdd3970fa5df38a91c16564c6d508dbb10d7e3e3a159af71de6e964a30377',
                                                                                                            'geometry_release': '38e0b9de817f645c4bec37c0d4a3e58baecccb040f5718dc069a72c7385a0bed',
                                                                                                            'identity_bridge_evidence': 'c236464380c33a31b91dd217d8bc30afa552fdc8ad4cdac1b8690038a07a5765',
                                                                                                            'imagery_posture': '4604ff6fc044a92f393ade7fbea0be0f7358bc639ac9de48a9780f22347e9870',
                                                                                                            'materialization_scope': '39541f7699bb81ca619518e2d85dd5190ecae6cd89e862dc67f023abac36acad',
                                                                                                            'review_decision': 'e034c937527944c62bb6a195f43fe387d0b879a8d41d1d9856aabed7918eac7d',
                                                                                                            'rights': 'aadb24043cc4b38283676a2ec59bae835fae1924d5ef2da2a62b0ead793ea6e3'},
                                                                                        'source_bytes': 12723,
                                                                                        'source_evidence_ids': ('6bf6472d-a1f7-53fd-b0d5-2317d80df706',
                                                                                                                'fa3a4aa6-5df4-57f3-be02-7f0764f605e1'),
                                                                                        'source_path': 'sources/curated-official-2026-07-19-cyrusone-fra7-frankfurt-westside.json',
                                                                                        'source_semantic_sha256': '531410ce4ca64f5d98046d930ff14d860870a43a4e8b0a61e4b2ee47b069d8cc',
                                                                                        'source_sha256': 'fa736c847a85f5d99bb4f0cd608cea5dc82f381905fea017a9a7d8e3b66324c0',
                                                                                        'target_entity_id': '58f9a985-bfa8-521c-ab85-132aec6799ed',
                                                                                        'target_kind': 'project',
                                                                                        'target_stable_key': 'curated:cyrusone-fra7-frankfurt-westside-campus:current-multi-building-development',
                                                                                        'topology_sha256': 'c57995fa212c7ece44fae82ad9238ee0c98840570ced4d1711b1cd297f2d1762'},
 'curated:oracle-project-jupiter-dona-ana-campus:current-campus-build': {'acceptance_sha256': '0699db6bd54a78162383f44db39ee495fb228813feafb838997e4274db0af127',
                                                                         'bridge_bytes': 31170,
                                                                         'bridge_id': 'verified-construction-core-v0.10-oracle-project-jupiter-campus-geometry-bridge',
                                                                         'bridge_path': 'sources/verified-construction-core-v0.10-oracle-project-jupiter-campus-geometry-bridge.json',
                                                                         'bridge_sha256': '29a6d9aaa71335918c2224fc491e1b9067cb92aef7dcc1d4ac55195c77d64c61',
                                                                         'construction_sha256': 'd99fcd552c876759da17514143ca787f8b350cc11e4a6b3c790e912cc7a0466f',
                                                                         'evidence_field': 'capacity_workload_evidence',
                                                                         'geometry_entity_id': 'a83a35df-3efb-505f-bfe6-950fff586dee',
                                                                         'geometry_evidence_id': '0cad69d1-3151-5073-af8e-73f8401bf2cb',
                                                                         'geometry_evidence_key': None,
                                                                         'geometry_kind': 'facility',
                                                                         'geometry_source_kind': 'openstreetmap',
                                                                         'geometry_stable_key': 'osm:way/1476734418',
                                                                         'latitude': 31.8182982,
                                                                         'longitude': -106.66530225,
                                                                         'overlay_sha256': 'dc3d520f76cc058fab3d0c990690edc8bd80676e05f97868f05d7b86fd482c86',
                                                                         'release_rows_sha256': '30d40e15d176e9aa661f3ff78b384033149f8f53effbc99a38249a4873d68428',
                                                                         'release_sha256': 'fc927c3f55d942fd40c0bd6addc5fe303aa82fe25ee1814e54e36adb0b58c432',
                                                                         'schema': 'verified-construction-core-v0.10-portable-global-v3-campus-geometry-bridge-input-v1',
                                                                         'semantic_hashes': {'claim_guardrails': '7bb3673c53444468fb7aa70ce87add507d8c85b0415e11537ee880c66549797e',
                                                                                             'false_match_guardrails': 'c7f9817f78b58551f055a4f6abf2af1052078fcafb1270912ed0c737628e3499',
                                                                                             'geometry_capture': '613dc63983089530584840b935fab17b4d35d1d14cbfdab4a90d7871b6289c02',
                                                                                             'geometry_entity': '3e1fca87bb98ac70cb2a43a0f74b97668df3d8fd05c7d0ba261216f789489005',
                                                                                             'geometry_evidence': 'f3f8b7101fd3d02574ade4c0b98f382d0b2ba0b3828230cd8d255b3b37ec20dd',
                                                                                             'geometry_release': 'c023a80963a2f35ad5074450f1219023f38c7216531a0ce7a99f3d3756da54aa',
                                                                                             'identity_bridge_evidence': '56a84a3237e2721723c0ab5884ac24830c9e06b4b65ba7b5f36f759efcb0e5eb',
                                                                                             'imagery_posture': '0fcbb8bd0d87e823ce79c41dad00badd0b76db28249c7e54f5bae4597e18cbee',
                                                                                             'materialization_scope': '39541f7699bb81ca619518e2d85dd5190ecae6cd89e862dc67f023abac36acad',
                                                                                             'review_decision': '30f9378b312332a11ae1369dc77cce49909c4e98ff90ffcc110f2c9c362f31c3',
                                                                                             'rights': '8c090fb447ba0b54662bc1ddd2d8dd3e72998afd371ac27dddeaf8549cb01c70'},
                                                                         'source_bytes': 17808,
                                                                         'source_evidence_ids': ('e0cbb7f3-f4ab-52c7-a4ed-10424c78e411',
                                                                                                 'b68ce146-3dea-57e1-bb04-fbe710b2f39d'),
                                                                         'source_path': 'sources/curated-official-2026-07-19-oracle-project-jupiter-dona-ana.json',
                                                                         'source_semantic_sha256': '52d1cf16facbefe3de4b4dfe969c9b13f88816cbf6ee8c86de9b1bf2597ee218',
                                                                         'source_sha256': '817a653cd86ca0e1dbacb84a3cdd7730597a657395623c2fe21896f781dae6cc',
                                                                         'target_entity_id': '993d8d6e-de3d-526a-9e01-09e291b66839',
                                                                         'target_kind': 'campus',
                                                                         'target_stable_key': 'curated:oracle-project-jupiter-dona-ana-campus',
                                                                         'topology_sha256': 'da2fb6d5fcd7648c4863dda0b2e8b1f495beab2cd74592dee5089f7f7ff8ac70'}}

V10_GEOMETRY_CONTRACTS = {
    "curated:oracle-project-jupiter-dona-ana-campus:current-campus-build": {
        "geometry_entity": "facility",
        "geometry_entity_stable_key": "osm:way/1476734418",
        "geometry_entity_id": "a83a35df-3efb-505f-bfe6-950fff586dee",
        "geometry_target_entity_kind": "campus",
        "geometry_target_entity_stable_key": (
            "curated:oracle-project-jupiter-dona-ana-campus"
        ),
        "geometry_target_entity_id": "993d8d6e-de3d-526a-9e01-09e291b66839",
        "geometry_source_kind": "openstreetmap",
        "geometry_derivation": "cross_source_geometry",
        "geometry_method": (
            "frozen_openstreetmap_exact_project_jupiter_named_development_polygon_"
            "as_parent_campus_locator"
        ),
        "geometry_scope_class": (
            "reviewed_openstreetmap_named_project_jupiter_development_polygon"
        ),
        "geometry_authority_class": "community_mapped",
        "geometry_use_scope": "campus_locator",
        "official_boundary": False,
        "bridge_decision": "accepted_as_campus_locator",
    },
    (
        "curated:cyrusone-fra7-frankfurt-westside-campus:"
        "current-multi-building-development"
    ): {
        "geometry_entity": "project",
        "geometry_entity_stable_key": (
            "curated:cyrusone-fra7-frankfurt-westside-campus:"
            "current-multi-building-development"
        ),
        "geometry_entity_id": "58f9a985-bfa8-521c-ab85-132aec6799ed",
        "geometry_target_entity_kind": "project",
        "geometry_target_entity_stable_key": (
            "curated:cyrusone-fra7-frankfurt-westside-campus:"
            "current-multi-building-development"
        ),
        "geometry_target_entity_id": "58f9a985-bfa8-521c-ab85-132aec6799ed",
        "geometry_source_kind": "government_permit",
        "geometry_derivation": "official_coordinate_transform",
        "geometry_method": (
            "rp_darmstadt_ff7_l1_etrs89_utm32_coordinate_to_wgs84_point"
        ),
        "geometry_scope_class": "official_source_reported_ff7_l1_reference_point",
        "geometry_authority_class": "official_source",
        "geometry_use_scope": "project_locator",
        "official_boundary": False,
        "bridge_decision": "accepted_as_project_locator",
    },
    "curated:colt-london-hayes-campus:london4-current-facility-build": {
        "geometry_entity": "building",
        "geometry_entity_stable_key": "osm:way/1495920404",
        "geometry_entity_id": "d34a5fdb-5c7e-5da7-8b79-ffeebc48be25",
        "geometry_target_entity_kind": "project",
        "geometry_target_entity_stable_key": (
            "curated:colt-london-hayes-campus:london4-current-facility-build"
        ),
        "geometry_target_entity_id": "0bac3fe4-c3dd-54ba-a500-e37599191432",
        "geometry_source_kind": "openstreetmap",
        "geometry_derivation": "capture_replayed_geometry",
        "geometry_method": (
            "capture_replayed_openstreetmap_exact_colt_london4_named_building_"
            "polygon_as_project_locator"
        ),
        "geometry_scope_class": (
            "community_mapped_named_colt_london4_building_polygon"
        ),
        "geometry_authority_class": "community_mapped",
        "geometry_use_scope": "project_locator",
        "official_boundary": False,
        "bridge_decision": "accepted_as_project_locator",
    },
}
V10_ACCEPTANCE_NON_SEMANTIC_FIELDS = frozenset(
    {
        "source_input_path",
        "source_input_bytes",
        "source_input_sha256",
        "bridge_path",
        "bridge_bytes",
        "bridge_sha256",
        "portable_input_binding",
        "portable_capture_bindings",
    }
)
V10_ACCEPTANCE_SEMANTICS_SHA256 = {
    "curated:oracle-project-jupiter-dona-ana-campus:current-campus-build": (
        "d5c5392d8f12ca08b01ba1f0b8719ce6c8b6871ac0d2f4c70d38c3ee36f606c8"
    ),
    (
        "curated:cyrusone-fra7-frankfurt-westside-campus:"
        "current-multi-building-development"
    ): "e513fde801c5dab2c90b956339483b835d81bafb475db4a5d260859a526a9e7a",
    "curated:colt-london-hayes-campus:london4-current-facility-build": (
        "433c233c18609f4ab05fe99f2553f5655c80410dd517b4d5c4366919850257a3"
    ),
}
V10_GEOMETRY_ENTITY_SHA256 = {
    "curated:oracle-project-jupiter-dona-ana-campus:current-campus-build": (
        "3e1fca87bb98ac70cb2a43a0f74b97668df3d8fd05c7d0ba261216f789489005"
    ),
    (
        "curated:cyrusone-fra7-frankfurt-westside-campus:"
        "current-multi-building-development"
    ): "27c9a09ef893751b72a4cedd2874a7f12d8a4254dacef9d04505717843fd8aa3",
    "curated:colt-london-hayes-campus:london4-current-facility-build": (
        "2edf84f15665301685d8c4403db22e9bedecb6765842b59bac1f575a43617ea8"
    ),
}


def _v10_acceptance_semantics(acceptance: Mapping[str, Any]) -> dict[str, Any]:
    return {
        field: value
        for field, value in acceptance.items()
        if field not in V10_ACCEPTANCE_NON_SEMANTIC_FIELDS
    }


def load_current_v10_profile(
    definition_sha256: Mapping[str, str],
) -> PreviewProfile:
    """Load v0.10 only after all four final definition hashes are supplied."""
    expected_fields = {field for field, _ in CURRENT_V10_DEFINITION_PATHS}
    if set(definition_sha256) != expected_fields:
        raise VerifiedConstructionCoreError(
            "current v0.10 definition pins are incomplete"
        )
    pins: list[tuple[str, Path, str]] = []
    for field, path in CURRENT_V10_DEFINITION_PATHS:
        expected = definition_sha256[field]
        if (
            re.fullmatch(r"[0-9a-f]{64}", expected) is None
            or path.is_symlink()
            or not path.is_file()
            or _sha256_file(path) != expected
        ):
            raise VerifiedConstructionCoreError(
                f"current v0.10 {field} source hash differs"
            )
        pins.append((field, path, expected))
    return PreviewProfile(
        preview_id=CURRENT_V10_PREVIEW_ID,
        preview_dir=CURRENT_V10_PREVIEW_DIR,
        reviewed_at=CURRENT_V10_REVIEW_DATE,
        base_preview_id=CURRENT_V09_PREVIEW_ID,
        base_preview_dir=LEGACY_PREVIEW_V09_DIR,
        base_manifest_sha256=LEGACY_PREVIEW_V09_MANIFEST_SHA256,
        base_commit=LEGACY_PREVIEW_V09_COMMIT,
        definition_pins=tuple(pins),
    )


def validate_frozen_v09(
    path: Path = LEGACY_PREVIEW_V09_DIR,
) -> dict[str, Any]:
    """Validate byte-frozen v0.9 from its immutable inventory only."""
    return _validate_frozen_preview_inventory(
        path,
        preview_id=CURRENT_V09_PREVIEW_ID,
        manifest_sha256=LEGACY_PREVIEW_V09_MANIFEST_SHA256,
        version_label="v0.9",
    )


_FROZEN_PREVIEW_VALIDATORS = (
    *_FROZEN_PREVIEW_VALIDATORS,
    (CURRENT_V09_PREVIEW_ID, validate_frozen_v09),
)


def _v10_definition_paths() -> dict[str, tuple[Path, str]]:
    return {
        "review_definition_sha256": (
            V10_REVIEW_DEFINITION,
            V10_REVIEW_DEFINITION_SHA256,
        ),
        "imagery_review_definition_sha256": (
            V10_IMAGERY_REVIEW_DEFINITION,
            V10_IMAGERY_REVIEW_DEFINITION_SHA256,
        ),
        "provenance_definition_sha256": (
            V10_PROVENANCE_DEFINITION,
            V10_PROVENANCE_DEFINITION_SHA256,
        ),
        "overlay_definition_sha256": (
            V10_OVERLAY_DEFINITION,
            V10_OVERLAY_DEFINITION_SHA256,
        ),
    }


def _validate_v10_definition_pins() -> None:
    for field, (path, expected) in _v10_definition_paths().items():
        if path.is_symlink() or not path.is_file() or _sha256_file(path) != expected:
            raise VerifiedConstructionCoreError(
                f"current v0.10 {field} source hash differs"
            )


def _v10_hydrated_crosscheck_available() -> bool:
    required = (
        ROOT / "releases/2026-07-22-open-seed-v97/construction_pipeline.csv",
        ROOT / "releases/2026-07-22-open-seed-v97/entities.csv",
        ROOT / "releases/2026-07-22-open-seed-v97/evidence.csv",
        ROOT / "releases/2026-07-22-open-seed-v97/capacity_estimates.csv",
        ROOT / "releases/2026-07-18-global-open-v3/entities.csv",
        ROOT / "releases/2026-07-18-global-open-v3/evidence.csv",
        ROOT
        / "exact_identity_decisions/2026-07-22-public-open-v14/component-members.csv",
        ROOT
        / "exact_identity_decisions/2026-07-22-public-open-v14/relationships.csv",
    )
    present = [path.is_file() for path in required]
    if any(present) and not all(present):
        raise VerifiedConstructionCoreError(
            "v0.10 hydrated cross-check inputs are only partially present"
        )
    return all(present)


def _v10_flat_source_row(
    binding: Any,
    member: Mapping[str, Any],
    fields: Sequence[str],
    *,
    label: str,
    hydrated_crosscheck: bool,
) -> dict[str, str]:
    if not isinstance(binding, dict) or set(binding) != {
        "path",
        "line",
        "bytes",
        "sha256",
        "raw_csv_record_base64",
    }:
        raise VerifiedConstructionCoreError(f"{label} flat source-row binding differs")
    if binding["path"] != member.get("path"):
        raise VerifiedConstructionCoreError(f"{label} source member differs")
    embedded = {
        field: binding[field]
        for field in ("line", "bytes", "sha256", "raw_csv_record_base64")
    }
    row = _embedded_csv_source_row(embedded, fields, label=label)
    if hydrated_crosscheck:
        path = _repository_input(
            str(member["path"]), str(member["sha256"]), f"{label} hydrated member"
        )
        if path.stat().st_size != member["bytes"]:
            raise VerifiedConstructionCoreError(
                f"{label} hydrated member byte count differs"
            )
        _validate_hydrated_source_row(path, embedded, fields, label=label)
    return row


def _v10_capacity_projection(row: Mapping[str, str]) -> dict[str, Any]:
    return {
        "as_of_date": row["as_of_date"],
        "base": _csv_float(row["base"], label="v0.10 capacity base"),
        "confidence": _csv_float(
            row["confidence"], label="v0.10 capacity confidence"
        ),
        "evidence_id": row["evidence_id"],
        "high": _csv_float(row["high"], label="v0.10 capacity high"),
        "low": _csv_float(row["low"], label="v0.10 capacity low"),
        "method": row["method"],
        "metric": row["metric"],
        "notes": row["notes"],
        "stage": row["stage"],
        "target_date": _nullable_csv_value(row["target_date"]),
        "unit": row["unit"],
    }


def _v10_validate_flat_topology(
    topology: Any,
    *,
    project: Mapping[str, Any],
    campus: Mapping[str, Any],
    acceptance: Mapping[str, Any],
    hydrated_crosscheck: bool,
) -> None:
    expected_fields = {
        "basis",
        "bundle_id",
        "manifest",
        "members",
        "relationship_id",
        "relationship_type",
        "subject_component_id",
        "subject_entity_id",
        "object_component_id",
        "object_entity_id",
        "decision_basis",
        "relationship_source_row",
        "subject_member_source_row",
        "object_member_source_row",
    }
    if (
        not isinstance(topology, dict)
        or set(topology) != expected_fields
        or topology.get("basis") != "v14_project_targets_explicit_parent"
        or topology.get("bundle_id") != "2026-07-22-public-open-v14"
    ):
        raise VerifiedConstructionCoreError("v0.10 topology fields differ")
    _, manifest = _manifest_binding(
        topology["manifest"], label="v0.10 exact-identity release"
    )
    _validate_manifest_members(
        manifest, topology["members"], label="v0.10 exact-identity release"
    )
    if set(topology["members"]) != {"component-members.csv", "relationships.csv"}:
        raise VerifiedConstructionCoreError("v0.10 topology members differ")
    subject = _v10_flat_source_row(
        topology["subject_member_source_row"],
        topology["members"]["component-members.csv"],
        EXACT_COMPONENT_FIELDS,
        label="v0.10 topology subject",
        hydrated_crosscheck=hydrated_crosscheck,
    )
    object_member = _v10_flat_source_row(
        topology["object_member_source_row"],
        topology["members"]["component-members.csv"],
        EXACT_COMPONENT_FIELDS,
        label="v0.10 topology object",
        hydrated_crosscheck=hydrated_crosscheck,
    )
    relationship = _v10_flat_source_row(
        topology["relationship_source_row"],
        topology["members"]["relationships.csv"],
        EXACT_RELATIONSHIP_FIELDS,
        label="v0.10 topology relationship",
        hydrated_crosscheck=hydrated_crosscheck,
    )
    for label, row, entity, kind in (
        ("subject", subject, project, "project"),
        ("object", object_member, campus, "campus"),
    ):
        occurrence_id = f"{SOURCE_RELEASE_ID}:{entity['entity_id']}"
        expected_component_id = _exact_component_id(kind, occurrence_id)
        if (
            row["component_id"] != expected_component_id
            or row["occurrence_id"] != occurrence_id
            or row["release_id"] != SOURCE_RELEASE_ID
            or row["entity_id"] != entity["entity_id"]
            or row["entity_kind"] != kind
            or row["stable_key"] != entity["stable_key"]
            or not row["source_family"]
            or row["source_root"] != row["source_family"]
            or not row["snapshot_evidence_id"]
            or _csv_integer(
                row["component_member_count"],
                label=f"v0.10 topology {label}",
            )
            != 1
            or row["identity_proof_parent_occurrence_id"]
            or row["identity_proof_token"]
            or _csv_json(
                row["typed_identity_tokens_json"],
                label=f"v0.10 topology {label} typed tokens",
            )
            != []
            or _csv_json(
                row["ambiguous_identity_tokens_json"],
                label=f"v0.10 topology {label} ambiguous tokens",
            )
            != []
        ):
            raise VerifiedConstructionCoreError(
                f"v0.10 topology {label} projection differs"
            )
    relationship_projection = _exact_relationship_projection(relationship)
    expected_relationship_id = _exact_relationship_id(
        "project_targets", subject["component_id"], object_member["component_id"]
    )
    if (
        relationship_projection["relationship_id"] != expected_relationship_id
        or relationship_projection["relationship_type"] != "project_targets"
        or relationship_projection["subject_component_id"]
        != subject["component_id"]
        or relationship_projection["subject_kind"] != "project"
        or relationship_projection["object_component_id"]
        != object_member["component_id"]
        or relationship_projection["object_kind"] != "campus"
        or relationship_projection["decision_basis"] != "explicit_parent"
        or relationship_projection["typed_identity_tokens"] != []
        or relationship_projection["source_release_ids"] != [SOURCE_RELEASE_ID]
        or relationship_projection["raw_relationship_count"] != 1
        or topology["relationship_id"] != expected_relationship_id
        or topology["relationship_type"] != "project_targets"
        or topology["subject_component_id"] != subject["component_id"]
        or topology["subject_entity_id"] != project["entity_id"]
        or topology["object_component_id"] != object_member["component_id"]
        or topology["object_entity_id"] != campus["entity_id"]
        or topology["decision_basis"] != "explicit_parent"
        or acceptance["project_to_campus_relationship_id"]
        != expected_relationship_id
        or acceptance["project_to_campus_relationship_type"] != "project_targets"
        or acceptance["project_to_campus_decision_basis"] != "explicit_parent"
    ):
        raise VerifiedConstructionCoreError(
            "v0.10 topology relationship projection differs"
        )


def _v10_validate_construction_source(
    bridge: Mapping[str, Any],
    acceptance: Mapping[str, Any],
    profile: Mapping[str, Any],
    *,
    hydrated_crosscheck: bool,
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    construction = bridge.get("construction_source")
    expected_evidence_field = profile["evidence_field"]
    expected_fields = {
        "input",
        "release",
        "release_rows",
        "project",
        "campus",
        "project_to_campus",
        "status_evidence",
        expected_evidence_field,
        "capacity_estimate",
    }
    if (
        not isinstance(construction, dict)
        or set(construction) != expected_fields
        or _sha256_bytes(_json_bytes(construction))
        != profile["construction_sha256"]
        or _sha256_bytes(_json_bytes(construction["release"]))
        != profile["release_sha256"]
        or _sha256_bytes(_json_bytes(construction["release_rows"]))
        != profile["release_rows_sha256"]
        or _sha256_bytes(_json_bytes(construction["project_to_campus"]))
        != profile["topology_sha256"]
    ):
        raise VerifiedConstructionCoreError(
            "v0.10 immutable construction profile differs"
        )
    expected_input = {
        "path": acceptance["source_input_path"],
        "bytes": acceptance["source_input_bytes"],
        "sha256": acceptance["source_input_sha256"],
    }
    if (
        construction["input"] != expected_input
        or acceptance["portable_input_binding"] != expected_input
        or expected_input
        != {
            "path": profile["source_path"],
            "bytes": profile["source_bytes"],
            "sha256": profile["source_sha256"],
        }
    ):
        raise VerifiedConstructionCoreError("v0.10 source input binding differs")
    source_path = _repository_input(
        expected_input["path"], expected_input["sha256"], "v0.10 curated source"
    )
    if source_path.stat().st_size != expected_input["bytes"]:
        raise VerifiedConstructionCoreError("v0.10 curated source bytes differ")
    source = _load_json(source_path)
    if (
        not isinstance(source, dict)
        or set(source)
        != {
            "schema_version",
            "project",
            "campus",
            "evidence",
            "lifecycle",
            "operating_models",
            "workloads",
            "capacities",
        }
        or source.get("schema_version") != "1.0"
        or _sha256_bytes(_json_bytes(source)) != profile["source_semantic_sha256"]
    ):
        raise VerifiedConstructionCoreError("v0.10 curated source semantics differ")
    release = construction["release"]
    if (
        not isinstance(release, dict)
        or set(release) != {"release_id", "manifest", "members"}
        or release["release_id"] != SOURCE_RELEASE_ID
    ):
        raise VerifiedConstructionCoreError("v0.10 v97 release differs")
    _, source_manifest = _manifest_binding(
        release["manifest"], label="v0.10 v97 release"
    )
    _validate_manifest_members(
        source_manifest, release["members"], label="v0.10 v97 release"
    )
    if set(release["members"]) != {
        "construction_pipeline.csv",
        "entities.csv",
        "evidence.csv",
        "capacity_estimates.csv",
    }:
        raise VerifiedConstructionCoreError("v0.10 v97 members differ")
    rows = construction["release_rows"]
    expected_row_keys = {
        "construction_pipeline_project",
        "entities_project",
        "entities_campus",
        "status_evidence",
        expected_evidence_field,
        "capacity_estimate",
    }
    if set(rows) != expected_row_keys:
        raise VerifiedConstructionCoreError("v0.10 flat release-row inventory differs")
    project_row = _v10_flat_source_row(
        rows["construction_pipeline_project"],
        release["members"]["construction_pipeline.csv"],
        SOURCE_ENTITY_FIELDS,
        label="v0.10 construction project",
        hydrated_crosscheck=hydrated_crosscheck,
    )
    entities_project_row = _v10_flat_source_row(
        rows["entities_project"],
        release["members"]["entities.csv"],
        SOURCE_ENTITY_FIELDS,
        label="v0.10 entities project",
        hydrated_crosscheck=hydrated_crosscheck,
    )
    campus_row = _v10_flat_source_row(
        rows["entities_campus"],
        release["members"]["entities.csv"],
        SOURCE_ENTITY_FIELDS,
        label="v0.10 entities campus",
        hydrated_crosscheck=hydrated_crosscheck,
    )
    if project_row != entities_project_row:
        raise VerifiedConstructionCoreError("v0.10 duplicate project rows differ")
    evidence_rows: dict[str, dict[str, Any]] = {}
    for field in ("status_evidence", expected_evidence_field):
        evidence_row = _v10_flat_source_row(
            rows[field],
            release["members"]["evidence.csv"],
            GLOBAL_GEOMETRY_EVIDENCE_FIELDS,
            label=f"v0.10 {field}",
            hydrated_crosscheck=hydrated_crosscheck,
        )
        normalized = {
            key: (value if value is not None else "")
            for key, value in _global_geometry_evidence_projection(
                evidence_row
            ).items()
        }
        evidence_rows[field] = normalized
    capacity_row = _v10_flat_source_row(
        rows["capacity_estimate"],
        release["members"]["capacity_estimates.csv"],
        V10_CAPACITY_FIELDS,
        label="v0.10 capacity estimate",
        hydrated_crosscheck=hydrated_crosscheck,
    )
    if capacity_row != construction["capacity_estimate"]:
        raise VerifiedConstructionCoreError(
            "v0.10 capacity source-row projection differs"
        )
    evidence_by_key = {
        row.get("key"): row
        for row in source["evidence"]
        if isinstance(row, dict) and isinstance(row.get("key"), str)
    }
    if len(evidence_by_key) != len(source["evidence"]):
        raise VerifiedConstructionCoreError("v0.10 source evidence keys differ")
    expected_project = _v08_source_entity_projection(
        source, evidence_by_key, entity_kind="project"
    )
    expected_project["normalized_roles"] = _v08_normalized_roles(source["project"])
    expected_campus = _v08_source_entity_projection(
        source, evidence_by_key, entity_kind="campus"
    )
    expected_campus["normalized_roles"] = _v08_normalized_roles(source["campus"])
    actual_project = _v08_actual_entity_projection(
        construction["project"], entity_kind="project"
    )
    actual_campus = _v08_actual_entity_projection(
        construction["campus"], entity_kind="campus"
    )
    if (
        actual_project != expected_project
        or actual_campus != expected_campus
        or _v08_csv_entity_projection(project_row, entity_kind="project")
        != actual_project
        or _v08_csv_entity_projection(campus_row, entity_kind="campus")
        != actual_campus
    ):
        raise VerifiedConstructionCoreError("v0.10 full v97 entity projection differs")
    source_evidence = _v08_source_evidence(source)
    for field, expected_id in zip(
        ("status_evidence", expected_evidence_field),
        profile["source_evidence_ids"],
        strict=True,
    ):
        construction_evidence = construction[field]
        if (
            construction_evidence.get("evidence_id") != expected_id
            or evidence_rows[field]
            != {
                key: source_evidence[expected_id][key]
                for key in GLOBAL_GEOMETRY_EVIDENCE_FIELDS
            }
            or evidence_rows[field]
            != {
                key: (
                    construction_evidence.get(key)
                    if construction_evidence.get(key) is not None
                    else ""
                )
                for key in GLOBAL_GEOMETRY_EVIDENCE_FIELDS
            }
        ):
            raise VerifiedConstructionCoreError(
                f"v0.10 {field} full evidence projection differs"
            )
    capacity_observations = (
        actual_project["capacity_estimates_json"]
        if capacity_row["entity_kind"] == "project"
        else actual_campus["capacity_estimates_json"]
    )
    expected_capacity_kind = (
        "project"
        if actual_project["stable_key"].startswith("curated:colt-london")
        else "campus"
    )
    if (
        capacity_row["entity_kind"] != expected_capacity_kind
        or capacity_row["entity_id"]
        != (
            actual_project["entity_id"]
            if capacity_row["entity_kind"] == "project"
            else actual_campus["entity_id"]
        )
        or len(capacity_observations) != 1
        or _v10_capacity_projection(capacity_row) != capacity_observations[0]
        or capacity_row["evidence_id"]
        != construction[expected_evidence_field]["evidence_id"]
    ):
        raise VerifiedConstructionCoreError(
            "v0.10 capacity observation projection differs"
        )
    if (
        actual_project["normalized_roles"]
        != {
            "owner": None,
            "operator": None,
            "users": [],
            "tenants": [],
            "customers": [],
        }
        or actual_campus["normalized_roles"]
        != {
            "owner": None,
            "operator": None,
            "users": [],
            "tenants": [],
            "customers": [],
        }
        or actual_project["workloads_json"] != []
        or actual_project["operating_model"] is not None
        or actual_project["operating_model_evidence_id"] is not None
    ):
        raise VerifiedConstructionCoreError(
            "v0.10 project role workload or model leakage differs"
        )
    project_key = actual_project["stable_key"]
    if project_key.startswith("curated:colt-london"):
        expected_project_capacity = [
            row
            for row in actual_project["capacity_estimates_json"]
            if row.get("metric") == "critical_it_mw"
            and row.get("stage") == "planned"
            and row.get("base") == 31.0
        ]
        if (
            len(expected_project_capacity) != 1
            or actual_campus["capacity_estimates_json"] != []
            or actual_campus["workloads_json"] != []
        ):
            raise VerifiedConstructionCoreError("v0.10 Colt metric scope differs")
    elif (
        actual_project["capacity_estimates_json"] != []
        or (
            project_key.startswith("curated:cyrusone")
            and (
                len(actual_campus["capacity_estimates_json"]) != 1
                or actual_campus["capacity_estimates_json"][0].get("base")
                != 81.0
                or actual_campus["workloads_json"] != []
            )
        )
        or (
            project_key.startswith("curated:oracle")
            and (
                len(actual_campus["capacity_estimates_json"]) != 1
                or actual_campus["capacity_estimates_json"][0].get("base")
                != 2450.0
                or actual_campus["workloads_json"]
                != [
                    {
                        "as_of_date": "2026-04-27",
                        "confidence": 0.99,
                        "evidence_id": "b68ce146-3dea-57e1-bb04-fbe710b2f39d",
                        "method": "company_disclosure",
                        "workload": "ai_specialized_unspecified",
                    }
                ]
            )
        )
    ):
        raise VerifiedConstructionCoreError("v0.10 campus context scope differs")
    _v10_validate_flat_topology(
        construction["project_to_campus"],
        project=actual_project,
        campus=actual_campus,
        acceptance=acceptance,
        hydrated_crosscheck=hydrated_crosscheck,
    )
    if (
        acceptance["project_stable_key"] != actual_project["stable_key"]
        or acceptance["project_entity_id"] != actual_project["entity_id"]
        or acceptance["physical_site_stable_key"] != actual_campus["stable_key"]
        or acceptance["physical_site_entity_id"] != actual_campus["entity_id"]
        or acceptance["country"] != actual_project["country"]
        or acceptance["country"] != actual_campus["country"]
    ):
        raise VerifiedConstructionCoreError(
            "v0.10 publication target projection differs"
        )
    return actual_project, actual_campus, source


def _v10_validate_oracle_geometry(
    bridge: Mapping[str, Any], *, hydrated_crosscheck: bool
) -> None:
    capture = bridge["geometry_capture"]
    release = bridge["geometry_release"]
    if (
        not isinstance(capture, dict)
        or set(capture)
        != {
            "type",
            "entity_source_row",
            "evidence_source_row",
            "live_version_probe",
        }
        or capture["type"] != "embedded_exact_csv_rows"
        or not isinstance(release, dict)
        or set(release)
        != {"release_id", "manifest", "members", "portable_row_authority"}
        or release["release_id"] != "global-open-v3"
    ):
        raise VerifiedConstructionCoreError("v0.10 Oracle geometry source differs")
    _, manifest = _manifest_binding(
        release["manifest"], label="v0.10 Oracle global-v3 release"
    )
    _validate_manifest_members(
        manifest, release["members"], label="v0.10 Oracle global-v3 release"
    )
    if set(release["members"]) != {"entities.csv", "evidence.csv"}:
        raise VerifiedConstructionCoreError("v0.10 Oracle geometry members differ")
    entity_row = _v10_flat_source_row(
        capture["entity_source_row"],
        release["members"]["entities.csv"],
        GLOBAL_GEOMETRY_ENTITY_FIELDS,
        label="v0.10 Oracle geometry entity",
        hydrated_crosscheck=hydrated_crosscheck,
    )
    evidence_row = _v10_flat_source_row(
        capture["evidence_source_row"],
        release["members"]["evidence.csv"],
        GLOBAL_GEOMETRY_EVIDENCE_FIELDS,
        label="v0.10 Oracle geometry evidence",
        hydrated_crosscheck=hydrated_crosscheck,
    )
    raw_entity = _global_geometry_entity_projection(entity_row)
    entity = bridge["geometry_entity"]
    evidence = bridge["geometry_evidence"]
    if (
        entity["entity_id"] != raw_entity["entity_id"]
        or entity["entity_kind"] != raw_entity["entity_kind"]
        or entity["stable_key"] != raw_entity["stable_key"]
        or entity["name"] != raw_entity["name"]
        or entity["latitude"] != raw_entity["latitude"]
        or entity["longitude"] != raw_entity["longitude"]
        or entity["country"] != raw_entity["country"]
        or entity["geometry"] != raw_entity["geometry"]
        or entity["tags"] != raw_entity["tags"]
        or entity["source_evidence_id"] != raw_entity["snapshot_evidence_id"]
        or entity["source_url"] != raw_entity["source_url"]
        or entity["source_publisher"] != raw_entity["source_publisher"]
        or entity["source_license"] != raw_entity["source_license"]
        or entity["source_retrieved_at"] != raw_entity["source_retrieved_at"]
        or entity["source_object_timestamp"] != raw_entity["snapshot_as_of"]
        or raw_entity["status"] != "unknown"
        or raw_entity["status_method"]
        != "osm_geometry_only_no_operational_inference"
        or raw_entity["workloads_json"] != []
        or raw_entity["capacity_estimates_json"] != []
    ):
        raise VerifiedConstructionCoreError(
            "v0.10 Oracle global entity projection differs"
        )
    raw_evidence = _global_geometry_evidence_projection(evidence_row)
    if (
        any(evidence.get(field) != raw_evidence[field] for field in raw_evidence)
        or type(evidence.get("public_evidence_projection")) is not bool
        or evidence["public_evidence_projection"] is not True
        or evidence.get("projection_scope") != "public_geometry_evidence"
    ):
        raise VerifiedConstructionCoreError(
            "v0.10 Oracle global evidence projection differs"
        )
    _validate_polygon(entity["geometry"], label="v0.10 Oracle geometry")
    ring = entity["geometry"]["coordinates"][0]
    midpoint = [
        (min(point[0] for point in ring) + max(point[0] for point in ring)) / 2,
        (min(point[1] for point in ring) + max(point[1] for point in ring)) / 2,
    ]
    if midpoint != [entity["longitude"], entity["latitude"]]:
        raise VerifiedConstructionCoreError("v0.10 Oracle midpoint differs")


def _v10_validate_cyrus_geometry(bridge: Mapping[str, Any]) -> None:
    entity = bridge["geometry_entity"]
    evidence = bridge["geometry_evidence"]
    capture = bridge["geometry_capture"]
    identities = bridge["identity_bridge_evidence"]
    if (
        not isinstance(capture, dict)
        or set(capture)
        != {
            "type",
            "source_evidence_id",
            "raw_document_redistributed",
            "replay_contract",
            "transform_audit",
        }
        or capture["type"] != "embedded_compact_official_permit_fact"
        or type(capture["raw_document_redistributed"]) is not bool
        or capture["raw_document_redistributed"] is not False
        or not isinstance(identities, list)
        or len(identities) != 2
    ):
        raise VerifiedConstructionCoreError("v0.10 Cyrus capture differs")
    public, hvbg = identities
    if (
        type(public.get("public_evidence_projection")) is not bool
        or public["public_evidence_projection"] is not True
        or type(hvbg.get("public_evidence_projection")) is not bool
        or hvbg["public_evidence_projection"] is not False
        or hvbg.get("evidence_id") is not None
        or "evidence_id" in hvbg
        or hvbg.get("projection_scope") != "derivation_only_crs_context"
        or _v07_evidence_projection(public) != _v07_evidence_projection(evidence)
        or evidence.get("canonical_public_evidence_origin")
        != "identity_bridge_evidence[0]"
        or evidence["evidence_id"] != _official_evidence_id(public)
    ):
        raise VerifiedConstructionCoreError(
            "v0.10 Cyrus public/HVBG projection differs"
        )
    fact = public.get("fact_payload")
    canonical = public.get("fact_payload_canonical")
    transform = capture.get("transform_audit")
    hvbg_fact = hvbg.get("fact_payload")
    hvbg_canonical = hvbg.get("fact_payload_canonical")
    if (
        not isinstance(fact, dict)
        or not isinstance(canonical, dict)
        or canonical.get("bytes") != len(_json_bytes(fact))
        or canonical.get("sha256") != _sha256_bytes(_json_bytes(fact))
        or canonical.get("bytes") != 1359
        or canonical.get("sha256")
        != "6645e92ef80313bc2726fbfc39a936031c17c761b907910ccbd8b5a5a1fd4d8e"
        or fact.get("coordinate_easting") != 469995
        or fact.get("coordinate_northing") != 5549104
        or fact.get("coordinate_source_crs_epsg") != 25832
        or fact.get("coordinate_output_crs_epsg") != 4326
        or fact.get("coordinate_output_lon") != 8.5805137288
        or fact.get("coordinate_output_lat") != 50.093438822
        or not isinstance(transform, dict)
        or transform.get("library_bytes") != 121969
        or transform.get("library_sha256")
        != "ae2c4005bd390f89472d0f241bb960c015c1ee5149ca972ba096ff9158f2244e"
        or transform.get("unrounded_output_lon_lat")
        != [8.580513728800428, 50.093438822045506]
        or entity["geometry"]
        != {
            "type": "Point",
            "coordinates": [
                fact["coordinate_output_lon"],
                fact["coordinate_output_lat"],
            ],
        }
        or entity["source_evidence_id"] != public["evidence_id"]
        or hvbg.get("key")
        != "hessen-etrs89-utm32-epsg25832-reference-captured-2026-08-20"
        or hvbg.get("publisher")
        != "Hessische Verwaltung für Bodenmanagement und Geoinformation"
        or hvbg.get("source_url")
        != (
            "https://hvbg.hessen.de/landesvermessung/geotopographie/3d-daten/"
            "bildbasierte-digitale-oberflaechenmodelle"
        )
        or hvbg.get("license") != "rights-status-not-established"
        or hvbg.get("captured_bytes") != 330153
        or hvbg.get("content_hash")
        != "9cf4caf3ed28f17caea5d824eac7dc57c7fad27f43a5f865f9a972c0abb05847"
        or hvbg.get("rights_scope")
        != "Only the compact CRS fact and hashes are redistributed."
        or hvbg_fact
        != {
            "official_hessian_horizontal_crs": (
                "EPSG:25832, ETRS89/UTM zone 32, GRS80"
            ),
            "source_context": (
                "Hessian state geodata reference-system page; used only to "
                "interpret the RP Darmstadt 32U coordinate notation."
            ),
        }
        or not isinstance(hvbg_canonical, dict)
        or hvbg_canonical.get("bytes") != len(_json_bytes(hvbg_fact))
        or hvbg_canonical.get("sha256") != _sha256_bytes(_json_bytes(hvbg_fact))
        or hvbg_canonical.get("bytes") != 205
        or hvbg_canonical.get("sha256")
        != "1b768082b10384f56bf0596475202e22d32e7a4b41d4818a06a6d33099e9bd1e"
    ):
        raise VerifiedConstructionCoreError("v0.10 Cyrus transform replay differs")


def _v10_validate_colt_geometry(
    bridge: Mapping[str, Any], profile: Mapping[str, Any]
) -> None:
    capture = bridge["geometry_capture"]
    replay = _v09_replay_osm_capture(capture, profile)
    entity = bridge["geometry_entity"]
    evidence = bridge["geometry_evidence"]
    identities = bridge["identity_bridge_evidence"]
    if not isinstance(identities, list) or len(identities) != 1:
        raise VerifiedConstructionCoreError("v0.10 Colt identity inventory differs")
    identity = identities[0]
    fact = identity.get("fact_payload")
    canonical = identity.get("fact_payload_canonical")
    stability = capture.get("capture_stability")
    capture_rights = capture.get("rights")
    if (
        set(capture)
        != {
            "type",
            "capture_id",
            "captured_at",
            "review_date_local",
            "request",
            "response",
            "payload",
            "object_expectations",
            "replay_checksums",
            "capture_stability",
            "rights",
        }
        or stability
        != {
            "body_refetch_reproducibility": "not_guaranteed",
            "reason": (
                "The OSM API generator string varies across backend hosts and "
                "therefore can change the exact response-body hash without changing "
                "the selected way."
            ),
            "portable_authority": (
                "The embedded bytes are the authority; replay must also prove raw-way, "
                "enriched-way, and polygon canonical hashes."
            ),
        }
        or capture_rights
        != {
            "publisher": "OpenStreetMap contributors",
            "attribution": "© OpenStreetMap contributors",
            "license": "ODbL-1.0",
            "license_url": "https://opendatacommons.org/licenses/odbl/1-0/",
            "copyright_and_attribution_url": (
                "https://www.openstreetmap.org/copyright"
            ),
            "redistribution_posture": (
                "Exact OSM data bytes may be redistributed under ODbL 1.0 with "
                "attribution and applicable share-alike obligations."
            ),
        }
        or type(capture.get("request", {}).get("credentials_supplied")) is not bool
        or capture["request"]["credentials_supplied"] is not False
        or type(capture.get("response", {}).get("decoded_body_ends_with_lf"))
        is not bool
        or capture["response"]["decoded_body_ends_with_lf"] is not False
        or type(identity.get("public_evidence_projection")) is not bool
        or identity["public_evidence_projection"] is not True
        or not isinstance(fact, dict)
        or fact.get("object_id") != profile["way_id"]
        or fact.get("object_version") != profile["way_version"]
        or fact.get("ordered_node_ids") != replay["way"]["nodes"]
        or fact.get("tags") != replay["way"]["tags"]
        or fact.get("ordered_ring_coordinates_lon_lat")
        != replay["geometry"]["coordinates"][0]
        or fact.get("representative_point", {}).get("coordinates")
        != replay["midpoint"]
        or not isinstance(canonical, dict)
        or canonical.get("bytes") != len(_json_bytes(fact))
        or canonical.get("sha256") != _sha256_bytes(_json_bytes(fact))
        or canonical.get("bytes") != 1593
        or canonical.get("sha256")
        != "0016b31daf2ded3854a999e662deb58d945e1fcdab615e589e7cd015709bde0f"
        or entity["geometry"] != replay["geometry"]
        or [entity["longitude"], entity["latitude"]] != replay["midpoint"]
        or entity["tags"] != replay["way"]["tags"]
        or entity["source_evidence_id"] != identity["evidence_id"]
        or _v07_evidence_projection(identity) != _v07_evidence_projection(evidence)
        or evidence.get("canonical_public_evidence_origin")
        != "identity_bridge_evidence[0]"
    ):
        raise VerifiedConstructionCoreError("v0.10 Colt OSM replay differs")


def _v10_validate_rights_semantics(
    bridge: Mapping[str, Any], project_key: str
) -> None:
    rights = bridge["rights"]
    entity = bridge["geometry_entity"]
    evidence = bridge["geometry_evidence"]
    if project_key.startswith(("curated:colt-london", "curated:oracle")):
        expected_rights = {
            "construction_source": (
                "Colt material is all-rights-reserved; only compact facts, hashes, "
                "and identifiers are retained."
                if project_key.startswith("curated:colt-london")
                else "Oracle material is all-rights-reserved; only compact facts, "
                "hashes, and identifiers are retained."
            ),
            "geometry_source": (
                "The exact OSM payload and derived polygon are redistributed under "
                "ODbL 1.0 with required attribution."
                if project_key.startswith("curated:colt-london")
                else "The embedded OSM rows and derived polygon are redistributed "
                "under ODbL 1.0 with OpenStreetMap contributor attribution."
            ),
            "geometry_license_url": (
                "https://opendatacommons.org/licenses/odbl/1-0/"
            ),
            "geometry_attribution_url": "https://www.openstreetmap.org/copyright",
            "mixed_rights": (
                "ODbL geometry does not relicense Colt material."
                if project_key.startswith("curated:colt-london")
                else "ODbL geometry does not relicense Oracle or state-source material."
            ),
        }
        if project_key.startswith("curated:oracle"):
            expected_rights["identity_bridge_source"] = (
                "New Mexico State Ethics Commission PDF redistribution rights were "
                "not established; only compact facts and hashes are retained."
            )
        if (
            rights != expected_rights
            or evidence.get("publisher") != "OpenStreetMap contributors"
            or evidence.get("source_family") != "openstreetmap"
            or evidence.get("license") != "ODbL-1.0"
            or evidence.get("attribution") != "© OpenStreetMap contributors"
            or evidence.get("source_url")
            != f"https://www.openstreetmap.org/way/{entity['stable_key'].split('/')[-1]}"
            or entity.get("source_publisher") != "OpenStreetMap contributors"
            or entity.get("source_license") != "ODbL-1.0"
            or (
                project_key.startswith("curated:colt-london")
                and entity.get("source_attribution")
                != "© OpenStreetMap contributors"
            )
        ):
            raise VerifiedConstructionCoreError("v0.10 OSM rights semantics differ")
        return
    expected_url = (
        "https://rp-darmstadt.hessen.de/sites/rp-darmstadt.hessen.de/files/"
        "2026-07/bescheid-4_ndma-ff7-cyrusone_end.pdf"
    )
    expected_rights = {
        "construction_source": (
            "CyrusOne material is all-rights-reserved; only compact facts, hashes, "
            "and identifiers are retained."
        ),
        "geometry_source": (
            "RP Darmstadt and HVBG redistribution rights were not established; "
            "official PDF/HTML bodies are not redistributed, only compact facts, "
            "hashes, and the derived point."
        ),
        "transform_library": (
            "Proj4js is used only as an audit implementation; its fetched bundle is "
            "not redistributed."
        ),
        "mixed_rights": "No source body is relicensed by this bridge.",
    }
    if (
        rights != expected_rights
        or evidence.get("publisher") != "Regierungspräsidium Darmstadt"
        or evidence.get("source_family")
        != "rp_darmstadt_environmental_permit_decisions"
        or evidence.get("license") != "rights-status-not-established"
        or evidence.get("attribution") != "Regierungspräsidium Darmstadt"
        or evidence.get("source_url") != expected_url
        or entity.get("source_publisher") != "Regierungspräsidium Darmstadt"
        or entity.get("source_license") != "rights-status-not-established"
        or entity.get("source_url") != expected_url
    ):
        raise VerifiedConstructionCoreError("v0.10 Cyrus rights semantics differ")


def _validate_v10_bridge(
    acceptance: Mapping[str, Any],
    overlay: Mapping[str, Any],
    *,
    hydrated_crosscheck: bool,
) -> tuple[dict[str, Any], dict[str, Any]]:
    project_key = acceptance["project_stable_key"]
    profile = V10_BRIDGE_PROFILES.get(project_key)
    geometry_contract = V10_GEOMETRY_CONTRACTS.get(project_key)
    acceptance_semantics_sha256 = V10_ACCEPTANCE_SEMANTICS_SHA256.get(project_key)
    if (
        profile is None
        or geometry_contract is None
        or acceptance_semantics_sha256 is None
        or _sha256_bytes(_json_bytes(_v10_acceptance_semantics(acceptance)))
        != acceptance_semantics_sha256
        or {
            field: acceptance.get(field)
            for field in geometry_contract
        }
        != geometry_contract
        or type(acceptance.get("official_boundary")) is not bool
        or _sha256_bytes(_json_bytes(acceptance))
        != profile["acceptance_sha256"]
        or _sha256_bytes(_json_bytes(overlay)) != profile["overlay_sha256"]
        or acceptance["bridge_path"] != profile["bridge_path"]
        or acceptance["bridge_bytes"] != profile["bridge_bytes"]
        or acceptance["bridge_sha256"] != profile["bridge_sha256"]
        or acceptance["source_input_path"] != profile["source_path"]
        or acceptance["source_input_bytes"] != profile["source_bytes"]
        or acceptance["source_input_sha256"] != profile["source_sha256"]
        or acceptance["bridge_id"] != profile["bridge_id"]
        or acceptance["bridge_schema"] != profile["schema"]
        or acceptance["geometry_entity"] != profile["geometry_kind"]
        or acceptance["geometry_entity_stable_key"]
        != profile["geometry_stable_key"]
        or acceptance["geometry_entity_id"] != profile["geometry_entity_id"]
        or acceptance["geometry_target_entity_kind"] != profile["target_kind"]
        or acceptance["geometry_target_entity_stable_key"]
        != profile["target_stable_key"]
        or acceptance["geometry_target_entity_id"] != profile["target_entity_id"]
        or acceptance["geometry_evidence_id"]
        != profile["geometry_evidence_id"]
        or acceptance["geometry_evidence_key"]
        != profile["geometry_evidence_key"]
        or acceptance["geometry_source_kind"]
        != profile["geometry_source_kind"]
        or acceptance["portable_capture_bindings"] != []
    ):
        raise VerifiedConstructionCoreError("v0.10 immutable bridge profile differs")
    bridge_path = _repository_input(
        acceptance["bridge_path"], acceptance["bridge_sha256"], "v0.10 bridge"
    )
    if bridge_path.stat().st_size != acceptance["bridge_bytes"]:
        raise VerifiedConstructionCoreError("v0.10 bridge bytes differ")
    bridge = _load_json(bridge_path)
    if (
        not isinstance(bridge, dict)
        or set(bridge)
        != {
            "bridge_id",
            "bridge_schema",
            "claim_guardrails",
            "construction_source",
            "false_match_guardrails",
            "geometry_capture",
            "geometry_entity",
            "geometry_evidence",
            "geometry_release",
            "identity_bridge_evidence",
            "imagery_posture",
            "materialization_scope",
            "review_decision",
            "reviewed_at",
            "rights",
            "schema_version",
        }
        or type(bridge.get("schema_version")) is not int
        or bridge["schema_version"] != 1
        or bridge["bridge_id"] != profile["bridge_id"]
        or bridge["bridge_schema"] != profile["schema"]
        or bridge["reviewed_at"] != CURRENT_V10_REVIEW_DATE.isoformat()
    ):
        raise VerifiedConstructionCoreError("v0.10 bridge schema differs")
    if _sha256_bytes(_json_bytes(bridge["geometry_entity"])) != (
        V10_GEOMETRY_ENTITY_SHA256.get(project_key)
    ):
        raise VerifiedConstructionCoreError(
            "v0.10 independent geometry source contract differs"
        )
    for field, expected_hash in profile["semantic_hashes"].items():
        if _sha256_bytes(_json_bytes(bridge.get(field))) != expected_hash:
            raise VerifiedConstructionCoreError(
                f"v0.10 {field.replace('_', ' ')} semantic profile differs"
            )
    _v10_validate_rights_semantics(bridge, project_key)
    project, campus, source = _v10_validate_construction_source(
        bridge,
        acceptance,
        profile,
        hydrated_crosscheck=hydrated_crosscheck,
    )
    entity = bridge["geometry_entity"]
    evidence = bridge["geometry_evidence"]
    if (
        entity.get("entity_kind") != profile["geometry_kind"]
        or entity.get("stable_key") != profile["geometry_stable_key"]
        or entity.get("entity_id") != profile["geometry_entity_id"]
        or entity.get("entity_id")
        != atlas_stable_id(
            "entity", entity.get("stable_key", ""), entity.get("entity_kind", "")
        )
        or [entity.get("longitude"), entity.get("latitude")]
        != [profile["longitude"], profile["latitude"]]
        or evidence.get("evidence_id") != profile["geometry_evidence_id"]
        or evidence.get("key") != profile["geometry_evidence_key"]
        or type(evidence.get("public_evidence_projection")) is not bool
        or evidence["public_evidence_projection"] is not True
    ):
        raise VerifiedConstructionCoreError("v0.10 geometry source identity differs")
    target = project if profile["target_kind"] == "project" else campus
    if (
        target["stable_key"] != profile["target_stable_key"]
        or target["entity_id"] != profile["target_entity_id"]
        or target["stable_key"] != acceptance["geometry_target_entity_stable_key"]
        or target["entity_id"] != acceptance["geometry_target_entity_id"]
    ):
        raise VerifiedConstructionCoreError("v0.10 geometry target differs")
    decision = bridge["review_decision"]
    bindings = {
        "decision": "bridge_decision",
        "decision_basis": "decision_basis",
        "geometry_target_entity_kind": "geometry_target_entity_kind",
        "geometry_target_entity_stable_key": "geometry_target_entity_stable_key",
        "geometry_target_entity_id": "geometry_target_entity_id",
        "geometry_derivation": "geometry_derivation",
        "geometry_method": "geometry_method",
        "geometry_scope_class": "geometry_scope_class",
        "geometry_authority_class": "geometry_authority_class",
        "geometry_use_scope": "geometry_use_scope",
        "horizontal_uncertainty_metres": "horizontal_uncertainty_metres",
        "horizontal_uncertainty_unknown_reason": "horizontal_uncertainty_unknown_reason",
        "precision_scope": "precision_scope",
    }
    if (
        not isinstance(decision, dict)
        or any(
            decision.get(field) != acceptance[target_field]
            for field, target_field in bindings.items()
        )
        or type(decision.get("official_boundary")) is not bool
        or decision["official_boundary"] is not False
        or type(acceptance.get("official_boundary")) is not bool
        or acceptance["official_boundary"] is not False
        or acceptance["geometry_use_scope"]
        != ("campus_locator" if profile["target_kind"] == "campus" else "project_locator")
    ):
        raise VerifiedConstructionCoreError("v0.10 review semantics differ")
    imagery = bridge["imagery_posture"]
    if (
        not isinstance(imagery, dict)
        or imagery.get("outcome") != "not_reviewed_for_core_preview"
        or type(imagery.get("independent_imagery_verification")) is not bool
        or imagery["independent_imagery_verification"] is not False
        or not isinstance(bridge["claim_guardrails"], dict)
        or not bridge["claim_guardrails"]
    ):
        raise VerifiedConstructionCoreError("v0.10 imagery guardrail differs")
    if project_key.startswith("curated:oracle"):
        _v10_validate_oracle_geometry(
            bridge, hydrated_crosscheck=hydrated_crosscheck
        )
    elif project_key.startswith("curated:cyrusone"):
        _v10_validate_cyrus_geometry(bridge)
        if (
            entity["entity_id"] != project["entity_id"]
            or entity["stable_key"] != project["stable_key"]
        ):
            raise VerifiedConstructionCoreError(
                "v0.10 Cyrus source-target identity differs"
            )
    else:
        _v10_validate_colt_geometry(bridge, profile)
    return bridge, source


def _v10_contracts(*, hydrated_crosscheck: bool) -> dict[str, Any]:
    _validate_v10_definition_pins()
    reviewed = _load_json(V10_REVIEW_DEFINITION)
    overlays = _load_json(V10_OVERLAY_DEFINITION)
    provenance = _load_json(V10_PROVENANCE_DEFINITION)
    imagery = _load_json(V10_IMAGERY_REVIEW_DEFINITION)
    if (
        not isinstance(reviewed, dict)
        or set(reviewed)
        != {
            "contract_id",
            "review_scope",
            "reviewed_as_of",
            "base_contract",
            "acceptances",
        }
        or reviewed.get("contract_id")
        != "verified-construction-core-v0.10-reviewed-sites"
        or reviewed.get("reviewed_as_of")
        != CURRENT_V10_REVIEW_DATE.isoformat()
        or reviewed.get("base_contract")
        != {
            "path": "definitions/verified-construction-core-v0.9-reviewed-sites.json",
            "sha256": V09_REVIEW_DEFINITION_SHA256,
            "default_geometry_entity": "project",
            "default_geometry_derivation": "direct_geometry",
        }
        or not isinstance(reviewed.get("acceptances"), list)
        or len(reviewed["acceptances"]) != 3
        or any(
            not isinstance(row, dict)
            or set(row) != V10_ACCEPTANCE_REQUIRED_FIELDS
            for row in reviewed["acceptances"]
        )
    ):
        raise VerifiedConstructionCoreError("v0.10 reviewed-site contract differs")
    if (
        not isinstance(overlays, dict)
        or set(overlays)
        != {
            "contract_id",
            "purpose",
            "reviewed_as_of",
            "base_contract",
            "required_fields",
            "allowed_decisions",
            "overlays",
        }
        or overlays.get("contract_id")
        != "verified-construction-core-reviewed-overlays-v8"
        or overlays.get("reviewed_as_of")
        != CURRENT_V10_REVIEW_DATE.isoformat()
        or overlays.get("base_contract")
        != {
            "path": "definitions/verified-construction-core-reviewed-overlays-v7.json",
            "sha256": V09_OVERLAY_DEFINITION_SHA256,
        }
        or tuple(overlays.get("required_fields", []))
        != V10_OVERLAY_REQUIRED_FIELD_ORDER
        or overlays.get("allowed_decisions") != ["queued", "accepted", "excluded"]
        or not isinstance(overlays.get("overlays"), list)
        or len(overlays["overlays"]) != 3
        or any(
            not isinstance(row, dict) or set(row) != V10_OVERLAY_REQUIRED_FIELDS
            for row in overlays["overlays"]
        )
    ):
        raise VerifiedConstructionCoreError("v0.10 overlay contract differs")
    acceptance_by_key = {
        row.get("project_stable_key"): row for row in reviewed["acceptances"]
    }
    overlay_by_key = {
        row.get("source_project_stable_key"): row for row in overlays["overlays"]
    }
    if (
        len(acceptance_by_key) != 3
        or len(overlay_by_key) != 3
        or set(acceptance_by_key) != set(V10_BRIDGE_PROFILES)
        or set(overlay_by_key) != set(V10_BRIDGE_PROFILES)
    ):
        raise VerifiedConstructionCoreError("v0.10 cohort differs")
    crosswalk = {
        field: field
        for field in V10_ACCEPTANCE_REQUIRED_FIELDS
        if field
        not in {
            "project_stable_key",
            "project_entity_id",
            "geometry_overlay_id",
            "decision_basis",
        }
    }
    bridges: dict[str, dict[str, Any]] = {}
    sources: dict[str, dict[str, Any]] = {}
    for project_key in sorted(acceptance_by_key):
        acceptance = acceptance_by_key[project_key]
        overlay = overlay_by_key[project_key]
        if (
            any(
                acceptance[left] != overlay[right]
                for left, right in crosswalk.items()
            )
            or acceptance["project_stable_key"]
            != overlay["source_project_stable_key"]
            or acceptance["project_entity_id"]
            != overlay["source_project_entity_id"]
            or acceptance["geometry_overlay_id"] != overlay["overlay_id"]
            or acceptance["decision_basis"] != overlay["decision_reason"]
            or overlay["decision"] != "accepted"
            or overlay["reviewed_at"] != CURRENT_V10_REVIEW_DATE.isoformat()
        ):
            raise VerifiedConstructionCoreError(
                "v0.10 acceptance-overlay binding differs"
            )
        bridge, source = _validate_v10_bridge(
            acceptance,
            overlay,
            hydrated_crosscheck=hydrated_crosscheck,
        )
        bridges[project_key] = bridge
        sources[project_key] = source
    if (
        not isinstance(provenance, dict)
        or set(provenance)
        != {
            "contract_id",
            "review_scope",
            "reviewed_as_of",
            "base_contract",
            "workload_scope_bindings",
            "role_bindings",
            "excluded_source_roles",
            "context_evidence_bindings",
        }
        or provenance.get("contract_id")
        != "verified-construction-core-v0.10-provenance"
        or provenance.get("reviewed_as_of")
        != CURRENT_V10_REVIEW_DATE.isoformat()
        or provenance.get("base_contract")
        != {
            "path": "definitions/verified-construction-core-v0.9-provenance.json",
            "sha256": V09_PROVENANCE_DEFINITION_SHA256,
        }
        or provenance.get("workload_scope_bindings") != []
        or provenance.get("role_bindings") != []
        or provenance.get("excluded_source_roles") != []
        or not isinstance(provenance.get("context_evidence_bindings"), list)
        or len(provenance["context_evidence_bindings"]) != 3
        or _sha256_bytes(
            _json_bytes(
                {
                    field: provenance[field]
                    for field in (
                        "workload_scope_bindings",
                        "role_bindings",
                        "excluded_source_roles",
                        "context_evidence_bindings",
                    )
                }
            )
        )
        != V10_PROVENANCE_SEMANTICS_SHA256
    ):
        raise VerifiedConstructionCoreError("v0.10 provenance contract differs")
    context_ids = set()
    for binding in provenance["context_evidence_bindings"]:
        project_key = binding.get("project_stable_key")
        bridge = bridges.get(project_key)
        if (
            not isinstance(binding, dict)
            or set(binding)
            != {
                "project_stable_key",
                "bridge_path",
                "bridge_sha256",
                "evidence_origin",
                "evidence_id",
                "usage_role",
                "semantic_scope",
            }
            or bridge is None
            or binding["bridge_path"]
            != acceptance_by_key[project_key]["bridge_path"]
            or binding["bridge_sha256"]
            != acceptance_by_key[project_key]["bridge_sha256"]
            or binding["evidence_origin"] != "identity_bridge_evidence"
            or binding["usage_role"] != "geometry_identity"
            or not isinstance(binding["semantic_scope"], str)
            or not binding["semantic_scope"]
        ):
            raise VerifiedConstructionCoreError(
                "v0.10 context binding differs"
            )
        public_ids = {
            row.get("evidence_id")
            for row in bridge["identity_bridge_evidence"]
            if isinstance(row, dict)
            and type(row.get("public_evidence_projection")) is bool
            and row["public_evidence_projection"] is True
        }
        if binding["evidence_id"] not in public_ids:
            raise VerifiedConstructionCoreError(
                "v0.10 context evidence origin differs"
            )
        context_ids.add(binding["evidence_id"])
    if context_ids != {
        "b0d7fbf5-7de2-58e0-a54f-d6fec6d08a6a",
        "850336d5-4d8f-5821-ae65-cb195961e7b6",
        "c0b3ca09-1341-5395-9301-9f6a2d67cf0c",
    }:
        raise VerifiedConstructionCoreError("v0.10 context inventory differs")
    if (
        not isinstance(imagery, dict)
        or set(imagery)
        != {
            "contract_id",
            "review_scope",
            "reviewed_as_of",
            "base_contract",
            "default_outcome",
            "records",
        }
        or imagery.get("contract_id")
        != "verified-construction-core-v0.10-imagery-reviews"
        or imagery.get("reviewed_as_of")
        != CURRENT_V10_REVIEW_DATE.isoformat()
        or imagery.get("base_contract")
        != {
            "path": "definitions/verified-construction-core-v0.9-imagery-reviews.json",
            "sha256": V09_IMAGERY_REVIEW_DEFINITION_SHA256,
        }
        or imagery.get("default_outcome")
        != "not_reviewed_for_core_preview"
        or imagery.get("records") != []
    ):
        raise VerifiedConstructionCoreError("v0.10 imagery contract differs")
    return {
        "reviewed": reviewed,
        "overlays": overlays,
        "provenance": provenance,
        "imagery": imagery,
        "acceptance_by_key": acceptance_by_key,
        "overlay_by_key": overlay_by_key,
        "bridges": bridges,
        "sources": sources,
    }


def _v10_build_delta_rows(contracts: Mapping[str, Any]) -> dict[str, Any]:
    provenance = contracts["provenance"]
    roles_by_project: dict[str, list[dict[str, Any]]] = defaultdict(list)
    for row in provenance["role_bindings"]:
        roles_by_project[row["project_stable_key"]].append(row)
    contexts_by_project: dict[str, list[dict[str, Any]]] = defaultdict(list)
    for row in provenance["context_evidence_bindings"]:
        contexts_by_project[row["project_stable_key"]].append(row)
    evidence_pool: dict[str, dict[str, Any]] = {}
    evidence_usage: dict[str, dict[str, set[str]]] = defaultdict(
        lambda: {"roles": set(), "project_ids": set()}
    )
    projects: list[dict[str, Any]] = []
    sites: list[dict[str, Any]] = []
    for project_key in sorted(contracts["bridges"]):
        bridge = contracts["bridges"][project_key]
        source = contracts["sources"][project_key]
        acceptance = contracts["acceptance_by_key"][project_key]
        construction = bridge["construction_source"]
        source_evidence = _v08_source_evidence(source)
        evidence_pool.update(source_evidence)
        geometry_evidence = _v07_evidence_projection(bridge["geometry_evidence"])
        evidence_pool[geometry_evidence["evidence_id"]] = geometry_evidence
        identity_evidence = bridge.get("identity_bridge_evidence")
        if not isinstance(identity_evidence, list):
            raise VerifiedConstructionCoreError(
                "v0.10 identity evidence inventory differs"
            )
        for identity_row in identity_evidence:
            public_projection = identity_row.get("public_evidence_projection")
            if type(public_projection) is not bool:
                raise VerifiedConstructionCoreError(
                    "v0.10 identity evidence visibility differs"
                )
            if public_projection is False:
                if "evidence_id" in identity_row:
                    raise VerifiedConstructionCoreError(
                        "v0.10 nonpublic evidence identifier differs"
                    )
                continue
            projected_identity = _v07_evidence_projection(identity_row)
            evidence_id = projected_identity["evidence_id"]
            existing = evidence_pool.get(evidence_id)
            if existing is not None and existing != projected_identity:
                raise VerifiedConstructionCoreError(
                    "v0.10 identity evidence projection differs"
                )
            evidence_pool[evidence_id] = projected_identity
        project = construction["project"]
        campus = construction["campus"]
        project_id = project["entity_id"]
        site_id = _stable_id("vcc-site", campus["stable_key"])
        status_evidence_id = project["status_evidence_id"]
        geometry_evidence_id = bridge["geometry_evidence"]["evidence_id"]
        evidence_usage[status_evidence_id]["roles"].add("physical_status")
        evidence_usage[status_evidence_id]["project_ids"].add(project_id)
        evidence_usage[geometry_evidence_id]["roles"].add("geometry")
        evidence_usage[geometry_evidence_id]["project_ids"].add(project_id)
        observations = project.get("capacity_estimates_json", [])
        if not isinstance(observations, list):
            raise VerifiedConstructionCoreError("v0.10 capacity observations differ")
        for observation in observations:
            _validate_typed_observation(
                observation, POWER_METRICS | ENERGY_METRICS | EFFICIENCY_METRICS
            )
            evidence_id = observation["evidence_id"]
            if evidence_id not in evidence_pool:
                raise VerifiedConstructionCoreError(
                    "v0.10 capacity evidence differs"
                )
            evidence_usage[evidence_id]["roles"].add(
                f"typed_metric:{observation['metric']}"
            )
            evidence_usage[evidence_id]["project_ids"].add(project_id)
        power = [row for row in observations if row["metric"] in POWER_METRICS]
        energy = [row for row in observations if row["metric"] in ENERGY_METRICS]
        efficiency = [
            row for row in observations if row["metric"] in EFFICIENCY_METRICS
        ]
        if project.get("workloads_json") != []:
            raise VerifiedConstructionCoreError("v0.10 workload promotion differs")
        role_claims: list[dict[str, str]] = []
        projected_roles = {role: [] for role in ROLE_COLUMNS}
        for binding in roles_by_project.get(project_key, []):
            evidence_id = binding["evidence_id"]
            if evidence_id not in evidence_pool:
                raise VerifiedConstructionCoreError("v0.10 role evidence differs")
            role_claims.append(
                {
                    "evidence_id": evidence_id,
                    "party": binding["party"],
                    "relationship_scope": binding["relationship_scope"],
                    "role": binding["role"],
                }
            )
            projected_roles[binding["role"]].append(binding["party"])
            evidence_usage[evidence_id]["roles"].add(f"role:{binding['role']}")
            evidence_usage[evidence_id]["project_ids"].add(project_id)
        role_claims.sort(
            key=lambda row: (row["role"], row["party"], row["evidence_id"])
        )
        operating_model = project.get("operating_model")
        operating_model_evidence_id = project.get("operating_model_evidence_id")
        if operating_model:
            if operating_model_evidence_id not in evidence_pool:
                raise VerifiedConstructionCoreError(
                    "v0.10 operating-model evidence differs"
                )
            evidence_usage[operating_model_evidence_id]["roles"].add(
                "operating_model"
            )
            evidence_usage[operating_model_evidence_id]["project_ids"].add(
                project_id
            )
        elif operating_model_evidence_id is not None:
            raise VerifiedConstructionCoreError(
                "v0.10 operating-model identity differs"
            )
        for binding in contexts_by_project.get(project_key, []):
            evidence_id = binding["evidence_id"]
            if evidence_id not in evidence_pool:
                raise VerifiedConstructionCoreError("v0.10 context evidence differs")
            evidence_usage[evidence_id]["roles"].add(
                f"context:{binding['usage_role']}"
            )
            evidence_usage[evidence_id]["project_ids"].add(project_id)
        entity = bridge["geometry_entity"]
        geometry = entity["geometry"]
        status_date = _calendar_date(project["status_as_of"], "v0.10 status_as_of")
        image_outcome = contracts["imagery"]["default_outcome"]
        row = {
            "project_id": project_id,
            "project_stable_key": project_key,
            "site_id": site_id,
            "physical_site_stable_key": campus["stable_key"],
            "name": project["name"],
            "country": acceptance["country"],
            "country_iso_a2": V10_COUNTRY_ISO_A2[acceptance["country"]],
            "latitude": entity["latitude"],
            "longitude": entity["longitude"],
            "geometry_json": _json_bytes(geometry).decode().strip(),
            "geometry_type": geometry["type"],
            "geometry_source_entity_kind": acceptance["geometry_entity"],
            "geometry_derivation": acceptance["geometry_derivation"],
            "geometry_method": acceptance["geometry_method"],
            "geometry_scope_class": acceptance["geometry_scope_class"],
            "geometry_authority_class": acceptance["geometry_authority_class"],
            "geometry_use_scope": acceptance["geometry_use_scope"],
            "geometry_precision_scope": acceptance["precision_scope"],
            "horizontal_uncertainty_metres": (
                ""
                if acceptance["horizontal_uncertainty_metres"] is None
                else acceptance["horizontal_uncertainty_metres"]
            ),
            "horizontal_uncertainty_unknown_reason": acceptance[
                "horizontal_uncertainty_unknown_reason"
            ],
            "geometry_evidence_id": geometry_evidence_id,
            "last_observed_physical_status": project["status"],
            "status_as_of": project["status_as_of"],
            "status_age_days_at_review": (
                CURRENT_V10_REVIEW_DATE - status_date
            ).days,
            "status_method": project["status_method"],
            "status_evidence_id": status_evidence_id,
            "verification_posture": _verification_posture(
                acceptance["geometry_authority_class"],
                acceptance["geometry_use_scope"],
            ),
            "independent_imagery_verification": "false",
            "imagery_review_outcome": image_outcome,
            "development_type": "unknown",
            "development_type_unknown_reason": "source evidence does not distinguish greenfield, expansion, or retrofit",
            "operating_model": operating_model or "unknown",
            "operating_model_unknown_reason": (
                "" if operating_model else "not established by selected evidence"
            ),
            "operating_model_evidence_id": operating_model_evidence_id or "",
            "workloads_json": "[]",
            "workload_unknown_reason": "not established by selected evidence",
            "role_claims_json": _json_bytes(role_claims).decode().strip(),
            "power_observations_json": _json_bytes(power).decode().strip(),
            "power_unknown_reason": (
                "" if power else "no typed project power observation; not estimated"
            ),
            "annual_energy_observations_json": _json_bytes(energy)
            .decode()
            .strip(),
            "annual_energy_unknown_reason": (
                ""
                if energy
                else "no scoped annual-energy inputs; not estimated"
            ),
            "efficiency_observations_json": _json_bytes(efficiency)
            .decode()
            .strip(),
            "efficiency_unknown_reason": (
                "" if efficiency else "no scoped PUE or WUE observation"
            ),
            "owner": "; ".join(sorted(projected_roles["owner"])),
            "operator": "; ".join(sorted(projected_roles["operator"])),
            "users": "; ".join(sorted(projected_roles["user"])),
            "tenants": "; ".join(sorted(projected_roles["tenant"])),
            "customers": "; ".join(sorted(projected_roles["customer"])),
            "status_source_url": evidence_pool[status_evidence_id]["source_url"],
            "geometry_source_url": geometry_evidence["source_url"],
        }
        projects.append(row)
        sites.append(
            {
                "site_id": site_id,
                "physical_site_stable_key": campus["stable_key"],
                "name": campus["name"],
                "country": row["country"],
                "country_iso_a2": row["country_iso_a2"],
                "latitude": row["latitude"],
                "longitude": row["longitude"],
                "geometry_json": row["geometry_json"],
                "geometry_type": row["geometry_type"],
                "geometry_source_entity_kinds_json": _json_bytes(
                    [row["geometry_source_entity_kind"]]
                )
                .decode()
                .strip(),
                "geometry_derivations_json": _json_bytes(
                    [row["geometry_derivation"]]
                )
                .decode()
                .strip(),
                "geometry_methods_json": _json_bytes([row["geometry_method"]])
                .decode()
                .strip(),
                "geometry_scope_classes_json": _json_bytes(
                    [row["geometry_scope_class"]]
                )
                .decode()
                .strip(),
                "geometry_authority_classes_json": _json_bytes(
                    [row["geometry_authority_class"]]
                )
                .decode()
                .strip(),
                "geometry_use_scopes_json": _json_bytes(
                    [row["geometry_use_scope"]]
                )
                .decode()
                .strip(),
                "geometry_precision_scopes_json": _json_bytes(
                    [row["geometry_precision_scope"]]
                )
                .decode()
                .strip(),
                "horizontal_uncertainty_metres": row[
                    "horizontal_uncertainty_metres"
                ],
                "horizontal_uncertainty_unknown_reason": row[
                    "horizontal_uncertainty_unknown_reason"
                ],
                "geometry_evidence_ids_json": _json_bytes(
                    [geometry_evidence_id]
                )
                .decode()
                .strip(),
                "project_count": 1,
                "project_ids_json": _json_bytes([project_id]).decode().strip(),
                "project_stable_keys_json": _json_bytes([project_key])
                .decode()
                .strip(),
                "statuses_json": _json_bytes(
                    [row["last_observed_physical_status"]]
                )
                .decode()
                .strip(),
                "oldest_status_as_of": row["status_as_of"],
                "newest_status_as_of": row["status_as_of"],
                "verification_posture": row["verification_posture"],
                "independent_imagery_verification": "false",
                "imagery_review_outcomes_json": _json_bytes([image_outcome])
                .decode()
                .strip(),
            }
        )
    if set(evidence_usage) != V10_EVIDENCE_IDS:
        raise VerifiedConstructionCoreError(
            "v0.10 projected evidence allowlist differs"
        )
    evidence = [
        {
            **evidence_pool[evidence_id],
            "roles_json": _json_bytes(sorted(usage["roles"])).decode().strip(),
            "project_ids_json": _json_bytes(sorted(usage["project_ids"]))
            .decode()
            .strip(),
        }
        for evidence_id, usage in sorted(evidence_usage.items())
    ]
    return {"projects": projects, "sites": sites, "evidence": evidence}


def _v10_portable_source_inputs(
    contracts: Mapping[str, Any],
) -> list[dict[str, Any]]:
    base_manifest = _load_json(LEGACY_PREVIEW_V09_DIR / "manifest.json")
    rows = list(base_manifest["portable_source_inputs"])
    new: dict[str, dict[str, Any]] = {}
    for acceptance in contracts["reviewed"]["acceptances"]:
        for binding in (
            acceptance["portable_input_binding"],
            {
                "path": acceptance["bridge_path"],
                "bytes": acceptance["bridge_bytes"],
                "sha256": acceptance["bridge_sha256"],
            },
        ):
            record = {
                "path": binding["path"],
                "bytes": binding["bytes"],
                "sha256": binding["sha256"],
                "parent_manifests": [],
            }
            previous = new.get(record["path"])
            if previous is not None and previous != record:
                raise VerifiedConstructionCoreError(
                    "v0.10 portable input collision differs"
                )
            new[record["path"]] = record
    rows.extend(new.values())
    rows.sort(key=lambda row: row["path"])
    if len(rows) != 72 or len({row["path"] for row in rows}) != 72:
        raise VerifiedConstructionCoreError("v0.10 portable input count differs")
    for row in rows:
        path = _repository_input(
            row["path"], row["sha256"], "v0.10 portable input"
        )
        if path.stat().st_size != row["bytes"]:
            raise VerifiedConstructionCoreError(
                "v0.10 portable input byte count differs"
            )
    return rows


def _v10_city_legal_notice_from_base() -> str:
    readme_path = LEGACY_PREVIEW_V09_DIR / "README.md"
    attribution_path = LEGACY_PREVIEW_V09_DIR / "ATTRIBUTION.txt"
    if (
        readme_path.is_symlink()
        or attribution_path.is_symlink()
        or not readme_path.is_file()
        or not attribution_path.is_file()
    ):
        raise VerifiedConstructionCoreError("v0.10 inherited City notice source differs")
    document = readme_path.read_text(encoding="utf-8")
    marker = "City of Chicago data notice"
    start = document.find(marker)
    end = document.find("\n\nThis is not the final", start)
    if start < 0 or end < 0:
        raise VerifiedConstructionCoreError("v0.10 inherited City notice differs")
    notice = document[start:end]
    disclaimer_marker = "Required disclaimer:\n"
    reminder_marker = "\n\nAdditional terms reminder:\n"
    disclaimer_start = notice.find(disclaimer_marker)
    reminder_start = notice.find(reminder_marker, disclaimer_start)
    if disclaimer_start < 0 or reminder_start < 0:
        raise VerifiedConstructionCoreError("v0.10 City notice sections differ")
    disclaimer = notice[
        disclaimer_start + len(disclaimer_marker) : reminder_start
    ]
    if (
        _sha256_bytes(disclaimer.encode("utf-8")) != V09_CITY_NOTICE_SHA256
        or "Attribution: City of Chicago" not in notice
        or f"Terms: {V09_CITY_TERMS_URL}" not in notice
        or not notice.split("Additional terms reminder:\n", 1)[1].strip()
        or notice.encode("utf-8") not in attribution_path.read_bytes()
    ):
        raise VerifiedConstructionCoreError("v0.10 City legal notice differs")
    return notice


def _v10_map_html(
    geojson: Mapping[str, Any], evidence: Sequence[Mapping[str, Any]]
) -> bytes:
    document = _v09_map_html(geojson, evidence).decode("utf-8")
    if "Verified Construction Core v0.9 preview" not in document:
        raise VerifiedConstructionCoreError("v0.10 map title seam differs")
    rendered = document.replace(
        "Verified Construction Core v0.9 preview",
        "Verified Construction Core v0.10 preview",
    )
    if (
        "© OpenStreetMap contributors" not in rendered
        or "https://www.openstreetmap.org/copyright" not in rendered
    ):
        raise VerifiedConstructionCoreError("v0.10 map attribution differs")
    return rendered.encode("utf-8")


def _v10_schema() -> dict[str, Any]:
    schema = _v09_schema()
    schema["format"] = "datacenter-atlas-verified-construction-core-schema-v10"
    schema["preview_id"] = CURRENT_V10_PREVIEW_ID
    return schema


def _v10_readme(report: Mapping[str, Any], legal_notice: str) -> bytes:
    return f"""# Verified Construction Core v0.10 preview

This tracked, non-final preview contains **{report['selected_physical_site_count']} physical sites**
and **{report['selected_project_count']} linked projects** in {len(report['country_counts'])}
countries. Every project retains a dated authoritative physical-status observation and an exact
reviewed geometry scope. Five project rows carry official boundaries; the remaining 45 are
explicit project or campus locators, never construction footprints by implication.

The three-row v0.10 delta adds Oracle Project Jupiter in Doña Ana County, CyrusOne FRA7 in
Frankfurt, and Colt London 4 in Hayes. Oracle uses a frozen exact-name OpenStreetMap polygon only
as a parent-campus locator. CyrusOne uses the Hessian FF7 L1 permit coordinate only as a project
locator. Colt uses a separately captured exact-name OpenStreetMap building only as a project
locator. None is an official boundary, parcel, complete campus, current-work extent, or imagery
verification. OSM and permit lifecycle or operator metadata do not create project lifecycle or
normalized-role claims.

Only Colt publishes a new project metric: planned 31 MW critical IT for London 4. CyrusOne's
campus-level 81 MW context and Oracle's campus-level 2,450 MW generation context and AI workload
are validated but excluded from project publication. The delta adds no normalized roles,
operating models, or workloads.

The artifact is deterministic and rebuildable in a clean clone from the 72 manifest-bound portable
inputs plus the frozen v0.9 artifact. Ignored v97, global-v3, and v14 payloads are optional complete
hydrated cross-checks, not build dependencies. All three v0.10 rows inherit the not-reviewed
imagery outcome and remain independent_imagery_verification=false.

## City of Chicago terms inherited from v0.9

{legal_notice}

This is not the final 100-site Verified Construction Core v1. Its final-release gates remain
unsatisfied and publishable_as_final is false.
""".encode("utf-8")


def _v10_attribution(
    evidence: Sequence[Mapping[str, Any]], legal_notice: str
) -> bytes:
    rows = {
        (row["publisher"], row["license"], row["source_url"])
        for row in evidence
    }
    lines = [
        "Data Center Atlas Verified Construction Core v0.10 preview",
        "",
        "Compact derived facts only; third-party terms remain controlling.",
        "",
        *(
            f"- {publisher} | {license_name} | {url}"
            for publisher, license_name, url in sorted(rows)
        ),
        "",
        "Map-provider terms:",
        *(
            f"- {label} | {license_name} | {url}"
            for label, license_name, url in _v07_map_attribution_notices(evidence)
        ),
        "",
        legal_notice,
    ]
    payload = ("\n".join(lines) + "\n").encode("utf-8")
    if (
        b"OpenStreetMap contributors" not in payload
        or legal_notice.encode("utf-8") not in payload
    ):
        raise VerifiedConstructionCoreError("v0.10 attribution posture differs")
    return payload


def _v10_payloads() -> tuple[dict[str, bytes], dict[str, Any], list[dict[str, Any]]]:
    validate_frozen_v09(LEGACY_PREVIEW_V09_DIR)
    contracts = _v10_contracts(
        hydrated_crosscheck=_v10_hydrated_crosscheck_available()
    )
    base_projects = _load_csv(
        LEGACY_PREVIEW_V09_DIR / "projects.csv", PROJECT_FIELDS
    )
    base_sites = _load_csv(LEGACY_PREVIEW_V09_DIR / "sites.csv", SITE_FIELDS)
    base_evidence = _load_csv(
        LEGACY_PREVIEW_V09_DIR / "evidence.csv", EVIDENCE_FIELDS
    )
    base_report = _load_json(
        LEGACY_PREVIEW_V09_DIR / "selection-report.json"
    )
    delta = _v10_build_delta_rows(contracts)
    projects = sorted(
        [*base_projects, *delta["projects"]],
        key=lambda row: row["project_stable_key"],
    )
    sites = sorted(
        [*base_sites, *delta["sites"]],
        key=lambda row: row["physical_site_stable_key"],
    )
    evidence = sorted(
        [*base_evidence, *delta["evidence"]],
        key=lambda row: row["evidence_id"],
    )
    if (
        len(projects) != 50
        or len(sites) != 47
        or len(evidence) != 116
        or len({row["project_id"] for row in projects}) != 50
        or len({row["site_id"] for row in sites}) != 47
        or len({row["evidence_id"] for row in evidence}) != 116
    ):
        raise VerifiedConstructionCoreError(
            "v0.10 cohort count or uniqueness differs"
        )
    project_by_id = {row["project_id"]: row for row in projects}
    site_by_id = {row["site_id"]: row for row in sites}
    evidence_by_id = {row["evidence_id"]: row for row in evidence}
    if (
        any(project_by_id[row["project_id"]] != row for row in base_projects)
        or any(site_by_id[row["site_id"]] != row for row in base_sites)
        or any(evidence_by_id[row["evidence_id"]] != row for row in base_evidence)
    ):
        raise VerifiedConstructionCoreError("v0.10 inherited row changed")
    gates = _final_release_gates(sites, projects)
    gates["clean_clone_rebuild"] = {
        "passed": True,
        "reason": (
            "all current-delta inputs and the frozen base artifact are tracked "
            "and hash-bound"
        ),
    }
    country_counts = dict(
        sorted(Counter(row["country"] for row in sites).items())
    )
    provenance_decisions = {
        key: [
            *base_report["provenance_decisions"][key],
            *contracts["provenance"][key],
        ]
        for key in (
            "workload_scope_bindings",
            "role_bindings",
            "excluded_source_roles",
            "context_evidence_bindings",
        )
    }
    report = {
        **base_report,
        "format": "datacenter-atlas-verified-construction-core-selection-v10",
        "publishable_as_final": all(
            gate.get("passed", False) for gate in gates.values()
        ),
        "reviewed_at": CURRENT_V10_REVIEW_DATE.isoformat(),
        "selected_project_count": 50,
        "selected_physical_site_count": 47,
        "official_boundary_project_count": 5,
        "reviewed_site_locator_project_count": 45,
        "non_selected_source_row_count": 481,
        "selection_first_failure_counts": V10_SELECTION_FIRST_FAILURE_COUNTS,
        "country_counts": country_counts,
        "final_release_gates": gates,
        "imagery_review_provenance": list(
            base_report["imagery_review_provenance"]
        ),
        "provenance_decisions": provenance_decisions,
        "reviewed_overlay_queue": [
            *base_report["reviewed_overlay_queue"],
            *contracts["overlays"]["overlays"],
        ],
    }
    if (
        len(country_counts) != 25
        or country_counts.get("United States") != 8
        or country_counts.get("Germany") != 5
        or country_counts.get("United Kingdom") != 3
        or sum(row["country_iso_a2"] != "US" for row in sites) != 39
        or sum(_is_official_boundary(row) for row in projects) != 5
        or sum(_is_reviewed_locator(row) for row in projects) != 45
        or sum(V10_SELECTION_FIRST_FAILURE_COUNTS.values()) != 531
        or gates["imagery_outcomes_complete"]
        != {"actual": 10, "required": 50, "passed": False}
        or len(provenance_decisions["context_evidence_bindings"]) != 7
        or any(
            row["workloads_json"] != "[]"
            or row["role_claims_json"] != "[]"
            or row["operating_model"] != "unknown"
            for row in delta["projects"]
        )
    ):
        raise VerifiedConstructionCoreError("v0.10 expected accounting differs")
    geojson = _geojson(sites)
    geojson["name"] = "Data Center Atlas Verified Construction Core v0.10 preview"
    legal_notice = _v10_city_legal_notice_from_base()
    payloads = {
        "ATTRIBUTION.txt": _v10_attribution(evidence, legal_notice),
        "README.md": _v10_readme(report, legal_notice),
        "evidence.csv": _csv_bytes(evidence, EVIDENCE_FIELDS),
        "map.html": _v10_map_html(geojson, evidence),
        "projects.csv": _csv_bytes(projects, PROJECT_FIELDS),
        "schema.json": _json_bytes(_v10_schema()),
        "selection-report.json": _json_bytes(report),
        "sites.csv": _csv_bytes(sites, SITE_FIELDS),
        "sites.geojson": _json_bytes(geojson),
    }
    portable = _v10_portable_source_inputs(contracts)
    return payloads, report, portable


def _v10_manifest(
    payloads: Mapping[str, bytes],
    report: Mapping[str, Any],
    portable: Sequence[Mapping[str, Any]],
) -> dict[str, Any]:
    return {
        "format": "datacenter-atlas-verified-construction-core-preview-v10",
        "preview_id": CURRENT_V10_PREVIEW_ID,
        "release_status": "preview",
        "publishable_as_final": report["publishable_as_final"],
        "base_preview_id": CURRENT_V09_PREVIEW_ID,
        "base_preview_manifest_sha256": LEGACY_PREVIEW_V09_MANIFEST_SHA256,
        "base_preview_commit": LEGACY_PREVIEW_V09_COMMIT,
        "source_release_id": SOURCE_RELEASE_ID,
        "source_release_manifest_path": (
            "releases/2026-07-22-open-seed-v97/manifest.json"
        ),
        "source_release_manifest_sha256": (
            "0a6f41f4239944df27f2ce70e81a089b91cec401f154bbae28412b27a4d00fdd"
        ),
        "definition_paths": {
            "imagery_reviews": (
                "definitions/verified-construction-core-v0.10-imagery-reviews.json"
            ),
            "provenance": (
                "definitions/verified-construction-core-v0.10-provenance.json"
            ),
            "reviewed_overlays": (
                "definitions/verified-construction-core-reviewed-overlays-v8.json"
            ),
            "reviewed_sites": (
                "definitions/verified-construction-core-v0.10-reviewed-sites.json"
            ),
        },
        "review_definition_sha256": V10_REVIEW_DEFINITION_SHA256,
        "imagery_review_definition_sha256": (
            V10_IMAGERY_REVIEW_DEFINITION_SHA256
        ),
        "provenance_definition_sha256": V10_PROVENANCE_DEFINITION_SHA256,
        "overlay_definition_sha256": V10_OVERLAY_DEFINITION_SHA256,
        "portable_source_inputs": list(portable),
        "reviewed_at": CURRENT_V10_REVIEW_DATE.isoformat(),
        "counts": {
            "physical_sites": 47,
            "projects": 50,
            "evidence": 116,
            "countries": 25,
            "non_us_sites": 39,
            "official_boundary_projects": 5,
            "reviewed_site_locator_projects": 45,
        },
        "files": {
            name: {"bytes": len(payload), "sha256": _sha256_bytes(payload)}
            for name, payload in sorted(payloads.items())
        },
    }


def build_preview(output_dir: Path = CURRENT_V10_PREVIEW_DIR) -> dict[str, Any]:
    """Build the current v0.10 preview from tracked, hash-bound inputs."""
    output_dir = Path(output_dir)
    if output_dir.exists():
        raise VerifiedConstructionCoreError(f"refusing to overwrite {output_dir}")
    payloads, report, portable = _v10_payloads()
    manifest = _v10_manifest(payloads, report, portable)
    manifest_bytes = _json_bytes(manifest)
    complete = {
        **payloads,
        "manifest.json": manifest_bytes,
        "manifest.sha256": (
            f"{_sha256_bytes(manifest_bytes)}  manifest.json\n".encode()
        ),
    }
    output_dir.parent.mkdir(parents=True, exist_ok=True)
    stage = Path(
        tempfile.mkdtemp(prefix=f".{output_dir.name}.", dir=output_dir.parent)
    )
    try:
        for name, payload in complete.items():
            with (stage / name).open("xb") as handle:
                handle.write(payload)
        os.replace(stage, output_dir)
    except Exception:
        shutil.rmtree(stage, ignore_errors=True)
        raise
    validate_preview(output_dir)
    return manifest


def _validate_v10_preview(path: Path) -> dict[str, Any]:
    path = Path(path)
    if path.is_symlink() or not path.is_dir():
        raise VerifiedConstructionCoreError("v0.10 preview directory differs")
    manifest_path = path / "manifest.json"
    checksum_path = path / "manifest.sha256"
    if (
        manifest_path.is_symlink()
        or not manifest_path.is_file()
        or checksum_path.is_symlink()
        or not checksum_path.is_file()
    ):
        raise VerifiedConstructionCoreError("v0.10 manifest trust root differs")
    manifest_bytes = manifest_path.read_bytes()
    if checksum_path.read_bytes() != (
        f"{_sha256_bytes(manifest_bytes)}  manifest.json\n".encode()
    ):
        raise VerifiedConstructionCoreError("v0.10 manifest checksum differs")
    manifest = _load_json(manifest_path)
    files = manifest.get("files") if isinstance(manifest, dict) else None
    if (
        not isinstance(manifest, dict)
        or manifest.get("format")
        != "datacenter-atlas-verified-construction-core-preview-v10"
        or manifest.get("preview_id") != CURRENT_V10_PREVIEW_ID
        or manifest.get("release_status") != "preview"
        or type(manifest.get("publishable_as_final")) is not bool
        or manifest["publishable_as_final"] is not False
        or not isinstance(files, dict)
    ):
        raise VerifiedConstructionCoreError("v0.10 manifest files differ")
    expected_inventory = set(files) | {"manifest.json", "manifest.sha256"}
    if {item.name for item in path.iterdir()} != expected_inventory:
        raise VerifiedConstructionCoreError("v0.10 preview inventory differs")
    for name, metadata in files.items():
        member = path / name
        if (
            Path(name).name != name
            or member.is_symlink()
            or not member.is_file()
            or type(metadata.get("bytes")) is not int
            or member.stat().st_size != metadata["bytes"]
            or _sha256_file(member) != metadata.get("sha256")
        ):
            raise VerifiedConstructionCoreError(
                f"v0.10 member differs: {name}"
            )
    payloads, report, portable = _v10_payloads()
    if manifest != _v10_manifest(payloads, report, portable):
        raise VerifiedConstructionCoreError("v0.10 manifest semantics differ")
    for name, expected in payloads.items():
        if (path / name).read_bytes() != expected:
            raise VerifiedConstructionCoreError(
                f"v0.10 generated member differs: {name}"
            )
    legal_notice = _v10_city_legal_notice_from_base().encode("utf-8")
    for name in ("README.md", "ATTRIBUTION.txt"):
        if legal_notice not in (path / name).read_bytes():
            raise VerifiedConstructionCoreError(
                f"v0.10 City legal notice missing from {name}"
            )
    return manifest


def validate_preview(path: Path = CURRENT_V10_PREVIEW_DIR) -> dict[str, Any]:
    """Validate current v0.10 or dispatch immutable v0.1-v0.9 previews."""
    path = Path(path)
    manifest_path = path / "manifest.json"
    if (
        path.is_symlink()
        or not path.is_dir()
        or manifest_path.is_symlink()
        or not manifest_path.is_file()
    ):
        raise VerifiedConstructionCoreError("preview manifest trust root differs")
    manifest = _load_json(manifest_path)
    preview_id = manifest.get("preview_id") if isinstance(manifest, dict) else None
    if preview_id == CURRENT_V10_PREVIEW_ID:
        return _validate_v10_preview(path)
    validator = _frozen_preview_validator(preview_id)
    if validator is not None:
        return validator(path)
    raise VerifiedConstructionCoreError("preview id differs")


__all__ = [
    "CURRENT_V07_DEFINITION_PATHS",
    "CURRENT_V07_PREVIEW_DIR",
    "CURRENT_V07_PREVIEW_ID",
    "CURRENT_V07_REVIEW_DATE",
    "CURRENT_V08_DEFINITION_PATHS",
    "CURRENT_V08_PREVIEW_DIR",
    "CURRENT_V08_PREVIEW_ID",
    "CURRENT_V08_REVIEW_DATE",
    "CURRENT_V09_DEFINITION_PATHS",
    "CURRENT_V09_PREVIEW_DIR",
    "CURRENT_V09_PREVIEW_ID",
    "CURRENT_V09_REVIEW_DATE",
    "CURRENT_V10_DEFINITION_PATHS",
    "CURRENT_V10_PREVIEW_DIR",
    "CURRENT_V10_PREVIEW_ID",
    "CURRENT_V10_REVIEW_DATE",
    "IMAGERY_REVIEW_DEFINITION",
    "LEGACY_PREVIEW_V01_DIR",
    "LEGACY_PREVIEW_V02_DIR",
    "LEGACY_PREVIEW_V03_DIR",
    "LEGACY_PREVIEW_V04_DIR",
    "LEGACY_PREVIEW_V05_COMMIT",
    "LEGACY_PREVIEW_V05_DIR",
    "LEGACY_PREVIEW_V06_COMMIT",
    "LEGACY_PREVIEW_V06_DIR",
    "LEGACY_PREVIEW_V06_MANIFEST_SHA256",
    "LEGACY_PREVIEW_V07_COMMIT",
    "LEGACY_PREVIEW_V07_DIR",
    "LEGACY_PREVIEW_V07_MANIFEST_SHA256",
    "LEGACY_PREVIEW_V08_COMMIT",
    "LEGACY_PREVIEW_V08_DIR",
    "LEGACY_PREVIEW_V08_MANIFEST_SHA256",
    "LEGACY_PREVIEW_V09_COMMIT",
    "LEGACY_PREVIEW_V09_DIR",
    "LEGACY_PREVIEW_V09_MANIFEST_SHA256",
    "PREVIEW_DIR",
    "PreviewProfile",
    "PROVENANCE_DEFINITION",
    "VerifiedConstructionCoreError",
    "build_preview",
    "load_current_v07_profile",
    "load_current_v08_profile",
    "load_current_v09_profile",
    "load_current_v10_profile",
    "validate_frozen_preview",
    "validate_frozen_v01",
    "validate_frozen_v02",
    "validate_frozen_v03",
    "validate_frozen_v04",
    "validate_frozen_v05",
    "validate_frozen_v06",
    "validate_frozen_v07",
    "validate_frozen_v08",
    "validate_frozen_v09",
    "validate_preview",
]

_build_v10_preview = build_preview
_validate_v10_preview_dispatch = validate_preview

LEGACY_PREVIEW_V10_DIR = (
    ROOT / "verified_construction_core" / "2026-08-20-preview-v0.10"
)
LEGACY_PREVIEW_V10_MANIFEST_SHA256 = (
    "379b3c287e2a0c3ac45928e64cd206b7cdc730fd8a913a8401e2cf0d76d9e00f"
)
LEGACY_PREVIEW_V10_COMMIT = "e22d82229dfd2730ab219969eba515ad3162056a"
CURRENT_V11_PREVIEW_ID = "2026-08-20-preview-v0.11"
CURRENT_V11_PREVIEW_DIR = ROOT / "verified_construction_core" / CURRENT_V11_PREVIEW_ID
CURRENT_V11_LIFECYCLE_REFERENCE_DATE = date(2026, 8, 20)
CURRENT_V11_GEOMETRY_IDENTITY_REVIEW_DATE = date(2026, 8, 23)
# PreviewProfile.reviewed_at is the legacy name for the cohort lifecycle clock.
CURRENT_V11_REVIEW_DATE = CURRENT_V11_LIFECYCLE_REFERENCE_DATE
V11_REVIEW_DEFINITION_SHA256 = (
    "da9580f99c2e86a1e5cc20e56ba5febf9b4ce3737c5fcfe0f4df5a32b9715b58"
)
V11_OVERLAY_DEFINITION_SHA256 = (
    "5deffc0f6c65080e1143777b0ee7824058d17bdd287c45056d82bd4517673a03"
)
V11_PROVENANCE_DEFINITION_SHA256 = (
    "63924830e1efd815316f428f82b76df17f3c07ac056a6b2b508e9e29a50bfc05"
)
V11_IMAGERY_REVIEW_DEFINITION_SHA256 = (
    "94ad485149934cd538ffef6e5984570c138e0fe3d5b626f793fdcf624635219e"
)
V11_REVIEW_DEFINITION = (
    ROOT / "definitions" / "verified-construction-core-v0.11-reviewed-sites.json"
)
V11_OVERLAY_DEFINITION = (
    ROOT / "definitions" / "verified-construction-core-reviewed-overlays-v9.json"
)
V11_PROVENANCE_DEFINITION = (
    ROOT / "definitions" / "verified-construction-core-v0.11-provenance.json"
)
V11_IMAGERY_REVIEW_DEFINITION = (
    ROOT / "definitions" / "verified-construction-core-v0.11-imagery-reviews.json"
)
CURRENT_V11_DEFINITION_PATHS = (
    ("review_definition_sha256", V11_REVIEW_DEFINITION),
    ("imagery_review_definition_sha256", V11_IMAGERY_REVIEW_DEFINITION),
    ("provenance_definition_sha256", V11_PROVENANCE_DEFINITION),
    ("overlay_definition_sha256", V11_OVERLAY_DEFINITION),
)
V11_SELECTION_FIRST_FAILURE_COUNTS = {
    "entity_kind_not_project": 49,
    "not_in_reviewed_site_geometry_allowlist": 92,
    "selected": 51,
    "status_not_physical": 12,
    "status_outside_90_day_window": 327,
}
V11_PROJECT_KEY = (
    "curated:iij-shiroi-data-center-campus:phase-3-server-building"
)
V11_CAMPUS_KEY = "curated:iij-shiroi-data-center-campus"
V11_PROJECT_ID = "1f33fed8-b0f9-50e2-8fac-56d135325c27"
V11_CAMPUS_ID = "1f6b0a4f-542b-51cf-b042-afe048791fc9"
V11_GEOMETRY_EVIDENCE_ID = "f6d41f05-04ff-5e9b-8be6-164d895d24d6"
V11_IDENTITY_EVIDENCE_ID = "846dc1a1-5268-544e-92c2-2df087877703"
V11_SOURCE_PATH = (
    ROOT / "sources" / "curated-official-2026-07-20-iij-shiroi-phase-3.json"
)
V11_SOURCE_SHA256 = (
    "ed214cfed7495ad6b1cf7a65f2cc1e90d80d12b58b615afb2931b98cb0bb6714"
)
V11_SOURCE_SEMANTICS_SHA256 = (
    "d3ded935516fbd6cc3d900ded5c0e0ae087c8e06d0c7f0dff4d22a1e6ffc8139"
)
V11_BRIDGE_PATH = (
    ROOT
    / "sources"
    / (
        "verified-construction-core-v0.11-iij-shiroi-phase3-campus-"
        "address-area-geometry-bridge.json"
    )
)
V11_BRIDGE_SHA256 = (
    "2dc78a324b0eb00d66c9d714ff7d989862577ea77d75ae0f2b8fbf195335ce74"
)
V11_BRIDGE_BYTES = 31503
V11_BRIDGE_SEMANTIC_HASHES = {
    "claim_guardrails": (
        "c2bf7938195ba5ba5899beaedc90c9377b51a6a1dbf1adbd85c73409a1130bfa"
    ),
    "construction_source": (
        "550532d6cd90b042c4c12ace734da55ab6dc10648d94a1a12cc6fe6c2de1cdc3"
    ),
    "false_match_guardrails": (
        "56c43565008ca54ecf048944c18718c0ec43bcfa2d8e8df812e885ba69c83f5f"
    ),
    "geometry_capture": (
        "c6ee6d6b971c5008c0207aedd80f3472cd0305fe2d8c1e636ddc6bdb7d7cc2c8"
    ),
    "geometry_entity": (
        "e4192cd97251fa81f9cc6cc41751f554a43c2605365cc7aeeb082194d948f5a4"
    ),
    "geometry_evidence": (
        "46eec4f8f42682d80ebc3b4f1a72bff8579fd712e3c6cc0b2a73d54a1ffa49c8"
    ),
    "identity_bridge_evidence": (
        "a8c6eaf124b5590fb6f5607a05796bfc35ff81c0defdfbeb550375e5e0845bfb"
    ),
    "imagery_posture": (
        "183b4a564aef04933819ba1d91e36f80578219a51e4a54a4cbf3db5876daa1ca"
    ),
    "review_decision": (
        "1298d315159f834b44691aa43dd6bbc87754abbecb248c8ab99ae63aa63f6e04"
    ),
    "rights": (
        "8adb4e54ffa1f7daa8bdb8724ab619ce7ea66f641bc61308a4a1b00f242dbd21"
    ),
}
V11_EVIDENCE_IDS = frozenset(
    {
        "4bd37265-9c6e-556b-8899-8015ffc11cae",
        "35d502eb-803a-5c40-8dc1-f0825babb831",
        V11_GEOMETRY_EVIDENCE_ID,
        V11_IDENTITY_EVIDENCE_ID,
    }
)


def load_current_v11_profile(
    definition_sha256: Mapping[str, str],
) -> PreviewProfile:
    """Load v0.11 only after all four final definition hashes are supplied."""
    expected_fields = {field for field, _ in CURRENT_V11_DEFINITION_PATHS}
    if set(definition_sha256) != expected_fields:
        raise VerifiedConstructionCoreError(
            "current v0.11 definition pins are incomplete"
        )
    pins: list[tuple[str, Path, str]] = []
    for field, path in CURRENT_V11_DEFINITION_PATHS:
        expected = definition_sha256[field]
        if (
            re.fullmatch(r"[0-9a-f]{64}", expected) is None
            or path.is_symlink()
            or not path.is_file()
            or _sha256_file(path) != expected
        ):
            raise VerifiedConstructionCoreError(
                f"current v0.11 {field} source hash differs"
            )
        pins.append((field, path, expected))
    return PreviewProfile(
        preview_id=CURRENT_V11_PREVIEW_ID,
        preview_dir=CURRENT_V11_PREVIEW_DIR,
        reviewed_at=CURRENT_V11_LIFECYCLE_REFERENCE_DATE,
        base_preview_id=CURRENT_V10_PREVIEW_ID,
        base_preview_dir=LEGACY_PREVIEW_V10_DIR,
        base_manifest_sha256=LEGACY_PREVIEW_V10_MANIFEST_SHA256,
        base_commit=LEGACY_PREVIEW_V10_COMMIT,
        definition_pins=tuple(pins),
    )


def validate_frozen_v10(
    path: Path = LEGACY_PREVIEW_V10_DIR,
) -> dict[str, Any]:
    """Validate byte-frozen v0.10 from its immutable inventory only."""
    return _validate_frozen_preview_inventory(
        path,
        preview_id=CURRENT_V10_PREVIEW_ID,
        manifest_sha256=LEGACY_PREVIEW_V10_MANIFEST_SHA256,
        version_label="v0.10",
    )


_FROZEN_PREVIEW_VALIDATORS = (
    *_FROZEN_PREVIEW_VALIDATORS,
    (CURRENT_V10_PREVIEW_ID, validate_frozen_v10),
)


def _v11_definition_paths() -> dict[str, tuple[Path, str]]:
    return {
        "review_definition_sha256": (
            V11_REVIEW_DEFINITION,
            V11_REVIEW_DEFINITION_SHA256,
        ),
        "imagery_review_definition_sha256": (
            V11_IMAGERY_REVIEW_DEFINITION,
            V11_IMAGERY_REVIEW_DEFINITION_SHA256,
        ),
        "provenance_definition_sha256": (
            V11_PROVENANCE_DEFINITION,
            V11_PROVENANCE_DEFINITION_SHA256,
        ),
        "overlay_definition_sha256": (
            V11_OVERLAY_DEFINITION,
            V11_OVERLAY_DEFINITION_SHA256,
        ),
    }


def _validate_v11_definition_pins() -> None:
    for field, (path, expected) in _v11_definition_paths().items():
        if path.is_symlink() or not path.is_file() or _sha256_file(path) != expected:
            raise VerifiedConstructionCoreError(
                f"current v0.11 {field} source hash differs"
            )


def _v11_hydrated_crosscheck_available(
    bridge: Mapping[str, Any],
) -> bool:
    release = bridge["construction_source"]["release"]
    topology = bridge["construction_source"]["project_to_campus"]
    bindings = [
        *release["members"].values(),
        *topology["members"].values(),
    ]
    paths = [ROOT / binding["path"] for binding in bindings]
    present = [path.is_file() for path in paths]
    if any(present) and not all(present):
        raise VerifiedConstructionCoreError(
            "v0.11 hydrated cross-check inputs are only partially present"
        )
    if not all(present):
        return False
    for binding in bindings:
        path = _repository_input(
            binding["path"], binding["sha256"], "v0.11 hydrated member"
        )
        if path.stat().st_size != binding["bytes"]:
            raise VerifiedConstructionCoreError(
                "v0.11 hydrated member byte count differs"
            )
    expected = {
        "construction_pipeline.csv": {
            "entity_id": V11_PROJECT_ID,
            "stable_key": V11_PROJECT_KEY,
            "status_evidence_id": "4bd37265-9c6e-556b-8899-8015ffc11cae",
        },
        "entities.csv": None,
        "evidence.csv": None,
        "capacity_estimates.csv": {
            "entity_id": V11_PROJECT_ID,
            "metric": "grid_connection_mw",
            "stage": "planned",
            "base": "10.0",
            "evidence_id": "35d502eb-803a-5c40-8dc1-f0825babb831",
        },
        "component-members.csv": None,
        "relationships.csv": {
            "relationship_id": (
                "relationship:"
                "18e12b64f2807c4cdd0093f0edb72229afa8b093d785eafbcb8d5db27160fe8a"
            ),
            "relationship_type": "project_targets",
            "decision_basis": "explicit_parent",
        },
    }
    by_name = {Path(binding["path"]).name: ROOT / binding["path"] for binding in bindings}
    for name, projection in expected.items():
        if projection is None:
            continue
        with by_name[name].open(newline="", encoding="utf-8") as handle:
            rows = list(csv.DictReader(handle))
        if not any(
            all(row.get(field) == value for field, value in projection.items())
            for row in rows
        ):
            raise VerifiedConstructionCoreError(
                f"v0.11 hydrated {name} projection differs"
            )
    with by_name["entities.csv"].open(newline="", encoding="utf-8") as handle:
        entities = {
            row["entity_id"]: row
            for row in csv.DictReader(handle)
            if row["entity_id"] in {V11_PROJECT_ID, V11_CAMPUS_ID}
        }
    if (
        set(entities) != {V11_PROJECT_ID, V11_CAMPUS_ID}
        or entities[V11_PROJECT_ID]["stable_key"] != V11_PROJECT_KEY
        or entities[V11_CAMPUS_ID]["stable_key"] != V11_CAMPUS_KEY
    ):
        raise VerifiedConstructionCoreError(
            "v0.11 hydrated entity projection differs"
        )
    with by_name["evidence.csv"].open(newline="", encoding="utf-8") as handle:
        evidence_ids = {row["evidence_id"] for row in csv.DictReader(handle)}
    if not {
        "4bd37265-9c6e-556b-8899-8015ffc11cae",
        "35d502eb-803a-5c40-8dc1-f0825babb831",
    }.issubset(evidence_ids):
        raise VerifiedConstructionCoreError(
            "v0.11 hydrated evidence projection differs"
        )
    with by_name["component-members.csv"].open(
        newline="", encoding="utf-8"
    ) as handle:
        component_ids = {row["component_id"] for row in csv.DictReader(handle)}
    if not {
        "exact:c3e7b0529b6b0862e9bc7e5092812edd276db783fa1cda2d349fa8a78bab10cb",
        "exact:d124734ecc014c3ab50697955b7437d1f764765f4d2fcba6c885465d7d13e8d2",
    }.issubset(component_ids):
        raise VerifiedConstructionCoreError(
            "v0.11 hydrated component projection differs"
        )
    return True


def _v11_validate_bridge(
    acceptance: Mapping[str, Any],
    overlay: Mapping[str, Any],
) -> tuple[dict[str, Any], dict[str, Any]]:
    if (
        acceptance["bridge_path"]
        != V11_BRIDGE_PATH.relative_to(ROOT).as_posix()
        or acceptance["bridge_bytes"] != V11_BRIDGE_BYTES
        or acceptance["bridge_sha256"] != V11_BRIDGE_SHA256
        or acceptance["source_input_path"]
        != V11_SOURCE_PATH.relative_to(ROOT).as_posix()
        or acceptance["source_input_bytes"] != 12574
        or acceptance["source_input_sha256"] != V11_SOURCE_SHA256
        or type(acceptance.get("official_boundary")) is not bool
        or acceptance["official_boundary"] is not False
        or acceptance["portable_capture_bindings"] != []
    ):
        raise VerifiedConstructionCoreError("v0.11 immutable bridge profile differs")
    bridge_path = _repository_input(
        acceptance["bridge_path"], V11_BRIDGE_SHA256, "v0.11 bridge"
    )
    if bridge_path.stat().st_size != V11_BRIDGE_BYTES:
        raise VerifiedConstructionCoreError("v0.11 bridge bytes differ")
    bridge = _load_json(bridge_path)
    if (
        not isinstance(bridge, dict)
        or set(bridge)
        != {
            "bridge_id",
            "bridge_schema",
            "claim_guardrails",
            "construction_source",
            "false_match_guardrails",
            "geometry_capture",
            "geometry_entity",
            "geometry_evidence",
            "geometry_release",
            "identity_bridge_evidence",
            "imagery_posture",
            "materialization_scope",
            "review_decision",
            "reviewed_at",
            "rights",
            "schema_version",
        }
        or type(bridge.get("schema_version")) is not int
        or bridge["schema_version"] != 1
        or bridge["reviewed_at"]
        != CURRENT_V11_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
        or bridge["geometry_release"] is not None
        or bridge["bridge_id"] != acceptance["bridge_id"]
        or bridge["bridge_schema"] != acceptance["bridge_schema"]
    ):
        raise VerifiedConstructionCoreError("v0.11 bridge schema differs")
    for field, expected in V11_BRIDGE_SEMANTIC_HASHES.items():
        if _sha256_bytes(_json_bytes(bridge.get(field))) != expected:
            raise VerifiedConstructionCoreError(
                f"v0.11 {field.replace('_', ' ')} semantic profile differs"
            )
    source_path = _repository_input(
        acceptance["source_input_path"],
        V11_SOURCE_SHA256,
        "v0.11 curated source",
    )
    if source_path.stat().st_size != 12574:
        raise VerifiedConstructionCoreError("v0.11 source bytes differ")
    source = _load_json(source_path)
    if (
        not isinstance(source, dict)
        or source.get("schema_version") != "1.0"
        or _sha256_bytes(_json_bytes(source)) != V11_SOURCE_SEMANTICS_SHA256
    ):
        raise VerifiedConstructionCoreError("v0.11 source semantics differ")
    evidence_by_key = {
        row["key"]: row for row in source["evidence"] if isinstance(row, dict)
    }
    expected_project = _v08_source_entity_projection(
        source, evidence_by_key, entity_kind="project"
    )
    expected_project["normalized_roles"] = _v08_normalized_roles(source["project"])
    expected_campus = _v08_source_entity_projection(
        source, evidence_by_key, entity_kind="campus"
    )
    expected_campus["normalized_roles"] = _v08_normalized_roles(source["campus"])
    construction = bridge["construction_source"]
    source_evidence = _v08_source_evidence(source)
    if (
        construction["input"]
        != {
            "path": acceptance["source_input_path"],
            "bytes": acceptance["source_input_bytes"],
            "sha256": acceptance["source_input_sha256"],
        }
        or acceptance["portable_input_binding"] != construction["input"]
        or construction["project"] != expected_project
        or construction["campus"] != expected_campus
        or construction["status_evidence"]
        != source_evidence["4bd37265-9c6e-556b-8899-8015ffc11cae"]
        or construction["capacity_evidence"]
        != source_evidence["35d502eb-803a-5c40-8dc1-f0825babb831"]
        or expected_project["status_age_days_at_review"] != 56
        or expected_project["operating_model"] is not None
        or expected_project["workloads_json"] != []
        or expected_project["normalized_roles"]
        != {
            "owner": None,
            "operator": None,
            "users": [],
            "tenants": [],
            "customers": [],
        }
        or len(expected_project["capacity_estimates_json"]) != 1
        or expected_project["capacity_estimates_json"][0]["metric"]
        != "grid_connection_mw"
        or expected_project["capacity_estimates_json"][0]["stage"] != "planned"
        or expected_project["capacity_estimates_json"][0]["base"] != 10.0
        or expected_project["capacity_estimates_json"][0]["evidence_id"]
        != "35d502eb-803a-5c40-8dc1-f0825babb831"
    ):
        raise VerifiedConstructionCoreError(
            "v0.11 construction-source projection differs"
        )
    topology = construction["project_to_campus"]
    if (
        topology["relationship_id"]
        != (
            "relationship:"
            "18e12b64f2807c4cdd0093f0edb72229afa8b093d785eafbcb8d5db27160fe8a"
        )
        or topology["relationship_type"] != "project_targets"
        or topology["decision_basis"] != "explicit_parent"
        or topology["subject_entity_id"] != V11_PROJECT_ID
        or topology["object_entity_id"] != V11_CAMPUS_ID
        or acceptance["project_to_campus_relationship_id"]
        != topology["relationship_id"]
        or acceptance["project_to_campus_relationship_type"]
        != topology["relationship_type"]
        or acceptance["project_to_campus_decision_basis"]
        != topology["decision_basis"]
    ):
        raise VerifiedConstructionCoreError("v0.11 topology differs")
    identities = bridge["identity_bridge_evidence"]
    if (
        not isinstance(identities, list)
        or len(identities) != 2
        or [row.get("evidence_id") for row in identities]
        != [V11_GEOMETRY_EVIDENCE_ID, V11_IDENTITY_EVIDENCE_ID]
        or any(
            type(row.get("public_evidence_projection")) is not bool
            or row["public_evidence_projection"] is not True
            or row.get("retrieved_at") != "2026-08-23"
            or row.get("cohort_lifecycle_reference_date") != "2026-08-20"
            for row in identities
        )
    ):
        raise VerifiedConstructionCoreError("v0.11 identity evidence differs")
    for identity in identities:
        fact = identity.get("fact_payload")
        canonical = identity.get("fact_payload_canonical")
        if (
            not isinstance(fact, dict)
            or not isinstance(canonical, dict)
            or canonical.get("bytes") != len(_json_bytes(fact))
            or canonical.get("sha256") != _sha256_bytes(_json_bytes(fact))
            or identity.get("content_hash") != canonical["sha256"]
            or identity["evidence_id"] != _official_evidence_id(identity)
        ):
            raise VerifiedConstructionCoreError(
                "v0.11 canonical official fact differs"
            )
    geometry_fact = identities[0]["fact_payload"]
    chiba_fact = identities[1]["fact_payload"]
    entity = bridge["geometry_entity"]
    evidence = bridge["geometry_evidence"]
    processing_attribution = (
        "Portal Site of Official Statistics of Japan (e-Stat); Statistics "
        "Bureau of Japan; selected Shapefile feature converted to GeoJSON by "
        "Data Center Atlas"
    )
    _validate_polygon(entity["geometry"], label="v0.11 e-Stat geometry")
    if (
        entity["entity_kind"] != "address_area"
        or entity["stable_key"] != "estat:2020:census-small-area:12232032005"
        or entity["entity_id"]
        != atlas_stable_id("entity", entity["stable_key"], entity["entity_kind"])
        or entity["geometry"] != geometry_fact["geometry"]
        or [entity["longitude"], entity["latitude"]] != [140.099553, 35.799113]
        or len(entity["geometry"]["coordinates"][0]) != 28
        or geometry_fact["members"]
        != {
            "r2ka12232.shp": {
                "bytes": 149660,
                "sha256": (
                    "bab6b51c533b99e0192fa86b9bb582e04ff29ba614c52c31e71f38c42413b805"
                ),
            },
            "r2ka12232.shx": {
                "bytes": 580,
                "sha256": (
                    "f03f25ddc3b2bb984661bad1e7b9010f797ab87492c059f37610b59ca52c49a8"
                ),
            },
            "r2ka12232.dbf": {
                "bytes": 18422,
                "sha256": (
                    "1f8e326325a52d30944a7c393074f0f8e340a3a5b2c06ce2ac7ed0d758847278"
                ),
            },
            "r2ka12232.prj": {
                "bytes": 147,
                "sha256": (
                    "7f9245352d5795209fde0ba46668aa9db40a41f80f0986ac5f72b036914a6a9d"
                ),
            },
        }
        or geometry_fact["selection"]["shapefile_feature_index_zero_based"]
        != 47
        or geometry_fact["selection"]["shp_record_number_one_based"] != 48
        or geometry_fact["selection"]["point_count_including_closure"] != 28
        or geometry_fact["selection"]["dbf_selected_row_sha256"]
        != "7bcd702b0539b4e794fef281e6bb8fe337b22bcf13adcf1b5c60de2258a28988"
        or geometry_fact["selection"]["shp_record_sha256"]
        != "e4511eae24936f192c293ff405ccaede6349c7290ec54427c3a93f185c3acf0f"
        or geometry_fact["selected_feature"]
        != {
            "KEY_CODE": "12232032005",
            "S_NAME": "桜台五丁目",
            "AREA": 55117.019,
            "X_CODE": 140.099553,
            "Y_CODE": 35.799113,
        }
        or evidence["evidence_id"] != V11_GEOMETRY_EVIDENCE_ID
        or evidence["content_hash"]
        != identities[0]["fact_payload_canonical"]["sha256"]
        or evidence["published_at"] is not None
        or identities[0]["attribution"] != processing_attribution
        or evidence["attribution"] != processing_attribution
        or entity["source_attribution"] != processing_attribution
        or chiba_fact["document_sha256"]
        != "631474fe20bfd1f146a98166778de159ed3d599e46c42adce28f7a4e950e43de"
        or chiba_fact["document_bytes"] != 615828
        or chiba_fact["exact_rows"][0]
        != {
            "facility_name": "白井データセンターキャンパス",
            "address": "白井市桜台５－１－１",
            "binding": "campus_identity",
        }
        or chiba_fact["exact_rows"][1]["binding"] != "false_match_guardrail_only"
    ):
        raise VerifiedConstructionCoreError("v0.11 official geometry fact differs")
    decision = bridge["review_decision"]
    bindings = {
        "decision": "bridge_decision",
        "decision_basis": "decision_basis",
        "geometry_target_entity_kind": "geometry_target_entity_kind",
        "geometry_target_entity_stable_key": "geometry_target_entity_stable_key",
        "geometry_target_entity_id": "geometry_target_entity_id",
        "geometry_derivation": "geometry_derivation",
        "geometry_method": "geometry_method",
        "geometry_scope_class": "geometry_scope_class",
        "geometry_authority_class": "geometry_authority_class",
        "geometry_use_scope": "geometry_use_scope",
        "horizontal_uncertainty_metres": "horizontal_uncertainty_metres",
        "horizontal_uncertainty_unknown_reason": (
            "horizontal_uncertainty_unknown_reason"
        ),
        "precision_scope": "precision_scope",
    }
    capture = bridge["geometry_capture"]
    imagery = bridge["imagery_posture"]
    rights = bridge["rights"]
    replay = capture["replay_contract"]
    if (
        any(
            decision.get(field) != acceptance[target]
            for field, target in bindings.items()
        )
        or type(decision.get("official_boundary")) is not bool
        or decision["official_boundary"] is not False
        or decision["geometry_target_entity_id"] != V11_CAMPUS_ID
        or decision["geometry_use_scope"] != "campus_locator"
        or type(capture.get("raw_archive_redistributed")) is not bool
        or capture["raw_archive_redistributed"] is not False
        or type(capture.get("raw_members_redistributed")) is not bool
        or capture["raw_members_redistributed"] is not False
        or replay["selected_feature_key"] != "12232032005"
        or replay["selected_feature_fact_canonical"]
        != identities[0]["fact_payload_canonical"]
        or replay["geometry_canonical"]
        != {
            "bytes": len(_json_bytes(entity["geometry"])),
            "sha256": _sha256_bytes(_json_bytes(entity["geometry"])),
            "canonicalization": (
                "UTF-8 JSON, sorted keys, compact separators, one trailing LF"
            ),
        }
        or type(capture["rights"].get("attribution_required")) is not bool
        or capture["rights"]["attribution_required"] is not True
        or type(capture["rights"].get("reuse_allowed")) is not bool
        or capture["rights"]["reuse_allowed"] is not True
        or capture["rights"]["terms_url"]
        != "https://www.e-stat.go.jp/terms-of-use"
        or capture["rights"]["terms_body_sha256"]
        != "134191804369c3371431e503b9f718efba9e43a1b0f8436d6a56ff0f7868a24f"
        or capture["rights"]["gis_terms_url"]
        != "https://www.e-stat.go.jp/gis-terms"
        or capture["rights"]["gis_terms_body_sha256"]
        != "50080a3478d10ea7352d4ff8d9c83707d5cc58e9ddd09dbfd7ebe3455438e80c"
        or capture["rights"]["processing_notice"]
        != "Selected Shapefile feature converted to GeoJSON by Data Center Atlas."
        or type(imagery.get("independent_imagery_verification")) is not bool
        or imagery["independent_imagery_verification"] is not False
        or imagery["outcome"] != "not_reviewed_for_core_preview"
        or rights["geometry_license_url"]
        != "https://www.e-stat.go.jp/terms-of-use"
        or rights["geometry_gis_terms_url"] != "https://www.e-stat.go.jp/gis-terms"
        or rights["geometry_attribution"] != processing_attribution
        or "55,117.019 square metres is only the selected statistical feature's area"
        not in decision["precision_scope"]
        or "no address point or point-in-polygon containment is asserted"
        not in decision["decision_basis"]
    ):
        raise VerifiedConstructionCoreError(
            "v0.11 rights scope or boolean semantics differ"
        )
    _v11_hydrated_crosscheck_available(bridge)
    return bridge, source


def _v11_contracts() -> dict[str, Any]:
    _validate_v11_definition_pins()
    reviewed = _load_json(V11_REVIEW_DEFINITION)
    overlays = _load_json(V11_OVERLAY_DEFINITION)
    provenance = _load_json(V11_PROVENANCE_DEFINITION)
    imagery = _load_json(V11_IMAGERY_REVIEW_DEFINITION)
    if (
        not isinstance(reviewed, dict)
        or set(reviewed)
        != {
            "contract_id",
            "review_scope",
            "reviewed_as_of",
            "base_contract",
            "acceptances",
        }
        or reviewed["contract_id"] != "verified-construction-core-v0.11-reviewed-sites"
        or reviewed["reviewed_as_of"]
        != CURRENT_V11_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
        or reviewed["base_contract"]
        != {
            "path": "definitions/verified-construction-core-v0.10-reviewed-sites.json",
            "sha256": V10_REVIEW_DEFINITION_SHA256,
            "default_geometry_entity": "project",
            "default_geometry_derivation": "direct_geometry",
        }
        or not isinstance(reviewed["acceptances"], list)
        or len(reviewed["acceptances"]) != 1
        or set(reviewed["acceptances"][0]) != V10_ACCEPTANCE_REQUIRED_FIELDS
    ):
        raise VerifiedConstructionCoreError("v0.11 reviewed-site contract differs")
    if (
        not isinstance(overlays, dict)
        or set(overlays)
        != {
            "contract_id",
            "purpose",
            "reviewed_as_of",
            "base_contract",
            "required_fields",
            "allowed_decisions",
            "overlays",
        }
        or overlays["contract_id"]
        != "verified-construction-core-reviewed-overlays-v9"
        or overlays["reviewed_as_of"]
        != CURRENT_V11_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
        or overlays["base_contract"]
        != {
            "path": (
                "definitions/verified-construction-core-reviewed-overlays-v8.json"
            ),
            "sha256": V10_OVERLAY_DEFINITION_SHA256,
        }
        or tuple(overlays["required_fields"]) != V10_OVERLAY_REQUIRED_FIELD_ORDER
        or overlays["allowed_decisions"] != ["queued", "accepted", "excluded"]
        or not isinstance(overlays["overlays"], list)
        or len(overlays["overlays"]) != 1
        or set(overlays["overlays"][0]) != V10_OVERLAY_REQUIRED_FIELDS
    ):
        raise VerifiedConstructionCoreError("v0.11 overlay contract differs")
    acceptance = reviewed["acceptances"][0]
    overlay = overlays["overlays"][0]
    crosswalk = {
        field: field
        for field in V10_ACCEPTANCE_REQUIRED_FIELDS
        if field
        not in {
            "project_stable_key",
            "project_entity_id",
            "geometry_overlay_id",
            "decision_basis",
        }
    }
    if (
        acceptance["project_stable_key"] != V11_PROJECT_KEY
        or acceptance["project_entity_id"] != V11_PROJECT_ID
        or any(
            acceptance[left] != overlay[right]
            for left, right in crosswalk.items()
        )
        or acceptance["project_stable_key"]
        != overlay["source_project_stable_key"]
        or acceptance["project_entity_id"] != overlay["source_project_entity_id"]
        or acceptance["geometry_overlay_id"] != overlay["overlay_id"]
        or acceptance["decision_basis"] != overlay["decision_reason"]
        or type(overlay.get("official_boundary")) is not bool
        or overlay["official_boundary"] is not False
        or overlay["decision"] != "accepted"
        or overlay["reviewed_at"]
        != CURRENT_V11_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
    ):
        raise VerifiedConstructionCoreError(
            "v0.11 acceptance-overlay binding differs"
        )
    bridge, source = _v11_validate_bridge(acceptance, overlay)
    if (
        not isinstance(provenance, dict)
        or set(provenance)
        != {
            "contract_id",
            "review_scope",
            "reviewed_as_of",
            "base_contract",
            "workload_scope_bindings",
            "role_bindings",
            "excluded_source_roles",
            "context_evidence_bindings",
        }
        or provenance["contract_id"]
        != "verified-construction-core-v0.11-provenance"
        or provenance["reviewed_as_of"]
        != CURRENT_V11_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
        or provenance["base_contract"]
        != {
            "path": "definitions/verified-construction-core-v0.10-provenance.json",
            "sha256": V10_PROVENANCE_DEFINITION_SHA256,
        }
        or provenance["workload_scope_bindings"] != []
        or provenance["role_bindings"] != []
        or len(provenance["excluded_source_roles"]) != 1
        or provenance["excluded_source_roles"][0]["role"] != "developer"
        or provenance["excluded_source_roles"][0]["party"]
        != "Internet Initiative Japan Inc."
        or provenance["excluded_source_roles"][0]["candidate_evidence_id"]
        != "35d502eb-803a-5c40-8dc1-f0825babb831"
        or len(provenance["context_evidence_bindings"]) != 2
        or {
            row["evidence_id"] for row in provenance["context_evidence_bindings"]
        }
        != {
            "35d502eb-803a-5c40-8dc1-f0825babb831",
            V11_IDENTITY_EVIDENCE_ID,
        }
        or any(
            row["bridge_path"] != acceptance["bridge_path"]
            or row["bridge_sha256"] != acceptance["bridge_sha256"]
            for row in provenance["context_evidence_bindings"]
        )
    ):
        raise VerifiedConstructionCoreError("v0.11 provenance contract differs")
    if (
        not isinstance(imagery, dict)
        or set(imagery)
        != {
            "contract_id",
            "review_scope",
            "reviewed_as_of",
            "base_contract",
            "default_outcome",
            "records",
        }
        or imagery["contract_id"]
        != "verified-construction-core-v0.11-imagery-reviews"
        or imagery["reviewed_as_of"]
        != CURRENT_V11_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
        or imagery["base_contract"]
        != {
            "path": (
                "definitions/verified-construction-core-v0.10-imagery-reviews.json"
            ),
            "sha256": V10_IMAGERY_REVIEW_DEFINITION_SHA256,
        }
        or imagery["default_outcome"] != "not_reviewed_for_core_preview"
        or imagery["records"] != []
    ):
        raise VerifiedConstructionCoreError("v0.11 imagery contract differs")
    return {
        "reviewed": reviewed,
        "overlays": overlays,
        "provenance": provenance,
        "imagery": imagery,
        "acceptance": acceptance,
        "bridge": bridge,
        "source": source,
    }


def _v11_build_delta_rows(contracts: Mapping[str, Any]) -> dict[str, Any]:
    bridge = contracts["bridge"]
    source = contracts["source"]
    acceptance = contracts["acceptance"]
    provenance = contracts["provenance"]
    construction = bridge["construction_source"]
    project = construction["project"]
    campus = construction["campus"]
    source_evidence = _v08_source_evidence(source)
    evidence_pool = dict(source_evidence)
    for identity in bridge["identity_bridge_evidence"]:
        projected = _v07_evidence_projection(identity)
        evidence_id = projected["evidence_id"]
        previous = evidence_pool.get(evidence_id)
        if previous is not None and previous != projected:
            raise VerifiedConstructionCoreError(
                "v0.11 public evidence projection differs"
            )
        evidence_pool[evidence_id] = projected
    geometry_evidence = _v07_evidence_projection(bridge["geometry_evidence"])
    if evidence_pool[V11_GEOMETRY_EVIDENCE_ID] != geometry_evidence:
        raise VerifiedConstructionCoreError(
            "v0.11 geometry evidence projection differs"
        )
    evidence_usage: dict[str, dict[str, set[str]]] = defaultdict(
        lambda: {"roles": set(), "project_ids": set()}
    )
    project_id = project["entity_id"]
    status_evidence_id = project["status_evidence_id"]
    capacity_evidence_id = "35d502eb-803a-5c40-8dc1-f0825babb831"
    evidence_usage[status_evidence_id]["roles"].add("physical_status")
    evidence_usage[status_evidence_id]["project_ids"].add(project_id)
    evidence_usage[V11_GEOMETRY_EVIDENCE_ID]["roles"].add("geometry")
    evidence_usage[V11_GEOMETRY_EVIDENCE_ID]["project_ids"].add(project_id)
    observations = project["capacity_estimates_json"]
    if len(observations) != 1:
        raise VerifiedConstructionCoreError("v0.11 capacity inventory differs")
    observation = observations[0]
    _validate_typed_observation(
        observation, POWER_METRICS | ENERGY_METRICS | EFFICIENCY_METRICS
    )
    evidence_usage[capacity_evidence_id]["roles"].add(
        "typed_metric:grid_connection_mw"
    )
    evidence_usage[capacity_evidence_id]["project_ids"].add(project_id)
    for binding in provenance["context_evidence_bindings"]:
        evidence_id = binding["evidence_id"]
        if evidence_id not in evidence_pool:
            raise VerifiedConstructionCoreError(
                "v0.11 context evidence projection differs"
            )
        evidence_usage[evidence_id]["roles"].add(
            f"context:{binding['usage_role']}"
        )
        evidence_usage[evidence_id]["project_ids"].add(project_id)
    if set(evidence_usage) != V11_EVIDENCE_IDS:
        raise VerifiedConstructionCoreError(
            "v0.11 projected evidence allowlist differs"
        )
    entity = bridge["geometry_entity"]
    geometry = entity["geometry"]
    status_date = _calendar_date(project["status_as_of"], "v0.11 status_as_of")
    image_outcome = contracts["imagery"]["default_outcome"]
    site_id = _stable_id("vcc-site", campus["stable_key"])
    power = list(observations)
    project_row = {
        "project_id": project_id,
        "project_stable_key": V11_PROJECT_KEY,
        "site_id": site_id,
        "physical_site_stable_key": V11_CAMPUS_KEY,
        "name": project["name"],
        "country": "Japan",
        "country_iso_a2": "JP",
        "latitude": entity["latitude"],
        "longitude": entity["longitude"],
        "geometry_json": _json_bytes(geometry).decode().strip(),
        "geometry_type": geometry["type"],
        "geometry_source_entity_kind": "address_area",
        "geometry_derivation": acceptance["geometry_derivation"],
        "geometry_method": acceptance["geometry_method"],
        "geometry_scope_class": acceptance["geometry_scope_class"],
        "geometry_authority_class": acceptance["geometry_authority_class"],
        "geometry_use_scope": acceptance["geometry_use_scope"],
        "geometry_precision_scope": acceptance["precision_scope"],
        "horizontal_uncertainty_metres": "",
        "horizontal_uncertainty_unknown_reason": acceptance[
            "horizontal_uncertainty_unknown_reason"
        ],
        "geometry_evidence_id": V11_GEOMETRY_EVIDENCE_ID,
        "last_observed_physical_status": project["status"],
        "status_as_of": project["status_as_of"],
        "status_age_days_at_review": (
            CURRENT_V11_LIFECYCLE_REFERENCE_DATE - status_date
        ).days,
        "status_method": project["status_method"],
        "status_evidence_id": status_evidence_id,
        "verification_posture": _verification_posture(
            acceptance["geometry_authority_class"],
            acceptance["geometry_use_scope"],
        ),
        "independent_imagery_verification": "false",
        "imagery_review_outcome": image_outcome,
        "development_type": "unknown",
        "development_type_unknown_reason": (
            "source evidence does not distinguish greenfield, expansion, or retrofit"
        ),
        "operating_model": "unknown",
        "operating_model_unknown_reason": "not established by selected evidence",
        "operating_model_evidence_id": "",
        "workloads_json": "[]",
        "workload_unknown_reason": "not established by selected evidence",
        "role_claims_json": "[]",
        "power_observations_json": _json_bytes(power).decode().strip(),
        "power_unknown_reason": "",
        "annual_energy_observations_json": "[]",
        "annual_energy_unknown_reason": (
            "no scoped annual-energy inputs; not estimated"
        ),
        "efficiency_observations_json": "[]",
        "efficiency_unknown_reason": "no scoped PUE or WUE observation",
        "owner": "",
        "operator": "",
        "users": "",
        "tenants": "",
        "customers": "",
        "status_source_url": evidence_pool[status_evidence_id]["source_url"],
        "geometry_source_url": geometry_evidence["source_url"],
    }
    site_row = {
        "site_id": site_id,
        "physical_site_stable_key": V11_CAMPUS_KEY,
        "name": campus["name"],
        "country": "Japan",
        "country_iso_a2": "JP",
        "latitude": entity["latitude"],
        "longitude": entity["longitude"],
        "geometry_json": project_row["geometry_json"],
        "geometry_type": "Polygon",
        "geometry_source_entity_kinds_json": _json_bytes(["address_area"])
        .decode()
        .strip(),
        "geometry_derivations_json": _json_bytes(
            [acceptance["geometry_derivation"]]
        )
        .decode()
        .strip(),
        "geometry_methods_json": _json_bytes([acceptance["geometry_method"]])
        .decode()
        .strip(),
        "geometry_scope_classes_json": _json_bytes(
            [acceptance["geometry_scope_class"]]
        )
        .decode()
        .strip(),
        "geometry_authority_classes_json": _json_bytes(
            [acceptance["geometry_authority_class"]]
        )
        .decode()
        .strip(),
        "geometry_use_scopes_json": _json_bytes(
            [acceptance["geometry_use_scope"]]
        )
        .decode()
        .strip(),
        "geometry_precision_scopes_json": _json_bytes(
            [acceptance["precision_scope"]]
        )
        .decode()
        .strip(),
        "horizontal_uncertainty_metres": "",
        "horizontal_uncertainty_unknown_reason": acceptance[
            "horizontal_uncertainty_unknown_reason"
        ],
        "geometry_evidence_ids_json": _json_bytes([V11_GEOMETRY_EVIDENCE_ID])
        .decode()
        .strip(),
        "project_count": 1,
        "project_ids_json": _json_bytes([project_id]).decode().strip(),
        "project_stable_keys_json": _json_bytes([V11_PROJECT_KEY])
        .decode()
        .strip(),
        "statuses_json": _json_bytes([project["status"]]).decode().strip(),
        "oldest_status_as_of": project["status_as_of"],
        "newest_status_as_of": project["status_as_of"],
        "verification_posture": project_row["verification_posture"],
        "independent_imagery_verification": "false",
        "imagery_review_outcomes_json": _json_bytes([image_outcome])
        .decode()
        .strip(),
    }
    evidence_rows = [
        {
            **evidence_pool[evidence_id],
            "roles_json": _json_bytes(sorted(usage["roles"])).decode().strip(),
            "project_ids_json": _json_bytes(sorted(usage["project_ids"]))
            .decode()
            .strip(),
        }
        for evidence_id, usage in sorted(evidence_usage.items())
    ]
    return {
        "projects": [project_row],
        "sites": [site_row],
        "evidence": evidence_rows,
    }


def _v11_portable_source_inputs(
    contracts: Mapping[str, Any],
) -> list[dict[str, Any]]:
    base_manifest = _load_json(LEGACY_PREVIEW_V10_DIR / "manifest.json")
    rows = list(base_manifest["portable_source_inputs"])
    acceptance = contracts["acceptance"]
    rows.extend(
        [
            {
                "path": acceptance["portable_input_binding"]["path"],
                "bytes": acceptance["portable_input_binding"]["bytes"],
                "sha256": acceptance["portable_input_binding"]["sha256"],
                "parent_manifests": [],
            },
            {
                "path": acceptance["bridge_path"],
                "bytes": acceptance["bridge_bytes"],
                "sha256": acceptance["bridge_sha256"],
                "parent_manifests": [],
            },
        ]
    )
    rows.sort(key=lambda row: row["path"])
    if len(rows) != 74 or len({row["path"] for row in rows}) != 74:
        raise VerifiedConstructionCoreError("v0.11 portable input count differs")
    for row in rows:
        path = _repository_input(
            row["path"], row["sha256"], "v0.11 portable input"
        )
        if path.stat().st_size != row["bytes"]:
            raise VerifiedConstructionCoreError(
                "v0.11 portable input byte count differs"
            )
    return rows


def _v11_map_html(
    geojson: Mapping[str, Any],
    sites: Sequence[Mapping[str, Any]],
    evidence: Sequence[Mapping[str, Any]],
) -> bytes:
    document = _v10_map_html(geojson, evidence).decode("utf-8")
    if "Verified Construction Core v0.10 preview" not in document:
        raise VerifiedConstructionCoreError("v0.11 map title seam differs")
    document = document.replace(
        "Verified Construction Core v0.10 preview",
        "Verified Construction Core v0.11 preview",
    )
    marker = '. <a href="ATTRIBUTION.txt">Full attribution and source terms</a>.'
    notice = (
        ' · <a href="https://www.e-stat.go.jp/terms-of-use">'
        "Portal Site of Official Statistics of Japan (e-Stat); Statistics "
        "Bureau of Japan; selected Shapefile feature converted to GeoJSON by "
        "Data Center Atlas</a> — e-Stat terms"
    )
    if marker not in document:
        raise VerifiedConstructionCoreError("v0.11 map attribution seam differs")
    document = document.replace(marker, f"{notice}{marker}", 1)
    display_anchors: dict[str, list[float]] = {}
    for site in sites:
        site_id = str(site["site_id"])
        longitude = float(site["longitude"])
        latitude = float(site["latitude"])
        if (
            site_id in display_anchors
            or not math.isfinite(longitude)
            or not math.isfinite(latitude)
            or not -180 <= longitude <= 180
            or not -90 <= latitude <= 90
        ):
            raise VerifiedConstructionCoreError("v0.11 map display anchor differs")
        display_anchors[site_id] = [longitude, latitude]
    feature_ids = {
        feature["properties"]["site_id"] for feature in geojson["features"]
    }
    if set(display_anchors) != feature_ids:
        raise VerifiedConstructionCoreError("v0.11 map display anchor differs")
    anchors_json = json.dumps(
        display_anchors,
        ensure_ascii=False,
        sort_keys=True,
        separators=(",", ":"),
    ).replace("<", "\\u003c")
    svg_declaration = "const svg=document.getElementById('map');"
    if document.count(svg_declaration) != 1:
        raise VerifiedConstructionCoreError("v0.11 map script seam differs")
    document = document.replace(
        svg_declaration,
        f"const displayAnchors={anchors_json};{svg_declaration}",
        1,
    )
    document, point_function_count = re.subn(
        r"function point\(g\)\{[^\n]*\}\n",
        "",
        document,
        count=1,
    )
    point_call = "const [lon,lat]=point(f.geometry);"
    if point_function_count != 1 or document.count(point_call) != 1:
        raise VerifiedConstructionCoreError("v0.11 map point seam differs")
    document = document.replace(
        point_call,
        "const [lon,lat]=displayAnchors[f.properties.site_id];",
        1,
    )
    return document.encode("utf-8")


def _v11_schema() -> dict[str, Any]:
    schema = _v10_schema()
    schema["format"] = "datacenter-atlas-verified-construction-core-schema-v11"
    schema["preview_id"] = CURRENT_V11_PREVIEW_ID
    fields = schema["tables"]["projects.csv"]["fields"]
    geometry_source = next(
        row for row in fields if row["name"] == "geometry_source_entity_kind"
    )
    if geometry_source["allowed_values"] != ["project", "campus"]:
        raise VerifiedConstructionCoreError("v0.11 schema inheritance differs")
    geometry_source["allowed_values"] = [
        "project",
        "campus",
        "facility",
        "building",
        "address_area",
    ]
    geometry_derivation = next(
        row for row in fields if row["name"] == "geometry_derivation"
    )
    if "official_coordinate_transform" in geometry_derivation["allowed_values"]:
        raise VerifiedConstructionCoreError("v0.11 schema inheritance differs")
    geometry_derivation["allowed_values"].append("official_coordinate_transform")
    if schema["map"]["derived_from"] != ["sites.geojson", "evidence.csv"]:
        raise VerifiedConstructionCoreError("v0.11 map schema inheritance differs")
    schema["map"]["derived_from"] = [
        "sites.geojson",
        "sites.csv",
        "evidence.csv",
    ]
    schema["v0_11_address_area_locator"] = {
        "geometry_source_entity_kind": "address_area",
        "published_geometry": "full Polygon only",
        "coordinate_semantics": (
            "CSV latitude/longitude are the source-published statistical-boundary "
            "shape center and a display anchor for the full Polygon. They must never "
            "be used standalone as campus, project, parcel, building, Phase 3, "
            "construction, centroid, or survey geometry."
        ),
        "area_semantics": (
            "55,117.019 square metres is only the e-Stat selected statistical "
            "feature's reported area, never IIJ site area."
        ),
    }
    schema["v0_11_temporal_scope"] = {
        "reviewed_at_semantics": (
            "Legacy alias for cohort_lifecycle_reference_date, retained for "
            "preview compatibility; it is not the geometry-identity review date."
        ),
        "cohort_lifecycle_reference_date": (
            CURRENT_V11_LIFECYCLE_REFERENCE_DATE.isoformat()
        ),
        "geometry_identity_reviewed_at": (
            CURRENT_V11_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
        ),
    }
    return schema


def _v11_readme(report: Mapping[str, Any], legal_notice: str) -> bytes:
    return f"""# Verified Construction Core v0.11 preview

This tracked, non-final preview contains **{report['selected_physical_site_count']} physical sites**
and **{report['selected_project_count']} linked projects** in {len(report['country_counts'])}
countries. Every project retains a dated authoritative physical-status observation and an exact
reviewed geometry scope. Five project rows carry official boundaries; the remaining 46 are
explicit project or campus locators, never construction footprints by implication.

The one-row v0.11 delta adds the IIJ Shiroi DCC Phase 3 Server Building. IIJ evidence remains the
sole lifecycle and planned-capacity authority: under construction as of 2026-06-25 and exactly one
planned 10 MW grid_connection_mw observation. The optional 25 MW expandability ceiling is not
published as installed, contracted, current, or additive capacity. No operating model, workload,
owner, operator, user, tenant, or customer is inferred; the source developer string is explicitly
excluded because developer is outside the public role schema.

Chiba Prefecture binds the exact campus name 白井データセンターキャンパス to
白井市桜台５－１－１. The 桜台５ address prefix corresponds textually to the e-Stat feature name
桜台五丁目; no address point or point-in-polygon containment is asserted. The full official 2020
census small-area Polygon is published only as a broad named-area campus locator. It is not a
campus, parcel, building, Phase 3, construction, security, cadastral, or administrative boundary.
The CSV latitude/longitude pair is e-Stat's source-published statistical-boundary shape center and
only a display anchor for that full Polygon; it must never be used standalone as site or project
geometry. The reported 55,117.019 square metres is only the statistical feature's area, never IIJ
site area.

Portal Site of Official Statistics of Japan (e-Stat); Statistics Bureau of Japan; selected Shapefile feature converted to GeoJSON by Data Center Atlas.

The fixed cohort lifecycle/status cutoff is 2026-08-20. The e-Stat and Chiba evidence was captured,
and its geometry/identity use accepted, on 2026-08-23; it does not update cohort lifecycle state.
For preview compatibility, `reviewed_at` remains an alias for the lifecycle cutoff; the manifest and
selection report separately expose `geometry_identity_reviewed_at`.

The artifact is deterministic and rebuildable in a clean clone from the 74 manifest-bound portable
inputs plus the byte-frozen v0.10 artifact. Ignored v97 and v14 payloads are optional complete
hydrated cross-checks, not build dependencies. The v0.11 row inherits the not-reviewed imagery
outcome and remains independent_imagery_verification=false.

## City of Chicago terms inherited from v0.10

{legal_notice}

This is not the final 100-site Verified Construction Core v1. Its final-release gates remain
unsatisfied and publishable_as_final is false.
""".encode("utf-8")


def _v11_attribution(
    evidence: Sequence[Mapping[str, Any]], legal_notice: str
) -> bytes:
    rows = {
        (row["publisher"], row["license"], row["source_url"])
        for row in evidence
    }
    processing = (
        "Portal Site of Official Statistics of Japan (e-Stat); Statistics "
        "Bureau of Japan; selected Shapefile feature converted to GeoJSON by "
        "Data Center Atlas"
    )
    lines = [
        "Data Center Atlas Verified Construction Core v0.11 preview",
        "",
        "Compact derived facts only; third-party terms remain controlling.",
        "",
        *(
            f"- {publisher} | {license_name} | {url}"
            for publisher, license_name, url in sorted(rows)
        ),
        "",
        f"Processed statistical boundary notice: {processing}.",
        "Terms: https://www.e-stat.go.jp/terms-of-use",
        "GIS terms: https://www.e-stat.go.jp/gis-terms",
        "",
        "Map-provider terms:",
        *(
            f"- {label} | {license_name} | {url}"
            for label, license_name, url in _v07_map_attribution_notices(evidence)
        ),
        "",
        legal_notice,
    ]
    payload = ("\n".join(lines) + "\n").encode("utf-8")
    if processing.encode("utf-8") not in payload:
        raise VerifiedConstructionCoreError(
            "v0.11 processed geometry attribution differs"
        )
    return payload


def _v11_payloads() -> tuple[dict[str, bytes], dict[str, Any], list[dict[str, Any]]]:
    validate_frozen_v10(LEGACY_PREVIEW_V10_DIR)
    contracts = _v11_contracts()
    base_projects = _load_csv(
        LEGACY_PREVIEW_V10_DIR / "projects.csv", PROJECT_FIELDS
    )
    base_sites = _load_csv(LEGACY_PREVIEW_V10_DIR / "sites.csv", SITE_FIELDS)
    base_evidence = _load_csv(
        LEGACY_PREVIEW_V10_DIR / "evidence.csv", EVIDENCE_FIELDS
    )
    base_report = _load_json(
        LEGACY_PREVIEW_V10_DIR / "selection-report.json"
    )
    delta = _v11_build_delta_rows(contracts)
    projects = sorted(
        [*base_projects, *delta["projects"]],
        key=lambda row: row["project_stable_key"],
    )
    sites = sorted(
        [*base_sites, *delta["sites"]],
        key=lambda row: row["physical_site_stable_key"],
    )
    evidence = sorted(
        [*base_evidence, *delta["evidence"]],
        key=lambda row: row["evidence_id"],
    )
    if (
        len(projects) != 51
        or len(sites) != 48
        or len(evidence) != 120
        or len({row["project_id"] for row in projects}) != 51
        or len({row["site_id"] for row in sites}) != 48
        or len({row["evidence_id"] for row in evidence}) != 120
    ):
        raise VerifiedConstructionCoreError(
            "v0.11 cohort count or uniqueness differs"
        )
    project_by_id = {row["project_id"]: row for row in projects}
    site_by_id = {row["site_id"]: row for row in sites}
    evidence_by_id = {row["evidence_id"]: row for row in evidence}
    if (
        any(project_by_id[row["project_id"]] != row for row in base_projects)
        or any(site_by_id[row["site_id"]] != row for row in base_sites)
        or any(
            evidence_by_id[row["evidence_id"]] != row for row in base_evidence
        )
    ):
        raise VerifiedConstructionCoreError("v0.11 inherited row changed")
    gates = _final_release_gates(sites, projects)
    gates["clean_clone_rebuild"] = {
        "passed": True,
        "reason": (
            "all current-delta inputs and the frozen base artifact are tracked "
            "and hash-bound"
        ),
    }
    country_counts = dict(
        sorted(Counter(row["country"] for row in sites).items())
    )
    provenance_decisions = {
        key: [
            *base_report["provenance_decisions"][key],
            *contracts["provenance"][key],
        ]
        for key in (
            "workload_scope_bindings",
            "role_bindings",
            "excluded_source_roles",
            "context_evidence_bindings",
        )
    }
    report = {
        **base_report,
        "format": "datacenter-atlas-verified-construction-core-selection-v11",
        "publishable_as_final": all(
            gate.get("passed", False) for gate in gates.values()
        ),
        "reviewed_at": CURRENT_V11_LIFECYCLE_REFERENCE_DATE.isoformat(),
        "cohort_lifecycle_reference_date": (
            CURRENT_V11_LIFECYCLE_REFERENCE_DATE.isoformat()
        ),
        "geometry_identity_reviewed_at": (
            CURRENT_V11_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
        ),
        "selected_project_count": 51,
        "selected_physical_site_count": 48,
        "official_boundary_project_count": 5,
        "reviewed_site_locator_project_count": 46,
        "non_selected_source_row_count": 480,
        "selection_first_failure_counts": V11_SELECTION_FIRST_FAILURE_COUNTS,
        "country_counts": country_counts,
        "final_release_gates": gates,
        "imagery_review_provenance": list(
            base_report["imagery_review_provenance"]
        ),
        "provenance_decisions": provenance_decisions,
        "reviewed_overlay_queue": [
            *base_report["reviewed_overlay_queue"],
            *contracts["overlays"]["overlays"],
        ],
    }
    delta_project = delta["projects"][0]
    if (
        len(country_counts) != 26
        or country_counts.get("Japan") != 1
        or sum(row["country_iso_a2"] != "US" for row in sites) != 40
        or sum(_is_official_boundary(row) for row in projects) != 5
        or sum(_is_reviewed_locator(row) for row in projects) != 46
        or sum(V11_SELECTION_FIRST_FAILURE_COUNTS.values()) != 531
        or gates["imagery_outcomes_complete"]
        != {"actual": 10, "required": 51, "passed": False}
        or len(provenance_decisions["context_evidence_bindings"]) != 9
        or len(provenance_decisions["excluded_source_roles"]) != 12
        or delta_project["geometry_source_entity_kind"] != "address_area"
        or delta_project["geometry_type"] != "Polygon"
        or delta_project["workloads_json"] != "[]"
        or delta_project["role_claims_json"] != "[]"
        or delta_project["operating_model"] != "unknown"
        or any(
            delta_project[field]
            for field in ("owner", "operator", "users", "tenants", "customers")
        )
    ):
        raise VerifiedConstructionCoreError("v0.11 expected accounting differs")
    power = _parse_json_field(
        delta_project["power_observations_json"], "v0.11 power"
    )
    if (
        len(power) != 1
        or power[0]["metric"] != "grid_connection_mw"
        or power[0]["stage"] != "planned"
        or power[0]["base"] != 10.0
        or power[0]["evidence_id"]
        != "35d502eb-803a-5c40-8dc1-f0825babb831"
    ):
        raise VerifiedConstructionCoreError("v0.11 metric scope differs")
    geojson = _geojson(sites)
    geojson["name"] = "Data Center Atlas Verified Construction Core v0.11 preview"
    delta_feature = next(
        row
        for row in geojson["features"]
        if row["properties"]["physical_site_stable_key"] == V11_CAMPUS_KEY
    )
    if (
        delta_feature["geometry"]
        != contracts["bridge"]["geometry_entity"]["geometry"]
        or delta_feature["geometry"]["type"] != "Polygon"
    ):
        raise VerifiedConstructionCoreError(
            "v0.11 full address-area Polygon publication differs"
        )
    legal_notice = _v10_city_legal_notice_from_base()
    payloads = {
        "ATTRIBUTION.txt": _v11_attribution(evidence, legal_notice),
        "README.md": _v11_readme(report, legal_notice),
        "evidence.csv": _csv_bytes(evidence, EVIDENCE_FIELDS),
        "map.html": _v11_map_html(geojson, sites, evidence),
        "projects.csv": _csv_bytes(projects, PROJECT_FIELDS),
        "schema.json": _json_bytes(_v11_schema()),
        "selection-report.json": _json_bytes(report),
        "sites.csv": _csv_bytes(sites, SITE_FIELDS),
        "sites.geojson": _json_bytes(geojson),
    }
    portable = _v11_portable_source_inputs(contracts)
    return payloads, report, portable


def _v11_manifest(
    payloads: Mapping[str, bytes],
    report: Mapping[str, Any],
    portable: Sequence[Mapping[str, Any]],
) -> dict[str, Any]:
    return {
        "format": "datacenter-atlas-verified-construction-core-preview-v11",
        "preview_id": CURRENT_V11_PREVIEW_ID,
        "release_status": "preview",
        "publishable_as_final": report["publishable_as_final"],
        "base_preview_id": CURRENT_V10_PREVIEW_ID,
        "base_preview_manifest_sha256": LEGACY_PREVIEW_V10_MANIFEST_SHA256,
        "base_preview_commit": LEGACY_PREVIEW_V10_COMMIT,
        "source_release_id": SOURCE_RELEASE_ID,
        "source_release_manifest_path": (
            "releases/2026-07-22-open-seed-v97/manifest.json"
        ),
        "source_release_manifest_sha256": (
            "0a6f41f4239944df27f2ce70e81a089b91cec401f154bbae28412b27a4d00fdd"
        ),
        "definition_paths": {
            "imagery_reviews": (
                "definitions/verified-construction-core-v0.11-imagery-reviews.json"
            ),
            "provenance": (
                "definitions/verified-construction-core-v0.11-provenance.json"
            ),
            "reviewed_overlays": (
                "definitions/verified-construction-core-reviewed-overlays-v9.json"
            ),
            "reviewed_sites": (
                "definitions/verified-construction-core-v0.11-reviewed-sites.json"
            ),
        },
        "review_definition_sha256": V11_REVIEW_DEFINITION_SHA256,
        "imagery_review_definition_sha256": (
            V11_IMAGERY_REVIEW_DEFINITION_SHA256
        ),
        "provenance_definition_sha256": V11_PROVENANCE_DEFINITION_SHA256,
        "overlay_definition_sha256": V11_OVERLAY_DEFINITION_SHA256,
        "portable_source_inputs": list(portable),
        "reviewed_at": CURRENT_V11_LIFECYCLE_REFERENCE_DATE.isoformat(),
        "cohort_lifecycle_reference_date": (
            CURRENT_V11_LIFECYCLE_REFERENCE_DATE.isoformat()
        ),
        "geometry_identity_reviewed_at": (
            CURRENT_V11_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
        ),
        "counts": {
            "physical_sites": 48,
            "projects": 51,
            "evidence": 120,
            "countries": 26,
            "non_us_sites": 40,
            "official_boundary_projects": 5,
            "reviewed_site_locator_projects": 46,
        },
        "files": {
            name: {"bytes": len(payload), "sha256": _sha256_bytes(payload)}
            for name, payload in sorted(payloads.items())
        },
    }


def build_preview(output_dir: Path = CURRENT_V11_PREVIEW_DIR) -> dict[str, Any]:
    """Build v0.11 from byte-frozen v0.10 and tracked hash-bound inputs."""
    output_dir = Path(output_dir)
    if output_dir.exists():
        raise VerifiedConstructionCoreError(f"refusing to overwrite {output_dir}")
    payloads, report, portable = _v11_payloads()
    manifest = _v11_manifest(payloads, report, portable)
    manifest_bytes = _json_bytes(manifest)
    complete = {
        **payloads,
        "manifest.json": manifest_bytes,
        "manifest.sha256": (
            f"{_sha256_bytes(manifest_bytes)}  manifest.json\n".encode()
        ),
    }
    output_dir.parent.mkdir(parents=True, exist_ok=True)
    stage = Path(
        tempfile.mkdtemp(prefix=f".{output_dir.name}.", dir=output_dir.parent)
    )
    try:
        for name, payload in complete.items():
            with (stage / name).open("xb") as handle:
                handle.write(payload)
        os.replace(stage, output_dir)
    except Exception:
        shutil.rmtree(stage, ignore_errors=True)
        raise
    validate_preview(output_dir)
    return manifest


def _validate_v11_preview(path: Path) -> dict[str, Any]:
    path = Path(path)
    if path.is_symlink() or not path.is_dir():
        raise VerifiedConstructionCoreError("v0.11 preview directory differs")
    manifest_path = path / "manifest.json"
    checksum_path = path / "manifest.sha256"
    if (
        manifest_path.is_symlink()
        or not manifest_path.is_file()
        or checksum_path.is_symlink()
        or not checksum_path.is_file()
    ):
        raise VerifiedConstructionCoreError("v0.11 manifest trust root differs")
    manifest_bytes = manifest_path.read_bytes()
    if checksum_path.read_bytes() != (
        f"{_sha256_bytes(manifest_bytes)}  manifest.json\n".encode()
    ):
        raise VerifiedConstructionCoreError("v0.11 manifest checksum differs")
    manifest = _load_json(manifest_path)
    files = manifest.get("files") if isinstance(manifest, dict) else None
    if (
        not isinstance(manifest, dict)
        or manifest.get("format")
        != "datacenter-atlas-verified-construction-core-preview-v11"
        or manifest.get("preview_id") != CURRENT_V11_PREVIEW_ID
        or manifest.get("release_status") != "preview"
        or type(manifest.get("publishable_as_final")) is not bool
        or manifest["publishable_as_final"] is not False
        or not isinstance(files, dict)
    ):
        raise VerifiedConstructionCoreError("v0.11 manifest files differ")
    expected_inventory = set(files) | {"manifest.json", "manifest.sha256"}
    if {item.name for item in path.iterdir()} != expected_inventory:
        raise VerifiedConstructionCoreError("v0.11 preview inventory differs")
    for name, metadata in files.items():
        member = path / name
        if (
            Path(name).name != name
            or member.is_symlink()
            or not member.is_file()
            or type(metadata.get("bytes")) is not int
            or member.stat().st_size != metadata["bytes"]
            or _sha256_file(member) != metadata.get("sha256")
        ):
            raise VerifiedConstructionCoreError(
                f"v0.11 member differs: {name}"
            )
    payloads, report, portable = _v11_payloads()
    if manifest != _v11_manifest(payloads, report, portable):
        raise VerifiedConstructionCoreError("v0.11 manifest semantics differ")
    for name, expected in payloads.items():
        if (path / name).read_bytes() != expected:
            raise VerifiedConstructionCoreError(
                f"v0.11 generated member differs: {name}"
            )
    processing = (
        "Portal Site of Official Statistics of Japan (e-Stat); Statistics "
        "Bureau of Japan; selected Shapefile feature converted to GeoJSON by "
        "Data Center Atlas"
    ).encode()
    for name in ("ATTRIBUTION.txt", "README.md", "map.html"):
        payload = (path / name).read_bytes()
        if processing not in payload:
            raise VerifiedConstructionCoreError(
                f"v0.11 processed e-Stat notice missing from {name}"
            )
    attribution = (path / "ATTRIBUTION.txt").read_bytes()
    if (
        b"https://www.e-stat.go.jp/terms-of-use" not in attribution
        or b"https://www.e-stat.go.jp/gis-terms" not in attribution
        or b"https://www.e-stat.go.jp/terms-of-use"
        not in (path / "map.html").read_bytes()
    ):
        raise VerifiedConstructionCoreError(
            "v0.11 e-Stat terms attribution differs"
        )
    schema = _load_json(path / "schema.json")
    geometry_source = next(
        row
        for row in schema["tables"]["projects.csv"]["fields"]
        if row["name"] == "geometry_source_entity_kind"
    )
    if geometry_source["allowed_values"] != [
        "project",
        "campus",
        "facility",
        "building",
        "address_area",
    ]:
        raise VerifiedConstructionCoreError(
            "v0.11 address-area schema seam differs"
        )
    geometry_derivation = next(
        row
        for row in schema["tables"]["projects.csv"]["fields"]
        if row["name"] == "geometry_derivation"
    )
    if "official_coordinate_transform" not in geometry_derivation["allowed_values"]:
        raise VerifiedConstructionCoreError(
            "v0.11 geometry-derivation schema seam differs"
        )
    return manifest


def validate_preview(path: Path = CURRENT_V11_PREVIEW_DIR) -> dict[str, Any]:
    """Validate current v0.11 or dispatch v0.1-v0.10 through the frozen seams."""
    path = Path(path)
    manifest_path = path / "manifest.json"
    if (
        path.is_symlink()
        or not path.is_dir()
        or manifest_path.is_symlink()
        or not manifest_path.is_file()
    ):
        raise VerifiedConstructionCoreError("preview manifest trust root differs")
    manifest = _load_json(manifest_path)
    preview_id = manifest.get("preview_id") if isinstance(manifest, dict) else None
    if preview_id == CURRENT_V11_PREVIEW_ID:
        return _validate_v11_preview(path)
    return _validate_v10_preview_dispatch(path)


__all__ = [
    *__all__,
    "CURRENT_V11_DEFINITION_PATHS",
    "CURRENT_V11_PREVIEW_DIR",
    "CURRENT_V11_PREVIEW_ID",
    "CURRENT_V11_GEOMETRY_IDENTITY_REVIEW_DATE",
    "CURRENT_V11_LIFECYCLE_REFERENCE_DATE",
    "CURRENT_V11_REVIEW_DATE",
    "LEGACY_PREVIEW_V10_COMMIT",
    "LEGACY_PREVIEW_V10_DIR",
    "LEGACY_PREVIEW_V10_MANIFEST_SHA256",
    "load_current_v11_profile",
    "validate_frozen_v10",
]


_build_v11_preview = build_preview
_validate_v11_preview_dispatch = validate_preview

LEGACY_PREVIEW_V11_DIR = CURRENT_V11_PREVIEW_DIR
LEGACY_PREVIEW_V11_MANIFEST_SHA256 = (
    "1a17dd9da2727ef80fbc86da6b49259b5031a326b0bab0df2f9bea77aa3b31bc"
)
LEGACY_PREVIEW_V11_COMMIT = "177aeee0a3c8b13df0a6a4e3fce6073c4ee11193"
CURRENT_V12_PREVIEW_ID = "2026-08-20-preview-v0.12"
CURRENT_V12_PREVIEW_DIR = ROOT / "verified_construction_core" / CURRENT_V12_PREVIEW_ID
CURRENT_V12_LIFECYCLE_REFERENCE_DATE = date(2026, 8, 20)
CURRENT_V12_GEOMETRY_IDENTITY_REVIEW_DATE = date(2026, 8, 23)
CURRENT_V12_REVIEW_DATE = CURRENT_V12_LIFECYCLE_REFERENCE_DATE
V12_BATCH_CONTRACT = (
    ROOT
    / "definitions"
    / "verified-construction-core-v0.12-address-locator-batch.json"
)
V12_BATCH_CONTRACT_SHA256 = (
    "01e3244760aac1450d5dd6cd1e7a486241bcad9aab2391e0256ce94d3982f37c"
)
V12_GEOMETRY_CAPTURE = (
    ROOT
    / "sources"
    / "verified-construction-core-v0.12-census-address-locators.json"
)
V12_GEOMETRY_CAPTURE_SHA256 = (
    "db625d4738dc9a0eed46bd54216090509b1882f16c8b57578f43ac0fa76b0905"
)
V12_GEOMETRY_EVIDENCE_ID = "6600dddb-f2d8-5458-8e71-b43f77cc5e01"
V12_PROJECT_KEYS = frozenset(
    {
        "curated:coreweave-chirisa-lancaster-greenfield-road-campus:phase-1-adaptive-reuse",
        "curated:lancium-clean-campus-abilene-ai-data-center:remaining-six-building-expansion",
        "curated:ntt-dallas-data-center-campus:tx4",
        "curated:qts-york-county-south-carolina-data-center-campus:initial-phase-building-1",
        "curated:trg-spring-cypress-campus:hou2",
    }
)
V12_SELECTION_FIRST_FAILURE_COUNTS = {
    "entity_kind_not_project": 49,
    "not_in_reviewed_site_geometry_allowlist": 87,
    "selected": 56,
    "status_not_physical": 12,
    "status_outside_90_day_window": 327,
}


def validate_frozen_v11(
    path: Path = LEGACY_PREVIEW_V11_DIR,
) -> dict[str, Any]:
    """Validate byte-frozen v0.11 without regenerating it."""
    return _validate_frozen_preview_inventory(
        path,
        preview_id=CURRENT_V11_PREVIEW_ID,
        manifest_sha256=LEGACY_PREVIEW_V11_MANIFEST_SHA256,
        version_label="v0.11",
    )


def _v12_hydrated_crosscheck_available(
    contract: Mapping[str, Any],
) -> bool:
    bindings = contract.get("hydrated_crosscheck_inputs")
    if not isinstance(bindings, list) or len(bindings) != 5:
        raise VerifiedConstructionCoreError(
            "v0.12 hydrated cross-check contract differs"
        )
    paths = [ROOT / str(binding.get("path", "")) for binding in bindings]
    present = [path.is_file() for path in paths]
    if any(present) and not all(present):
        raise VerifiedConstructionCoreError(
            "v0.12 hydrated cross-check inputs are only partially present"
        )
    if not all(present):
        return False
    for binding, path in zip(bindings, paths, strict=True):
        if (
            path.is_symlink()
            or path.stat().st_size != binding.get("bytes")
            or _sha256_file(path) != binding.get("sha256")
        ):
            raise VerifiedConstructionCoreError(
                "v0.12 hydrated cross-check input differs"
            )
    return True


def _v12_source_evidence(
    source: Mapping[str, Any], key: str, evidence_id: str
) -> dict[str, Any]:
    matches = [
        row
        for row in source.get("evidence", [])
        if isinstance(row, dict) and row.get("key") == key
    ]
    if len(matches) != 1:
        raise VerifiedConstructionCoreError(
            f"v0.12 source evidence differs: {key}"
        )
    row = matches[0]
    expected_id = atlas_stable_id(
        "evidence", "curated-official", key, row.get("content_hash")
    )
    if evidence_id != expected_id:
        raise VerifiedConstructionCoreError(
            f"v0.12 source evidence identifier differs: {key}"
        )
    return _v07_evidence_projection({**row, "evidence_id": evidence_id})


def _v12_validate_hydrated_crosscheck(
    contract: Mapping[str, Any],
    acceptances: Sequence[Mapping[str, Any]],
    sources: Mapping[str, Mapping[str, Any]],
) -> None:
    if not _v12_hydrated_crosscheck_available(contract):
        return
    pipeline = {
        row["stable_key"]: row
        for row in _load_csv(SOURCE_RELEASE / "construction_pipeline.csv")
    }
    entities = {
        row["stable_key"]: row
        for row in _load_csv(SOURCE_RELEASE / "entities.csv")
    }
    evidence = {
        row["evidence_id"]: row
        for row in _load_csv(SOURCE_RELEASE / "evidence.csv")
    }
    members = _load_csv(
        ROOT
        / "exact_identity_decisions"
        / "2026-07-22-public-open-v14"
        / "component-members.csv"
    )
    relationships = _load_csv(
        ROOT
        / "exact_identity_decisions"
        / "2026-07-22-public-open-v14"
        / "relationships.csv"
    )
    members_by_component: dict[str, list[dict[str, str]]] = defaultdict(list)
    for member in members:
        members_by_component[member["component_id"]].append(member)
    for acceptance in acceptances:
        project_key = acceptance["project_stable_key"]
        campus_key = acceptance["parent_campus_stable_key"]
        source = sources[project_key]
        status = acceptance["status"]
        project = pipeline.get(project_key)
        campus = entities.get(campus_key)
        if (
            project is None
            or campus is None
            or project["entity_id"] != acceptance["project_entity_id"]
            or campus["entity_id"] != acceptance["parent_campus_entity_id"]
            or project["status"] != status["value"]
            or project["status_as_of"] != status["as_of_date"]
            or project["status_method"] != status["method"]
            or project["status_evidence_id"] != status["evidence_id"]
        ):
            raise VerifiedConstructionCoreError(
                f"v0.12 hydrated source projection differs: {project_key}"
            )
        for evidence_binding in (
            acceptance["location_evidence"],
            {
                "key": status["evidence_key"],
                "evidence_id": status["evidence_id"],
            },
        ):
            projected = _v12_source_evidence(
                source,
                evidence_binding["key"],
                evidence_binding["evidence_id"],
            )
            hydrated = evidence.get(evidence_binding["evidence_id"])
            if hydrated is None or any(
                hydrated[field] != projected[field]
                for field in projected
            ):
                raise VerifiedConstructionCoreError(
                    f"v0.12 hydrated evidence projection differs: {project_key}"
                )
        project_member = next(
            (
                row
                for row in members
                if row["release_id"] == SOURCE_RELEASE_ID
                and row["stable_key"] == project_key
                and row["entity_id"] == acceptance["project_entity_id"]
            ),
            None,
        )
        relationship = acceptance["relationship"]
        topology = next(
            (
                row
                for row in relationships
                if row["relationship_id"] == relationship["relationship_id"]
                and project_member is not None
                and row["subject_component_id"]
                == project_member["component_id"]
            ),
            None,
        )
        target_members = (
            []
            if topology is None
            else members_by_component[topology["object_component_id"]]
        )
        if (
            topology is None
            or topology["relationship_type"] != "project_targets"
            or topology["decision_basis"] != "explicit_parent"
            or not any(
                row["release_id"] == SOURCE_RELEASE_ID
                and row["stable_key"] == campus_key
                and row["entity_id"] == acceptance["parent_campus_entity_id"]
                for row in target_members
            )
        ):
            raise VerifiedConstructionCoreError(
                f"v0.12 hydrated topology differs: {project_key}"
            )


def _v12_contracts() -> dict[str, Any]:
    if (
        V12_BATCH_CONTRACT.is_symlink()
        or not V12_BATCH_CONTRACT.is_file()
        or _sha256_file(V12_BATCH_CONTRACT) != V12_BATCH_CONTRACT_SHA256
        or V12_GEOMETRY_CAPTURE.is_symlink()
        or not V12_GEOMETRY_CAPTURE.is_file()
        or _sha256_file(V12_GEOMETRY_CAPTURE) != V12_GEOMETRY_CAPTURE_SHA256
    ):
        raise VerifiedConstructionCoreError("v0.12 contract hash differs")
    contract = _load_json(V12_BATCH_CONTRACT)
    capture = _load_json(V12_GEOMETRY_CAPTURE)
    if (
        contract.get("contract_id")
        != "verified-construction-core-v0.12-address-locator-batch"
        or contract.get("reviewed_as_of")
        != CURRENT_V12_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
        or contract.get("cohort_lifecycle_reference_date")
        != CURRENT_V12_LIFECYCLE_REFERENCE_DATE.isoformat()
        or contract.get("base_preview", {}).get("manifest_sha256")
        != LEGACY_PREVIEW_V11_MANIFEST_SHA256
        or contract.get("geometry_capture", {}).get("sha256")
        != V12_GEOMETRY_CAPTURE_SHA256
        or contract.get("geometry_capture", {}).get("bytes")
        != V12_GEOMETRY_CAPTURE.stat().st_size
        or contract.get("geometry_capture", {}).get("evidence_id")
        != V12_GEOMETRY_EVIDENCE_ID
        or capture.get("benchmark") != "Public_AR_Current"
        or capture.get("match_status") != "Match"
        or capture.get("captured_on")
        != CURRENT_V12_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
    ):
        raise VerifiedConstructionCoreError("v0.12 batch contract differs")
    semantics = contract.get("geometry_semantics")
    if (
        not isinstance(semantics, dict)
        or semantics.get("geometry_source_entity_kind") != "campus"
        or semantics.get("geometry_derivation") != "official_address_geocode"
        or semantics.get("geometry_authority_class") != "official_source"
        or semantics.get("geometry_use_scope") != "campus_locator"
        or any(
            type(semantics.get(field)) is not bool
            or semantics[field] is not False
            for field in (
                "official_boundary",
                "parcel_or_building_geometry",
                "project_footprint",
                "campus_boundary",
                "imagery_verification",
            )
        )
    ):
        raise VerifiedConstructionCoreError("v0.12 geometry semantics differ")
    acceptances = contract.get("acceptances")
    results = capture.get("results")
    if (
        not isinstance(acceptances, list)
        or len(acceptances) != 5
        or {row.get("project_stable_key") for row in acceptances}
        != V12_PROJECT_KEYS
        or not isinstance(results, list)
        or len(results) != 5
    ):
        raise VerifiedConstructionCoreError("v0.12 batch inventory differs")
    results_by_id = {row.get("locator_id"): row for row in results}
    if len(results_by_id) != 5:
        raise VerifiedConstructionCoreError("v0.12 locator identities differ")
    sources: dict[str, Mapping[str, Any]] = {}
    seen_campuses: set[str] = set()
    for acceptance in acceptances:
        project_key = acceptance["project_stable_key"]
        campus_key = acceptance["parent_campus_stable_key"]
        source_binding = acceptance["source_input"]
        source_path = _repository_input(
            source_binding["path"],
            source_binding["sha256"],
            "v0.12 curated source",
        )
        if source_path.stat().st_size != source_binding["bytes"]:
            raise VerifiedConstructionCoreError(
                f"v0.12 source bytes differ: {project_key}"
            )
        source = _load_json(source_path)
        project = source.get("project")
        campus = source.get("campus")
        if (
            not isinstance(project, dict)
            or not isinstance(campus, dict)
            or project.get("stable_key") != project_key
            or campus.get("stable_key") != campus_key
            or project.get("country") != "United States"
            or campus.get("country") != "United States"
            or project.get("coordinates") is not None
            or project.get("geometry") is not None
            or campus.get("coordinates") is not None
            or campus.get("geometry") is not None
            or acceptance["project_entity_id"]
            != atlas_stable_id("entity", project_key, "project")
            or acceptance["parent_campus_entity_id"]
            != atlas_stable_id("entity", campus_key, "campus")
            or campus_key in seen_campuses
        ):
            raise VerifiedConstructionCoreError(
                f"v0.12 source identity differs: {project_key}"
            )
        seen_campuses.add(campus_key)
        locator = results_by_id.get(acceptance["locator_id"])
        if (
            locator is None
            or locator.get("requested_address") != campus.get("address")
            or type(locator.get("longitude")) not in {int, float}
            or type(locator.get("latitude")) not in {int, float}
            or not math.isfinite(locator["longitude"])
            or not math.isfinite(locator["latitude"])
            or not -180 <= locator["longitude"] <= 180
            or not -90 <= locator["latitude"] <= 90
        ):
            raise VerifiedConstructionCoreError(
                f"v0.12 Census locator differs: {project_key}"
            )
        relationship = acceptance.get("relationship")
        if (
            not isinstance(relationship, dict)
            or not str(relationship.get("relationship_id", "")).startswith(
                "relationship:"
            )
            or relationship.get("relationship_type") != "project_targets"
            or relationship.get("decision_basis") != "explicit_parent"
        ):
            raise VerifiedConstructionCoreError(
                f"v0.12 project-to-campus relationship differs: {project_key}"
            )
        status = acceptance.get("status")
        if not isinstance(status, dict):
            raise VerifiedConstructionCoreError(
                f"v0.12 lifecycle differs: {project_key}"
            )
        lifecycle = [
            row
            for row in source.get("lifecycle", [])
            if isinstance(row, dict)
            and row.get("entity") == "project"
            and row.get("evidence_key") == status.get("evidence_key")
        ]
        if (
            len(lifecycle) != 1
            or lifecycle[0].get("value") != status.get("value")
            or lifecycle[0].get("as_of_date") != status.get("as_of_date")
            or lifecycle[0].get("method") != status.get("method")
            or lifecycle[0].get("confidence") != status.get("confidence")
            or status.get("value") not in PHYSICAL_STATUSES
            or status.get("method") not in AUTHORITATIVE_STATUS_METHODS
            or not 0
            <= (
                CURRENT_V12_LIFECYCLE_REFERENCE_DATE
                - _calendar_date(status["as_of_date"], "v0.12 status_as_of")
            ).days
            <= MAX_STATUS_AGE_DAYS
        ):
            raise VerifiedConstructionCoreError(
                f"v0.12 lifecycle differs: {project_key}"
            )
        _v12_source_evidence(
            source, status["evidence_key"], status["evidence_id"]
        )
        location = acceptance.get("location_evidence")
        if not isinstance(location, dict):
            raise VerifiedConstructionCoreError(
                f"v0.12 location evidence differs: {project_key}"
            )
        _v12_source_evidence(source, location["key"], location["evidence_id"])
        if project_key.endswith(":tx4"):
            conflicts = [
                row.get("metadata", {}).get("location_conflict", "")
                for row in source["evidence"]
                if isinstance(row, dict)
            ]
            if not any("2060 Lookout Drive" in row and "2008 Lookout Drive" in row for row in conflicts):
                raise VerifiedConstructionCoreError(
                    "v0.12 NTT address conflict differs"
                )
        if project_key.startswith("curated:qts-york") and (
            acceptance.get("address_scope") != "parent_campus_only"
            or project.get("address") == campus.get("address")
        ):
            raise VerifiedConstructionCoreError(
                "v0.12 QTS parent-campus scope differs"
            )
        sources[project_key] = source
    _v12_validate_hydrated_crosscheck(contract, acceptances, sources)
    return {
        "contract": contract,
        "capture": capture,
        "acceptances": sorted(
            acceptances, key=lambda row: row["project_stable_key"]
        ),
        "results_by_id": results_by_id,
        "sources": sources,
    }


def _v12_geometry_evidence(capture: Mapping[str, Any]) -> dict[str, Any]:
    return {
        "evidence_id": V12_GEOMETRY_EVIDENCE_ID,
        "kind": "government_record",
        "title": (
            "United States Census Geocoder Public_AR_Current exact-address "
            "matches for the v0.12 locator batch"
        ),
        "source_url": capture["source_url"],
        "publisher": capture["publisher"],
        "source_family": "us_census_geocoder",
        "license": capture["rights"]["license"],
        "attribution": capture["rights"]["attribution"],
        "published_at": "",
        "retrieved_at": capture["captured_on"],
        "content_hash": V12_GEOMETRY_CAPTURE_SHA256,
    }


def _v12_build_delta_rows(contracts: Mapping[str, Any]) -> dict[str, Any]:
    contract = contracts["contract"]
    semantics = contract["geometry_semantics"]
    sources = contracts["sources"]
    results = contracts["results_by_id"]
    evidence_pool: dict[str, dict[str, Any]] = {
        V12_GEOMETRY_EVIDENCE_ID: _v12_geometry_evidence(
            contracts["capture"]
        )
    }
    evidence_usage: dict[str, dict[str, set[str]]] = defaultdict(
        lambda: {"roles": set(), "project_ids": set()}
    )
    projects: list[dict[str, Any]] = []
    sites: list[dict[str, Any]] = []
    for acceptance in contracts["acceptances"]:
        project_key = acceptance["project_stable_key"]
        project_id = acceptance["project_entity_id"]
        campus_key = acceptance["parent_campus_stable_key"]
        source = sources[project_key]
        project = source["project"]
        campus = source["campus"]
        status = acceptance["status"]
        location = acceptance["location_evidence"]
        locator = results[acceptance["locator_id"]]
        for binding, role in (
            (status, "physical_status"),
            (location, "context:campus_address_identity"),
        ):
            key = binding.get("evidence_key", binding.get("key"))
            evidence_id = binding["evidence_id"]
            projected = _v12_source_evidence(source, key, evidence_id)
            previous = evidence_pool.get(evidence_id)
            if previous is not None and previous != projected:
                raise VerifiedConstructionCoreError(
                    f"v0.12 evidence collision differs: {project_key}"
                )
            evidence_pool[evidence_id] = projected
            evidence_usage[evidence_id]["roles"].add(role)
            evidence_usage[evidence_id]["project_ids"].add(project_id)
        evidence_usage[V12_GEOMETRY_EVIDENCE_ID]["roles"].add("geometry")
        evidence_usage[V12_GEOMETRY_EVIDENCE_ID]["project_ids"].add(
            project_id
        )
        geometry = {
            "type": "Point",
            "coordinates": [locator["longitude"], locator["latitude"]],
        }
        geometry_json = _json_bytes(geometry).decode().strip()
        site_id = _stable_id("vcc-site", campus_key)
        status_date = _calendar_date(
            status["as_of_date"], "v0.12 status_as_of"
        )
        project_row = {
            "project_id": project_id,
            "project_stable_key": project_key,
            "site_id": site_id,
            "physical_site_stable_key": campus_key,
            "name": project["name"],
            "country": "United States",
            "country_iso_a2": "US",
            "latitude": locator["latitude"],
            "longitude": locator["longitude"],
            "geometry_json": geometry_json,
            "geometry_type": "Point",
            "geometry_source_entity_kind": semantics[
                "geometry_source_entity_kind"
            ],
            "geometry_derivation": semantics["geometry_derivation"],
            "geometry_method": semantics["geometry_method"],
            "geometry_scope_class": semantics["geometry_scope_class"],
            "geometry_authority_class": semantics[
                "geometry_authority_class"
            ],
            "geometry_use_scope": semantics["geometry_use_scope"],
            "geometry_precision_scope": (
                f"{semantics['precision_scope']} {acceptance['scope_guardrail']}"
            ),
            "horizontal_uncertainty_metres": "",
            "horizontal_uncertainty_unknown_reason": semantics[
                "horizontal_uncertainty_unknown_reason"
            ],
            "geometry_evidence_id": V12_GEOMETRY_EVIDENCE_ID,
            "last_observed_physical_status": status["value"],
            "status_as_of": status["as_of_date"],
            "status_age_days_at_review": (
                CURRENT_V12_LIFECYCLE_REFERENCE_DATE - status_date
            ).days,
            "status_method": status["method"],
            "status_evidence_id": status["evidence_id"],
            "verification_posture": _verification_posture(
                semantics["geometry_authority_class"],
                semantics["geometry_use_scope"],
            ),
            "independent_imagery_verification": "false",
            "imagery_review_outcome": "not_reviewed_for_core_preview",
            "development_type": "unknown",
            "development_type_unknown_reason": (
                "not selected by the v0.12 address-locator batch"
            ),
            "operating_model": "unknown",
            "operating_model_unknown_reason": (
                "not established by selected evidence"
            ),
            "operating_model_evidence_id": "",
            "workloads_json": "[]",
            "workload_unknown_reason": "not established by selected evidence",
            "role_claims_json": "[]",
            "power_observations_json": "[]",
            "power_unknown_reason": (
                "no typed project power observation selected by this batch; "
                "not estimated"
            ),
            "annual_energy_observations_json": "[]",
            "annual_energy_unknown_reason": (
                "no scoped annual-energy inputs selected; not estimated"
            ),
            "efficiency_observations_json": "[]",
            "efficiency_unknown_reason": (
                "no scoped PUE or WUE observation selected"
            ),
            "owner": "",
            "operator": "",
            "users": "",
            "tenants": "",
            "customers": "",
            "status_source_url": evidence_pool[status["evidence_id"]][
                "source_url"
            ],
            "geometry_source_url": contracts["capture"]["source_url"],
        }
        projects.append(project_row)
        sites.append(
            {
                "site_id": site_id,
                "physical_site_stable_key": campus_key,
                "name": campus["name"],
                "country": "United States",
                "country_iso_a2": "US",
                "latitude": locator["latitude"],
                "longitude": locator["longitude"],
                "geometry_json": geometry_json,
                "geometry_type": "Point",
                "geometry_source_entity_kinds_json": _json_bytes(
                    [semantics["geometry_source_entity_kind"]]
                )
                .decode()
                .strip(),
                "geometry_derivations_json": _json_bytes(
                    [semantics["geometry_derivation"]]
                )
                .decode()
                .strip(),
                "geometry_methods_json": _json_bytes(
                    [semantics["geometry_method"]]
                )
                .decode()
                .strip(),
                "geometry_scope_classes_json": _json_bytes(
                    [semantics["geometry_scope_class"]]
                )
                .decode()
                .strip(),
                "geometry_authority_classes_json": _json_bytes(
                    [semantics["geometry_authority_class"]]
                )
                .decode()
                .strip(),
                "geometry_use_scopes_json": _json_bytes(
                    [semantics["geometry_use_scope"]]
                )
                .decode()
                .strip(),
                "geometry_precision_scopes_json": _json_bytes(
                    [project_row["geometry_precision_scope"]]
                )
                .decode()
                .strip(),
                "horizontal_uncertainty_metres": "",
                "horizontal_uncertainty_unknown_reason": semantics[
                    "horizontal_uncertainty_unknown_reason"
                ],
                "geometry_evidence_ids_json": _json_bytes(
                    [V12_GEOMETRY_EVIDENCE_ID]
                )
                .decode()
                .strip(),
                "project_count": 1,
                "project_ids_json": _json_bytes([project_id]).decode().strip(),
                "project_stable_keys_json": _json_bytes([project_key])
                .decode()
                .strip(),
                "statuses_json": _json_bytes([status["value"]])
                .decode()
                .strip(),
                "oldest_status_as_of": status["as_of_date"],
                "newest_status_as_of": status["as_of_date"],
                "verification_posture": project_row["verification_posture"],
                "independent_imagery_verification": "false",
                "imagery_review_outcomes_json": _json_bytes(
                    ["not_reviewed_for_core_preview"]
                )
                .decode()
                .strip(),
            }
        )
    evidence_rows = [
        {
            **evidence_pool[evidence_id],
            "roles_json": _json_bytes(sorted(usage["roles"])).decode().strip(),
            "project_ids_json": _json_bytes(sorted(usage["project_ids"]))
            .decode()
            .strip(),
        }
        for evidence_id, usage in sorted(evidence_usage.items())
    ]
    if (
        len(projects) != 5
        or len(sites) != 5
        or len(evidence_rows) != 9
        or len({row["project_id"] for row in projects}) != 5
        or len({row["site_id"] for row in sites}) != 5
        or len({row["evidence_id"] for row in evidence_rows}) != 9
    ):
        raise VerifiedConstructionCoreError("v0.12 delta accounting differs")
    return {"projects": projects, "sites": sites, "evidence": evidence_rows}


def _v12_portable_source_inputs(
    contracts: Mapping[str, Any],
) -> list[dict[str, Any]]:
    base_manifest = _load_json(LEGACY_PREVIEW_V11_DIR / "manifest.json")
    rows = list(base_manifest["portable_source_inputs"])
    rows.extend(
        [
            {
                "path": V12_BATCH_CONTRACT.relative_to(ROOT).as_posix(),
                "bytes": V12_BATCH_CONTRACT.stat().st_size,
                "sha256": V12_BATCH_CONTRACT_SHA256,
                "parent_manifests": [],
            },
            {
                "path": V12_GEOMETRY_CAPTURE.relative_to(ROOT).as_posix(),
                "bytes": V12_GEOMETRY_CAPTURE.stat().st_size,
                "sha256": V12_GEOMETRY_CAPTURE_SHA256,
                "parent_manifests": [],
            },
            *(
                {
                    **acceptance["source_input"],
                    "parent_manifests": [],
                }
                for acceptance in contracts["acceptances"]
            ),
        ]
    )
    rows.sort(key=lambda row: row["path"])
    if len(rows) != 81 or len({row["path"] for row in rows}) != 81:
        raise VerifiedConstructionCoreError("v0.12 portable input count differs")
    for row in rows:
        path = _repository_input(
            row["path"], row["sha256"], "v0.12 portable input"
        )
        if path.stat().st_size != row["bytes"]:
            raise VerifiedConstructionCoreError(
                "v0.12 portable input byte count differs"
            )
    return rows


def _v12_review_queue(
    contracts: Mapping[str, Any],
) -> list[dict[str, Any]]:
    semantics = contracts["contract"]["geometry_semantics"]
    return [
        {
            "batch_contract_path": V12_BATCH_CONTRACT.relative_to(ROOT).as_posix(),
            "batch_contract_sha256": V12_BATCH_CONTRACT_SHA256,
            "decision": "accepted",
            "decision_reason": acceptance["scope_guardrail"],
            "geometry_authority_class": semantics[
                "geometry_authority_class"
            ],
            "geometry_derivation": semantics["geometry_derivation"],
            "geometry_entity": "campus",
            "geometry_evidence_id": V12_GEOMETRY_EVIDENCE_ID,
            "geometry_evidence_key": contracts["contract"][
                "geometry_capture"
            ]["evidence_key"],
            "geometry_method": semantics["geometry_method"],
            "geometry_scope_class": semantics["geometry_scope_class"],
            "geometry_target_entity_id": acceptance[
                "parent_campus_entity_id"
            ],
            "geometry_target_entity_kind": "campus",
            "geometry_target_entity_stable_key": acceptance[
                "parent_campus_stable_key"
            ],
            "geometry_use_scope": "campus_locator",
            "horizontal_uncertainty_metres": None,
            "horizontal_uncertainty_unknown_reason": semantics[
                "horizontal_uncertainty_unknown_reason"
            ],
            "locator_id": acceptance["locator_id"],
            "official_boundary": False,
            "precision_scope": semantics["precision_scope"],
            "project_to_campus_decision_basis": "explicit_parent",
            "project_to_campus_relationship_id": acceptance["relationship"][
                "relationship_id"
            ],
            "project_to_campus_relationship_type": "project_targets",
            "rejected_claims": contracts["contract"]["rejected_claims"],
            "reviewed_at": CURRENT_V12_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat(),
            "source_input_bytes": acceptance["source_input"]["bytes"],
            "source_input_path": acceptance["source_input"]["path"],
            "source_input_sha256": acceptance["source_input"]["sha256"],
            "source_project_entity_id": acceptance["project_entity_id"],
            "source_project_stable_key": acceptance["project_stable_key"],
        }
        for acceptance in contracts["acceptances"]
    ]


def _v12_context_evidence_bindings(
    contracts: Mapping[str, Any],
) -> list[dict[str, Any]]:
    semantic_scopes = {
        (
            "curated:coreweave-chirisa-lancaster-greenfield-road-campus:"
            "phase-1-adaptive-reuse"
        ): (
            "The selected Chirisa status evidence binds the adaptive-reuse "
            "project to the 216 Greenfield Road property. It supports campus "
            "address identity only in this role; the Census address-match point "
            "does not establish the existing shell, Phase 1, parcel, campus, or "
            "construction footprint."
        ),
        (
            "curated:lancium-clean-campus-abilene-ai-data-center:"
            "remaining-six-building-expansion"
        ): (
            "The Abilene planning evidence binds the remaining campus development "
            "to 617 FM 2404. It supports parent-campus address identity only; the "
            "Census point is not a location or footprint for any one of the six "
            "remaining buildings."
        ),
        "curated:ntt-dallas-data-center-campus:tx4": (
            "The current NTT page binds TX4 and its parent Dallas campus to 2060 "
            "Lookout Drive. The selected locator follows that current company "
            "address while retaining the Texas TDLR record's conflicting 2008 "
            "Lookout Drive address; neither address establishes a TX4 footprint, "
            "parcel, or campus boundary."
        ),
        (
            "curated:qts-york-county-south-carolina-data-center-campus:"
            "initial-phase-building-1"
        ): (
            "The QTS location page binds 2143 Hands Mill Highway only to the "
            "parent York campus. Building 1 retains its source-explicit York "
            "locality; the Census point is a parent-campus locator and never a "
            "Building 1 location or footprint."
        ),
        "curated:trg-spring-cypress-campus:hou2": (
            "The TRG brochure binds HOU2 to the two-facility Spring Cypress campus "
            "at 2626 Spring Cypress Road. It supports campus address identity only; "
            "the Census point is not an HOU2 building or project footprint."
        ),
    }
    rows = []
    for acceptance in contracts["acceptances"]:
        project_key = acceptance["project_stable_key"]
        location = acceptance["location_evidence"]
        source_input = acceptance["source_input"]
        rows.append(
            {
                "evidence_id": location["evidence_id"],
                "evidence_key": location["key"],
                "project_stable_key": project_key,
                "semantic_scope": semantic_scopes[project_key],
                "source_input_path": source_input["path"],
                "source_input_sha256": source_input["sha256"],
                "usage_role": "geometry_identity",
            }
        )
    return rows


def _v12_map_html(
    geojson: Mapping[str, Any],
    sites: Sequence[Mapping[str, Any]],
    evidence: Sequence[Mapping[str, Any]],
) -> bytes:
    document = _v11_map_html(geojson, sites, evidence).decode("utf-8")
    title = "Verified Construction Core v0.11 preview"
    if document.count(title) != 1:
        raise VerifiedConstructionCoreError("v0.12 map title seam differs")
    document = document.replace(
        title, "Verified Construction Core v0.12 preview", 1
    )
    marker = '. <a href="ATTRIBUTION.txt">Full attribution and source terms</a>.'
    census_notice = (
        ' · <a href="https://geocoding.geo.census.gov/geocoder/">'
        "United States Census Bureau, Census Geocoder Public_AR_Current "
        "address-match points</a> — public-domain U.S. government data; "
        "street-range locator semantics only"
    )
    if document.count(marker) != 1:
        raise VerifiedConstructionCoreError(
            "v0.12 map attribution seam differs"
        )
    document = document.replace(marker, f"{census_notice}{marker}", 1)
    return document.encode("utf-8")


def _v12_schema() -> dict[str, Any]:
    schema = _v11_schema()
    schema["format"] = "datacenter-atlas-verified-construction-core-schema-v12"
    schema["preview_id"] = CURRENT_V12_PREVIEW_ID
    fields = schema["tables"]["projects.csv"]["fields"]
    geometry_derivation = next(
        row for row in fields if row["name"] == "geometry_derivation"
    )
    if (
        "official_address_geocode"
        in geometry_derivation["allowed_values"]
    ):
        raise VerifiedConstructionCoreError(
            "v0.12 schema inheritance differs"
        )
    geometry_derivation["allowed_values"].append(
        "official_address_geocode"
    )
    schema["v0_12_address_locator_batch"] = {
        "geometry_source_entity_kind": "campus",
        "geometry_derivation": "official_address_geocode",
        "geometry_method": (
            "census_public_ar_current_street_range_interpolation"
        ),
        "geometry_scope_class": (
            "official_census_address_locator_point"
        ),
        "geometry_use_scope": "campus_locator",
        "coordinate_semantics": (
            "The longitude/latitude pair is an official Census "
            "Public_AR_Current exact-address match used only as a locator for "
            "the source-linked parent campus. Street-range interpolation is "
            "never a parcel, building, campus boundary, project footprint, "
            "construction extent, entrance, survey point, or site centroid."
        ),
        "horizontal_uncertainty_semantics": (
            "The capture provides no bounded horizontal-accuracy value; an "
            "empty horizontal_uncertainty_metres field means unknown, not zero."
        ),
    }
    schema["v0_12_temporal_scope"] = {
        "reviewed_at_semantics": (
            "Legacy alias for cohort_lifecycle_reference_date, retained for "
            "preview compatibility; it is not the geometry-identity review date."
        ),
        "cohort_lifecycle_reference_date": (
            CURRENT_V12_LIFECYCLE_REFERENCE_DATE.isoformat()
        ),
        "geometry_identity_reviewed_at": (
            CURRENT_V12_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
        ),
    }
    return schema


def _v12_readme(report: Mapping[str, Any], legal_notice: str) -> bytes:
    return f"""# Verified Construction Core v0.12 preview

This tracked, non-final preview contains **{report['selected_physical_site_count']} physical sites**
and **{report['selected_project_count']} linked projects** in {len(report['country_counts'])}
countries. Every project retains a dated authoritative physical-status observation and a reviewed
geometry scope. Five project rows carry official boundaries; the remaining 51 are explicit project
or campus locators, never construction footprints by implication.

The five-row v0.12 delta adds CoreWeave Lancaster Phase 1, Lancium Abilene's remaining six-building
expansion, NTT TX4, QTS York Building 1, and TRG HOU2. Each project is bound by an explicit
project_targets relationship to a distinct parent campus and by hash-pinned curated source evidence.
Only physical status, campus-address identity, and the shared geometry evidence are selected. No
source metric, role, workload, operating model, owner, operator, user, tenant, or customer is added.

The United States Census Bureau Census Geocoder returned exact-address Match results under the
Public_AR_Current benchmark for the five parent-campus addresses. These points are official Census
street-range interpolation/address locators only. They are never parcel, cadastral, campus, building,
project, or construction boundaries; building or project footprints; entrances; survey positions;
site centroids; or point-in-boundary evidence. Numeric horizontal uncertainty is unavailable and is
preserved as unknown. No map tiles or imagery are included.

The current NTT address at 2060 Lookout Drive controls its locator while the conflicting Texas TDLR
address at 2008 Lookout Drive remains disclosed. The QTS 2143 Hands Mill Highway address is scoped
only to the parent York campus, never Building 1.

United States Census Bureau, Census Geocoder Public_AR_Current; compact factual address-match
results are treated as public-domain U.S. government data.

The fixed cohort lifecycle/status cutoff is 2026-08-20. The Census geometry and its identity use were
accepted on 2026-08-23; they do not update cohort lifecycle state. For preview compatibility,
`reviewed_at` remains an alias for the lifecycle cutoff; the manifest and selection report separately
expose `geometry_identity_reviewed_at`.

The artifact is deterministic and rebuildable in a clean clone from 81 manifest-bound portable inputs
plus the byte-frozen v0.11 artifact. Ignored v97 and v14 payloads are optional, complete, all-or-nothing
hydrated cross-checks, not build dependencies. All five new rows remain
independent_imagery_verification=false.

## City of Chicago terms inherited from v0.10

{legal_notice}

This is not the final 100-site Verified Construction Core v1. Its final-release gates remain
unsatisfied and publishable_as_final is false.
""".encode("utf-8")


def _v12_attribution(
    evidence: Sequence[Mapping[str, Any]], legal_notice: str
) -> bytes:
    document = _v11_attribution(evidence, legal_notice).decode("utf-8")
    heading = "Data Center Atlas Verified Construction Core v0.11 preview"
    if document.count(heading) != 1:
        raise VerifiedConstructionCoreError(
            "v0.12 attribution heading seam differs"
        )
    document = document.replace(
        heading,
        "Data Center Atlas Verified Construction Core v0.12 preview",
        1,
    )
    marker = "Map-provider terms:\n"
    notice = (
        "Census locator notice: United States Census Bureau, Census Geocoder "
        "Public_AR_Current exact-address matches; compact factual locator "
        "results only. Street-range interpolation is not parcel, building, "
        "campus, project, or construction geometry. No map tiles or imagery "
        "are redistributed.\n\n"
    )
    if document.count(marker) != 1:
        raise VerifiedConstructionCoreError(
            "v0.12 attribution notice seam differs"
        )
    document = document.replace(marker, f"{notice}{marker}", 1)
    return document.encode("utf-8")


def _v12_payloads() -> tuple[dict[str, bytes], dict[str, Any], list[dict[str, Any]]]:
    validate_frozen_v11(LEGACY_PREVIEW_V11_DIR)
    contracts = _v12_contracts()
    base_projects = _load_csv(
        LEGACY_PREVIEW_V11_DIR / "projects.csv", PROJECT_FIELDS
    )
    base_sites = _load_csv(
        LEGACY_PREVIEW_V11_DIR / "sites.csv", SITE_FIELDS
    )
    base_evidence = _load_csv(
        LEGACY_PREVIEW_V11_DIR / "evidence.csv", EVIDENCE_FIELDS
    )
    base_report = _load_json(
        LEGACY_PREVIEW_V11_DIR / "selection-report.json"
    )
    delta = _v12_build_delta_rows(contracts)
    if (
        {row["project_id"] for row in base_projects}
        & {row["project_id"] for row in delta["projects"]}
        or {row["site_id"] for row in base_sites}
        & {row["site_id"] for row in delta["sites"]}
        or {row["evidence_id"] for row in base_evidence}
        & {row["evidence_id"] for row in delta["evidence"]}
    ):
        raise VerifiedConstructionCoreError("v0.12 delta identity collision")
    projects = sorted(
        [*base_projects, *delta["projects"]],
        key=lambda row: row["project_stable_key"],
    )
    sites = sorted(
        [*base_sites, *delta["sites"]],
        key=lambda row: row["physical_site_stable_key"],
    )
    evidence = sorted(
        [*base_evidence, *delta["evidence"]],
        key=lambda row: row["evidence_id"],
    )
    if (
        len(projects) != 56
        or len(sites) != 53
        or len(evidence) != 129
        or len({row["project_id"] for row in projects}) != 56
        or len({row["site_id"] for row in sites}) != 53
        or len({row["evidence_id"] for row in evidence}) != 129
    ):
        raise VerifiedConstructionCoreError(
            "v0.12 cohort count or uniqueness differs"
        )
    project_by_id = {row["project_id"]: row for row in projects}
    site_by_id = {row["site_id"]: row for row in sites}
    evidence_by_id = {row["evidence_id"]: row for row in evidence}
    if (
        any(project_by_id[row["project_id"]] != row for row in base_projects)
        or any(site_by_id[row["site_id"]] != row for row in base_sites)
        or any(
            evidence_by_id[row["evidence_id"]] != row
            for row in base_evidence
        )
    ):
        raise VerifiedConstructionCoreError("v0.12 inherited row changed")
    gates = _final_release_gates(sites, projects)
    gates["clean_clone_rebuild"] = {
        "passed": True,
        "reason": (
            "all current-delta inputs and the frozen base artifact are tracked "
            "and hash-bound"
        ),
    }
    country_counts = dict(
        sorted(Counter(row["country"] for row in sites).items())
    )
    provenance_decisions = {
        key: list(base_report["provenance_decisions"][key])
        for key in (
            "workload_scope_bindings",
            "role_bindings",
            "excluded_source_roles",
            "context_evidence_bindings",
        )
    }
    provenance_decisions["context_evidence_bindings"].extend(
        _v12_context_evidence_bindings(contracts)
    )
    semantics = contracts["contract"]["geometry_semantics"]
    report = {
        **base_report,
        "format": "datacenter-atlas-verified-construction-core-selection-v12",
        "publishable_as_final": all(
            gate.get("passed", False) for gate in gates.values()
        ),
        "reviewed_at": CURRENT_V12_LIFECYCLE_REFERENCE_DATE.isoformat(),
        "cohort_lifecycle_reference_date": (
            CURRENT_V12_LIFECYCLE_REFERENCE_DATE.isoformat()
        ),
        "geometry_identity_reviewed_at": (
            CURRENT_V12_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
        ),
        "selected_project_count": 56,
        "selected_physical_site_count": 53,
        "official_boundary_project_count": 5,
        "reviewed_site_locator_project_count": 51,
        "non_selected_source_row_count": 475,
        "selection_first_failure_counts": V12_SELECTION_FIRST_FAILURE_COUNTS,
        "country_counts": country_counts,
        "final_release_gates": gates,
        "imagery_review_provenance": list(
            base_report["imagery_review_provenance"]
        ),
        "provenance_decisions": provenance_decisions,
        "reviewed_overlay_queue": [
            *base_report["reviewed_overlay_queue"],
            *_v12_review_queue(contracts),
        ],
        "address_locator_batch": {
            "accepted_project_count": 5,
            "benchmark": contracts["capture"]["benchmark"],
            "batch_contract_path": (
                V12_BATCH_CONTRACT.relative_to(ROOT).as_posix()
            ),
            "batch_contract_sha256": V12_BATCH_CONTRACT_SHA256,
            "capture_path": V12_GEOMETRY_CAPTURE.relative_to(ROOT).as_posix(),
            "capture_sha256": V12_GEOMETRY_CAPTURE_SHA256,
            "geometry_authority_class": semantics[
                "geometry_authority_class"
            ],
            "geometry_derivation": semantics["geometry_derivation"],
            "geometry_evidence_id": V12_GEOMETRY_EVIDENCE_ID,
            "geometry_method": semantics["geometry_method"],
            "geometry_scope_class": semantics["geometry_scope_class"],
            "geometry_use_scope": semantics["geometry_use_scope"],
            "match_status": contracts["capture"]["match_status"],
            "official_boundary": False,
            "project_stable_keys": sorted(V12_PROJECT_KEYS),
            "rejected_claims": contracts["contract"]["rejected_claims"],
        },
    }
    delta_by_key = {
        row["project_stable_key"]: row for row in delta["projects"]
    }
    if (
        len(country_counts) != 26
        or country_counts.get("United States") != 13
        or sum(row["country_iso_a2"] != "US" for row in sites) != 40
        or sum(_is_official_boundary(row) for row in projects) != 5
        or sum(_is_reviewed_locator(row) for row in projects) != 51
        or sum(V12_SELECTION_FIRST_FAILURE_COUNTS.values()) != 531
        or gates["imagery_outcomes_complete"]
        != {"actual": 10, "required": 56, "passed": False}
        or len(provenance_decisions["context_evidence_bindings"]) != 14
        or len(provenance_decisions["excluded_source_roles"]) != 12
        or len(report["reviewed_overlay_queue"]) != 43
        or set(delta_by_key) != V12_PROJECT_KEYS
        or any(
            row["geometry_source_entity_kind"] != "campus"
            or row["geometry_type"] != "Point"
            or row["geometry_derivation"] != "official_address_geocode"
            or row["geometry_use_scope"] != "campus_locator"
            or row["workloads_json"] != "[]"
            or row["role_claims_json"] != "[]"
            or row["power_observations_json"] != "[]"
            or row["annual_energy_observations_json"] != "[]"
            or row["efficiency_observations_json"] != "[]"
            or row["operating_model"] != "unknown"
            or any(
                row[field]
                for field in (
                    "owner",
                    "operator",
                    "users",
                    "tenants",
                    "customers",
                )
            )
            for row in delta_by_key.values()
        )
    ):
        raise VerifiedConstructionCoreError("v0.12 expected accounting differs")
    geojson = _geojson(sites)
    geojson["name"] = (
        "Data Center Atlas Verified Construction Core v0.12 preview"
    )
    delta_features = {
        row["properties"]["physical_site_stable_key"]: row
        for row in geojson["features"]
        if row["properties"]["physical_site_stable_key"]
        in {
            acceptance["parent_campus_stable_key"]
            for acceptance in contracts["acceptances"]
        }
    }
    if (
        len(delta_features) != 5
        or any(
            feature["geometry"]["type"] != "Point"
            for feature in delta_features.values()
        )
    ):
        raise VerifiedConstructionCoreError(
            "v0.12 Census point publication differs"
        )
    legal_notice = _v10_city_legal_notice_from_base()
    payloads = {
        "ATTRIBUTION.txt": _v12_attribution(evidence, legal_notice),
        "README.md": _v12_readme(report, legal_notice),
        "evidence.csv": _csv_bytes(evidence, EVIDENCE_FIELDS),
        "map.html": _v12_map_html(geojson, sites, evidence),
        "projects.csv": _csv_bytes(projects, PROJECT_FIELDS),
        "schema.json": _json_bytes(_v12_schema()),
        "selection-report.json": _json_bytes(report),
        "sites.csv": _csv_bytes(sites, SITE_FIELDS),
        "sites.geojson": _json_bytes(geojson),
    }
    portable = _v12_portable_source_inputs(contracts)
    return payloads, report, portable


def _v12_manifest(
    payloads: Mapping[str, bytes],
    report: Mapping[str, Any],
    portable: Sequence[Mapping[str, Any]],
) -> dict[str, Any]:
    return {
        "format": "datacenter-atlas-verified-construction-core-preview-v12",
        "preview_id": CURRENT_V12_PREVIEW_ID,
        "release_status": "preview",
        "publishable_as_final": report["publishable_as_final"],
        "base_preview_id": CURRENT_V11_PREVIEW_ID,
        "base_preview_manifest_sha256": LEGACY_PREVIEW_V11_MANIFEST_SHA256,
        "base_preview_commit": LEGACY_PREVIEW_V11_COMMIT,
        "source_release_id": SOURCE_RELEASE_ID,
        "source_release_manifest_path": (
            "releases/2026-07-22-open-seed-v97/manifest.json"
        ),
        "source_release_manifest_sha256": (
            "0a6f41f4239944df27f2ce70e81a089b91cec401f154bbae28412b27a4d00fdd"
        ),
        "definition_paths": {
            "address_locator_batch": (
                V12_BATCH_CONTRACT.relative_to(ROOT).as_posix()
            )
        },
        "address_locator_batch_sha256": V12_BATCH_CONTRACT_SHA256,
        "geometry_capture": {
            "path": V12_GEOMETRY_CAPTURE.relative_to(ROOT).as_posix(),
            "bytes": V12_GEOMETRY_CAPTURE.stat().st_size,
            "sha256": V12_GEOMETRY_CAPTURE_SHA256,
            "evidence_id": V12_GEOMETRY_EVIDENCE_ID,
        },
        "portable_source_inputs": list(portable),
        "reviewed_at": CURRENT_V12_LIFECYCLE_REFERENCE_DATE.isoformat(),
        "cohort_lifecycle_reference_date": (
            CURRENT_V12_LIFECYCLE_REFERENCE_DATE.isoformat()
        ),
        "geometry_identity_reviewed_at": (
            CURRENT_V12_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
        ),
        "counts": {
            "physical_sites": 53,
            "projects": 56,
            "evidence": 129,
            "countries": 26,
            "non_us_sites": 40,
            "official_boundary_projects": 5,
            "reviewed_site_locator_projects": 51,
        },
        "files": {
            name: {"bytes": len(payload), "sha256": _sha256_bytes(payload)}
            for name, payload in sorted(payloads.items())
        },
    }


def build_preview(output_dir: Path = CURRENT_V12_PREVIEW_DIR) -> dict[str, Any]:
    """Build v0.12 from byte-frozen v0.11 and tracked hash-bound inputs."""
    output_dir = Path(output_dir)
    if output_dir.exists():
        raise VerifiedConstructionCoreError(f"refusing to overwrite {output_dir}")
    payloads, report, portable = _v12_payloads()
    manifest = _v12_manifest(payloads, report, portable)
    manifest_bytes = _json_bytes(manifest)
    complete = {
        **payloads,
        "manifest.json": manifest_bytes,
        "manifest.sha256": (
            f"{_sha256_bytes(manifest_bytes)}  manifest.json\n".encode()
        ),
    }
    output_dir.parent.mkdir(parents=True, exist_ok=True)
    stage = Path(
        tempfile.mkdtemp(prefix=f".{output_dir.name}.", dir=output_dir.parent)
    )
    try:
        for name, payload in complete.items():
            with (stage / name).open("xb") as handle:
                handle.write(payload)
        os.replace(stage, output_dir)
    except Exception:
        shutil.rmtree(stage, ignore_errors=True)
        raise
    validate_preview(output_dir)
    return manifest


def _validate_v12_preview(path: Path) -> dict[str, Any]:
    path = Path(path)
    if path.is_symlink() or not path.is_dir():
        raise VerifiedConstructionCoreError("v0.12 preview directory differs")
    manifest_path = path / "manifest.json"
    checksum_path = path / "manifest.sha256"
    if (
        manifest_path.is_symlink()
        or not manifest_path.is_file()
        or checksum_path.is_symlink()
        or not checksum_path.is_file()
    ):
        raise VerifiedConstructionCoreError(
            "v0.12 manifest trust root differs"
        )
    manifest_bytes = manifest_path.read_bytes()
    if checksum_path.read_bytes() != (
        f"{_sha256_bytes(manifest_bytes)}  manifest.json\n".encode()
    ):
        raise VerifiedConstructionCoreError("v0.12 manifest checksum differs")
    manifest = _load_json(manifest_path)
    files = manifest.get("files") if isinstance(manifest, dict) else None
    if (
        not isinstance(manifest, dict)
        or manifest.get("format")
        != "datacenter-atlas-verified-construction-core-preview-v12"
        or manifest.get("preview_id") != CURRENT_V12_PREVIEW_ID
        or manifest.get("release_status") != "preview"
        or type(manifest.get("publishable_as_final")) is not bool
        or manifest["publishable_as_final"] is not False
        or manifest.get("base_preview_manifest_sha256")
        != LEGACY_PREVIEW_V11_MANIFEST_SHA256
        or not isinstance(files, dict)
    ):
        raise VerifiedConstructionCoreError("v0.12 manifest files differ")
    expected_inventory = set(files) | {"manifest.json", "manifest.sha256"}
    if {item.name for item in path.iterdir()} != expected_inventory:
        raise VerifiedConstructionCoreError("v0.12 preview inventory differs")
    for name, metadata in files.items():
        member = path / name
        if (
            Path(name).name != name
            or member.is_symlink()
            or not member.is_file()
            or type(metadata.get("bytes")) is not int
            or member.stat().st_size != metadata["bytes"]
            or _sha256_file(member) != metadata.get("sha256")
        ):
            raise VerifiedConstructionCoreError(
                f"v0.12 member differs: {name}"
            )
    payloads, report, portable = _v12_payloads()
    if manifest != _v12_manifest(payloads, report, portable):
        raise VerifiedConstructionCoreError("v0.12 manifest semantics differ")
    for name, expected in payloads.items():
        if (path / name).read_bytes() != expected:
            raise VerifiedConstructionCoreError(
                f"v0.12 generated member differs: {name}"
            )
    schema = _load_json(path / "schema.json")
    geometry_derivation = next(
        row
        for row in schema["tables"]["projects.csv"]["fields"]
        if row["name"] == "geometry_derivation"
    )
    if (
        "official_address_geocode"
        not in geometry_derivation["allowed_values"]
        or schema.get("v0_12_address_locator_batch", {}).get(
            "geometry_use_scope"
        )
        != "campus_locator"
    ):
        raise VerifiedConstructionCoreError(
            "v0.12 address-locator schema seam differs"
        )
    census_notice = b"Census Geocoder Public_AR_Current"
    for name in ("ATTRIBUTION.txt", "README.md", "map.html"):
        if census_notice not in (path / name).read_bytes():
            raise VerifiedConstructionCoreError(
                f"v0.12 Census notice missing from {name}"
            )
    projects = {
        row["project_stable_key"]: row
        for row in _load_csv(path / "projects.csv", PROJECT_FIELDS)
        if row["project_stable_key"] in V12_PROJECT_KEYS
    }
    if set(projects) != V12_PROJECT_KEYS:
        raise VerifiedConstructionCoreError("v0.12 project inventory differs")
    for row in projects.values():
        if (
            row["geometry_derivation"] != "official_address_geocode"
            or row["geometry_method"]
            != "census_public_ar_current_street_range_interpolation"
            or row["geometry_scope_class"]
            != "official_census_address_locator_point"
            or row["geometry_use_scope"] != "campus_locator"
            or row["geometry_type"] != "Point"
            or row["horizontal_uncertainty_metres"]
            or row["independent_imagery_verification"] != "false"
            or any(
                row[field] != "[]"
                for field in (
                    "workloads_json",
                    "role_claims_json",
                    "power_observations_json",
                    "annual_energy_observations_json",
                    "efficiency_observations_json",
                )
            )
        ):
            raise VerifiedConstructionCoreError(
                "v0.12 project semantic scope differs"
            )
    geometry_evidence = next(
        row
        for row in _load_csv(path / "evidence.csv", EVIDENCE_FIELDS)
        if row["evidence_id"] == V12_GEOMETRY_EVIDENCE_ID
    )
    if (
        _parse_json_field(
            geometry_evidence["roles_json"], "v0.12 geometry roles"
        )
        != ["geometry"]
        or set(
            _parse_json_field(
                geometry_evidence["project_ids_json"],
                "v0.12 geometry projects",
            )
        )
        != {row["project_id"] for row in projects.values()}
    ):
        raise VerifiedConstructionCoreError(
            "v0.12 geometry evidence usage differs"
        )
    return manifest


def validate_preview(path: Path = CURRENT_V12_PREVIEW_DIR) -> dict[str, Any]:
    """Validate current v0.12 or dispatch v0.1-v0.11 via frozen seams."""
    path = Path(path)
    manifest_path = path / "manifest.json"
    if (
        path.is_symlink()
        or not path.is_dir()
        or manifest_path.is_symlink()
        or not manifest_path.is_file()
    ):
        raise VerifiedConstructionCoreError("preview manifest trust root differs")
    manifest = _load_json(manifest_path)
    preview_id = manifest.get("preview_id") if isinstance(manifest, dict) else None
    if preview_id == CURRENT_V12_PREVIEW_ID:
        return _validate_v12_preview(path)
    return _validate_v11_preview_dispatch(path)


__all__ = [
    *__all__,
    "CURRENT_V12_PREVIEW_DIR",
    "CURRENT_V12_PREVIEW_ID",
    "CURRENT_V12_GEOMETRY_IDENTITY_REVIEW_DATE",
    "CURRENT_V12_LIFECYCLE_REFERENCE_DATE",
    "CURRENT_V12_REVIEW_DATE",
    "LEGACY_PREVIEW_V11_COMMIT",
    "LEGACY_PREVIEW_V11_DIR",
    "LEGACY_PREVIEW_V11_MANIFEST_SHA256",
    "V12_BATCH_CONTRACT",
    "V12_BATCH_CONTRACT_SHA256",
    "V12_GEOMETRY_CAPTURE",
    "V12_GEOMETRY_CAPTURE_SHA256",
    "V12_GEOMETRY_EVIDENCE_ID",
    "V12_PROJECT_KEYS",
    "validate_frozen_v11",
]


_build_v12_preview = build_preview
_validate_v12_preview_dispatch = validate_preview

LEGACY_PREVIEW_V12_DIR = CURRENT_V12_PREVIEW_DIR
LEGACY_PREVIEW_V12_MANIFEST_SHA256 = (
    "cdd84e2137b2963600a923fff775f79da4bf8af513a6eeb45805e2c9cac192d1"
)
LEGACY_PREVIEW_V12_COMMIT = "2e3363e088929439230a83cf00551cc1ed5e2476"
CURRENT_V13_PREVIEW_ID = "2026-08-20-preview-v0.13"
CURRENT_V13_PREVIEW_DIR = ROOT / "verified_construction_core" / CURRENT_V13_PREVIEW_ID
CURRENT_V13_LIFECYCLE_REFERENCE_DATE = date(2026, 8, 20)
CURRENT_V13_GEOMETRY_IDENTITY_REVIEW_DATE = date(2026, 8, 23)
CURRENT_V13_REVIEW_DATE = CURRENT_V13_LIFECYCLE_REFERENCE_DATE
V13_BATCH_CONTRACT = (
    ROOT
    / "definitions"
    / "verified-construction-core-v0.13-six-site-geometry-batch.json"
)
V13_BATCH_CONTRACT_SHA256 = (
    "8ac7a56a2036010f37a8af0e563a47a546a6c30afeb4a12c025f34b464a4a45b"
)
V13_GEOMETRY_CAPTURE = (
    ROOT
    / "sources"
    / "verified-construction-core-v0.13-six-site-geometry-facts.json"
)
V13_GEOMETRY_CAPTURE_SHA256 = (
    "b2831a4d5028b0de4757ccba4774b0987794f2fa9f6669b0682b6cfcedfe96fd"
)
V13_PROJECT_KEYS = frozenset(
    {
        "curated:bitzero-namsskogan-data-center-campus:2026-power-infrastructure-expansion",
        "curated:ezditek-ruh01-pnu-riyadh-data-center:phase-1-current-build",
        "curated:firstcolo-fra7-rosbach-campus:fra7-current-build",
        "curated:goodman-syd01-macquarie-park-data-centre:current-single-building-development",
        "curated:qts-cambois-data-centre-campus:2026-earthworks",
        "curated:xtx-markets-kajaani-data-center-campus:second-data-center",
    }
)
V13_SELECTION_FIRST_FAILURE_COUNTS = {
    "entity_kind_not_project": 49,
    "not_in_reviewed_site_geometry_allowlist": 81,
    "selected": 62,
    "status_not_physical": 12,
    "status_outside_90_day_window": 327,
}
V13_COUNTRY_ISO_A2 = {
    "Australia": "AU",
    "Finland": "FI",
    "Germany": "DE",
    "Norway": "NO",
    "Saudi Arabia": "SA",
    "United Kingdom": "GB",
}
V13_CAPTURE_EVIDENCE_IDS = frozenset(
    {
        "1a606443-bf19-54d8-8025-8c1e1102c25e",
        "23365ee9-f538-56fe-acec-e41f43182ded",
        "706814f6-ce3c-5619-83a7-174fea885db2",
        "817642c1-265e-5c90-9684-93ab069621a4",
        "8f3069b1-7b32-5df4-9329-c365ae2a09bb",
        "90d3590f-be24-5554-809a-36313784fe51",
        "9d76095e-ffd9-5ac9-a910-1b0bf3dacc91",
        "a4ff7674-bfdc-5a88-92cd-673e02250b1a",
        "b91493e8-2d06-5029-8df8-bfc06d77f418",
    }
)
V13_SOURCE_DOCUMENT_EXPECTATIONS = {
    "bitzero-namsskogan-kartverket-parcel": (
        842,
        "a29e44a76c1cb22d8e4ee697c78ab84843819a031ea37e1ccf64603093d277c8",
        "90d3590f-be24-5554-809a-36313784fe51",
        "CC-BY-4.0",
    ),
    "bitzero-namsskogan-kartverket-point": (
        477,
        "3fec07c823639bf1eac26e5ae98de06b01e754c45d43421ab7df702cbb5e0ab3",
        None,
        "CC-BY-4.0",
    ),
    "bitzero-namsskogan-sec-aif": (
        929809,
        "459f619d5ad1586a6e12214d7f9892d4ee3423eb3b2beca378e7d19395e5683c",
        "1a606443-bf19-54d8-8025-8c1e1102c25e",
        "all-rights-reserved-fact-extraction-only",
    ),
    "bitzero-namsskogan-sec-lease": (
        420349,
        "379454e37fc2b040a5ddb74fe6fa612289c9db2e08f83aa311addbe8ea99b56e",
        "9d76095e-ffd9-5ac9-a910-1b0bf3dacc91",
        "all-rights-reserved-fact-extraction-only",
    ),
    "ezditek-ruh01-osm-changeset": (
        823,
        "d2adf517afe2f4322d73a9cd09ce8359a43334f799b699405c74c58e46ec3b07",
        None,
        "ODbL-1.0",
    ),
    "ezditek-ruh01-osm-way": (
        1205,
        "b5ba6cc30c8c10bba2887c067dd1f1d48644123fcf83b7fedb6902f9fad540b8",
        "b91493e8-2d06-5029-8df8-bfc06d77f418",
        "ODbL-1.0",
    ),
    "firstcolo-fra7-factsheet": (
        2261778,
        "ea1df880294c544a81b5d67f9bc6dcfed9a84b87c55ad588d5e52bd691dc0cd5",
        "706814f6-ce3c-5619-83a7-174fea885db2",
        "all-rights-reserved-fact-extraction-only",
    ),
    "goodman-syd01-nsw-planning-application": (
        515049,
        "d69c321182f25e379fd519ed9367e3e7b6deda9f42c5cb2dfcc2dd8371bef9e5",
        "8f3069b1-7b32-5df4-9329-c365ae2a09bb",
        "all-rights-reserved-fact-extraction-only",
    ),
    "qts-cambois-environment-agency-report": (
        24912891,
        "25235f3c8187b67df890d8460cf520537ddad603a87c56e84bf06728cd7946f4",
        "a4ff7674-bfdc-5a88-92cd-673e02250b1a",
        "all-rights-reserved-fact-extraction-only",
    ),
    "xtx-kajaani-city-permit": (
        390873,
        "b5a3a02ac2abcece2631c3da711054448317b4f4a7a482c61bcea2963cef0ac7",
        "817642c1-265e-5c90-9684-93ab069621a4",
        "rights-status-uncertain-fact-extraction-only",
    ),
    "xtx-kajaani-nls-parcel": (
        7419,
        "2f83cf299f5258c92fd4c61306c5a5aed91bd20a56c48dae51b94c99714164af",
        "23365ee9-f538-56fe-acec-e41f43182ded",
        "CC-BY-4.0",
    ),
}
V13_GEOMETRY_EXPECTATIONS = {
    "bitzero-namsskogan-parcel-5044-50-44": {
        "project_stable_key": (
            "curated:bitzero-namsskogan-data-center-campus:"
            "2026-power-infrastructure-expansion"
        ),
        "geometry_source_document_id": "bitzero-namsskogan-kartverket-parcel",
        "identity_source_document_ids": [
            "bitzero-namsskogan-sec-lease",
            "bitzero-namsskogan-sec-aif",
        ],
        "geometry_type": "Polygon",
        "geometry_sha256": (
            "2fa5ce3ba9eb4f4d43ec5baf6e8faf1ae13385b1ca2338e8ec3f0e5631e7bf84"
        ),
        "anchor": [12.83406, 64.71427],
        "geometry_source_entity_kind": "campus",
        "geometry_derivation": "cross_source_geometry",
        "geometry_method": (
            "kartverket_official_cadastral_parcel_polygon_as_campus_locator"
        ),
        "geometry_scope_class": "official_cadastral_parcel_as_campus_locator",
        "geometry_authority_class": "official_source",
        "geometry_use_scope": "campus_locator",
    },
    "ezditek-ruh01-osm-way-1544103564": {
        "project_stable_key": (
            "curated:ezditek-ruh01-pnu-riyadh-data-center:phase-1-current-build"
        ),
        "geometry_source_document_id": "ezditek-ruh01-osm-way",
        "identity_source_document_ids": ["ezditek-ruh01-osm-way"],
        "geometry_type": "Polygon",
        "geometry_sha256": (
            "ee593fe28cb596db83e396f59c5922569d7a140399cdc05c78b7a4895d46e51f"
        ),
        "anchor": [46.713968972719, 24.866954286488],
        "geometry_source_entity_kind": "building",
        "geometry_derivation": "capture_replayed_geometry",
        "geometry_method": (
            "capture_replayed_openstreetmap_exact_ezditek_ruh01_named_"
            "building_polygon_as_project_locator"
        ),
        "geometry_scope_class": (
            "community_mapped_named_ezditek_ruh01_building_polygon"
        ),
        "geometry_authority_class": "community_mapped",
        "geometry_use_scope": "project_locator",
    },
    "firstcolo-fra7-factsheet-point": {
        "project_stable_key": "curated:firstcolo-fra7-rosbach-campus:fra7-current-build",
        "geometry_source_document_id": "firstcolo-fra7-factsheet",
        "identity_source_document_ids": ["firstcolo-fra7-factsheet"],
        "geometry_type": "Point",
        "geometry_sha256": (
            "bf5f898ced294134512457ebd83d494eaec97db1e2c936b3fd2f6511640fe3fa"
        ),
        "anchor": [8.699444444444445, 50.29480555555556],
        "geometry_source_entity_kind": "facility",
        "geometry_derivation": "coordinates_to_point",
        "geometry_method": "first_party_published_dms_coordinate_to_point",
        "geometry_scope_class": "first_party_facility_reference_point",
        "geometry_authority_class": "official_source",
        "geometry_use_scope": "project_locator",
    },
    "goodman-syd01-ssd-66777221-point": {
        "project_stable_key": (
            "curated:goodman-syd01-macquarie-park-data-centre:"
            "current-single-building-development"
        ),
        "geometry_source_document_id": (
            "goodman-syd01-nsw-planning-application"
        ),
        "identity_source_document_ids": [
            "goodman-syd01-nsw-planning-application"
        ],
        "geometry_type": "Point",
        "geometry_sha256": (
            "607ea1bcafa63380d2b5c9c080ffd7da25c07b18c5c6bd0f8924bec1287fcac1"
        ),
        "anchor": [151.19, -33.818441],
        "geometry_source_entity_kind": "project",
        "geometry_derivation": "coordinates_to_point",
        "geometry_method": (
            "nsw_planning_portal_applicant_project_coordinate_to_point"
        ),
        "geometry_scope_class": "official_applicant_project_site_reference_point",
        "geometry_authority_class": "official_source",
        "geometry_use_scope": "project_locator",
    },
    "qts-cambois-wider-campus-ngr-point": {
        "project_stable_key": (
            "curated:qts-cambois-data-centre-campus:2026-earthworks"
        ),
        "geometry_source_document_id": (
            "qts-cambois-environment-agency-report"
        ),
        "identity_source_document_ids": [
            "qts-cambois-environment-agency-report"
        ],
        "geometry_type": "Point",
        "geometry_sha256": (
            "454261ae4755e08026a94bba6e85ecd00794212777d49767e9344d7385d50d25"
        ),
        "anchor": [-1.533627047468273, 55.15092301981584],
        "geometry_source_entity_kind": "campus",
        "geometry_derivation": "official_coordinate_transform",
        "geometry_method": (
            "applicant_report_osgb36_national_grid_reference_to_wgs84"
        ),
        "geometry_scope_class": (
            "government_hosted_applicant_report_wider_campus_reference_point"
        ),
        "geometry_authority_class": "official_source",
        "geometry_use_scope": "campus_locator",
    },
    "xtx-kajaani-parcel-205-7-4-1": {
        "project_stable_key": (
            "curated:xtx-markets-kajaani-data-center-campus:second-data-center"
        ),
        "geometry_source_document_id": "xtx-kajaani-nls-parcel",
        "identity_source_document_ids": ["xtx-kajaani-city-permit"],
        "geometry_type": "Polygon",
        "geometry_sha256": (
            "c1e67ac736a39f6970c1d5e4e8c1ceebdfcffa083b96d5b5cee4464d084df221"
        ),
        "anchor": [27.655443809824, 64.237214640662],
        "geometry_source_entity_kind": "campus",
        "geometry_derivation": "official_coordinate_transform",
        "geometry_method": (
            "nls_official_cadastral_parcel_polygon_as_campus_locator"
        ),
        "geometry_scope_class": "official_cadastral_parcel_as_campus_locator",
        "geometry_authority_class": "official_source",
        "geometry_use_scope": "campus_locator",
    },
}


def validate_frozen_v12(
    path: Path = LEGACY_PREVIEW_V12_DIR,
) -> dict[str, Any]:
    """Validate byte-frozen v0.12 without regenerating it."""
    return _validate_frozen_preview_inventory(
        path,
        preview_id=CURRENT_V12_PREVIEW_ID,
        manifest_sha256=LEGACY_PREVIEW_V12_MANIFEST_SHA256,
        version_label="v0.12",
    )


def _v13_hydrated_crosscheck_available(contract: Mapping[str, Any]) -> bool:
    bindings = contract.get("hydrated_crosscheck_inputs")
    if not isinstance(bindings, list) or len(bindings) != 5:
        raise VerifiedConstructionCoreError(
            "v0.13 hydrated cross-check contract differs"
        )
    paths = [ROOT / str(binding.get("path", "")) for binding in bindings]
    present = [path.is_file() for path in paths]
    if any(present) and not all(present):
        raise VerifiedConstructionCoreError(
            "v0.13 hydrated cross-check inputs are only partially present"
        )
    if not all(present):
        return False
    for binding, path in zip(bindings, paths, strict=True):
        if (
            path.is_symlink()
            or path.stat().st_size != binding.get("bytes")
            or _sha256_file(path) != binding.get("sha256")
        ):
            raise VerifiedConstructionCoreError(
                "v0.13 hydrated cross-check input differs"
            )
    return True


def _v13_source_evidence(
    source: Mapping[str, Any], key: str, evidence_id: str
) -> dict[str, Any]:
    matches = [
        row
        for row in source.get("evidence", [])
        if isinstance(row, dict) and row.get("key") == key
    ]
    if len(matches) != 1:
        raise VerifiedConstructionCoreError(
            f"v0.13 source evidence differs: {key}"
        )
    row = matches[0]
    expected_id = atlas_stable_id(
        "evidence", "curated-official", key, row.get("content_hash")
    )
    if evidence_id != expected_id:
        raise VerifiedConstructionCoreError(
            f"v0.13 source evidence identifier differs: {key}"
        )
    return _v07_evidence_projection({**row, "evidence_id": evidence_id})


def _v13_capture_evidence(document: Mapping[str, Any]) -> dict[str, Any]:
    evidence_id = document.get("evidence_id")
    expected_id = atlas_stable_id(
        "evidence",
        "verified-construction-core-v0.13",
        document.get("source_id"),
        document.get("content_hash"),
    )
    if not isinstance(evidence_id, str) or evidence_id != expected_id:
        raise VerifiedConstructionCoreError(
            "v0.13 capture evidence identifier differs"
        )
    return _v07_evidence_projection(document)


def _v13_validate_geometry(record: Mapping[str, Any]) -> None:
    geometry = record.get("geometry")
    if not isinstance(geometry, dict) or set(geometry) != {
        "type",
        "coordinates",
    }:
        raise VerifiedConstructionCoreError("v0.13 geometry structure differs")
    geometry_type = geometry.get("type")
    coordinates = geometry.get("coordinates")

    def validate_position(position: Any) -> None:
        if (
            not isinstance(position, list)
            or len(position) != 2
            or any(type(value) not in {int, float} for value in position)
            or any(not math.isfinite(value) for value in position)
            or not -180 <= position[0] <= 180
            or not -90 <= position[1] <= 90
        ):
            raise VerifiedConstructionCoreError(
                "v0.13 geometry coordinate differs"
            )

    if geometry_type == "Point":
        validate_position(coordinates)
    elif geometry_type == "Polygon":
        if not isinstance(coordinates, list) or not coordinates:
            raise VerifiedConstructionCoreError(
                "v0.13 polygon structure differs"
            )
        for ring in coordinates:
            if (
                not isinstance(ring, list)
                or len(ring) < 4
                or ring[0] != ring[-1]
            ):
                raise VerifiedConstructionCoreError(
                    "v0.13 polygon ring differs"
                )
            for position in ring:
                validate_position(position)
    else:
        raise VerifiedConstructionCoreError("v0.13 geometry type differs")
    anchor = record.get("display_anchor")
    if not isinstance(anchor, dict):
        raise VerifiedConstructionCoreError("v0.13 display anchor differs")
    validate_position(anchor.get("coordinates"))


def _v13_validate_hydrated_crosscheck(
    contract: Mapping[str, Any],
    acceptances: Sequence[Mapping[str, Any]],
    sources: Mapping[str, Mapping[str, Any]],
) -> None:
    if not _v13_hydrated_crosscheck_available(contract):
        return
    pipeline = {
        row["stable_key"]: row
        for row in _load_csv(SOURCE_RELEASE / "construction_pipeline.csv")
    }
    entities = {
        row["stable_key"]: row
        for row in _load_csv(SOURCE_RELEASE / "entities.csv")
    }
    evidence = {
        row["evidence_id"]: row
        for row in _load_csv(SOURCE_RELEASE / "evidence.csv")
    }
    members = _load_csv(
        ROOT
        / "exact_identity_decisions"
        / "2026-07-22-public-open-v14"
        / "component-members.csv"
    )
    relationships = _load_csv(
        ROOT
        / "exact_identity_decisions"
        / "2026-07-22-public-open-v14"
        / "relationships.csv"
    )
    members_by_component: dict[str, list[dict[str, str]]] = defaultdict(list)
    for member in members:
        members_by_component[member["component_id"]].append(member)
    for acceptance in acceptances:
        project_key = acceptance["project_stable_key"]
        campus_key = acceptance["parent_campus_stable_key"]
        source = sources[project_key]
        status = acceptance["status"]
        project = pipeline.get(project_key)
        campus = entities.get(campus_key)
        if (
            project is None
            or campus is None
            or project["entity_id"] != acceptance["project_entity_id"]
            or campus["entity_id"] != acceptance["parent_campus_entity_id"]
            or project["status"] != status["value"]
            or project["status_as_of"] != status["as_of_date"]
            or project["status_method"] != status["method"]
            or project["status_evidence_id"] != status["evidence_id"]
        ):
            raise VerifiedConstructionCoreError(
                f"v0.13 hydrated source projection differs: {project_key}"
            )
        for evidence_binding in (
            acceptance["location_evidence"],
            {
                "key": status["evidence_key"],
                "evidence_id": status["evidence_id"],
            },
        ):
            projected = _v13_source_evidence(
                source,
                evidence_binding["key"],
                evidence_binding["evidence_id"],
            )
            hydrated = evidence.get(evidence_binding["evidence_id"])
            if hydrated is None or any(
                hydrated[field] != projected[field] for field in projected
            ):
                raise VerifiedConstructionCoreError(
                    f"v0.13 hydrated evidence projection differs: {project_key}"
                )
        project_member = next(
            (
                row
                for row in members
                if row["release_id"] == SOURCE_RELEASE_ID
                and row["stable_key"] == project_key
                and row["entity_id"] == acceptance["project_entity_id"]
            ),
            None,
        )
        relationship = acceptance["relationship"]
        topology = next(
            (
                row
                for row in relationships
                if row["relationship_id"] == relationship["relationship_id"]
                and project_member is not None
                and row["subject_component_id"]
                == project_member["component_id"]
            ),
            None,
        )
        target_members = (
            []
            if topology is None
            else members_by_component[topology["object_component_id"]]
        )
        if (
            topology is None
            or topology["relationship_type"] != "project_targets"
            or topology["decision_basis"] != "explicit_parent"
            or not any(
                row["release_id"] == SOURCE_RELEASE_ID
                and row["stable_key"] == campus_key
                and row["entity_id"] == acceptance["parent_campus_entity_id"]
                for row in target_members
            )
        ):
            raise VerifiedConstructionCoreError(
                f"v0.13 hydrated topology differs: {project_key}"
            )


def _v13_contracts() -> dict[str, Any]:
    if (
        V13_BATCH_CONTRACT.is_symlink()
        or not V13_BATCH_CONTRACT.is_file()
        or _sha256_file(V13_BATCH_CONTRACT) != V13_BATCH_CONTRACT_SHA256
        or V13_GEOMETRY_CAPTURE.is_symlink()
        or not V13_GEOMETRY_CAPTURE.is_file()
        or _sha256_file(V13_GEOMETRY_CAPTURE) != V13_GEOMETRY_CAPTURE_SHA256
    ):
        raise VerifiedConstructionCoreError("v0.13 contract hash differs")
    contract = _load_json(V13_BATCH_CONTRACT)
    capture = _load_json(V13_GEOMETRY_CAPTURE)
    capture_binding = contract.get("geometry_capture", {})
    base = contract.get("base_preview", {})
    if (
        contract.get("contract_id")
        != "verified-construction-core-v0.13-six-site-geometry-batch"
        or contract.get("reviewed_as_of")
        != CURRENT_V13_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
        or contract.get("cohort_lifecycle_reference_date")
        != CURRENT_V13_LIFECYCLE_REFERENCE_DATE.isoformat()
        or base.get("preview_id") != CURRENT_V12_PREVIEW_ID
        or base.get("manifest_sha256")
        != LEGACY_PREVIEW_V12_MANIFEST_SHA256
        or base.get("commit") != LEGACY_PREVIEW_V12_COMMIT
        or capture_binding.get("path")
        != V13_GEOMETRY_CAPTURE.relative_to(ROOT).as_posix()
        or capture_binding.get("bytes") != V13_GEOMETRY_CAPTURE.stat().st_size
        or capture_binding.get("sha256") != V13_GEOMETRY_CAPTURE_SHA256
        or capture_binding.get("capture_id")
        != "verified-construction-core-v0.13-six-site-geometry-facts"
        or capture.get("capture_id") != capture_binding.get("capture_id")
        or capture.get("captured_on")
        != CURRENT_V13_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
        or capture.get("cohort_lifecycle_reference_date")
        != CURRENT_V13_LIFECYCLE_REFERENCE_DATE.isoformat()
    ):
        raise VerifiedConstructionCoreError("v0.13 batch contract differs")
    expected_invariants = {
        "accepted_physical_site_count": 6,
        "accepted_project_count": 6,
        "geometry_authority_classes": {
            "community_mapped": 1,
            "official_source": 5,
        },
        "geometry_types": {"Point": 3, "Polygon": 3},
        "geometry_use_scopes": {
            "campus_locator": 3,
            "project_locator": 3,
        },
        "independent_imagery_verification": False,
        "official_boundary": False,
        "raw_all_rights_artifacts_redistributed": False,
    }
    if contract.get("batch_invariants") != expected_invariants:
        raise VerifiedConstructionCoreError("v0.13 batch invariants differ")
    acceptances = contract.get("acceptances")
    documents = capture.get("source_documents")
    geometries = capture.get("geometries")
    if (
        not isinstance(acceptances, list)
        or len(acceptances) != 6
        or {row.get("project_stable_key") for row in acceptances}
        != V13_PROJECT_KEYS
        or not isinstance(documents, list)
        or len(documents) != 11
        or not isinstance(geometries, list)
        or len(geometries) != 6
    ):
        raise VerifiedConstructionCoreError("v0.13 batch inventory differs")
    documents_by_id = {row.get("source_id"): row for row in documents}
    geometries_by_id = {row.get("locator_id"): row for row in geometries}
    if (
        set(documents_by_id) != set(V13_SOURCE_DOCUMENT_EXPECTATIONS)
        or set(geometries_by_id) != set(V13_GEOMETRY_EXPECTATIONS)
    ):
        raise VerifiedConstructionCoreError("v0.13 capture identities differ")
    evidence_documents: dict[str, Mapping[str, Any]] = {}
    for source_id, expected in V13_SOURCE_DOCUMENT_EXPECTATIONS.items():
        document = documents_by_id[source_id]
        raw = document.get("raw_capture")
        expected_bytes, expected_hash, expected_evidence_id, expected_license = expected
        if (
            not isinstance(raw, dict)
            or raw.get("bytes") != expected_bytes
            or raw.get("sha256") != expected_hash
            or raw.get("redistributed") is not False
            or document.get("content_hash") != expected_hash
            or document.get("evidence_id") != expected_evidence_id
            or document.get("license") != expected_license
            or not isinstance(document.get("source_url"), str)
            or not document["source_url"].startswith("https://")
        ):
            raise VerifiedConstructionCoreError(
                f"v0.13 source-document semantics differ: {source_id}"
            )
        if expected_evidence_id is not None:
            projected = _v13_capture_evidence(document)
            if projected["evidence_id"] in evidence_documents:
                raise VerifiedConstructionCoreError(
                    "v0.13 capture evidence collision"
                )
            evidence_documents[projected["evidence_id"]] = document
    if (
        set(evidence_documents) != V13_CAPTURE_EVIDENCE_IDS
        or capture_binding.get("evidence_document_count") != 9
        or capture_binding.get("geometry_record_count") != 6
    ):
        raise VerifiedConstructionCoreError(
            "v0.13 capture evidence inventory differs"
        )
    for locator_id, expected in V13_GEOMETRY_EXPECTATIONS.items():
        record = geometries_by_id[locator_id]
        semantics = record.get("semantics")
        _v13_validate_geometry(record)
        if (
            record.get("project_stable_key")
            != expected["project_stable_key"]
            or record.get("geometry_source_document_id")
            != expected["geometry_source_document_id"]
            or record.get("identity_source_document_ids")
            != expected["identity_source_document_ids"]
            or record["geometry"].get("type") != expected["geometry_type"]
            or _sha256_bytes(_json_bytes(record["geometry"]))
            != expected["geometry_sha256"]
            or record["display_anchor"].get("coordinates")
            != expected["anchor"]
            or not isinstance(semantics, dict)
            or any(
                semantics.get(field) != expected[field]
                for field in (
                    "geometry_source_entity_kind",
                    "geometry_derivation",
                    "geometry_method",
                    "geometry_scope_class",
                    "geometry_authority_class",
                    "geometry_use_scope",
                )
            )
            or semantics.get("official_boundary") is not False
            or not isinstance(semantics.get("precision_scope"), str)
            or not semantics["precision_scope"]
            or not isinstance(
                semantics.get("horizontal_uncertainty_unknown_reason"), str
            )
            or not semantics["horizontal_uncertainty_unknown_reason"]
        ):
            raise VerifiedConstructionCoreError(
                f"v0.13 geometry semantics differ: {locator_id}"
            )
    xtx_document = documents_by_id["xtx-kajaani-nls-parcel"]
    xtx_record = geometries_by_id["xtx-kajaani-parcel-205-7-4-1"]
    bitzero_document = documents_by_id[
        "bitzero-namsskogan-kartverket-parcel"
    ]
    bitzero_point = documents_by_id[
        "bitzero-namsskogan-kartverket-point"
    ]
    osm_document = documents_by_id["ezditek-ruh01-osm-way"]
    osm_changeset = documents_by_id["ezditek-ruh01-osm-changeset"]
    if (
        xtx_document.get("facts", {}).get("feature_id")
        != "FI_CP_CADASTRALPARCEL_150470261"
        or xtx_document.get("facts", {}).get("label") != "205-7-4-1"
        or xtx_document.get("facts", {}).get("vertex_count") != 264
        or "not a claim that a later live response is byte-identical"
        not in xtx_document.get("raw_capture", {}).get("volatility_notice", "")
        or xtx_record.get("transform")
        != {
            "always_xy": True,
            "proj_version": "9.5.1",
            "pyproj_version": "3.7.2",
            "source_crs": "EPSG:3067",
            "target_crs": "EPSG:4326",
        }
        or bitzero_document.get("facts", {}).get("lokalid") != 6425160282
        or bitzero_document.get("facts", {}).get("matrikkelnummertekst")
        != "50/44"
        or bitzero_point.get("facts", {}).get("coordinates")
        != [12.83406, 64.71427]
        or osm_document.get("facts", {}).get("way_id") != 1544103564
        or osm_document.get("facts", {}).get("version") != 1
        or osm_document.get("facts", {}).get("changeset") != 186307556
        or osm_document.get("facts", {}).get("tags")
        != {
            "building": "yes",
            "operator": "Ezditek",
            "ref": "RUH01",
            "telecom": "data_center",
        }
        or osm_changeset.get("facts", {}).get("imagery_used")
        != "Esri World Imagery"
        or "No Esri imagery or pixels"
        not in geometries_by_id[
            "ezditek-ruh01-osm-way-1544103564"
        ].get("imagery_guardrail", "")
    ):
        raise VerifiedConstructionCoreError(
            "v0.13 source-specific geometry facts differ"
        )
    sources: dict[str, Mapping[str, Any]] = {}
    seen_campuses: set[str] = set()
    for acceptance in acceptances:
        project_key = acceptance["project_stable_key"]
        campus_key = acceptance["parent_campus_stable_key"]
        source_binding = acceptance["source_input"]
        source_path = _repository_input(
            source_binding["path"],
            source_binding["sha256"],
            "v0.13 curated source",
        )
        if source_path.stat().st_size != source_binding["bytes"]:
            raise VerifiedConstructionCoreError(
                f"v0.13 source bytes differ: {project_key}"
            )
        source = _load_json(source_path)
        project = source.get("project")
        campus = source.get("campus")
        if (
            not isinstance(project, dict)
            or not isinstance(campus, dict)
            or project.get("stable_key") != project_key
            or campus.get("stable_key") != campus_key
            or project.get("country") != campus.get("country")
            or project.get("country") not in V13_COUNTRY_ISO_A2
            or project.get("coordinates") is not None
            or project.get("geometry") is not None
            or campus.get("coordinates") is not None
            or campus.get("geometry") is not None
            or acceptance["project_entity_id"]
            != atlas_stable_id("entity", project_key, "project")
            or acceptance["parent_campus_entity_id"]
            != atlas_stable_id("entity", campus_key, "campus")
            or campus_key in seen_campuses
        ):
            raise VerifiedConstructionCoreError(
                f"v0.13 source identity differs: {project_key}"
            )
        seen_campuses.add(campus_key)
        record = geometries_by_id.get(acceptance.get("locator_id"))
        if record is None or record.get("project_stable_key") != project_key:
            raise VerifiedConstructionCoreError(
                f"v0.13 locator identity differs: {project_key}"
            )
        relationship = acceptance.get("relationship")
        if (
            not isinstance(relationship, dict)
            or not str(relationship.get("relationship_id", "")).startswith(
                "relationship:"
            )
            or relationship.get("relationship_type") != "project_targets"
            or relationship.get("decision_basis") != "explicit_parent"
        ):
            raise VerifiedConstructionCoreError(
                f"v0.13 project-to-campus relationship differs: {project_key}"
            )
        status = acceptance.get("status")
        if not isinstance(status, dict):
            raise VerifiedConstructionCoreError(
                f"v0.13 lifecycle differs: {project_key}"
            )
        lifecycle = [
            row
            for row in source.get("lifecycle", [])
            if isinstance(row, dict)
            and row.get("entity") == "project"
            and row.get("evidence_key") == status.get("evidence_key")
        ]
        if (
            len(lifecycle) != 1
            or lifecycle[0].get("value") != status.get("value")
            or lifecycle[0].get("as_of_date") != status.get("as_of_date")
            or lifecycle[0].get("method") != status.get("method")
            or lifecycle[0].get("confidence") != status.get("confidence")
            or status.get("value") not in PHYSICAL_STATUSES
            or status.get("method") not in AUTHORITATIVE_STATUS_METHODS
            or not 0
            <= (
                CURRENT_V13_LIFECYCLE_REFERENCE_DATE
                - _calendar_date(status["as_of_date"], "v0.13 status_as_of")
            ).days
            <= MAX_STATUS_AGE_DAYS
        ):
            raise VerifiedConstructionCoreError(
                f"v0.13 lifecycle differs: {project_key}"
            )
        _v13_source_evidence(
            source, status["evidence_key"], status["evidence_id"]
        )
        location = acceptance.get("location_evidence")
        if not isinstance(location, dict):
            raise VerifiedConstructionCoreError(
                f"v0.13 location evidence differs: {project_key}"
            )
        _v13_source_evidence(source, location["key"], location["evidence_id"])
        sources[project_key] = source
    _v13_validate_hydrated_crosscheck(contract, acceptances, sources)
    return {
        "contract": contract,
        "capture": capture,
        "acceptances": sorted(
            acceptances, key=lambda row: row["project_stable_key"]
        ),
        "documents_by_id": documents_by_id,
        "geometries_by_id": geometries_by_id,
        "sources": sources,
    }


def _v13_build_delta_rows(contracts: Mapping[str, Any]) -> dict[str, Any]:
    sources = contracts["sources"]
    documents = contracts["documents_by_id"]
    geometries = contracts["geometries_by_id"]
    evidence_pool: dict[str, dict[str, Any]] = {}
    evidence_usage: dict[str, dict[str, set[str]]] = defaultdict(
        lambda: {"roles": set(), "project_ids": set()}
    )
    projects: list[dict[str, Any]] = []
    sites: list[dict[str, Any]] = []
    for acceptance in contracts["acceptances"]:
        project_key = acceptance["project_stable_key"]
        project_id = acceptance["project_entity_id"]
        campus_key = acceptance["parent_campus_stable_key"]
        source = sources[project_key]
        project = source["project"]
        campus = source["campus"]
        status = acceptance["status"]
        location = acceptance["location_evidence"]
        record = geometries[acceptance["locator_id"]]
        semantics = record["semantics"]
        for binding, role in (
            (status, "physical_status"),
            (location, "context:source_location_identity"),
        ):
            key = binding.get("evidence_key", binding.get("key"))
            evidence_id = binding["evidence_id"]
            projected = _v13_source_evidence(source, key, evidence_id)
            previous = evidence_pool.get(evidence_id)
            if previous is not None and previous != projected:
                raise VerifiedConstructionCoreError(
                    f"v0.13 evidence collision differs: {project_key}"
                )
            evidence_pool[evidence_id] = projected
            evidence_usage[evidence_id]["roles"].add(role)
            evidence_usage[evidence_id]["project_ids"].add(project_id)
        geometry_source_id = record["geometry_source_document_id"]
        for source_id, role in (
            (geometry_source_id, "geometry"),
            *(
                (source_id, "context:geometry_identity")
                for source_id in record["identity_source_document_ids"]
            ),
        ):
            document = documents[source_id]
            projected = _v13_capture_evidence(document)
            evidence_id = projected["evidence_id"]
            previous = evidence_pool.get(evidence_id)
            if previous is not None and previous != projected:
                raise VerifiedConstructionCoreError(
                    f"v0.13 capture evidence collision differs: {project_key}"
                )
            evidence_pool[evidence_id] = projected
            evidence_usage[evidence_id]["roles"].add(role)
            evidence_usage[evidence_id]["project_ids"].add(project_id)
        geometry_document = documents[geometry_source_id]
        geometry_evidence_id = geometry_document["evidence_id"]
        longitude, latitude = record["display_anchor"]["coordinates"]
        geometry = record["geometry"]
        geometry_json = _json_bytes(geometry).decode().strip()
        site_id = _stable_id("vcc-site", campus_key)
        status_date = _calendar_date(
            status["as_of_date"], "v0.13 status_as_of"
        )
        country = project["country"]
        project_row = {
            "project_id": project_id,
            "project_stable_key": project_key,
            "site_id": site_id,
            "physical_site_stable_key": campus_key,
            "name": project["name"],
            "country": country,
            "country_iso_a2": V13_COUNTRY_ISO_A2[country],
            "latitude": latitude,
            "longitude": longitude,
            "geometry_json": geometry_json,
            "geometry_type": geometry["type"],
            "geometry_source_entity_kind": semantics[
                "geometry_source_entity_kind"
            ],
            "geometry_derivation": semantics["geometry_derivation"],
            "geometry_method": semantics["geometry_method"],
            "geometry_scope_class": semantics["geometry_scope_class"],
            "geometry_authority_class": semantics[
                "geometry_authority_class"
            ],
            "geometry_use_scope": semantics["geometry_use_scope"],
            "geometry_precision_scope": semantics["precision_scope"],
            "horizontal_uncertainty_metres": "",
            "horizontal_uncertainty_unknown_reason": semantics[
                "horizontal_uncertainty_unknown_reason"
            ],
            "geometry_evidence_id": geometry_evidence_id,
            "last_observed_physical_status": status["value"],
            "status_as_of": status["as_of_date"],
            "status_age_days_at_review": (
                CURRENT_V13_LIFECYCLE_REFERENCE_DATE - status_date
            ).days,
            "status_method": status["method"],
            "status_evidence_id": status["evidence_id"],
            "verification_posture": _verification_posture(
                semantics["geometry_authority_class"],
                semantics["geometry_use_scope"],
            ),
            "independent_imagery_verification": "false",
            "imagery_review_outcome": "not_reviewed_for_core_preview",
            "development_type": "unknown",
            "development_type_unknown_reason": (
                "not selected by the v0.13 geometry batch"
            ),
            "operating_model": "unknown",
            "operating_model_unknown_reason": (
                "not established by selected evidence"
            ),
            "operating_model_evidence_id": "",
            "workloads_json": "[]",
            "workload_unknown_reason": "not established by selected evidence",
            "role_claims_json": "[]",
            "power_observations_json": "[]",
            "power_unknown_reason": (
                "no typed project power observation selected by this batch; "
                "not estimated"
            ),
            "annual_energy_observations_json": "[]",
            "annual_energy_unknown_reason": (
                "no scoped annual-energy inputs selected; not estimated"
            ),
            "efficiency_observations_json": "[]",
            "efficiency_unknown_reason": (
                "no scoped PUE or WUE observation selected"
            ),
            "owner": "",
            "operator": "",
            "users": "",
            "tenants": "",
            "customers": "",
            "status_source_url": evidence_pool[status["evidence_id"]][
                "source_url"
            ],
            "geometry_source_url": geometry_document["source_url"],
        }
        projects.append(project_row)
        sites.append(
            {
                "site_id": site_id,
                "physical_site_stable_key": campus_key,
                "name": campus["name"],
                "country": country,
                "country_iso_a2": V13_COUNTRY_ISO_A2[country],
                "latitude": latitude,
                "longitude": longitude,
                "geometry_json": geometry_json,
                "geometry_type": geometry["type"],
                "geometry_source_entity_kinds_json": _json_bytes(
                    [semantics["geometry_source_entity_kind"]]
                )
                .decode()
                .strip(),
                "geometry_derivations_json": _json_bytes(
                    [semantics["geometry_derivation"]]
                )
                .decode()
                .strip(),
                "geometry_methods_json": _json_bytes(
                    [semantics["geometry_method"]]
                )
                .decode()
                .strip(),
                "geometry_scope_classes_json": _json_bytes(
                    [semantics["geometry_scope_class"]]
                )
                .decode()
                .strip(),
                "geometry_authority_classes_json": _json_bytes(
                    [semantics["geometry_authority_class"]]
                )
                .decode()
                .strip(),
                "geometry_use_scopes_json": _json_bytes(
                    [semantics["geometry_use_scope"]]
                )
                .decode()
                .strip(),
                "geometry_precision_scopes_json": _json_bytes(
                    [semantics["precision_scope"]]
                )
                .decode()
                .strip(),
                "horizontal_uncertainty_metres": "",
                "horizontal_uncertainty_unknown_reason": semantics[
                    "horizontal_uncertainty_unknown_reason"
                ],
                "geometry_evidence_ids_json": _json_bytes(
                    [geometry_evidence_id]
                )
                .decode()
                .strip(),
                "project_count": 1,
                "project_ids_json": _json_bytes([project_id]).decode().strip(),
                "project_stable_keys_json": _json_bytes([project_key])
                .decode()
                .strip(),
                "statuses_json": _json_bytes([status["value"]])
                .decode()
                .strip(),
                "oldest_status_as_of": status["as_of_date"],
                "newest_status_as_of": status["as_of_date"],
                "verification_posture": project_row["verification_posture"],
                "independent_imagery_verification": "false",
                "imagery_review_outcomes_json": _json_bytes(
                    ["not_reviewed_for_core_preview"]
                )
                .decode()
                .strip(),
            }
        )
    evidence_rows = [
        {
            **evidence_pool[evidence_id],
            "roles_json": _json_bytes(sorted(usage["roles"])).decode().strip(),
            "project_ids_json": _json_bytes(sorted(usage["project_ids"]))
            .decode()
            .strip(),
        }
        for evidence_id, usage in sorted(evidence_usage.items())
    ]
    if (
        len(projects) != 6
        or len(sites) != 6
        or len(evidence_rows) != 16
        or len({row["project_id"] for row in projects}) != 6
        or len({row["site_id"] for row in sites}) != 6
        or len({row["evidence_id"] for row in evidence_rows}) != 16
    ):
        raise VerifiedConstructionCoreError("v0.13 delta accounting differs")
    return {"projects": projects, "sites": sites, "evidence": evidence_rows}


def _v13_portable_source_inputs(
    contracts: Mapping[str, Any],
) -> list[dict[str, Any]]:
    base_manifest = _load_json(LEGACY_PREVIEW_V12_DIR / "manifest.json")
    rows = list(base_manifest["portable_source_inputs"])
    rows.extend(
        [
            {
                "path": V13_BATCH_CONTRACT.relative_to(ROOT).as_posix(),
                "bytes": V13_BATCH_CONTRACT.stat().st_size,
                "sha256": V13_BATCH_CONTRACT_SHA256,
                "parent_manifests": [],
            },
            {
                "path": V13_GEOMETRY_CAPTURE.relative_to(ROOT).as_posix(),
                "bytes": V13_GEOMETRY_CAPTURE.stat().st_size,
                "sha256": V13_GEOMETRY_CAPTURE_SHA256,
                "parent_manifests": [],
            },
            *(
                {
                    **acceptance["source_input"],
                    "parent_manifests": [],
                }
                for acceptance in contracts["acceptances"]
            ),
        ]
    )
    rows.sort(key=lambda row: row["path"])
    if len(rows) != 89 or len({row["path"] for row in rows}) != 89:
        raise VerifiedConstructionCoreError("v0.13 portable input count differs")
    for row in rows:
        path = _repository_input(
            row["path"], row["sha256"], "v0.13 portable input"
        )
        if path.stat().st_size != row["bytes"]:
            raise VerifiedConstructionCoreError(
                "v0.13 portable input byte count differs"
            )
    return rows


def _v13_review_queue(
    contracts: Mapping[str, Any],
) -> list[dict[str, Any]]:
    rows = []
    for acceptance in contracts["acceptances"]:
        record = contracts["geometries_by_id"][acceptance["locator_id"]]
        semantics = record["semantics"]
        geometry_document = contracts["documents_by_id"][
            record["geometry_source_document_id"]
        ]
        use_scope = semantics["geometry_use_scope"]
        target_is_campus = use_scope == "campus_locator"
        rows.append(
            {
                "batch_contract_path": V13_BATCH_CONTRACT.relative_to(
                    ROOT
                ).as_posix(),
                "batch_contract_sha256": V13_BATCH_CONTRACT_SHA256,
                "decision": "accepted",
                "decision_reason": semantics["precision_scope"],
                "geometry_authority_class": semantics[
                    "geometry_authority_class"
                ],
                "geometry_derivation": semantics["geometry_derivation"],
                "geometry_entity": semantics["geometry_source_entity_kind"],
                "geometry_evidence_id": geometry_document["evidence_id"],
                "geometry_evidence_key": geometry_document["source_id"],
                "geometry_method": semantics["geometry_method"],
                "geometry_scope_class": semantics["geometry_scope_class"],
                "geometry_target_entity_id": (
                    acceptance["parent_campus_entity_id"]
                    if target_is_campus
                    else acceptance["project_entity_id"]
                ),
                "geometry_target_entity_kind": (
                    "campus" if target_is_campus else "project"
                ),
                "geometry_target_entity_stable_key": (
                    acceptance["parent_campus_stable_key"]
                    if target_is_campus
                    else acceptance["project_stable_key"]
                ),
                "geometry_use_scope": use_scope,
                "horizontal_uncertainty_metres": None,
                "horizontal_uncertainty_unknown_reason": semantics[
                    "horizontal_uncertainty_unknown_reason"
                ],
                "locator_id": acceptance["locator_id"],
                "official_boundary": False,
                "precision_scope": semantics["precision_scope"],
                "project_to_campus_decision_basis": "explicit_parent",
                "project_to_campus_relationship_id": acceptance[
                    "relationship"
                ]["relationship_id"],
                "project_to_campus_relationship_type": "project_targets",
                "rejected_claims": contracts["contract"]["rejected_claims"],
                "reviewed_at": (
                    CURRENT_V13_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
                ),
                "source_input_bytes": acceptance["source_input"]["bytes"],
                "source_input_path": acceptance["source_input"]["path"],
                "source_input_sha256": acceptance["source_input"]["sha256"],
                "source_project_entity_id": acceptance["project_entity_id"],
                "source_project_stable_key": acceptance[
                    "project_stable_key"
                ],
            }
        )
    return rows


def _v13_context_evidence_bindings(
    contracts: Mapping[str, Any],
) -> list[dict[str, Any]]:
    semantic_scopes = {
        (
            "curated:bitzero-namsskogan-data-center-campus:"
            "2026-power-infrastructure-expansion"
        ): (
            "The selected company update places transformer-foundation work at "
            "Bitzero's Namsskogan campus. SEC property disclosures bind the "
            "Bitzero/Exanorth site to property 50/44, and Kartverket supplies "
            "that parcel only as a campus locator; it is not a transformer, "
            "building, current-work, or complete-campus footprint."
        ),
        (
            "curated:ezditek-ruh01-pnu-riyadh-data-center:"
            "phase-1-current-build"
        ): (
            "First-party evidence identifies the current Phase 1 build as RUH01 "
            "at PNU. The separately captured OSM way has exact Ezditek and RUH01 "
            "tags and is used only as a community-mapped project locator, never "
            "as official or Phase 1 construction geometry."
        ),
        "curated:firstcolo-fra7-rosbach-campus:fra7-current-build": (
            "The firstcolo construction disclosure identifies FRA7 in Rosbach. "
            "The separately captured first-party FRA7 factsheet supplies the "
            "facility coordinate and exact address only as a project locator, "
            "not a building, parcel, campus, or current-work boundary."
        ),
        (
            "curated:goodman-syd01-macquarie-park-data-centre:"
            "current-single-building-development"
        ): (
            "Goodman's current page binds the SYD01 build to 2-8 Lanceley Place. "
            "The NSW application independently binds the same applicant, project "
            "name, address and lots to its reported coordinate, which remains a "
            "project locator rather than a lot or project boundary."
        ),
        "curated:qts-cambois-data-centre-campus:2026-earthworks": (
            "The QTS page identifies current earthworks at the Cambois campus. "
            "The government-hosted applicant report supplies only the wider "
            "campus's approximate centre; its separate Phase A point is rejected "
            "and no earthworks or project footprint is inferred."
        ),
        (
            "curated:xtx-markets-kajaani-data-center-campus:"
            "second-data-center"
        ): (
            "The Bravida update identifies current work on XTX's second Kajaani "
            "data center. A Kajaani permit binds XTX's worksite to parcel "
            "205-7-4-1, and NLS supplies that parcel only as a parent-campus "
            "locator, not a DC2 or complete-campus boundary."
        ),
    }
    rows = []
    for acceptance in contracts["acceptances"]:
        project_key = acceptance["project_stable_key"]
        location = acceptance["location_evidence"]
        source_input = acceptance["source_input"]
        rows.append(
            {
                "evidence_id": location["evidence_id"],
                "evidence_key": location["key"],
                "project_stable_key": project_key,
                "semantic_scope": semantic_scopes[project_key],
                "source_input_path": source_input["path"],
                "source_input_sha256": source_input["sha256"],
                "usage_role": "geometry_identity",
            }
        )
    return rows


def _v13_map_html(
    geojson: Mapping[str, Any],
    sites: Sequence[Mapping[str, Any]],
    evidence: Sequence[Mapping[str, Any]],
) -> bytes:
    document = _v12_map_html(geojson, sites, evidence).decode("utf-8")
    title = "Verified Construction Core v0.12 preview"
    if document.count(title) != 1:
        raise VerifiedConstructionCoreError("v0.13 map title seam differs")
    document = document.replace(
        title, "Verified Construction Core v0.13 preview", 1
    )
    marker = '. <a href="ATTRIBUTION.txt">Full attribution and source terms</a>.'
    notices = (
        ' · <a href="https://www.maanmittauslaitos.fi/en/opendata-licence-cc40">'
        "National Land Survey of Finland INSPIRE Cadastral Parcels</a> — "
        "CC BY 4.0 · "
        '<a href="https://kartkatalog.geonorge.no/api/getdata/'
        'fd16464c-648a-4576-83e0-231f93d687ad">Kartverket property data</a> — '
        "CC BY 4.0, © Kartverket · "
        '<a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> — '
        "ODbL 1.0, © OpenStreetMap contributors"
    )
    if document.count(marker) != 1:
        raise VerifiedConstructionCoreError(
            "v0.13 map attribution seam differs"
        )
    document = document.replace(marker, f"{notices}{marker}", 1)
    return document.encode("utf-8")


def _v13_schema() -> dict[str, Any]:
    schema = _v12_schema()
    schema["format"] = "datacenter-atlas-verified-construction-core-schema-v13"
    schema["preview_id"] = CURRENT_V13_PREVIEW_ID
    schema["v0_13_six_site_geometry_batch"] = {
        "accepted_project_count": 6,
        "geometry_authority_classes": {
            "official_source": 5,
            "community_mapped": 1,
        },
        "geometry_types": {"Point": 3, "Polygon": 3},
        "geometry_use_scopes": {
            "campus_locator": 3,
            "project_locator": 3,
        },
        "official_boundary": False,
        "coordinate_semantics": (
            "Three reviewed source points and three reviewed polygons locate six "
            "distinct physical sites. The NLS and Kartverket cadastral polygons "
            "remain parent-campus locators, and the RUH01 OSM building polygon "
            "remains a community-mapped project locator. No v0.13 geometry is a "
            "publication-target official boundary or current-work footprint."
        ),
        "horizontal_uncertainty_semantics": (
            "Every v0.13 row leaves horizontal_uncertainty_metres empty because "
            "the selected source publishes no bounded numeric value applicable "
            "to the selected geometry; transformed or converted digits do not "
            "add accuracy."
        ),
    }
    schema["v0_13_temporal_scope"] = {
        "reviewed_at_semantics": (
            "Legacy alias for cohort_lifecycle_reference_date, retained for "
            "preview compatibility; it is not the geometry-identity review date."
        ),
        "cohort_lifecycle_reference_date": (
            CURRENT_V13_LIFECYCLE_REFERENCE_DATE.isoformat()
        ),
        "geometry_identity_reviewed_at": (
            CURRENT_V13_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
        ),
    }
    return schema


def _v13_readme(report: Mapping[str, Any], legal_notice: str) -> bytes:
    return f"""# Verified Construction Core v0.13 preview

This tracked, non-final preview contains **{report['selected_physical_site_count']} physical sites**
and **{report['selected_project_count']} linked projects** in {len(report['country_counts'])}
countries. Every project retains a dated authoritative physical-status observation and a reviewed
geometry scope. Five project rows carry official boundaries; the remaining 57 are explicit project
or campus locators, never construction footprints by implication.

The six-row v0.13 delta adds Goodman SYD01, firstcolo FRA7, XTX Kajaani DC2, QTS Cambois
earthworks, Bitzero Namsskogan power-infrastructure foundations, and Ezditek RUH01 Phase 1. It
uses three source points and three source polygons. The XTX and Bitzero cadastral polygons are
official-source parent-campus locators, not official publication boundaries; the RUH01 building
polygon is community mapped and remains non-official. QTS uses only the wider-campus approximate
NGR point, never the separate Phase A point. All v0.13 lifecycle dates are no more than 90 days old
at the fixed **2026-08-20** cohort cutoff.

The tracked capture retains compact facts, derived geometry, exact raw-source byte counts and
hashes, and source-specific rights. It does not redistribute the all-rights PDFs or SEC HTML, and it
does not retain or interpret Esri imagery. The volatile NLS WFS raw-response hash identifies the
reviewed response only; it is not a promise that a later live response will be byte-identical.

The National Land Survey of Finland parcel geometry is redistributed under CC BY 4.0 with NLS
attribution. The Kartverket parcel geometry is redistributed under CC BY 4.0 with © Kartverket.
The RUH01 geometry is derived from OpenStreetMap and is made available under ODbL 1.0 with
© OpenStreetMap contributors. Applicant, issuer, and company sources remain fact-extraction-only.

Final-release gates remain explicit in `selection-report.json`. The final artifact still requires at
least 100 physical sites, 40 countries, 50 non-U.S. sites, complete imagery-review outcomes, a
20-site blind review, and a clean-clone rebuild. Until every gate passes,
`publishable_as_final` is false.

## Files

- `projects.csv`: one row per selected physical construction project
- `sites.csv`: one row per distinct physical site
- `sites.geojson`: reviewed locator or boundary geometry for each physical site
- `evidence.csv`: locally bound evidence and exact usage roles
- `selection-report.json`: cohort, provenance, rejection, and release-gate audit
- `schema.json`: field and v0.13 scope semantics
- `map.html`: deterministic offline map; no tile requests
- `manifest.json` and `manifest.sha256`: byte inventory and trust root
- `ATTRIBUTION.txt`: source-specific attribution and terms

## Legal notice

{legal_notice}

This preview is an evidence product, not legal, surveying, investment, engineering, or operational
advice. Unknowns remain unknown, locators are not silently promoted to footprints, and no capacity,
role, workload, energy, efficiency, or lifecycle claim is inferred from geometry. The preview is
not final while `publishable_as_final` is false.
""".encode("utf-8")


def _v13_attribution(
    evidence: Sequence[Mapping[str, Any]], legal_notice: str
) -> bytes:
    document = _v12_attribution(evidence, legal_notice).decode("utf-8")
    heading = "Data Center Atlas Verified Construction Core v0.12 preview"
    if document.count(heading) != 1:
        raise VerifiedConstructionCoreError(
            "v0.13 attribution heading seam differs"
        )
    document = document.replace(
        heading,
        "Data Center Atlas Verified Construction Core v0.13 preview",
        1,
    )
    marker = "Map-provider terms:\n"
    notice = (
        "v0.13 geometry notice: Contains data from the National Land Survey "
        "of Finland INSPIRE Cadastral Parcels (CP) WFS, licensed under the "
        "National Land Survey open data Attribution CC 4.0 licence; retrieved "
        "2026-08-23. Kartverket property geometry is CC BY 4.0, © Kartverket. "
        "RUH01 geometry is © OpenStreetMap contributors and ODbL 1.0. Raw "
        "all-rights PDFs and issuer HTML are not redistributed or relicensed; "
        "only compact facts and hashes are retained. No Esri imagery or pixels "
        "are redistributed or interpreted.\n\n"
    )
    if document.count(marker) != 1:
        raise VerifiedConstructionCoreError(
            "v0.13 attribution notice seam differs"
        )
    document = document.replace(marker, f"{notice}{marker}", 1)
    return document.encode("utf-8")


def _v13_payloads() -> tuple[dict[str, bytes], dict[str, Any], list[dict[str, Any]]]:
    validate_frozen_v12(LEGACY_PREVIEW_V12_DIR)
    contracts = _v13_contracts()
    base_projects = _load_csv(
        LEGACY_PREVIEW_V12_DIR / "projects.csv", PROJECT_FIELDS
    )
    base_sites = _load_csv(LEGACY_PREVIEW_V12_DIR / "sites.csv", SITE_FIELDS)
    base_evidence = _load_csv(
        LEGACY_PREVIEW_V12_DIR / "evidence.csv", EVIDENCE_FIELDS
    )
    base_report = _load_json(
        LEGACY_PREVIEW_V12_DIR / "selection-report.json"
    )
    delta = _v13_build_delta_rows(contracts)
    if (
        {row["project_id"] for row in base_projects}
        & {row["project_id"] for row in delta["projects"]}
        or {row["site_id"] for row in base_sites}
        & {row["site_id"] for row in delta["sites"]}
        or {row["evidence_id"] for row in base_evidence}
        & {row["evidence_id"] for row in delta["evidence"]}
    ):
        raise VerifiedConstructionCoreError("v0.13 delta identity collision")
    projects = sorted(
        [*base_projects, *delta["projects"]],
        key=lambda row: row["project_stable_key"],
    )
    sites = sorted(
        [*base_sites, *delta["sites"]],
        key=lambda row: row["physical_site_stable_key"],
    )
    evidence = sorted(
        [*base_evidence, *delta["evidence"]],
        key=lambda row: row["evidence_id"],
    )
    if (
        len(projects) != 62
        or len(sites) != 59
        or len(evidence) != 145
        or len({row["project_id"] for row in projects}) != 62
        or len({row["site_id"] for row in sites}) != 59
        or len({row["evidence_id"] for row in evidence}) != 145
    ):
        raise VerifiedConstructionCoreError(
            "v0.13 cohort count or uniqueness differs"
        )
    project_by_id = {row["project_id"]: row for row in projects}
    site_by_id = {row["site_id"]: row for row in sites}
    evidence_by_id = {row["evidence_id"]: row for row in evidence}
    if (
        any(project_by_id[row["project_id"]] != row for row in base_projects)
        or any(site_by_id[row["site_id"]] != row for row in base_sites)
        or any(
            evidence_by_id[row["evidence_id"]] != row
            for row in base_evidence
        )
    ):
        raise VerifiedConstructionCoreError("v0.13 inherited row changed")
    gates = _final_release_gates(sites, projects)
    gates["clean_clone_rebuild"] = {
        "passed": True,
        "reason": (
            "all current-delta inputs and the frozen base artifact are tracked "
            "and hash-bound"
        ),
    }
    country_counts = dict(
        sorted(Counter(row["country"] for row in sites).items())
    )
    provenance_decisions = {
        key: list(base_report["provenance_decisions"][key])
        for key in (
            "workload_scope_bindings",
            "role_bindings",
            "excluded_source_roles",
            "context_evidence_bindings",
        )
    }
    provenance_decisions["context_evidence_bindings"].extend(
        _v13_context_evidence_bindings(contracts)
    )
    report = {
        **base_report,
        "format": "datacenter-atlas-verified-construction-core-selection-v13",
        "publishable_as_final": all(
            gate.get("passed", False) for gate in gates.values()
        ),
        "reviewed_at": CURRENT_V13_LIFECYCLE_REFERENCE_DATE.isoformat(),
        "cohort_lifecycle_reference_date": (
            CURRENT_V13_LIFECYCLE_REFERENCE_DATE.isoformat()
        ),
        "geometry_identity_reviewed_at": (
            CURRENT_V13_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
        ),
        "selected_project_count": 62,
        "selected_physical_site_count": 59,
        "official_boundary_project_count": 5,
        "reviewed_site_locator_project_count": 57,
        "non_selected_source_row_count": 469,
        "selection_first_failure_counts": V13_SELECTION_FIRST_FAILURE_COUNTS,
        "country_counts": country_counts,
        "final_release_gates": gates,
        "imagery_review_provenance": list(
            base_report["imagery_review_provenance"]
        ),
        "provenance_decisions": provenance_decisions,
        "reviewed_overlay_queue": [
            *base_report["reviewed_overlay_queue"],
            *_v13_review_queue(contracts),
        ],
        "six_site_geometry_batch": {
            "accepted_project_count": 6,
            "accepted_physical_site_count": 6,
            "batch_contract_path": V13_BATCH_CONTRACT.relative_to(
                ROOT
            ).as_posix(),
            "batch_contract_sha256": V13_BATCH_CONTRACT_SHA256,
            "capture_path": V13_GEOMETRY_CAPTURE.relative_to(ROOT).as_posix(),
            "capture_sha256": V13_GEOMETRY_CAPTURE_SHA256,
            "capture_source_document_count": 11,
            "geometry_authority_classes": {
                "official_source": 5,
                "community_mapped": 1,
            },
            "geometry_types": {"Point": 3, "Polygon": 3},
            "geometry_use_scopes": {
                "campus_locator": 3,
                "project_locator": 3,
            },
            "official_boundary": False,
            "project_stable_keys": sorted(V13_PROJECT_KEYS),
            "rejected_claims": contracts["contract"]["rejected_claims"],
        },
    }
    delta_by_key = {
        row["project_stable_key"]: row for row in delta["projects"]
    }
    if (
        len(country_counts) != 27
        or country_counts.get("United States") != 13
        or country_counts.get("Saudi Arabia") != 1
        or sum(row["country_iso_a2"] != "US" for row in sites) != 46
        or sum(_is_official_boundary(row) for row in projects) != 5
        or sum(_is_reviewed_locator(row) for row in projects) != 57
        or sum(V13_SELECTION_FIRST_FAILURE_COUNTS.values()) != 531
        or gates["imagery_outcomes_complete"]
        != {"actual": 10, "required": 62, "passed": False}
        or len(provenance_decisions["context_evidence_bindings"]) != 20
        or len(provenance_decisions["excluded_source_roles"]) != 12
        or len(report["reviewed_overlay_queue"]) != 49
        or set(delta_by_key) != V13_PROJECT_KEYS
        or any(
            row["geometry_use_scope"]
            not in {"campus_locator", "project_locator"}
            or row["workloads_json"] != "[]"
            or row["role_claims_json"] != "[]"
            or row["power_observations_json"] != "[]"
            or row["annual_energy_observations_json"] != "[]"
            or row["efficiency_observations_json"] != "[]"
            or row["operating_model"] != "unknown"
            or row["independent_imagery_verification"] != "false"
            or any(
                row[field]
                for field in (
                    "owner",
                    "operator",
                    "users",
                    "tenants",
                    "customers",
                )
            )
            for row in delta_by_key.values()
        )
    ):
        raise VerifiedConstructionCoreError("v0.13 expected accounting differs")
    geojson = _geojson(sites)
    geojson["name"] = (
        "Data Center Atlas Verified Construction Core v0.13 preview"
    )
    delta_features = {
        row["properties"]["physical_site_stable_key"]: row
        for row in geojson["features"]
        if row["properties"]["physical_site_stable_key"]
        in {
            acceptance["parent_campus_stable_key"]
            for acceptance in contracts["acceptances"]
        }
    }
    if (
        len(delta_features) != 6
        or Counter(
            feature["geometry"]["type"]
            for feature in delta_features.values()
        )
        != {"Point": 3, "Polygon": 3}
    ):
        raise VerifiedConstructionCoreError(
            "v0.13 geometry publication differs"
        )
    legal_notice = _v10_city_legal_notice_from_base()
    payloads = {
        "ATTRIBUTION.txt": _v13_attribution(evidence, legal_notice),
        "README.md": _v13_readme(report, legal_notice),
        "evidence.csv": _csv_bytes(evidence, EVIDENCE_FIELDS),
        "map.html": _v13_map_html(geojson, sites, evidence),
        "projects.csv": _csv_bytes(projects, PROJECT_FIELDS),
        "schema.json": _json_bytes(_v13_schema()),
        "selection-report.json": _json_bytes(report),
        "sites.csv": _csv_bytes(sites, SITE_FIELDS),
        "sites.geojson": _json_bytes(geojson),
    }
    portable = _v13_portable_source_inputs(contracts)
    return payloads, report, portable


def _v13_manifest(
    payloads: Mapping[str, bytes],
    report: Mapping[str, Any],
    portable: Sequence[Mapping[str, Any]],
) -> dict[str, Any]:
    return {
        "format": "datacenter-atlas-verified-construction-core-preview-v13",
        "preview_id": CURRENT_V13_PREVIEW_ID,
        "release_status": "preview",
        "publishable_as_final": report["publishable_as_final"],
        "base_preview_id": CURRENT_V12_PREVIEW_ID,
        "base_preview_manifest_sha256": LEGACY_PREVIEW_V12_MANIFEST_SHA256,
        "base_preview_commit": LEGACY_PREVIEW_V12_COMMIT,
        "source_release_id": SOURCE_RELEASE_ID,
        "source_release_manifest_path": (
            "releases/2026-07-22-open-seed-v97/manifest.json"
        ),
        "source_release_manifest_sha256": (
            "0a6f41f4239944df27f2ce70e81a089b91cec401f154bbae28412b27a4d00fdd"
        ),
        "definition_paths": {
            "six_site_geometry_batch": V13_BATCH_CONTRACT.relative_to(
                ROOT
            ).as_posix()
        },
        "six_site_geometry_batch_sha256": V13_BATCH_CONTRACT_SHA256,
        "geometry_capture": {
            "path": V13_GEOMETRY_CAPTURE.relative_to(ROOT).as_posix(),
            "bytes": V13_GEOMETRY_CAPTURE.stat().st_size,
            "sha256": V13_GEOMETRY_CAPTURE_SHA256,
            "evidence_ids": sorted(V13_CAPTURE_EVIDENCE_IDS),
        },
        "portable_source_inputs": list(portable),
        "reviewed_at": CURRENT_V13_LIFECYCLE_REFERENCE_DATE.isoformat(),
        "cohort_lifecycle_reference_date": (
            CURRENT_V13_LIFECYCLE_REFERENCE_DATE.isoformat()
        ),
        "geometry_identity_reviewed_at": (
            CURRENT_V13_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
        ),
        "counts": {
            "physical_sites": 59,
            "projects": 62,
            "evidence": 145,
            "countries": 27,
            "non_us_sites": 46,
            "official_boundary_projects": 5,
            "reviewed_site_locator_projects": 57,
        },
        "files": {
            name: {"bytes": len(payload), "sha256": _sha256_bytes(payload)}
            for name, payload in sorted(payloads.items())
        },
    }


def build_preview(output_dir: Path = CURRENT_V13_PREVIEW_DIR) -> dict[str, Any]:
    """Build v0.13 from byte-frozen v0.12 and tracked hash-bound inputs."""
    output_dir = Path(output_dir)
    if output_dir.exists():
        raise VerifiedConstructionCoreError(f"refusing to overwrite {output_dir}")
    payloads, report, portable = _v13_payloads()
    manifest = _v13_manifest(payloads, report, portable)
    manifest_bytes = _json_bytes(manifest)
    complete = {
        **payloads,
        "manifest.json": manifest_bytes,
        "manifest.sha256": (
            f"{_sha256_bytes(manifest_bytes)}  manifest.json\n".encode()
        ),
    }
    output_dir.parent.mkdir(parents=True, exist_ok=True)
    stage = Path(
        tempfile.mkdtemp(prefix=f".{output_dir.name}.", dir=output_dir.parent)
    )
    try:
        for name, payload in complete.items():
            with (stage / name).open("xb") as handle:
                handle.write(payload)
        os.replace(stage, output_dir)
    except Exception:
        shutil.rmtree(stage, ignore_errors=True)
        raise
    validate_preview(output_dir)
    return manifest


def _validate_v13_preview(path: Path) -> dict[str, Any]:
    path = Path(path)
    if path.is_symlink() or not path.is_dir():
        raise VerifiedConstructionCoreError("v0.13 preview directory differs")
    manifest_path = path / "manifest.json"
    checksum_path = path / "manifest.sha256"
    if (
        manifest_path.is_symlink()
        or not manifest_path.is_file()
        or checksum_path.is_symlink()
        or not checksum_path.is_file()
    ):
        raise VerifiedConstructionCoreError(
            "v0.13 manifest trust root differs"
        )
    manifest_bytes = manifest_path.read_bytes()
    if checksum_path.read_bytes() != (
        f"{_sha256_bytes(manifest_bytes)}  manifest.json\n".encode()
    ):
        raise VerifiedConstructionCoreError("v0.13 manifest checksum differs")
    manifest = _load_json(manifest_path)
    files = manifest.get("files") if isinstance(manifest, dict) else None
    if (
        not isinstance(manifest, dict)
        or manifest.get("format")
        != "datacenter-atlas-verified-construction-core-preview-v13"
        or manifest.get("preview_id") != CURRENT_V13_PREVIEW_ID
        or manifest.get("release_status") != "preview"
        or type(manifest.get("publishable_as_final")) is not bool
        or manifest["publishable_as_final"] is not False
        or manifest.get("base_preview_manifest_sha256")
        != LEGACY_PREVIEW_V12_MANIFEST_SHA256
        or manifest.get("base_preview_commit") != LEGACY_PREVIEW_V12_COMMIT
        or not isinstance(files, dict)
    ):
        raise VerifiedConstructionCoreError("v0.13 manifest files differ")
    expected_inventory = set(files) | {"manifest.json", "manifest.sha256"}
    if {item.name for item in path.iterdir()} != expected_inventory:
        raise VerifiedConstructionCoreError("v0.13 preview inventory differs")
    for name, metadata in files.items():
        member = path / name
        if (
            Path(name).name != name
            or member.is_symlink()
            or not member.is_file()
            or type(metadata.get("bytes")) is not int
            or member.stat().st_size != metadata["bytes"]
            or _sha256_file(member) != metadata.get("sha256")
        ):
            raise VerifiedConstructionCoreError(
                f"v0.13 member differs: {name}"
            )
    payloads, report, portable = _v13_payloads()
    if manifest != _v13_manifest(payloads, report, portable):
        raise VerifiedConstructionCoreError("v0.13 manifest semantics differ")
    for name, expected in payloads.items():
        if (path / name).read_bytes() != expected:
            raise VerifiedConstructionCoreError(
                f"v0.13 generated member differs: {name}"
            )
    schema = _load_json(path / "schema.json")
    batch_schema = schema.get("v0_13_six_site_geometry_batch", {})
    if (
        batch_schema.get("accepted_project_count") != 6
        or batch_schema.get("official_boundary") is not False
        or batch_schema.get("geometry_use_scopes")
        != {"campus_locator": 3, "project_locator": 3}
    ):
        raise VerifiedConstructionCoreError(
            "v0.13 geometry schema seam differs"
        )
    notices = (
        b"National Land Survey",
        b"Kartverket",
        b"OpenStreetMap",
    )
    for name in ("ATTRIBUTION.txt", "README.md", "map.html"):
        content = (path / name).read_bytes()
        if any(notice not in content for notice in notices):
            raise VerifiedConstructionCoreError(
                f"v0.13 attribution notice missing from {name}"
            )
    projects = {
        row["project_stable_key"]: row
        for row in _load_csv(path / "projects.csv", PROJECT_FIELDS)
        if row["project_stable_key"] in V13_PROJECT_KEYS
    }
    if set(projects) != V13_PROJECT_KEYS:
        raise VerifiedConstructionCoreError("v0.13 project inventory differs")
    contracts = _v13_contracts()
    evidence = {
        row["evidence_id"]: row
        for row in _load_csv(path / "evidence.csv", EVIDENCE_FIELDS)
    }
    for acceptance in contracts["acceptances"]:
        project_key = acceptance["project_stable_key"]
        project = projects[project_key]
        record = contracts["geometries_by_id"][acceptance["locator_id"]]
        expected = V13_GEOMETRY_EXPECTATIONS[acceptance["locator_id"]]
        semantics = record["semantics"]
        geometry_document = contracts["documents_by_id"][
            record["geometry_source_document_id"]
        ]
        geometry_evidence = evidence.get(geometry_document["evidence_id"])
        if (
            project["geometry_type"] != expected["geometry_type"]
            or _sha256_bytes(
                _json_bytes(
                    _parse_json_field(
                        project["geometry_json"], "v0.13 project geometry"
                    )
                )
            )
            != expected["geometry_sha256"]
            or project["geometry_source_entity_kind"]
            != expected["geometry_source_entity_kind"]
            or project["geometry_derivation"]
            != expected["geometry_derivation"]
            or project["geometry_method"] != expected["geometry_method"]
            or project["geometry_scope_class"]
            != expected["geometry_scope_class"]
            or project["geometry_authority_class"]
            != expected["geometry_authority_class"]
            or project["geometry_use_scope"]
            != expected["geometry_use_scope"]
            or project["geometry_precision_scope"]
            != semantics["precision_scope"]
            or project["horizontal_uncertainty_metres"]
            or project["independent_imagery_verification"] != "false"
            or project["verification_posture"]
            != "recent_authoritative_physical_observation_plus_reviewed_site_locator"
            or geometry_evidence is None
            or "geometry"
            not in _parse_json_field(
                geometry_evidence["roles_json"], "v0.13 geometry roles"
            )
            or project["project_id"]
            not in _parse_json_field(
                geometry_evidence["project_ids_json"],
                "v0.13 geometry projects",
            )
            or any(
                project[field] != "[]"
                for field in (
                    "workloads_json",
                    "role_claims_json",
                    "power_observations_json",
                    "annual_energy_observations_json",
                    "efficiency_observations_json",
                )
            )
            or any(
                project[field]
                for field in (
                    "owner",
                    "operator",
                    "users",
                    "tenants",
                    "customers",
                )
            )
        ):
            raise VerifiedConstructionCoreError(
                f"v0.13 project semantic scope differs: {project_key}"
            )
    base_projects = {
        row["project_id"]: row
        for row in _load_csv(
            LEGACY_PREVIEW_V12_DIR / "projects.csv", PROJECT_FIELDS
        )
    }
    current_projects = {
        row["project_id"]: row
        for row in _load_csv(path / "projects.csv", PROJECT_FIELDS)
    }
    if any(current_projects[row_id] != row for row_id, row in base_projects.items()):
        raise VerifiedConstructionCoreError("v0.13 inherited project changed")
    return manifest


def validate_preview(path: Path = CURRENT_V13_PREVIEW_DIR) -> dict[str, Any]:
    """Validate current v0.13 or dispatch v0.1-v0.12 via frozen seams."""
    path = Path(path)
    manifest_path = path / "manifest.json"
    if (
        path.is_symlink()
        or not path.is_dir()
        or manifest_path.is_symlink()
        or not manifest_path.is_file()
    ):
        raise VerifiedConstructionCoreError("preview manifest trust root differs")
    manifest = _load_json(manifest_path)
    preview_id = manifest.get("preview_id") if isinstance(manifest, dict) else None
    if preview_id == CURRENT_V13_PREVIEW_ID:
        return _validate_v13_preview(path)
    return _validate_v12_preview_dispatch(path)


__all__ = [
    *__all__,
    "CURRENT_V13_PREVIEW_DIR",
    "CURRENT_V13_PREVIEW_ID",
    "CURRENT_V13_GEOMETRY_IDENTITY_REVIEW_DATE",
    "CURRENT_V13_LIFECYCLE_REFERENCE_DATE",
    "CURRENT_V13_REVIEW_DATE",
    "LEGACY_PREVIEW_V12_COMMIT",
    "LEGACY_PREVIEW_V12_DIR",
    "LEGACY_PREVIEW_V12_MANIFEST_SHA256",
    "V13_BATCH_CONTRACT",
    "V13_BATCH_CONTRACT_SHA256",
    "V13_CAPTURE_EVIDENCE_IDS",
    "V13_GEOMETRY_CAPTURE",
    "V13_GEOMETRY_CAPTURE_SHA256",
    "V13_PROJECT_KEYS",
    "validate_frozen_v12",
]


_build_v13_preview = build_preview
_validate_v13_preview_dispatch = validate_preview
build_v13_preview = _build_v13_preview

LEGACY_PREVIEW_V13_DIR = CURRENT_V13_PREVIEW_DIR
LEGACY_PREVIEW_V13_MANIFEST_SHA256 = (
    "28f291619658f7e678f5b888b5475d762366ada8138244b79341c6abab1462fb"
)
LEGACY_PREVIEW_V13_COMMIT = "cbeaa15121e909e93b9698cfd90deabc2ece215f"
CURRENT_V14_PREVIEW_ID = "2026-08-20-preview-v0.14"
CURRENT_V14_PREVIEW_DIR = ROOT / "verified_construction_core" / CURRENT_V14_PREVIEW_ID
CURRENT_V14_LIFECYCLE_REFERENCE_DATE = date(2026, 8, 20)
CURRENT_V14_GEOMETRY_IDENTITY_REVIEW_DATE = date(2026, 8, 23)
CURRENT_V14_REVIEW_DATE = CURRENT_V14_LIFECYCLE_REFERENCE_DATE
V14_BATCH_CONTRACT = (
    ROOT
    / "definitions"
    / "verified-construction-core-v0.14-nine-us-address-locator-batch.json"
)
V14_BATCH_CONTRACT_SHA256 = (
    "b28e0a9589b8bb296589592e073ad9c948161eb04901c2a0ef899368968f8ce3"
)
V14_GEOMETRY_CAPTURE = (
    ROOT
    / "sources"
    / "verified-construction-core-v0.14-us-address-locator-facts.json"
)
V14_GEOMETRY_CAPTURE_SHA256 = (
    "7b93cad5f094a9380efc02f174e3466b7e641ddf9c3ec7e556b27b946a93a835"
)
V14_PROJECT_KEYS = frozenset(
    {
        "curated:bitdeer-massillon-ohio-campus:2026-fire-damaged-buildings-reconstruction",
        "curated:bitdeer-wenatchee-washington-campus:2026-ai-conversion-site-preparation",
        "curated:jefferson-lab-newport-news-campus:jldc-building",
        "curated:microsoft-alviso-san-jose-datacenter-campus:phase-2-greenfield-development",
        "curated:microsoft-heath-oh-datacenter:initial-site-preparation",
        "curated:microsoft-hebron-oh-datacenter:initial-site-preparation",
        "curated:microsoft-new-albany-oh-datacenter:initial-site-preparation",
        "curated:qts-cedar-rapids-data-center-campus:current-campus-development",
        "curated:sabey-sdc-austin-round-rock-campus:building-b-current-build",
    }
)
V14_SELECTION_FIRST_FAILURE_COUNTS = {
    "entity_kind_not_project": 49,
    "not_in_reviewed_site_geometry_allowlist": 72,
    "selected": 71,
    "status_not_physical": 12,
    "status_outside_90_day_window": 327,
}
V14_CAPTURE_EVIDENCE_IDS = frozenset(
    {
        "1de91fed-9b38-5c47-886d-5fa766be2aa0",
        "2803efb1-75a9-5146-8bbd-483714ab9841",
        "34a0d6a4-02e3-57e3-8b39-6c7e64363b3a",
        "35c41e3e-909b-57dd-b9a0-879467ec17cb",
        "3d7442c7-1c78-5134-8b9c-12581486464e",
        "5b7da16c-3a43-5e42-97ad-816ae894e900",
        "626c5c59-9386-58ac-9de1-f81bc82d4900",
        "6a672c19-631c-578d-8988-5d1d7287ab6a",
        "7f58e6cc-7e09-55a4-8202-38d31f72d03d",
        "8d48305b-09aa-5bef-bd06-ebaf0ba48924",
        "a31b2e5c-d82b-52aa-aa82-f330ee5d689a",
        "a45fecbd-1054-5b4f-9b28-57e440c00a0c",
        "a6b1e5df-d16d-5edd-a0cc-2c4e6ae25110",
        "b4bdfbf2-b320-5186-8978-0076403b94c3",
        "c0c12077-d7b0-52e3-b28e-36e17780232d",
        "fa1a4303-efd0-5426-adb3-2878bca8bb28",
    }
)
V14_SOURCE_DOCUMENT_EXPECTATIONS = {
    "bitdeer-massillon-sec-property-exhibit": (
        82918,
        "d807149ac1adf344c0da21c1c9295f2bc667304a2523a9d858c99a289a8bdda0",
        "all-rights-reserved-fact-extraction-only",
    ),
    "bitdeer-wenatchee-sec-property-exhibit": (
        338418,
        "9d35bc4a6e992c7b8f49ce0c79e3d404aa1f61a40302e658ae540cf8a54d1b21",
        "all-rights-reserved-fact-extraction-only",
    ),
    "census-bitdeer-massillon-address-match": (
        5032,
        "41868595018e02af291eaf247ad5b495b729685b0a5be0bf944ed427096c8fb7",
        "public-domain-us-government",
    ),
    "census-bitdeer-wenatchee-address-match": (
        3970,
        "c002b13bb9723ebc52fa67512636ee29a13cadd5ebe9d2df050ce76e4606ae37",
        "public-domain-us-government",
    ),
    "census-jefferson-lab-address-match": (
        5131,
        "b1b7f05939322797233febc86f63dd51296c13424a3d2f32d13fdbee4087b2a6",
        "public-domain-us-government",
    ),
    "census-microsoft-heath-address-match": (
        4277,
        "75521d137c83d066a96548ad00594813dc9a22e7c1c57170d6ecf66191bd14da",
        "public-domain-us-government",
    ),
    "census-microsoft-hebron-address-match": (
        5022,
        "70c9ee69586590257deb71dc27cb0878d8d205b8c16be886dc909e8e00b37833",
        "public-domain-us-government",
    ),
    "census-microsoft-new-albany-address-match": (
        4728,
        "639b956e41113b6ebc44c5c7e9da794248c1222203fd287c250cbdbbbc99c025",
        "public-domain-us-government",
    ),
    "census-qts-cedar-rapids-address-match": (
        4710,
        "4278336c8a64459e98f0ba2ad67c300ff6c64e0357d8db6e437f28dc9a8405e7",
        "public-domain-us-government",
    ),
    "census-sabey-austin-address-match": (
        4708,
        "6815c7a64fb9ed59ceee8a132aef1ad4fee57ddb3c260fdeba09e5673592d666",
        "public-domain-us-government",
    ),
    "licking-county-microsoft-owner-address-facts": (
        5008,
        "7074823e9d79249fef18aba0774be26a8dc34a49a5a9d281c13973ce020cd6c5",
        "rights-status-uncertain-fact-extraction-only",
    ),
    "qts-cedar-rapids-first-party-map-pin": (
        318912,
        "bdcb839b965128700a65a2dd34e2c2c0d119659f7c397e3161fceaf07859e9e0",
        "all-rights-reserved-fact-extraction-only",
    ),
    "san-jose-alviso-address-point": (
        2060,
        "24cd686fd7dc424fc29639e238c2480aa1c1472d572168bfdab0e7a3ccf9f634",
        "city-open-data-disclaimer",
    ),
    "san-jose-alviso-private-development-record": (
        2292,
        "7ce046444d2a1ee327d8994d17d2d13a2016367b8c43c2aef047eab52ec1da08",
        "rights-status-disclaimer-fact-extraction-only",
    ),
    "village-of-hebron-microsoft-data-center-minutes": (
        116645,
        "1fa58cfb5e19b8d95b18284001325a0d1cc5baa07204017265abdd61a89dc21e",
        "rights-status-uncertain-fact-extraction-only",
    ),
    "washington-current-parcels-wenatchee-address": (
        752,
        "b93425b1680a8fdf0f3bb8d905676b583f1cc2e782c3bd244e6d319863aa23fd",
        "use-restrictions-fact-extraction-only",
    ),
}
V14_IDENTITY_SOURCE_EXPECTATIONS = {
    "bitdeer-massillon-577-oberlin-avenue": [
        "bitdeer-massillon-sec-property-exhibit"
    ],
    "bitdeer-wenatchee-624-urban-industrial-way": [
        "washington-current-parcels-wenatchee-address",
        "bitdeer-wenatchee-sec-property-exhibit",
    ],
    "jefferson-lab-12000-jefferson-avenue": [],
    "microsoft-alviso-city-address-point": [
        "san-jose-alviso-private-development-record"
    ],
    "microsoft-heath-975-thornwood-census-point": [
        "licking-county-microsoft-owner-address-facts"
    ],
    "microsoft-hebron-323-n-high-census-point": [
        "licking-county-microsoft-owner-address-facts",
        "village-of-hebron-microsoft-data-center-minutes",
    ],
    "microsoft-new-albany-3287-beech-census-point": [
        "licking-county-microsoft-owner-address-facts"
    ],
    "qts-cedar-rapids-dc1-first-party-pin": [
        "census-qts-cedar-rapids-address-match"
    ],
    "sabey-austin-1300-louis-henna-boulevard": [],
}
V14_RESULT_EXPECTATIONS = {
    "bitdeer-massillon-577-oberlin-avenue": (
        -81.522276076625,
        40.780446738591,
        "census-bitdeer-massillon-address-match",
        "census",
    ),
    "bitdeer-wenatchee-624-urban-industrial-way": (
        -120.209264185842,
        47.413902371239,
        "census-bitdeer-wenatchee-address-match",
        "census",
    ),
    "jefferson-lab-12000-jefferson-avenue": (
        -76.48969366502,
        37.098357742086,
        "census-jefferson-lab-address-match",
        "census",
    ),
    "microsoft-alviso-city-address-point": (
        -121.92873874095102,
        37.425200580527488,
        "san-jose-alviso-address-point",
        "alviso",
    ),
    "microsoft-heath-975-thornwood-census-point": (
        -82.480274580568,
        40.025835523589,
        "census-microsoft-heath-address-match",
        "census",
    ),
    "microsoft-hebron-323-n-high-census-point": (
        -82.488898833962,
        39.972018466398,
        "census-microsoft-hebron-address-match",
        "census",
    ),
    "microsoft-new-albany-3287-beech-census-point": (
        -82.753523444119,
        40.103804132848,
        "census-microsoft-new-albany-address-match",
        "census",
    ),
    "qts-cedar-rapids-dc1-first-party-pin": (
        -91.754301,
        41.907294,
        "qts-cedar-rapids-first-party-map-pin",
        "first_party",
    ),
    "sabey-austin-1300-louis-henna-boulevard": (
        -97.650967159813,
        30.48575752775,
        "census-sabey-austin-address-match",
        "census",
    ),
}


def validate_frozen_v13(
    path: Path = LEGACY_PREVIEW_V13_DIR,
) -> dict[str, Any]:
    """Validate byte-frozen v0.13 without regenerating it."""
    return _validate_frozen_preview_inventory(
        path,
        preview_id=CURRENT_V13_PREVIEW_ID,
        manifest_sha256=LEGACY_PREVIEW_V13_MANIFEST_SHA256,
        version_label="v0.13",
    )


def _v14_hydrated_crosscheck_available(contract: Mapping[str, Any]) -> bool:
    bindings = contract.get("hydrated_crosscheck_inputs")
    if not isinstance(bindings, list) or len(bindings) != 5:
        raise VerifiedConstructionCoreError(
            "v0.14 hydrated cross-check contract differs"
        )
    paths = [ROOT / str(binding.get("path", "")) for binding in bindings]
    present = [path.is_file() for path in paths]
    if any(present) and not all(present):
        raise VerifiedConstructionCoreError(
            "v0.14 hydrated cross-check inputs are only partially present"
        )
    if not all(present):
        return False
    for binding, path in zip(bindings, paths, strict=True):
        if (
            path.is_symlink()
            or path.stat().st_size != binding.get("bytes")
            or _sha256_file(path) != binding.get("sha256")
        ):
            raise VerifiedConstructionCoreError(
                "v0.14 hydrated cross-check input differs"
            )
    return True


def _v14_source_evidence(
    source: Mapping[str, Any], key: str, evidence_id: str
) -> dict[str, Any]:
    matches = [
        row
        for row in source.get("evidence", [])
        if isinstance(row, dict) and row.get("key") == key
    ]
    if len(matches) != 1:
        raise VerifiedConstructionCoreError(
            f"v0.14 source evidence differs: {key}"
        )
    row = matches[0]
    expected_id = atlas_stable_id(
        "evidence", "curated-official", key, row.get("content_hash")
    )
    if evidence_id != expected_id:
        raise VerifiedConstructionCoreError(
            f"v0.14 source evidence identifier differs: {key}"
        )
    return _v07_evidence_projection({**row, "evidence_id": evidence_id})


def _v14_capture_evidence(document: Mapping[str, Any]) -> dict[str, Any]:
    evidence_id = atlas_stable_id(
        "evidence",
        "verified-construction-core-v0.14",
        document.get("source_id"),
        document.get("content_hash"),
    )
    if evidence_id not in V14_CAPTURE_EVIDENCE_IDS:
        raise VerifiedConstructionCoreError(
            "v0.14 capture evidence identifier differs"
        )
    return _v07_evidence_projection({**document, "evidence_id": evidence_id})


def _v14_validate_hydrated_crosscheck(
    contract: Mapping[str, Any],
    acceptances: Sequence[Mapping[str, Any]],
    sources: Mapping[str, Mapping[str, Any]],
) -> None:
    if not _v14_hydrated_crosscheck_available(contract):
        return
    pipeline = {
        row["stable_key"]: row
        for row in _load_csv(SOURCE_RELEASE / "construction_pipeline.csv")
    }
    entities = {
        row["stable_key"]: row
        for row in _load_csv(SOURCE_RELEASE / "entities.csv")
    }
    evidence = {
        row["evidence_id"]: row
        for row in _load_csv(SOURCE_RELEASE / "evidence.csv")
    }
    members = _load_csv(
        ROOT
        / "exact_identity_decisions"
        / "2026-07-22-public-open-v14"
        / "component-members.csv"
    )
    relationships = _load_csv(
        ROOT
        / "exact_identity_decisions"
        / "2026-07-22-public-open-v14"
        / "relationships.csv"
    )
    members_by_component: dict[str, list[dict[str, str]]] = defaultdict(list)
    for member in members:
        members_by_component[member["component_id"]].append(member)
    for acceptance in acceptances:
        project_key = acceptance["project_stable_key"]
        campus_key = acceptance["parent_campus_stable_key"]
        source = sources[project_key]
        status = acceptance["status"]
        project = pipeline.get(project_key)
        campus = entities.get(campus_key)
        if (
            project is None
            or campus is None
            or project["entity_id"] != acceptance["project_entity_id"]
            or campus["entity_id"] != acceptance["parent_campus_entity_id"]
            or project["status"] != status["value"]
            or project["status_as_of"] != status["as_of_date"]
            or project["status_method"] != status["method"]
            or project["status_evidence_id"] != status["evidence_id"]
        ):
            raise VerifiedConstructionCoreError(
                f"v0.14 hydrated source projection differs: {project_key}"
            )
        for evidence_binding in (
            acceptance["location_evidence"],
            {
                "key": status["evidence_key"],
                "evidence_id": status["evidence_id"],
            },
        ):
            projected = _v14_source_evidence(
                source,
                evidence_binding["key"],
                evidence_binding["evidence_id"],
            )
            hydrated = evidence.get(evidence_binding["evidence_id"])
            if hydrated is None or any(
                hydrated[field] != projected[field] for field in projected
            ):
                raise VerifiedConstructionCoreError(
                    f"v0.14 hydrated evidence projection differs: {project_key}"
                )
        project_member = next(
            (
                row
                for row in members
                if row["release_id"] == SOURCE_RELEASE_ID
                and row["stable_key"] == project_key
                and row["entity_id"] == acceptance["project_entity_id"]
            ),
            None,
        )
        relationship = acceptance["relationship"]
        topology = next(
            (
                row
                for row in relationships
                if row["relationship_id"] == relationship["relationship_id"]
                and project_member is not None
                and row["subject_component_id"]
                == project_member["component_id"]
            ),
            None,
        )
        target_members = (
            []
            if topology is None
            else members_by_component[topology["object_component_id"]]
        )
        if (
            topology is None
            or topology["relationship_type"] != "project_targets"
            or topology["decision_basis"] != "explicit_parent"
            or not any(
                row["release_id"] == SOURCE_RELEASE_ID
                and row["stable_key"] == campus_key
                and row["entity_id"] == acceptance["parent_campus_entity_id"]
                for row in target_members
            )
        ):
            raise VerifiedConstructionCoreError(
                f"v0.14 hydrated topology differs: {project_key}"
            )


def _v14_contracts() -> dict[str, Any]:
    if (
        V14_BATCH_CONTRACT.is_symlink()
        or not V14_BATCH_CONTRACT.is_file()
        or _sha256_file(V14_BATCH_CONTRACT) != V14_BATCH_CONTRACT_SHA256
        or V14_GEOMETRY_CAPTURE.is_symlink()
        or not V14_GEOMETRY_CAPTURE.is_file()
        or _sha256_file(V14_GEOMETRY_CAPTURE) != V14_GEOMETRY_CAPTURE_SHA256
    ):
        raise VerifiedConstructionCoreError("v0.14 contract hash differs")
    contract = _load_json(V14_BATCH_CONTRACT)
    capture = _load_json(V14_GEOMETRY_CAPTURE)
    base = contract.get("base_preview", {})
    capture_binding = contract.get("geometry_capture", {})
    if (
        contract.get("contract_id")
        != "verified-construction-core-v0.14-nine-us-address-locator-batch"
        or contract.get("reviewed_as_of")
        != CURRENT_V14_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
        or contract.get("cohort_lifecycle_reference_date")
        != CURRENT_V14_LIFECYCLE_REFERENCE_DATE.isoformat()
        or base.get("preview_id") != CURRENT_V13_PREVIEW_ID
        or base.get("manifest_sha256")
        != LEGACY_PREVIEW_V13_MANIFEST_SHA256
        or base.get("commit") != LEGACY_PREVIEW_V13_COMMIT
        or capture_binding.get("path")
        != V14_GEOMETRY_CAPTURE.relative_to(ROOT).as_posix()
        or capture_binding.get("bytes") != V14_GEOMETRY_CAPTURE.stat().st_size
        or capture_binding.get("sha256") != V14_GEOMETRY_CAPTURE_SHA256
        or capture.get("capture_id")
        != "verified-construction-core-v0.14-us-address-locator-facts"
        or capture.get("captured_on")
        != CURRENT_V14_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
    ):
        raise VerifiedConstructionCoreError("v0.14 batch contract differs")
    semantics_by_profile = {
        "census": contract.get("census_geometry_semantics"),
        "first_party": contract.get("first_party_geometry_semantics"),
        "alviso": contract.get("alviso_geometry_semantics"),
    }
    for profile, semantics in semantics_by_profile.items():
        expected_scope = (
            "project_locator" if profile == "alviso" else "campus_locator"
        )
        expected_entity = "project" if profile == "alviso" else "campus"
        if (
            not isinstance(semantics, dict)
            or semantics.get("geometry_source_entity_kind") != expected_entity
            or semantics.get("geometry_authority_class") != "official_source"
            or semantics.get("geometry_use_scope") != expected_scope
            or any(
                type(semantics.get(field)) is not bool
                or semantics[field] is not False
                for field in (
                    "official_boundary",
                    "parcel_or_building_geometry",
                    "project_footprint",
                    "campus_boundary",
                    "imagery_verification",
                )
            )
        ):
            raise VerifiedConstructionCoreError(
                f"v0.14 {profile} geometry semantics differ"
            )
    documents = capture.get("source_documents")
    results = capture.get("results")
    acceptances = contract.get("acceptances")
    if (
        not isinstance(documents, list)
        or len(documents) != 16
        or any(not isinstance(row, dict) for row in documents)
        or not isinstance(results, list)
        or len(results) != 9
        or any(not isinstance(row, dict) for row in results)
        or not isinstance(acceptances, list)
        or len(acceptances) != 9
        or any(not isinstance(row, dict) for row in acceptances)
        or {row.get("project_stable_key") for row in acceptances}
        != V14_PROJECT_KEYS
    ):
        raise VerifiedConstructionCoreError("v0.14 batch inventory differs")
    documents_by_id = {row.get("source_id"): row for row in documents}
    results_by_id = {row.get("locator_id"): row for row in results}
    if (
        len(documents_by_id) != 16
        or set(documents_by_id) != set(V14_SOURCE_DOCUMENT_EXPECTATIONS)
        or set(results_by_id) != set(V14_RESULT_EXPECTATIONS)
        or set(V14_IDENTITY_SOURCE_EXPECTATIONS) != set(V14_RESULT_EXPECTATIONS)
    ):
        raise VerifiedConstructionCoreError("v0.14 capture identities differ")
    projected_capture_evidence = {
        _v14_capture_evidence(document)["evidence_id"]
        for document in documents
    }
    if projected_capture_evidence != V14_CAPTURE_EVIDENCE_IDS:
        raise VerifiedConstructionCoreError(
            "v0.14 capture evidence inventory differs"
        )
    if capture.get("census_rights_basis") != {
        "source_url": (
            "https://www.census.gov/topics/research/"
            "research-transparency-public-access/open-data.html"
        ),
        "raw_capture_bytes": 328419,
        "raw_capture_sha256": (
            "d902347d06b66e81d2574e0d88aac4aa6a5f13f730cc6fc175c94e413c69d432"
        ),
        "redistributed": False,
        "conclusion": (
            "The Census address-match facts are United States government data "
            "treated as public domain; the source page itself is not "
            "redistributed."
        ),
    }:
        raise VerifiedConstructionCoreError("v0.14 Census rights pin differs")
    for document in documents:
        raw_capture = document.get("raw_capture")
        expected_bytes, expected_hash, expected_license = (
            V14_SOURCE_DOCUMENT_EXPECTATIONS[document["source_id"]]
        )
        if (
            not isinstance(raw_capture, dict)
            or set(raw_capture) != {"bytes", "sha256", "redistributed"}
            or raw_capture.get("bytes") != expected_bytes
            or raw_capture.get("sha256") != expected_hash
            or document.get("content_hash") != expected_hash
            or document.get("license") != expected_license
            or document.get("retrieved_at")
            != CURRENT_V14_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
            or raw_capture.get("redistributed") is not False
        ):
            raise VerifiedConstructionCoreError(
                "v0.14 raw-source guardrail differs"
            )
    sources: dict[str, Mapping[str, Any]] = {}
    seen_campuses: set[str] = set()
    for acceptance in acceptances:
        project_key = acceptance["project_stable_key"]
        campus_key = acceptance["parent_campus_stable_key"]
        source_binding = acceptance["source_input"]
        source_path = _repository_input(
            source_binding["path"],
            source_binding["sha256"],
            "v0.14 curated source",
        )
        if source_path.stat().st_size != source_binding["bytes"]:
            raise VerifiedConstructionCoreError(
                f"v0.14 source bytes differ: {project_key}"
            )
        source = _load_json(source_path)
        project = source.get("project")
        campus = source.get("campus")
        profile = acceptance.get("semantics_profile")
        result = results_by_id.get(acceptance.get("locator_id"))
        expected = V14_RESULT_EXPECTATIONS.get(acceptance.get("locator_id"))
        if (
            not isinstance(project, dict)
            or not isinstance(campus, dict)
            or project.get("stable_key") != project_key
            or campus.get("stable_key") != campus_key
            or project.get("country") != "United States"
            or campus.get("country") != "United States"
            or project.get("coordinates") is not None
            or project.get("geometry") is not None
            or campus.get("coordinates") is not None
            or campus.get("geometry") is not None
            or acceptance["project_entity_id"]
            != atlas_stable_id("entity", project_key, "project")
            or acceptance["parent_campus_entity_id"]
            != atlas_stable_id("entity", campus_key, "campus")
            or campus_key in seen_campuses
            or profile not in semantics_by_profile
            or expected is None
            or not isinstance(result, dict)
            or result.get("project_stable_key") != project_key
            or result.get("geometry_source_document_id") != expected[2]
            or profile != expected[3]
            or result.get("identity_source_document_ids")
            != V14_IDENTITY_SOURCE_EXPECTATIONS[acceptance["locator_id"]]
            or result.get("longitude") != expected[0]
            or result.get("latitude") != expected[1]
            or not math.isfinite(result["longitude"])
            or not math.isfinite(result["latitude"])
            or result["geometry_source_document_id"] not in documents_by_id
            or any(
                source_id not in documents_by_id
                for source_id in result.get("identity_source_document_ids", [])
            )
        ):
            raise VerifiedConstructionCoreError(
                f"v0.14 source or locator identity differs: {project_key}"
            )
        seen_campuses.add(campus_key)
        if (
            (
                profile in {"census", "first_party"}
                and acceptance.get("address_scope") != "parent_campus_only"
            )
            or (
                profile == "alviso"
                and acceptance.get("address_scope") != "project_and_parent_campus"
            )
        ):
            raise VerifiedConstructionCoreError(
                f"v0.14 address scope differs: {project_key}"
            )
        relationship = acceptance.get("relationship")
        if (
            not isinstance(relationship, dict)
            or not str(relationship.get("relationship_id", "")).startswith(
                "relationship:"
            )
            or relationship.get("relationship_type") != "project_targets"
            or relationship.get("decision_basis") != "explicit_parent"
        ):
            raise VerifiedConstructionCoreError(
                f"v0.14 project-to-campus relationship differs: {project_key}"
            )
        status = acceptance.get("status")
        lifecycle = [
            row
            for row in source.get("lifecycle", [])
            if isinstance(row, dict)
            and row.get("entity") == "project"
            and row.get("evidence_key") == status.get("evidence_key")
        ]
        if (
            not isinstance(status, dict)
            or len(lifecycle) != 1
            or lifecycle[0].get("value") != status.get("value")
            or lifecycle[0].get("as_of_date") != status.get("as_of_date")
            or lifecycle[0].get("method") != status.get("method")
            or lifecycle[0].get("confidence") != status.get("confidence")
            or status.get("value") not in PHYSICAL_STATUSES
            or status.get("method") not in AUTHORITATIVE_STATUS_METHODS
            or not 0
            <= (
                CURRENT_V14_LIFECYCLE_REFERENCE_DATE
                - _calendar_date(status["as_of_date"], "v0.14 status_as_of")
            ).days
            <= MAX_STATUS_AGE_DAYS
        ):
            raise VerifiedConstructionCoreError(
                f"v0.14 lifecycle differs: {project_key}"
            )
        _v14_source_evidence(
            source, status["evidence_key"], status["evidence_id"]
        )
        location = acceptance.get("location_evidence")
        if not isinstance(location, dict):
            raise VerifiedConstructionCoreError(
                f"v0.14 location evidence differs: {project_key}"
            )
        _v14_source_evidence(source, location["key"], location["evidence_id"])
        sources[project_key] = source
    qts = results_by_id["qts-cedar-rapids-dc1-first-party-pin"]
    jlab = results_by_id["jefferson-lab-12000-jefferson-avenue"]
    city_record = documents_by_id["san-jose-alviso-private-development-record"]
    city_address = documents_by_id["san-jose-alviso-address-point"]
    qts_facts = documents_by_id[
        "qts-cedar-rapids-first-party-map-pin"
    ].get("facts", {})
    washington_facts = documents_by_id[
        "washington-current-parcels-wenatchee-address"
    ].get("facts", {})
    licking = documents_by_id[
        "licking-county-microsoft-owner-address-facts"
    ]
    hebron_facts = documents_by_id[
        "village-of-hebron-microsoft-data-center-minutes"
    ].get("facts", {})
    if (
        qts.get("geometry_source_document_id")
        != "qts-cedar-rapids-first-party-map-pin"
        or "Fairfax" not in qts.get("normalization_note", "")
        or "23605" not in jlab.get("matched_address", "")
        or "23606" not in jlab.get("normalization_note", "")
        or city_record.get("facts", {}).get("developer_name") != "Microsoft"
        or city_record.get("facts", {}).get("facility_id") != 749
        or "CP23-016" not in city_record.get("facts", {}).get("file_numbers", [])
        or city_record.get("facts", {}).get("polygon_rejected") is not True
        or city_address.get("facts", {}).get("longitude")
        != V14_RESULT_EXPECTATIONS["microsoft-alviso-city-address-point"][0]
        or city_address.get("facts", {}).get("latitude")
        != V14_RESULT_EXPECTATIONS["microsoft-alviso-city-address-point"][1]
        or qts_facts.get("map_pin_longitude")
        != V14_RESULT_EXPECTATIONS["qts-cedar-rapids-dc1-first-party-pin"][0]
        or qts_facts.get("map_pin_latitude")
        != V14_RESULT_EXPECTATIONS["qts-cedar-rapids-dc1-first-party-pin"][1]
        or qts_facts.get("dc2_dc3_geometry_not_inferred") is not True
        or washington_facts.get("original_parcel_id") != "93700000002"
        or washington_facts.get("parcel_polygon_rejected") is not True
        or licking.get("metadata_capture")
        != {
            "bytes": 1746,
            "sha256": (
                "ac0500fcff383d32b47784db089ffabcf5e013f632ffd6ca8d2afb755b813691"
            ),
            "license_and_terms_blank": True,
            "redistributed": False,
        }
        or licking.get("facts")
        != [
            {
                "parcel": "097-330018-00.000",
                "owner_name": "MICROSOFT CORPORATION",
                "site_address": "975 THORNWOOD DR",
                "site_city": "HEATH",
                "site_zip": "43056",
            },
            {
                "parcel": "095-112206-00.000",
                "owner_name": "MICROSOFT CORPORATION",
                "site_address": "3287 BEECH RD NW",
                "site_city": "NEW ALBANY",
                "site_zip": "43031",
            },
            {
                "parcel": "078-330000-00.000",
                "owner_name": "MICROSOFT CORPORATION",
                "site_address": "323 N HIGH ST",
                "site_city": "HEBRON",
                "site_zip": "43025",
            },
        ]
        or licking.get("parcel_polygons_rejected") is not True
        or hebron_facts.get("current_323_n_high_parcel_not_claimed_by_minutes")
        is not True
    ):
        raise VerifiedConstructionCoreError(
            "v0.14 normalization or Alviso identity guardrail differs"
        )
    _v14_validate_hydrated_crosscheck(contract, acceptances, sources)
    return {
        "contract": contract,
        "capture": capture,
        "acceptances": sorted(
            acceptances, key=lambda row: row["project_stable_key"]
        ),
        "documents_by_id": documents_by_id,
        "results_by_id": results_by_id,
        "semantics_by_profile": semantics_by_profile,
        "sources": sources,
    }


def _v14_build_delta_rows(contracts: Mapping[str, Any]) -> dict[str, Any]:
    sources = contracts["sources"]
    results = contracts["results_by_id"]
    documents = contracts["documents_by_id"]
    evidence_pool: dict[str, dict[str, Any]] = {}
    evidence_usage: dict[str, dict[str, set[str]]] = defaultdict(
        lambda: {"roles": set(), "project_ids": set()}
    )
    projects: list[dict[str, Any]] = []
    sites: list[dict[str, Any]] = []
    for acceptance in contracts["acceptances"]:
        project_key = acceptance["project_stable_key"]
        project_id = acceptance["project_entity_id"]
        campus_key = acceptance["parent_campus_stable_key"]
        source = sources[project_key]
        project = source["project"]
        campus = source["campus"]
        status = acceptance["status"]
        location = acceptance["location_evidence"]
        result = results[acceptance["locator_id"]]
        semantics = contracts["semantics_by_profile"][
            acceptance["semantics_profile"]
        ]
        for binding, role in (
            (status, "physical_status"),
            (location, "context:source_location_identity"),
        ):
            key = binding.get("evidence_key", binding.get("key"))
            evidence_id = binding["evidence_id"]
            projected = _v14_source_evidence(source, key, evidence_id)
            previous = evidence_pool.get(evidence_id)
            if previous is not None and previous != projected:
                raise VerifiedConstructionCoreError(
                    f"v0.14 evidence collision differs: {project_key}"
                )
            evidence_pool[evidence_id] = projected
            evidence_usage[evidence_id]["roles"].add(role)
            evidence_usage[evidence_id]["project_ids"].add(project_id)
        geometry_source_id = result["geometry_source_document_id"]
        for source_id, role in (
            (geometry_source_id, "geometry"),
            *(
                (source_id, "context:geometry_identity")
                for source_id in result["identity_source_document_ids"]
            ),
        ):
            projected = _v14_capture_evidence(documents[source_id])
            evidence_id = projected["evidence_id"]
            previous = evidence_pool.get(evidence_id)
            if previous is not None and previous != projected:
                raise VerifiedConstructionCoreError(
                    f"v0.14 capture evidence collision differs: {project_key}"
                )
            evidence_pool[evidence_id] = projected
            evidence_usage[evidence_id]["roles"].add(role)
            evidence_usage[evidence_id]["project_ids"].add(project_id)
        geometry_document = documents[geometry_source_id]
        geometry_evidence_id = _v14_capture_evidence(geometry_document)[
            "evidence_id"
        ]
        longitude = result["longitude"]
        latitude = result["latitude"]
        geometry = {"coordinates": [longitude, latitude], "type": "Point"}
        geometry_json = _json_bytes(geometry).decode().strip()
        site_id = _stable_id("vcc-site", campus_key)
        status_date = _calendar_date(status["as_of_date"], "v0.14 status_as_of")
        project_row = {
            "project_id": project_id,
            "project_stable_key": project_key,
            "site_id": site_id,
            "physical_site_stable_key": campus_key,
            "name": project["name"],
            "country": "United States",
            "country_iso_a2": "US",
            "latitude": latitude,
            "longitude": longitude,
            "geometry_json": geometry_json,
            "geometry_type": "Point",
            "geometry_source_entity_kind": semantics[
                "geometry_source_entity_kind"
            ],
            "geometry_derivation": semantics["geometry_derivation"],
            "geometry_method": semantics["geometry_method"],
            "geometry_scope_class": semantics["geometry_scope_class"],
            "geometry_authority_class": semantics[
                "geometry_authority_class"
            ],
            "geometry_use_scope": semantics["geometry_use_scope"],
            "geometry_precision_scope": semantics["precision_scope"],
            "horizontal_uncertainty_metres": "",
            "horizontal_uncertainty_unknown_reason": semantics[
                "horizontal_uncertainty_unknown_reason"
            ],
            "geometry_evidence_id": geometry_evidence_id,
            "last_observed_physical_status": status["value"],
            "status_as_of": status["as_of_date"],
            "status_age_days_at_review": (
                CURRENT_V14_LIFECYCLE_REFERENCE_DATE - status_date
            ).days,
            "status_method": status["method"],
            "status_evidence_id": status["evidence_id"],
            "verification_posture": _verification_posture(
                semantics["geometry_authority_class"],
                semantics["geometry_use_scope"],
            ),
            "independent_imagery_verification": "false",
            "imagery_review_outcome": "not_reviewed_for_core_preview",
            "development_type": "unknown",
            "development_type_unknown_reason": (
                "not selected by the v0.14 address-locator batch"
            ),
            "operating_model": "unknown",
            "operating_model_unknown_reason": (
                "not established by selected evidence"
            ),
            "operating_model_evidence_id": "",
            "workloads_json": "[]",
            "workload_unknown_reason": "not established by selected evidence",
            "role_claims_json": "[]",
            "power_observations_json": "[]",
            "power_unknown_reason": (
                "no typed project power observation selected by this batch; "
                "not estimated"
            ),
            "annual_energy_observations_json": "[]",
            "annual_energy_unknown_reason": (
                "no scoped annual-energy inputs selected; not estimated"
            ),
            "efficiency_observations_json": "[]",
            "efficiency_unknown_reason": (
                "no scoped PUE or WUE observation selected"
            ),
            "owner": "",
            "operator": "",
            "users": "",
            "tenants": "",
            "customers": "",
            "status_source_url": evidence_pool[status["evidence_id"]][
                "source_url"
            ],
            "geometry_source_url": geometry_document["source_url"],
        }
        projects.append(project_row)
        sites.append(
            {
                "site_id": site_id,
                "physical_site_stable_key": campus_key,
                "name": campus["name"],
                "country": "United States",
                "country_iso_a2": "US",
                "latitude": latitude,
                "longitude": longitude,
                "geometry_json": geometry_json,
                "geometry_type": "Point",
                "geometry_source_entity_kinds_json": _json_bytes(
                    [semantics["geometry_source_entity_kind"]]
                )
                .decode()
                .strip(),
                "geometry_derivations_json": _json_bytes(
                    [semantics["geometry_derivation"]]
                )
                .decode()
                .strip(),
                "geometry_methods_json": _json_bytes(
                    [semantics["geometry_method"]]
                )
                .decode()
                .strip(),
                "geometry_scope_classes_json": _json_bytes(
                    [semantics["geometry_scope_class"]]
                )
                .decode()
                .strip(),
                "geometry_authority_classes_json": _json_bytes(
                    [semantics["geometry_authority_class"]]
                )
                .decode()
                .strip(),
                "geometry_use_scopes_json": _json_bytes(
                    [semantics["geometry_use_scope"]]
                )
                .decode()
                .strip(),
                "geometry_precision_scopes_json": _json_bytes(
                    [semantics["precision_scope"]]
                )
                .decode()
                .strip(),
                "horizontal_uncertainty_metres": "",
                "horizontal_uncertainty_unknown_reason": semantics[
                    "horizontal_uncertainty_unknown_reason"
                ],
                "geometry_evidence_ids_json": _json_bytes(
                    [geometry_evidence_id]
                )
                .decode()
                .strip(),
                "project_count": 1,
                "project_ids_json": _json_bytes([project_id]).decode().strip(),
                "project_stable_keys_json": _json_bytes([project_key])
                .decode()
                .strip(),
                "statuses_json": _json_bytes([status["value"]])
                .decode()
                .strip(),
                "oldest_status_as_of": status["as_of_date"],
                "newest_status_as_of": status["as_of_date"],
                "verification_posture": project_row["verification_posture"],
                "independent_imagery_verification": "false",
                "imagery_review_outcomes_json": _json_bytes(
                    ["not_reviewed_for_core_preview"]
                )
                .decode()
                .strip(),
            }
        )
    evidence_rows = [
        {
            **evidence_pool[evidence_id],
            "roles_json": _json_bytes(sorted(usage["roles"])).decode().strip(),
            "project_ids_json": _json_bytes(sorted(usage["project_ids"]))
            .decode()
            .strip(),
        }
        for evidence_id, usage in sorted(evidence_usage.items())
    ]
    if (
        len(projects) != 9
        or len(sites) != 9
        or len(evidence_rows) != 25
        or len({row["project_id"] for row in projects}) != 9
        or len({row["site_id"] for row in sites}) != 9
        or len({row["evidence_id"] for row in evidence_rows}) != 25
    ):
        raise VerifiedConstructionCoreError("v0.14 delta accounting differs")
    return {"projects": projects, "sites": sites, "evidence": evidence_rows}


def _v14_portable_source_inputs(
    contracts: Mapping[str, Any],
) -> list[dict[str, Any]]:
    base_manifest = _load_json(LEGACY_PREVIEW_V13_DIR / "manifest.json")
    rows = list(base_manifest["portable_source_inputs"])
    rows.extend(
        [
            {
                "path": V14_BATCH_CONTRACT.relative_to(ROOT).as_posix(),
                "bytes": V14_BATCH_CONTRACT.stat().st_size,
                "sha256": V14_BATCH_CONTRACT_SHA256,
                "parent_manifests": [],
            },
            {
                "path": V14_GEOMETRY_CAPTURE.relative_to(ROOT).as_posix(),
                "bytes": V14_GEOMETRY_CAPTURE.stat().st_size,
                "sha256": V14_GEOMETRY_CAPTURE_SHA256,
                "parent_manifests": [],
            },
            *(
                {
                    **acceptance["source_input"],
                    "parent_manifests": [],
                }
                for acceptance in contracts["acceptances"]
            ),
        ]
    )
    rows.sort(key=lambda row: row["path"])
    if len(rows) != 100 or len({row["path"] for row in rows}) != 100:
        raise VerifiedConstructionCoreError("v0.14 portable input count differs")
    for row in rows:
        path = _repository_input(
            row["path"], row["sha256"], "v0.14 portable input"
        )
        if path.stat().st_size != row["bytes"]:
            raise VerifiedConstructionCoreError(
                "v0.14 portable input byte count differs"
            )
    return rows


def _v14_review_queue(
    contracts: Mapping[str, Any],
) -> list[dict[str, Any]]:
    rows = []
    for acceptance in contracts["acceptances"]:
        result = contracts["results_by_id"][acceptance["locator_id"]]
        semantics = contracts["semantics_by_profile"][
            acceptance["semantics_profile"]
        ]
        geometry_document = contracts["documents_by_id"][
            result["geometry_source_document_id"]
        ]
        use_scope = semantics["geometry_use_scope"]
        target_is_campus = use_scope == "campus_locator"
        rows.append(
            {
                "batch_contract_path": V14_BATCH_CONTRACT.relative_to(
                    ROOT
                ).as_posix(),
                "batch_contract_sha256": V14_BATCH_CONTRACT_SHA256,
                "decision": "accepted",
                "decision_reason": acceptance["scope_guardrail"],
                "geometry_authority_class": semantics[
                    "geometry_authority_class"
                ],
                "geometry_derivation": semantics["geometry_derivation"],
                "geometry_entity": semantics["geometry_source_entity_kind"],
                "geometry_evidence_id": _v14_capture_evidence(
                    geometry_document
                )["evidence_id"],
                "geometry_evidence_key": geometry_document["source_id"],
                "geometry_method": semantics["geometry_method"],
                "geometry_scope_class": semantics["geometry_scope_class"],
                "geometry_target_entity_id": (
                    acceptance["parent_campus_entity_id"]
                    if target_is_campus
                    else acceptance["project_entity_id"]
                ),
                "geometry_target_entity_kind": (
                    "campus" if target_is_campus else "project"
                ),
                "geometry_target_entity_stable_key": (
                    acceptance["parent_campus_stable_key"]
                    if target_is_campus
                    else acceptance["project_stable_key"]
                ),
                "geometry_use_scope": use_scope,
                "horizontal_uncertainty_metres": None,
                "horizontal_uncertainty_unknown_reason": semantics[
                    "horizontal_uncertainty_unknown_reason"
                ],
                "locator_id": acceptance["locator_id"],
                "official_boundary": False,
                "precision_scope": semantics["precision_scope"],
                "project_to_campus_decision_basis": "explicit_parent",
                "project_to_campus_relationship_id": acceptance[
                    "relationship"
                ]["relationship_id"],
                "project_to_campus_relationship_type": "project_targets",
                "rejected_claims": contracts["contract"]["rejected_claims"],
                "reviewed_at": (
                    CURRENT_V14_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
                ),
                "source_input_bytes": acceptance["source_input"]["bytes"],
                "source_input_path": acceptance["source_input"]["path"],
                "source_input_sha256": acceptance["source_input"]["sha256"],
                "source_project_entity_id": acceptance["project_entity_id"],
                "source_project_stable_key": acceptance[
                    "project_stable_key"
                ],
            }
        )
    return rows


def _v14_context_evidence_bindings(
    contracts: Mapping[str, Any],
) -> list[dict[str, Any]]:
    rows = []
    for acceptance in contracts["acceptances"]:
        location = acceptance["location_evidence"]
        source_input = acceptance["source_input"]
        rows.append(
            {
                "evidence_id": location["evidence_id"],
                "evidence_key": location["key"],
                "project_stable_key": acceptance["project_stable_key"],
                "semantic_scope": acceptance["scope_guardrail"],
                "source_input_path": source_input["path"],
                "source_input_sha256": source_input["sha256"],
                "usage_role": "geometry_identity",
            }
        )
    return rows


def _v14_map_html(
    geojson: Mapping[str, Any],
    sites: Sequence[Mapping[str, Any]],
    evidence: Sequence[Mapping[str, Any]],
) -> bytes:
    document = _v13_map_html(geojson, sites, evidence).decode("utf-8")
    title = "Verified Construction Core v0.13 preview"
    if document.count(title) != 1:
        raise VerifiedConstructionCoreError("v0.14 map title seam differs")
    document = document.replace(
        title, "Verified Construction Core v0.14 preview", 1
    )
    marker = '. <a href="ATTRIBUTION.txt">Full attribution and source terms</a>.'
    notices = (
        ' · <a href="https://geocoding.geo.census.gov/geocoder/">'
        "United States Census Bureau Geocoder</a> — U.S. government data · "
        '<a href="https://geo.sanjoseca.gov/server/rest/services/OPN/'
        'OPN_OpenDataService/MapServer/36">City of San Jose Addresses</a> — '
        "City open-data disclaimer"
    )
    if document.count(marker) != 1:
        raise VerifiedConstructionCoreError(
            "v0.14 map attribution seam differs"
        )
    document = document.replace(marker, f"{notices}{marker}", 1)
    return document.encode("utf-8")


def _v14_schema() -> dict[str, Any]:
    schema = _v13_schema()
    schema["format"] = "datacenter-atlas-verified-construction-core-schema-v14"
    schema["preview_id"] = CURRENT_V14_PREVIEW_ID
    schema["v0_14_nine_us_address_locator_batch"] = {
        "accepted_project_count": 9,
        "accepted_physical_site_count": 9,
        "geometry_authority_classes": {"official_source": 9},
        "geometry_types": {"Point": 9},
        "geometry_use_scopes": {
            "campus_locator": 8,
            "project_locator": 1,
        },
        "geometry_methods": {
            "census_public_ar_current_street_range_interpolation": 7,
            "qts_first_party_cdr1_dc1_page_map_pin_as_shared_campus_locator": 1,
            "city_of_san_jose_address_point_cross_identified_by_named_development_record": 1,
        },
        "official_boundary": False,
        "coordinate_semantics": (
            "Seven Census Public_AR_Current address-match points, one QTS "
            "first-party page map pin, and one City of San Jose address point "
            "locate nine distinct sites. The eight non-Alviso points patch "
            "parent campuses only. Alviso is a project locator cross-identified "
            "by the named City development record. No point is a footprint, "
            "boundary, entrance, survey point, or work-at-point claim."
        ),
        "horizontal_uncertainty_semantics": (
            "Every v0.14 row leaves horizontal_uncertainty_metres empty because "
            "the selected sources publish no bounded numeric accuracy applicable "
            "to the selected point; coordinate digits do not add accuracy."
        ),
        "normalization_disclosures": {
            "qts_cedar_rapids": (
                "QTS supplies the selected CDR1 DC1 page pin and labels 6200 "
                "76th Avenue SW as Cedar Rapids; the corroborating Census match "
                "normalizes the same address and ZIP to Fairfax."
            ),
            "jefferson_lab": (
                "The official laboratory address uses ZIP 23606; Census returns "
                "23605. The selected point remains a broad laboratory-campus "
                "locator, never the JLDC building location."
            ),
        },
    }
    schema["v0_14_temporal_scope"] = {
        "reviewed_at_semantics": (
            "Legacy alias for cohort_lifecycle_reference_date, retained for "
            "preview compatibility; it is not the geometry-identity review date."
        ),
        "cohort_lifecycle_reference_date": (
            CURRENT_V14_LIFECYCLE_REFERENCE_DATE.isoformat()
        ),
        "geometry_identity_reviewed_at": (
            CURRENT_V14_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
        ),
    }
    return schema


def _v14_readme(report: Mapping[str, Any], legal_notice: str) -> bytes:
    return f"""# Verified Construction Core v0.14 preview

This tracked, non-final preview contains **{report['selected_physical_site_count']} physical sites**
and **{report['selected_project_count']} linked projects** in {len(report['country_counts'])}
countries. Every project retains a dated authoritative physical-status observation and a reviewed
geometry scope. Five project rows carry official boundaries; the remaining 66 are explicit project
or campus locators, never construction footprints by implication.

The nine-row v0.14 delta adds Microsoft Alviso, Bitdeer Wenatchee, Sabey Austin, Microsoft Heath,
Microsoft New Albany, Bitdeer Massillon, Microsoft Hebron, QTS Cedar Rapids, and Jefferson Lab
JLDC. Seven sites use United States Census Bureau Public_AR_Current address matches only as
parent-campus locators. QTS
uses its first-party CDR1 DC1 page pin only as a shared-campus locator, while the Census Fairfax
normalization remains corroboration. Alviso uses the City of San Jose address point cross-identified
to Microsoft and CP23-016 as a project locator. Every point has `official_boundary=false` and unknown
numeric horizontal accuracy.

QTS publishes the selected street address with a Cedar Rapids label, while Census returns Fairfax for
the same address and ZIP. Jefferson Lab publishes ZIP 23606, while Census returns 23605; its point is
only a broad laboratory-campus locator, not the JLDC building. The Washington and Licking County
parcel polygons, the City Major Private Development polygon, all source HTML/PDF, and all imagery are
excluded. Only compact facts and exact raw-response byte/hash pins are retained.

Final-release gates remain explicit in `selection-report.json`. The final artifact still requires at
least 100 physical sites, 40 countries, 50 non-U.S. sites, complete imagery-review outcomes, a
20-site blind review, and a clean-clone rebuild. Until every gate passes,
`publishable_as_final` is false.

## Files

- `projects.csv`: one row per selected physical construction project
- `sites.csv`: one row per distinct physical site
- `sites.geojson`: reviewed locator or boundary geometry for each physical site
- `evidence.csv`: locally bound evidence and exact usage roles
- `selection-report.json`: cohort, provenance, rejection, and release-gate audit
- `schema.json`: field and v0.14 scope semantics
- `map.html`: deterministic offline map; no tile requests
- `manifest.json` and `manifest.sha256`: byte inventory and trust root
- `ATTRIBUTION.txt`: source-specific attribution and terms

## Legal notice

{legal_notice}

This preview is an evidence product, not legal, surveying, investment, engineering, or operational
advice. Unknowns remain unknown, locators are not silently promoted to footprints, and no capacity,
role, workload, energy, efficiency, or lifecycle claim is inferred from geometry. The preview is
not final while `publishable_as_final` is false.
""".encode("utf-8")


def _v14_attribution(
    evidence: Sequence[Mapping[str, Any]], legal_notice: str
) -> bytes:
    document = _v13_attribution(evidence, legal_notice).decode("utf-8")
    heading = "Data Center Atlas Verified Construction Core v0.13 preview"
    if document.count(heading) != 1:
        raise VerifiedConstructionCoreError(
            "v0.14 attribution heading seam differs"
        )
    document = document.replace(
        heading,
        "Data Center Atlas Verified Construction Core v0.14 preview",
        1,
    )
    marker = "Map-provider terms:\n"
    notice = (
        "v0.14 geometry notice: Seven selected points are factual address-match "
        "results from the United States Census Bureau Public_AR_Current "
        "benchmark and are treated as U.S. government public-domain data. One "
        "selected point is a compact fact from the QTS Cedar Rapids page; one "
        "is a City of San Jose address point subject to the City's open-data "
        "disclaimer. Washington and Licking County parcel data, SEC exhibits, "
        "the Village of Hebron PDF, the City development polygon, and all source "
        "HTML/PDF remain fact-extraction-only and are not redistributed. No "
        "parcel polygon, source imagery, map tile, or third-party pixel is "
        "redistributed or interpreted.\n\n"
    )
    if document.count(marker) != 1:
        raise VerifiedConstructionCoreError(
            "v0.14 attribution notice seam differs"
        )
    document = document.replace(marker, f"{notice}{marker}", 1)
    return document.encode("utf-8")


def _v14_payloads() -> tuple[dict[str, bytes], dict[str, Any], list[dict[str, Any]]]:
    validate_frozen_v13(LEGACY_PREVIEW_V13_DIR)
    contracts = _v14_contracts()
    base_projects = _load_csv(
        LEGACY_PREVIEW_V13_DIR / "projects.csv", PROJECT_FIELDS
    )
    base_sites = _load_csv(LEGACY_PREVIEW_V13_DIR / "sites.csv", SITE_FIELDS)
    base_evidence = _load_csv(
        LEGACY_PREVIEW_V13_DIR / "evidence.csv", EVIDENCE_FIELDS
    )
    base_report = _load_json(
        LEGACY_PREVIEW_V13_DIR / "selection-report.json"
    )
    delta = _v14_build_delta_rows(contracts)
    if (
        {row["project_id"] for row in base_projects}
        & {row["project_id"] for row in delta["projects"]}
        or {row["site_id"] for row in base_sites}
        & {row["site_id"] for row in delta["sites"]}
        or {row["evidence_id"] for row in base_evidence}
        & {row["evidence_id"] for row in delta["evidence"]}
    ):
        raise VerifiedConstructionCoreError("v0.14 delta identity collision")
    projects = sorted(
        [*base_projects, *delta["projects"]],
        key=lambda row: row["project_stable_key"],
    )
    sites = sorted(
        [*base_sites, *delta["sites"]],
        key=lambda row: row["physical_site_stable_key"],
    )
    evidence = sorted(
        [*base_evidence, *delta["evidence"]],
        key=lambda row: row["evidence_id"],
    )
    if (
        len(projects) != 71
        or len(sites) != 68
        or len(evidence) != 170
        or len({row["project_id"] for row in projects}) != 71
        or len({row["site_id"] for row in sites}) != 68
        or len({row["evidence_id"] for row in evidence}) != 170
    ):
        raise VerifiedConstructionCoreError(
            "v0.14 cohort count or uniqueness differs"
        )
    project_by_id = {row["project_id"]: row for row in projects}
    site_by_id = {row["site_id"]: row for row in sites}
    evidence_by_id = {row["evidence_id"]: row for row in evidence}
    if (
        any(project_by_id[row["project_id"]] != row for row in base_projects)
        or any(site_by_id[row["site_id"]] != row for row in base_sites)
        or any(
            evidence_by_id[row["evidence_id"]] != row
            for row in base_evidence
        )
    ):
        raise VerifiedConstructionCoreError("v0.14 inherited row changed")
    gates = _final_release_gates(sites, projects)
    gates["clean_clone_rebuild"] = {
        "passed": True,
        "reason": (
            "all current-delta inputs and the frozen base artifact are tracked "
            "and hash-bound"
        ),
    }
    country_counts = dict(
        sorted(Counter(row["country"] for row in sites).items())
    )
    provenance_decisions = {
        key: list(base_report["provenance_decisions"][key])
        for key in (
            "workload_scope_bindings",
            "role_bindings",
            "excluded_source_roles",
            "context_evidence_bindings",
        )
    }
    provenance_decisions["context_evidence_bindings"].extend(
        _v14_context_evidence_bindings(contracts)
    )
    report = {
        **base_report,
        "format": "datacenter-atlas-verified-construction-core-selection-v14",
        "publishable_as_final": all(
            gate.get("passed", False) for gate in gates.values()
        ),
        "reviewed_at": CURRENT_V14_LIFECYCLE_REFERENCE_DATE.isoformat(),
        "cohort_lifecycle_reference_date": (
            CURRENT_V14_LIFECYCLE_REFERENCE_DATE.isoformat()
        ),
        "geometry_identity_reviewed_at": (
            CURRENT_V14_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
        ),
        "selected_project_count": 71,
        "selected_physical_site_count": 68,
        "official_boundary_project_count": 5,
        "reviewed_site_locator_project_count": 66,
        "non_selected_source_row_count": 460,
        "selection_first_failure_counts": V14_SELECTION_FIRST_FAILURE_COUNTS,
        "country_counts": country_counts,
        "final_release_gates": gates,
        "imagery_review_provenance": list(
            base_report["imagery_review_provenance"]
        ),
        "provenance_decisions": provenance_decisions,
        "reviewed_overlay_queue": [
            *base_report["reviewed_overlay_queue"],
            *_v14_review_queue(contracts),
        ],
        "nine_us_address_locator_batch": {
            "accepted_project_count": 9,
            "accepted_physical_site_count": 9,
            "batch_contract_path": V14_BATCH_CONTRACT.relative_to(
                ROOT
            ).as_posix(),
            "batch_contract_sha256": V14_BATCH_CONTRACT_SHA256,
            "capture_path": V14_GEOMETRY_CAPTURE.relative_to(ROOT).as_posix(),
            "capture_sha256": V14_GEOMETRY_CAPTURE_SHA256,
            "capture_source_document_count": 16,
            "geometry_authority_classes": {"official_source": 9},
            "geometry_methods": {
                "census_public_ar_current_street_range_interpolation": 7,
                "qts_first_party_cdr1_dc1_page_map_pin_as_shared_campus_locator": 1,
                "city_of_san_jose_address_point_cross_identified_by_named_development_record": 1,
            },
            "geometry_types": {"Point": 9},
            "geometry_use_scopes": {
                "campus_locator": 8,
                "project_locator": 1,
            },
            "normalization_disclosures": {
                "qts_cedar_rapids": "Census corroborator returns Fairfax for the QTS-published Cedar Rapids address.",
                "jefferson_lab": "Census returns ZIP 23605 for the official laboratory address published with ZIP 23606.",
            },
            "official_boundary": False,
            "project_stable_keys": sorted(V14_PROJECT_KEYS),
            "rejected_claims": contracts["contract"]["rejected_claims"],
        },
    }
    delta_by_key = {
        row["project_stable_key"]: row for row in delta["projects"]
    }
    if (
        len(country_counts) != 27
        or country_counts.get("United States") != 22
        or sum(row["country_iso_a2"] != "US" for row in sites) != 46
        or sum(_is_official_boundary(row) for row in projects) != 5
        or sum(_is_reviewed_locator(row) for row in projects) != 66
        or sum(V14_SELECTION_FIRST_FAILURE_COUNTS.values()) != 531
        or gates["imagery_outcomes_complete"]
        != {"actual": 10, "required": 71, "passed": False}
        or len(provenance_decisions["context_evidence_bindings"]) != 29
        or len(provenance_decisions["excluded_source_roles"]) != 12
        or len(report["reviewed_overlay_queue"]) != 58
        or set(delta_by_key) != V14_PROJECT_KEYS
        or any(
            row["geometry_type"] != "Point"
            or row["geometry_authority_class"] != "official_source"
            or row["geometry_use_scope"]
            not in {"campus_locator", "project_locator"}
            or row["horizontal_uncertainty_metres"]
            or row["workloads_json"] != "[]"
            or row["role_claims_json"] != "[]"
            or row["power_observations_json"] != "[]"
            or row["annual_energy_observations_json"] != "[]"
            or row["efficiency_observations_json"] != "[]"
            or row["operating_model"] != "unknown"
            or row["independent_imagery_verification"] != "false"
            or any(
                row[field]
                for field in (
                    "owner",
                    "operator",
                    "users",
                    "tenants",
                    "customers",
                )
            )
            for row in delta_by_key.values()
        )
    ):
        raise VerifiedConstructionCoreError("v0.14 expected accounting differs")
    geojson = _geojson(sites)
    geojson["name"] = (
        "Data Center Atlas Verified Construction Core v0.14 preview"
    )
    delta_features = {
        row["properties"]["physical_site_stable_key"]: row
        for row in geojson["features"]
        if row["properties"]["physical_site_stable_key"]
        in {
            acceptance["parent_campus_stable_key"]
            for acceptance in contracts["acceptances"]
        }
    }
    if (
        len(delta_features) != 9
        or Counter(
            feature["geometry"]["type"]
            for feature in delta_features.values()
        )
        != {"Point": 9}
    ):
        raise VerifiedConstructionCoreError(
            "v0.14 geometry publication differs"
        )
    legal_notice = _v10_city_legal_notice_from_base()
    payloads = {
        "ATTRIBUTION.txt": _v14_attribution(evidence, legal_notice),
        "README.md": _v14_readme(report, legal_notice),
        "evidence.csv": _csv_bytes(evidence, EVIDENCE_FIELDS),
        "map.html": _v14_map_html(geojson, sites, evidence),
        "projects.csv": _csv_bytes(projects, PROJECT_FIELDS),
        "schema.json": _json_bytes(_v14_schema()),
        "selection-report.json": _json_bytes(report),
        "sites.csv": _csv_bytes(sites, SITE_FIELDS),
        "sites.geojson": _json_bytes(geojson),
    }
    portable = _v14_portable_source_inputs(contracts)
    return payloads, report, portable


def _v14_manifest(
    payloads: Mapping[str, bytes],
    report: Mapping[str, Any],
    portable: Sequence[Mapping[str, Any]],
) -> dict[str, Any]:
    return {
        "format": "datacenter-atlas-verified-construction-core-preview-v14",
        "preview_id": CURRENT_V14_PREVIEW_ID,
        "release_status": "preview",
        "publishable_as_final": report["publishable_as_final"],
        "base_preview_id": CURRENT_V13_PREVIEW_ID,
        "base_preview_manifest_sha256": LEGACY_PREVIEW_V13_MANIFEST_SHA256,
        "base_preview_commit": LEGACY_PREVIEW_V13_COMMIT,
        "source_release_id": SOURCE_RELEASE_ID,
        "source_release_manifest_path": (
            "releases/2026-07-22-open-seed-v97/manifest.json"
        ),
        "source_release_manifest_sha256": (
            "0a6f41f4239944df27f2ce70e81a089b91cec401f154bbae28412b27a4d00fdd"
        ),
        "definition_paths": {
            "nine_us_address_locator_batch": V14_BATCH_CONTRACT.relative_to(
                ROOT
            ).as_posix()
        },
        "nine_us_address_locator_batch_sha256": V14_BATCH_CONTRACT_SHA256,
        "geometry_capture": {
            "path": V14_GEOMETRY_CAPTURE.relative_to(ROOT).as_posix(),
            "bytes": V14_GEOMETRY_CAPTURE.stat().st_size,
            "sha256": V14_GEOMETRY_CAPTURE_SHA256,
            "evidence_ids": sorted(V14_CAPTURE_EVIDENCE_IDS),
        },
        "portable_source_inputs": list(portable),
        "reviewed_at": CURRENT_V14_LIFECYCLE_REFERENCE_DATE.isoformat(),
        "cohort_lifecycle_reference_date": (
            CURRENT_V14_LIFECYCLE_REFERENCE_DATE.isoformat()
        ),
        "geometry_identity_reviewed_at": (
            CURRENT_V14_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
        ),
        "counts": {
            "physical_sites": 68,
            "projects": 71,
            "evidence": 170,
            "countries": 27,
            "non_us_sites": 46,
            "official_boundary_projects": 5,
            "reviewed_site_locator_projects": 66,
        },
        "files": {
            name: {"bytes": len(payload), "sha256": _sha256_bytes(payload)}
            for name, payload in sorted(payloads.items())
        },
    }


def build_preview(output_dir: Path = CURRENT_V14_PREVIEW_DIR) -> dict[str, Any]:
    """Build v0.14 from byte-frozen v0.13 and tracked hash-bound inputs."""
    output_dir = Path(output_dir)
    if output_dir.exists():
        raise VerifiedConstructionCoreError(f"refusing to overwrite {output_dir}")
    payloads, report, portable = _v14_payloads()
    manifest = _v14_manifest(payloads, report, portable)
    manifest_bytes = _json_bytes(manifest)
    complete = {
        **payloads,
        "manifest.json": manifest_bytes,
        "manifest.sha256": (
            f"{_sha256_bytes(manifest_bytes)}  manifest.json\n".encode()
        ),
    }
    output_dir.parent.mkdir(parents=True, exist_ok=True)
    stage = Path(
        tempfile.mkdtemp(prefix=f".{output_dir.name}.", dir=output_dir.parent)
    )
    try:
        for name, payload in complete.items():
            with (stage / name).open("xb") as handle:
                handle.write(payload)
        os.replace(stage, output_dir)
    except Exception:
        shutil.rmtree(stage, ignore_errors=True)
        raise
    validate_preview(output_dir)
    return manifest


def _validate_v14_preview(path: Path) -> dict[str, Any]:
    path = Path(path)
    if path.is_symlink() or not path.is_dir():
        raise VerifiedConstructionCoreError("v0.14 preview directory differs")
    manifest_path = path / "manifest.json"
    checksum_path = path / "manifest.sha256"
    if (
        manifest_path.is_symlink()
        or not manifest_path.is_file()
        or checksum_path.is_symlink()
        or not checksum_path.is_file()
    ):
        raise VerifiedConstructionCoreError(
            "v0.14 manifest trust root differs"
        )
    manifest_bytes = manifest_path.read_bytes()
    if checksum_path.read_bytes() != (
        f"{_sha256_bytes(manifest_bytes)}  manifest.json\n".encode()
    ):
        raise VerifiedConstructionCoreError("v0.14 manifest checksum differs")
    manifest = _load_json(manifest_path)
    files = manifest.get("files") if isinstance(manifest, dict) else None
    if (
        not isinstance(manifest, dict)
        or manifest.get("format")
        != "datacenter-atlas-verified-construction-core-preview-v14"
        or manifest.get("preview_id") != CURRENT_V14_PREVIEW_ID
        or manifest.get("release_status") != "preview"
        or type(manifest.get("publishable_as_final")) is not bool
        or manifest["publishable_as_final"] is not False
        or manifest.get("base_preview_manifest_sha256")
        != LEGACY_PREVIEW_V13_MANIFEST_SHA256
        or manifest.get("base_preview_commit") != LEGACY_PREVIEW_V13_COMMIT
        or not isinstance(files, dict)
    ):
        raise VerifiedConstructionCoreError("v0.14 manifest files differ")
    expected_inventory = set(files) | {"manifest.json", "manifest.sha256"}
    if {item.name for item in path.iterdir()} != expected_inventory:
        raise VerifiedConstructionCoreError("v0.14 preview inventory differs")
    for name, metadata in files.items():
        member = path / name
        if (
            Path(name).name != name
            or member.is_symlink()
            or not member.is_file()
            or type(metadata.get("bytes")) is not int
            or member.stat().st_size != metadata["bytes"]
            or _sha256_file(member) != metadata.get("sha256")
        ):
            raise VerifiedConstructionCoreError(
                f"v0.14 member differs: {name}"
            )
    payloads, report, portable = _v14_payloads()
    if manifest != _v14_manifest(payloads, report, portable):
        raise VerifiedConstructionCoreError("v0.14 manifest semantics differ")
    for name, expected in payloads.items():
        if (path / name).read_bytes() != expected:
            raise VerifiedConstructionCoreError(
                f"v0.14 generated member differs: {name}"
            )
    schema = _load_json(path / "schema.json")
    batch_schema = schema.get("v0_14_nine_us_address_locator_batch", {})
    if (
        batch_schema.get("accepted_project_count") != 9
        or batch_schema.get("official_boundary") is not False
        or batch_schema.get("geometry_types") != {"Point": 9}
        or batch_schema.get("geometry_use_scopes")
        != {"campus_locator": 8, "project_locator": 1}
    ):
        raise VerifiedConstructionCoreError(
            "v0.14 geometry schema seam differs"
        )
    notices = (b"United States Census", b"City of San Jose")
    for name in ("ATTRIBUTION.txt", "README.md", "map.html"):
        content = (path / name).read_bytes()
        if any(notice not in content for notice in notices):
            raise VerifiedConstructionCoreError(
                f"v0.14 attribution notice missing from {name}"
            )
    projects = {
        row["project_stable_key"]: row
        for row in _load_csv(path / "projects.csv", PROJECT_FIELDS)
        if row["project_stable_key"] in V14_PROJECT_KEYS
    }
    if set(projects) != V14_PROJECT_KEYS:
        raise VerifiedConstructionCoreError("v0.14 project inventory differs")
    contracts = _v14_contracts()
    evidence = {
        row["evidence_id"]: row
        for row in _load_csv(path / "evidence.csv", EVIDENCE_FIELDS)
    }
    for acceptance in contracts["acceptances"]:
        project_key = acceptance["project_stable_key"]
        project = projects[project_key]
        result = contracts["results_by_id"][acceptance["locator_id"]]
        semantics = contracts["semantics_by_profile"][
            acceptance["semantics_profile"]
        ]
        geometry_document = contracts["documents_by_id"][
            result["geometry_source_document_id"]
        ]
        geometry_evidence_id = _v14_capture_evidence(geometry_document)[
            "evidence_id"
        ]
        geometry_evidence = evidence.get(geometry_evidence_id)
        geometry = _parse_json_field(
            project["geometry_json"], "v0.14 project geometry"
        )
        if (
            geometry
            != {
                "coordinates": [result["longitude"], result["latitude"]],
                "type": "Point",
            }
            or project["geometry_type"] != "Point"
            or project["geometry_source_entity_kind"]
            != semantics["geometry_source_entity_kind"]
            or project["geometry_derivation"]
            != semantics["geometry_derivation"]
            or project["geometry_method"] != semantics["geometry_method"]
            or project["geometry_scope_class"]
            != semantics["geometry_scope_class"]
            or project["geometry_authority_class"] != "official_source"
            or project["geometry_use_scope"]
            != semantics["geometry_use_scope"]
            or project["geometry_precision_scope"]
            != semantics["precision_scope"]
            or project["horizontal_uncertainty_metres"]
            or project["independent_imagery_verification"] != "false"
            or project["verification_posture"]
            != "recent_authoritative_physical_observation_plus_reviewed_site_locator"
            or geometry_evidence is None
            or "geometry"
            not in _parse_json_field(
                geometry_evidence["roles_json"], "v0.14 geometry roles"
            )
            or project["project_id"]
            not in _parse_json_field(
                geometry_evidence["project_ids_json"],
                "v0.14 geometry projects",
            )
            or any(
                project[field] != "[]"
                for field in (
                    "workloads_json",
                    "role_claims_json",
                    "power_observations_json",
                    "annual_energy_observations_json",
                    "efficiency_observations_json",
                )
            )
            or any(
                project[field]
                for field in (
                    "owner",
                    "operator",
                    "users",
                    "tenants",
                    "customers",
                )
            )
        ):
            raise VerifiedConstructionCoreError(
                f"v0.14 project semantic scope differs: {project_key}"
            )
    qts = projects[
        "curated:qts-cedar-rapids-data-center-campus:current-campus-development"
    ]
    jlab = projects[
        "curated:jefferson-lab-newport-news-campus:jldc-building"
    ]
    capture = contracts["capture"]
    result_by_project = {
        row["project_stable_key"]: row for row in capture["results"]
    }
    if (
        qts["geometry_method"]
        != "qts_first_party_cdr1_dc1_page_map_pin_as_shared_campus_locator"
        or "Fairfax"
        not in result_by_project[qts["project_stable_key"]][
            "normalization_note"
        ]
        or "23605"
        not in result_by_project[jlab["project_stable_key"]]["matched_address"]
        or "23606"
        not in result_by_project[jlab["project_stable_key"]][
            "normalization_note"
        ]
    ):
        raise VerifiedConstructionCoreError(
            "v0.14 normalization disclosure differs"
        )
    for filename, key, fields in (
        ("projects.csv", "project_id", PROJECT_FIELDS),
        ("sites.csv", "site_id", SITE_FIELDS),
        ("evidence.csv", "evidence_id", EVIDENCE_FIELDS),
    ):
        inherited = {
            row[key]: row
            for row in _load_csv(LEGACY_PREVIEW_V13_DIR / filename, fields)
        }
        current = {row[key]: row for row in _load_csv(path / filename, fields)}
        if any(current[row_id] != row for row_id, row in inherited.items()):
            raise VerifiedConstructionCoreError(
                f"v0.14 inherited {filename} row changed"
            )
    return manifest


def validate_preview(path: Path = CURRENT_V14_PREVIEW_DIR) -> dict[str, Any]:
    """Validate current v0.14 or dispatch v0.1-v0.13 via frozen seams."""
    path = Path(path)
    manifest_path = path / "manifest.json"
    if (
        path.is_symlink()
        or not path.is_dir()
        or manifest_path.is_symlink()
        or not manifest_path.is_file()
    ):
        raise VerifiedConstructionCoreError("preview manifest trust root differs")
    manifest = _load_json(manifest_path)
    preview_id = manifest.get("preview_id") if isinstance(manifest, dict) else None
    if preview_id == CURRENT_V14_PREVIEW_ID:
        return _validate_v14_preview(path)
    return _validate_v13_preview_dispatch(path)


__all__ = [
    *__all__,
    "CURRENT_V14_PREVIEW_DIR",
    "CURRENT_V14_PREVIEW_ID",
    "CURRENT_V14_GEOMETRY_IDENTITY_REVIEW_DATE",
    "CURRENT_V14_LIFECYCLE_REFERENCE_DATE",
    "CURRENT_V14_REVIEW_DATE",
    "LEGACY_PREVIEW_V13_COMMIT",
    "LEGACY_PREVIEW_V13_DIR",
    "LEGACY_PREVIEW_V13_MANIFEST_SHA256",
    "V14_BATCH_CONTRACT",
    "V14_BATCH_CONTRACT_SHA256",
    "V14_CAPTURE_EVIDENCE_IDS",
    "V14_GEOMETRY_CAPTURE",
    "V14_GEOMETRY_CAPTURE_SHA256",
    "V14_PROJECT_KEYS",
    "build_v13_preview",
    "validate_frozen_v13",
]


_build_v14_preview = build_preview
_validate_v14_preview_dispatch = validate_preview
build_v14_preview = _build_v14_preview

LEGACY_PREVIEW_V14_DIR = CURRENT_V14_PREVIEW_DIR
LEGACY_PREVIEW_V14_MANIFEST_SHA256 = (
    "8599b3753a0fb2a62a48bbadf5d32e732bdd225a1b977a5f2502c33c866c06e7"
)
LEGACY_PREVIEW_V14_COMMIT = "76d488b38c46e6e2dd5bfb6f3fc3916e9fe17ca2"
CURRENT_V15_PREVIEW_ID = "2026-08-20-preview-v0.15"
CURRENT_V15_PREVIEW_DIR = ROOT / "verified_construction_core" / CURRENT_V15_PREVIEW_ID
CURRENT_V15_LIFECYCLE_REFERENCE_DATE = date(2026, 8, 20)
CURRENT_V15_GEOMETRY_IDENTITY_REVIEW_DATE = date(2026, 8, 23)
CURRENT_V15_REVIEW_DATE = CURRENT_V15_LIFECYCLE_REFERENCE_DATE
V15_BATCH_CONTRACT = (
    ROOT
    / "definitions"
    / "verified-construction-core-v0.15-five-global-site-geometry-batch.json"
)
V15_BATCH_CONTRACT_SHA256 = (
    "12472107c67d989a08f50773c76ebb27a354e9e7362080c19089c2829ab77295"
)
V15_GEOMETRY_CAPTURE = (
    ROOT
    / "sources"
    / "verified-construction-core-v0.15-five-global-site-geometry-facts.json"
)
V15_GEOMETRY_CAPTURE_SHA256 = (
    "9b77228521c563dc2603f132d902219d45f34babfe9c57ea845bac4b22a382c8"
)
V15_PROJECT_KEYS = frozenset(
    {
        "curated:atnorth-fin04-kouvola-campus:phase-1",
        "curated:cdc-beard-campus:be1-current-build",
        "curated:estructure-cal3-rocky-view-campus:initial-build",
        "curated:green-mountain-fra-mainz-campus:current-three-building-development",
        "curated:hyperco-loviisa-data-center-campus:current-development",
    }
)
V15_SELECTION_FIRST_FAILURE_COUNTS = {
    "entity_kind_not_project": 49,
    "not_in_reviewed_site_geometry_allowlist": 67,
    "selected": 76,
    "status_not_physical": 12,
    "status_outside_90_day_window": 327,
}
V15_CAPTURE_EVIDENCE_IDS = frozenset(
    {
        "275a19af-0f87-5309-a7b4-a466dea142d5",
        "29bdb34b-54f1-5e00-b28e-877efbf2e828",
        "4ea54f07-1a89-5363-94ed-ba23c10b334d",
        "6fa1a931-3b5c-5259-9b15-853187c9ae2a",
        "89aab19e-2eb3-59f5-a3c3-71366d5cb026",
        "9677a40a-6a16-59e3-b098-3bf415f47dfd",
        "a1f1c613-7606-5e9d-baf9-31cdb38d34f0",
        "f2719198-4703-59c9-96b5-e3d57823149f",
    }
)
V15_SOURCE_DOCUMENT_EXPECTATIONS = {
    "atnorth-fin04-kouvola-permit": (
        117956,
        "2305975f68b9880463abbe24359a83cfc760214da02228eb0009a3014245de79",
        "rights-status-uncertain-fact-extraction-only",
    ),
    "atnorth-fin04-nls-parcel": (
        32483,
        "534a72639b63c3b868d4f8b2f5bea61c444381e627ef01d3ee07712bdeb6bc1c",
        "CC-BY-4.0",
    ),
    "cdc-beard-be1-act-retired-block": (
        2563,
        "f13feaababe686fc3a97c45d7e30147bcaeaa95b25655e0c7d6139e9dc2e78df",
        "CC-BY-4.0",
    ),
    "cdc-beard-be1-landscape-plan": (
        1574536,
        "8da77a17f55bef0a288e5e17fd8ebd572d649b03d417a15842ab9fa6c718fc04",
        "rights-status-uncertain-fact-extraction-only",
    ),
    "estructure-cal3-datasheet": (
        577477,
        "befe2961598a8c4e307abea854dc288c804b4db0a7ba8945a20b49fed5adaad4",
        "all-rights-reserved-fact-extraction-only",
    ),
    "green-mountain-fra-mainz-energieatlas-point": (
        4598,
        "0ce08cddbf9abfd8377c390c2f7def9ba1df95ee6447a0400ae8d2a00ebacbde",
        "all-rights-reserved-fact-extraction-only",
    ),
    "hyperco-loviisa-building-permit": (
        167777,
        "1694e8038e5f30d2996103a7f27478530c088f19b2d4e64f0870d78a2ba6c42f",
        "rights-status-uncertain-fact-extraction-only",
    ),
    "hyperco-loviisa-ryhti-building-point": (
        4225,
        "37e4f018b66e8250a0523fa42ab8ef10e95c9649f5526c526ca210bb54826b22",
        "CC-BY-4.0",
    ),
}
V15_RESULT_EXPECTATIONS = {
    "atnorth-fin04-property-286-423-20-1": (
        "MultiPolygon",
        [26.76440419247778, 60.78344549044987],
        "atnorth-fin04-nls-parcel",
        ["atnorth-fin04-kouvola-permit"],
    ),
    "cdc-beard-be1-historical-block-24-section-11": (
        "Polygon",
        [149.20923007524493, -35.34021820343456],
        "cdc-beard-be1-act-retired-block",
        ["cdc-beard-be1-landscape-plan"],
    ),
    "estructure-cal3-datasheet-point": (
        "Point",
        [-113.91467685853051, 51.21083654630298],
        "estructure-cal3-datasheet",
        [],
    ),
    "green-mountain-fra-mainz-energieatlas-project-point": (
        "Point",
        [8.24061, 50.02764],
        "green-mountain-fra-mainz-energieatlas-point",
        [],
    ),
    "hyperco-loviisa-ryhti-building-104259934n": (
        "Point",
        [26.26809647, 60.47436397],
        "hyperco-loviisa-ryhti-building-point",
        ["hyperco-loviisa-building-permit"],
    ),
}
V15_COUNTRY_ISO_A2 = {
    "Australia": "AU",
    "Canada": "CA",
    "Finland": "FI",
    "Germany": "DE",
}


def validate_frozen_v14(
    path: Path = LEGACY_PREVIEW_V14_DIR,
) -> dict[str, Any]:
    """Validate byte-frozen v0.14 without regenerating it."""
    return _validate_frozen_preview_inventory(
        path,
        preview_id=CURRENT_V14_PREVIEW_ID,
        manifest_sha256=LEGACY_PREVIEW_V14_MANIFEST_SHA256,
        version_label="v0.14",
    )


def _v15_source_evidence(
    source: Mapping[str, Any], key: str, evidence_id: str
) -> dict[str, Any]:
    matches = [
        row
        for row in source.get("evidence", [])
        if isinstance(row, dict) and row.get("key") == key
    ]
    if len(matches) != 1:
        raise VerifiedConstructionCoreError(
            f"v0.15 source evidence differs: {key}"
        )
    row = matches[0]
    expected_id = atlas_stable_id(
        "evidence", "curated-official", key, row.get("content_hash")
    )
    if evidence_id != expected_id:
        raise VerifiedConstructionCoreError(
            f"v0.15 source evidence identifier differs: {key}"
        )
    return _v07_evidence_projection({**row, "evidence_id": evidence_id})


def _v15_capture_evidence(document: Mapping[str, Any]) -> dict[str, Any]:
    evidence_id = document.get("evidence_id")
    expected_id = atlas_stable_id(
        "evidence",
        "verified-construction-core-v0.15",
        document.get("source_id"),
        document.get("content_hash"),
    )
    if (
        not isinstance(evidence_id, str)
        or evidence_id != expected_id
        or evidence_id not in V15_CAPTURE_EVIDENCE_IDS
    ):
        raise VerifiedConstructionCoreError(
            "v0.15 capture evidence identifier differs"
        )
    return _v07_evidence_projection(document)


def _v15_result_geometry(record: Mapping[str, Any]) -> dict[str, Any]:
    direct = record.get("geometry")
    encoded = record.get("geometry_encoding")
    if (direct is None) == (encoded is None):
        raise VerifiedConstructionCoreError(
            "v0.15 geometry representation differs"
        )
    if direct is not None:
        geometry = direct
    else:
        if not isinstance(encoded, dict) or set(encoded) != {
            "codec",
            "data",
            "uncompressed_bytes",
            "uncompressed_sha256",
        }:
            raise VerifiedConstructionCoreError(
                "v0.15 encoded geometry structure differs"
            )
        if encoded.get("codec") != "zlib+base64-canonical-json":
            raise VerifiedConstructionCoreError(
                "v0.15 encoded geometry codec differs"
            )
        try:
            compressed = base64.b64decode(encoded["data"], validate=True)
            raw = zlib.decompress(compressed)
            geometry = json.loads(raw.decode("utf-8"))
        except (
            binascii.Error,
            KeyError,
            TypeError,
            UnicodeDecodeError,
            json.JSONDecodeError,
            zlib.error,
        ) as error:
            raise VerifiedConstructionCoreError(
                "v0.15 encoded geometry cannot be decoded"
            ) from error
        if (
            len(raw) != encoded["uncompressed_bytes"]
            or _sha256_bytes(raw) != encoded["uncompressed_sha256"]
            or raw != _json_bytes(geometry)
        ):
            raise VerifiedConstructionCoreError(
                "v0.15 encoded geometry canonical bytes differ"
            )
    if not isinstance(geometry, dict) or set(geometry) != {
        "type",
        "coordinates",
    }:
        raise VerifiedConstructionCoreError(
            "v0.15 geometry structure differs"
        )

    def validate_position(position: Any) -> None:
        if (
            not isinstance(position, list)
            or len(position) != 2
            or any(type(value) not in {int, float} for value in position)
            or any(not math.isfinite(value) for value in position)
            or not -180 <= position[0] <= 180
            or not -90 <= position[1] <= 90
        ):
            raise VerifiedConstructionCoreError(
                "v0.15 geometry coordinate differs"
            )

    def validate_polygon(coordinates: Any) -> None:
        if not isinstance(coordinates, list) or not coordinates:
            raise VerifiedConstructionCoreError(
                "v0.15 polygon structure differs"
            )
        for ring in coordinates:
            if (
                not isinstance(ring, list)
                or len(ring) < 4
                or ring[0] != ring[-1]
            ):
                raise VerifiedConstructionCoreError(
                    "v0.15 polygon ring differs"
                )
            for position in ring:
                validate_position(position)

    geometry_type = geometry["type"]
    coordinates = geometry["coordinates"]
    if geometry_type == "Point":
        validate_position(coordinates)
    elif geometry_type == "Polygon":
        validate_polygon(coordinates)
    elif geometry_type == "MultiPolygon":
        if not isinstance(coordinates, list) or not coordinates:
            raise VerifiedConstructionCoreError(
                "v0.15 multipolygon structure differs"
            )
        for polygon in coordinates:
            validate_polygon(polygon)
    else:
        raise VerifiedConstructionCoreError("v0.15 geometry type differs")
    anchor = record.get("display_anchor")
    if not isinstance(anchor, dict):
        raise VerifiedConstructionCoreError("v0.15 display anchor differs")
    validate_position(anchor.get("coordinates"))
    return geometry


def _v15_contracts() -> dict[str, Any]:
    if (
        V15_BATCH_CONTRACT.is_symlink()
        or not V15_BATCH_CONTRACT.is_file()
        or _sha256_file(V15_BATCH_CONTRACT) != V15_BATCH_CONTRACT_SHA256
        or V15_GEOMETRY_CAPTURE.is_symlink()
        or not V15_GEOMETRY_CAPTURE.is_file()
        or _sha256_file(V15_GEOMETRY_CAPTURE) != V15_GEOMETRY_CAPTURE_SHA256
    ):
        raise VerifiedConstructionCoreError("v0.15 contract hash differs")
    contract = _load_json(V15_BATCH_CONTRACT)
    capture = _load_json(V15_GEOMETRY_CAPTURE)
    base = contract.get("base_preview", {})
    capture_binding = contract.get("geometry_capture", {})
    if (
        contract.get("contract_id")
        != "verified-construction-core-v0.15-five-global-site-geometry-batch"
        or contract.get("reviewed_as_of")
        != CURRENT_V15_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
        or contract.get("cohort_lifecycle_reference_date")
        != CURRENT_V15_LIFECYCLE_REFERENCE_DATE.isoformat()
        or base.get("preview_id") != CURRENT_V14_PREVIEW_ID
        or base.get("manifest_sha256")
        != LEGACY_PREVIEW_V14_MANIFEST_SHA256
        or base.get("commit") != LEGACY_PREVIEW_V14_COMMIT
        or capture_binding.get("path")
        != V15_GEOMETRY_CAPTURE.relative_to(ROOT).as_posix()
        or capture_binding.get("bytes") != V15_GEOMETRY_CAPTURE.stat().st_size
        or capture_binding.get("sha256") != V15_GEOMETRY_CAPTURE_SHA256
        or capture_binding.get("geometry_record_count") != 5
        or capture_binding.get("evidence_document_count") != 8
        or capture.get("capture_id")
        != "verified-construction-core-v0.15-five-global-site-geometry-facts"
        or capture.get("captured_on")
        != CURRENT_V15_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
        or capture.get("cohort_lifecycle_reference_date")
        != CURRENT_V15_LIFECYCLE_REFERENCE_DATE.isoformat()
    ):
        raise VerifiedConstructionCoreError("v0.15 batch contract differs")
    invariants = contract.get("batch_invariants")
    if invariants != {
        "accepted_physical_site_count": 5,
        "accepted_project_count": 5,
        "geometry_authority_classes": {"official_source": 5},
        "geometry_types": {"MultiPolygon": 1, "Point": 3, "Polygon": 1},
        "geometry_use_scopes": {"campus_locator": 1, "project_locator": 4},
        "independent_imagery_verification": False,
        "official_boundary": False,
        "raw_all_rights_artifacts_redistributed": False,
    }:
        raise VerifiedConstructionCoreError("v0.15 batch invariants differ")
    documents = capture.get("source_documents")
    results = capture.get("results")
    acceptances = contract.get("acceptances")
    if (
        not isinstance(documents, list)
        or len(documents) != 8
        or any(not isinstance(row, dict) for row in documents)
        or not isinstance(results, list)
        or len(results) != 5
        or any(not isinstance(row, dict) for row in results)
        or not isinstance(acceptances, list)
        or len(acceptances) != 5
        or any(not isinstance(row, dict) for row in acceptances)
        or {row.get("project_stable_key") for row in acceptances}
        != V15_PROJECT_KEYS
    ):
        raise VerifiedConstructionCoreError("v0.15 batch inventory differs")
    documents_by_id = {row.get("source_id"): row for row in documents}
    results_by_id = {row.get("locator_id"): row for row in results}
    if (
        len(documents_by_id) != 8
        or set(documents_by_id) != set(V15_SOURCE_DOCUMENT_EXPECTATIONS)
        or len(results_by_id) != 5
        or set(results_by_id) != set(V15_RESULT_EXPECTATIONS)
    ):
        raise VerifiedConstructionCoreError("v0.15 capture identities differ")
    if {
        _v15_capture_evidence(document)["evidence_id"]
        for document in documents
    } != V15_CAPTURE_EVIDENCE_IDS:
        raise VerifiedConstructionCoreError(
            "v0.15 capture evidence inventory differs"
        )
    for document in documents:
        raw_capture = document.get("raw_capture")
        expected_bytes, expected_hash, expected_license = (
            V15_SOURCE_DOCUMENT_EXPECTATIONS[document["source_id"]]
        )
        if (
            not isinstance(raw_capture, dict)
            or set(raw_capture) != {"bytes", "sha256", "redistributed"}
            or raw_capture.get("bytes") != expected_bytes
            or raw_capture.get("sha256") != expected_hash
            or document.get("content_hash") != expected_hash
            or document.get("license") != expected_license
            or raw_capture.get("redistributed") is not False
        ):
            raise VerifiedConstructionCoreError(
                "v0.15 raw-source guardrail differs"
            )
        if document.get("license") == "CC-BY-4.0" and (
            not isinstance(document.get("rights_urls"), list)
            or "https://creativecommons.org/licenses/by/4.0/"
            not in document["rights_urls"]
        ):
            raise VerifiedConstructionCoreError(
                "v0.15 CC BY 4.0 rights basis differs"
            )
    sources: dict[str, Mapping[str, Any]] = {}
    geometries_by_id: dict[str, dict[str, Any]] = {}
    seen_campuses: set[str] = set()
    for acceptance in acceptances:
        project_key = acceptance["project_stable_key"]
        campus_key = acceptance["parent_campus_stable_key"]
        source_binding = acceptance["source_input"]
        source_path = _repository_input(
            source_binding["path"],
            source_binding["sha256"],
            "v0.15 curated source",
        )
        if source_path.stat().st_size != source_binding["bytes"]:
            raise VerifiedConstructionCoreError(
                f"v0.15 source bytes differ: {project_key}"
            )
        source = _load_json(source_path)
        project = source.get("project")
        campus = source.get("campus")
        result = results_by_id.get(acceptance.get("locator_id"))
        expected = V15_RESULT_EXPECTATIONS.get(acceptance.get("locator_id"))
        if (
            not isinstance(project, dict)
            or not isinstance(campus, dict)
            or project.get("stable_key") != project_key
            or campus.get("stable_key") != campus_key
            or project.get("country") not in V15_COUNTRY_ISO_A2
            or campus.get("country") != project.get("country")
            or project.get("coordinates") is not None
            or project.get("geometry") is not None
            or campus.get("coordinates") is not None
            or campus.get("geometry") is not None
            or acceptance["project_entity_id"]
            != atlas_stable_id("entity", project_key, "project")
            or acceptance["parent_campus_entity_id"]
            != atlas_stable_id("entity", campus_key, "campus")
            or campus_key in seen_campuses
            or not isinstance(result, dict)
            or expected is None
            or result.get("project_stable_key") != project_key
            or result.get("geometry_source_document_id") != expected[2]
            or result.get("identity_source_document_ids") != expected[3]
            or result.get("display_anchor", {}).get("coordinates") != expected[1]
            or result.get("geometry_source_document_id") not in documents_by_id
            or any(
                source_id not in documents_by_id
                for source_id in result.get("identity_source_document_ids", [])
            )
        ):
            raise VerifiedConstructionCoreError(
                f"v0.15 source or locator identity differs: {project_key}"
            )
        geometry = _v15_result_geometry(result)
        if geometry.get("type") != expected[0]:
            raise VerifiedConstructionCoreError(
                f"v0.15 geometry type differs: {project_key}"
            )
        semantics = result.get("semantics")
        if (
            not isinstance(semantics, dict)
            or semantics.get("geometry_authority_class") != "official_source"
            or semantics.get("geometry_use_scope")
            not in {"campus_locator", "project_locator"}
            or semantics.get("official_boundary") is not False
            or not isinstance(semantics.get("precision_scope"), str)
            or not semantics["precision_scope"]
            or not isinstance(
                semantics.get("horizontal_uncertainty_unknown_reason"), str
            )
            or not semantics["horizontal_uncertainty_unknown_reason"]
        ):
            raise VerifiedConstructionCoreError(
                f"v0.15 geometry semantics differ: {project_key}"
            )
        relationship = acceptance.get("relationship")
        if (
            not isinstance(relationship, dict)
            or not str(relationship.get("relationship_id", "")).startswith(
                "relationship:"
            )
            or relationship.get("relationship_type") != "project_targets"
            or relationship.get("decision_basis") != "explicit_parent"
        ):
            raise VerifiedConstructionCoreError(
                f"v0.15 project-to-campus relationship differs: {project_key}"
            )
        status = acceptance.get("status")
        lifecycle = [
            row
            for row in source.get("lifecycle", [])
            if isinstance(row, dict)
            and row.get("entity") == "project"
            and row.get("evidence_key") == status.get("evidence_key")
        ]
        if (
            not isinstance(status, dict)
            or len(lifecycle) != 1
            or lifecycle[0].get("value") != status.get("value")
            or lifecycle[0].get("as_of_date") != status.get("as_of_date")
            or lifecycle[0].get("method") != status.get("method")
            or lifecycle[0].get("confidence") != status.get("confidence")
            or status.get("value") not in PHYSICAL_STATUSES
            or status.get("method") not in AUTHORITATIVE_STATUS_METHODS
            or not 0
            <= (
                CURRENT_V15_LIFECYCLE_REFERENCE_DATE
                - _calendar_date(status["as_of_date"], "v0.15 status_as_of")
            ).days
            <= MAX_STATUS_AGE_DAYS
        ):
            raise VerifiedConstructionCoreError(
                f"v0.15 lifecycle differs: {project_key}"
            )
        _v15_source_evidence(
            source, status["evidence_key"], status["evidence_id"]
        )
        location = acceptance.get("location_evidence")
        if not isinstance(location, dict):
            raise VerifiedConstructionCoreError(
                f"v0.15 location evidence differs: {project_key}"
            )
        _v15_source_evidence(source, location["key"], location["evidence_id"])
        seen_campuses.add(campus_key)
        sources[project_key] = source
        geometries_by_id[acceptance["locator_id"]] = geometry
    fin = documents_by_id["atnorth-fin04-nls-parcel"]
    hyperco_permit = documents_by_id["hyperco-loviisa-building-permit"]
    hyperco_point = documents_by_id["hyperco-loviisa-ryhti-building-point"]
    cdc_plan = documents_by_id["cdc-beard-be1-landscape-plan"]
    cdc_block = documents_by_id["cdc-beard-be1-act-retired-block"]
    mainz = documents_by_id["green-mountain-fra-mainz-energieatlas-point"]
    if (
        fin.get("facts", {}).get("national_cadastral_reference")
        != "28642300200001"
        or fin.get("facts", {}).get("feature_ids")
        != [
            "FI_CP_CADASTRALPARCEL_422233887",
            "FI_CP_CADASTRALPARCEL_422233892",
            "FI_CP_CADASTRALPARCEL_424036070",
        ]
        or fin.get("rights_metadata")
        != {
            "bytes": 38857,
            "redistributed": False,
            "sha256": (
                "5f80971db6c2bdcc95a09b6a04ac1e817cb1bdd9140356c4c0ef4a6ff3d48afb"
            ),
            "source_url": (
                "https://www.maanmittauslaitos.fi/en/opendata-licence-cc40"
            ),
        }
        or hyperco_permit.get("facts", {}).get("permit_identifier")
        != "2025-157"
        or hyperco_point.get("facts", {}).get("permanent_permit_identifier")
        != "302396994R"
        or hyperco_point.get("facts", {}).get("property_identifier")
        != "43440500010033"
        or hyperco_point.get("rights_metadata")
        != {
            "bytes": 232513,
            "redistributed": False,
            "sha256": (
                "62134c6821a748c5a5a89dc6d3d284a7882e3ae9831564a711e9ef9ad687571e"
            ),
            "source_url": "https://ryhti.syke.fi/en/front-page/services/",
        }
        or cdc_plan.get("facts", {}).get("project") != "BE1 DATA CENTRE"
        or cdc_plan.get("facts", {}).get("block") != 24
        or cdc_plan.get("facts", {}).get("section") != 11
        or cdc_block.get("facts", {}).get("lifecycle_stage") != "RETIRED"
        or cdc_block.get("publisher") != "Australian Capital Territory"
        or cdc_block.get("attribution")
        != (
            "Australian Capital Territory cadastral data, licensed under "
            "CC BY 4.0; retrieved 2026-08-23."
        )
        or cdc_block.get("rights_urls")
        != [
            (
                "https://www.arcgis.com/sharing/rest/content/items/"
                "802b1fe9b1bc480ba41d6a653ec40b62?f=json"
            ),
            "https://creativecommons.org/licenses/by/4.0/",
            "https://www.actmapi.act.gov.au/terms-and-conditions",
        ]
        or not str(cdc_block.get("source_url", "")).startswith(
            "https://services1.arcgis.com/E5n4f1VY84i0xSjy/ArcGIS/"
            "rest/services/ACTGOV_BLOCKS/FeatureServer/0/query?"
        )
        or cdc_block.get("rights_metadata")
        != {
            "item_metadata": {
                "bytes": 4366,
                "redistributed": False,
                "sha256": (
                    "1b2a42f718e59418e1688639b1a79571bd8896a9396d650b0ed1b448088a0ce3"
                ),
                "source_url": (
                    "https://www.arcgis.com/sharing/rest/content/items/"
                    "802b1fe9b1bc480ba41d6a653ec40b62?f=json"
                ),
            },
            "layer_id": 0,
            "layer_name": "ACTGOV BLOCK",
            "license_label": "Creative Commons By Attribution 4.0",
            "license_url": "https://creativecommons.org/licenses/by/4.0/",
            "licensor": "Australian Capital Territory",
            "redistributed": False,
            "retrieved_at": "2026-08-23",
            "service_item_id": "802b1fe9b1bc480ba41d6a653ec40b62",
            "service_metadata": {
                "bytes": 7885,
                "redistributed": False,
                "sha256": (
                    "058ce2f6b406a3355151af5c6009fea6434182f5c5ee0e5ed03c770ed2684743"
                ),
                "source_url": (
                    "https://services1.arcgis.com/E5n4f1VY84i0xSjy/ArcGIS/"
                    "rest/services/ACTGOV_BLOCKS/FeatureServer?f=pjson"
                ),
            },
            "terms_url": "https://www.actmapi.act.gov.au/terms-and-conditions",
        }
        or mainz.get("facts", {}).get("feature_id") != "praxisbeispiele.149"
        or mainz.get("facts", {}).get("partners")
        != ["Green Mountain", "Kraftwerke Mainz-Wiesbaden AG (KMW)"]
    ):
        raise VerifiedConstructionCoreError(
            "v0.15 source-specific identity guardrail differs"
        )
    _v14_validate_hydrated_crosscheck(contract, acceptances, sources)
    return {
        "contract": contract,
        "capture": capture,
        "acceptances": sorted(
            acceptances, key=lambda row: row["project_stable_key"]
        ),
        "documents_by_id": documents_by_id,
        "geometries_by_id": geometries_by_id,
        "results_by_id": results_by_id,
        "sources": sources,
    }


def _v15_build_delta_rows(contracts: Mapping[str, Any]) -> dict[str, Any]:
    evidence_pool: dict[str, dict[str, Any]] = {}
    evidence_usage: dict[str, dict[str, set[str]]] = defaultdict(
        lambda: {"roles": set(), "project_ids": set()}
    )
    projects: list[dict[str, Any]] = []
    sites: list[dict[str, Any]] = []
    for acceptance in contracts["acceptances"]:
        project_key = acceptance["project_stable_key"]
        project_id = acceptance["project_entity_id"]
        campus_key = acceptance["parent_campus_stable_key"]
        source = contracts["sources"][project_key]
        project = source["project"]
        campus = source["campus"]
        status = acceptance["status"]
        location = acceptance["location_evidence"]
        result = contracts["results_by_id"][acceptance["locator_id"]]
        semantics = result["semantics"]
        for binding, role in (
            (status, "physical_status"),
            (location, "context:source_location_identity"),
        ):
            key = binding.get("evidence_key", binding.get("key"))
            evidence_id = binding["evidence_id"]
            projected = _v15_source_evidence(source, key, evidence_id)
            previous = evidence_pool.get(evidence_id)
            if previous is not None and previous != projected:
                raise VerifiedConstructionCoreError(
                    f"v0.15 evidence collision differs: {project_key}"
                )
            evidence_pool[evidence_id] = projected
            evidence_usage[evidence_id]["roles"].add(role)
            evidence_usage[evidence_id]["project_ids"].add(project_id)
        geometry_source_id = result["geometry_source_document_id"]
        for source_id, role in (
            (geometry_source_id, "geometry"),
            *(
                (source_id, "context:geometry_identity")
                for source_id in result["identity_source_document_ids"]
            ),
        ):
            projected = _v15_capture_evidence(
                contracts["documents_by_id"][source_id]
            )
            evidence_id = projected["evidence_id"]
            previous = evidence_pool.get(evidence_id)
            if previous is not None and previous != projected:
                raise VerifiedConstructionCoreError(
                    f"v0.15 capture evidence collision differs: {project_key}"
                )
            evidence_pool[evidence_id] = projected
            evidence_usage[evidence_id]["roles"].add(role)
            evidence_usage[evidence_id]["project_ids"].add(project_id)
        geometry_document = contracts["documents_by_id"][geometry_source_id]
        geometry_evidence_id = _v15_capture_evidence(geometry_document)[
            "evidence_id"
        ]
        geometry = contracts["geometries_by_id"][acceptance["locator_id"]]
        longitude, latitude = result["display_anchor"]["coordinates"]
        geometry_json = _json_bytes(geometry).decode().strip()
        site_id = _stable_id("vcc-site", campus_key)
        country = project["country"]
        country_iso = V15_COUNTRY_ISO_A2[country]
        status_date = _calendar_date(status["as_of_date"], "v0.15 status_as_of")
        project_row = {
            "project_id": project_id,
            "project_stable_key": project_key,
            "site_id": site_id,
            "physical_site_stable_key": campus_key,
            "name": project["name"],
            "country": country,
            "country_iso_a2": country_iso,
            "latitude": latitude,
            "longitude": longitude,
            "geometry_json": geometry_json,
            "geometry_type": geometry["type"],
            "geometry_source_entity_kind": semantics[
                "geometry_source_entity_kind"
            ],
            "geometry_derivation": semantics["geometry_derivation"],
            "geometry_method": semantics["geometry_method"],
            "geometry_scope_class": semantics["geometry_scope_class"],
            "geometry_authority_class": semantics[
                "geometry_authority_class"
            ],
            "geometry_use_scope": semantics["geometry_use_scope"],
            "geometry_precision_scope": semantics["precision_scope"],
            "horizontal_uncertainty_metres": "",
            "horizontal_uncertainty_unknown_reason": semantics[
                "horizontal_uncertainty_unknown_reason"
            ],
            "geometry_evidence_id": geometry_evidence_id,
            "last_observed_physical_status": status["value"],
            "status_as_of": status["as_of_date"],
            "status_age_days_at_review": (
                CURRENT_V15_LIFECYCLE_REFERENCE_DATE - status_date
            ).days,
            "status_method": status["method"],
            "status_evidence_id": status["evidence_id"],
            "verification_posture": _verification_posture(
                semantics["geometry_authority_class"],
                semantics["geometry_use_scope"],
            ),
            "independent_imagery_verification": "false",
            "imagery_review_outcome": "not_reviewed_for_core_preview",
            "development_type": "unknown",
            "development_type_unknown_reason": (
                "not selected by the v0.15 global geometry batch"
            ),
            "operating_model": "unknown",
            "operating_model_unknown_reason": (
                "not established by selected evidence"
            ),
            "operating_model_evidence_id": "",
            "workloads_json": "[]",
            "workload_unknown_reason": "not established by selected evidence",
            "role_claims_json": "[]",
            "power_observations_json": "[]",
            "power_unknown_reason": (
                "no typed project power observation selected by this batch; "
                "not estimated"
            ),
            "annual_energy_observations_json": "[]",
            "annual_energy_unknown_reason": (
                "no scoped annual-energy inputs selected; not estimated"
            ),
            "efficiency_observations_json": "[]",
            "efficiency_unknown_reason": (
                "no scoped PUE or WUE observation selected"
            ),
            "owner": "",
            "operator": "",
            "users": "",
            "tenants": "",
            "customers": "",
            "status_source_url": evidence_pool[status["evidence_id"]][
                "source_url"
            ],
            "geometry_source_url": geometry_document["source_url"],
        }
        projects.append(project_row)
        sites.append(
            {
                "site_id": site_id,
                "physical_site_stable_key": campus_key,
                "name": campus["name"],
                "country": country,
                "country_iso_a2": country_iso,
                "latitude": latitude,
                "longitude": longitude,
                "geometry_json": geometry_json,
                "geometry_type": geometry["type"],
                "geometry_source_entity_kinds_json": _json_bytes(
                    [semantics["geometry_source_entity_kind"]]
                ).decode().strip(),
                "geometry_derivations_json": _json_bytes(
                    [semantics["geometry_derivation"]]
                ).decode().strip(),
                "geometry_methods_json": _json_bytes(
                    [semantics["geometry_method"]]
                ).decode().strip(),
                "geometry_scope_classes_json": _json_bytes(
                    [semantics["geometry_scope_class"]]
                ).decode().strip(),
                "geometry_authority_classes_json": _json_bytes(
                    [semantics["geometry_authority_class"]]
                ).decode().strip(),
                "geometry_use_scopes_json": _json_bytes(
                    [semantics["geometry_use_scope"]]
                ).decode().strip(),
                "geometry_precision_scopes_json": _json_bytes(
                    [semantics["precision_scope"]]
                ).decode().strip(),
                "horizontal_uncertainty_metres": "",
                "horizontal_uncertainty_unknown_reason": semantics[
                    "horizontal_uncertainty_unknown_reason"
                ],
                "geometry_evidence_ids_json": _json_bytes(
                    [geometry_evidence_id]
                ).decode().strip(),
                "project_count": 1,
                "project_ids_json": _json_bytes([project_id]).decode().strip(),
                "project_stable_keys_json": _json_bytes([project_key])
                .decode()
                .strip(),
                "statuses_json": _json_bytes([status["value"]]).decode().strip(),
                "oldest_status_as_of": status["as_of_date"],
                "newest_status_as_of": status["as_of_date"],
                "verification_posture": project_row["verification_posture"],
                "independent_imagery_verification": "false",
                "imagery_review_outcomes_json": _json_bytes(
                    ["not_reviewed_for_core_preview"]
                ).decode().strip(),
            }
        )
    evidence_rows = [
        {
            **evidence_pool[evidence_id],
            "roles_json": _json_bytes(sorted(usage["roles"])).decode().strip(),
            "project_ids_json": _json_bytes(sorted(usage["project_ids"]))
            .decode()
            .strip(),
        }
        for evidence_id, usage in sorted(evidence_usage.items())
    ]
    if (
        len(projects) != 5
        or len(sites) != 5
        or len(evidence_rows) != 13
        or len({row["project_id"] for row in projects}) != 5
        or len({row["site_id"] for row in sites}) != 5
        or len({row["evidence_id"] for row in evidence_rows}) != 13
    ):
        raise VerifiedConstructionCoreError("v0.15 delta accounting differs")
    return {"projects": projects, "sites": sites, "evidence": evidence_rows}


def _v15_portable_source_inputs(
    contracts: Mapping[str, Any],
) -> list[dict[str, Any]]:
    base_manifest = _load_json(LEGACY_PREVIEW_V14_DIR / "manifest.json")
    rows = list(base_manifest["portable_source_inputs"])
    rows.extend(
        [
            {
                "path": V15_BATCH_CONTRACT.relative_to(ROOT).as_posix(),
                "bytes": V15_BATCH_CONTRACT.stat().st_size,
                "sha256": V15_BATCH_CONTRACT_SHA256,
                "parent_manifests": [],
            },
            {
                "path": V15_GEOMETRY_CAPTURE.relative_to(ROOT).as_posix(),
                "bytes": V15_GEOMETRY_CAPTURE.stat().st_size,
                "sha256": V15_GEOMETRY_CAPTURE_SHA256,
                "parent_manifests": [],
            },
            *(
                {
                    **acceptance["source_input"],
                    "parent_manifests": [],
                }
                for acceptance in contracts["acceptances"]
            ),
        ]
    )
    rows.sort(key=lambda row: row["path"])
    if len(rows) != 107 or len({row["path"] for row in rows}) != 107:
        raise VerifiedConstructionCoreError("v0.15 portable input count differs")
    for row in rows:
        path = _repository_input(
            row["path"], row["sha256"], "v0.15 portable input"
        )
        if path.stat().st_size != row["bytes"]:
            raise VerifiedConstructionCoreError(
                "v0.15 portable input byte count differs"
            )
    return rows


def _v15_review_queue(
    contracts: Mapping[str, Any],
) -> list[dict[str, Any]]:
    rows = []
    for acceptance in contracts["acceptances"]:
        result = contracts["results_by_id"][acceptance["locator_id"]]
        semantics = result["semantics"]
        geometry_document = contracts["documents_by_id"][
            result["geometry_source_document_id"]
        ]
        use_scope = semantics["geometry_use_scope"]
        target_is_campus = use_scope == "campus_locator"
        rows.append(
            {
                "batch_contract_path": V15_BATCH_CONTRACT.relative_to(
                    ROOT
                ).as_posix(),
                "batch_contract_sha256": V15_BATCH_CONTRACT_SHA256,
                "decision": "accepted",
                "decision_reason": acceptance["scope_guardrail"],
                "geometry_authority_class": semantics[
                    "geometry_authority_class"
                ],
                "geometry_derivation": semantics["geometry_derivation"],
                "geometry_entity": semantics["geometry_source_entity_kind"],
                "geometry_evidence_id": _v15_capture_evidence(
                    geometry_document
                )["evidence_id"],
                "geometry_evidence_key": geometry_document["source_id"],
                "geometry_method": semantics["geometry_method"],
                "geometry_scope_class": semantics["geometry_scope_class"],
                "geometry_target_entity_id": (
                    acceptance["parent_campus_entity_id"]
                    if target_is_campus
                    else acceptance["project_entity_id"]
                ),
                "geometry_target_entity_kind": (
                    "campus" if target_is_campus else "project"
                ),
                "geometry_target_entity_stable_key": (
                    acceptance["parent_campus_stable_key"]
                    if target_is_campus
                    else acceptance["project_stable_key"]
                ),
                "geometry_use_scope": use_scope,
                "horizontal_uncertainty_metres": None,
                "horizontal_uncertainty_unknown_reason": semantics[
                    "horizontal_uncertainty_unknown_reason"
                ],
                "locator_id": acceptance["locator_id"],
                "official_boundary": False,
                "precision_scope": semantics["precision_scope"],
                "project_to_campus_decision_basis": "explicit_parent",
                "project_to_campus_relationship_id": acceptance[
                    "relationship"
                ]["relationship_id"],
                "project_to_campus_relationship_type": "project_targets",
                "rejected_claims": contracts["contract"]["rejected_claims"],
                "reviewed_at": (
                    CURRENT_V15_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
                ),
                "source_input_bytes": acceptance["source_input"]["bytes"],
                "source_input_path": acceptance["source_input"]["path"],
                "source_input_sha256": acceptance["source_input"]["sha256"],
                "source_project_entity_id": acceptance["project_entity_id"],
                "source_project_stable_key": acceptance[
                    "project_stable_key"
                ],
            }
        )
    return rows


def _v15_context_evidence_bindings(
    contracts: Mapping[str, Any],
) -> list[dict[str, Any]]:
    rows = []
    for acceptance in contracts["acceptances"]:
        location = acceptance["location_evidence"]
        source_input = acceptance["source_input"]
        rows.append(
            {
                "evidence_id": location["evidence_id"],
                "evidence_key": location["key"],
                "project_stable_key": acceptance["project_stable_key"],
                "semantic_scope": acceptance["scope_guardrail"],
                "source_input_path": source_input["path"],
                "source_input_sha256": source_input["sha256"],
                "usage_role": "geometry_identity",
            }
        )
    return rows


def _v15_map_html(
    geojson: Mapping[str, Any],
    sites: Sequence[Mapping[str, Any]],
    evidence: Sequence[Mapping[str, Any]],
) -> bytes:
    document = _v14_map_html(geojson, sites, evidence).decode("utf-8")
    title = "Verified Construction Core v0.14 preview"
    if document.count(title) != 1:
        raise VerifiedConstructionCoreError("v0.15 map title seam differs")
    document = document.replace(
        title, "Verified Construction Core v0.15 preview", 1
    )
    marker = '. <a href="ATTRIBUTION.txt">Full attribution and source terms</a>.'
    notices = (
        ' · <a href="https://www.maanmittauslaitos.fi/en/'
        'opendata-licence-cc40">National Land Survey of Finland</a> — '
        'CC BY 4.0 · <a href="https://ryhti.syke.fi/en/front-page/services/">'
        'Ryhti</a> — CC BY 4.0 · <a href="https://www.arcgis.com/sharing/'
        'rest/content/items/802b1fe9b1bc480ba41d6a653ec40b62?f=json">'
        'Australian Capital Territory cadastral data</a> — CC BY 4.0 · '
        '<a href="https://www.energieatlas.rlp.de/earp/praxisbeispiele/'
        'projektsteckbriefe/projekt-steckbriefe/anzeigen/unternehmen/149">'
        'Energieatlas Rheinland-Pfalz</a> — compact facts only'
    )
    if document.count(marker) != 1:
        raise VerifiedConstructionCoreError(
            "v0.15 map attribution seam differs"
        )
    document = document.replace(marker, f"{notices}{marker}", 1)
    return document.encode("utf-8")


def _v15_schema() -> dict[str, Any]:
    schema = _v14_schema()
    schema["format"] = "datacenter-atlas-verified-construction-core-schema-v15"
    schema["preview_id"] = CURRENT_V15_PREVIEW_ID
    schema["v0_15_five_global_site_geometry_batch"] = {
        "accepted_project_count": 5,
        "accepted_physical_site_count": 5,
        "geometry_authority_classes": {"official_source": 5},
        "geometry_types": {"MultiPolygon": 1, "Point": 3, "Polygon": 1},
        "geometry_use_scopes": {
            "campus_locator": 1,
            "project_locator": 4,
        },
        "official_boundary": False,
        "coordinate_semantics": (
            "Three official points, one exact NLS MultiPolygon, and one exact "
            "ACT Polygon locate five distinct non-US sites. The NLS property "
            "is only a shared-campus locator, and the ACT source explicitly "
            "marks Block 24 Section 11 RETIRED, making it only a historical "
            "BE1 project locator. No v0.15 geometry is a publication-target "
            "official boundary or current-work footprint."
        ),
        "horizontal_uncertainty_semantics": (
            "Every v0.15 row leaves horizontal_uncertainty_metres empty because "
            "the selected sources publish no bounded numeric accuracy applicable "
            "to the selected geometry; coordinate digits and transformation do "
            "not add accuracy."
        ),
        "rights_semantics": (
            "NLS, Ryhti, and ACT derived geometries are attributed under CC BY "
            "4.0. CAL-3, the two permits, the BE1 plan, and Energieatlas remain "
            "compact fact extractions with raw PDF, HTML, and response bytes "
            "excluded."
        ),
    }
    schema["v0_15_temporal_scope"] = {
        "reviewed_at_semantics": (
            "Legacy alias for cohort_lifecycle_reference_date, retained for "
            "preview compatibility; it is not the geometry-identity review date."
        ),
        "cohort_lifecycle_reference_date": (
            CURRENT_V15_LIFECYCLE_REFERENCE_DATE.isoformat()
        ),
        "geometry_identity_reviewed_at": (
            CURRENT_V15_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
        ),
    }
    return schema


def _v15_readme(report: Mapping[str, Any], legal_notice: str) -> bytes:
    return f"""# Verified Construction Core v0.15 preview

This tracked, non-final preview contains **{report['selected_physical_site_count']} physical sites**
and **{report['selected_project_count']} linked projects** in {len(report['country_counts'])}
countries. Every project retains a dated authoritative physical-status observation and a reviewed
geometry scope. Five project rows carry official boundaries; the remaining 71 are explicit project
or campus locators, never construction footprints by implication.

The five-row v0.15 delta adds eStruxture CAL-3 in Canada, atNorth FIN04 and Hyperco Loviisa in
Finland, CDC Beard BE1 in Australia, and Green Mountain FRA-Mainz in Germany. CAL-3 uses the exact
point published in its first-party datasheet. FIN04 uses the exact three-component
National Land Survey of Finland (NLS) property
MultiPolygon under CC BY 4.0 only as a shared-campus locator after a Kouvola permit binds atNorth,
Lossitie 26, and property 286-423-20-1. Hyperco uses a CC BY 4.0 Ryhti permit-building point
cross-identified to the Loviisa permit. FRA-Mainz uses the official Energieatlas project point as a
compact factual locator.

The ACT development-application plan identifies CDC's BE1 Data Centre at Block 24 Section 11 Beard.
The corresponding CC BY 4.0 ACT block polygon is published exactly, but its source lifecycle is
`RETIRED`; it is explicitly a historical project locator only, never a current parcel, present legal
boundary, campus boundary, project footprint, building footprint, or current-work extent. Every
v0.15 row has `official_boundary=false` and unknown numeric horizontal accuracy. Raw PDFs, HTML,
all-rights-reserved responses, and imagery are excluded.

Final-release gates remain explicit in `selection-report.json`. The final artifact still requires at
least 100 physical sites, 40 countries, complete imagery-review outcomes, a 20-site blind review,
and a clean-clone rebuild. The 50 non-U.S.-site minimum is now met. Until every gate passes,
`publishable_as_final` is false.

## Files

- `projects.csv`: one row per selected physical construction project
- `sites.csv`: one row per distinct physical site
- `sites.geojson`: reviewed locator or boundary geometry for each physical site
- `evidence.csv`: locally bound evidence and exact usage roles
- `selection-report.json`: cohort, provenance, rejection, and release-gate audit
- `schema.json`: field and v0.15 scope semantics
- `map.html`: deterministic offline map; no tile requests
- `manifest.json` and `manifest.sha256`: byte inventory and trust root
- `ATTRIBUTION.txt`: source-specific attribution and terms

## Legal notice

{legal_notice}

This preview is an evidence product, not legal, surveying, investment, engineering, or operational
advice. Unknowns remain unknown, locators are not silently promoted to footprints, and no capacity,
role, workload, energy, efficiency, or lifecycle claim is inferred from geometry. The preview is
not final while `publishable_as_final` is false.
""".encode("utf-8")


def _v15_attribution(
    evidence: Sequence[Mapping[str, Any]], legal_notice: str
) -> bytes:
    document = _v14_attribution(evidence, legal_notice).decode("utf-8")
    heading = "Data Center Atlas Verified Construction Core v0.14 preview"
    if document.count(heading) != 1:
        raise VerifiedConstructionCoreError(
            "v0.15 attribution heading seam differs"
        )
    document = document.replace(
        heading,
        "Data Center Atlas Verified Construction Core v0.15 preview",
        1,
    )
    marker = "Map-provider terms:\n"
    notice = (
        "v0.15 geometry notice: The exact FIN04 NLS MultiPolygon and Hyperco "
        "Ryhti building point contain Finnish public-sector data licensed under "
        "CC BY 4.0. The exact CDC Beard Block 24 Section 11 polygon contains "
        "Australian Capital Territory cadastral data licensed under CC BY "
        "4.0; the source "
        "marks it RETIRED and this preview uses it only as a historical BE1 "
        "locator, never a present legal boundary. CAL-3 and the Energieatlas "
        "FRA-Mainz points are compact factual extractions from their named "
        "official sources. "
        "All raw PDFs, HTML, all-rights-reserved responses, source imagery, map "
        "tiles, and third-party pixels are excluded.\n\n"
    )
    if document.count(marker) != 1:
        raise VerifiedConstructionCoreError(
            "v0.15 attribution notice seam differs"
        )
    document = document.replace(marker, f"{notice}{marker}", 1)
    return document.encode("utf-8")


def _v15_payloads() -> tuple[dict[str, bytes], dict[str, Any], list[dict[str, Any]]]:
    validate_frozen_v14(LEGACY_PREVIEW_V14_DIR)
    contracts = _v15_contracts()
    base_projects = _load_csv(
        LEGACY_PREVIEW_V14_DIR / "projects.csv", PROJECT_FIELDS
    )
    base_sites = _load_csv(
        LEGACY_PREVIEW_V14_DIR / "sites.csv", SITE_FIELDS
    )
    base_evidence = _load_csv(
        LEGACY_PREVIEW_V14_DIR / "evidence.csv", EVIDENCE_FIELDS
    )
    base_report = _load_json(
        LEGACY_PREVIEW_V14_DIR / "selection-report.json"
    )
    delta = _v15_build_delta_rows(contracts)
    if (
        {row["project_id"] for row in base_projects}
        & {row["project_id"] for row in delta["projects"]}
        or {row["site_id"] for row in base_sites}
        & {row["site_id"] for row in delta["sites"]}
        or {row["evidence_id"] for row in base_evidence}
        & {row["evidence_id"] for row in delta["evidence"]}
    ):
        raise VerifiedConstructionCoreError("v0.15 delta identity collision")
    projects = sorted(
        [*base_projects, *delta["projects"]],
        key=lambda row: row["project_stable_key"],
    )
    sites = sorted(
        [*base_sites, *delta["sites"]],
        key=lambda row: row["physical_site_stable_key"],
    )
    evidence = sorted(
        [*base_evidence, *delta["evidence"]],
        key=lambda row: row["evidence_id"],
    )
    if (
        len(projects) != 76
        or len(sites) != 73
        or len(evidence) != 183
        or len({row["project_id"] for row in projects}) != 76
        or len({row["site_id"] for row in sites}) != 73
        or len({row["evidence_id"] for row in evidence}) != 183
    ):
        raise VerifiedConstructionCoreError(
            "v0.15 cohort count or uniqueness differs"
        )
    project_by_id = {row["project_id"]: row for row in projects}
    site_by_id = {row["site_id"]: row for row in sites}
    evidence_by_id = {row["evidence_id"]: row for row in evidence}
    if (
        any(project_by_id[row["project_id"]] != row for row in base_projects)
        or any(site_by_id[row["site_id"]] != row for row in base_sites)
        or any(
            evidence_by_id[row["evidence_id"]] != row
            for row in base_evidence
        )
    ):
        raise VerifiedConstructionCoreError("v0.15 inherited row changed")
    gates = _final_release_gates(sites, projects)
    gates["clean_clone_rebuild"] = {
        "passed": True,
        "reason": (
            "all current-delta inputs and the frozen base artifact are tracked "
            "and hash-bound"
        ),
    }
    country_counts = dict(
        sorted(Counter(row["country"] for row in sites).items())
    )
    provenance_decisions = {
        key: list(base_report["provenance_decisions"][key])
        for key in (
            "workload_scope_bindings",
            "role_bindings",
            "excluded_source_roles",
            "context_evidence_bindings",
        )
    }
    provenance_decisions["context_evidence_bindings"].extend(
        _v15_context_evidence_bindings(contracts)
    )
    report = {
        **base_report,
        "format": "datacenter-atlas-verified-construction-core-selection-v15",
        "publishable_as_final": all(
            gate.get("passed", False) for gate in gates.values()
        ),
        "reviewed_at": CURRENT_V15_LIFECYCLE_REFERENCE_DATE.isoformat(),
        "cohort_lifecycle_reference_date": (
            CURRENT_V15_LIFECYCLE_REFERENCE_DATE.isoformat()
        ),
        "geometry_identity_reviewed_at": (
            CURRENT_V15_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
        ),
        "selected_project_count": 76,
        "selected_physical_site_count": 73,
        "official_boundary_project_count": 5,
        "reviewed_site_locator_project_count": 71,
        "non_selected_source_row_count": 455,
        "selection_first_failure_counts": V15_SELECTION_FIRST_FAILURE_COUNTS,
        "country_counts": country_counts,
        "final_release_gates": gates,
        "imagery_review_provenance": list(
            base_report["imagery_review_provenance"]
        ),
        "provenance_decisions": provenance_decisions,
        "reviewed_overlay_queue": [
            *base_report["reviewed_overlay_queue"],
            *_v15_review_queue(contracts),
        ],
        "five_global_site_geometry_batch": {
            "accepted_project_count": 5,
            "accepted_physical_site_count": 5,
            "batch_contract_path": V15_BATCH_CONTRACT.relative_to(
                ROOT
            ).as_posix(),
            "batch_contract_sha256": V15_BATCH_CONTRACT_SHA256,
            "capture_path": V15_GEOMETRY_CAPTURE.relative_to(ROOT).as_posix(),
            "capture_sha256": V15_GEOMETRY_CAPTURE_SHA256,
            "capture_source_document_count": 8,
            "geometry_authority_classes": {"official_source": 5},
            "geometry_methods": {
                "act_official_retired_block_polygon_as_be1_historical_project_locator": 1,
                "energieatlas_named_green_rocks_project_point": 1,
                "first_party_published_decimal_coordinate_to_point": 1,
                "nls_official_cadastral_multipolygon_as_fin04_shared_campus_locator": 1,
                "ryhti_official_permit_building_point_as_project_locator": 1,
            },
            "geometry_types": {
                "MultiPolygon": 1,
                "Point": 3,
                "Polygon": 1,
            },
            "geometry_use_scopes": {
                "campus_locator": 1,
                "project_locator": 4,
            },
            "official_boundary": False,
            "project_stable_keys": sorted(V15_PROJECT_KEYS),
            "rejected_claims": contracts["contract"]["rejected_claims"],
        },
    }
    delta_by_key = {
        row["project_stable_key"]: row for row in delta["projects"]
    }
    if (
        len(country_counts) != 27
        or country_counts.get("United States") != 22
        or sum(row["country_iso_a2"] != "US" for row in sites) != 51
        or sum(_is_official_boundary(row) for row in projects) != 5
        or sum(_is_reviewed_locator(row) for row in projects) != 71
        or sum(V15_SELECTION_FIRST_FAILURE_COUNTS.values()) != 531
        or gates["imagery_outcomes_complete"]
        != {"actual": 10, "required": 76, "passed": False}
        or gates["non_us_site_count"]
        != {"actual": 51, "required_minimum": 50, "passed": True}
        or len(provenance_decisions["context_evidence_bindings"]) != 34
        or len(provenance_decisions["excluded_source_roles"]) != 12
        or len(report["reviewed_overlay_queue"]) != 63
        or set(delta_by_key) != V15_PROJECT_KEYS
        or Counter(row["geometry_type"] for row in delta_by_key.values())
        != {"Point": 3, "MultiPolygon": 1, "Polygon": 1}
        or Counter(row["geometry_use_scope"] for row in delta_by_key.values())
        != {"project_locator": 4, "campus_locator": 1}
        or any(
            row["geometry_authority_class"] != "official_source"
            or row["horizontal_uncertainty_metres"]
            or row["workloads_json"] != "[]"
            or row["role_claims_json"] != "[]"
            or row["power_observations_json"] != "[]"
            or row["annual_energy_observations_json"] != "[]"
            or row["efficiency_observations_json"] != "[]"
            or row["operating_model"] != "unknown"
            or row["independent_imagery_verification"] != "false"
            or any(
                row[field]
                for field in (
                    "owner",
                    "operator",
                    "users",
                    "tenants",
                    "customers",
                )
            )
            for row in delta_by_key.values()
        )
    ):
        raise VerifiedConstructionCoreError("v0.15 expected accounting differs")
    geojson = _geojson(sites)
    geojson["name"] = (
        "Data Center Atlas Verified Construction Core v0.15 preview"
    )
    delta_features = {
        row["properties"]["physical_site_stable_key"]: row
        for row in geojson["features"]
        if row["properties"]["physical_site_stable_key"]
        in {
            acceptance["parent_campus_stable_key"]
            for acceptance in contracts["acceptances"]
        }
    }
    if (
        len(delta_features) != 5
        or Counter(
            feature["geometry"]["type"]
            for feature in delta_features.values()
        )
        != {"Point": 3, "MultiPolygon": 1, "Polygon": 1}
    ):
        raise VerifiedConstructionCoreError(
            "v0.15 geometry publication differs"
        )
    legal_notice = _v10_city_legal_notice_from_base()
    payloads = {
        "ATTRIBUTION.txt": _v15_attribution(evidence, legal_notice),
        "README.md": _v15_readme(report, legal_notice),
        "evidence.csv": _csv_bytes(evidence, EVIDENCE_FIELDS),
        "map.html": _v15_map_html(geojson, sites, evidence),
        "projects.csv": _csv_bytes(projects, PROJECT_FIELDS),
        "schema.json": _json_bytes(_v15_schema()),
        "selection-report.json": _json_bytes(report),
        "sites.csv": _csv_bytes(sites, SITE_FIELDS),
        "sites.geojson": _json_bytes(geojson),
    }
    portable = _v15_portable_source_inputs(contracts)
    return payloads, report, portable


def _v15_manifest(
    payloads: Mapping[str, bytes],
    report: Mapping[str, Any],
    portable: Sequence[Mapping[str, Any]],
) -> dict[str, Any]:
    return {
        "format": "datacenter-atlas-verified-construction-core-preview-v15",
        "preview_id": CURRENT_V15_PREVIEW_ID,
        "release_status": "preview",
        "publishable_as_final": report["publishable_as_final"],
        "base_preview_id": CURRENT_V14_PREVIEW_ID,
        "base_preview_manifest_sha256": LEGACY_PREVIEW_V14_MANIFEST_SHA256,
        "base_preview_commit": LEGACY_PREVIEW_V14_COMMIT,
        "source_release_id": SOURCE_RELEASE_ID,
        "source_release_manifest_path": (
            "releases/2026-07-22-open-seed-v97/manifest.json"
        ),
        "source_release_manifest_sha256": (
            "0a6f41f4239944df27f2ce70e81a089b91cec401f154bbae28412b27a4d00fdd"
        ),
        "definition_paths": {
            "five_global_site_geometry_batch": V15_BATCH_CONTRACT.relative_to(
                ROOT
            ).as_posix()
        },
        "five_global_site_geometry_batch_sha256": V15_BATCH_CONTRACT_SHA256,
        "geometry_capture": {
            "path": V15_GEOMETRY_CAPTURE.relative_to(ROOT).as_posix(),
            "bytes": V15_GEOMETRY_CAPTURE.stat().st_size,
            "sha256": V15_GEOMETRY_CAPTURE_SHA256,
            "evidence_ids": sorted(V15_CAPTURE_EVIDENCE_IDS),
        },
        "portable_source_inputs": list(portable),
        "reviewed_at": CURRENT_V15_LIFECYCLE_REFERENCE_DATE.isoformat(),
        "cohort_lifecycle_reference_date": (
            CURRENT_V15_LIFECYCLE_REFERENCE_DATE.isoformat()
        ),
        "geometry_identity_reviewed_at": (
            CURRENT_V15_GEOMETRY_IDENTITY_REVIEW_DATE.isoformat()
        ),
        "counts": {
            "physical_sites": 73,
            "projects": 76,
            "evidence": 183,
            "countries": 27,
            "non_us_sites": 51,
            "official_boundary_projects": 5,
            "reviewed_site_locator_projects": 71,
        },
        "files": {
            name: {"bytes": len(payload), "sha256": _sha256_bytes(payload)}
            for name, payload in sorted(payloads.items())
        },
    }


def build_preview(output_dir: Path = CURRENT_V15_PREVIEW_DIR) -> dict[str, Any]:
    """Build v0.15 from byte-frozen v0.14 and tracked hash-bound inputs."""
    output_dir = Path(output_dir)
    if output_dir.exists():
        raise VerifiedConstructionCoreError(f"refusing to overwrite {output_dir}")
    payloads, report, portable = _v15_payloads()
    manifest = _v15_manifest(payloads, report, portable)
    manifest_bytes = _json_bytes(manifest)
    complete = {
        **payloads,
        "manifest.json": manifest_bytes,
        "manifest.sha256": (
            f"{_sha256_bytes(manifest_bytes)}  manifest.json\n".encode()
        ),
    }
    output_dir.parent.mkdir(parents=True, exist_ok=True)
    stage = Path(
        tempfile.mkdtemp(prefix=f".{output_dir.name}.", dir=output_dir.parent)
    )
    try:
        for name, payload in complete.items():
            with (stage / name).open("xb") as handle:
                handle.write(payload)
        os.replace(stage, output_dir)
    except Exception:
        shutil.rmtree(stage, ignore_errors=True)
        raise
    validate_preview(output_dir)
    return manifest


def _validate_v15_preview(path: Path) -> dict[str, Any]:
    path = Path(path)
    if path.is_symlink() or not path.is_dir():
        raise VerifiedConstructionCoreError("v0.15 preview directory differs")
    manifest_path = path / "manifest.json"
    checksum_path = path / "manifest.sha256"
    if (
        manifest_path.is_symlink()
        or not manifest_path.is_file()
        or checksum_path.is_symlink()
        or not checksum_path.is_file()
    ):
        raise VerifiedConstructionCoreError(
            "v0.15 manifest trust root differs"
        )
    manifest_bytes = manifest_path.read_bytes()
    if checksum_path.read_bytes() != (
        f"{_sha256_bytes(manifest_bytes)}  manifest.json\n".encode()
    ):
        raise VerifiedConstructionCoreError("v0.15 manifest checksum differs")
    manifest = _load_json(manifest_path)
    files = manifest.get("files") if isinstance(manifest, dict) else None
    if (
        not isinstance(manifest, dict)
        or manifest.get("format")
        != "datacenter-atlas-verified-construction-core-preview-v15"
        or manifest.get("preview_id") != CURRENT_V15_PREVIEW_ID
        or manifest.get("release_status") != "preview"
        or type(manifest.get("publishable_as_final")) is not bool
        or manifest["publishable_as_final"] is not False
        or manifest.get("base_preview_manifest_sha256")
        != LEGACY_PREVIEW_V14_MANIFEST_SHA256
        or manifest.get("base_preview_commit") != LEGACY_PREVIEW_V14_COMMIT
        or not isinstance(files, dict)
    ):
        raise VerifiedConstructionCoreError("v0.15 manifest files differ")
    expected_inventory = set(files) | {"manifest.json", "manifest.sha256"}
    if {item.name for item in path.iterdir()} != expected_inventory:
        raise VerifiedConstructionCoreError("v0.15 preview inventory differs")
    for name, metadata in files.items():
        member = path / name
        if (
            Path(name).name != name
            or member.is_symlink()
            or not member.is_file()
            or type(metadata.get("bytes")) is not int
            or member.stat().st_size != metadata["bytes"]
            or _sha256_file(member) != metadata.get("sha256")
        ):
            raise VerifiedConstructionCoreError(
                f"v0.15 member differs: {name}"
            )
    payloads, report, portable = _v15_payloads()
    if manifest != _v15_manifest(payloads, report, portable):
        raise VerifiedConstructionCoreError("v0.15 manifest semantics differ")
    for name, expected in payloads.items():
        if (path / name).read_bytes() != expected:
            raise VerifiedConstructionCoreError(
                f"v0.15 generated member differs: {name}"
            )
    schema = _load_json(path / "schema.json")
    batch_schema = schema.get("v0_15_five_global_site_geometry_batch", {})
    if (
        batch_schema.get("accepted_project_count") != 5
        or batch_schema.get("official_boundary") is not False
        or batch_schema.get("geometry_types")
        != {"MultiPolygon": 1, "Point": 3, "Polygon": 1}
        or batch_schema.get("geometry_use_scopes")
        != {"campus_locator": 1, "project_locator": 4}
    ):
        raise VerifiedConstructionCoreError(
            "v0.15 geometry schema seam differs"
        )
    notices = (
        b"National Land Survey of Finland",
        b"Ryhti",
        b"RETIRED",
        b"Energieatlas",
    )
    for name in ("ATTRIBUTION.txt", "README.md", "map.html"):
        content = (path / name).read_bytes()
        if any(notice not in content for notice in notices):
            raise VerifiedConstructionCoreError(
                f"v0.15 attribution notice missing from {name}"
            )
    projects = {
        row["project_stable_key"]: row
        for row in _load_csv(path / "projects.csv", PROJECT_FIELDS)
        if row["project_stable_key"] in V15_PROJECT_KEYS
    }
    if set(projects) != V15_PROJECT_KEYS:
        raise VerifiedConstructionCoreError("v0.15 project inventory differs")
    contracts = _v15_contracts()
    evidence = {
        row["evidence_id"]: row
        for row in _load_csv(path / "evidence.csv", EVIDENCE_FIELDS)
    }
    for acceptance in contracts["acceptances"]:
        project_key = acceptance["project_stable_key"]
        project = projects[project_key]
        result = contracts["results_by_id"][acceptance["locator_id"]]
        geometry = contracts["geometries_by_id"][acceptance["locator_id"]]
        semantics = result["semantics"]
        geometry_document = contracts["documents_by_id"][
            result["geometry_source_document_id"]
        ]
        geometry_evidence_id = _v15_capture_evidence(geometry_document)[
            "evidence_id"
        ]
        geometry_evidence = evidence.get(geometry_evidence_id)
        if (
            _parse_json_field(
                project["geometry_json"], "v0.15 project geometry"
            )
            != geometry
            or project["geometry_type"] != geometry["type"]
            or project["geometry_source_entity_kind"]
            != semantics["geometry_source_entity_kind"]
            or project["geometry_derivation"]
            != semantics["geometry_derivation"]
            or project["geometry_method"] != semantics["geometry_method"]
            or project["geometry_scope_class"]
            != semantics["geometry_scope_class"]
            or project["geometry_authority_class"] != "official_source"
            or project["geometry_use_scope"]
            != semantics["geometry_use_scope"]
            or project["geometry_precision_scope"]
            != semantics["precision_scope"]
            or project["horizontal_uncertainty_metres"]
            or project["horizontal_uncertainty_unknown_reason"]
            != semantics["horizontal_uncertainty_unknown_reason"]
            or project["independent_imagery_verification"] != "false"
            or project["verification_posture"]
            != "recent_authoritative_physical_observation_plus_reviewed_site_locator"
            or project["last_observed_physical_status"]
            != acceptance["status"]["value"]
            or project["status_as_of"] != acceptance["status"]["as_of_date"]
            or project["status_evidence_id"]
            != acceptance["status"]["evidence_id"]
            or geometry_evidence is None
            or "geometry"
            not in _parse_json_field(
                geometry_evidence["roles_json"], "v0.15 geometry roles"
            )
            or project["project_id"]
            not in _parse_json_field(
                geometry_evidence["project_ids_json"],
                "v0.15 geometry projects",
            )
            or any(
                project[field] != "[]"
                for field in (
                    "workloads_json",
                    "role_claims_json",
                    "power_observations_json",
                    "annual_energy_observations_json",
                    "efficiency_observations_json",
                )
            )
            or any(
                project[field]
                for field in (
                    "owner",
                    "operator",
                    "users",
                    "tenants",
                    "customers",
                )
            )
        ):
            raise VerifiedConstructionCoreError(
                f"v0.15 project semantic scope differs: {project_key}"
            )
    cdc = projects["curated:cdc-beard-campus:be1-current-build"]
    fin = projects["curated:atnorth-fin04-kouvola-campus:phase-1"]
    if (
        cdc["geometry_scope_class"]
        != "official_historical_cadastral_parcel_as_project_locator"
        or "RETIRED" not in cdc["geometry_precision_scope"]
        or fin["geometry_type"] != "MultiPolygon"
        or "three disconnected" not in fin["geometry_precision_scope"]
    ):
        raise VerifiedConstructionCoreError(
            "v0.15 historical or multipolygon disclosure differs"
        )
    for filename, key, fields in (
        ("projects.csv", "project_id", PROJECT_FIELDS),
        ("sites.csv", "site_id", SITE_FIELDS),
        ("evidence.csv", "evidence_id", EVIDENCE_FIELDS),
    ):
        inherited = {
            row[key]: row
            for row in _load_csv(LEGACY_PREVIEW_V14_DIR / filename, fields)
        }
        current = {row[key]: row for row in _load_csv(path / filename, fields)}
        if any(current[row_id] != row for row_id, row in inherited.items()):
            raise VerifiedConstructionCoreError(
                f"v0.15 inherited {filename} row changed"
            )
    return manifest


def validate_preview(path: Path = CURRENT_V15_PREVIEW_DIR) -> dict[str, Any]:
    """Validate current v0.15 or dispatch v0.1-v0.14 via frozen seams."""
    path = Path(path)
    manifest_path = path / "manifest.json"
    if (
        path.is_symlink()
        or not path.is_dir()
        or manifest_path.is_symlink()
        or not manifest_path.is_file()
    ):
        raise VerifiedConstructionCoreError("preview manifest trust root differs")
    manifest = _load_json(manifest_path)
    preview_id = manifest.get("preview_id") if isinstance(manifest, dict) else None
    if preview_id == CURRENT_V15_PREVIEW_ID:
        return _validate_v15_preview(path)
    return _validate_v14_preview_dispatch(path)


__all__ = [
    *__all__,
    "CURRENT_V15_PREVIEW_DIR",
    "CURRENT_V15_PREVIEW_ID",
    "CURRENT_V15_GEOMETRY_IDENTITY_REVIEW_DATE",
    "CURRENT_V15_LIFECYCLE_REFERENCE_DATE",
    "CURRENT_V15_REVIEW_DATE",
    "LEGACY_PREVIEW_V14_COMMIT",
    "LEGACY_PREVIEW_V14_DIR",
    "LEGACY_PREVIEW_V14_MANIFEST_SHA256",
    "V15_BATCH_CONTRACT",
    "V15_BATCH_CONTRACT_SHA256",
    "V15_CAPTURE_EVIDENCE_IDS",
    "V15_GEOMETRY_CAPTURE",
    "V15_GEOMETRY_CAPTURE_SHA256",
    "V15_PROJECT_KEYS",
    "build_v14_preview",
    "validate_frozen_v14",
]
